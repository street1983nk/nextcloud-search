---
phase: 01-integrationsbeweis
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/src/findling/__init__.py
  - backend/tests/test_readonly_gate.py
  - .github/workflows/python.yml
  - .github/workflows/integration.yml
autonomous: true
requirements: [IDX-07, COMP-01]

must_haves:
  truths:
    - "Ein Verstoss gegen die Nur-Lesen-Invariante bricht die CI, bevor der erste Lesepfad existiert"
    - "Jeder Python-Commit laeuft durch ruff, ruff format, pyright, vulture und pytest"
    - "Die End-to-End-Kanarienprobe existiert und ist nachweislich rot, weil noch kein Provider registriert ist"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "uv-Projekt mit exakten Pins und dem vollstaendigen ruff-Regelsatz"
      contains: "ASYNC"
    - path: "backend/tests/test_readonly_gate.py"
      provides: "Gate A, statischer AST-Test der drei Nur-Lesen-Invarianten"
      min_lines: 60
    - path: ".github/workflows/python.yml"
      provides: "ruff, ruff format, pyright, vulture, pytest"
      contains: "vulture"
    - path: ".github/workflows/integration.yml"
      provides: "Nextcloud-Testinstanz plus Kanarienprobe auf die Providerliste"
      contains: "maintenance:install"
  key_links:
    - from: ".github/workflows/integration.yml"
      to: "/ocs/v2.php/search/providers"
      via: "curl plus jq-Assertion"
      pattern: "search/providers"
    - from: "backend/tests/test_readonly_gate.py"
      to: "backend/src/findling"
      via: "ast.parse ueber alle Module"
      pattern: "ast\\.(parse|walk)"
---

<objective>
Die Qualitäts- und Sicherheitsgates entstehen vor dem ersten Fachcode, und die End-to-End-Probe wird als rote Messlatte aufgehängt.

Purpose: CONTEXT.md verlangt, dass das Nur-Lesen-Gate existiert, BEVOR der erste Lesepfad entsteht. Die rote Kanarienprobe legt gleichzeitig fest, woran der Erfolg dieser Phase gemessen wird, statt ihn hinterher zu behaupten.
Output: Lauffähiges uv-Projekt mit allen fünf Gates, Gate A als AST-Test mit eigenem Negativbeweis, und ein Integrations-Workflow, der eine echte Nextcloud hochzieht und den noch fehlenden Provider anprangert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-integrationsbeweis/01-CONTEXT.md
@.planning/phases/01-integrationsbeweis/01-RESEARCH.md
@CLAUDE.md
@docs/store-identity.md

<interfaces>
Exakte Pins (RESEARCH.md "Standard Stack", alle am 15.08.2026 gegen die PyPI-JSON-API geprueft,
Legitimitaetsaudit 9 von 9 [OK]):

  nc-py-api[app] == 0.30.3      fastapi == 0.141.1     starlette >= 1.0.1 (CVE-2026-48710)
  httpx == 0.28.1
  dev: ruff == 0.16.3   pyright == 1.1.411   vulture == 2.16   pytest == 9.1.1   pytest-asyncio

ruff-Regelsatz (CLAUDE.md, global verbindlich):
  select = ["E","F","I","UP","B","ASYNC","S","SIM","C4","RUF","PT","RET","A","ISC"]
  per-file-ignores: "tests/*" = ["S101"]

Gate A, die drei Invarianten (RESEARCH.md "Nur-Lesen-Invariante: der konkrete Gate-Bauplan"):
  1. Nur findling/nc/client.py darf nc_py_api importieren.
  2. In findling/nc/client.py erscheint keiner der Bezeichner:
     set_user, upload, upload_stream, delete, move, copy, mkdir, makedirs, trash
  3. Kein Aufruf von nc.ocs bzw. des rohen Adapters mit PUT, POST, PATCH oder DELETE
     auf einen Pfad ausserhalb einer Allowlist. Die Allowlist ist in Phase 1 leer.

CI-Rezeptur fuer die Testinstanz (RESEARCH.md "Frage 5", verifiziert aus
nextcloud/context_chat_backend/.github/workflows/integration-test.yml):
  actions/checkout von nextcloud/server @ stable34, submodules recursive
  shivammathur/setup-php@v2 mit mbstring, iconv, fileinfo, intl, sqlite, pdo_sqlite, gd, zip
  actions/checkout von nextcloud/app_api @ stable34 nach apps/app_api
  mkdir data && ./occ maintenance:install --database=sqlite --admin-user admin --admin-pass password
  composer run serve &      # PHP-Builtin-Server auf :8080
  ./occ app:enable -vvv -f app_api
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv-Projekt mit exakten Pins und allen fuenf Qualitaetsgates</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/src/findling/__init__.py, .github/workflows/python.yml</files>
  <read_first>
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Standard Stack" und "Package Legitimacy Audit"
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Frage 7", Unterabschnitt "Python-Gates"
    - CLAUDE.md, Abschnitt "Python-Qualitaetsgates"
  </read_first>
  <action>
    In backend/ ein uv-Paketprojekt anlegen (`uv init --package .` bzw. pyproject von Hand) mit
    name findling, requires-python ">=3.13,<3.14", license AGPL-3.0-or-later, packages unter src/.

    Runtime-Dependencies exakt pinnen: nc-py-api[app]==0.30.3, fastapi==0.141.1, httpx==0.28.1.
    starlette nicht direkt pinnen, aber ueber ein constraint >=1.0.1 sicherstellen, dass der
    CVE-2026-48710-Boden nicht unterlaufen wird. Dev-Dependencies exakt pinnen: ruff==0.16.3,
    pyright==1.1.411, vulture==2.16, pytest==9.1.1, pytest-asyncio.

    [tool.ruff.lint] mit dem select-Satz aus dem interfaces-Block, per-file-ignores fuer tests
    mit S101 und Begruendungskommentar. [tool.ruff] line-length 120, target-version py313.
    [tool.pyright] mit typeCheckingMode "basic", pythonVersion "3.13", include ["src", "tests"].
    [tool.vulture] min_confidence 80, paths src und tests.
    [tool.pytest.ini_options] mit `filterwarnings = ["error::DeprecationWarning"]`, weil sonst der
    synchrone enabled_handler aus Pitfall 7 unbemerkt durchrutscht, und asyncio_mode "auto".

    backend/src/findling/__init__.py mit Docstring und `__version__ = "0.1.0"` (dieselbe Version
    fuehrt spaeter die PHP-App, gekoppelte Versionierung ist Store-Vorgabe).

    .github/workflows/python.yml: ein Job, `on: push` und `pull_request` mit
    `paths: ['backend/**', '.github/workflows/python.yml']`, working-directory backend,
    astral-sh/setup-uv mit gepinntem Commit-SHA, dann die fuenf Schritte
    `uv sync --frozen`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`,
    `uv run vulture src tests --min-confidence 80`, `uv run pytest -q`.
    Alle Actions mit Commit-SHA pinnen, nicht mit Tag (Supply-Chain-Vorgabe aus dem Security-Domain-Abschnitt).
  </action>
  <acceptance_criteria>
    - `cd backend && uv sync --frozen` laeuft ohne Fehler durch
    - `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` endet mit Exit 0
    - `grep -v '^#' backend/pyproject.toml | grep -c 'ASYNC'` ist groesser als 0
    - `grep -v '^#' backend/pyproject.toml | grep -c 'error::DeprecationWarning'` ist groesser als 0
    - `grep -Ec 'uses: .*@[0-9a-f]{40}' .github/workflows/python.yml` entspricht der Zahl der `uses:`-Zeilen
    - `grep -v '^#' backend/pyproject.toml | grep -c 'nc-py-api\[app\]==0.30.3'` liefert 1
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv sync --frozen && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <done>Alle fuenf Gates laufen lokal gruen und sind als Workflow gepinnt hinterlegt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate A, statischer Nur-Lesen-Test mit eigenem Negativbeweis</name>
  <files>backend/tests/test_readonly_gate.py</files>
  <behavior>
    - Ein synthetischer Modulquelltext mit `import nc_py_api` unter einem anderen Pfad als nc/client.py wird als Verstoss gemeldet
    - Derselbe Import in nc/client.py wird nicht gemeldet
    - Ein Quelltext mit `nc.set_user("bob")` wird als Verstoss gemeldet
    - Ein Quelltext mit `await nc._session.ocs("PUT", "/foo")` wird als Verstoss gemeldet, `"GET"` dagegen nicht
    - Der Scan ueber das echte Paket backend/src/findling meldet null Verstoesse
  </behavior>
  <read_first>
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Nur-Lesen-Invariante: der konkrete Gate-Bauplan (IDX-07)"
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Security Domain", Tabelle "Known Threat Patterns"
    - backend/pyproject.toml (Ergebnis von Task 1)
  </read_first>
  <action>
    backend/tests/test_readonly_gate.py schreiben. Kern ist eine reine Funktion
    `scan_source(relative_path: str, source: str) -> list[str]`, die den Quelltext mit `ast.parse`
    liest und die drei Invarianten aus dem interfaces-Block prueft. Danach zwei Testgruppen:

    Gruppe 1, Selbsttest des Gates mit synthetischen Quelltexten (das ist der Grund, warum das Gate
    heute schon Wert hat, obwohl das Paket noch leer ist): jeder der vier Faelle aus <behavior> wird
    einzeln geprueft. Ohne diese Gruppe waere der Test auf einem leeren Paket vakuum-gruen und wuerde
    einen Bruch spaeter nicht zuverlaessig melden.

    Gruppe 2, Anwendung auf das echte Paket: alle .py-Dateien unter backend/src/findling rekursiv
    einlesen, `scan_source` anwenden, und `assert violations == []` mit einer Fehlermeldung, die
    Datei, Zeile und verletzte Invariante nennt.

    Verbotene Bezeichner als Modulkonstante fuehren, damit spaetere Phasen sie erweitern koennen.
    Allowlist fuer schreibende OCS-Aufrufe als leere Frozenset-Konstante mit Kommentar anlegen.
    Hinweis fuer den Executor: die Pruefung auf verbotene Bezeichner muss Attributzugriffe erfassen
    (`ast.Attribute`), nicht nur Namen, sonst entgeht ihr `nc.files.upload`.
  </action>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_readonly_gate.py -q` endet mit Exit 0
    - Der Selbsttest deckt alle vier Negativfaelle ab: `grep -c 'def test_' backend/tests/test_readonly_gate.py` ist mindestens 5
    - Manipulationsprobe: Wird eine Datei `backend/src/findling/_tmp_probe.py` mit dem Inhalt `import nc_py_api` angelegt, endet `uv run pytest tests/test_readonly_gate.py -q` mit Exit ungleich 0; nach dem Loeschen der Probe wieder Exit 0
    - `uv run ruff check tests` und `uv run pyright` bleiben gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_readonly_gate.py -q && printf 'import nc_py_api\n' > src/findling/_tmp_probe.py && ! uv run pytest tests/test_readonly_gate.py -q; rm -f src/findling/_tmp_probe.py</automated>
  </verify>
  <done>Gate A ist gruen, faengt nachweislich alle vier Verstossklassen und bricht bei einer echten Manipulation im Paket.</done>
</task>

<task type="auto">
  <name>Task 3: Rote Kanarienprobe, echte Nextcloud in der CI</name>
  <files>.github/workflows/integration.yml</files>
  <read_first>
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Frage 5: CI-Setup" vollstaendig
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Pitfall 3: Der Provider erscheint gar nicht in der Suchleiste"
    - docs/store-identity.md
  </read_first>
  <action>
    .github/workflows/integration.yml nach der verifizierten Rezeptur im interfaces-Block anlegen,
    Job-Name `walking-skeleton`, Trigger push und pull_request auf `php/**`, `backend/**` und die
    Workflow-Datei selbst, Server-Version stable34 als einzige Matrix-Zeile (die volle Matrix
    stable32/33/34 kommt als schedule-Lauf in Phase 5), Datenbank sqlite.

    Reihenfolge der Schritte: nextcloud/server auschecken, setup-php 8.2 mit den genannten
    Erweiterungen, app_api nach apps/app_api auschecken, unser Repo nach einem Unterpfad auschecken
    und `php/` per Verschieben oder Symlink nach `apps/findling` bringen (der Verzeichnisname MUSS
    exakt die App-ID sein, sonst findet der Autoloader die Klassen nicht, Pitfall 3 Ursache drei),
    `mkdir data && ./occ maintenance:install --database=sqlite --admin-user admin --admin-pass password`,
    `composer run serve &` plus Warteschleife auf http://localhost:8080/status.php,
    `./occ app:enable -vvv -f app_api`, dann `./occ app:enable -vvv -f findling`
    (dieser Schritt ist heute rot, weil php/ noch leer ist, und das ist beabsichtigt).

    Danach der eigentliche Kanarienvogel als eigener Schritt mit `id: canary`:
    `curl -sf -u admin:password -H 'OCS-APIRequest: true' -H 'Accept: application/json'
    'http://localhost:8080/ocs/v2.php/search/providers' | tee providers.json` gefolgt von
    `jq -e '[.ocs.data[] | select(.id == "findling")] | length == 1' providers.json`.
    Das ist bewusst die Diagnosefrage aus Pitfall 3: steht findling in der Providerliste, liegt ein
    spaeterer Fehler im Container, steht es nicht drin, liegt er in der PHP-App.

    Am Ende einen `if: failure()`-Schritt, der `data/nextcloud.log` und die letzten 200 Zeilen des
    Serverlogs ausgibt. Alle Actions mit Commit-SHA pinnen. Workflow committen und pushen, damit
    der rote Lauf dokumentiert vorliegt.
  </action>
  <acceptance_criteria>
    - `gh workflow list` fuehrt integration.yml
    - Der Lauf auf dem Push ist rot: `gh run list --workflow=integration.yml --limit 1 --json conclusion -q '.[0].conclusion'` liefert `failure`
    - Der Lauf scheitert nachweislich erst an `app:enable findling` oder an der canary-Assertion, nicht am Nextcloud-Setup: `gh run view --log-failed` zeigt keinen Fehler in `maintenance:install` oder `composer run serve`
    - `grep -c 'search/providers' .github/workflows/integration.yml` ist groesser als 0
    - `grep -Ec 'uses: .*@[0-9a-f]{40}' .github/workflows/integration.yml` entspricht der Zahl der `uses:`-Zeilen
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=integration.yml --limit 1 --json conclusion -q '.[0].conclusion' | grep -qx failure && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>Die Kanarienprobe steht, zieht eine echte Nextcloud hoch und ist aus genau dem erwarteten Grund rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI und GitHub Actions zu unserer CI | Fremder Code wird in der Build-Umgebung ausgefuehrt |
| ExApp-Code zu Nutzerdateien | Jeder kuenftige Schreibpfad waere ein Datenverlustrisiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Nutzerdateien | mitigate | Gate A als AST-Test mit Selbstbeweis, aktiv bevor der erste Lesepfad existiert; Manipulationsprobe als Acceptance-Kriterium |
| T-01-05 | Elevation of Privilege | nc_py_api-Nutzung | mitigate | `set_user` steht auf der Verbotsliste von Gate A, damit spaeterer Impersonation-Code die CI bricht |
| T-01-SC | Tampering | uv-Installs und GitHub Actions | mitigate | Alle neun Pakete sind im Legitimitaetsaudit der RESEARCH.md mit [OK] bewertet, keine [ASSUMED] oder [SUS]; exakte Versionspins plus uv.lock; alle Actions mit Commit-SHA statt Tag |
| T-01-06 | Information Disclosure | CI-Logs | accept | Die Testinstanz nutzt das Passwort `password` und ausschliesslich synthetische Daten; kein Produktionsgeheimnis beruehrt den Workflow |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` gruen, alle Linter gruen
- integration.yml erzeugt einen roten Lauf, dessen Fehlerstelle die fehlende PHP-App ist
- Kein Actions-`uses:` ohne Commit-SHA
</verification>

<success_criteria>
- Gate A existiert, faengt Verstoesse nachweislich und laeuft in der CI
- Alle fuenf Python-Gates laufen lokal und in der CI
- Die End-to-End-Messlatte haengt und ist rot
</success_criteria>

<output>
Create `.planning/phases/01-integrationsbeweis/01-02-SUMMARY.md` when done
</output>
