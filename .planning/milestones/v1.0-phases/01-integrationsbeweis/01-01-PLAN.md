---
phase: 01-integrationsbeweis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
  - .gitignore
  - .github/workflows/.gitkeep
  - php/.gitkeep
  - backend/.gitkeep
  - testdata/corpus/.gitkeep
  - docs/store-identity.md
autonomous: false
requirements: [PKG-02]

must_haves:
  truths:
    - "Die beiden App-IDs sind vom Owner bestaetigt und schriftlich fixiert, bevor der erste Commit sie in Code oder Verzeichnisnamen schreibt"
    - "Das oeffentliche GitHub-Repo existiert unter street1983nk/nextcloud-search mit AGPL-3.0"
    - "Jeder Commit dieses Repos traegt die Identitaet street1983nk <k.cherif@outlook.de> und keinen Co-Authored-By-Trailer"
    - "Private Schluessel koennen strukturell nicht committet werden"
  artifacts:
    - path: "LICENSE"
      provides: "AGPL-3.0-Volltext"
      contains: "GNU AFFERO GENERAL PUBLIC LICENSE"
    - path: ".gitignore"
      provides: "Schluessel- und Secret-Ausschluss"
      contains: "*.key"
    - path: "docs/store-identity.md"
      provides: "Verbindliche Fixierung beider App-IDs mit Datum und Begruendung"
      contains: "findling_backend"
    - path: "README.md"
      provides: "Projektbeschreibung Englisch, Zwei-App-Modell, Lizenz"
      min_lines: 30
  key_links:
    - from: "lokales Repo"
      to: "github.com/street1983nk/nextcloud-search"
      via: "git remote origin"
      pattern: "street1983nk/nextcloud-search"
---

<objective>
Die Store-Identität wird unwiderruflich festgelegt und das öffentliche Repository entsteht, bevor irgendein Code eine App-ID in eine Datei oder einen Verzeichnisnamen schreibt.

Purpose: Das Zertifikat ist über den CN an die App-ID gebunden. Eine spätere Umbenennung entwertet es und kostet eine neue CSR-Runde von bis zu elf Tagen (RESEARCH Pitfall 8). Deshalb steht der Freeze vor allem anderen.
Output: Öffentliches AGPL-3.0-Repo mit Verzeichnisgerüst, festgeschriebener Identität in docs/store-identity.md und einer Commit-Identität, die die Kontotrennung erzwingt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-integrationsbeweis/01-CONTEXT.md
@.planning/phases/01-integrationsbeweis/01-RESEARCH.md
@CLAUDE.md

<interfaces>
Verbindliche Namen fuer das gesamte Projekt (aus CONTEXT.md "Identitaet und Store",
bestaetigt durch RESEARCH.md "Frage 6" und die Live-Pruefung beider Store-Feeds am 15.08.2026):

  PHP-Companion (Store-Bereich "Apps")            id = findling
  Python-ExApp  (Store-Bereich "External Apps")   id = findling_backend
  PHP-Namespace                                    OCA\Findling
  Python-Paket                                     findling
  Container-Image                                  ghcr.io/street1983nk/findling_backend
  GitHub-Repo                                      street1983nk/nextcloud-search
  Commit-Identitaet                                street1983nk <k.cherif@outlook.de>

Verzeichnislayout (Mono-Repo, RESEARCH "Recommended Project Structure"):
  php/       Companion-App, wird in CI nach apps/findling gemappt
  backend/   ExApp, Python-Paket unter backend/src/findling/
  testdata/corpus/   Referenzkorpus fuer das Pruefsummen-Gate
  docs/      Betriebs- und Prozessdokumentation
  .github/workflows/   python.yml, php.yml, integration.yml, docker.yml
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: App-IDs einfrieren und Verfuegbarkeit unmittelbar vor dem Freeze belegen</name>
  <read_first>
    - .planning/phases/01-integrationsbeweis/01-CONTEXT.md, Abschnitt "Identitaet und Store"
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Frage 6: CSR-Prozess fuer zwei Apps" und "Open Questions" Nr. 1
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Pitfall 8: Spaete Umbenennung entwertet das Zertifikat"
  </read_first>
  <action>
    Vor dem Anhalten: beide Store-Feeds live abfragen und die Freiheit der IDs `findling` und
    `findling_backend` belegen, docs/store-identity.md mit der Fixierung anlegen (IDs, Store-Bereiche,
    Namespace OCA\Findling, Python-Paket findling, Image ghcr.io/street1983nk/findling_backend,
    Freeze-Datum, Begruendung, Verweis auf Pitfall 8). Danach dem Owner die Bestaetigungsfrage stellen
    und warten. Keine Datei ausserhalb docs/store-identity.md anfassen, solange der Freeze nicht bestaetigt ist.
  </action>
  <what-built>
    Noch kein Code. Vor dem Checkpoint prueft der Executor automatisiert, dass beide IDs in beiden
    Store-Feeds weiterhin frei sind, und legt docs/store-identity.md mit der Fixierung an
    (IDs, Store-Bereiche, Namespace, Image-Name, Datum, Begruendung, Verweis auf Pitfall 8).
    RESEARCH.md Open Question 1 beschrieb einen Konflikt zwischen einer aelteren CONTEXT-Formulierung
    und der Store-Konvention. CONTEXT.md loest ihn bereits auf: Companion = findling,
    ExApp = findling_backend. Dieser Checkpoint holt dazu die letzte, danach irreversible Bestaetigung ein.
  </what-built>
  <how-to-verify>
    1. docs/store-identity.md lesen: stehen dort exakt `findling` (Apps) und `findling_backend` (External Apps)?
    2. Ausgabe des Verfuegbarkeitschecks im Terminal ansehen: beide IDs muessen 0 Treffer in
       apps.json und appapi_apps.json haben.
    3. Bewusst machen: nach dem CSR-Merge ist eine Umbenennung nur ueber eine neue CSR-Runde
       (gemessener Median 3 bis 4 Tage, Ausreisser 11 Tage) moeglich.
  </how-to-verify>
  <acceptance_criteria>
    - `curl -s https://apps.nextcloud.com/api/v1/platform/34.0.0/apps.json | grep -c '"id": *"findling"'` liefert 0
    - `curl -s https://apps.nextcloud.com/api/v1/appapi_apps.json | grep -c '"id": *"findling_backend"'` liefert 0
    - docs/store-identity.md existiert und enthaelt beide IDs, beide Store-Bereiche und das Freeze-Datum
    - Owner hat den Freeze ausdruecklich bestaetigt (Antwort im Transcript)
  </acceptance_criteria>
  <verify>
    <automated>curl -sf https://apps.nextcloud.com/api/v1/appapi_apps.json | grep -c '"findling_backend"' | grep -qx 0 && echo FREE</automated>
  </verify>
  <resume-signal>Type "approved" (IDs eingefroren) oder nenne abweichende IDs</resume-signal>
  <done>Beide IDs sind bestaetigt, in docs/store-identity.md fixiert und als frei belegt.</done>
</task>

<task type="auto">
  <name>Task 2: Oeffentliches Repo, Lizenz, Verzeichnisgeruest und Commit-Identitaet</name>
  <files>LICENSE, README.md, .gitignore, .github/workflows/.gitkeep, php/.gitkeep, backend/.gitkeep, testdata/corpus/.gitkeep</files>
  <read_first>
    - docs/store-identity.md (Ergebnis von Task 1)
    - .planning/phases/01-integrationsbeweis/01-RESEARCH.md, "Frage 7", Unterabschnitt "Repo-Grundeinrichtung"
    - CLAUDE.md, Abschnitte Constraints und Sprache
  </read_first>
  <action>
    Repo-Identitaet lokal setzen, nicht global: `git config user.name street1983nk` und
    `git config user.email k.cherif@outlook.de` im Repo-Verzeichnis. Keine Co-Authored-By-Trailer,
    Conventional Commits.

    LICENSE mit dem vollstaendigen AGPL-3.0-Text anlegen (Quelle: https://www.gnu.org/licenses/agpl-3.0.txt).

    README.md auf Englisch: Zweck (zero-config full text search for Nextcloud), das Zwei-App-Modell
    (findling = Apps entry, findling_backend = External Apps entry, both must be installed and share
    the same major.minor version), Zielhardware (4 bis 8 GB RAM, ARM-tauglich), Nextcloud-Fenster
    min-version 32 / max-version 35, Lizenz AGPL-3.0-or-later, Statuszeile "Phase 1: walking skeleton,
    not usable yet". Ausdrueckliche Privacy-Aussage aufnehmen: no file content leaves the server,
    no telemetry, user files are never modified. Keine Em-Dashes, keine Emojis, ASCII in Bezeichnern.

    .gitignore anlegen mit mindestens: `*.key`, `*.pem`, `.env`, `__pycache__/`, `.venv/`, `.pytest_cache/`,
    `node_modules/`, `data/`, `*.tar.gz`. Der `*.key`-Eintrag ist eine Sicherheitsmassnahme, nicht Kosmetik:
    das Zertifikats-Repo enthaelt reale Vorgaenge mit dem Titel "private key exposed".

    Verzeichnisgeruest per .gitkeep anlegen: php/, backend/, testdata/corpus/, .github/workflows/.

    Repo auf GitHub anlegen und pushen:
    `gh repo create street1983nk/nextcloud-search --public --source . --remote origin --push`
    (Repo darf ausschliesslich den Owner als Contributor haben, keine Collaborator einladen).
    Erster Commit als `chore: initialise public repository with AGPL-3.0 and directory layout`.
  </action>
  <acceptance_criteria>
    - `gh repo view street1983nk/nextcloud-search --json visibility,licenseInfo` zeigt "PUBLIC" und AGPL-3.0
    - `git log -1 --format='%an <%ae>'` liefert exakt `street1983nk <k.cherif@outlook.de>`
    - `git log --format=%B | grep -ci 'co-authored-by'` liefert 0
    - `grep -c '^\*\.key$' .gitignore` liefert 1
    - `git ls-files | grep -Ec '\.key$|\.pem$'` liefert 0
    - `git ls-remote origin HEAD` liefert einen Commit-Hash (push erfolgreich)
    - `grep -v '^#' README.md | grep -c 'findling_backend'` ist groesser als 0
  </acceptance_criteria>
  <verify>
    <automated>gh repo view street1983nk/nextcloud-search --json visibility -q .visibility | grep -qx PUBLIC && git log -1 --format='%ae' | grep -qx 'k.cherif@outlook.de' && echo OK</automated>
  </verify>
  <done>Das oeffentliche AGPL-Repo existiert, ist gepusht, die Commit-Identitaet ist repo-lokal erzwungen und Schluesseldateien sind strukturell ausgeschlossen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lokales Arbeitsverzeichnis zu oeffentlichem GitHub-Repo | Alles, was hier committet wird, ist danach dauerhaft oeffentlich |
| privates Konto street1983nk zu Arbeitgeberkonto Akara | Konto- und Identitaetstrennung ist eine ausdrueckliche Projektvorgabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Commit-Identitaet | mitigate | `git config user.name/user.email` repo-lokal statt global; Acceptance-Kriterium prueft `git log -1 --format='%an <%ae>'`; keine Co-Authored-By-Trailer |
| T-01-02 | Information Disclosure | oeffentliches Repo | mitigate | `.gitignore` mit `*.key`, `*.pem`, `.env` vor dem ersten Push; `git ls-files`-Gate auf Schluesselendungen |
| T-01-03 | Tampering | App-ID-Identitaet | mitigate | Freeze als blockierender Checkpoint vor jedem ID-schreibenden Commit; Live-Verfuegbarkeitspruefung beider Store-Feeds unmittelbar davor |
</threat_model>

<verification>
- `gh repo view street1983nk/nextcloud-search` zeigt ein oeffentliches Repo mit AGPL-3.0
- `git log --format='%an <%ae>' | sort -u` enthaelt genau eine Identitaet
- docs/store-identity.md dokumentiert beide IDs mit Datum und Begruendung
</verification>

<success_criteria>
- Beide App-IDs sind vom Owner bestaetigt, als frei belegt und schriftlich fixiert
- Das oeffentliche Repo existiert mit AGPL-3.0, Verzeichnisgeruest und korrekter Commit-Identitaet
- Kein Schluesselmaterial kann versehentlich committet werden
</success_criteria>

<output>
Create `.planning/phases/01-integrationsbeweis/01-01-SUMMARY.md` when done
</output>
