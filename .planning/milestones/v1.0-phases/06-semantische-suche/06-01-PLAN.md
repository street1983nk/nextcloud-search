---
phase: 06-semantische-suche
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/Dockerfile
  - scripts/dev/quantize_model.py
  - THIRD-PARTY.md
  - backend/tests/test_vec_extension_probe.py
  - .github/workflows/docker.yml
  - docs/measurements/2026-09-05-welle0-proben/README.md
autonomous: true
requirements: [SEM-01]
must_haves:
  truths:
    - "D-02: Das Abbild traegt ein selbst aus dem fp32-Original quantisiertes int8-Modell; die mitgelieferte AVX512-Datei wird nirgends benutzt"
    - "D-06: Ein Bau, dessen quantisierte Datei ausserhalb des erwarteten Groessenfensters liegt, bricht ab statt still 266 MB Einbettungstabelle mitzuschleppen"
    - "D-09: Die vec0-Bibliothek liegt an einem festen Pfad im Abbild, mit Pruefsumme beim Bau geprueft, und ihre Herkunft steht in THIRD-PARTY.md"
    - "D-18: Probe A12 (vec0-KNN unter PRAGMA query_only = 1) und Probe A13 (ladbare SQLite-Erweiterungen in der CPython-Uebersetzung des Abbilds) sind aktenkundig beantwortet, bevor irgendein Schema entsteht"
    - "Der Container braucht fuer Modell und Erweiterung kein Netzwerk: HF_HUB_OFFLINE=1 und feste Pfade"
    - "Der Zuwachs der Abbildgroesse ist gemessen und benannt; die Box hat nur 40 GB Systemplatte, und Abbilder liegen seit Docker 29 unter /var/lib/containerd"
  artifacts:
    - path: "scripts/dev/quantize_model.py"
      provides: "fp32-ONNX nach int8, mit Groessenpruefung und ohne Netzwerkzugriff zur Laufzeit"
      contains: "quantize_dynamic"
    - path: "backend/Dockerfile"
      provides: "Modell-Baustufe mit fail-closed Groessen- und Pruefsummen-Gate, vec0.so an festem Pfad, HF_HUB_OFFLINE"
      contains: "HF_HUB_OFFLINE"
    - path: "docs/measurements/2026-09-05-welle0-proben/README.md"
      provides: "die Antworten auf A12 und A13, mit Kommandozeile, Architektur und Datum"
      contains: "query_only"
    - path: "THIRD-PARTY.md"
      provides: "Herkunft und Lizenz von sqlite-vec, fastembed, onnxruntime, semantic-text-splitter und dem Modell"
      contains: "sqlite-vec"
  key_links:
    - from: "backend/Dockerfile"
      to: "scripts/dev/quantize_model.py"
      via: "die Modell-Baustufe ruft das Skript und prueft danach die Dateigroesse"
      pattern: "quantize_model"
    - from: "backend/tests/test_vec_extension_probe.py"
      to: "vec0.so"
      via: "die Probe laedt die Erweiterung ueber den festen Pfad aus dem Abbild"
      pattern: "vec0"
---

## Phase Goal

Der Nutzer findet Dokumente auch über Umschreibungen statt nur über exakte Wörter,
im selben RAM-Budget und durch dieselbe Rechtekette.

(Wörtlich aus ROADMAP.md übernommen. Die Zeile steht dort nicht in der
Drei-Slot-Form "Als ... möchte ich ... damit ..."; sie wurde nicht erfunden.)

<objective>
Die Bausteine, ohne die keine einzige Messung dieser Phase laufen kann: vier
gepinnte Pakete, ein selbst quantisiertes Modell im Abbild, die vec0-Bibliothek
an einem festen Pfad, und die beiden Fünf-Minuten-Proben, deren Antwort die
gesamte Leseseitenarchitektur berührt.

Purpose: Die Welle-0-Messungen aus D-18 brauchen ein arm64-Abbild, in dem das
quantisierte Modell und die Erweiterung schon liegen. Zwei Annahmen aus dem
Assumptions Log (A12, A13) sind hier billig zu beantworten und teuer, wenn man
sie erst nach der Schemafestlegung stellt: fällt A12 negativ aus, kann die
Leseseite ihr `PRAGMA query_only = 1` für den Vektorspeicher nicht behalten,
und das ist eine Entwurfsentscheidung, keine Zeile. Fällt A13 negativ aus,
braucht das Abbild eine eigene Python-Übersetzung, und das gehört gewusst,
bevor jemand gegen vec0 programmiert.

Der teuerste stille Fehler der ganzen Phase steckt ebenfalls hier: quantisiert
`quantize_dynamic` die Einbettungstabelle nicht mit, wiegt das Ergebnis rund
384 MB statt 118 MB, nichts schlägt fehl, und 266 MB liegen ab dann dauerhaft
im Arbeitsspeicher einer 4-GB-Box. Zwei Zeilen im Bauablauf fangen das ab.

Dies ist keine Nutzerscheibe, und das ist Absicht: Erfolgskriterium 4 verlangt
ausdrücklich, dass das Vektorschema erst nach einem Lasttest festgezurrt wird,
und der Owner hat am 04.09. verbindlich gemacht, dass Welle 0 vor jeder
Schemafixierung läuft.

Output: ein Abbild, das Modell und Erweiterung offline bereithält, plus zwei
aktenkundige Antworten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-semantische-suche/06-CONTEXT.md
@.planning/phases/06-semantische-suche/06-RESEARCH.md
@.planning/phases/06-semantische-suche/06-PATTERNS.md

<interfaces>
Die belegten Eckdaten (06-RESEARCH.md 2.4, HuggingFace-API vom 04.09.2026):

  Modell: intfloat/multilingual-e5-small, Lizenz MIT, 384 Dimensionen,
  Kontextfenster 512 Token.
  onnx/model.onnx (fp32)            470.268.510 Byte
  onnx/tokenizer.json                17.082.730 Byte
  onnx/sentencepiece.bpe.model        5.069.051 Byte
  onnx/model_qint8_avx512_vnni.onnx 118.346.824 Byte -> NICHT verwenden, x86-only

  470.268.510 / 4 = 117.567.128, also traegt eine gelungene int8-Quantisierung
  rund 118 MB. 81,7 Prozent der Parameter (250.002 x 384 = 96,0 Mio) stecken in
  der Einbettungstabelle; erfasst quantize_dynamic sie nicht, wiegt die Datei
  rund 384 MB.

Die Paketpruefung (06-RESEARCH.md, Package Legitimacy Audit, slopcheck lief,
5 gescannt, 5 OK, keine [SLOP], keine [SUS], keine [ASSUMED]):
  sqlite-vec              0.1.9   Apache-2.0  py3-none manylinux_2_17_aarch64
  fastembed               0.8.0   Apache-2.0  reines Python
  onnxruntime             1.29.0  MIT         cp313 manylinux_2_28_aarch64
  semantic-text-splitter  0.32.0  MIT         cp310-abi3 manylinux_2_28_aarch64
  usearch                 2.26.2  Apache-2.0  NUR Ausweichpfad, wird NICHT installiert

Vertraeglichkeit (06-RESEARCH.md 2.4): fastembed 0.8.0 verlangt fuer Python
3.13 onnxruntime > 1.21.0 mit != 1.24.0 und != 1.24.1, numpy >= 2.1.0,
tokenizers >= 0.15 < 1.0, huggingface-hub >= 0.20 < 2.0, requests >= 2.31 < 3.0,
pillow >= 11.0.0 < 13.0. Der bestehende Pin pillow==12.3.0 passt.

huggingface-hub und requests sind neue Netzwerkbibliotheken in einem Container,
der nichts nach draussen sprechen darf. HF_HUB_OFFLINE=1 ist das Netz, der
Offline-Test in Plan 06-10 ist der Beweis.

Bestehende Muster im Dockerfile, die woertlich zu erben sind:
  Z. 19-22  Basisabbild per Digest gepinnt, nicht per Tag
  Z. 26-29  uv aus seinem eigenen, per Digest gepinnten Abbild
  Z. 73-110 der wngerman-Block: apt-Pin, fail-closed test -s im selben RUN,
            Lizenzkopie nach /usr/local/share/findling/COPYING.*
  Z. 112-164 der tesseract-Block: die Begruendung, warum Daten hart gepinnt
            werden und Werkzeuge nicht ("die traineddata entscheidet, was die
            OCR liest")
  Z. 228-230 der ENV-Block der Laufzeitstufe

Der Kopf des Dockerfiles sagt heute "the frpc stage owns the one download this
image needs". Dieser Satz wird mit der Modellstufe falsch und ist mitzuziehen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vier neue Kanten, exakt gepinnt, jede mit ihrer Begruendung</name>
  <files>backend/pyproject.toml, backend/uv.lock, THIRD-PARTY.md</files>
  <read_first>
    - backend/pyproject.toml vollstaendig, besonders der Pillow-Block (Z. 22-28: die Kommentarform, die jede Kante traegt) und der constraint-dependencies-Block (Z. 48-52)
    - THIRD-PARTY.md vollstaendig, besonders der OCR-Abschnitt (Aufbau je Eintrag: Name, Version, Lizenz, Herkunft, wofuer)
    - .planning/phases/06-semantische-suche/06-RESEARCH.md Abschnitt "Package Legitimacy Audit" und Abschnitt 2.4 (die Vertraeglichkeitstabelle)
    - backend/tests/test_workflow_pins.py (was dieses Repo ueber Pins bereits durchsetzt)
  </read_first>
  <action>
    Vier neue direkte Kanten in backend/pyproject.toml, jede exakt mit `==`
    gepinnt und jede mit einem Begruendungskommentar darueber nach dem Muster des
    Pillow-Blocks:
      fastembed==0.8.0
      onnxruntime==1.29.0
      sqlite-vec==0.1.9
      semantic-text-splitter==0.32.0

    Die Kommentare sagen je, warum das Paket da ist und warum diese Fassung:
    sqlite-vec traegt zusaetzlich den Satz, dass hier ausdruecklich kein `>=`
    steht, weil die 0.1.10-Reihe ausschliesslich aus Vorabfassungen besteht und
    seit dem 18.05.2026 kein Commit mehr in das Repo geflossen ist (D-07); das
    ist dasselbe Argument, mit dem der tesseract-Block seine Sprachpakete pinnt.
    onnxruntime traegt den Satz, dass 1.24.0 und 1.24.1 von fastembed
    ausdruecklich ausgeschlossen sind.

    usearch wird NICHT installiert (D-10): es ist ein dokumentierter Ausweichpfad
    und keine Abhaengigkeit. Falls jemand spaeter nach dem Paket sucht, soll der
    Kommentar an der sqlite-vec-Zeile sagen, wo der Ausweichpfad beschrieben ist
    (docs/embeddings.md, Plan 06-04).

    huggingface-hub und requests kommen transitiv ueber fastembed. Wenn eine
    Sicherheitsuntergrenze noetig ist, gehoert sie in constraint-dependencies,
    nicht als direkte Kante; wenn nicht, gehoert ein Satz in den Kommentar der
    fastembed-Zeile, dass diese beiden Netzwerkbibliotheken mitkommen und dass
    HF_HUB_OFFLINE plus der Offline-Test aus Plan 06-10 die Antwort darauf sind.

    Danach `uv lock` in backend/ ausfuehren, damit uv.lock die aufgeloesten
    Hashes traegt.

    THIRD-PARTY.md bekommt fuenf neue Eintraege im Format des OCR-Abschnitts:
    die vier Pakete plus das Modell intfloat/multilingual-e5-small (MIT, Quelle
    HuggingFace, verteilt als selbst quantisierte int8-ONNX-Datei im Abbild).
    Beim sqlite-vec-Eintrag steht ausdruecklich, dass die vorgebaute Bibliothek
    aus dem py3-none-Rad im Abbild festgehalten wird (D-09).

    Kommentare und Bezeichner englisch. Deutsche Prosa nur in .planning und docs.
    Keine Em-Dashes, keine En-Dashes.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv lock --check &amp;&amp; uv run python -c "import sqlite_vec, fastembed, onnxruntime, semantic_text_splitter; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sqlite-vec==0.1.9' backend/pyproject.toml` ist 1, und `grep -c 'sqlite-vec>=' backend/pyproject.toml` ist 0.
    - Alle vier neuen Zeilen benutzen `==`; `grep -n 'fastembed\|onnxruntime\|sqlite-vec\|semantic-text-splitter' backend/pyproject.toml` zeigt keine Zeile mit `>=` oder `~=`.
    - `uv lock --check` in backend/ ist gruen (uv.lock passt zu pyproject.toml).
    - `usearch` kommt in backend/pyproject.toml und backend/uv.lock nicht vor.
    - THIRD-PARTY.md nennt alle fuenf Eintraege mit Lizenz; `grep -c 'Apache-2.0' THIRD-PARTY.md` ist mindestens 2 und `grep -c 'MIT' THIRD-PARTY.md` mindestens 3.
    - `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` sind gruen.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den drei Dateien.
  </acceptance_criteria>
  <done>Die vier Pakete sind exakt gepinnt, aufgeloest, begruendet und in THIRD-PARTY.md mit Lizenz und Herkunft gefuehrt.</done>
</task>

<task type="auto">
  <name>Task 2: Das Modell selbst quantisieren und mit der Erweiterung ins Abbild backen</name>
  <files>scripts/dev/quantize_model.py, backend/Dockerfile</files>
  <read_first>
    - backend/Dockerfile vollstaendig, besonders Z. 1-40 (Kopf und Stufenaufbau), Z. 73-110 (wngerman: fail-closed test -s plus Lizenzkopie im selben RUN), Z. 112-164 (tesseract: warum Daten gepinnt werden und Werkzeuge nicht), Z. 200-235 (Benutzer, Rechte, ENV-Block)
    - scripts/dev/measure_wordlist.sh (Aufrufform und Ausgabestil der Werkzeuge unter scripts/dev)
    - scripts/dev/build_load_corpus.py (CLI-Muster eines Python-Werkzeugs dieses Repos: argparse, Ausgabe, Fehlerpfad)
    - backend/src/findling/config.py Z. 194-230 und Z. 317-401 (wie Pfade und Schalter in Settings ankommen; hier wird nur der Pfadname festgelegt, die Settings-Felder kommen in Plan 06-05)
    - .planning/phases/06-semantische-suche/06-RESEARCH.md Abschnitt 2.4 und Fallstrick 2
  </read_first>
  <action>
    Erstens scripts/dev/quantize_model.py. Es nimmt `--input` (Pfad der
    fp32-Datei onnx/model.onnx), `--output` (Pfad der int8-Datei) und
    `--max-bytes` beziehungsweise `--min-bytes`. Es ruft
    `onnxruntime.quantization.quantize_dynamic` mit `weight_type=QuantType.QInt8`
    und prueft danach die Ausgabegroesse gegen beide Grenzen. Beide Grenzen, nicht
    nur die obere: eine zu kleine Datei ist genauso ein Befund wie eine zu grosse.
    Vorgabewerte: min 100.000.000 Byte, max 136.314.880 Byte (130 MiB).
    Der Erwartungswert 117.567.128 Byte und seine Herleitung
    (470.268.510 / 4) stehen als Kommentar darueber, zusammen mit dem Satz, was
    ein Ueberschreiten bedeutet: die Einbettungstabelle mit 81,7 Prozent aller
    Parameter ist fp32 geblieben, und das kostet 266 MB Abbild und 266 MB
    dauerhaften Arbeitsspeicher, ohne dass irgendetwas fehlschlaegt.
    Das Skript gibt ausschliesslich Zahlen und Pfade aus, nie Modellinhalte, und
    endet bei Verletzung einer Grenze mit einem Exitcode ungleich 0.

    Zweitens eine eigene Baustufe `model` im Dockerfile, zwischen build und
    runtime. Sie zieht vom HuggingFace-Repo intfloat/multilingual-e5-small genau
    die Dateien, die fastembed fuer ein selbst registriertes Modell braucht:
    mindestens onnx/model.onnx, onnx/tokenizer.json, onnx/sentencepiece.bpe.model
    plus die Konfigurationsdateien, die die eigene Registrierung verlangt
    (config.json, tokenizer_config.json, special_tokens_map.json; die genaue
    Pflichtmenge ist aus dem fastembed-Vertrag fuer eigene Modelle abzulesen und
    im Kommentar zu nennen). Jede gezogene Datei wird gegen eine im Dockerfile
    stehende sha256-Summe geprueft, fail-closed im selben RUN, nach dem Muster des
    wngerman-Blocks. Danach laeuft quantize_model.py, und der Bau bricht ab, wenn
    die Groessenpruefung anschlaegt.

    Das Ergebnis der Stufe: die int8-Datei, der Tokenizer und die
    Konfigurationsdateien unter einem festen Pfad, plus die MIT-Lizenz des
    Modells als /usr/local/share/findling/COPYING.multilingual-e5-small.
    Fester Pfad: /usr/local/share/findling/model. Die fp32-Datei wird NICHT in
    die Laufzeitstufe uebernommen.

    Drittens die vec0-Bibliothek. Sie kommt ueber das Rad sqlite-vec==0.1.9, das
    uv.lock mit seinem Hash festhaelt, also ist die Herkunft gepinnt. Zusaetzlich
    wird sie in der Laufzeitstufe an einen festen Pfad kopiert
    (/usr/local/lib/findling/vec0.so), damit der Ladepfad eine Eigenschaft des
    Abbilds ist und nicht eine Eigenschaft des Paketlayouts. Der Pfad innerhalb
    des Rads wird beim Bau ermittelt, nicht geraten (sqlite_vec stellt dafuer eine
    Funktion bereit, ihr Name ist beim Lesen des installierten Pakets zu
    bestimmen), und die Kopie wird mit einer sha256-Summe im selben RUN geprueft.
    Die Apache-2.0-Lizenz wird als /usr/local/share/findling/COPYING.sqlite-vec
    mitkopiert (D-09).

    Viertens der ENV-Block der Laufzeitstufe (heute Z. 228-230) bekommt drei
    Zeilen dazu:
      HF_HUB_OFFLINE=1
      FINDLING_EMBED_MODEL_DIR=/usr/local/share/findling/model
      FINDLING_VEC0_PATH=/usr/local/lib/findling/vec0.so
    Der Kommentar darueber sagt, was HF_HUB_OFFLINE ist und was es nicht ist: ein
    Netz, kein Beweis, und der Beweis ist der Offline-Test aus Plan 06-10.

    Fuenftens der Dateikopf: der Satz "the frpc stage owns the one download this
    image needs" stimmt ab jetzt nicht mehr und wird auf die zwei Downloads
    gehoben, mit einem Satz, warum das Modell im Abbild liegt und nicht beim
    ersten Start geladen wird (Zero-Config bei Firewall, Proxy und
    Offline-Installation).

    Sechstens der Zuwachs der Abbildgroesse. Nach dem Bau wird die Groesse des
    Abbilds gegen die Groesse vor dieser Aenderung gehalten und die Differenz im
    SUMMARY benannt; die Recherche erwartet 200 bis 260 MB. Der Grund gehoert
    dazu: die Box hat nur 40 GB Systemplatte, und Abbilder liegen seit Docker 29
    unter /var/lib/containerd und nicht im data-root, weshalb die
    containerd-Korrektur aus Plan 05-14 weiter gilt. Ein Zuwachs deutlich
    ueber 400 MB ist ein Befund und wird als solcher berichtet, weil er auf eine
    nicht quantisierte Einbettungstabelle oder auf ein versehentlich
    mitkopiertes fp32-Modell hindeutet.

    Kommentare englisch. Keine Em-Dashes, keine En-Dashes.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; docker build -f backend/Dockerfile -t findling-sem-probe:local backend &amp;&amp; docker run --rm --network none findling-sem-probe:local python -c "import os,pathlib; d=pathlib.Path(os.environ['FINDLING_EMBED_MODEL_DIR']); m=[p for p in d.rglob('*.onnx')]; assert len(m)==1, m; s=m[0].stat().st_size; assert 100_000_000 < s < 136_314_880, s; assert pathlib.Path(os.environ['FINDLING_VEC0_PATH']).is_file(); print('ok', s)"</automated>
  </verify>
  <acceptance_criteria>
    - `docker build` fuer backend/Dockerfile laeuft auf amd64 und arm64 durch (der bestehende docker.yml-Matrixjob bleibt gruen).
    - Im fertigen Abbild liegt genau eine .onnx-Datei unter $FINDLING_EMBED_MODEL_DIR, ihre Groesse liegt zwischen 100.000.000 und 136.314.880 Byte, und die fp32-Datei mit 470.268.510 Byte ist NICHT im Abbild.
    - `docker run --rm findling-sem-probe:local sh -c 'ls -l $FINDLING_VEC0_PATH'` zeigt eine vorhandene Datei.
    - `docker run --rm findling-sem-probe:local sh -c 'ls /usr/local/share/findling/'` zeigt COPYING.multilingual-e5-small und COPYING.sqlite-vec.
    - `docker run --rm findling-sem-probe:local sh -c 'echo $HF_HUB_OFFLINE'` gibt 1 aus.
    - Ein absichtlich manipuliertes Groessenfenster (max-bytes auf 1) laesst `uv run python scripts/dev/quantize_model.py` mit Exitcode ungleich 0 enden: das Gate kann rot werden.
    - `grep -v '^#' backend/Dockerfile | grep -c 'model_qint8_avx512_vnni'` ist 0.
    - Der Zuwachs der Abbildgroesse gegenueber dem Stand vor diesem Plan ist gemessen und im SUMMARY genannt; er liegt unter 400 MB, oder die Abweichung ist als Befund benannt.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in beiden Dateien.
  </acceptance_criteria>
  <done>Das Abbild traegt ein selbst quantisiertes int8-Modell im belegten Groessenfenster und die vec0-Bibliothek an einem festen Pfad, beides ohne Netzwerkzugriff zur Laufzeit und beides mit einem Bau-Gate, das rot werden kann.</done>
</task>

<task type="auto">
  <name>Task 3: Die zwei Fuenf-Minuten-Proben, A12 und A13, aktenkundig</name>
  <files>backend/tests/test_vec_extension_probe.py, .github/workflows/docker.yml, docs/measurements/2026-09-05-welle0-proben/README.md</files>
  <read_first>
    - backend/src/findling/store/repo.py Z. 407-470 (_connect, enable_wal, die beiden Verbindungsarten und die Stelle, an der enable_load_extension fehlt)
    - backend/tests/test_store_repo.py (Fixture-Form fuer eine echte SQLite-Datei in diesem Repo)
    - .github/workflows/docker.yml vollstaendig, besonders Z. 74-80 (die Plattform-Matrix mit ubuntu-24.04-arm) und die Stelle, an der ein Schritt im gebauten Abbild laeuft
    - docs/measurements/2026-09-04-volllauf-cpx22/README.md (Aufbau eines Messberichts in diesem Repo: Datum, Hardware, Kommandozeile, Zahl, Vorbehalt)
    - .planning/phases/06-semantische-suche/06-RESEARCH.md Abschnitt 1.2 und Assumptions Log A12/A13
  </read_first>
  <action>
    Probe A13 zuerst, weil A12 sie voraussetzt: ein Test stellt fest, ob die
    CPython-Uebersetzung im Abbild ladbare SQLite-Erweiterungen erlaubt. Das ist
    das Vorhandensein von `enable_load_extension` auf einer sqlite3-Verbindung
    plus ein tatsaechlich gelungener Ladevorgang der Datei aus
    $FINDLING_VEC0_PATH. Ein fehlendes Attribut und ein fehlgeschlagener
    Ladevorgang sind zwei verschiedene Befunde und werden getrennt gemeldet.

    Probe A12 danach: eine vec0-Virtualtabelle mit int8[384] anlegen, ein paar
    hundert Zufallsvektoren schreiben, die Verbindung schliessen, die Datei
    erneut oeffnen, diesmal mit `PRAGMA query_only = 1` wie auf der Leseseite von
    repo.py, und eine KNN-Abfrage fahren. Der Test haelt beide moeglichen
    Ausgaenge fest: laeuft sie, ist die Leseseite unveraendert; laeuft sie nicht,
    wird die genaue Fehlermeldungsklasse festgehalten, weil Plan 06-04 daraus
    seine Verbindungsentscheidung ableitet. Der Test ist so geschrieben, dass er
    in BEIDEN Faellen gruen ist und die Antwort als Tatsache festhaelt; er ist
    eine Messung, kein Gate. Genau ein Test in der Datei ist ein echtes Gate: die
    Erweiterung muss ladbar sein (A13), denn ohne sie ist die Phase nicht baubar.

    Die Zufallsvektoren werden mit festem Startwert erzeugt. Es wird kein einziges
    Zeichen echten Textes angefasst und nichts ausser Zahlen ausgegeben
    (T-02-14).

    In .github/workflows/docker.yml kommt ein Schritt dazu, der diese Testdatei im
    frisch gebauten Abbild ausfuehrt, auf beiden Architekturen der bestehenden
    Matrix. Der Schritt laeuft mit `--network none`, weil er nebenbei belegt, dass
    der Ladevorgang kein Netzwerk braucht. Actions bleiben so gepinnt, wie
    test_workflow_pins.py es verlangt.

    docs/measurements/2026-09-05-welle0-proben/README.md haelt beide Antworten
    fest, im Aufbau des bestehenden Messberichts: Datum, Architektur, Abbild-
    Digest, exakte Kommandozeile, die Antwort, und was aus ihr folgt. Fuer A12
    steht ausdruecklich drin, welche Konsequenz die Antwort fuer Plan 06-04 hat.

    Kommentare und Bezeichner englisch, der Messbericht deutsch. Keine
    Em-Dashes, keine En-Dashes.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -m pytest tests/test_vec_extension_probe.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -m pytest tests/test_vec_extension_probe.py -q` ist gruen.
    - Der Test schlaegt fehl, wenn $FINDLING_VEC0_PATH auf eine nicht ladbare Datei zeigt (durch eine absichtliche Fehlleitung im Testlauf belegt).
    - Ein Lauf von docker.yml ist auf amd64 UND arm64 gruen und das Log zeigt fuer beide Architekturen die Antwort auf A12 und A13.
    - docs/measurements/2026-09-05-welle0-proben/README.md nennt fuer beide Proben: Datum, Architektur, Abbild-Digest, Kommandozeile, Antwort, Konsequenz.
    - `grep -c 'query_only' docs/measurements/2026-09-05-welle0-proben/README.md` ist mindestens 1.
    - Die Testausgabe enthaelt keinen Text aus testdata und keinen Dateinamen eines Nutzerdokuments.
    - `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` sind gruen.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den drei Dateien.
  </acceptance_criteria>
  <done>A12 und A13 sind auf beiden Architekturen beantwortet und die Antworten stehen mit ihrer Konsequenz im Repo, bevor irgendein Vektorschema entsteht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI und HuggingFace zum Bauablauf | Fremde Artefakte betreten das Abbild; nach dem Bau ist niemand mehr da, der sie prueft |
| Abbild zum Netzwerk | Der Container soll nichts nach draussen sprechen; fastembed bringt huggingface-hub und requests mit |
| SQLite-Prozess zur ladbaren Erweiterung | enable_load_extension oeffnet einen Weg, beliebigen Maschinencode in den Prozess zu laden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Modellartefakt aus HuggingFace | mitigate | Jede gezogene Datei per sha256 im selben RUN geprueft, fail-closed nach dem wngerman-Muster; Bau bricht ab statt weiterzulaufen |
| T-06-02 | Tampering | Quantisierung liefert still ein anderes Modell als erwartet | mitigate | Zweiseitiges Groessenfenster 100 MB bis 130 MiB in quantize_model.py, Exitcode ungleich 0, Erwartungswert und Herleitung als Kommentar |
| T-06-03 | Elevation of Privilege | enable_load_extension laedt eine beliebige .so | mitigate | Der Ladepfad ist eine Abbildkonstante ($FINDLING_VEC0_PATH), kein Wert aus einer Anfrage; die Datei wird beim Bau per sha256 geprueft; /app und /usr/local sind fuer den Prozessbenutzer nicht schreibbar |
| T-06-04 | Information Disclosure | huggingface-hub und requests telefonieren zur Laufzeit | mitigate | HF_HUB_OFFLINE=1 im ENV-Block, Modell und Tokenizer an festen Pfaden im Abbild, Probe laeuft mit --network none; der belastbare Beweis ist der Offline-Test aus Plan 06-10 |
| T-06-05 | Information Disclosure | Die Probe druckt Inhalte aus testdata | mitigate | Ausschliesslich Zufallsvektoren mit festem Startwert, Ausgabe nur Zahlen (T-02-14) |
| T-06-SC | Tampering | pip/uv installs | mitigate | Package Legitimacy Audit aus 06-RESEARCH.md liegt vor: 5 gescannt, 5 OK, keine [SLOP], keine [SUS], keine [ASSUMED]. Alle Fassungen exakt gepinnt, uv.lock haelt die Hashes, usearch wird nicht installiert |
</threat_model>

<verification>
- `cd backend && uv run python -m pytest -q` gruen, ruff, pyright, vulture gruen
- `uv lock --check` gruen
- docker.yml auf amd64 und arm64 gruen, der Probenschritt beantwortet A12 und A13
- Die int8-Datei im Abbild liegt im Groessenfenster, die fp32-Datei ist nicht im Abbild
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien
</verification>

<success_criteria>
Ein arm64- und ein amd64-Abbild tragen das selbst quantisierte int8-Modell im
belegten Groessenfenster und die vec0-Bibliothek an einem festen Pfad, beide
ohne Netzwerkzugriff zur Laufzeit. Die Fragen A12 (vec0-KNN unter query_only)
und A13 (ladbare Erweiterungen) sind auf beiden Architekturen beantwortet und
mit ihrer Konsequenz im Repo festgehalten.
</success_criteria>

<output>
Create `.planning/phases/06-semantische-suche/06-01-SUMMARY.md` when done
</output>
