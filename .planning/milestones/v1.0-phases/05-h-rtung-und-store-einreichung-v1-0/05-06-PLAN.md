---
phase: 05-h-rtung-und-store-einreichung-v1-0
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/findling/index/search.py
  - backend/src/findling/extract/sandbox.py
  - backend/src/findling/store/repo.py
  - backend/src/findling/api/resources.py
  - php/lib/Service/PathResolverService.php
  - php/lib/Settings/Section.php
  - php/lib/Service/ExclusionService.php
  - backend/tests/test_snippet_offsets.py
  - backend/tests/test_search_library.py
  - backend/tests/test_sandbox.py
  - backend/tests/test_store_repo.py
  - backend/tests/test_exclusion_path_space.py
autonomous: true
requirements: [PKG-05]
must_haves:
  truths:
    - "D-20: Die Review-Kleinreste ohne eigenen Beweisaufbau aus den Phase-2/3/4-Audits sind geschlossen, keine Position stillschweigend vertagt"
    - "Zwei benachbarte Treffer im Snippet bleiben zwei Hervorhebungen und verschmelzen nicht"
    - "Ein Byte-Offset mitten in einem Mehrbyte-Zeichen fuehrt nicht mehr zu einem Fehler in der Suche"
    - "Nach einem Zeitablauf im Extraktions-Sandkasten wird der Kindprozess wirklich erneuert, statt weitergezaehlt zu werden"
    - "Ein frischer Index erzeugt beim Statuspoll keine Warnung mehr, und die Statuszahlen kosten keinen Full-Scan je Aufruf"
    - "Die Zustandsdaten der Suchoberflaeche werden nicht mehr ohne Sperre aus mehreren Threads bearbeitet, und keine Store-Verbindung bleibt offen"
    - "Eine Ausschlussregel mit einem Punkt-Segment wird nicht mehr gespeichert, oder ihre Wirkungslosigkeit ist dokumentiert und geprueft"
  artifacts:
    - path: "backend/src/findling/index/search.py"
      provides: "Snippet-Hervorhebungen ohne Verschmelzung, robuste Dekodierung, Praefix nur einmal dekodiert"
      contains: "errors="
    - path: "backend/src/findling/api/resources.py"
      provides: "Zustandsdaten unter Sperre, Verbindungen geschlossen, degraded nicht je Suche neu berechnet"
      contains: "Lock"
    - path: "php/lib/Service/ExclusionService.php"
      provides: "eine Ausschlussregel, die trifft, oder eine Ablehnung mit Begruendung"
      contains: "excluded"
  key_links:
    - from: "backend/src/findling/api/resources.py"
      to: "backend/src/findling/store/repo.py"
      via: "geoeffnete Store-Verbindungen werden in einem finally geschlossen"
      pattern: "close"
    - from: "backend/src/findling/index/search.py"
      to: "backend/src/findling/index/analyzer.py"
      via: "Snippet-Offsets bleiben nach ascii_fold auf Zeichengrenzen"
      pattern: "char_ranges"
---

<objective>
Die kleinen Befunde aus den Audits der Phasen 2, 3 und 4 werden abgearbeitet, weil sie zusammen den Unterschied zwischen einem Release und einem Release mit bekannten Maengeln ausmachen.

Purpose: D-20: ein ausdruecklicher Owner-Entscheid gegen die schlankere Empfehlung: alle offenen Review-Reste werden in Phase 5 geschlossen. Dieser Plan nimmt die Positionen, die keinen eigenen Beweisaufbau brauchen, aber jede fuer sich eine falsche Aussage im Produkt sind: eine Warnung, die den Normalzustand meldet und damit das Log entwertet; zwei Fehler in der Snippet-Erzeugung, von denen einer eine Ausnahme werfen kann; ein Zaehler, der einen Recycle-Reset ueberschreibt; Zustandsdaten, die ohne Sperre aus Worker-Threads bearbeitet werden; und eine Ausschlussregel, die der Admin speichern kann und die nie trifft. Der letzte Punkt ist der teuerste, weil er wie eine Einstellung aussieht, die wirkt.

Nach diesem Plan bekommt der Admin etwas Neues: ein Log, in dem eine Warnung wieder etwas bedeutet, und eine Ausschlussregel, die entweder trifft oder abgelehnt wird.

Output: neun benannte Positionen geschlossen, jede mit einem Test oder einer dokumentierten Entscheidung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-CONTEXT.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-RESEARCH.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-PATTERNS.md
@.planning/phases/04-admin-sichtbarkeit-und-diagnose/04-REVIEW.md

<interfaces>
Die neun Positionen mit Ort, alle am 03.09.2026 gegen den Arbeitsbaum geprueft
(05-RESEARCH.md, Abschnitt "Offen, gehoert nach D-20 in diese Phase"):

Container:
  Bug-L1   index/search.py:229    ein <= verschmilzt angrenzende Hervorhebungen
  Bug-L2   index/search.py:225    decode() ohne errors= kann bei einem Byte-Offset mitten im
                                  Zeichen werfen
  Perf-LOW index/search.py:203    char_ranges dekodiert den Praefix je Bereich neu
  Bug-L8   extract/sandbox.py:254 _files_handled++ nach _ask() ueberschreibt den
                                  Recycle-Reset nach einem Zeitablauf (gegen :330)
  IN-01    store/repo.py:915      index_bytes() warnt bei jedem Poll fuer den normalen
                                  Frischzustand; gerufen aus api/status.py:151 und
                                  api/rates.py:138
  Perf-LOW store/repo.py:840      acl_totals als COUNT(DISTINCT) ist ein Full-Scan; auf 50k
                                  Dateien wird der Admin-Poll teuer
  IN-06    api/resources.py:58-59, 156-194  _OPEN und _MARKS werden ohne Sperre aus
                                  Worker-Threads bearbeitet, und eine Store-Verbindung leckt.
                                  Identisch mit Bug-Audit M7, der einzige nicht kosmetische
                                  Rest der Phase-4-Infos
  Perf-LOW api/resources.py:197   degraded() rechnet je Suche read_meta plus disk_usage

PHP:
  IN-04    php/lib/Service/PathResolverService.php:204  die Backslash-Umwandlung macht Dateien
                                  mit Backslash im Namen nicht diagnostizierbar; Entscheidung
                                  noetig, fixen oder dokumentieren
  IN-05    php/lib/Settings/Section.php:22-27  der Docblock begruendet den Verzicht auf
                                  #[\Override] falsch
  IN-07    php/lib/Service/ExclusionService.php:207-225  ein Punkt-Segment in einer
                                  Ausschlussregel wird gespeichert und trifft nie

Regeln, die fuer alles gelten:
  Ein Snippet ist Dateiinhalt und bleibt an SRCH-02 gebunden; nichts hier lockert das.
  Der eine Pfadraum der Ausschluesse ist von backend/tests/test_exclusion_path_space.py
  gepinnt und bleibt einer.
  Logregel: statischer Satz plus exception-Feld, nie eine Bibliotheksmeldung, nie ein Pfad.
  Die Snippet-Offsets sind gegen ascii_fold empfindlich; die vorhandenen Tests dazu liegen in
  backend/tests/test_snippet_offsets.py und sind der Rahmen fuer die neuen Faelle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snippets und Sandkasten: vier Positionen in zwei Dateien</name>
  <files>backend/src/findling/index/search.py, backend/src/findling/extract/sandbox.py, backend/tests/test_snippet_offsets.py, backend/tests/test_search_library.py, backend/tests/test_sandbox.py</files>
  <read_first>
    - backend/src/findling/index/search.py (die Zeilen 190 bis 240 vollstaendig: char_ranges, die Dekodierung und die Bereichszusammenfassung)
    - backend/src/findling/index/analyzer.py (was ascii_fold mit den Offsets macht, damit die Korrektur die vorhandene Zusage nicht bricht)
    - backend/src/findling/extract/sandbox.py (die Zeilen 240 bis 340: _ask, der Zeitablaufzweig, der Recycle-Reset und _files_handled)
    - backend/tests/test_snippet_offsets.py, backend/tests/test_search_library.py, backend/tests/test_sandbox.py (die bestehenden Faelle und ihre Bauform)
    - docs/german-analyzer.md (welche Zusage die Snippet-Erzeugung nach aussen gibt)
  </read_first>
  <behavior>
    - Zwei Treffer, die im Text unmittelbar aneinander grenzen, ergeben zwei Hervorhebungen und nicht eine.
    - Zwei Treffer, die sich ueberlappen, ergeben weiterhin eine Hervorhebung (das bestehende Verhalten bleibt).
    - Ein Byte-Offset, der mitten in einem Mehrbyte-Zeichen liegt, fuehrt zu einem gekuerzten oder verschobenen Snippet, aber nie zu einer Ausnahme.
    - Ein Text mit Umlauten liefert dieselben Hervorhebungsgrenzen wie vor der Aenderung (Regressionsschutz ueber die bestehenden Faelle).
    - char_ranges dekodiert den Praefix nicht je Bereich neu; das Ergebnis bleibt bei mehreren Bereichen identisch zum Ergebnis vorher.
    - Nach einem Zeitablauf im Sandkasten ist der Kindprozess erneuert, und der Zaehler der bearbeiteten Dateien beginnt bei diesem Prozess bei null.
    - Ein Zeitablauf fuehrt weiterhin zum bestehenden Verdikt und veraendert es nicht.
  </behavior>
  <action>
    Erst die Tests, dann der Code, in beiden Dateien.

    index/search.py: Das <= in der Bereichszusammenfassung wird zu <, sodass angrenzende
    Bereiche getrennt bleiben; der Kommentar sagt, welcher Fall welcher ist, weil die
    Unterscheidung zwischen angrenzend und ueberlappend genau der Fehler war. Die
    Dekodierung bekommt ein ausdrueckliches errors-Verhalten; welches, ist zu begruenden, und
    die Begruendung gehoert in den Kommentar: ein Snippet darf lieber ein Zeichen verlieren als
    eine Suche mit einer Ausnahme beenden. char_ranges dekodiert den Praefix genau einmal und
    fuehrt die Position mit, statt sie je Bereich neu zu berechnen; das Ergebnis muss
    unveraendert bleiben, und die bestehenden Offset-Tests sind der Beweis dafuer.

    extract/sandbox.py: Der Zaehler _files_handled wird nicht mehr nach _ask() erhoeht, wenn der
    Zweig davor den Prozess wegen Zeitablauf erneuert hat; die Reihenfolge wird so gedreht,
    dass ein Reset nicht ueberschrieben werden kann. Der Kommentar nennt die Folge des alten
    Verhaltens: ein erneuerter Prozess galt sofort als benutzt und wurde zu frueh erneut
    erneuert, was jeden Zeitablauf teurer machte als noetig.

    Bezeichner und Kommentare englisch. Keine Em-Dashes.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -m pytest tests/test_snippet_offsets.py tests/test_search_library.py tests/test_sandbox.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -m pytest -q` ist gruen, und die sieben Verhaltensweisen liegen als Faelle vor.
    - Die drei neuen Faelle (angrenzende Treffer, Offset mitten im Zeichen, Zaehler nach Zeitablauf) waren vor der Aenderung rot.
    - `grep -c 'errors=' backend/src/findling/index/search.py` ist grosser als 0.
    - Die bestehenden Offset-Tests mit Umlauten sind unveraendert gruen (kein Test wurde angepasst, um die Aenderung zu ermoeglichen; falls doch, steht die Begruendung in der Zusammenfassung).
    - `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` sind alle gruen.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Angrenzende Treffer bleiben getrennt, ein ungluecklicher Byte-Offset wirft nicht mehr, der Praefix wird einmal dekodiert, und ein erneuerter Sandkastenprozess beginnt bei null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Statuszahlen ohne Warnflut, ohne Full-Scan und ohne offene Verbindung</name>
  <files>backend/src/findling/store/repo.py, backend/src/findling/api/resources.py, backend/tests/test_store_repo.py</files>
  <read_first>
    - backend/src/findling/store/repo.py (die Zeilen um 840 und um 915 vollstaendig: acl_totals und index_bytes, plus die Nachbarmethoden und die Tabellenform)
    - backend/src/findling/api/resources.py (die ganze Datei: _OPEN, _MARKS, wer sie aus welchem Thread anfasst, wo eine Verbindung geoeffnet und nicht geschlossen wird, und degraded)
    - backend/src/findling/api/status.py und backend/src/findling/api/rates.py (die beiden Aufrufer von index_bytes, damit die Aenderung ihre Antwortform nicht verschiebt)
    - backend/tests/test_store_repo.py und backend/tests/test_status_endpoint.py (die bestehenden Erwartungen an die Zahlen)
    - .planning/phases/04-admin-sichtbarkeit-und-diagnose/04-REVIEW.md (IN-01 und IN-06 im Wortlaut)
  </read_first>
  <behavior>
    - Ein frischer, leerer Index erzeugt beim Statuspoll keine Warnung; ein wirklich unerwarteter Zustand erzeugt weiterhin eine.
    - Die Antwortform von GET /status und GET /rates bleibt unveraendert; keine Zahl aendert ihre Bedeutung.
    - acl_totals liefert dieselben Zahlen wie vorher, ohne einen Full-Scan je Aufruf (nachweisbar an der Abfrageform oder an einer Messung auf einem grossen Testbestand).
    - Zustandsdaten der Suchoberflaeche werden unter einer Sperre bearbeitet; ein Test, der aus mehreren Threads zugreift, laeuft ohne verlorene Eintraege.
    - Jede geoeffnete Store-Verbindung wird geschlossen, auch im Fehlerfall (nachweisbar durch einen Test, der die Zahl offener Verbindungen vor und nach einem Fehlerlauf vergleicht).
    - degraded wird nicht je Suche neu aus read_meta und disk_usage berechnet; der Wert bleibt aktuell genug, dass ein ausgefallenes Backend weiterhin als solches erscheint, und die gewaehlte Frist steht als benannte Konstante mit Begruendung.
  </behavior>
  <action>
    Erst die Tests, dann der Code.

    store/repo.py: index_bytes warnt nur noch fuer einen Zustand, der wirklich unerwartet ist;
    der Normalfall eines frischen Index wird still behandelt, und der Kommentar sagt, warum:
    eine Warnung, die den Normalzustand meldet, entwertet jede andere Warnung derselben Datei.
    acl_totals bekommt eine Abfrageform ohne COUNT(DISTINCT) ueber die ganze Tabelle; welche,
    ist zu bestimmen und zu begruenden (Vorschlag: eine getrennte Zaehlung ueber die
    vorhandenen Indizes oder eine gefuehrte Summe), und die Zahlen muessen identisch bleiben.
    Ein Test mit mehreren tausend synthetischen Zeilen belegt Gleichheit und Laufzeit.

    api/resources.py: _OPEN und _MARKS bekommen eine Sperre, und jede Bearbeitung laeuft unter
    ihr; der Kommentar nennt den Grund, naemlich dass Worker-Threads dieselbe Struktur
    anfassen. Jede geoeffnete Verbindung wird in einem try/finally geschlossen. degraded
    bekommt eine kurze Gueltigkeitsfrist als benannte Konstante mit Begruendung, sodass eine
    Suche nicht jedes Mal read_meta und disk_usage bezahlt, ein ausgefallenes Backend aber
    weiterhin innerhalb der Frist erkannt wird. Die Frist gehoert in den Docstring, weil sie
    bestimmt, wie schnell die Suche sich als degradiert meldet.

    Die Antwortformen von /status und /rates werden nicht angefasst; wer eine Zahl anders
    berechnet, aendert nicht ihre Bedeutung. Die bestehenden Endpunkttests bleiben unveraendert
    gruen.

    Bezeichner und Kommentare englisch. Keine Em-Dashes. Kein Pfad in einer Logzeile.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -m pytest tests/test_store_repo.py tests/test_status_endpoint.py tests/test_rates_endpoint.py tests/test_search_endpoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -m pytest -q` ist gruen, und die sechs Verhaltensweisen liegen als Faelle vor.
    - Kein bestehender Endpunkttest wurde geaendert; die Antwortformen von /status und /rates sind identisch.
    - Ein Test greift aus mindestens vier Threads auf die Zustandsdaten zu und verliert keinen Eintrag.
    - Ein Test belegt, dass nach einem Fehlerlauf keine Store-Verbindung offen bleibt.
    - Der Vergleich der acl_totals-Zahlen vor und nach der Aenderung ist auf einem Testbestand von mindestens 5.000 Zeilen gleich, und die Laufzeit ist protokolliert.
    - `grep -c 'COUNT(DISTINCT' backend/src/findling/store/repo.py` ist 0 oder die verbleibenden Vorkommen sind im Kommentar als unkritisch begruendet.
    - Die Gueltigkeitsfrist von degraded steht als benannte Konstante mit Begruendung im Docstring.
    - `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` sind alle gruen.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Der Statuspoll warnt nicht mehr ohne Grund, kostet keinen Full-Scan, laesst keine Verbindung offen, und die Zustandsdaten der Suche werden nicht mehr ohne Sperre bearbeitet.</done>
</task>

<task type="auto">
  <name>Task 3: Drei Positionen der PHP-Haelfte, davon eine mit Entscheidung</name>
  <files>php/lib/Service/PathResolverService.php, php/lib/Settings/Section.php, php/lib/Service/ExclusionService.php, backend/tests/test_exclusion_path_space.py</files>
  <read_first>
    - php/lib/Service/PathResolverService.php (die ganze Datei, besonders die Zeilen um 204: die Backslash-Umwandlung und was sie fuer einen Dateinamen mit Backslash bedeutet)
    - php/lib/Settings/Section.php (die Zeilen 22-27: der Docblock und seine Begruendung fuer den Verzicht auf das Override-Attribut)
    - php/lib/Service/ExclusionService.php (die Zeilen 200 bis 230: die Validierung einer Regel, die Segmentbehandlung und wo ein Punkt-Segment durchfaellt)
    - backend/tests/test_exclusion_path_space.py (das Gate ueber den einen Pfadraum, das gruen bleiben muss)
    - php/lib/Service/StorageService.php (der Praefixvergleich, der eine Regel anwendet, damit die Ablehnung dort nicht noetig wird)
    - .planning/phases/04-admin-sichtbarkeit-und-diagnose/04-REVIEW.md (IN-04, IN-05 und IN-07 im Wortlaut)
  </read_first>
  <action>
    Drei Positionen, jede mit ihrer eigenen Form.

    IN-07, ExclusionService: Ein Segment, das nur aus einem Punkt besteht, wird heute
    gespeichert und trifft nie, weil der Praefixvergleich es nie so sieht. Die Regel wird bei
    der Eingabe abgelehnt, mit einer Meldung, die den Fall nennt und nicht den Wert (die
    Ausgaberegel aus DiagnoseCommand gilt auch fuer die Admin-Antwort). Auch ein Segment
    aus zwei Punkten wird abgelehnt und nicht gefiltert, falls das nicht schon geschieht.
    Zusaetzlich: eine bereits gespeicherte Regel mit Punkt-Segment darf nicht stillschweigend
    verschwinden; sie wird beim Lesen als unwirksam erkannt und dem Admin als solche gemeldet.
    Das Gate backend/tests/test_exclusion_path_space.py wird um die Feststellung erweitert, dass
    die Validierung und der Praefixvergleich denselben Pfadraum benutzen und dass ein
    Punkt-Segment auf beiden Seiten denselben Ausgang hat.

    IN-04, PathResolverService: Hier ist eine Entscheidung zu treffen und aufzuschreiben.
    Variante eins, die Umwandlung entfaellt fuer den Diagnosepfad, sodass ein Dateiname mit
    Backslash diagnostizierbar wird. Variante zwei, das Verhalten bleibt und die Grenze wird
    dokumentiert. Vorzug hat Variante eins, wenn sie ohne neuen Pfadraum auskommt, denn ein
    Admin, der eine Datei nicht nachschlagen kann, bekommt genau die Antwort, die Phase 4
    abschaffen wollte. Ist Variante eins nur mit einem zweiten Pfadraum machbar, gilt Variante
    zwei, und die Grenze wird in docs/admin-page.md nicht angefasst, sondern im Docblock der
    Methode und in der Zusammenfassung festgehalten. Die Entscheidung samt Begruendung gehoert
    in beiden Faellen in die Zusammenfassung.

    IN-05, Section.php: Der Docblock begruendet den Verzicht auf das Override-Attribut falsch.
    Die richtige Begruendung steht in den Entscheidungen der Phase 4: PHP 8.3 gegen die
    deklarierte min-version 8.2. Der Docblock wird auf diese Begruendung korrigiert, ohne dass
    am Code etwas geaendert wird.

    Bezeichner und Kommentare englisch. Keine Em-Dashes. Kein Pfad und kein abgelehnter
    Eingabewert in einer Logzeile.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -m pytest tests/test_exclusion_path_space.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -m pytest -q` ist gruen, und `tests/test_exclusion_path_space.py` enthaelt die neue Feststellung zum Punkt-Segment.
    - Auf dem lokalen Stack protokolliert: eine Ausschlussregel mit einem Punkt-Segment wird abgelehnt, die Meldung nennt den Fall und nicht den Wert, und eine gueltige Regel wird weiterhin angenommen und raeumt wie bisher.
    - Eine bereits gespeicherte Regel mit Punkt-Segment wird dem Admin als unwirksam gemeldet und verschwindet nicht still.
    - Die Entscheidung zu IN-04 steht in der Zusammenfassung, mit Begruendung, und der Docblock der betroffenen Methode nennt sie ebenfalls.
    - `php/lib/Settings/Section.php` begruendet den Verzicht auf das Override-Attribut mit der Versionsspanne PHP 8.2 gegen 8.3, und am Code der Klasse hat sich nichts geaendert (`git diff --stat` zeigt fuer diese Datei nur Kommentarzeilen).
    - `docker compose -f scripts/dev/compose.yaml exec -T app sh -c "find /var/www/html/custom_apps/findling/lib -name '*.php' -print0 | xargs -0 -n1 php -l"` meldet ausschliesslich "No syntax errors detected".
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Eine Ausschlussregel, die nie treffen kann, wird abgelehnt statt gespeichert, die Diagnose eines Dateinamens mit Backslash ist entschieden und begruendet, und ein falscher Docblock sagt nicht mehr das Falsche.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dokumenttext zu Snippet | Ein Byte-Offset entscheidet, welcher Text ausgeschnitten wird |
| Worker-Threads zu Zustandsdaten | Mehrere Threads bearbeiten dieselbe Struktur |
| Admin-Eingabe zu Ausschlussregel | Eine Regel entscheidet, was nicht indexiert wird |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Denial of Service | Eine Ausnahme in der Snippet-Erzeugung beendet eine Suche | mitigate | Ausdrueckliches errors-Verhalten bei der Dekodierung, mit Test fuer einen Offset mitten im Zeichen; ein Snippet verliert lieber ein Zeichen als eine Suche zu scheitern |
| T-05-23 | Tampering | Zustandsdaten der Suchoberflaeche werden aus mehreren Threads ohne Sperre bearbeitet | mitigate | Sperre um jede Bearbeitung, Test mit vier Threads, jede Verbindung in try/finally geschlossen |
| T-05-24 | Repudiation | Eine gespeicherte Ausschlussregel wirkt nicht und der Admin haelt sie fuer wirksam | mitigate | Punkt-Segmente werden abgelehnt, bestehende unwirksame Regeln werden als unwirksam gemeldet, und das Pfadraum-Gate prueft beide Seiten |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Kein Paket wird installiert; alle Aenderungen liegen in bereits vorhandenen Modulen |
</threat_model>

<verification>
- `cd backend && uv run python -m pytest -q` gruen
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` gruen
- Alle PHP-Dateien der App melden `php -l` ohne Fehler
- Kein bestehender Endpunkttest wurde geaendert, und die Antwortformen von /status und /rates sind identisch
- Die Entscheidung zu IN-04 ist mit Begruendung protokolliert
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien
</verification>

<success_criteria>
Neun benannte Audit-Positionen sind geschlossen: angrenzende Hervorhebungen verschmelzen nicht mehr, ein ungluecklicher Byte-Offset wirft nicht, der Praefix wird einmal dekodiert, ein erneuerter Sandkastenprozess beginnt bei null, der Statuspoll warnt nicht ohne Grund und kostet keinen Full-Scan, die Zustandsdaten der Suche stehen unter einer Sperre und lassen keine Verbindung offen, eine unwirksame Ausschlussregel wird abgelehnt, die Backslash-Frage ist entschieden und begruendet, und ein falscher Docblock ist korrigiert.
</success_criteria>

<output>
Create `.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-06-SUMMARY.md` when done
</output>
