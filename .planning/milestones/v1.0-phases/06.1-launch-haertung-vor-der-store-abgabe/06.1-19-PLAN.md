---
phase: 06.1-launch-haertung-vor-der-store-abgabe
plan: 19
type: execute
wave: 8
# Welle 8, allein und zuletzt: dieser Plan nimmt die Phase ab. Alles, was danach
# noch geaendert wuerde, waere von der Abnahme nicht gedeckt.
depends_on: [06.1-13, 06.1-15, 06.1-17, 06.1-18]
files_modified:
  - .planning/ROADMAP.md
  - .github/workflows/deploy-harp.yml
  - docs/install-check.md
  - docs/testing.md
  - docs/performance.md
  - store/media/README.md
autonomous: false
requirements: [PKG-05]
must_haves:
  truths:
    - "Hinter jeder screenshot-Adresse beider info.xml liegt wirklich ein Bild, und das ist geprueft und nicht angenommen"
    - "Der NC-35-Rest aus E-H1 ist geschlossen, bevor die Phase abgenommen wird: der tolerate-failure-Schalter ist nach seiner eigenen Aufhebungsbedingung entschieden"
    - "Kriterium 3 der ROADMAP sagt das, was diese Phase wirklich abgedeckt hat, in der Fassung, die aus E-H5 folgt"
    - "Die Gastnutzer-Probe ist auf der frischen Instanz gefahren, und ihr Ergebnis steht in docs/testing.md"
    - "Der Owner hat auf einer frisch installierten Nextcloud ueber den Store-Weg selbst gesucht und die Phase danach abgenommen (D-H5)"
    - "APPSTORE_TOKEN ist rotiert, bevor irgendetwas eingereicht wird"
    - "Die Box ist abgebaut, mit Nichtexistenz-Pruefung fuer Instanz, Volumen und Security Group, und erst nach ausdruecklicher Freigabe (D-H3)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Kriterium 3 in der Fassung, die aus E-H5 folgt, und die Plaene dieser Phase als erledigt"
      contains: "06.1"
    - path: "docs/install-check.md"
      provides: "das Protokoll der Sichtprobe: Instanz, Weg, Fassungen, was der Owner gesucht und gefunden hat"
      contains: "Sichtprobe"
    - path: "docs/performance.md"
      provides: "der Abbau der Box mit Endkosten und den drei Nichtexistenz-Feststellungen"
      contains: "Abbau"
  key_links:
    - from: "docs/install-check.md"
      to: ".github/workflows/release.yml"
      via: "die Sichtprobe installiert die Archive des Probelaufs, nicht den Arbeitsbaum"
      pattern: "release"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-ENTSCHEIDE.md"
      via: "die Fassung von Kriterium 3 nennt die Entscheidung E-H5, aus der sie folgt"
      pattern: "E-H5"
---

<objective>
Der Owner sieht sich das Produkt so an, wie ein Fremder es bekommt, nimmt die
Phase ab, rotiert das Store-Token und gibt die Box frei. Danach, und erst danach,
beginnt Plan 06-12.

Purpose: D-H5 ist das Abnahmegate dieser Phase: eine Sichtprobe auf einer frisch
installierten Nextcloud über den Store-Weg, aus den Release-Artefakten, nicht aus
dem Dev-Compose. Alles davor waren Tests, die Maschinen fahren. Dieser Schritt
ist der einzige, den keine Maschine ersetzt, weil er die Frage stellt, die kein
Gate stellt: sieht das aus wie etwas, das man installieren will, und tut es, was
es verspricht.

Drei Reste hängen daran und werden vorher geschlossen. Erstens die
Bildadressen: `test_store_metadata.py` sagt seit Plan 06.1-13, dass hinter jeder
`screenshot`-Adresse ein Bild liegen muss, und prüft es bewusst ohne Netzzugriff;
die Live-Bestätigung wurde hierher verschoben. Zweitens der NC-35-Rest: der
`tolerate-failure`-Schalter in der Matrix trägt seine Aufhebungsbedingung
wörtlich im Workflow ("as soon as Nextcloud 35 is generally available"), Nextcloud
35 wird am 16.09.2026 final, und eine Phase, die vorher abgenommen wird, ohne den
Schalter zu entscheiden, lässt eine Zusage über eine Fassung offen, die dann
existiert (Fallstrick 9). Drittens Kriterium 3 der ROADMAP: es verspricht AIO und
docker-compose, NC 32 bis 35, amd64 und arm64; was davon wirklich gefahren wurde,
steht nach E-H5 fest und die ROADMAP sagt es dann auch.

Zuletzt zwei Dinge, die nur der Owner tun kann: `APPSTORE_TOKEN` rotieren, weil
das Token einmal im Gespräch stand und vor der ersten Einreichung nicht mehr im
Umlauf sein darf (Pflichtpunkt 8), und die Box freigeben. Der Abbau kommt mit
Nichtexistenz-Prüfung, weil eine vergessene Instanz still Geld kostet und ein
vergessenes Volumen den Bestand einer Messreihe enthält.

Output: die geschlossenen Reste, das Protokoll der Sichtprobe, die Abnahme, das
rotierte Token und eine Box, die nachweislich nicht mehr existiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-CONTEXT.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-ENTSCHEIDE.md

<interfaces>
Die Bildadressen (backend/appinfo/info.xml Z. 202-203, php/appinfo/info.xml
Z. 210-212), alle in der Form
  https://raw.githubusercontent.com/street1983nk/nextcloud-search/main/store/media/<datei>.png
Drei Dateien: header.png, screenshot-admin.png, screenshot-search.png.
Die Adressen zeigen auf den Zweig main, also traegt sie jeder Stand, der dort
liegt; die Live-Bestaetigung gehoert deshalb hinter den Merge dieser Phase.
test_store_metadata.py prueft sie bewusst OHNE Netz (ein Gate, das eine fremde
Seite braucht, faerbt den Bau rot, wenn jemand anderes sie umbaut); die
Bestaetigung hier ist einmalig und von Hand.

Der NC-35-Rest (E-H1, 06.1-09):
  .github/workflows/deploy-harp.yml fuehrt die Matrix stable32 bis stable35 und
  einen tolerate-failure-Schalter fuer die neueste Fassung, dessen
  Aufhebungsbedingung woertlich im Workflow steht. Nextcloud 35 wird am
  16.09.2026 final. E-H1 enthaelt die Antwort des Owners auf die Zusatzfrage, ob
  der Schalter danach umgelegt wird.

Kriterium 3 der ROADMAP im heutigen Wortlaut:
  "Eine frische Nextcloud (AIO und docker-compose, NC 32 bis 35, amd64 und arm64)
  installiert beide Apps aus den Release-Artefakten und findet ohne Handgriff
  Inhalte, und die Deinstallation raeumt auf"
  Was davon gefahren wurde, steht in docs/install-check.md (amd64, Plan 06.1-16)
  und gegebenenfalls im arm64-Abschnitt aus Plan 06.1-18. Die Fassung folgt aus
  E-H5 und wird hier vollzogen, nicht neu entschieden.

Die Release-Archive fuer die Sichtprobe:
  .github/workflows/release.yml hat zwei Wege hinein: ein Tag v<major>.<minor>.<patch>
  (das echte Release) und workflow_dispatch (der Probelauf). Der Probelauf baut,
  signiert und laedt die Artefakte hoch und erzeugt KEIN Release, solange der
  Schalter create_release auf seiner Vorgabe false steht (T-05-77). Fuer die
  Sichtprobe wird der Probelauf gefahren; diese Phase taggt nicht und
  veroeffentlicht nichts, das ist Sache von Plan 06-12.

Die Sichtprobe-Instanz:
  lokale WSL2-Instanz, Port 8090. Port 8080 gehoert der MCP-Sitzung und wird
  nicht angefasst. Der Installationsweg ist scripts/dev/aio_install_check.sh aus
  Plan 06.1-16, also derselbe Fahrplan, den schon ein Lauf gegangen ist.

Die Gastnutzer-Probe:
  scripts/dev/guest_parity.sh aus Plan 06.1-15, ausdruecklich nicht in CI (D-22).
  Sie faehrt hier auf der frischen Instanz, und ihr Ergebnis loest den Zustand
  "steht aus" in docs/testing.md ab. Annahme A8: laeuft die guests-App auf der
  Fassung der Instanz nicht, faehrt die Probe auf NC 34 und die Fassung wird
  benannt.

Die Box (~/.findling-loadtest/box.env, Zugangsdaten C:/Users/Student/.findling-aws.env):
  Instanz i-06b1d913f5c6f669b, Volumen vol-04c5b59fe9417babd,
  Security Group sg-0e782f5233d73a847.
  Abbau ueber scripts/ops/aws_box.sh destroy, das nach Plan 06.1-18 auch stop und
  start kennt. Das Skript entfernt die Zustandsdatei am Ende; die
  Nichtexistenz-Pruefung fuer alle drei Betriebsmittel gehoert davor.
  Der DNS-Eintrag loadtest.infranode.dev wird vom Orchestrator entfernt.

APPSTORE_TOKEN:
  vorhanden, vor der ersten Einreichung zu rotieren (Pflichtpunkt 8,
  06.1-RESEARCH Environment Availability und Security Domain: "APPSTORE_TOKEN im
  Umlauf vor der Einreichung, Elevation of Privilege, Offen"). Owner-Aufgabe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die drei Reste schliessen und die Sichtprobe vorbereiten</name>
  <files>.github/workflows/deploy-harp.yml, .planning/ROADMAP.md, store/media/README.md, docs/testing.md, docs/install-check.md</files>
  <read_first>
    - backend/appinfo/info.xml und php/appinfo/info.xml, die screenshot-Elemente samt der Begruendung daneben
    - backend/tests/test_store_metadata.py, der Modulkopf und die Bildpruefungen aus Plan 06.1-13 (was sie bewusst nicht prueft und warum)
    - .github/workflows/deploy-harp.yml, die Matrix und der tolerate-failure-Schalter samt seiner Aufhebungsbedingung im Wortlaut
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-ENTSCHEIDE.md, E-H1 (Zusatzfrage zum Schalter) und E-H5 (arm64 und AIO)
    - docs/install-check.md vollstaendig (der amd64-Lauf aus 06.1-16 und gegebenenfalls der arm64-Abschnitt aus 06.1-18)
    - .planning/ROADMAP.md, der Abschnitt Phase 06.1 mit den fuenf Erfolgskriterien und der Planliste
    - scripts/dev/guest_parity.sh und scripts/dev/aio_install_check.sh, jeweils die Hilfeausgabe und die Argumente
  </read_first>
  <action>
    Erstens die Bildadressen. Jede der Adressen aus beiden info.xml wird einmal
    von Hand abgerufen, nachdem der Stand dieser Phase auf main liegt. Geprueft
    werden drei Dinge je Adresse: Statuscode 200, ein Bild-Inhaltstyp, und eine
    Groesse, die zu der in store/media/README.md gefuehrten passt. Das Ergebnis
    kommt als Zeile je Bild nach store/media/README.md, mit Datum. Kein
    Netzzugriff wandert dabei in ein Gate; test_store_metadata.py bleibt
    unveraendert, und der Kommentar dort sagt weiterhin, warum.

    Zweitens der NC-35-Rest. Die Aufhebungsbedingung des tolerate-failure-Schalters
    wird gegen den Kalender und gegen E-H1 gehalten. Ist Nextcloud 35 final und
    hat E-H1 das Umlegen entschieden, wird der Schalter umgelegt und die Matrix
    faehrt stable35 ohne Nachsicht; der Lauf muss dann gruen sein, und ein roter
    Lauf ist ein Befund und kein Grund, den Schalter zurueckzudrehen. Ist NC 35
    noch nicht final oder hat E-H1 anders entschieden, bleibt der Schalter, und
    die Begruendung mitsamt dem Datum, an dem die Bedingung erneut zu pruefen ist,
    steht im Workflow neben ihm. Was nicht passiert: den Schalter stehen lassen,
    ohne die Frage zu beantworten.

    Drittens Kriterium 3 der ROADMAP. Es bekommt die Fassung, die aus E-H5 und
    aus docs/install-check.md folgt: was gefahren wurde, steht als Zusage; was
    nicht gefahren wurde, steht als benannte Nichtabdeckung mit dem Verweis auf
    den Abschnitt in docs/install-check.md. Die urspruengliche Fassung bleibt als
    durchgestrichene Zeile oder als Fussnote erkennbar, damit sichtbar bleibt, dass
    hier etwas kleiner geworden ist und nicht immer schon so stand. Im selben
    Zug wird die Planliste der Phase in der ROADMAP auf die neunzehn Plaene
    gesetzt und abgehakt.

    Viertens die Vorbereitung der Sichtprobe:
      - Den Probelauf von release.yml ueber workflow_dispatch fahren, mit
        create_release auf false. Die beiden Archive werden als Artefakte
        heruntergeladen; ihre Pruefsummen kommen ins Protokoll.
      - Eine frische Nextcloud auf Port 8090 aufsetzen, Port 8080 bleibt
        unberuehrt.
      - scripts/dev/aio_install_check.sh gegen diese Instanz fahren, mit den
        Archiven des Probelaufs.
      - scripts/dev/guest_parity.sh gegen dieselbe Instanz fahren; das Ergebnis
        loest in docs/testing.md den Zustand "steht aus" ab. Faellt Annahme A8,
        faehrt die Probe auf NC 34 und die Fassung steht dabei.
      - Ein paar deutsche Dokumente hochladen, darunter einen Scan, damit der
        Owner etwas zu suchen hat, das nicht aus dem Testkorpus stammt.
      - Das Protokoll kommt nach docs/install-check.md, in den Abschnitt
        "Sichtprobe", mit Instanz, Fassungen, Archivpruefsummen und dem Zustand,
        in dem der Owner die Instanz vorfindet.

    Die Instanz bleibt danach laufen, bis der Owner sie gesehen hat.

    Deutsch mit echten Umlauten in der Prosa, Workflows und Kommentare englisch.
    Keine Gedankenstriche.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "import pathlib; m=pathlib.Path('store/media/README.md').read_text(encoding='utf-8'); t=pathlib.Path('docs/testing.md').read_text(encoding='utf-8'); i=pathlib.Path('docs/install-check.md').read_text(encoding='utf-8'); r=pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); assert '200' in m; assert 'steht aus' not in t; assert 'Sichtprobe' in i; assert 'E-H5' in r; assert all(c not in s for s in (m,t,i) for c in ('—','–')); print('ok')" &amp;&amp; cd backend &amp;&amp; uv run python -m pytest tests/test_store_metadata.py tests/test_workflow_pins.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Jede der Bildadressen aus beiden info.xml ist einmal abgerufen; Statuscode, Inhaltstyp und Groesse stehen je Bild mit Datum in store/media/README.md.
    - test_store_metadata.py ist unveraendert und braucht weiterhin kein Netz.
    - Der tolerate-failure-Schalter ist entschieden: umgelegt und der Lauf gruen, oder mit Begruendung und Wiedervorlagedatum stehen geblieben.
    - Kriterium 3 der ROADMAP steht in der Fassung aus E-H5, mit Verweis auf docs/install-check.md; die urspruengliche Fassung bleibt erkennbar.
    - Die Planliste der Phase in der ROADMAP nennt alle neunzehn Plaene.
    - Der Probelauf von release.yml ist gefahren, ohne ein Release zu erzeugen; beide Archivpruefsummen stehen im Protokoll.
    - Die frische Instanz laeuft auf Port 8090, beide Haelften sind aus den Archiven installiert, und Port 8080 ist unberuehrt.
    - Die Gastnutzer-Probe ist gefahren, ihr Ergebnis steht in docs/testing.md, und "steht aus" kommt dort nicht mehr vor.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Die drei Reste sind geschlossen, und auf Port 8090 laeuft eine Nextcloud, die Findling ueber den Store-Weg bekommen hat und auf die der Owner nur noch schauen muss.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Die Sichtprobe des Owners und die Abnahme der Phase</name>
  <what-built>
    Auf http://localhost:8090 laeuft eine Nextcloud, die Findling noch nie gesehen
    hatte. Beide Haelften sind aus den Archiven des Release-Probelaufs
    installiert, ueber denselben Weg, den ein Fremder aus dem Store geht; aus dem
    Arbeitsbaum stammt nichts. Es wurde nichts eingestellt: kein Suchbackend, kein
    Index, kein Modell, kein Handgriff. Im Nutzerordner liegen deutsche
    Dokumente, darunter ein Scan.

    Die Phase hat davor achtzehn Plaene gefahren: die geteilte Engine mit
    Nachmessung auf der ARM-Box, Fehler- und Randpfade, Rechte-Grenzen samt
    Gastnutzer, Betriebsuebergaenge, Ressourcengrenzen, die Fremdinstallation aus
    Release-Artefakten, die Store-Formalien und die drei Audits.
  </what-built>
  <how-to-verify>
    1. In der gewoehnlichen Suche von Nextcloud nach einem Wort suchen, das nur im
       TEXT eines Dokuments steht und in keinem Dateinamen. Kommt eine Gruppe mit
       Inhaltstreffern, und steht darin ein Auszug aus dem Text?
    2. Nach einem Wort suchen, das nur auf dem Scan steht. Findet die OCR es?
    3. Ein eigenes Dokument hochladen und nach ein paar Minuten danach suchen.
       Ohne Handgriff gefunden?
    4. Die Verwaltungsseite ansehen: sagt sie verstaendlich, was der Container
       gerade tut, wie viel er geschafft hat und was er nicht konnte?
    5. Den Messbericht der Nachmessung ansehen
       (docs/measurements/2026-09-nachmessung-m7g/) und die Store-Aussage in
       README.md: traegt die Zahl, die dort steht, und ist die alte als Vergleich
       erkennbar?
    6. docs/install-check.md, Abschnitt "Was nicht abgedeckt ist": ist jede
       Nichtabdeckung so formuliert, dass Sie sie einem Nutzer gegenueber
       vertreten wuerden?
    7. Die drei Auditberichte ueberfliegen: ist ein Befund darunter, den Sie
       anders bewertet haetten?
    8. Abnahme: "haertung abgenommen". Erst danach beginnt Plan 06-12, und erst
       danach wird die Box freigegeben.
    9. Falls etwas nicht traegt: bitte sagen, was nachgebessert wird und ob dafuer
       ein eigener Plan noetig ist. Eine Nachbesserung nach der Abnahme waere von
       der Abnahme nicht gedeckt.
  </how-to-verify>
  <resume-signal>"haertung abgenommen" oder die Liste der Nachbesserungen</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: APPSTORE_TOKEN rotieren und die Box abbauen</name>
  <what-built>
    Die Phase ist abgenommen. Zwei Dinge bleiben, die nur der Owner tun kann, und
    beide gehoeren vor die Abgabe.
  </what-built>
  <how-to-verify>
    1. **APPSTORE_TOKEN rotieren.** Das Token stand einmal im Gespraech und ist
       damit im Umlauf; vor der ersten Einreichung gehoert es ersetzt
       (Pflichtpunkt 8). Weg: auf apps.nextcloud.com im Konto ein neues Token
       erzeugen, das alte widerrufen, das neue als GitHub-Secret APPSTORE_TOKEN
       hinterlegen. Das alte Token wird widerrufen und nicht nur ersetzt: ein
       ersetztes Token, das noch gilt, ist kein rotiertes Token.
       Bitte danach bestaetigen, dass das alte widerrufen ist. Das Token selbst
       wird nirgendwo hingeschrieben, auch nicht in eine Zusammenfassung.
    2. **Box freigeben.** Die AWS-ARM-Box i-06b1d913f5c6f669b steht angehalten und
       ist vollstaendig ausgewertet: Nachmessung, Endungsvergleich und
       gegebenenfalls der arm64-Lauf sind gefahren, die Rohdaten liegen im Repo.
       Angehalten kostet sie rund 0,31 USD je Tag. Nach dem Abbau ist der Bestand
       auf dem Volumen unwiederbringlich weg, also auch state.db und der Korpus
       aus 50.000 Dateien; ein neuer Volllauf waere danach ein neuer Aufbau von
       rund 19 Stunden.
       Bitte freigeben mit "box abbauen", oder sagen, dass sie fuer 06-12 noch
       stehen bleiben soll.
    3. Nach der Freigabe wird abgebaut: `scripts/ops/aws_box.sh destroy` mit den
       Zugangsdaten aus C:/Users/Student/.findling-aws.env, danach die
       Nichtexistenz-Pruefung einzeln fuer Instanz i-06b1d913f5c6f669b, Volumen
       vol-04c5b59fe9417babd und Security Group sg-0e782f5233d73a847. Die drei
       Feststellungen und die Endkosten kommen nach docs/performance.md, box.env
       wird ein letztes Mal fortgeschrieben, und der Orchestrator entfernt den
       A-Record loadtest.infranode.dev.
  </how-to-verify>
  <resume-signal>"token rotiert" plus "box abbauen" oder "box bleibt stehen"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository zum Store-Eintrag | Der Store speichert keine Bilder, nur Adressen; eine tote Adresse ist ein leerer Rahmen, der jede Schemapruefung besteht |
| Arbeitsbaum zur Sichtprobe | Was der Owner sieht, muss aus den Release-Artefakten kommen, sonst prueft er etwas, das niemand bekommt |
| Altes Token zum Konto | Ein ersetztes, aber nicht widerrufenes Token ist ein zweiter gueltiger Schluessel |
| Betriebsmittel zur Zustandsdatei | Das Skript loescht seine Zustandsdatei am Ende; danach ist eine vergessene Instanz nur noch in der Rechnung sichtbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-80 | Repudiation | Eine screenshot-Adresse zeigt ins Leere und der Store-Eintrag hat einen leeren Rahmen | mitigate | Einmalige Live-Bestaetigung von Hand mit Status, Inhaltstyp und Groesse je Bild; kein Netzzugriff im Gate |
| T-06.1-81 | Tampering | Die Sichtprobe laeuft gegen den Arbeitsbaum statt gegen die Release-Archive | mitigate | Archive aus dem Probelauf mit Pruefsummen im Protokoll; Installation ueber denselben Fahrplan wie 06.1-16 |
| T-06.1-82 | Elevation of Privilege | APPSTORE_TOKEN bleibt im Umlauf, waehrend die Einreichung laeuft | mitigate | Rotation als Checkpoint vor 06-12; das alte Token wird ausdruecklich widerrufen und die Bestaetigung eingeholt |
| T-06.1-83 | Repudiation | Ein Erfolgskriterium der ROADMAP verspricht, was diese Phase nicht gefahren hat | mitigate | Kriterium 3 wird nach E-H5 gefasst, mit Verweis auf docs/install-check.md; die alte Fassung bleibt erkennbar |
| T-06.1-84 | Denial of Service | Ein Betriebsmittel ueberlebt den Abbau und kostet still weiter | mitigate | Nichtexistenz-Pruefung einzeln fuer Instanz, Volumen und Security Group, vor dem Loeschen der Zustandsdatei; Endkosten in docs/performance.md |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Pakete in diesem Plan; installiert werden ausschliesslich die signierten Release-Archive |
</threat_model>

<verification>
- Jede der fuenf screenshot-Adressen ist mit Status, Inhaltstyp und Groesse bestaetigt, Ergebnis in store/media/README.md
- Der tolerate-failure-Schalter ist entschieden, und deploy-harp.yml ist gruen
- Kriterium 3 der ROADMAP steht in der Fassung aus E-H5, die Planliste nennt neunzehn Plaene
- Die Gastnutzer-Probe ist gefahren, docs/testing.md fuehrt ihr Ergebnis statt "steht aus"
- Der Owner hat die Phase abgenommen; das Protokoll der Sichtprobe steht in docs/install-check.md
- APPSTORE_TOKEN ist rotiert und das alte widerrufen (Bestaetigung des Owners, kein Token in einem Artefakt)
- Die Box ist abgebaut, die drei Nichtexistenz-Feststellungen und die Endkosten stehen in docs/performance.md
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien
</verification>

<success_criteria>
Der Owner hat auf einer frisch installierten Nextcloud ueber den Store-Weg selbst
gesucht und gefunden, die Phase abgenommen, das Store-Token rotiert und die Box
freigegeben; die Reste aus 06.1-13 und E-H1 sind geschlossen, die ROADMAP sagt,
was wirklich abgedeckt ist, und Plan 06-12 darf beginnen.
</success_criteria>

<output>
Create `.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-19-SUMMARY.md` when done
</output>
