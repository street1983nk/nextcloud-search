---
phase: 06.1-launch-haertung-vor-der-store-abgabe
plan: 15
type: execute
wave: 5
# Welle 5 ist zugleich das aus depends_on folgende Minimum (06.1-11, -13, -14, -16 liegen
# in Welle 4): die Gate-Landschaft kann erst vollstaendig sein, wenn die Gate-bauenden
# Plaene 01, 04 bis 09 und 11 bis 16 fertig sind.
depends_on: [06.1-07, 06.1-11, 06.1-13, 06.1-14, 06.1-16]
files_modified:
  - scripts/dev/guest_parity.sh
  - backend/tests/test_guest_parity.py
  - docs/testing.md
  - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-PLAN.md
autonomous: true
requirements: [PKG-05]
must_haves:
  truths:
    - "Die Gastnutzer-Probe ist ein Werkzeug und keine Erinnerung: sie laeuft als Skript, mit demselben Vergleich wie der CI-Job, und ihr Ergebnis ist eine Datei"
    - "Die guests-App bleibt aus der CI-Matrix draussen, und das ist mechanisch gesichert und nicht nur zugesagt (D-22)"
    - "Die Probe setzt den Trefferdeckel der Instanz hoch und fragt mit ausdruecklichem limit; ohne beides vergliche sie zwei gekuerzte Listen"
    - "docs/testing.md fuehrt keine Luecke mehr, die es nicht mehr gibt: der Abschnitt The gap ist durch die Abbildung der Verhaltensweisen auf ihre Tests ersetzt"
    - "Plan 05-19 ist aufgeloest und nicht halb geoeffnet: seine zwei hierher gehoerenden Teile tragen ihren neuen Ort, der Tag bleibt bei 06-12"
  artifacts:
    - path: "scripts/dev/guest_parity.sh"
      provides: "die Gastnutzer-Probe als fahrbares Skript, ueber occ guests:add und scripts/ci/parity_diff.py"
      contains: "parity_diff"
    - path: "backend/tests/test_guest_parity.py"
      provides: "die mechanische Zusicherung zu D-22: keine guests-Abhaengigkeit in einem Workflow"
      contains: "guests"
    - path: "docs/testing.md"
      provides: "die Gate-Landschaft nach dieser Phase, jedes Gate mit dem, was es nicht beweist"
      contains: "search-parity"
  key_links:
    - from: "scripts/dev/guest_parity.sh"
      to: "scripts/ci/parity_diff.py"
      via: "die Probe vergleicht mit demselben Werkzeug wie der CI-Job, nicht mit einem zweiten Rechenweg"
      pattern: "parity_diff"
    - from: "backend/tests/test_guest_parity.py"
      to: ".github/workflows"
      via: "ein Zaehler ueber alle Workflows wird rot, wenn die guests-App in die CI-Matrix wandert"
      pattern: "guests"
---

<objective>
Der letzte offene Rechte-Fall bekommt eine Probe, die man fahren kann, und die
Datei, die sagt was bewiesen ist, sagt es wieder richtig.

Purpose: Pflichtpunkt 3 zaehlt Gastnutzer auf, und genau dieser Fall ist der
einzige der Liste ohne Nachweis. Er hat auch schon einen Ort: Plan 05-19 ist der
einzige Plan der Phase 5, der nie ausgeführt wurde, und er enthält drei Dinge,
von denen zwei hierher gehören (Gastnutzer-Probe aus D-22, Gate-Landschaft in
`docs/testing.md`) und eines ausdrücklich nicht (der Tag v1.0.0, der bei 06-12
bleibt). Die Recherche empfiehlt genau das, statt einen Plan aus einer
abgeschlossenen Phase halb wieder aufzumachen (Open Question 6).

D-22 ist dabei die Einschränkung, die den Zuschnitt bestimmt: Szenario 6 wird
doppelt belegt, in CI als gruppenloser Minimal-Nutzer (seit 05-09 vorhanden) und
zusätzlich als echter Gastnutzer über die `guests`-App, und die `guests`-App
kommt ausdrücklich NICHT in die CI-Matrix. Eine manuelle Probe ohne Werkzeug
wäre allerdings genau das "manuell geprüft, ok" ohne Artefakt, das diese Phase
verbietet. Also entsteht hier das Werkzeug samt seiner Hausordnung, und gefahren
wird es in Plan 06.1-19 auf der frischen Instanz der Owner-Sichtprobe.

Der zweite Teil ist `docs/testing.md`. Die Datei heißt heute "What is tested, and
the one gap that is left" und führt einen Abschnitt "The gap" mit zwölf
Verhaltensweisen, die es seit Plan 05-16 nicht mehr gibt. Eine Datei, die eine
Lücke behauptet, die geschlossen ist, ist schlechter als keine.

Output: die Gastnutzer-Probe als Skript mit Gate, die Gate-Landschaft dieser
Phase, und ein aufgelöster Plan 05-19.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-CONTEXT.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md

<interfaces>
D-22 im Wortlaut (aus 05-19-PLAN.md):
  Szenario 6 wird DOPPELT belegt. Als CI-Szenario ein gruppenloser Minimal-Nutzer
  (keine Gruppen, kein Team-Folder-Zugang, genau ein empfangener
  View-only-Share), seit Plan 05-09 im Job search-parity. Zusaetzlich ein
  Gastnutzer ueber die guests-App als manuelle Probe vor der Einreichung, und
  ausdruecklich KEINE guests-Abhaengigkeit in der CI-Matrix.

Der Weg fuer den Gastnutzer (06.1-RESEARCH Teil 3.2):
  occ guests:add [--uid UID] [--password-from-env] <created-by> <email>
  guests 4.9.0 vom 05.08.2026, belegt fuer NC 32, 33 und 34; fuer NC 35 offen
  (Annahme A8: die Probe faehrt dann auf 34 und die Fassung wird benannt).

Die Vergleichsprozedur, unveraendert aus dem CI-Job:
  scripts/ci/parity_diff.py, symmetrischer Vergleich der fileid-Mengen zwischen
  der nativen Suche und Findling, verpasste Treffer sind Fehlschlag.
  Vorbedingung vor dem ersten Aufruf:
    occ config:app:set core unified_search_max_results_per_request --value=100
  Ohne sie und ohne ausdruecklichen limit-Parameter vergleicht die Probe zwei auf
  fuenf gekuerzte Listen und sagt nichts (Fallstrick 6).

Was docs/testing.md heute fuehrt:
  Zeile 1   "What is tested, and the one gap that is left"
  Zeile 153 "## The gap" mit den zwoelf Verhaltensweisen aus der Zeit vor 05-16
  Zeile 208 "## What closes it"
  Zeile 130 "## The gates of phase 6, and the boundary of each one" (das Muster,
            in dem die neuen Gates dieser Phase beschrieben werden)

Was diese Phase an Gates und Jobs baut, und was deshalb in die Landschaft gehoert:
  01 resilience.yml Gegenprobe zu DI-05-36 mit Negativkontrolle
  04 RSS-Ratsche um die erste Suche
  05 backend/tests/test_extract_edge_paths.py
  06 Korpusdateien mit Verdikt-Tabelle, readonly-gate zieht sie mit
  07 drei neue Paritaets-Szenarien plus die Datei, die sich waehrend des Lesens aendert
  08 php/tests/Unit/GroupEventListenerTest.php und das Szenario Gruppenbeitritt
  09 backend/tests/test_lockstep_versions.py mit dem Versionsfenster, Drift-Schritt
  11 Loop-Image-Plattendrill, beide Seiten der Budget-Kante, scripts/ops/search_load.py
  12 Installationsweg aus den Release-Artefakten in deploy-harp.yml
  13 Store-Validierungspfad von Hand, Bildgroessen, Kategorien, Vokabularregel
  14 backend/src/findling/extract/ocr_quality.py und die DACH-Messung
  16 scripts/dev/aio_install_check.sh und docs/install-check.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Die Gastnutzer-Probe als Werkzeug, mit dem Gate gegen D-22</name>
  <files>scripts/dev/guest_parity.sh, backend/tests/test_guest_parity.py</files>
  <read_first>
    - .github/workflows/integration.yml, der Job search-parity vollstaendig: die vier Konten, die Marker-Fixtures, die Vorbedingung unified_search_max_results_per_request, die Hilfsfunktion, die je Szenario dieselbe curl-Form fuer beide Provider faehrt, und die Aufrufform von parity_diff.py mit --scenario
    - scripts/ci/parity_diff.py vollstaendig (Argumente, Ausgabeform, Rueckgabecode, was als Fehlschlag zaehlt)
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-PLAN.md, Task 1 (die Probe, wie sie dort beschrieben ist, samt D-22 im Wortlaut)
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md Teil 3.2 (die guests-Zeile), Environment Availability (die guests-Zeile und A8), Open Question 6
    - backend/tests/test_ops_scripts.py (das Muster einer Hausordnung ueber ein Skript, mechanisch geprueft)
    - scripts/dev/register-exapp.sh (die Form der Skripte unter scripts/dev: Argumente, Fehlerbehandlung, set -euo pipefail)
  </read_first>
  <behavior>
    - Das Skript nimmt Adresse und Verwalterzugang als Argumente oder aus der Umgebung und legt einen Gastnutzer ueber occ guests:add an.
    - Es setzt vor dem ersten Vergleich unified_search_max_results_per_request auf 100 und stellt den alten Wert am Ende wieder her.
    - Es teilt dem Gastnutzer genau eine Datei mit bekanntem Marker und fragt danach beide Provider mit demselben Begriff und ausdruecklichem limit.
    - Es reicht beide Antworten an scripts/ci/parity_diff.py und gibt dessen Urteil unveraendert weiter.
    - Es schreibt ein Protokoll mit Instanzfassung, guests-Fassung, Kontoname, Begriff, beiden fileid-Mengen und dem Urteil.
    - Es raeumt auf: Gastnutzer und Freigabe verschwinden, auch wenn ein Schritt scheitert.
    - Es kommt in keinem Workflow vor, und ein Test faerbt rot, wenn die guests-App in einem Workflow auftaucht.
  </behavior>
  <action>
    Das Skript entsteht unter scripts/dev, nicht unter scripts/ci: scripts/ci ist
    der Ort der Dinge, die CI faehrt, und dieses Werkzeug faehrt ausdruecklich
    niemand in CI (D-22). Der Kopf des Skripts sagt genau das, mit der
    Begruendung: die guests-App ist eine Fremdabhaengigkeit mit eigenem
    Fassungsfenster, und eine rote CI-Matrix wegen einer fremden App sagt nichts
    ueber Findling.

    Der Vergleich wird NICHT nachgebaut. Das Skript ruft parity_diff.py, weil
    zwei Rechenwege fuer dieselbe Frage frueher oder spaeter zwei Antworten
    geben. Die Aufrufform, das ausdrueckliche limit und die Vorbedingung des
    Trefferdeckels werden aus dem CI-Job uebernommen, nicht neu erfunden.

    Der Gastnutzer bekommt genau einen Inhalt, wie das CI-Szenario 6: ein
    empfangener Share mit einem eigenen Marker. Die erwartete Menge ist damit
    von der Groesse eins, und eine Abweichung in beide Richtungen ist ein Befund:
    fehlt der Treffer, sieht der Gast zu wenig; kommt ein zweiter, sieht er zu
    viel, und das waere der einzige Fall dieser Phase, der ein Sicherheitsbefund
    waere.

    Das Aufraeumen laeuft ueber ein trap-EXIT, weil eine abgebrochene Probe sonst
    ein Gastkonto auf einer Instanz stehen laesst, auf der gleich der Owner seine
    Sichtprobe macht.

    Die Zusicherungen kommen in backend/tests/test_guest_parity.py, in der Form
    von test_ops_scripts.py:
      1. Das Skript ruft parity_diff.py und baut keinen eigenen Mengenvergleich
         (kein sort/comm/diff ueber fileid-Listen im Skript).
      2. Es setzt unified_search_max_results_per_request und stellt es zurueck.
      3. Jeder Suchaufruf traegt einen ausdruecklichen limit-Parameter.
      4. Es hat ein trap-EXIT, das Gastkonto und Freigabe entfernt.
      5. D-22 mechanisch: in keiner Datei unter .github/workflows kommt guests
         als App vor. Dieser Fall wird einmal probeweise rot gefahren, indem die
         Zeichenkette in einer Kopie eingesetzt wird, und das Ergebnis steht in
         der Zusammenfassung; ein Gate, das nicht rot werden kann, ist keins.
      6. Kein Em-Dash, kein En-Dash, kein Wagenruecklauf.

    Der Zaehler aus 5 filtert Kommentarzeilen heraus, bevor er zaehlt (Muster
    grep -v '^#' vor grep -c), sonst faerbt der eigene Erklaertext eines
    Workflows das Gate rot.

    Skript, Bezeichner und Kommentare englisch. Das Protokoll deutsch. Keine
    Gedankenstriche.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -m pytest tests/test_guest_parity.py -q &amp;&amp; uv run ruff check . &amp;&amp; uv run pyright &amp;&amp; cd /c/Users/Student/nextcloud-search &amp;&amp; bash -n scripts/dev/guest_parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - Alle sieben Verhaltensweisen sind belegt: sechs als Zusicherung in test_guest_parity.py, die siebte durch `bash -n` plus die Hilfeausgabe des Skripts in der Zusammenfassung.
    - Der Probelauf mit der eingesetzten Zeichenkette hat das D-22-Gate rot gefaerbt, und das Ergebnis steht in der Zusammenfassung.
    - Der Zaehler ueber die Workflows filtert Kommentarzeilen, bevor er zaehlt.
    - Das Skript baut keinen zweiten Mengenvergleich; parity_diff.py ist der einzige Richter.
    - Die guests-App kommt in keinem Workflow vor, und die CI-Matrix ist unveraendert.
    - Kein Em-Dash (U+2014), kein En-Dash (U+2013) und kein Wagenruecklauf in den neuen Dateien.
  </acceptance_criteria>
  <done>Die Gastnutzer-Probe ist fahrbar, vergleicht mit demselben Richter wie CI, und die Zusage, dass sie CI nicht anfasst, ist ein Test statt eines Satzes.</done>
</task>

<task type="auto">
  <name>Task 2: Die Gate-Landschaft nach dieser Phase, und The gap verschwindet</name>
  <files>docs/testing.md</files>
  <read_first>
    - docs/testing.md vollstaendig, besonders Zeile 1 (die Ueberschrift), Abschnitt "The gates of phase 6, and the boundary of each one" (das Muster) und die Abschnitte "The gap" und "What closes it"
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-16-SUMMARY.md (welche der zwoelf Verhaltensweisen dort Tests bekommen haben)
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-PLAN.md, Task 2 (die dort beschriebene Form der Landschaft)
    - die SUMMARYs der Plaene 06.1-01, 06.1-04 bis 06.1-09 und 06.1-11 bis 06.1-16, jeweils nur der Abschnitt ueber die neuen Gates und was sie nicht beweisen
  </read_first>
  <action>
    Erstens der Abschnitt "The gap". Er wird nicht geloescht, sondern ersetzt:
    an seine Stelle kommt die Abbildung der zwoelf Verhaltensweisen auf die
    Tests, die sie seit 05-16 abdecken, je eine Zeile. Wo eine Verhaltensweise
    heute wirklich keinen Test hat, bleibt sie stehen, mit dem Grund. Der
    Unterschied zu heute ist, dass die Liste dann stimmt.

    Die Ueberschrift der Datei folgt derselben Wahrheit: "the one gap that is
    left" ist eine Aussage ueber einen Zustand, den es nicht mehr gibt.

    Zweitens ein neuer Abschnitt fuer diese Phase, in der Form des
    Phase-6-Abschnitts: je Gate eine Zeile mit dem, was es verhindert, und einer
    zweiten mit dem, was es NICHT beweist. Die zweite Zeile ist die wertvollere,
    weil sie verhindert, dass ein gruenes Gate spaeter als Beleg fuer etwas
    zitiert wird, das es nie gemessen hat. Die Liste steht in <interfaces>; wo
    eine SUMMARY einen Vorbehalt nennt, wird der Vorbehalt uebernommen und nicht
    geglaettet.

    Drittens die Gastnutzer-Probe: sie bekommt ihren eigenen kurzen Abschnitt mit
    der Aussage, dass sie ausdruecklich nicht in CI laeuft, mit der Begruendung
    aus D-22, mit ihrer Kommandozeile und mit dem Hinweis, dass ihr Ergebnis in
    Plan 06.1-19 entsteht. Solange sie nicht gefahren ist, steht dort "steht
    aus" und nicht "bestanden".

    Was dieser Task NICHT tut: Zahlen aus Messreihen wiederholen. Die Datei sagt,
    was geprueft wird; docs/performance.md sagt, was gemessen wurde.

    Englisch, wie die Datei heute. Keine Gedankenstriche.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "import pathlib; s=pathlib.Path('docs/testing.md').read_text(encoding='utf-8'); assert '## The gap' not in s, 'the stale gap section is still there'; assert 'guest' in s.lower() and 'search-parity' in s; assert '—' not in s and '–' not in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Der Abschnitt "The gap" ist durch die Abbildung der zwoelf Verhaltensweisen auf ihre Tests ersetzt; jede Zeile nennt entweder ihren Test oder den Grund, warum sie offen ist.
    - Die Ueberschrift der Datei behauptet keine Luecke mehr, die geschlossen ist.
    - Jedes Gate dieser Phase hat eine Zeile "was es verhindert" und eine Zeile "was es nicht beweist".
    - Die Gastnutzer-Probe steht mit ihrer Kommandozeile, mit der D-22-Begruendung und mit dem Zustand "steht aus".
    - Keine Messzahl aus docs/performance.md ist hier ein zweites Mal aufgeschrieben.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in der Datei.
  </acceptance_criteria>
  <done>Ein Fremder liest docs/testing.md und weiss danach, was belegt ist, was nicht, und woran das jeweils haengt.</done>
</task>

<task type="auto">
  <name>Task 3: Plan 05-19 aufloesen statt halb oeffnen</name>
  <files>.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-PLAN.md</files>
  <read_first>
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-PLAN.md vollstaendig (alle drei Tasks und die must_haves)
    - .planning/phases/06-semantische-suche/06-12-PLAN.md, Task 1 (die drei Optionen zum Tag v1.0.0, die dort und nur dort entschieden werden)
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md, Open Question 6
  </read_first>
  <action>
    An den Anfang von 05-19-PLAN.md kommt ein Auflösungsvermerk, deutsch, mit
    Datum, und er sagt drei Dinge:

      1. Dieser Plan wurde nie ausgefuehrt und wird nicht mehr ausgefuehrt.
      2. Wohin seine Teile gegangen sind, einzeln:
         - Task 1 (Gastnutzer-Probe, D-22): das Werkzeug in Plan 06.1-15, der
           Lauf in Plan 06.1-19.
         - Task 2 (Gate-Landschaft in docs/testing.md): Plan 06.1-15.
         - Task 3 (Abnahme und Tag v1.0.0, D-26): NICHT hierher. Der Tag gehoert
           zu Plan 06-12, dort steht er als Entscheidung mit drei Optionen, und
           diese Phase darf nicht taggen (CLAUDE.md, Owner-Regel 06.09.).
      3. Die must_haves des Plans gelten unveraendert weiter; sie sind jetzt in
         den genannten Plaenen zu erfuellen, nicht hier.

    Die Datei wird sonst nicht angefasst. Ein Plan aus einer abgeschlossenen
    Phase wird nicht umgeschrieben: er bekommt einen Vermerk, der sagt, was aus
    ihm geworden ist.

    Zusaetzlich: in 06.1-RESEARCH.md ist Open Question 6 bereits als (RESOLVED)
    markiert; dieser Task prueft nur, dass der Verweis dort auf dieselben
    Plannummern zeigt wie der Vermerk hier. Weichen sie ab, gilt der Vermerk und
    die Recherche wird nachgezogen.

    Deutsch mit echten Umlauten. Keine Gedankenstriche.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "import pathlib; p=pathlib.Path('.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-PLAN.md').read_text(encoding='utf-8'); r=pathlib.Path('.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md').read_text(encoding='utf-8'); assert '06.1-15' in p and '06.1-19' in p and '06-12' in p; assert 'RESOLVED' in r; assert '—' not in p and '–' not in p; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - 05-19-PLAN.md traegt einen datierten Aufloesungsvermerk an seinem Anfang.
    - Der Vermerk nennt fuer jeden der drei Tasks seinen neuen Ort oder ausdruecklich, dass er nicht hierher gehoert.
    - Der Tag v1.0.0 ist ausdruecklich als Sache von Plan 06-12 benannt, mit dem Verweis auf die Owner-Regel.
    - Der Rest der Datei ist unveraendert (`git diff` zeigt nur den eingefuegten Block).
    - Open Question 6 in 06.1-RESEARCH.md zeigt auf dieselben Plannummern.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in der Datei.
  </acceptance_criteria>
  <done>Es gibt keinen halb offenen Plan mehr aus Phase 5; jedes seiner Stuecke hat einen Ort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gastnutzer zur Instanz | Ein Gast ist der Nutzer mit den wenigsten Rechten; sieht er zu viel, ist das der einzige echte Rechtebefund dieser Phase |
| Fremde App zur CI-Matrix | Die guests-App hat ein eigenes Fassungsfenster; in der Matrix wuerde ihr Ausfall Findling rot faerben |
| Dokumentation zum Leser | Eine Datei, die eine geschlossene Luecke behauptet, erzeugt falsche Vorsicht und verdeckt die echten Luecken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-62 | Information Disclosure | Ein Gastnutzer findet Inhalte, die ihm nicht freigegeben sind | mitigate | Probe mit genau einem freigegebenen Marker; eine Menge groesser eins ist ein Befund und wird als Sicherheitsbefund gefuehrt |
| T-06.1-63 | Repudiation | Die Probe vergleicht zwei auf fuenf gekuerzte Listen und meldet gruen | mitigate | Trefferdeckel der Instanz wird gesetzt und zurueckgestellt, jeder Aufruf traegt ein ausdrueckliches limit, beides als Zusicherung |
| T-06.1-64 | Denial of Service | Die guests-App wandert in die CI-Matrix und faerbt sie fremdverschuldet rot | mitigate | Zaehler ueber alle Workflows mit Kommentarfilterung, einmal probeweise rot gefahren |
| T-06.1-65 | Repudiation | Ein gruenes Gate wird spaeter als Beleg fuer etwas zitiert, das es nie gemessen hat | mitigate | Je Gate eine Zeile "was es nicht beweist" in docs/testing.md |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neue Abhaengigkeit; das Skript ist POSIX-Shell plus occ, der Vergleich das vorhandene parity_diff.py |
</threat_model>

<verification>
- `cd backend && uv run python -m pytest tests/test_guest_parity.py -q` gruen, ruff, pyright und vulture gruen
- `bash -n scripts/dev/guest_parity.sh` ohne Befund, `--help` beschreibt Adresse, Zugang und Protokollpfad
- docs/testing.md fuehrt keinen Abschnitt "The gap" mehr und nennt jedes Gate dieser Phase mit seiner Grenze
- 05-19-PLAN.md traegt den Aufloesungsvermerk, Open Question 6 zeigt auf dieselben Nummern
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien
</verification>

<success_criteria>
Der Gastnutzer-Fall hat ein Werkzeug statt einer Erinnerung, die CI-Matrix bleibt
frei von einer fremden App und das ist ein Test, und die Datei, die sagt was
bewiesen ist, sagt es wieder richtig.
</success_criteria>

<output>
Create `.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-15-SUMMARY.md` when done
</output>
