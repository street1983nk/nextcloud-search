---
phase: 06.1-launch-haertung-vor-der-store-abgabe
plan: 18
type: execute
wave: 7
# Welle 7, allein und nach dem Audit: jede Anfahrt der Box kostet IP-, DNS- und
# Bewaffnungsarbeit, deshalb faehrt alles, was die Box braucht, in diesem einen Plan,
# und zwar auf dem Stand, der auch ausgeliefert wird.
depends_on: [06.1-04, 06.1-11, 06.1-16, 06.1-17]
files_modified:
  - scripts/ops/aws_box.sh
  - backend/tests/test_ops_scripts.py
  - docs/measurements/2026-09-nachmessung-m7g/
  - docs/performance.md
  - docs/embeddings.md
  - docs/install-check.md
  - README.md
autonomous: false
requirements: [SEM-01, SEM-03, PKG-05]
must_haves:
  truths:
    - "Die Store-Zahl ist nach dem Fix der zweiten Modellinstanz auf derselben Hardware und in derselben Form neu gemessen, und sie liegt unter der aus 06-11 (1.837,8 MB)"
    - "Die Store-Aussage steht in der Form D-H2: oom, oom_kill und oom_group_kill auf null, anon-Spitze unter 2,0 GB, max als ausgewiesene Kennzahl"
    - "Die alte Zahl bleibt als Vergleich stehen, in README.md und in docs/performance.md; ersetzt wird die Aussage, nicht die Geschichte"
    - "Die Nebenlaeufigkeit der Suchlast ist gemessen und danach als Zusage festgelegt, nicht vorher"
    - "Der Endungsvergleich der Verdikte gegen die Generator-Verteilung ist gefahren, und jede Abweichung hat einen Namen"
    - "Die Box wird ueber aws_box.sh angehalten und gestartet, nicht an dem Skript vorbei, das die Kosten ausrechnet"
    - "Die Box bleibt nach diesem Plan angehalten; der Abbau ist Sache von Plan 06.1-19 und braucht die Owner-Freigabe (D-H3)"
  artifacts:
    - path: "docs/measurements/2026-09-nachmessung-m7g/README.md"
      provides: "der Messbericht in der Form von 06-11: Umgebung, Beweis des Standes, Grundlast, Spitzen je Phase, memory.events, Suchlast, Endungsvergleich"
      contains: "memory.events"
    - path: "scripts/ops/aws_box.sh"
      provides: "die zwei fehlenden Unterbefehle stop und start, mit derselben Kostenrechnung wie die uebrigen"
      contains: "stop"
    - path: "docs/performance.md"
      provides: "die neue Store-Zahl in der D-H2-Form, mit der alten als Vergleichszeile daneben"
      contains: "oom_group_kill"
  key_links:
    - from: "docs/measurements/2026-09-nachmessung-m7g/README.md"
      to: "docs/measurements/2026-09-05-semantiklauf-m7g/README.md"
      via: "dieselbe Messform, damit die Zahlen vergleichbar sind statt nur nebeneinander zu stehen"
      pattern: "anon"
    - from: "README.md"
      to: "docs/measurements/2026-09-nachmessung-m7g/"
      via: "die Kernaussage nennt ihre Messreihe"
      pattern: "measurements"
    - from: "scripts/ops/aws_box.sh"
      to: "backend/tests/test_ops_scripts.py"
      via: "die Hausordnung von scripts/ops gilt auch fuer die zwei neuen Unterbefehle"
      pattern: "aws_box"
---

<objective>
Die Box fährt noch einmal an, ein einziges Mal, und trägt alles ab, was nur auf
echter Zielhardware zu klären ist: die neue Store-Zahl, die Nebenläufigkeit, der
Endungsvergleich, und was aus CI zurückgereicht wurde.

Purpose: Plan 06.1-02 hat die zweite Modellinstanz beseitigt, Plan 06.1-04 hat
die Regressionsratsche auf amd64 dafür gebaut. Beide zusammen können die eine
Zahl nicht liefern, die in den Store-Text kommt: die anon-Spitze auf 4 GB ARM.
Die Ratsche fängt die Rückkehr des Fehlers, sie setzt die Zusage nicht. Der
06-11-Bericht ist die Ausgangsmessung, und ein Nachlauf muss seine Form
wiederholen statt sie neu zu erfinden, sonst sind die Zahlen nicht vergleichbar
und der ganze Aufwand ist eine zweite Geschichte statt eines Vergleichs.

Zum Zuschnitt: der Plan trägt fünf Tasks, davon zwei Checkpoints (Freigabe vor
der Anfahrt, Abnahme danach) und drei Arbeitsschritte. Ein Split wurde geprüft
und verworfen, weil jede Trennung eine zweite Box-Anfahrt bedeutet (IP wechselt,
SG-22, DNS, Bewaffnung) und D-H3 die Anfahrten auf das Nötigste begrenzt. Die
SUMMARY wiederholt diese Begründung, damit niemand sie neu herleiten muss.

Jede Anfahrt der Box kostet IP-, SG-, DNS- und Bewaffnungsarbeit. Deshalb fährt
alles hier mit, was sie braucht: die Nebenläufigkeitsmessung, die Plan 06.1-11
ausdrücklich hierher gegeben hat, der Plattendrill aus demselben Plan, falls
Annahme A5 gefallen ist und der GitHub-Runner nicht mounten durfte, die
arm64-Fremdinstallation, falls E-H5 sie verlangt, und der Endungsvergleich der
Verdikte, der seit 06-11 offen steht und der nur solange möglich ist, wie das
Volumen mit `state.db` existiert. Er gehört deshalb vor den Abbau und nicht
danach.

D-H2 bekommt hier ihren Ort: die Store-Aussage steht auf den drei Schadenszählern
plus der anon-Spitze, und `max` wird als Kennzahl ausgewiesen statt verschwiegen.
Der 06-11-Lauf hat `max 2796` gemessen, weil der Dateicache des Index bei 1,5 bis
1,8 GB anon gegen die 2-GB-Grenze anlag; diese Zahl verschwindet nicht durch
einen Fix an der Modellinstanz, und der Bericht wird das sagen.

Die Box bleibt danach angehalten. Der Abbau ist die letzte Aufgabe der Phase und
braucht die ausdrückliche Freigabe des Owners (D-H3); er steht in Plan 06.1-19.

Output: der Messbericht, die neue Store-Zahl an ihren drei Stellen, der
Endungsvergleich, und zwei Unterbefehle, die das Box-Skript bisher nicht hatte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-CONTEXT.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md
@.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-ENTSCHEIDE.md
@.planning/phases/06-semantische-suche/06-11-SUMMARY.md

<interfaces>
Der Zustand der Box (~/.findling-loadtest/box.env, ausserhalb des Repos, enthaelt
keine Geheimnisse; Zugangsdaten in C:/Users/Student/.findling-aws.env):
  Instanz i-06b1d913f5c6f669b, m7g.large, 4 GB, eu-central-1
  Volumen vol-04c5b59fe9417babd, 60 GB gp3, /mnt/findling, ext4, per UUID in der
    fstab mit nofail; Docker data-root und containerd-Wurzel liegen darauf
  Security Group sg-0e782f5233d73a847
  Angehalten am 06.09. nach der Abnahme von 06-11. Kosten angehalten rund
    0,31 USD je Tag, laufend 0,1158 USD je Stunde.

Die vier Handgriffe bei jeder Anfahrt (box.env, letzte Zeilen, und 06.1-RESEARCH
Teil 1.6):
  1. Die oeffentliche IP wechselt. BOX_IP in box.env nachziehen.
  2. Die SG-Regel fuer Port 22 zeigt auf die IP des Owners, und die wechselt
     ebenfalls. Die aktuelle Adresse kommt von api.ipify.org, danach revoke der
     alten und authorize der neuen Regel.
  3. Der A-Record loadtest.infranode.dev wird nachgezogen.
  4. Der Container startet nach dem Maschinenstart NICHT von AppAPI, also
     indexiert er nicht (DI-05-36). Heilung:
       occ app_api:app:disable findling_backend
       occ app_api:app:enable findling_backend
     Plan 06.1-01 hat die Bewaffnung ueber einen Neustart hinweg behoben; ob die
     Heilung jetzt entfaellt, ist die erste Messung dieses Laufs und keine
     Annahme.
  5. Die harte Speichergrenze ueberlebt eine Neuregistrierung nicht: nach jedem
     app_api:app:register ist docker update --memory=2g --memory-swap=2g neu zu
     setzen (06.1-RESEARCH Teil 5.2, Fallstrick 5).

Das Skript und seine Luecke:
  scripts/ops/aws_box.sh kennt prices, create, volume, status, destroy. Es kennt
  KEIN stop und KEIN start, obwohl die Box genau dazwischen lebt. Wer sie an dem
  Skript vorbei anfaehrt, faehrt auch an seiner Kostenrechnung vorbei.

Die Messform, die wiederholt wird (docs/measurements/2026-09-05-semantiklauf-m7g/):
  README.md als Bericht; semantiklauf.csv als 5-Sekunden-Reihe des Samplers
  (anon, file, slab, current, peak); statusseite.jsonl als Statusreihe;
  07-oom-beweis.txt fuer memory.events; 49-modellgrundlast.txt,
  50-grundlinie-woher.txt, 51-ab-grundlinie.txt und 52-woher-die-grundlast.txt
  fuer die Aufschluesselung der Grundlast; 45-suchlast.py fuer die Suchlast.
  Zwei Fallen aus 06-11, die jeder Nachlauf wiederholt, wenn niemand sie
  aufschreibt: memory.events wird VOR jedem Eingriff gelesen, sonst misst man den
  Aufraeumvorgang mit; und ein Skript, das erst am Ende eines langen Laufs faehrt,
  wird vorher einmal trocken gegen das echte Schema gefahren.

Die Zahlen aus 06-11, gegen die gemessen wird:
  Grundlast der Semantik im Leerlauf 595 MB (Tokenizer 268,8, Chunker 272,8,
    Gewichte 397,1 bei der ersten Einbettung)
  anon-Spitze 1.837,8 MB, beginnend auf die Sekunde mit der ersten semantischen
    Suche; die zweite Modellinstanz kostete +276 MB dauerhaft
  OCR-Phase 1.562,7 MB
  memory.events: oom 0, oom_kill 0, oom_group_kill 0, max 2796
  Suchlast p95 1.129 ms waehrend des Nachlaufs, 524 ms danach, Budget 2.500 ms
  145.854 Chunks, 51.961 Dokumente, 1.321,0 Byte je Dokument
  Verdikte: 51.961 indexiert, 37 uebersprungen (21 too_large, 14 empty_text,
    2 image_not_ocrable), 0 fehlgeschlagen

Der Endungsvergleich (06.1-RESEARCH Teil 9, Annahme A10):
  Haelfte eins: die Endungsverteilung aus scripts/dev/build_load_corpus.py, aus
  dem Seed rechenbar, lokal, ohne Box.
  Haelfte zwei: SELECT ueber state.db auf dem Volumen, gruppiert nach Endung,
  Zustand und Grund. Die Endung steht dort moeglicherweise nicht als Spalte,
  sondern nur im Pfad; das Schema wird nachgesehen, bevor das Skript gebaut wird.
  Die Falle ist protokolliert: die erste Messung in 06-11 scheiterte an einem
  Spaltennamen (verdict statt state).

Uebergaben aus anderen Plaenen:
  06.1-11, Task 1: faellt Annahme A5 (Runner darf kein Loop-Image mounten), wird
    der Plattendrill hier gefahren, mit der Fehlermeldung des Runners als
    Begruendung. Kein tmpfs-Ersatz.
  06.1-11, Task 3: scripts/ops/search_load.py macht keine Zusage; welche
    Nebenlaeufigkeit eine sinnvolle Zusage ist, entscheidet diese Messung
    (Open Question 5).
  06.1-16: derselbe Fahrplan scripts/dev/aio_install_check.sh laeuft hier auf
    arm64, falls E-H5 es so entschieden hat; das AIO-Abbild-Tag ist dort eine
    Variable und heisst auf arm64 latest-arm64.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Box frei, Dauer und Kosten bestaetigt</name>
  <what-built>
    Die geteilte Engine ist gebaut (06.1-02), die Ratsche auf amd64 zieht
    (06.1-04), die Randpfade und Rechte-Szenarien sind gruen, und die drei Audits
    sind gefahren und ihre Befunde behoben (06.1-17). Was fehlt, ist die eine
    Zahl, die nur auf 4 GB ARM entsteht.
  </what-built>
  <how-to-verify>
    1. Darf die Box i-06b1d913f5c6f669b fuer diesen Lauf gestartet werden? Sie
       ist seit dem 06.09. angehalten und kostet angehalten rund 0,31 USD je Tag,
       laufend 0,1158 USD je Stunde.
    2. Der Lauf ist kuerzer als der aus 06-11: gemessen werden Grundlast,
       anon-Spitze und Suchlast, nicht 50.000 Dateien neu. Der Bestand auf dem
       Volumen wird weiterbenutzt. Bitte bestaetigen, dass das so gewollt ist;
       die Alternative waere ein zweiter Volllauf ueber rund 19 Stunden, und der
       waere fuer die Frage dieser Phase nicht noetig.
    3. Der DNS-Eintrag loadtest.infranode.dev wird wie in 06-11 vom Orchestrator
       gesetzt. Bitte bestaetigen, dass derselbe Name benutzt wird.
    4. Gilt eine Kostenobergrenze fuer diesen Lauf?
    5. Der Abbau der Box ist NICHT Teil dieses Plans (D-H3). Er steht in Plan
       06.1-19 und braucht eine eigene Freigabe.
  </how-to-verify>
  <resume-signal>"box frei, weiter" plus die Antworten auf 2 bis 4</resume-signal>
</task>

<task type="auto">
  <name>Task 2: stop und start im Box-Skript, dann die Anfahrt</name>
  <files>scripts/ops/aws_box.sh, backend/tests/test_ops_scripts.py</files>
  <read_first>
    - scripts/ops/aws_box.sh vollstaendig: die fuenf vorhandenen Unterbefehle, die Kostenrechnung, die Zustandsdatei, die Bestaetigungsabfragen in cmd_destroy, die Form der Fehlerbehandlung
    - backend/tests/test_ops_scripts.py vollstaendig (die Hausordnung, die auch fuer neue Unterbefehle gilt)
    - ~/.findling-loadtest/box.env vollstaendig, besonders die letzten Zeilen mit den vier Handgriffen bei der Anfahrt
    - .planning/phases/06-semantische-suche/06-11-SUMMARY.md, Abschnitte zur Anfahrt und zur Waechter-Korrektur
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-01-SUMMARY.md (ob die Bewaffnung ueber einen Neustart hinweg jetzt haelt)
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md Teil 1.6 und Teil 5.2, Fallstrick 5
  </read_first>
  <action>
    Erstens die zwei fehlenden Unterbefehle. `stop` haelt die Instanz an und
    schreibt Zeitpunkt und Kostenstand in die Zustandsdatei; `start` faehrt sie an
    und gibt die neue oeffentliche Adresse aus, mit dem ausdruecklichen Hinweis
    auf die drei nachzuziehenden Dinge (BOX_IP, SG-22, A-Record). Beide folgen der
    Form der vorhandenen Unterbefehle, benutzen dieselbe Kostenrechnung und
    dieselbe Zustandsdatei. `start` fragt die aktuelle Owner-Adresse ueber
    api.ipify.org ab und zieht die SG-Regel fuer Port 22 nach (revoke der alten,
    authorize der neuen); das ist der Handgriff, der bisher jedes Mal von Hand
    lief und jedes Mal vergessen werden konnte.

    Die Hausordnung von scripts/ops gilt auch hier: keine Speicherzahl aus dem
    Docker-Client, kein Geheimnis in einer Ausgabe, keine Gedankenstriche, kein
    Wagenruecklauf. Die vorhandenen Zusicherungen in test_ops_scripts.py werden um
    die zwei Unterbefehle erweitert, plus eine neue: `start` gibt die IP aus und
    nennt die drei Nachzieharbeiten, damit der naechste Anfahrende sie nicht aus
    einer Notiz suchen muss.

    Zweitens die Anfahrt selbst, nach dem Weg aus <interfaces>. Die erste Messung
    des Laufs ist die Gegenprobe zu DI-05-36 an der echten Box: kommt der
    Container nach dem Maschinenstart bewaffnet hoch, seit Plan 06.1-01, oder
    braucht er weiterhin disable und enable? Die Antwort ist eine Zahl
    (Poller-Durchgaenge, Zeile "pass finished") und keine Vermutung, und sie ist
    der erste Absatz des Berichts. Danach wird die harte Grenze neu gesetzt
    (Fallstrick 5) und geprueft, dass sie steht, bevor irgendetwas gemessen wird.

    Das Abbild des Laufs wird auf der Box gebaut, mit dem Baumhash-Beweis aus
    05-14, damit der Bericht sagen kann, welcher Stand gemessen wurde. Der Stand
    ist der nach 06.1-17, also der, der ausgeliefert wird.

    Skript und Kommentare englisch. Keine Gedankenstriche.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; bash -n scripts/ops/aws_box.sh &amp;&amp; python -c "import pathlib; s=pathlib.Path('scripts/ops/aws_box.sh').read_text(encoding='utf-8'); assert 'stop)' in s and 'start)' in s and 'ipify' in s; assert '—' not in s and '–' not in s and '\r' not in s; print('ok')" &amp;&amp; cd backend &amp;&amp; uv run python -m pytest tests/test_ops_scripts.py -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - `aws_box.sh` kennt sieben Unterbefehle, und `usage` nennt alle sieben.
    - `start` gibt die neue Adresse aus, zieht die SG-Regel fuer Port 22 auf die aktuelle Owner-Adresse nach und nennt die drei Nachzieharbeiten.
    - `stop` schreibt Zeitpunkt und Kostenstand in die Zustandsdatei.
    - Die erweiterten Zusicherungen in test_ops_scripts.py sind gruen, und eine davon wird rot, wenn `start` die Nachzieharbeiten verschweigt.
    - ruff (Vollregelsatz), ruff format --check, pyright und vulture sind fuer backend/ lokal gruen, bevor committet wird (CLAUDE.md-Gate).
    - Die Gegenprobe zu DI-05-36 auf der Box ist als Zahl gemessen und steht im ersten Absatz des Berichts.
    - Die harte Grenze ist nach der Registrierung neu gesetzt und ihr Stand ist geprueft, bevor gemessen wird.
    - Der Baumhash des Abbilds ist gleich dem des Arbeitsbaums, und beide stehen im Bericht.
    - Kein Em-Dash (U+2014), kein En-Dash (U+2013) und kein Wagenruecklauf in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Die Box faehrt ueber ihr eigenes Skript an, die vier Handgriffe sind erledigt statt erinnert, und der gemessene Stand ist bewiesen.</done>
</task>

<task type="auto">
  <name>Task 3: Die Nachmessung, die Nebenlaeufigkeit, der Endungsvergleich</name>
  <files>docs/measurements/2026-09-nachmessung-m7g/, docs/install-check.md</files>
  <read_first>
    - docs/measurements/2026-09-05-semantiklauf-m7g/README.md vollstaendig (der Aufbau, den dieser Bericht wiederholt)
    - docs/measurements/2026-09-05-semantiklauf-m7g/skripte/ vollstaendig, besonders 45-suchlast.py, 49-modellgrundlast.sh, 50-grundlinie-woher.sh, 51-ab-grundlinie.sh, 52-woher-die-grundlast.py, 42b-wachter.sh und 42c-lesen.py samt der Korrektur
    - scripts/ops/rss_sampler.sh, scripts/ops/rss_digest.py und scripts/ops/search_load.py (das neue Werkzeug aus 06.1-11)
    - scripts/dev/build_load_corpus.py (die Endungsverteilung des Generators, aus dem Seed rechenbar)
    - backend/src/findling/store/repo.py (das Schema von state.db: wo Zustand, Grund und Pfad stehen, und ob es eine Endungsspalte gibt)
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-11-SUMMARY.md (ob Annahme A5 gehalten hat)
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-ENTSCHEIDE.md, E-H5
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-RESEARCH.md Teil 1.6, Teil 5.2, Teil 5.3, Teil 9
  </read_first>
  <action>
    Der Lauf laeuft detached, mit Sampler und Statusbeobachter daneben, und der
    Plan kehrt als Checkpoint zurueck; der Beobachter meldet ueber ntfy, wenn er
    fertig ist, und der Fertig-Vertrag ist eine Datei im Dateisystem und nicht die
    Meldekette (Muster aus 06-11). Jedes Leseskript wird vorher einmal trocken
    gegen das echte Schema gefahren, bevor es scharfgestellt wird; das ist die
    Lehre, die 06-11 neun Stunden gekostet haette.

    Gemessen wird in dieser Reihenfolge, und memory.events wird VOR jedem Eingriff
    gelesen:

      1. Grundlast im Leerlauf, mit der Aufschluesselung wie in 06-11 (Tokenizer,
         Chunker, Gewichte). Die Frage ist, wie viel von den 595 MB der Fix
         zurueckgibt und was davon Dauerlast bleibt.
      2. Die anon-Spitze mit aktiver Semantik, getrennt fuer die Phase mit OCR und
         die Phase mit der ersten semantischen Suche. Die Spitze aus 06-11 begann
         auf die Sekunde mit der ersten Suche; wenn der Fix wirkt, tut sie das
         nicht mehr, und genau dieser Zeitpunkt ist die Aussage.
      3. memory.events vollstaendig, alle sechs Zaehler.
      4. Die Suchlast: p50, p95 und max ueber scripts/ops/search_load.py, in
         mehreren Nebenlaeufigkeitsstufen (mindestens 1, 4, 8, 16), jeweils mit
         memory.current neben anon, weil ein Brute-Force-Scan den Vektorbestand
         in den Seitencache derselben cgroup zieht. Aus dieser Reihe folgt die
         Zusage: die hoechste Stufe, die das Budget von 2.500 ms mit p95 haelt und
         die drei Schadenszaehler auf null laesst. Die Zahl wird danach
         festgelegt, nicht vorher (Open Question 5), und der Modulkopf von
         search_load.py bekommt sie mit ihrer Messreihe.
      5. Der Endungsvergleich: die Verteilung des Generators lokal rechnen, die
         Verdikte aus state.db gruppiert lesen, beide gegenueberstellen. Bestanden
         ist er, wenn jede Endung des Generators im Bestand mit der erwarteten
         Anzahl auftaucht und jede Abweichung einen Namen hat. Der Erwartungswert
         steht in <interfaces>. Steht die Endung nicht als Spalte in state.db,
         kommt sie ueber den Pfad oder ueber die file_id aus dem Generator
         (Annahme A10); welcher Weg gegangen wurde, steht im Bericht.
      6. Falls Annahme A5 gefallen ist: der Plattendrill waehrend des Einbettens
         mit einem Loop-Image fester Groesse, hier statt in CI, mit denselben drei
         Feststellungen wie in 06.1-11 (die Zweitspur pausiert, kein Arbeitsvorrat
         wird abgeschrieben, nach dem Freigeben waechst der Vektorbestand wieder).
         Hat A5 gehalten, entfaellt dieser Punkt und der Bericht sagt es mit
         Verweis auf den CI-Lauf.
      7. Falls E-H5 fuer arm64 entschieden hat: scripts/dev/aio_install_check.sh
         auf der Box, mit dem Abbild-Tag latest-arm64. Das Protokoll wird an
         docs/install-check.md angehaengt, in derselben Form wie der amd64-Lauf,
         damit beide vergleichbar sind. Hat E-H5 dagegen entschieden, entfaellt
         dieser Punkt und docs/install-check.md bleibt unberuehrt.

    Der Bericht entsteht unter docs/measurements/2026-09-nachmessung-m7g/, mit
    README, Rohdaten und Skripten daneben, in der Form von 06-11. Er stellt jede
    Zahl neben ihre Entsprechung aus 06-11, mit der Differenz. Wo eine Zahl nicht
    besser geworden ist, steht das so da: eine Nachmessung, die nur die guenstigen
    Zahlen nennt, ist keine.

    Skripte und Kommentare englisch, der Bericht deutsch mit echten Umlauten.
    Keine Gedankenstriche, keine Emojis.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "import pathlib; p=pathlib.Path('docs/measurements/2026-09-nachmessung-m7g/README.md'); s=p.read_text(encoding='utf-8'); assert 'memory.events' in s and 'anon' in s and 'oom_group_kill' in s and '1.837,8' in s; assert 'Endung' in s; assert '—' not in s and '–' not in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Der Bericht liegt unter docs/measurements/2026-09-nachmessung-m7g/ mit README, Rohdaten und Skripten, in der Form von 06-11.
    - Die Grundlast steht aufgeschluesselt neben der aus 06-11, mit der Differenz.
    - Die anon-Spitze steht getrennt nach Phase, und der Zeitpunkt der Spitze ist benannt; die erste semantische Suche ist als Ereignis geprueft und nicht angenommen.
    - memory.events steht vollstaendig, alle sechs Zaehler, VOR jedem Eingriff gelesen.
    - Die Suchlast steht in mindestens vier Nebenlaeufigkeitsstufen mit p50, p95, max, memory.current und anon; aus der Reihe folgt die Zusage, und sie steht im Modulkopf von search_load.py mit ihrer Messreihe.
    - Der Endungsvergleich ist gefahren, gegen die Generator-Verteilung, und jede Abweichung hat einen Namen; der benutzte Weg zur Endung ist genannt.
    - Der Plattendrill ist gefahren oder mit Verweis auf den gruenen CI-Lauf als nicht noetig ausgewiesen.
    - Der arm64-Fremdinstallationslauf ist gefahren und in docs/install-check.md protokolliert, oder als nach E-H5 entfallen benannt.
    - Jede Zahl steht neben ihrer Entsprechung aus 06-11, auch die, die nicht besser geworden ist.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den neuen Dateien.
  </acceptance_criteria>
  <done>Alles, was nur auf der Zielhardware zu klaeren war, ist in einem Lauf geklaert, und der Bericht nennt auch die Zahlen, die nicht schmeicheln.</done>
</task>

<task type="auto">
  <name>Task 4: Die neue Store-Zahl einsetzen, in der Form aus D-H2</name>
  <files>README.md, docs/performance.md, docs/embeddings.md</files>
  <read_first>
    - README.md, der Abschnitt "What it costs in memory, measured" in seiner heutigen Fassung samt der zwei ehrlichen Saetze zu max und zur zweiten Modellinstanz
    - docs/performance.md vollstaendig, besonders der Abschnitt "Der Semantik-Volllauf: dieselbe Box, mit der zweiten Spur", die Uebersichtstabelle und der Kostenabschnitt
    - docs/embeddings.md, Abschnitt 4 (die gemessene Kennzahl) und Abschnitt 7 (die gemessene Dauer)
    - .planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-CONTEXT.md, D-H2 im Wortlaut
    - der Bericht aus Task 3
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-CONTEXT.md, D-12 (was der Store-Text zusagen darf)
  </read_first>
  <action>
    Die Store-Aussage bekommt die Form aus D-H2, und zwar an jeder Stelle
    gleichlautend: die drei Schadenszaehler oom, oom_kill und oom_group_kill auf
    null, die anon-Spitze mit ihrer Zahl unter 2,0 GB, und `max` als ausgewiesene
    Kennzahl mit einem Satz dazu, was sie bedeutet (der Dateicache des Index lag
    gegen die Grenze an, der Kernel hat zurueckgedraengt, kein Prozess wurde
    getoetet). Die Aussage verschweigt `max` nicht, und sie erklaert es auch nicht
    weg.

    Die neue Zahl ersetzt die alte als Aussage. Ersetzen heisst ersetzen: die
    Kernaussage in README.md nennt ab jetzt die neue Zahl, und die alte steht
    daneben als Vergleichszeile mit dem Datum ihres Laufs und dem Grund der
    Differenz (die zweite Modellinstanz). Zwei Zahlen ohne Kennzeichnung waeren
    schlimmer als eine falsche.

    docs/performance.md bekommt einen eigenen Abschnitt fuer die Nachmessung,
    neben dem des Semantiklaufs, nicht anstelle. Jede Zahl nennt ihre Messreihe
    unter docs/measurements/. Die Nebenlaeufigkeitszusage kommt hier zum ersten
    Mal als Zusage vor, mit der Reihe, aus der sie folgt, und mit dem Vorbehalt
    aus 06.1-RESEARCH Teil 5.3: die Unified Search fragt alle Provider
    gleichzeitig, also setzt auch der PHP-Prozesspool der Instanz eine Grenze,
    und eine Zahl von der Box ist keine Zusage ueber jede Instanz.

    docs/embeddings.md bekommt einen Nachtrag zur Grundlast: die Aufschluesselung
    aus 06-11 gilt weiter, aber der Posten der zweiten Instanz ist weg, und die
    neue Grundlast steht mit ihrem Datum daneben.

    Falls README.en.md dieselbe Zahl fuehrt, wird sie dort gleich nachgezogen; die
    Regel ist, dass beide Fassungen gepflegt werden.

    Der Store-Text selbst wird hier NICHT geschrieben. Er gehoert zu Plan 06-12,
    der nach der Abnahme dieser Phase beginnt. Was dieser Plan liefert, sind die
    Zahlen, auf die jener Text sich stuetzt.

    Deutsch mit echten Umlauten in der Prosa, README englisch wie bisher. Keine
    Gedankenstriche.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "import pathlib; r=pathlib.Path('README.md').read_text(encoding='utf-8'); p=pathlib.Path('docs/performance.md').read_text(encoding='utf-8'); assert 'oom_group_kill' in r or 'oom_group_kill' in p; assert '1.837,8' in p, 'the old number must remain as a comparison'; assert 'measurements/2026-09-nachmessung-m7g' in p; assert '—' not in r and '–' not in r; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Die Store-Aussage steht in der D-H2-Form: drei Schadenszaehler auf null, anon-Spitze mit Zahl, max als Kennzahl mit einem erklaerenden Satz.
    - README.md nennt in seiner Kernaussage die neue Zahl; die alte steht als Vergleichszeile mit Datum und Grund der Differenz.
    - docs/performance.md fuehrt einen eigenen Abschnitt fuer die Nachmessung, und der Abschnitt des Semantiklaufs steht unveraendert daneben.
    - Jede neue Zahl nennt ihre Messreihe unter docs/measurements/.
    - Die Nebenlaeufigkeitszusage steht mit ihrer Messreihe und mit dem Vorbehalt zum PHP-Prozesspool.
    - docs/embeddings.md nennt die neue Grundlast mit Datum, und die alte Aufschluesselung bleibt lesbar.
    - README.en.md fuehrt dieselbe Zahl, falls sie sie fuehrt.
    - Der Store-Text ist unveraendert; die Store-Metadaten sind nicht angefasst.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Jede Zahl, die dieses Projekt ueber seinen Speicherbedarf behauptet, ist nach dem Fix gemessen, steht in der Form, die der Owner festgelegt hat, und die alte steht als Vergleich daneben.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Berichtsabnahme; die Box bleibt angehalten</name>
  <what-built>
    Die Nachmessung ist gefahren. Der Bericht liegt unter
    docs/measurements/2026-09-nachmessung-m7g/, README.md und docs/performance.md
    fuehren die neue Store-Zahl in der D-H2-Form mit der alten daneben, die
    Nebenlaeufigkeitszusage folgt aus einer Messreihe, und der Endungsvergleich
    ist nachgeholt.
  </what-built>
  <how-to-verify>
    1. Den Bericht lesen. Die vier Zahlen, an denen Kriterium 2 haengt:
       Grundlast, anon-Spitze gesamt, anon-Spitze je Phase, memory.events. Liegt
       die anon-Spitze unter den 1.837,8 MB aus 06-11, und stehen die drei
       Schadenszaehler auf null?
    2. README.md und docs/performance.md gegen den Bericht halten: steht die
       Aussage in der D-H2-Form, ist die alte Zahl als Vergleich erkennbar
       geblieben, nennt jede Zahl ihre Messreihe?
    3. Die Nebenlaeufigkeitszusage ansehen: ist sie aus der Reihe hergeleitet und
       nicht gesetzt, und steht der Vorbehalt zum PHP-Prozesspool daneben?
    4. Den Endungsvergleich ansehen: hat jede Abweichung einen Namen?
    5. Falls eine Zahl nicht traegt (die Spitze nicht gesunken, ein
       Schadenszaehler nicht null, der Endungsvergleich mit unerklaerten
       Abweichungen): bitte sagen, ob nachgebessert und erneut gemessen werden
       soll oder ob die Zusage kleiner wird.
    6. Die Box bleibt angehalten, nicht abgebaut (D-H3). Der Abbau mit
       Nichtexistenz-Pruefung ist die letzte Aufgabe von Plan 06.1-19 und braucht
       dort eine eigene Freigabe.
  </how-to-verify>
  <resume-signal>"nachmessung abgenommen" oder die Entscheidung nach Punkt 5</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zielhardware zur Zusage | Die Store-Zahl gilt fuer 4 GB ARM; jede andere Quelle misst etwas anderes |
| Volumen zur Auswertung | state.db lebt auf dem Volumen der Box; nach dem Abbau ist der Endungsvergleich unmoeglich |
| Messskript zum Schema | Ein Leseskript, das erst am Ende faehrt, faellt am Ende aus, wenn ein Spaltenname nicht stimmt |
| Kostenrechnung zum Lebenszyklus | Eine Box, die an ihrem Skript vorbei angefahren wird, faehrt auch an ihrer Kostenrechnung vorbei |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-74 | Repudiation | Die Nachmessung wird in einer anderen Form gefahren und ist deshalb mit 06-11 nicht vergleichbar | mitigate | Dieselbe Messform, dieselben Skripte, jede Zahl neben ihrer Entsprechung mit Differenz |
| T-06.1-75 | Repudiation | memory.events wird nach einem Eingriff gelesen und misst den Aufraeumvorgang | mitigate | Lesen VOR jedem Eingriff, als Schritt im Ablauf und als Zusicherung im Bericht |
| T-06.1-76 | Denial of Service | Die harte Grenze faellt bei der Neuregistrierung weg und der Lauf misst 4 GB statt 2 | mitigate | docker update nach jedem register, Stand der Grenze vor der ersten Messung geprueft (Fallstrick 5) |
| T-06.1-77 | Repudiation | Ein Leseskript scheitert am Schema und kostet den ganzen Lauf | mitigate | Trockenlauf jedes Leseskripts gegen das echte Schema vor dem Scharfstellen (Lehre aus 06-11) |
| T-06.1-78 | Information Disclosure | Die SSH-Regel bleibt auf einer fremd gewordenen Adresse offen | mitigate | `start` zieht die SG-Regel fuer Port 22 auf die aktuelle Owner-Adresse nach, revoke vor authorize |
| T-06.1-79 | Repudiation | Der Endungsvergleich wird nach dem Abbau der Box unmoeglich und bleibt fuer immer offen | mitigate | Er faehrt in diesem Plan, vor dem Abbau; der Abbau steht in 06.1-19 |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neue Abhaengigkeit; gemessen wird mit den vorhandenen Skripten, uv.lock bleibt unveraendert |
</threat_model>

<verification>
- `bash -n scripts/ops/aws_box.sh` ohne Befund, `usage` nennt sieben Unterbefehle
- `cd backend && uv run python -m pytest tests/test_ops_scripts.py -q` gruen
- Der Bericht unter docs/measurements/2026-09-nachmessung-m7g/ ist vollstaendig und vergleicht jede Zahl mit 06-11
- README.md und docs/performance.md fuehren die Store-Aussage in der D-H2-Form, die alte Zahl als Vergleich
- Der Endungsvergleich ist gefahren und jede Abweichung benannt
- Die Box ist nach dem Lauf ueber `aws_box.sh stop` angehalten, und box.env ist fortgeschrieben
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien
</verification>

<success_criteria>
Die Store-Zahl ist auf 4 GB ARM neu gemessen und liegt unter der aus 06-11, die
Aussage steht in der Form aus D-H2, die Nebenlaeufigkeit ist gemessen statt
zugesagt, der Endungsvergleich ist nachgeholt, und die Box steht angehalten und
vollstaendig ausgewertet fuer ihren Abbau bereit.
</success_criteria>

<output>
Create `.planning/phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-18-SUMMARY.md` when done
</output>
