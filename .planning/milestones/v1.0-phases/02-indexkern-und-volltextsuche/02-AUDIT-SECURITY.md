# Security-Audit Findling Phase 2 (01.09.2026, Stand c17a362) - 2 CRIT, 1 HIGH, 8 MED, 10 LOW

Vorbemerkung: backend/appinfo/info.xml:81/95 deklariert /search + /snippets mit access_level USER -> JEDER eingeloggte NC-Nutzer ruft sie mit frei gewaehltem JSON-Body ueber den AppAPI-Proxy. PHP-Provider ist NICHT der einzige Weg hinein. Grundlage fuer C1/C2/M1/M3.

## CRITICAL (Container-DoS durch jeden Instanz-Nutzer, NICHT in Python abfangbar)
- C1 api/search.py:80 offset ohne le= -> index/search.py:116 searcher.search(offset). Auch Provider.php:396 startOffset + ExAppService.php:137 max(0,$offset) ohne Deckel. tantivy allokiert (limit+offset)*24 Byte; offset=2^31 -> "memory allocation of 51 GB failed" Exit 127 (gemessen). Rust-Abbruch, kein Python-Handler greift; auf 4-GB-Box ab ~170 Mio. Uvicorn+Poller tot, wiederholbar = Dauer-DoS Suche UND Index. Fix: Field(..., le=SEARCH_OFFSET_MAX ~1200), Deckel auch in ExAppService+Provider.
- C2 query/rewrite.py:215 parse_query_lenient, _PLAIN_TOKEN matcht keine Klammern -> rekursiver Rust-Parser. "((((...))))" ~8kB: Windows 1MB-Stack Tiefe 1000 STACK_OVERFLOW, Linux 8MB Tiefe 4000 Access Violation (gemessen). Laeuft im to_thread DESSELBEN Prozesses wie ASGI-App. Fix: Klammertiefe vor parse zaehlen, ab 32 ablehnen (leere Query + errors-Eintrag).

## HIGH
- H1(sec) extract/text.py:142 rtf_to_text, striprtf HYPERLINKS-Regex quadratisches Backtracking. Gemessen 252kB=10,7s, 404kB=26,5s, 656kB>120s. Sandbox faengt als failed(timeout) ABER INDEX_WORKERS=1 -> jede Datei kostet 120s des einzigen Indexers; 100MB Quota = 3,3h Stillstand. Fix sofort: RTF-Groessengrenze (256kB) vor rtf_to_text, oder rtf aus ALLOWED_MIMETYPES (StorageService.php:76 + dispatch.py:69). Sauber: HYPERLINKS besitzergreifend patchen + upstream.

## MEDIUM
- M1 index/search.py:149: has_more=seen<_hits_in_total(result) leakt UNGEFILTERTE Gesamttrefferzahl (vor ACL-Prefilter) nach aussen -> Existenzorakel/Zaehlung fremder Dokumente per Binaersuche (limit=1, offset variieren). Widerspricht Mitigation T-02-93. Fix: has_more = len(result.hits)==limit; count=True entfaellt.
- M2 test_readonly_gate.py:126/60/208: Gate A Invariante 3 flaggt nur Empfaenger in {nc,ocs,_session,session,adapter}; put/post/patch NICHT in FORBIDDEN_IDENTIFIERS. Gemessen: client.put/post/request/stream(PUT) auf nc/client.py = DURCH; GatewayClient.put aus poller.py = [] (loest auch Invariante 1 nicht aus, kein httpx-Import). Heute keine Verletzung, aber Kontrolle lueckenhaft. Fix: put/post/patch/request/stream in Methodenpruefung; REMOTE_RECEIVERS umdrehen (Schreibmethode=remote ausser Lokal-Allowlist); GatewayClient nicht re-exportieren; je Bypass ein Selbsttest.
- M3 api/search.py:78 + snippets.py:73: query ohne max_length. Gemessen build_query 4,4MB=6,4s (add_umlaut_variants verdoppelt, count=True ueber echten Index). Fix: max_length=512.
- M4 odf.py:64 + office.py:57/106: Dekompressionsbombe, keine file_size/Ratio-Pruefung. Gemessen 815kB ODT -> 800MB content.xml eingelesen, indexed(truncated) in 3,1s. RLIMIT_AS 512MB faengt als out_of_memory (Container faellt nicht), Preis = Worker-Slot + 120s. Fix: ZipInfo.file_size vor read() gegen Deckel. (== Bug/Perf M6/M3.)
- M5 nc/client.py:203 + poller.py:444: kein Byte-Deckel beim Download in Scratch (TOCTOU auf size aus Queue-Metadaten); Nutzer ersetzt Datei unter gleicher File-ID durch riesige -> Volume voll (min_free_bytes schuetzt nur Commit). Fix: in _stream_file mitzaehlen, bei >max_file_bytes abbrechen.
- M6 sandbox.py:248: mp spawn erbt os.environ -> Extraktionskind (einzige RCE-Flaeche, angreiferkontrollierte Dokumente durch C-Libs) haelt APP_SECRET+HP_SHARED_KEY, spricht aber nie mit NC. RCE im Kind -> Content-Gateway userId frei -> jede Datei jedes Nutzers. Fix: in _child_main VOR Dispatcher-Import APP_SECRET/HP_SHARED_KEY/NEXTCLOUD_URL/AA_VERSION aus environ loeschen (3 Zeilen).
- M7 docker.yml:122 vs 204: Smoke-Test prueft Build A (load), nach ghcr geht Build B (push-by-digest, nie geprueft). "getestet"-Zusage falsch. Fix: einmal bauen, per Digest pushen, pull @digest, Smoke gegen Digest, dann imagetools.
- M8 integration.yml u.a.: Action-SHA-Kommentare falsch (checkout@3d3c42 kommentiert v5.0.0 liegt v7.0.1; @fbc6f39 v5.0.0 liegt v5.1.0). Pinning-Verteidigung wertlos wenn Kommentar-SHA-Paar nicht stimmt. Fix: Kommentare korrigieren, Abgleich scripten.

## LOW
L1 info.xml PHP verschweigt gespeicherten Dokumenttext (Backend-info.xml ehrlich) -> Absatz uebernehmen. L2 text.py:127 script/style im XHTML-Zweig (namespaced) nicht entfernt -> Indexverschmutzung. L3 sandbox.py:218 kill() nur Kind nicht Prozessgruppe (ab Phase-3-tesseract kritisch, jetzt billig fixen: start_new_session+killpg). L4 kein CI-Gate fuer ExApp-Vertrauensgrenze der 4 Queue-Routen (Schutz vorhanden, Regression ungesichert) -> Python-Test analog Gate A. L5 Provider.php:243 kein isReadable()-Check nach Typpruefung (Team-Folder-ACL-Wrapper-Restrisiko). L6 GatewayController.php:109 + QueueController.php:96/150/185/211 $e->getMessage() im Log (Pfad/SQL-Leak) entgegen eigener Regel. L7 setup-test-nc action.yml:106 inputs.* direkt in run: interpoliert (heute nur Literale). L8 setup-uv zwei Major (v10 vs v6). L9 Provenance-Zusage nie verifiziert (nur grep auf Plattformen). L10 Actions laden zur Laufzeit Binaries (setup-php/setup-uv).

## Geprueft und SAUBER
ACL-Kette korrekt (Recheck loest via getFirstNodeById, Titel/Pfad aus Knoten nie Container-Antwort, Candidate hat kein Textfeld, Snippets erst nach Recheck, Kanarienvogel eng fileId<=0+exakter Titel); Nur-Lesen-Invariante heute nirgends verletzt (429 Tests); ExApp-Grenze alle 5 Routen ExAppRequired+rejectForeignCaller; XXE/externe Entities/SSRF in ODF/HTML/DOCX/PPTX/XLSX geschlossen+gemessen; Billion-Laughs blockiert (libxml2/expat, nicht Findling-Code, haengt am Dockerfile-Digest); Zip-Slip strukturell unmoeglich (kein extractall); keine Kommando/Argv-Injection (kein subprocess/shell); keine Query-Injection auf ACL (Berechtigungen in SQLite nicht im Index); SQL durchgaengig IQueryBuilder/? -parametrisiert; Secrets/Logs sauber ausser L6; CI ohne pull_request_target/workflow_run, SHAs gepinnt+existent, permissions minimal; Dockerfile Digest-gepinnt non-root uid1000.

## Abschluss + Reihenfolge (Auditor)
Kernbefund: fehlende EINGABE-DECKELUNG an der Aussenkante (C1/C2/M3/M1 = derselbe blinde Fleck). C1+C2+M3 in EINEM Commit (le= offset, max_length query, Klammertiefe, Konstanten nach config.py), dann H1(RTF), dann M1 (1 Zeile), M2 (Gate+Selbsttests), M4-M6. M7/M8 naechster Release. L3 VOR OCR (Phase 3).
