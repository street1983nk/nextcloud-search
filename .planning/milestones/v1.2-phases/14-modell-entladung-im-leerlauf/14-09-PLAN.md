---
phase: 14-modell-entladung-im-leerlauf
plan: 09
type: execute
wave: 6
depends_on: [14-06, 14-07]
files_modified:
  - backend/src/findling/embed/engine.py
  - php/lib/Service/AdminViewService.php
  - php/templates/admin.php
  - php/js/admin.js
  - php/l10n/de.json
  - php/l10n/de.js
  - php/l10n/de_DE.json
  - php/l10n/de_DE.js
  - php/l10n/fr.json
  - php/l10n/fr.js
  - backend/tests/test_admin_ui_contract.py
  - docs/admin-page.md
autonomous: true
requirements: [MEM-05]
gated_by: "14-02. Faellt dort E1, wird dieser Plan nicht ausgefuehrt."
owner_decision: "Zweig B, das sechste Wort `unloaded`, entschieden 19.09.2026 (14-CONTEXT.md). Wird hier vollzogen, nicht erneut verhandelt."

must_haves:
  truths:
    - "Die Admin-Seite unterscheidet 'noch nie gelesen' von 'zum Sparen freigegeben, die naechste Suche kostet das Nachladen'"
    - "Beide Haelften der Seite bilden dasselbe Wort auf denselben Satz ab"
    - "Alle sechs Kataloge tragen denselben Schluesselsatz, der franzoesische Wortlaut ist echtes Franzoesisch"
    - "docs/admin-page.md sagt nicht mehr, es gebe kein sechstes Wort, wenn es eines gibt"
  artifacts:
    - path: "backend/src/findling/embed/engine.py"
      provides: "ENGINE_UNLOADED, der sechste Eintrag in ENGINE_STATES und der Zweig in engine_state()"
      contains: "unloaded"
    - path: "php/lib/Service/AdminViewService.php"
      provides: "Die gespiegelte geschlossene Liste mit sechs Woertern"
      contains: "unloaded"
    - path: "docs/admin-page.md"
      provides: "Die Zustandstabelle mit sechs Meldungen plus der siebten Zeile fuer den stummen Container"
      contains: "unloaded"
  key_links:
    - from: "php/templates/admin.php"
      to: "php/js/admin.js"
      via: "wortgleiche Zuordnung Zustand auf Satz, gehalten von test_both_halves_of_the_page_map_the_same_state_to_the_same_sentence"
      pattern: "unloaded"
    - from: "backend/src/findling/embed/engine.py::ENGINE_STATES"
      to: "backend/tests/test_admin_ui_contract.py"
      via: "das Gate importiert den Satz aus dem Container"
      pattern: "ENGINE_STATES"
---

<objective>
Das sechste Wort. `unloaded` wird ein Zustand des Containers, bekommt seinen Satz
in beiden Haelften der Admin-Seite, seine sechs Katalogeintraege in drei Sprachen
und seine Zeile in `docs/admin-page.md`.

Purpose: MEM-05, zweite Haelfte, und Erfolgskriterium 5. Der Owner hat am
19.09.2026 Zweig B entschieden (14-CONTEXT.md), mit ausgeschriebener
Kostenfolge. Dieser Plan vollzieht den Entscheid; er verhandelt ihn nicht neu.

Output: ein Wort an zwei Stellen, ein Satz an zwei Stellen, ein Schluessel in
sechs Katalogdateien, zwei Gate-Literale nachgezogen, eine Dokumentationstabelle.

**Warum das alles in EINEM Plan liegt.** `ENGINE_STATES` wird von
`test_admin_ui_contract.py` aus dem Container importiert und gegen die
PHP-Haelfte gehalten. Ein Plan, der nur das Wort hinzufuegt, hinterlaesst vier
rote Katalog-Gates fuer die Dauer einer Welle. Der Preis dafuer ist ein Plan mit
zwoelf Dateien, von denen zehn eine Zeile aendern.

**Der franzoesische Wortlaut ist NICHT muttersprachlich geprueft** und faellt
unter die Owner-Abnahme am Phasen-Checkpoint (Plan 14-12), genau wie die 23
Wortlaute aus 13-11. Dieser Plan traegt den Nachtrag in `docs/l10n-french.md`
ein, so wie 13-11 es getan hat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md
@.planning/phases/14-modell-entladung-im-leerlauf/14-CONTEXT.md
@docs/l10n-french.md
</context>

<interfaces>
<!-- Die exakten Stellen, an denen das fuenfte Wort heute steht. Alle gelesen. -->

```python
# backend/src/findling/embed/engine.py:100-108
ENGINE_LOADED: Final = "loaded"
ENGINE_COLD: Final = "cold"
ENGINE_DISABLED: Final = "disabled"
ENGINE_MISSING: Final = "missing"
ENGINE_RETRY_PENDING: Final = "waiting_for_retry"
ENGINE_STATES: Final = frozenset({ENGINE_LOADED, ENGINE_COLD, ENGINE_DISABLED, ENGINE_MISSING, ENGINE_RETRY_PENDING})

# engine_state(), Reihenfolge der Antworten (fix, und nicht die Feldreihenfolge):
# disabled -> missing -> waiting_for_retry -> loaded -> cold
```

```php
// php/lib/Service/AdminViewService.php:177
private const ENGINE_STATES = ['loaded', 'cold', 'disabled', 'missing', 'waiting_for_retry'];
```

```php
// php/templates/admin.php:66-72, wortgleich in php/js/admin.js:440-458
'loaded' => 'The model is in memory, the semantic search is answering.'
'cold' => 'The model is read when it is first needed. That is the normal state.'
'disabled' => 'The semantic half is switched off in the settings of the container.'
'missing' => 'There is no model in this image. ...'
'waiting_for_retry' => 'Reading the model failed once and is tried again shortly. ...'
// default: 'This container does not report the state of the model yet.'
```

Die vier Gates in `backend/tests/test_admin_ui_contract.py`:
1. `test_both_halves_of_the_page_map_the_same_state_to_the_same_sentence`, mit
   `assert set(template) == set(ENGINE_STATES)`
2. `test_the_six_sentences_of_the_engine_line_are_in_the_german_catalogue`, mit
   `assert len(sentences) == 6`  ->  wird **7**
3. `test_the_two_translation_files_carry_the_same_keys`, mit
   `assert len(keys_of["de.json"]) == 198`  ->  wird **199**
4. `test_all_six_catalogues_carry_the_same_keys` und
   `test_every_french_value_carries_a_french_wording` ueber
   `L10N_CATALOGUES = (de.json, de.js, de_DE.json, de_DE.js, fr.json, fr.js)`

Aus Plan 14-05: `unload_count() -> int`, monoton.
Aus Plan 14-06: `released_count() -> int` reicht ihn durch.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Der sechste Zustand im Container</name>
  <files>backend/src/findling/embed/engine.py</files>
  <read_first>
    - backend/src/findling/embed/engine.py Zeilen 95 bis 115 und 249 bis 307 (die Konstanten und `engine_state` samt seiner ausgeschriebenen Rangfolge)
    - .planning/phases/14-modell-entladung-im-leerlauf/14-CONTEXT.md (der Owner-Entscheid im Wortlaut)
    - .planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md Abschnitt 8.1 bis 8.3
  </read_first>
  <behavior>
    - Schalter aus, nie geladen: `engine_state()` ist `cold` (unveraendert)
    - Schalter an, nie geladen: `cold`
    - Geladen: `loaded` (unveraendert)
    - Nach einer Freigabe, Artefakte vorhanden, kein Cooldown: `unloaded`
    - Nach einer Freigabe UND danach wieder geladen: `loaded`
    - Nach einer Freigabe, aber Semantik abgeschaltet: `disabled` (Vorrang bleibt)
    - Nach einer Freigabe, aber Artefakte fehlen: `missing` (Vorrang bleibt)
    - Nach einer Freigabe, aber ein Laden ist in seiner Abkuehlung: `waiting_for_retry` (Vorrang bleibt)
    - Ein frischer Prozess meldet nie `unloaded`, weil der Entladezaehler bei null steht
  </behavior>
  <action>
Drei Aenderungen in `backend/src/findling/embed/engine.py`.

1. `ENGINE_UNLOADED: Final = "unloaded"` neben die fuenf bestehenden Konstanten,
   und in `ENGINE_STATES` aufnehmen. Der Kommentar ueber dem Block sagt heute,
   dass eine sechste Antwort nicht ankommen kann, ohne dass diese Zeile sie
   sieht; er bleibt richtig und bekommt einen Satz zum Anlass: der Owner hat am
   19.09.2026 Zweig B entschieden, weil der Schalter eine sichtbare Rueckmeldung
   braucht und "genau ein Laden je warmem Fenster" am Verlauf
   `cold -> ready -> unloaded -> ready` nachvollziehbar wird.
2. In `engine_state()` einen Zweig **zwischen** `loaded` und `cold`:
   `if unload_count() > 0: return ENGINE_UNLOADED`. Die Rangfolge der Antworten
   bleibt sonst unveraendert: `disabled`, `missing`, `waiting_for_retry`,
   `loaded`, `unloaded`, `cold`. Der bestehende Absatz im Docstring, der die
   Reihenfolge ausschreibt, wird um den sechsten Platz ergaenzt, mit der
   Begruendung: `unloaded` steht hinter `loaded`, weil ein Container, der nach
   einer Freigabe wieder geladen hat, geladen ist und nicht entladen; und vor
   `cold`, weil `cold` der Zustand ist, der uebrig bleibt, wenn nichts anderes
   wahr ist.
3. Der Docstring von `engine_state` sagt heute "Which of five states". Das wird
   sechs, und der Absatz zur Frage "was sieht ein Admin" bekommt einen Satz:
   `unloaded` unterscheidet "noch nie gelesen" von "zum Sparen freigegeben, die
   naechste Suche kostet das Nachladen". Beide sehen auf der Deckungszahl gleich
   aus, und der Milestone verlangt, die Wiederaufwaerm-Kosten auszuweisen.

Der Zaehler bleibt die Quelle, nicht ein neues Feld: er ist monoton, ueberlebt
`reset()` nicht ruecksetzbar, und ein Prozess, der nie entladen hat, kann
`unloaded` damit gar nicht melden.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_embed_engine.py -q && python -c "from findling.embed.engine import ENGINE_STATES; assert len(ENGINE_STATES)==6; print(sorted(ENGINE_STATES))"</automated>
  </verify>
  <acceptance_criteria>
    - `ENGINE_STATES` hat genau sechs Eintraege und traegt `unloaded`.
    - Der `unloaded`-Zweig steht in `engine_state()` hinter `loaded` und vor dem abschliessenden `cold`.
    - `engine_state()` baut und laedt weiterhin nichts (kein Aufruf von `shared_model`), belegt durch den bestehenden Testfall dazu.
    - Ein Testfall je Zeile des Behavior-Blocks, alle gruen.
  </acceptance_criteria>
  <done>Der Container kann sagen, dass er zum Sparen losgelassen hat.</done>
</task>

<task type="auto">
  <name>Task 2: Der Satz in beiden Haelften der Seite und die zwei Gate-Zahlen</name>
  <files>php/lib/Service/AdminViewService.php, php/templates/admin.php, php/js/admin.js, backend/tests/test_admin_ui_contract.py</files>
  <read_first>
    - php/lib/Service/AdminViewService.php Zeilen 165 bis 180 (die gespiegelte geschlossene Liste samt ihrer Begruendung)
    - php/templates/admin.php Zeilen 55 bis 76 (die sechs Saetze und der Kommentar darueber)
    - php/js/admin.js Zeilen 425 bis 460 (dieselben sechs Saetze im Skript)
    - backend/tests/test_admin_ui_contract.py Zeilen 1380 bis 1430 und 1495 bis 1530 (die vier Gates und ihre Literale)
  </read_first>
  <action>
Vier Aenderungen, in dieser Reihenfolge, damit der Baum zwischen ihnen nie
laenger als noetig rot ist.

1. `AdminViewService.php:177`: `'unloaded'` in die Konstante aufnehmen. Der
   Docstring darueber sagt heute "The five states"; das wird sechs. Der Satz,
   dass die Woerter eine Etage weiter in `embed/engine.py` entschieden werden,
   bleibt unveraendert richtig.
2. `php/templates/admin.php`: einen Eintrag in `$engineSentences`, Schluessel
   `'unloaded'`, Wert ueber `$l->t(...)`. Der englische Quellsatz lautet
   **woertlich**:
   `The model was released to save memory. The next search answers with full text hits and loads it again in the background.`
   Der Kommentar ueber dem Block sagt heute "Six sentences for five words"; das
   wird "Seven sentences for six words", mit demselben Nachsatz ueber den
   Container, der nichts meldet.
3. `php/js/admin.js`: derselbe Satz, byteweise identisch, als `case 'unloaded':`
   in `engineSentence`. Der Kommentar "Die sechs Saetze" wird "Die sieben Saetze",
   und der Satz "Der Container sendet eines von fuenf Woertern" wird "eines von
   sechs Woertern".
4. `backend/tests/test_admin_ui_contract.py`: zwei Literale nachziehen, und
   **nur diese zwei**:
   - `assert len(sentences) == 6` wird `== 7` in
     `test_the_six_sentences_of_the_engine_line_are_in_the_german_catalogue`.
     Der Funktionsname enthaelt das Zahlwort; er wird mit umbenannt auf
     `test_the_seven_sentences_of_the_engine_line_are_in_the_german_catalogue`,
     damit Name und Zusicherung nicht auseinanderlaufen. Der Docstring sagt, was
     die siebte ist.
   - `assert len(keys_of["de.json"]) == 198` wird `== 199` in
     `test_the_two_translation_files_carry_the_same_keys`.
   Keine weitere Zahl wird angefasst. Faellt ein drittes Literal auf, ist das ein
   Befund und gehoert in die SUMMARY, nicht in eine stille Korrektur.

**Eine Falle, die in den Kommentar gehoert** (PITFALLS 13): ein Container, der
das neue Wort meldet, und eine PHP-Haelfte, die es noch nicht kennt, zeigt den
Ausweichsatz "Dieser Container meldet den Zustand des Modells noch nicht", was
wie ein kaputtes Backend aussieht. Beide Haelften reisen als Paar (REL-02
verlangt den Gleichschritt), aber ein Admin kann sie getrennt aktualisieren. Der
Satz gehoert in den Kommentar ueber `$engineSentences` und in
`docs/admin-page.md` (Task 4).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_admin_ui_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'unloaded'" php/lib/Service/AdminViewService.php php/templates/admin.php php/js/admin.js` nennt je mindestens 1.
    - Der englische Quellsatz steht byteweise identisch in `admin.php` und `admin.js` (Gate 1 belegt es).
    - `grep -c '== 7' backend/tests/test_admin_ui_contract.py` zeigt genau das eine neue Vorkommen im Satz-Gate; `grep -c '== 6'` zeigt dort kein Ueberbleibsel.
    - `grep -c '== 199' backend/tests/test_admin_ui_contract.py` ist 1, `grep -c '== 198'` ist 0.
    - Auf dieser Maschine gibt es kein PHP: der Syntaxnachweis fuer die drei PHP-Dateien wird ueber `docker run --rm -v "$PWD/php:/php:ro" php:8.2-cli php -l /php/<datei>` gefuehrt (dieselbe Version wie der Lint-Job in CI, Vorgehen aus 13-04), und das Ergebnis steht in der SUMMARY.
  </acceptance_criteria>
  <done>Beide Haelften der Seite kennen das sechste Wort und sagen dazu denselben Satz.</done>
</task>

<task type="auto">
  <name>Task 3: Sechs Katalogdateien in drei Sprachen, und die Doku der Seite</name>
  <files>php/l10n/de.json, php/l10n/de.js, php/l10n/de_DE.json, php/l10n/de_DE.js, php/l10n/fr.json, php/l10n/fr.js, docs/l10n-french.md, docs/admin-page.md</files>
  <read_first>
    - php/l10n/de.json und php/l10n/de.js (Aufbau, Sortierung, die Klammerform der .js-Datei)
    - php/l10n/fr.json (Wortlaute der fuenf bestehenden Engine-Saetze, als Massstab fuer den neuen)
    - docs/l10n-french.md (der Abnahmeabschnitt und der Nachtrag, den 13-11 dort gesetzt hat)
    - .planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-11-SUMMARY.md (die Lehre: der erste Einfuegelauf schrieb die Eintraege ohne Trennkommas)
    - docs/admin-page.md Zeilen 120 bis 185 (die Zustandstabelle, der Absatz zu `waiting_for_retry` mit dem Satz "also gibt es dafuer kein sechstes Wort", und der Absatz zur sechsten Zeile)
  </read_first>
  <action>
Einen Schluessel in sechs Dateien, mit drei Wortlauten.

- `de.json` und `de.js`: der deutsche Satz. Vorschlag, in der Tonlage der fuenf
  bestehenden: `Das Modell wurde zum Sparen freigegeben. Die naechste Suche
  antwortet mit Volltexttreffern und laedt es im Hintergrund nach.` In der
  **Katalogdatei** stehen echte Umlaute, also "naechste" als "nächste". Der
  englische Quellsatz ist der Schluessel und bleibt byteweise der aus Task 2.
- `de_DE.json` und `de_DE.js`: byteweise identisch zu den beiden de-Dateien.
  Gate `test_the_two_translation_files_carry_the_same_keys` vergleicht den
  gesamten Dateitext, nicht nur die Schluesselmenge.
- `fr.json` und `fr.js`: der franzoesische Wortlaut. Er muss echtes
  Franzoesisch sein (Gate `test_every_french_value_carries_a_french_wording`
  erkennt einen aus `de.js` kopierten Wert und einen leeren Wert) und darf
  keinen Platzhalter verlieren oder erfinden (Gate G3); dieser Satz traegt
  keinen, also ist G3 trivial erfuellt.
- **Keine neue Ausnahme** in `FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY`. Die Liste
  bleibt bei fuenf Eintraegen.

Nach dem Einfuegen **sofort** jede der sechs Dateien durch einen Parser
schicken, nicht durch eine Diff-Durchsicht: die Lehre aus 13-11 ist, dass ein
Einfuegelauf ohne Trennkommas weder in der Klammerbilanz noch im Diff auffaellt,
der JSON-Parser es aber sofort meldet.

In `docs/l10n-french.md` einen datierten Nachtrag im Abschnitt Abnahme, nach dem
Muster des Nachtrags aus 13-11: der neue Wortlaut ist nicht muttersprachlich
geprueft und liegt dem Owner am Phasen-Checkpoint vor. Eine abgenommene Datei
darf keine ungelesenen Zeilen stillschweigend mittragen.

**Teil B, docs/admin-page.md.** Drei Aenderungen, damit die Dokumentation dem
Produkt nicht widerspricht:

1. **Die Tabelle** bekommt eine Zeile `unloaded`, zwischen `cold` und
   `disabled`, mit dem deutschen Satz von oben und der Spalte "Was ein Admin tun
   kann": nichts, das ist der eingeschaltete Sparbetrieb; wer die Nachladekosten
   nicht will, setzt `FINDLING_EMBED_IDLE_RELEASE_SECONDS` auf 0. Die Einleitung
   darueber sagt heute "als eines von fuenf Woertern"; das wird sechs.
2. **Der Satz "also gibt es dafuer kein sechstes Wort"** wird praezisiert und
   nicht geloescht. Er steht ueber `waiting_for_retry` und ist dort korrekt: die
   zwei Quellen dieses Zustands sind fuer einen Admin derselbe Satz. Die neue
   Fassung sagt genau das und ergaenzt, dass es seit dieser Phase ein sechstes
   Wort gibt, aber fuer eine andere Lage, naemlich die Freigabe im Leerlauf. Wer
   den Satz stehen laesst, liefert eine Dokumentation, die dem eigenen Produkt
   widerspricht; wer ihn loescht, verliert die Begruendung fuer die zwei Quellen
   von `waiting_for_retry`.
3. **Ein Absatz zum Paar aus zwei Haelften**: ein Container, der `unloaded`
   meldet, und eine PHP-Haelfte, die es noch nicht kennt, zeigt die siebte Zeile
   ("Dieser Container meldet den Zustand des Modells noch nicht"). Das sieht wie
   ein kaputtes Backend aus und ist nur eine Aktualisierung in der falschen
   Reihenfolge. Beide Haelften reisen als Paar.

Deutsche Prosa mit echten Umlauten, keine Em-Dashes, keine Emojis.
  </action>
  <verify>
    <automated>cd backend && python -c "
import json,pathlib
root=pathlib.Path('../php/l10n')
for n in ('de.json','de_DE.json','fr.json'):
    d=json.loads((root/n).read_text(encoding='utf-8'))['translations']; print(n,len(d))
for n in ('de.js','de_DE.js','fr.js'):
    t=(root/n).read_text(encoding='utf-8'); json.loads(t[t.index('{'):t.rindex('}')+1]); print(n,'ok')
" && uv run pytest tests/test_admin_ui_contract.py -q && cd .. && grep -c 'unloaded' docs/admin-page.md
  </verify>
  <acceptance_criteria>
    - Alle sechs Dateien parsen.
    - `de.json` traegt 199 Schluessel, und alle vier deutschen Dateien tragen dieselbe Schluesselmenge.
    - `de.json` und `de_DE.json` sind byteweise gleich, ebenso `de.js` und `de_DE.js`.
    - Der franzoesische Wert ist weder leer noch gleich seinem Schluessel noch gleich dem deutschen Wert.
    - `FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY` ist unveraendert bei fuenf Eintraegen.
    - `docs/l10n-french.md` traegt einen datierten Nachtrag, der den neuen Wortlaut als ungeprueft ausweist.
    - `uv run pytest tests/test_admin_ui_contract.py -q` gruen, alle vier Katalog-Gates inbegriffen.
    - `docs/admin-page.md`: die Zustandstabelle traegt sieben Zeilen (sechs Meldungen plus "kein Feld"), die Einleitung nennt sechs Woerter.
    - Die Zeichenkette "also gibt es dafür kein sechstes Wort" kommt in `docs/admin-page.md` nicht mehr vor, und an ihrer Stelle steht die praezisierte Fassung samt Begruendung fuer die zwei Quellen von `waiting_for_retry`.
    - Der Absatz zur Aktualisierung in der falschen Reihenfolge steht in `docs/admin-page.md`.
    - Kein U+2014 und kein U+2013 in `docs/admin-page.md`.
  </acceptance_criteria>
  <done>Alle drei Sprachen sagen denselben Satz, die franzoesische Zeile ist als ungeprueft gekennzeichnet, und die Dokumentation der Seite widerspricht dem Produkt an keiner Stelle mehr.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container zu PHP-Haelfte | Der Container sendet ein Protokollwort, nie einen Text, den ein Admin liest (T-07-01) |
| Katalogdatei zu Browser | Uebersetzungswerte werden von Nextcloud per `vsprintf` gefuellt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-33 | Spoofing | Container meldet einen Text statt eines Wortes | mitigate | `ENGINE_STATES` bleibt eine geschlossene Menge, `AdminViewService` prueft gegen die gespiegelte Liste und reicht nur bekannte Werte durch |
| T-14-34 | Tampering | Ein Wort auf einer Seite, nicht auf der anderen | mitigate | Gate 1 haelt beide Haelften gegen den importierten Satz; die Reihenfolge der vier Schritte in Task 2 haelt das Fenster kurz |
| T-14-35 | Information Disclosure | Der neue Satz verraet Bestandsgroessen | accept | Der Satz nennt keine Zahl, kein Dokument und keinen Pfad |
| T-14-36 | Availability | Halb aktualisiertes Paar zeigt "Container meldet nichts" | accept | Dokumentiert in `docs/admin-page.md`; REL-02 verlangt den Gleichschritt beider Haelften |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neue Abhaengigkeit in dieser Phase (14-RESEARCH.md Abschnitt 10) |
</threat_model>

<verification>
1. `cd backend && uv run pytest tests/test_admin_ui_contract.py -q` gruen, alle vier Katalog-Gates und das Satz-Gate.
2. `cd backend && uv run pytest -q` (VOLLE Suite) gruen.
3. `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` gruen.
4. `php -l` ueber `php:8.2-cli` im Container fuer die drei PHP-Dateien, Ergebnis in der SUMMARY.
5. Sichtprobe an der laufenden Instanz (Nextcloud 8090): Admin-Seite oeffnen, `FINDLING_EMBED_IDLE_RELEASE_SECONDS=60` gesetzt, nach einer Suche und einer Ruhephase zeigt die Engine-Zeile den neuen Satz, und drei Sekunden spaeter (nach dem ersten Poll des Skripts) steht derselbe Satz noch da.
</verification>

<success_criteria>
- `ENGINE_STATES` hat sechs Woerter, `engine_state()` meldet `unloaded` nur nach einer Freigabe.
- Beide Haelften der Seite bilden das Wort auf denselben Satz ab.
- Sechs Kataloge, 199 Schluessel, franzoesischer Wortlaut vorhanden und als ungeprueft ausgewiesen.
- `docs/admin-page.md` stimmt wieder.
- Alle sechs Gate-Stufen gruen.
</success_criteria>

<output>
Create `.planning/phases/14-modell-entladung-im-leerlauf/14-09-SUMMARY.md` when done
</output>
