---
phase: 14-modell-entladung-im-leerlauf
plan: 11
type: execute
wave: 7
depends_on: [14-07, 14-08, 14-09, 14-10]
files_modified:
  - docs/embeddings.md
  - docs/performance.md
  - docs/runbook-messbox.md
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03]
gated_by: "14-02. Faellt dort E1, wird dieser Plan nicht ausgefuehrt."

must_haves:
  truths:
    - "Ein Admin findet an einer Stelle, was der Schalter tut, was er kostet und wie er ihn wieder ausschaltet"
    - "Die Messgroesse heisst Rueckkehr zur Grundlast nach einem Indexlauf und nirgends Grundlast minus X"
    - "Das Runbook der Phase 15 kennt den A/B-Schalter und die Nebenwirkung auf die Diagnose-Route"
    - "Der Bodensatz, der nie zurueckkommt, steht in der Dokumentation"
  artifacts:
    - path: "docs/embeddings.md"
      provides: "Der Abschnitt zur Entladung im Leerlauf, aus Admin-Sicht"
      contains: "FINDLING_EMBED_IDLE_RELEASE_SECONDS"
    - path: "docs/performance.md"
      provides: "Die Messgroesse, der Bodensatz und die Zahl aus dem Vorprueflauf"
      contains: "Rueckkehr zur Grundlast"
    - path: "docs/runbook-messbox.md"
      provides: "A/B-Schritt fuer den Schalter und die Aufwaermregel vor einer Kaltmessung"
      contains: "FINDLING_EMBED_IDLE_RELEASE_SECONDS"
  key_links:
    - from: "docs/runbook-messbox.md"
      to: "backend/src/findling/api/diagnose.py"
      via: "der Warnsatz, dass ein Diagnoseaufruf den Container aufwaermt"
      pattern: "ranked_sides"
---

<objective>
Drei Dokumente werden nachgezogen: was der Schalter fuer einen Admin bedeutet,
welche Messgroesse gilt, und was die eine bezahlte Box-Anfahrt der Phase 15
darueber wissen muss.

Purpose: Die Nebenwirkung auf `ranked_sides` gehoert in diese Phase und nicht
erst in Phase 15 (14-RESEARCH.md Abschnitt 11): das Messwerkzeug der Phase 12
haengt an dieser Route, und nach einer Entladung meldet sie eine leere
semantische Seite. Wer das erst auf der Box merkt, misst einen Nullwert und
nennt ihn Befund.

Output: ein Admin-Abschnitt, ein Mess-Abschnitt, zwei Runbook-Ergaenzungen.

**Kein Store-Text in diesem Plan.** Store-Texte und README fuer 1.2.0 sind ein
eigener Task mit Owner-Checkpoint in Phase 16 (Owner-Ansage 19.09.2026,
14-CONTEXT.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md
@.planning/phases/14-modell-entladung-im-leerlauf/14-CONTEXT.md
@docs/measurements/2026-09-entladung-vorpruefung/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/embeddings.md, der Schalter aus Admin-Sicht</name>
  <files>docs/embeddings.md</files>
  <read_first>
    - docs/embeddings.md vollstaendig (Aufbau, Tonlage, wo Einstellungen heute erklaert werden)
    - backend/appinfo/info.xml (die Beschreibung der Variablen, damit beide nicht driften)
    - docs/measurements/2026-09-entladung-vorpruefung/README.md (die gemessene Zahl, die hier zitiert wird)
  </read_first>
  <action>
Ergaenze `docs/embeddings.md` um einen Abschnitt "Entladung im Leerlauf".
Deutsche Prosa mit echten Umlauten, Abschnittsueberschrift ohne Umlaute, keine
Em-Dashes, keine Emojis.

Inhalt, knapp und in dieser Reihenfolge:

1. Was passiert: nach der eingestellten Ruhezeit gibt der Container Tokenizer,
   Splitter und Inferenzsitzung frei und gibt die freien Seiten an das
   Betriebssystem zurueck.
2. Wie eingeschaltet wird: `FINDLING_EMBED_IDLE_RELEASE_SECONDS`, Sekunden,
   erlaubt sind 0 (aus, Werksstand) und 60 bis 86400. Vorschlagswert 900. Ein
   Wert ausserhalb faellt auf den Werksstand zurueck und schreibt eine Warnzeile
   mit dem Variablennamen; der Container startet in jedem Fall.
3. Was es kostet: die erste Suche nach einer Ruhephase antwortet mit
   Volltexttreffern statt mit semantischen, und das Modell waermt im Hintergrund
   nach. Die zweite Suche ist wieder vollstaendig. Genau ein Laden je warmem
   Fenster.
4. Was es bringt: die gemessene Rueckgabequote aus dem Vorprueflauf, mit Datum,
   Architektur und Verweis auf `docs/measurements/2026-09-entladung-vorpruefung/`.
   **Eine Zahl, nicht drei** (Owner-Regel zu kurzen Produkttexten gilt fuer
   Store-Texte; hier ist mehr erlaubt, aber die Zahl bekommt ihre Maschine
   daneben).
5. Was ein Admin auf der Seite sieht: der Zustand `unloaded` und sein Satz, mit
   Verweis auf `docs/admin-page.md`.
6. Wann man es NICHT einschaltet: auf einer Box, die dauerhaft indexiert, und
   auf einer Instanz, auf der die Suche der Hauptweg ist und die Nutzer jede
   erste Suche nach einer Pause als langsam erleben wuerden.
  </action>
  <verify>
    <automated>grep -c 'FINDLING_EMBED_IDLE_RELEASE_SECONDS' docs/embeddings.md && python -c "import sys,pathlib;bad=[p for p in ['docs/embeddings.md'] if chr(8212) in pathlib.Path(p).read_text(encoding='utf-8') or chr(8211) in pathlib.Path(p).read_text(encoding='utf-8')];print('dash in',bad) or sys.exit(1 if bad else 0)" && cd backend && uv run pytest tests/test_store_metadata.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Abschnitt nennt den Variablennamen, die erlaubten Werte 0 und 60 bis 86400 und den Vorschlagswert 900, alle identisch zu `config.py` und `info.xml`.
    - Der Abschnitt nennt die gemessene Rueckgabequote mit Datum und Architektur und verlinkt das Messverzeichnis.
    - Der Abschnitt nennt `unloaded` und verweist auf `docs/admin-page.md`.
    - Kein U+2014, kein U+2013, kein Emoji.
  </acceptance_criteria>
  <done>Ein Admin kann den Schalter einschalten, verstehen und wieder ausschalten, ohne in den Quelltext zu sehen.</done>
</task>

<task type="auto">
  <name>Task 2: docs/performance.md, die Messgroesse und der Bodensatz</name>
  <files>docs/performance.md</files>
  <read_first>
    - docs/performance.md vollstaendig (wo die Grundlastzahlen heute stehen und wie sie belegt sind)
    - docs/measurements/2026-09-grundlast-fein/README.md (die 103,2 MB und warum sie bereits ohne Modell und Cutter gemessen sind)
    - .planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md 3.4 Punkt 3 und Pitfall 5
  </read_first>
  <action>
Ergaenze `docs/performance.md` um einen Abschnitt zur Entladung, der drei Dinge
festhaelt und einen Fehler ausschliesst.

1. **Die Messgroesse heisst "Rueckkehr zur Grundlast nach einem Indexlauf".**
   Der Absatz schreibt aus, warum sie nicht "Grundlast minus X" heissen darf: die
   Grundlast von 103,2 MB ist seit dem faulen Bau von Plan 07-03 bereits **ohne**
   Modell und Cutter gemessen, eine Differenz zu ihr waere erfunden. Das
   Warnzeichen steht daneben: eine Zahl, die hoeher ist als die gemessene
   Differenz zwischen "vor der Entladung" und "nach der Entladung".
2. **Der Aktivierungsspeicher.** Der erste `run` der Sitzung legt einen grossen
   Posten an, der ueber weitere Laeufe nicht mehr waechst und der erst mit der
   Entladung zurueckkommt, obwohl `enable_cpu_mem_arena=False` gesetzt ist. Das
   ist die quantitative Begruendung fuer Punkt 1: nach einem Indexlauf steht
   genau dieser Posten im Container.
3. **Der Bodensatz.** Die Modulimporte von onnxruntime und numpy kommen nie
   zurueck, weil die Module geladen bleiben. Die Entladung fuehrt also nicht auf
   die Grundlast eines Containers, der nie eingebettet hat, sondern auf diese
   plus den Bodensatz. Die gemessene Groesse aus dem Vorprueflauf steht dabei,
   mit ihrer Architektur.

Dazu ein Satz, dass die Ladezeit nach einer Entladung (mit und ohne warmen
Seitencache) in Phase 15 auf der Box gemessen wird und bis dahin keine Zahl in
diesem Dokument hat.
  </action>
  <verify>
    <automated>grep -c 'Grundlast' docs/performance.md && grep -qi 'bodensatz\|kommt nie zurueck\|kommt nie zurueck' docs/performance.md && python -c "import sys,pathlib;bad=[p for p in ['docs/performance.md'] if chr(8212) in pathlib.Path(p).read_text(encoding='utf-8') or chr(8211) in pathlib.Path(p).read_text(encoding='utf-8')];print('dash in',bad) or sys.exit(1 if bad else 0)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Der Abschnitt nennt die Messgroesse woertlich und die verbotene Alternative samt Begruendung.
    - Der Bodensatz ist mit Zahl und Architektur benannt.
    - Es steht keine Ladezeit in Millisekunden fuer die Box in diesem Dokument; stattdessen der Verweis auf Phase 15.
    - Kein U+2014, kein U+2013.
  </acceptance_criteria>
  <done>Die Zahl, die spaeter im Store-Text landen soll, kann aus diesem Dokument nicht falsch abgeschrieben werden.</done>
</task>

<task type="auto">
  <name>Task 3: Das Runbook der Box-Anfahrt</name>
  <files>docs/runbook-messbox.md</files>
  <read_first>
    - docs/runbook-messbox.md Abschnitt 6 (Vergleichbarkeitsbedingungen, protokollpflichtig) und Abschnitt 7 (Messreihenfolge mit Abbruchpfaden)
    - docs/runbook-messbox.md Abschnitt 5 (Zustandspruefung mit Abbruchbedingung)
    - .planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md Pitfall 9 und Abschnitt 11 (Zeile Runbook Phase 15)
    - backend/src/findling/api/diagnose.py (die Route, um die es geht)
  </read_first>
  <action>
Zwei Ergaenzungen in `docs/runbook-messbox.md`, beide in der Bauform der
bestehenden Abschnitte (Ueberschriften ohne Umlaute, Fliesstext mit echten
Umlauten, keine Werte im Klartext, Platzhalter mit einem Satz zur Herkunft).

1. **Abschnitt 6 (Vergleichbarkeitsbedingungen):** die Stellung von
   `FINDLING_EMBED_IDLE_RELEASE_SECONDS` wird protokollpflichtig, wie das
   Cron-Intervall es seit 12-06 ist. Eine Pflichtzeile mit dem Namen der
   Variablen und ihrem Wert je Messschritt; ein Lauf ohne diese Zeile gilt als
   unvollstaendig. Begruendung im Text: die A/B-Messung der Wiederaufwaerm-Kosten
   ist genau der Vergleich zweier Stellungen, und eine Zahl ohne ihre Stellung
   ist keine Haelfte des Vergleichs.

2. **Abschnitt 7 (Messreihenfolge):** ein Messschritt "Wiederaufwaerm-Kosten,
   A/B" mit seinem Abbruchpfad und seiner Fortsetzung der Exit-Code-Reihe. Der
   Exit-Code-Katalog dieses Laufverzeichnisses steht laut 12-08 bei 28; die neuen
   Codes setzen bei 29 fort. Der Schritt beschreibt:
   - Messung mit `0` (aus) und mit dem Vorschlagswert, je mit warmem und kaltem
     Seitencache, in einer festen Reihenfolge.
   - **Die Aufwaermregel**, als eigener, fett gesetzter Warnsatz: ein Aufruf der
     Diagnose-Route (`ranked_sides`) LAEDT das Modell, weil diese Route
     ausdruecklich laden darf. Vor einer Kaltmessung darf sie deshalb nicht
     aufgerufen werden. Wer die Fremdbestands-Vorpruefung aus Phase 12 fahren
     will, faehrt sie NACH den Kaltmessungen oder nimmt den Aufwaermeffekt ins
     Protokoll.
   - Der zweite Teil derselben Falle: nach einer Entladung meldet die
     Diagnose-Route eine vollstaendige semantische Seite (weil sie laedt), die
     Nutzerrouten dagegen eine leere. Wer die zwei verwechselt, misst zwei
     verschiedene Dinge und nennt sie eine Zahl.
  </action>
  <verify>
    <automated>grep -c 'FINDLING_EMBED_IDLE_RELEASE_SECONDS' docs/runbook-messbox.md && grep -c 'ranked_sides' docs/runbook-messbox.md && ! grep -qi 'archiv' docs/runbook-messbox.md && python -c "import sys,pathlib;bad=[p for p in ['docs/runbook-messbox.md'] if chr(8212) in pathlib.Path(p).read_text(encoding='utf-8') or chr(8211) in pathlib.Path(p).read_text(encoding='utf-8')];print('dash in',bad) or sys.exit(1 if bad else 0)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 6 fuehrt die Stellung des Schalters als protokollpflichtige Groesse, mit dem Satz, dass ein Lauf ohne sie unvollstaendig ist.
    - Abschnitt 7 traegt den A/B-Messschritt mit Abbruchpfad und Exit-Codes ab 29.
    - Die Aufwaermregel zur Diagnose-Route steht als eigener Warnsatz und nennt `ranked_sides`.
    - Kein Wert im Klartext ausser der bereits committeten Snapshotkennung; Adressen und Instanzkennungen bleiben Platzhalter.
    - Das Wort "Archiv" kommt in der Datei nicht vor (Vokabular-Gate, `docs/` ist oeffentlich).
    - Kein U+2014, kein U+2013.
  </acceptance_criteria>
  <done>Die eine bezahlte Anfahrt kann das A/B fahren, ohne unterwegs eine Regel zu erfinden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/` zu Oeffentlichkeit | Alles in diesem Verzeichnis ist oeffentlich lesbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-41 | Information Disclosure | Runbook nennt Adressen oder Kennungen | mitigate | Platzhalter mit einem Satz zur Herkunft, wie in Abschnitt 7 des Runbooks seit 12-07 |
| T-14-42 | Repudiation | Eine Ersparniszahl ohne ihre Maschine | mitigate | Jede Zahl in `embeddings.md` und `performance.md` traegt Datum und Architektur und verweist auf das Messverzeichnis |
| T-14-43 | Availability | Messlauf misst einen Nullwert und nennt ihn Befund | mitigate | Die Aufwaermregel zur Diagnose-Route steht als Warnsatz im Runbook |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neue Abhaengigkeit in dieser Phase (14-RESEARCH.md Abschnitt 10) |
</threat_model>

<verification>
1. `cd backend && uv run pytest tests/test_store_metadata.py tests/test_measurement_scripts.py -q` gruen.
2. Keine der drei Dateien traegt U+2014 oder U+2013 (Pruefung ueber einen Python-Einzeiler mit `chr(8212)` und `chr(8211)`).
3. Die drei genannten Zahlen (0, 60 bis 86400, 900) stimmen byteweise mit `config.py` und `info.xml` ueberein.
4. Kein Store-Text und kein README wurde angefasst: `git diff --name-only` nennt genau die drei Dateien der Frontmatter.
</verification>

<success_criteria>
- Admin-, Mess- und Runbook-Sicht sind nachgezogen.
- Die Messgroesse ist festgeschrieben und die verbotene Alternative benannt.
- Die Nebenwirkung auf `ranked_sides` steht im Runbook, bevor die Box angefahren wird.
</success_criteria>

<output>
Create `.planning/phases/14-modell-entladung-im-leerlauf/14-11-SUMMARY.md` when done
</output>
