---
phase: 14-modell-entladung-im-leerlauf
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - docs/measurements/2026-09-entladung-vorpruefung/README.md
  - docs/measurements/2026-09-entladung-vorpruefung/rohdaten/
autonomous: false
requirements: [MEM-04]
gate_role: "DAS TOR DER PHASE. Faellt E1 aus 00-ablauf.md, endet Phase 14 hier mit 'gemessen, Ergebnis negativ'. Die Plaene 14-03 bis 14-12 werden dann NICHT ausgefuehrt."

must_haves:
  truths:
    - "Die RSS-Rueckgabe ist auf aarch64 gegen das ausgelieferte Image gemessen, nicht emuliert und nicht geschaetzt"
    - "Das Urteil steht gegen die vor dem Lauf notierte Erwartung und nicht gegen eine nachtraeglich gewaehlte Zahl"
    - "Ein negatives Ergebnis ist dokumentiert statt ausgeliefert"
    - "Der Owner hat den Ausgang gesehen und gibt den Rest der Phase frei oder nicht"
  artifacts:
    - path: "docs/measurements/2026-09-entladung-vorpruefung/README.md"
      provides: "Bericht mit Maschine, Rohzahlen, Urteil je Erwartung und Folge fuer die Phase"
      contains: "Urteil"
    - path: "docs/measurements/2026-09-entladung-vorpruefung/rohdaten/machine.txt"
      provides: "Die Maschine, gegen die jede Zahl dieses Berichts steht"
      contains: "runner="
    - path: "docs/measurements/2026-09-entladung-vorpruefung/rohdaten/rss-rueckgabe-aarch64.txt"
      provides: "Die fuenf Zyklen des Zielastes, roh"
      contains: "median_rueckgabe_prozent="
  key_links:
    - from: "docs/measurements/2026-09-entladung-vorpruefung/README.md"
      to: "docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md"
      via: "Urteil je Erwartung E1 bis E4"
      pattern: "E1"
---

<objective>
Der Vorprueflauf wird gefahren, sein Ergebnis wird gegen die in 14-01 notierte
Erwartung gestellt, und der Owner entscheidet am Ausgang, ob die Phase
weitergeht.

Purpose: Erfolgskriterium 1 der Phase und MEM-04. Dieser Plan ist das Tor: er
gibt Wave 3 und alles danach frei oder beendet die Phase mit einer
dokumentierten Absage. 14-CONTEXT.md sagt es woertlich: "sein dokumentiertes
Ergebnis gibt den Rest frei".

Output: ein Messbericht mit Rohdaten und ein Owner-Entscheid.

**Kein Produktcode in diesem Plan.** Wer hier anfaengt, `release()` zu bauen,
hat das Tor bereits umgangen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md
@.planning/phases/14-modell-entladung-im-leerlauf/14-CONTEXT.md
@docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md
@docs/measurements/2026-09-grundlast-fein/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Den Lauf fahren und die Rohdaten holen</name>
  <files>docs/measurements/2026-09-entladung-vorpruefung/rohdaten/</files>
  <read_first>
    - .github/workflows/measure.yml (Eingaben des Dispatch, Name des Artefakts, Ausgabedateien)
    - docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md (die Erwartung, gegen die spaeter geurteilt wird)
    - docs/measurements/2026-09-grundlast-fein/rohdaten/ (wie Rohdaten dieses Projekts abgelegt sind)
  </read_first>
  <action>
Fahre den Lauf ueber die GitHub-CLI, die auf dieser Maschine unter
`gh 2.92.0` vorhanden ist.

1. Sicherstellen, dass Schritt E auf `main` steht: der Workflow laeuft gegen den
   Stand des Standardzweigs. Ist der Commit aus 14-01 noch nicht auf main, wird er
   zuerst gemergt; ein Dispatch gegen einen Zweig ohne Schritt E misst nichts.
2. `gh workflow run measure.yml -f image_ref=dev` und die Laufnummer aus
   `gh run list --workflow=measure.yml --limit 1 --json databaseId,status` lesen.
3. Auf beide Matrixaeste warten (`gh run watch <id>`), Abbruch nach dem im
   Workflow gesetzten `timeout-minutes: 40` je Ast.
4. Artefakte holen: `gh run download <id>` legt `welle0-arm64/` und
   `welle0-amd64/` ab.
5. Ablegen unter `docs/measurements/2026-09-entladung-vorpruefung/rohdaten/`:
   `machine.txt` (vom arm64-Ast), `rss-rueckgabe-aarch64.txt`,
   `rss-rueckgabe-x86_64.txt`, dazu `machine-x86_64.txt`. Die uebrigen Dateien
   des Artefakts (Messungen A bis D) gehoeren nicht in dieses Verzeichnis: sie
   sind nicht die Frage dieses Laufs und ihre Zahlen wuerden neben den
   bestehenden Berichten stehen, ohne deren Vergleichbarkeitsbedingungen zu
   erfuellen.
6. Ist der `role: target`-Ast rot, wird NICHT interpretiert: der Fehlertext
   kommt in den Bericht und der Lauf wird einmal wiederholt. Bleibt er rot, ist
   das ein eigener Befund und geht als solcher an den Owner-Checkpoint.

Wenn `gh` keinen Zugang hat (fehlender Scope, Konto nicht verbunden), ist das ein
Authentifizierungs-Tor: melden, nicht umgehen, und keinesfalls durch eine lokale
qemu-Messung ersetzen. Emulierte aarch64-Zahlen sind laut 14-RESEARCH.md 3.3
ausdruecklich nicht zitierfaehig.
  </action>
  <verify>
    <automated>test -s docs/measurements/2026-09-entladung-vorpruefung/rohdaten/rss-rueckgabe-aarch64.txt && grep -c '^zyklus=' docs/measurements/2026-09-entladung-vorpruefung/rohdaten/rss-rueckgabe-aarch64.txt | grep -qx 5 && grep -q 'aarch64' docs/measurements/2026-09-entladung-vorpruefung/rohdaten/machine.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `rohdaten/rss-rueckgabe-aarch64.txt` traegt genau fuenf `zyklus=`-Zeilen und je eine Zeile `median_rueckgabe_prozent=` und `zyklus1_minus_zyklus5_punkte=`.
    - `rohdaten/machine.txt` nennt `runner=ubuntu-24.04-arm`, `role=target`, `arch=aarch64`, `digest=sha256:...` und `commit=`.
    - Der Vergleichsast x86_64 liegt ebenfalls als Rohdatei vor.
    - Keine Datei der Messungen A bis D wurde in dieses Verzeichnis kopiert.
    - Keine Datei unter `backend/src/` wurde in diesem Task veraendert.
  </acceptance_criteria>
  <done>Die Zahlen der Zielarchitektur liegen roh im Repository, mit ihrer Maschine daneben.</done>
</task>

<task type="auto">
  <name>Task 2: Der Bericht und das Urteil gegen die Erwartung</name>
  <files>docs/measurements/2026-09-entladung-vorpruefung/README.md</files>
  <read_first>
    - docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md (E1 bis E4 im Wortlaut)
    - docs/measurements/2026-09-entladung-vorpruefung/rohdaten/rss-rueckgabe-aarch64.txt
    - docs/measurements/2026-09-grundlast-fein/README.md (Tonlage und Aufbau eines Messberichts dieses Projekts)
    - .planning/phases/14-modell-entladung-im-leerlauf/14-RESEARCH.md 3.4 (die drei Schlussfolgerungen, besonders der Import-Bodensatz)
  </read_first>
  <action>
Schreibe `docs/measurements/2026-09-entladung-vorpruefung/README.md`. Deutsche
Prosa mit echten Umlauten, keine Em-Dashes, keine Emojis, Ueberschriften ohne
Umlaute.

Abschnitte:

1. **Geltung.** Datum, Image-Digest, beide Runner, Commit. Ein Satz, dass die
   Erwartung vor dem Lauf feststand, mit Verweis auf `skripte/00-ablauf.md` und
   dem Commit, in dem sie entstanden ist.
2. **Die Zahlen.** Eine Tabelle je Architekturast: Zyklus, `baseline`, `loaded`,
   `after_gc`, `after_trim`, `trim_rc`, `rueckgabe_prozent`. Werte in MB mit
   einer Nachkommastelle, die Rohdatei in KB bleibt daneben verlinkt.
3. **Urteil je Erwartung.** Vier Zeilen, je `E1` bis `E4`, je mit "gehalten" oder
   "gerissen" und der Zahl, an der es haengt. Kein Urteil ohne die Zahl daneben.
4. **Gesamturteil.** Genau einer der drei Ausgaenge aus dem Ablaufdokument, im
   Wortlaut. Bei einem negativen Ausgang steht hier der Satz "gemessen, Ergebnis
   negativ" woertlich, dazu, was das fuer die Phase heisst.
5. **Was diese Messung nicht sagt.** Die Ladezeit in Millisekunden (Vier-Kern-
   Runner, nicht `m7g.large`), die Wiederaufwaerm-Kosten mit kaltem Seitencache
   und das A/B ueber den Schalter. Alle drei liegen in Phase 15.
6. **Der Bodensatz.** Die Differenz zwischen `after_trim` und `baseline`, benannt
   als der Teil, der nie zurueckkommt (Modulimporte von onnxruntime und numpy),
   mit dem Satz, dass der Store-Text ihn nicht verschweigen darf.
7. **Architekturvergleich.** Zielast gegen Vergleichsast in einem Absatz. Die
   Rueckgabequote ist die uebertragbare Groesse, die Zeit ist es nicht.
  </action>
  <verify>
    <automated>grep -q 'E1' docs/measurements/2026-09-entladung-vorpruefung/README.md && grep -qE 'gehalten|gerissen' docs/measurements/2026-09-entladung-vorpruefung/README.md && python -c "import sys,pathlib;bad=[p for p in ['docs/measurements/2026-09-entladung-vorpruefung/README.md'] if chr(8212) in pathlib.Path(p).read_text(encoding='utf-8') or chr(8211) in pathlib.Path(p).read_text(encoding='utf-8')];print('dash in',bad) or sys.exit(1 if bad else 0)" && cd backend && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Bericht nennt E1 bis E4 je einmal mit "gehalten" oder "gerissen" und der tragenden Zahl.
    - Das Gesamturteil ist einer der drei im Ablaufdokument vorgesehenen Ausgaenge, im Wortlaut.
    - Bei negativem Ausgang steht "gemessen, Ergebnis negativ" woertlich im Bericht.
    - Der Abschnitt "Was diese Messung nicht sagt" nennt die Ladezeit, den kalten Seitencache und das A/B.
    - Kein U+2014, kein U+2013, kein Emoji.
  </acceptance_criteria>
  <done>Jede Zahl des Laufs steht neben ihrer Maschine und neben dem Satz, den sie belegt oder widerlegt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner-Tor, der Ausgang des Vorprueflaufs</name>
  <files>-</files>
  <read_first>
    - docs/measurements/2026-09-entladung-vorpruefung/README.md
    - docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md
  </read_first>
  <action>
Halte an und lege dem Owner den Ausgang vor. Nichts wird in diesem Task gebaut,
nichts gefixt und nichts interpretiert: der Bericht steht, und der Entscheid
ueber den Rest der Phase liegt beim Owner. Trage die Antwort danach im Wortlaut
in die SUMMARY und in `.planning/STATE.md` ein.
  </action>
  <what-built>
    Der Vorprueflauf ist auf `ubuntu-24.04-arm` gegen das ausgelieferte Image
    gefahren, fuenf Zyklen, vier Marken je Zyklus. Rohdaten und Bericht liegen im
    Repository, das Urteil steht gegen die vor dem Lauf notierte Erwartung.
  </what-built>
  <how-to-verify>
    1. `docs/measurements/2026-09-entladung-vorpruefung/README.md` lesen,
       Abschnitte "Urteil je Erwartung" und "Gesamturteil".
    2. Gegenprobe: `docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md`
       lesen und pruefen, dass die dort notierten Zahlen (60 Prozent, 10
       Prozentpunkte) nicht nachtraeglich bewegt wurden. `git log --follow` auf
       beide Dateien zeigt, dass das Ablaufdokument vor den Rohdaten committet ist.
    3. Entscheiden:
       - **freigegeben**: die Plaene 14-03 bis 14-12 werden ausgefuehrt.
       - **negativ**: die Phase endet hier. Der Bericht bleibt, MEM-04 gilt als
         erfuellt (gemessen), MEM-01 bis MEM-03 und MEM-05 wandern mit dem
         dokumentierten Grund in die Wiedervorlage; es wird kein Produktcode
         gebaut.
       - **teilweise**: E1 gehalten, E2 bis E4 gerissen. Owner sagt, ob gebaut
         wird und was in den Bericht als Vorbehalt gehoert.
  </how-to-verify>
  <resume-signal>Antworte "freigegeben", "negativ" oder "teilweise: <Auflage>"</resume-signal>
  <verify>
    <human-check>Der Owner hat den Bericht gelesen und einen der drei Ausgaenge genannt.</human-check>
    <automated>git log --format=%ct -1 -- docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md</automated>
  </verify>
  <acceptance_criteria>
    - Der Owner hat geantwortet, und die Antwort steht im Wortlaut in der SUMMARY.
    - Bei "negativ": kein Produktcode ist entstanden, `git diff --stat` gegen den Phasenanfang nennt keine Datei unter `backend/src/` oder `php/`, und `.planning/STATE.md` traegt den Ausgang mit dem Satz "gemessen, Ergebnis negativ".
    - Bei "freigegeben" oder "teilweise": die SUMMARY nennt die freigegebenen Plaene (14-03 bis 14-12) und jede Auflage einzeln.
    - `git log --format=%ci -1` auf `skripte/00-ablauf.md` ist frueher als auf `rohdaten/rss-rueckgabe-aarch64.txt`; der Owner hat das am Checkpoint gesehen.
  </acceptance_criteria>
  <done>Der Ausgang des Tors steht fest und ist schriftlich festgehalten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Artefakt zu Repository | Fremd erzeugte Dateien wandern in einen oeffentlichen Baum |
| Messbericht zu Oeffentlichkeit | `docs/` ist oeffentlich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | Rohdaten im oeffentlichen `docs/` | mitigate | Die Rohdateien tragen nur Zahlen und Kopfzeilen; vor dem Commit wird auf Dateinamen, Pfade und das gesperrte Wort "Archiv" geprueft |
| T-14-06 | Repudiation | Schwelle nachtraeglich bewegt | mitigate | `00-ablauf.md` ist in einem frueheren Commit als die Rohdaten; der Owner prueft das am Checkpoint per `git log` |
| T-14-07 | Tampering | Lauf gegen einen anderen Baum als behauptet | mitigate | `machine.txt` traegt Digest und Commit, beide werden im Bericht zitiert |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Diese Phase installiert kein Paket (14-RESEARCH.md Abschnitt 10) |
</threat_model>

<verification>
1. `rohdaten/rss-rueckgabe-aarch64.txt` traegt fuenf Zyklen und beide Kennzahlzeilen.
2. `README.md` urteilt ueber E1 bis E4 je mit Zahl.
3. `cd backend && uv run pytest tests/test_measurement_scripts.py -q` gruen.
4. `git log --format=%H -1 -- docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md` bezeichnet einen Commit, der aelter ist als der der Rohdaten.
5. Der Owner hat geantwortet.
</verification>

<success_criteria>
- Die Zahl der Zielarchitektur liegt vor, gemessen gegen das ausgelieferte Image.
- Das Urteil steht gegen die vorher notierte Erwartung.
- Der Owner hat freigegeben, abgesagt oder mit Auflage freigegeben.
- Bei Absage ist kein Produktcode entstanden.
</success_criteria>

<output>
Create `.planning/phases/14-modell-entladung-im-leerlauf/14-02-SUMMARY.md` when done
</output>
