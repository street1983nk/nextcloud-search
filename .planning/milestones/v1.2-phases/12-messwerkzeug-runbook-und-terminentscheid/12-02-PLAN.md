---
phase: 12-messwerkzeug-runbook-und-terminentscheid
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md
  - .github/workflows/deploy-harp.yml
autonomous: false
requirements: [HART-03]

must_haves:
  truths:
    - "Der stable35-Fenster-Entscheid ist am 16.09.2026 getroffen und mit Begründung dokumentiert"
    - "Der Entscheid ist an zwei Stellen sichtbar: im YAML-Kommentar von deploy-harp.yml und in der Entscheidungsnotiz"
    - "Der Matrixeintrag server-version: stable35 steht nach dem Vollzug weiterhin in der Matrix"
    - "Bei Option a belegt eine Laufnummer den grünen deploy-harp-Lauf gegen stable35; bei Option b steht ein neuer RE-CHECK-Termin mit benannter Nachfolge-Adresse"
  artifacts:
    - path: ".planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md"
      provides: "gefüllter Abschnitt Vollzug am 16.09.2026 mit Zweig, Beleg und Datum"
      contains: "greifender Zweig"
    - path: ".github/workflows/deploy-harp.yml"
      provides: "fortgeschriebener stable35-Eintrag nach dem greifenden Zweig"
      contains: "server-version: stable35"
  key_links:
    - from: ".github/workflows/deploy-harp.yml"
      to: ".planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md"
      via: "Aktenzeichen der Entscheidungsnotiz im YAML-Kommentar"
      pattern: "12-STABLE35-ENTSCHEID"
    - from: ".github/workflows/deploy-harp.yml"
      to: "backend/tests/test_lockstep_versions.py"
      via: "Matrix deckt weiterhin jede Version des Fensters 33 bis 35 ab"
      pattern: "server-version: stable3"
---

<objective>
Der fristgebundene stable35-Fenster-Entscheid (HART-03) wird am 16.09.2026
vollzogen: Releasestand lesen, greifenden Zweig bestimmen, den in Plan 12-01
vorformulierten Ersatztext einsetzen, Entscheidungsnotiz füllen, Owner
bestätigen lassen.

Purpose: Erfolgskriterium 5 der Phase. Die Frist ist der 16.09.2026. Dieser Plan
enthält bewusst KEINE Entwurfsarbeit; er setzt nur ein, was Plan 12-01 fertig
hingelegt hat.

Output: fortgeschriebener `stable35`-Eintrag in `.github/workflows/deploy-harp.yml`
und gefüllter Abschnitt "Vollzug am 16.09.2026" in der Entscheidungsnotiz.

**Terminbindung:** Dieser Plan wird am 16.09.2026 ausgeführt, nicht früher.
Task 1 bricht vor diesem Datum fail-closed ab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md

<interfaces>
Aus Plan 12-01 übernommen, hier nur die Anknüpfungspunkte:

- Die Entscheidungsnotiz trägt `### Ersatztext Option a` und
  `### Ersatztext Option b` als kopierfertige YAML-Blöcke sowie den leeren
  Abschnitt `## Vollzug am 16.09.2026`.
- Zu ersetzen ist ausschliesslich der Bereich zwischen der Kommentarzeile
  `# stable35 exists in both repositories this job checks out` und der Zeile
  `runner: ubuntu-24.04` unterhalb von `tolerate-failure:` in
  `.github/workflows/deploy-harp.yml` (heute Zeilen 211 bis 273).
- Der Matrixeintrag `- server-version: stable35` mit `php-version: '8.3'` und
  `runner: ubuntu-24.04` bleibt in JEDEM Zweig stehen.
  `backend/tests/test_lockstep_versions.py:387` verlangt, dass die Matrix jede
  Version des Fensters 33 bis 35 abdeckt.
- Kommando für den Releasestand:
  `gh api repos/nextcloud/server/releases --jq '.[] | "\(.tag_name) prerelease=\(.prerelease) \(.published_at)"' | head -12`
- Kommando für den Beweislauf (nur Option a):
  `gh workflow run deploy-harp.yml` und danach `gh run list --workflow deploy-harp.yml --limit 3`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datum prüfen, Releasestand lesen, greifenden Zweig bestimmen</name>
  <files>.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md</files>
  <read_first>
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md (Option a, Option b, die Vollzugs-Checkliste und die Feldliste des Abschnitts Vollzug)
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md (D-01, D-02, D-03: die Entscheidungsregel im Wortlaut)
  </read_first>
  <action>
    Erster Schritt, fail-closed: Prüfe das heutige Datum. Ist es vor dem
    2026-09-16, brich diesen Plan mit einer klaren Meldung ab und melde ihn
    als nicht ausführbar zurück. Der Entscheid darf nicht vor dem Stichtag
    getroffen werden, weil der Releasestand sich bis dahin ändert.

    Zweiter Schritt: Lies den Releasestand mit
    `gh api repos/nextcloud/server/releases` und werte aus, ob eine
    35er-Marke mit `prerelease=false` existiert.

    Dritter Schritt: Bestimme den greifenden Zweig nach der Regel aus D-01 bis
    D-03. Existiert eine finale 35er-Freigabe, greift Option a. Existiert keine,
    greift Option b. Der Release-Status allein entscheidet NICHT über den
    Vollzug von Option a: nach D-02 gehört der grüne deploy-harp-Lauf dazu, der
    in Task 2 folgt.

    Vierter Schritt: Fülle den Abschnitt `## Vollzug am 16.09.2026` der
    Entscheidungsnotiz mit Datum, dem abgesetzten `gh`-Kommando, der Antwort als
    Tabelle (Tag, prerelease, published_at, die obersten Marken), dem greifenden
    Zweig (`a` oder `b`) und der Begründung in einem Satz. Die Felder
    "Laufnummer des Beweislaufs" und "vollzogen am / durch Plan" bleiben für
    Task 2 offen.

    Keine Anmeldedaten und keine Tokenwerte in die Notiz. Git-Identität NICHT
    per `-c` übersteuern.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md && grep -q 'greifender Zweig' "$f" && grep -Eq 'greifender Zweig[^a-z]*(a|b)\b' "$f" && grep -q 'gh api repos/nextcloud/server/releases' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Der Lauf findet am oder nach dem 2026-09-16 statt; ein früherer Lauf bricht mit einer Meldung ab und ändert keine Datei
    - Der Abschnitt `## Vollzug am 16.09.2026` trägt ein ausgefülltes Datumsfeld
    - Der Abschnitt trägt die Ausgabe von `gh api repos/nextcloud/server/releases` als Tabelle mit mindestens drei Marken, je mit `prerelease`-Kennzeichen und Datum
    - Das Feld "greifender Zweig" trägt genau einen der Werte `a` oder `b`, mit einer Begründung in einem Satz
    - Die Notiz enthält keinen Tokenwert und keine Anmeldedaten
  </acceptance_criteria>
  <done>Der greifende Zweig steht belegt in der Notiz; die Entscheidung stützt sich auf eine gelesene API-Antwort und nicht auf den Kalender.</done>
</task>

<task type="auto">
  <name>Task 2: Ersatztext einsetzen und, bei Option a, den Beweislauf führen</name>
  <files>.github/workflows/deploy-harp.yml, .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md</files>
  <read_first>
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md (der in Task 1 bestimmte Zweig und der zugehörige fertige Ersatztext)
    - .github/workflows/deploy-harp.yml (Zeilen 200 bis 280: der zu ersetzende Bereich im Ist-Zustand)
    - backend/tests/test_lockstep_versions.py (die Zusicherung um Zeile 387: die Matrix muss jede Version des Fensters abdecken)
  </read_first>
  <action>
    Setze den in Plan 12-01 vorformulierten Ersatztext des greifenden Zweiges
    wortwörtlich in `.github/workflows/deploy-harp.yml` ein und ersetze die
    Platzhalter durch die in Task 1 gelesenen Tatsachen.

    Bei Option a: Der RE-CHECK-Absatz entfällt, `tolerate-failure:` wird auf
    `false` gesetzt, `<FINAL-TAG>` und `<FINAL-DATUM>` werden durch Tag und
    Datum der finalen 35er-Freigabe ersetzt. Starte den Beweislauf nach D-02 mit
    `gh workflow run deploy-harp.yml`, lies die Laufnummer mit
    `gh run list --workflow deploy-harp.yml --limit 3` und warte das Ergebnis ab.
    Trage die Laufnummer für `<RUN-ID>` ein. Ist der Lauf rot, wird das Flag
    NICHT zurückgesetzt: der rote Lauf ist nach dem eigenen Wortlaut des
    bisherigen Kommentars ein Befund. Halte den Befund in der Notiz fest und
    trage ihn als offenen Punkt für Phase 16 ein.

    Bei Option b: `tolerate-failure: true` bleibt. Der bestehende RE-CHECK-Absatz
    wird um den vorformulierten Fortschreibe-Eintrag ergänzt,
    `# RE-CHECK DATE: 2026-09-16.` wird auf den neuen Termin umgeschrieben,
    `<NEUESTE-35-MARKE>` und `<NEUESTE-FREIGABE>` werden durch die gelesenen
    Marken ersetzt, und die neue Nachfolge-Adresse ist die Store-Einreichung der
    Phase 16.

    In BEIDEN Zweigen bleibt der Matrixeintrag `- server-version: stable35` mit
    `php-version: '8.3'` und `runner: ubuntu-24.04` stehen.

    Fülle danach in der Notiz die Felder "Laufnummer des Beweislaufs" (bei
    Option b: "entfällt, Zweig b") und "vollzogen am / durch Plan" (Plan 12-02).

    Prüfe vor dem Fertigmelden `cd backend && uv run pytest tests/test_lockstep_versions.py`
    lokal grün. Keine Em-Dashes im YAML. Git-Identität NICHT per `-c` übersteuern.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "server-version: stable35" .github/workflows/deploy-harp.yml && grep -q "12-STABLE35-ENTSCHEID" .github/workflows/deploy-harp.yml && ! grep -q $'—\|–' .github/workflows/deploy-harp.yml && cd "$(git rev-parse --show-toplevel)/backend" && uv run pytest tests/test_lockstep_versions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy-harp.yml` enthält weiterhin die Zeile `- server-version: stable35` sowie `php-version: '8.3'` und `runner: ubuntu-24.04` in demselben Eintrag
    - Der Kommentar am stable35-Eintrag nennt Datum des Vollzugs, Plan 12-02 und das Aktenzeichen `12-STABLE35-ENTSCHEID.md`
    - Bei Option a: `tolerate-failure: false` steht am stable35-Eintrag, der Kommentar nennt Tag und Datum der finalen 35er-Freigabe sowie eine numerische Laufnummer; kein Platzhalter der Form `<...>` ist übrig
    - Bei Option b: `tolerate-failure: true` steht weiterhin, die Zeile `RE-CHECK DATE:` trägt ein Datum nach dem 2026-09-16, und der Kommentar nennt die Store-Einreichung der Phase 16 als Nachfolge-Adresse; kein Platzhalter der Form `<...>` ist übrig
    - `pytest tests/test_lockstep_versions.py` läuft grün
    - Die Notiz trägt in "vollzogen am / durch Plan" das Vollzugsdatum und `Plan 12-02`
    - `.github/workflows/deploy-harp.yml` enthält weder U+2014 noch U+2013
  </acceptance_criteria>
  <done>Der Entscheid steht an beiden Stellen, mit Beleg, und die Versions-Gleichschritt-Gates sind grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner bestätigt den stable35-Entscheid</name>
  <files>.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md, .github/workflows/deploy-harp.yml</files>
  <read_first>
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md (der in Task 1 und 2 gefüllte Abschnitt "Vollzug am 16.09.2026")
    - .github/workflows/deploy-harp.yml (der stable35-Matrixeintrag nach dem Vollzug)
  </read_first>
  <action>
    Lege dem Owner den vollzogenen Entscheid zur Bestätigung vor: den gefüllten
    Abschnitt "Vollzug am 16.09.2026" der Entscheidungsnotiz und den geänderten
    stable35-Eintrag in `.github/workflows/deploy-harp.yml`, bei Option a
    zusätzlich die Laufnummer des Beweislaufs. Nimm ohne Owner-Freigabe keine
    weitere Änderung an einer der beiden Dateien vor und melde die Phase nicht
    als erfüllt. Benennt der Owner etwas zu Änderndem, wird es in derselben
    Sitzung eingearbeitet und erneut vorgelegt.
  </action>
  <acceptance_criteria>
    - Der Owner hat "freigegeben" geschrieben oder benannt, was zu ändern ist
    - Benannte Änderungen sind eingearbeitet und erneut vorgelegt worden
    - Ohne Freigabe wurde HART-03 nicht als erfüllt gemeldet
  </acceptance_criteria>
  <what-built>
    Der Entscheid ist getroffen und an zwei Stellen dokumentiert: im
    YAML-Kommentar des stable35-Eintrags in `.github/workflows/deploy-harp.yml`
    und im Abschnitt "Vollzug am 16.09.2026" der Entscheidungsnotiz. Bei Option a
    liegt zusätzlich die Laufnummer eines deploy-harp-Laufs gegen stable35 vor.
  </what-built>
  <how-to-verify>
    1. Lies `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md`,
       Abschnitt "Vollzug am 16.09.2026": stimmt der greifende Zweig mit dem
       gelesenen Releasestand überein?
    2. Lies den stable35-Eintrag in `.github/workflows/deploy-harp.yml`: ist der
       Eintrag noch da, und trägt das Flag den erwarteten Wert
       (Option a: `false`, Option b: `true`)?
    3. Nur bei Option a: öffne den genannten Lauf mit
       `gh run view <RUN-ID>` und prüfe, ob der stable35-Ast grün ist. Ist er
       rot, ist das ein Befund und kein Grund, das Flag zurückzusetzen; der
       Befund muss in der Notiz als offener Punkt für Phase 16 stehen.
    4. Nur bei Option b: ist der neue RE-CHECK-Termin so gesetzt, dass er vor
       dem Beginn der Phase-16-Einreichung liegt?
  </how-to-verify>
  <resume-signal>Schreibe "freigegeben" oder benenne, was zu ändern ist</resume-signal>
  <done>Der Owner hat den Entscheid bestätigt; HART-03 gilt erst danach als erfüllt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub-API → Entwicklungsmaschine | `gh api` und `gh workflow run` liefern die Tatsachen, auf denen der Entscheid ruht |
| Entwicklungsmaschine → CI-Konfiguration | Eine Änderung an `deploy-harp.yml` verändert, welche CI-Äste scheitern dürfen |
| Repositorium (öffentlich) ← Entscheidungsnotiz und Workflow-Kommentar | Beide Artefakte sind veröffentlicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | `.github/workflows/deploy-harp.yml`, stable35-Matrixeintrag | mitigate | Der Eintrag darf nur sein Flag verlieren, nie selbst entfernt werden; durchgesetzt durch `backend/tests/test_lockstep_versions.py` als Pflichtlauf im `<verify>` von Task 2 |
| T-12-05 | Repudiation | Entscheid ohne prüfbaren Beleg | mitigate | Pflichtfelder Datum, gelesene API-Antwort, Laufnummer beziehungsweise neuer RE-CHECK-Termin; ein roter Beweislauf wird als Befund festgehalten statt stillschweigend rückgängig gemacht |
| T-12-06 | Information Disclosure | `gh`-Anmeldung | mitigate | `gh` liest sein Token aus der eigenen Konfiguration; kein Token wird in eine Datei, ein Argument oder ein Protokoll geschrieben. Acceptance-Criterion von Task 1 prüft, dass die Notiz keine Anmeldedaten trägt |
| T-12-07 | Elevation of Privilege | CI-Ast wird versehentlich must-be-green, obwohl NC 35 nicht final ist | mitigate | Task 1 bricht vor dem 2026-09-16 fail-closed ab, und der Zweig wird aus der API-Antwort bestimmt und nicht aus dem Kalenderdatum |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket; die Recherche hält Package Legitimacy Audit als nicht anwendbar fest |
</threat_model>

<verification>
- Erfolgskriterium 5 der Phase ist erfüllt: der Entscheid ist bis zum 16.09.2026 getroffen und begründet dokumentiert
- `pytest tests/test_lockstep_versions.py` grün
- Der stable35-Matrixeintrag steht weiterhin in der Matrix
- Keine Em-Dashes in geänderten Dateien
- Repo-Regeln: Git-Identität NICHT per `-c` übersteuern; sequenziell auf main, Basis-SHA gepinnt
</verification>

<success_criteria>
Am 16.09.2026 ist der stable35-Fenster-Entscheid vollzogen, an zwei Stellen
belegt dokumentiert und vom Owner bestätigt. HART-03 ist damit erfüllt, egal
welcher Zweig gegriffen hat.
</success_criteria>

<output>
Create `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-02-SUMMARY.md` when done
</output>
