---
phase: 12-messwerkzeug-runbook-und-terminentscheid
plan: 06
type: execute
wave: 3
depends_on: [12-04, 12-05]
files_modified:
  - docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh
  - docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md
autonomous: true
requirements: [MESS-04, MESS-06]

must_haves:
  truths:
    - "Das Cron-Intervall der Zielinstanz ist Pflichtfeld im Messprotokoll: ein Lauf ohne protokolliertes Intervall bricht ab, statt weiterzulaufen (D-07)"
    - "Die Durchsetzung prüft nicht nur die Konfiguration, sondern auch die Wirkung: der gemessene Abstand zwischen zwei Zulaufscheiben steht im Protokoll (D-08)"
    - "Der Ablaufplan des Laufs schreibt die Erwartung VOR der Messung auf und wird danach nicht mehr angepasst"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh"
      provides: "zweiteiliger, fail-closed durchgesetzter Cron-Check: Konfigurationszweig vorher, Wirkungszweig waehrend"
      contains: "waehrend"
      min_lines: 150
    - path: "docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md"
      provides: "Ablaufplan des v1.2-Laufs: was er misst, Schrittfolge, vorher aufgeschriebene Erwartung, Abbruchbedingungen, was danach geschieht"
      contains: "Die Erwartung, vorher aufgeschrieben"
      min_lines: 110
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh"
      to: "das Messprotokoll der Anfahrt"
      via: "Pflichtzeile cron-intervall-ist, fail-closed nach der tee-Pipeline"
      pattern: "cron-intervall-ist"
    - from: "docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md"
      to: "docs/measurements/2026-09-v12-messung/skripte/"
      via: "Schrittfolge nennt jedes Skript des Laufverzeichnisses mit Rohdatei und Aussage"
      pattern: "98c-sprachfaelle.sh"
---

<objective>
Das Cron-Intervall wird zur durchgesetzten Messbedingung (MESS-06). Der Check ist
ZWEITEILIG: ein Konfigurationszweig vor dem Lauf, der das Intervall setzt,
zurückliest und protokolliert, und ein Wirkungszweig während des Laufs, der den
tatsächlichen Abstand zwischen zwei Zulaufscheiben misst. Dazu entsteht der
Ablaufplan des v1.2-Laufverzeichnisses.

Purpose: Erfolgskriterium 4 der Phase. Der v1.1-Befund war eine Diskrepanz
zwischen Takt und Wirkung: der Systemcron stand NOMINAL auf 5 Minuten, die
tatsächliche Scheibenauslieferung lag bei rund 12 Minuten, und das kostete
5,85 h Leerlauf von 26,6 h. Ein Check, der nur die Konfiguration liest, hätte am
10.09.2026 GRUEN gemeldet, während der Befund vorlag. Eine Runbook-Checkliste
allein reicht nicht (D-08): menschliche Schritte werden vergessen, das war die
v1.1-Falle.

Output: `97-cron-vorpruefung.sh` und `00-ablauf.md` im Laufverzeichnis
`docs/measurements/2026-09-v12-messung/skripte/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-PATTERNS.md

<interfaces>
Der Befund, wörtlich aus `docs/performance.md:158`:

```
der Lauf war 5,85 h von 26,6 h ohne Arbeitsvorrat (194 von 812 Lesungen,
Baseline: 0,10 h), weil der Zulauf am 5-Minuten-Systemcron hing, der auf der
Box nur alle rund 12 Minuten eine Scheibe von etwa 870 Zeilen lieferte,
waehrend die Baseline bis zu 49.601 Zeilen Vorlauf hielt
```

Zweite Zahlenreihe zur selben Sache, aus
`docs/measurements/2026-09-vergleichsmessung-m7g/rohdaten/00-ende.txt:142-143`:
`vorrat=0 in 62 von 325 Lesungen`. Beide Paare ergeben rund 24 beziehungsweise
19 Prozent und stammen sehr wahrscheinlich aus zwei verschiedenen
Ableseintervallen (Statusbeobachter alle 120 s gegen Waechter alle 300 s).
Das ist Annahme A1 und ausdruecklich nicht gesichert. Der Ablaufplan und das
Protokoll benennen deshalb, WELCHE Reihe die Protokollzahl liefert, statt eine
Scheingenauigkeit zu erzeugen.

Analoge im Repositorium:

| Zweig | Analog | Was daraus kommt |
|---|---|---|
| Konfigurationszweig und Abbruch | `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/93-nullstand.sh` | Kopf-Muster mit nummerierter Quellenliste, Frist gegen die langsamste Uhr als Variable mit Begründung, Urteil in eine Datei, Abbruch NACH der Pipeline |
| Wirkungszweig | `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/96b-waechter.sh` | Beobachtungsschleife mit Deckel und gezählten Lesungen, aus der die Zahl "vorrat=0 in N von M Lesungen" entsteht |
| Statuszeilen alle 120 s | `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/96d-statusbeobachter.py` | die feinere der beiden Ableseintervall-Reihen |
| Ablaufplan | `docs/measurements/2026-09-werkzeugfixe/skripte/00-ablauf.md` (133 Zeilen) | exakt dieselbe Gattung, Gliederung wörtlich übernehmbar |

Muster "Urteil in eine Datei, Abbruch nach der Pipeline", `93-nullstand.sh:165-176`:

```sh
} 2>&1 | tee "$ZIEL"

# Read back out of the file rather than out of the pipeline, because the exit
# status of a pipeline that ends in tee is the status of tee.
vorrat=$(cat "$WORK/vorrat" 2>/dev/null || echo 0)
if [ "$vorrat" -le 0 ]; then
    echo "93-nullstand: the work stock is still nought after $FRIST seconds" >&2
    exit 10
fi
```

Frist gegen die langsamste Uhr, `93-nullstand.sh:47-50`:

```sh
# The frist, in seconds, against the slowest clock involved. 300 s poller backoff
# plus two rounds of the five minute system cron, so 360 is the floor and not the
# target.
FRIST="${FRIST:-360}"
```

`occ`-Wrapper ohne Passwort, `93-nullstand.sh:57-59`, und die Funktion, die den
Arbeitsvorrat aus EINEM Aufruf liest, `93-nullstand.sh:61-67`:

```sh
vorrat_von() {
    awk '/^Work stock/ {found = 1; next}
         found && /^  (scheduled|handed to the worker)/ {sum += $NF}
         END {print sum + 0}' "$1"
}
```

Was über den Cron bekannt ist: All-in-One ruft `cron.php` alle fünf Minuten,
und der ERSTE Auftrag der App reiht noch nichts ein; deshalb sind zwei Runden
abzuwarten (`93-nullstand.sh:25-26`, `96-volllauf.sh:38`, `98b:117`).

Fortsetzung des Exit-Code-Katalogs dieses Laufverzeichnisses (15 bis 24 sind in
`98c-sprachfaelle.sh` vergeben):

```
25 die Cron-Konfiguration war auf keiner der bekannten Quellen lesbar
26 das gelesene Intervall weicht vom Soll ab
27 der Wirkungszweig wurde nicht gefahren oder nicht protokolliert
28 der gemessene Scheibenabstand liegt ueber dem Deckel
```

Gliederung des Ablaufplan-Analogs `2026-09-werkzeugfixe/skripte/00-ablauf.md`:

1. Kopf mit zwei Sätzen vorweg: was der Lauf beweist und was nicht, plus Deckel
   und Owner-Freigabe (`:9-20`)
2. `## 1. Was dieser Lauf misst` mit Befundtabelle (`:30-33`)
3. `## 2. Die Schrittfolge` als Tabelle `Nr | Schritt | Rohdatei | Die Aussage,
   an der der Schritt haengt` (`:54-63`)
4. `## 3. Die Erwartung, vorher aufgeschrieben` als Tabelle `Nr | Erwartung |
   Woher sie kommt`, E1 bis E8 (`:84-93`)
5. `## 4. Woran der Lauf abgebrochen wird` als Tabelle `Bedingung | Wo sie
   greift | Folge` mit den Rückgabewerten (`:107-117`)
6. `## 5. Nach dem Lauf` (`:125-133`)

Der Satz, der die Datei trägt (`:79-82`), wörtlich zu übernehmen:

```
Dieser Abschnitt ist der Grund, warum diese Datei vor der Anfahrt entsteht. Was
hier steht, wird nach der Messung **nicht** angepasst.
```

Schlusszeile zum Vokabular-Gate (`:132-133`), ebenfalls wörtlich zu übernehmen:

```
Vor dem Fertigmelden laeuft das Vokabular-Gate lokal ueber die Dateien dieses
Verzeichnisses, wie ueber jede nach aussen sichtbare Datei dieses Projekts.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Konfigurationszweig des Cron-Checks, fail-closed</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh</files>
  <read_first>
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/93-nullstand.sh (vollständig: Kopf mit nummerierter Quellenliste, Frist-Variable mit Begründung, occ-Wrapper, vorrat_von, Urteil in eine Datei und Abbruch nach der Pipeline)
    - docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh (aus Plan 12-05: der bereits vergebene Exit-Code-Katalog 15 bis 24, damit die neuen Codes ihn fortsetzen und nicht verschieben)
    - docs/performance.md (Zeile 158 und Umgebung: der Befund mit den Zahlen 5,85 h von 26,6 h und 194 von 812 Lesungen)
  </read_first>
  <action>
    Lege `docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh` an.
    Shebang exakt `#!/bin/sh`, LF-Zeilenenden. Aufrufform:
    `97-cron-vorpruefung.sh <vorher|waehrend>`. Ohne Argument oder mit einem
    unbekannten Argument druckt das Skript seine Benutzung und endet mit 2.

    Kopf nach dem Muster von `93-nullstand.sh:1-16`: erst warum dieser Schritt
    überhaupt existiert, dann eine nummerierte Liste der Quellen, die dieser
    Schritt liest. Der Kopf muss den Befund benennen und die Lesart, die den
    zweiten Zweig erzwingt: der Systemcron stand NOMINAL auf 5 Minuten, die
    tatsächliche Scheibenauslieferung lag bei rund 12 Minuten, und ein Check,
    der nur die Konfiguration liest, hätte am 10.09.2026 grün gemeldet, während
    der Befund vorlag.

    Exit-Code-Katalog im Kopf, als Fortsetzung des Katalogs dieses
    Laufverzeichnisses: 25 Cron-Konfiguration auf keiner bekannten Quelle
    lesbar, 26 gelesenes Intervall weicht vom Soll ab, 27 Wirkungszweig nicht
    gefahren oder nicht protokolliert, 28 gemessener Scheibenabstand über dem
    Deckel. Danach `set -eu`.

    Variablenkopf ohne Maschinenpfad, jede Grösse als Variable mit Vorgabe:
    `SKRIPTE`, `REPO`, `OUT`, `ZIEL` mit einer Vorgabe innerhalb des
    Laufverzeichnisses, `NEXTCLOUD` mit der Vorgabe `nextcloud-aio-nextcloud`,
    `SOLL_INTERVALL` mit der Vorgabe `300` und dem Kommentar, dass fünf Minuten
    die Messbedingung aus D-07 sind. Dazu `occ`-Wrapper in der einfachen
    Fassung ohne Passwort aus `93-nullstand.sh:57-59` und `vorrat_von` aus
    `93-nullstand.sh:61-67`.

    Zweig `vorher`, alles innerhalb eines Blocks, der in `tee "$ZIEL"` mündet:

    1. Den Hintergrundauftrags-Modus lesen: `occ config:app:get core backgroundjobs_mode`.
       Der Sollwert ist `cron`. Der gelesene Wert wird als Zeile
       `cron-modus-ist <wert>` gedruckt.
    2. Das Intervall aus mehreren bekannten Quellen der Reihe nach versuchen und
       protokollieren, WELCHE Quelle geantwortet hat, als Zeile
       `cron-intervall-quelle <name>`. Kandidaten in dieser Reihenfolge, jeder
       mit einer erklärenden Zeile daneben, welche Wette er darstellt:
       die Wartezeit des Cron-Laufs im All-in-One-Container (das Skript, das
       `cron.php` in einer Schleife ruft, ist im Container lesbar), die crontab
       des Wirts für den Dienstnutzer, und der Abstand zwischen den beiden
       jüngsten Werten von `occ config:app:get core lastcron` bei zwei Lesungen
       im Abstand von `SOLL_INTERVALL` Sekunden als letzte, langsamste Quelle.
       Schreibe als Kommentar dazu, dass die dritte Quelle bereits eine Messung
       und keine Konfiguration ist, aber besser ist als gar keine Zahl.
    3. Das Ergebnis als Pflichtzeile `cron-intervall-ist <sekunden>` drucken und
       zusätzlich nach `$WORK/cron-intervall` schreiben. Antwortet keine Quelle,
       wird `unlesbar` geschrieben.
    4. Das Soll explizit setzen und zurücklesen, nicht nur prüfen: D-07 verlangt,
       dass der Ablauf den Systemcron der Messinstanz explizit auf fünf Minuten
       setzt. Das Setzen ist ein Handgriff auf der Box; das Skript druckt den
       dafür nötigen Befehl als Zeile aus, führt die Rücklesung durch und
       markiert diesen Schritt ausdrücklich als "in Phase 15 erstmals vollzogen",
       statt Gewissheit vorzutäuschen.
    5. `cron-modus-ist`, `cron-intervall-quelle` und `cron-intervall-ist` gehören
       zusammen in eine Protokollblock-Ausgabe, damit ein Bericht sie als Block
       zitieren kann.

    Unterhalb der Pipeline, und NUR dort, die Abbrüche: Inhalt von
    `$WORK/cron-intervall` gleich `unlesbar` führt zu Exit 25; eine Zahl, die um
    mehr als zehn Prozent vom `SOLL_INTERVALL` abweicht, führt zu Exit 26. Die
    Begründung steht als Kommentar daneben: der Rückgabewert einer Pipeline
    gehört zu `tee`, ein Abbruch innerhalb des Blocks verliesse nur die
    Subshell, und die Verweigerung wäre eine Zeile in einer Rohdatei, die
    niemand liest.

    Kein Passwort auf einer Kommandozeile, keine Maschinenpfade, keine
    Em-Dashes, keine Wagenrückläufe, ASCII-Bezeichner im Code.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh && head -c 9 "$f" | grep -q '#!/bin/sh' && sh -n "$f" && grep -q 'cron-intervall-ist' "$f" && grep -q 'cron-intervall-quelle' "$f" && grep -q 'SOLL_INTERVALL' "$f" && grep -q 'exit 25' "$f" && grep -q 'exit 26' "$f" && test "$(grep -n 'tee "$ZIEL"' "$f" | head -1 | cut -d: -f1)" -lt "$(grep -n 'exit 25' "$f" | tail -1 | cut -d: -f1)" && ! grep -q $'\r' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei beginnt exakt mit `#!/bin/sh` und einem LF; `sh -n` meldet keinen Syntaxfehler
    - Der Kopf nennt den Befund mit den Zahlen aus `docs/performance.md:158` und begründet, warum ein reiner Konfigurationscheck den Befund verfehlt hätte
    - Der Exit-Code-Katalog nennt 25, 26, 27 und 28 und beginnt nicht bei 1, sondern setzt den Katalog des Laufverzeichnisses fort
    - Der Aufruf ohne Argument oder mit unbekanntem Argument druckt die Benutzung und endet mit 2
    - Der Zweig `vorher` druckt die drei Protokollzeilen `cron-modus-ist`, `cron-intervall-quelle` und `cron-intervall-ist`
    - Mindestens drei Quellen für das Intervall werden der Reihe nach versucht, jede mit einer Erklärzeile
    - Der Schritt, der das Soll setzt, ist ausdrücklich als "in Phase 15 erstmals vollzogen" markiert
    - `exit 25` und `exit 26` stehen NACH der Zeile mit `tee "$ZIEL"`
    - Kein Passwort auf einer Kommandozeile, kein `/home/` im Code, keine Wagenrückläufe, kein U+2014, kein U+2013
  </acceptance_criteria>
  <done>Ein Lauf ohne protokolliertes, passendes Cron-Intervall bricht ab, statt weiterzulaufen. Die Durchsetzung steht im Skript und nicht in einer Checkliste.</done>
</task>

<task type="auto">
  <name>Task 2: Wirkungszweig des Cron-Checks</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh (der in Task 1 geschriebene Stand)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/96b-waechter.sh (die Beobachtungsschleife mit Deckel und gezählten Lesungen, aus der die Zahl "vorrat=0 in N von M Lesungen" entsteht)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/96d-statusbeobachter.py (die feinere Ablesereihe alle 120 s, damit der Ablaufplan benennen kann, welche Reihe die Protokollzahl liefert)
  </read_first>
  <action>
    Ergänze `97-cron-vorpruefung.sh` um den Zweig `waehrend`.

    Zusätzliche Variablen mit Vorgabe: `INTERVALL` als Ableseabstand in Sekunden
    (Vorgabe 120, damit die Reihe dieselbe Auflösung hat wie der
    Statusbeobachter), `DECKEL` als Zahl der Runden, und `WIRKUNGS_DECKEL` als
    obere Schranke für den gemessenen Scheibenabstand in Sekunden mit der
    Vorgabe 420 und der ausgeschriebenen Begründung: fünf Minuten Systemcron plus
    Spielraum, und die gemessenen rund 12 Minuten des v1.1-Laufs liegen deutlich
    darüber.

    Ablauf des Zweiges, innerhalb des Blocks, der in `tee "$ZIEL"` mündet, nach
    dem Muster der Beobachtungsschleife von `96b-waechter.sh`:

    1. Eine Schleife über `DECKEL` Runden. Je Runde `occ findling:index` genau
       EINMAL rufen, mit `vorrat_von` auswerten, Zeitstempel und Vorrat drucken
       und die Zahl der auswertbaren Lesungen mitzählen.
    2. Eine Zulaufscheibe ist erkannt, wenn der Vorrat gegenüber der vorigen
       Lesung STEIGT. Die Zeitpunkte dieser Anstiege werden mitgeschrieben, und
       aus aufeinanderfolgenden Zeitpunkten entstehen die Abstände.
    3. Am Ende werden gedruckt und in Dateien des Arbeitsverzeichnisses
       geschrieben: `scheiben-erkannt <n>`, `scheibenabstand-min <sekunden>`,
       `scheibenabstand-median <sekunden>`, `scheibenabstand-max <sekunden>`,
       `vorrat-null-in <n>-von-<m>-lesungen` und
       `ablesereihe-intervall <sekunden>`. Die letzte Zeile ist Pflicht, weil
       zwei verschiedene Ableseintervalle in v1.1 zwei verschiedene Prozentzahlen
       für denselben Sachverhalt erzeugt haben; das Protokoll muss sagen, welche
       Reihe die Zahl liefert.
    4. Werden weniger als zwei Scheiben erkannt, ist der Abstand nicht bestimmbar:
       es wird `scheibenabstand-median unbestimmbar` geschrieben, und der Lauf
       endet unterhalb der Pipeline mit Exit 27, weil ein Wirkungszweig ohne Zahl
       kein Protokoll ist.

    Unterhalb der Pipeline, und nur dort: fehlende oder unbestimmbare
    Wirkungszahlen führen zu Exit 27, ein Median oberhalb von `WIRKUNGS_DECKEL`
    führt zu Exit 28. Schreibe als Kommentar dazu, dass Exit 28 ein Befund und
    keine Störung ist: die Anfahrt soll an dieser Stelle halten, statt eine
    unvergleichbare Laufzeit zu erzeugen.

    Setze ganz am Ende eine Fertigzeile `97-CRON-VORPRUEFUNG-FERTIG`.

    Weiterhin: kein Passwort auf einer Kommandozeile, keine Maschinenpfade,
    keine Em-Dashes, keine Wagenrückläufe.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh && sh -n "$f" && grep -q 'scheibenabstand-median' "$f" && grep -q 'ablesereihe-intervall' "$f" && grep -q 'WIRKUNGS_DECKEL' "$f" && grep -q 'exit 27' "$f" && grep -q 'exit 28' "$f" && grep -q '97-CRON-VORPRUEFUNG-FERTIG' "$f" && ! grep -q $'\r' "$f" && cd "$(git rev-parse --show-toplevel)/backend" && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n` meldet keinen Syntaxfehler
    - Der Zweig `waehrend` ruft `occ findling:index` genau einmal je Runde und wertet beide Blöcke über `vorrat_von` aus einem Aufruf aus
    - Die sechs Protokollzeilen `scheiben-erkannt`, `scheibenabstand-min`, `scheibenabstand-median`, `scheibenabstand-max`, `vorrat-null-in` und `ablesereihe-intervall` werden gedruckt
    - `WIRKUNGS_DECKEL` existiert als Variable mit Vorgabe 420 und ausgeschriebener Begründung
    - Weniger als zwei erkannte Scheiben führen zu `scheibenabstand-median unbestimmbar` und, unterhalb der Pipeline, zu `exit 27`
    - `exit 27` und `exit 28` stehen NACH der Zeile mit `tee "$ZIEL"`
    - Die letzte ausgegebene Zeile ist `97-CRON-VORPRUEFUNG-FERTIG`
    - `pytest tests/test_measurement_scripts.py` läuft grün
  </acceptance_criteria>
  <done>Der Check misst die Wirkung und nicht nur die Einstellung. MESS-06 ist damit dem Befund nach erfüllt und nicht nur dem Buchstaben nach.</done>
</task>

<task type="auto">
  <name>Task 3: Ablaufplan 00-ablauf.md des v1.2-Laufverzeichnisses</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md</files>
  <read_first>
    - docs/measurements/2026-09-werkzeugfixe/skripte/00-ablauf.md (vollständig, 133 Zeilen: die Gliederung, die wörtlich übernommen wird, inklusive der beiden zu zitierenden Sätze)
    - docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh (aus Plan 12-05: Exit-Codes und Abschnittsfolge)
    - docs/measurements/2026-09-v12-messung/skripte/97-cron-vorpruefung.sh (aus Task 1 und 2: Zweige, Protokollzeilen und Exit-Codes)
    - docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py (aus Plan 12-04: was die Sonde misst und wie sie gefahren wird)
  </read_first>
  <action>
    Lege `docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md` an, nach der
    Gliederung des Analogs `2026-09-werkzeugfixe/skripte/00-ablauf.md`.

    Kopf mit zwei Sätzen vorweg: was der Lauf beweist und was nicht, plus der
    Hinweis auf Deckel und Owner-Freigabe, die in Phase 15 am Checkpoint fallen
    und nicht hier.

    `## 1. Was dieser Lauf misst` als Befundtabelle mit den Spalten Befund, was
    schiefging, was der Fix tut, wo der Fix liegt. Mindestens diese Zeilen:
    DI-10-02 und DI-11-01 (Fremdbestand mass den Antwortdeckel 26 statt des
    Bestands; Fix: In-Container-Sonde und Rangsemantik; liegt in
    `73-bestand-sonde.py` und `98c-sprachfaelle.sh`) und der Cron-Befund
    (nominal 5 Minuten, effektiv rund 12 Minuten, 5,85 h Leerlauf von 26,6 h;
    Fix: zweiteiliger, fail-closed durchgesetzter Check; liegt in
    `97-cron-vorpruefung.sh`).

    `## 2. Die Schrittfolge` als Tabelle mit den Spalten
    `Nr | Schritt | Rohdatei | Die Aussage, an der der Schritt haengt`. Die
    Schritte in der Reihenfolge, in der sie in Phase 15 gefahren werden:
    Cron-Konfigurationszweig, Bestandsvorlauf der Sonde, Sprachfall-Lauf mit
    Abschnitt 3b, Cron-Wirkungszweig während des Volllaufs. Übernimm aus dem
    Analog die Zeile, die die Anfahrt beendet, bevor sie Geld kostet, wörtlich:
    der Index ist intakt und die Box ist die, gegen die Phase 10 gemessen hat,
    52.111 indexiert, 37 übersprungen, 0 fehlgeschlagen, 3.9Gi, 2 Kerne,
    aarch64; stimmt das nicht, endet die Anfahrt hier.

    `## 3. Die Erwartung, vorher aufgeschrieben` als Tabelle
    `Nr | Erwartung | Woher sie kommt`, mit mindestens sechs Einträgen E1 bis E6.
    Erwartungen, die hierher gehören: der Bestand je Begriff liegt über 26 (weil
    26 der Antwortdeckel der alten Route war); die Fensterbelegung sättigt bei
    100 (weil `SEARCH_RRF_WINDOW` 100 ist); mindestens ein Fall, der in v1.1 rot
    war, wird jetzt messbar oder ausdrücklich als nicht messbar mit Rangangabe
    ausgewiesen; das protokollierte Cron-Intervall beträgt 300 Sekunden; der
    gemessene Scheibenabstand liegt unter 420 Sekunden; die Ablesereihe des
    Wirkungszweiges ist mit ihrem Intervall benannt. Füge darunter den Satz
    wörtlich ein, der diese Datei trägt: dieser Abschnitt ist der Grund, warum
    diese Datei vor der Anfahrt entsteht, und was hier steht wird nach der
    Messung nicht angepasst.

    `## 4. Woran der Lauf abgebrochen wird` als Tabelle
    `Bedingung | Wo sie greift | Folge`, mit den Rückgabewerten aller Skripte
    dieses Verzeichnisses: 15, 16, 17, 18, 19, 22, 23, 24 aus
    `98c-sprachfaelle.sh` und 25, 26, 27, 28 aus `97-cron-vorpruefung.sh`.

    `## 5. Nach dem Lauf` mit dem, was aus den Rohdaten entsteht, und mit der
    Schlusszeile zum Vokabular-Gate, wörtlich aus dem Analog übernommen.

    Deutsche Prosa mit echten Umlauten, keine Em-Dashes, keine Emojis, keine
    Wagenrückläufe. Keine lebenden Instanz- oder Volume-Kennungen, keine
    IP-Adressen, keine Passwortdateiinhalte.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md && test "$(wc -l < "$f")" -ge 100 && grep -q '## 1. Was dieser Lauf misst' "$f" && grep -q '## 2. Die Schrittfolge' "$f" && grep -q 'Die Erwartung, vorher aufgeschrieben' "$f" && grep -q '## 4. Woran der Lauf abgebrochen wird' "$f" && grep -q '## 5. Nach dem Lauf' "$f" && grep -q '52.111' "$f" && grep -q 'Vokabular-Gate' "$f" && grep -q '98c-sprachfaelle.sh' "$f" && grep -q '97-cron-vorpruefung.sh' "$f" && ! grep -q $'\r' "$f" && ! grep -q $'—' "$f" && ! grep -q $'–' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei hat mindestens 100 Zeilen und trägt die fünf Abschnittsüberschriften der Vorlage
    - Abschnitt 1 nennt beide Befunde (DI-10-02 und DI-11-01 sowie der Cron-Befund) mit ihren Zahlen und der Fundstelle des Fixes
    - Abschnitt 2 nennt die vier Schritte mit Rohdatei und der Aussage, an der sie hängen, und enthält die Abbruchzeile mit 52.111, 37, 0, 3.9Gi, 2 Kernen und aarch64
    - Abschnitt 3 hat mindestens sechs Erwartungen E1 bis E6 und den wörtlich übernommenen Satz, dass nach der Messung nichts mehr angepasst wird
    - Abschnitt 4 listet alle zwölf Rückgabewerte 15 bis 19 und 22 bis 28 mit Bedingung, Greifstelle und Folge
    - Abschnitt 5 endet mit der wörtlich übernommenen Zeile zum Vokabular-Gate
    - Keine Wagenrückläufe, kein U+2014, kein U+2013, keine IP-Adresse und keine lebende Instanz- oder Volume-Kennung
  </acceptance_criteria>
  <done>Die Erwartung des Laufs steht schriftlich fest, bevor die Box Geld kostet, und jeder Abbruchpfad ist benannt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Box (Messinstanz) → Nextcloud-Container | `occ` läuft über `sudo docker exec --user www-data` |
| Messbedingung → Messergebnis | Ein unprotokolliertes Intervall macht den Laufzeitvergleich unbelegt |
| Repositorium (öffentlich) ← Skript, Ablaufplan und Rohdaten | Alle drei werden committet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-25 | Repudiation | Messlauf ohne protokolliertes Cron-Intervall | mitigate | Pflichtzeile `cron-intervall-ist` plus Abbruch mit Exit 25 beziehungsweise 26 unterhalb der `tee`-Pipeline; die Durchsetzung liegt im Skript und nicht in einer Checkliste (D-08) |
| T-12-26 | Repudiation | Konfigurationscheck meldet grün, während der Befund vorliegt | mitigate | Zweiter Zweig `waehrend`, der den tatsächlichen Scheibenabstand misst, mit Exit 27 bei fehlender Zahl und Exit 28 über dem Deckel |
| T-12-27 | Information Disclosure | Protokollzeilen und Ablaufplan im öffentlichen Repositorium | mitigate | Das Skript druckt Zeitstempel, Vorratszahlen und Intervalle, keine Dokumentinhalte und keine Adressen; der Ablaufplan nennt keine lebende Instanz- oder Volume-Kennung und keine IP-Adresse, geprüft als Acceptance-Criterion |
| T-12-28 | Information Disclosure | Passwort beim `occ`-Aufruf | mitigate | Der `occ`-Wrapper dieses Skripts ist die passwortlose Fassung aus `93-nullstand.sh`; `passwords_on_a_command_line` läuft ab Plan 12-04 über dieses Verzeichnis |
| T-12-29 | Denial of Service (gegen den eigenen Messlauf) | Beobachtungsschleife fragt zu oft | mitigate | Ein `occ findling:index` je Runde und ein Ableseabstand als Variable mit Vorgabe 120 s; kein enges Polling, Deckel als Rundenzahl |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- Erfolgskriterium 4 der Phase: ein Lauf ohne protokolliertes Intervall gilt als unvollständig und bricht ab
- `sh -n` grün für beide Zweige, `pytest tests/test_measurement_scripts.py` grün
- Der Ablaufplan schreibt die Erwartung VOR der Messung auf
- Keine Em-Dashes, keine Wagenrückläufe, kein Passwort auf einer Kommandozeile
- Repo-Regeln: Git-Identität NICHT per `-c` übersteuern; sequenziell auf main, Basis-SHA gepinnt
</verification>

<success_criteria>
Die Cron-Bedingung ist im Skript durchgesetzt statt in einer Checkliste
beschrieben, sie prüft Konfiguration UND Wirkung, und der Ablaufplan des Laufs
steht mit vorher aufgeschriebener Erwartung und vollständiger Abbruchtabelle.
</success_criteria>

<output>
Create `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-06-SUMMARY.md` when done
</output>
