---
phase: 12-messwerkzeug-runbook-und-terminentscheid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md
autonomous: true
requirements: [HART-03]

must_haves:
  truths:
    - "Am 16.09.2026 muss kein Text mehr entworfen werden: beide Zweige des stable35-Entscheids liegen fertig ausformuliert vor (D-01, D-03)"
    - "Ein Leser der Notiz sieht ohne Rückfrage, welcher Zweig bei welchem Releasestand greift und welcher Beleg dafür verlangt wird (D-02)"
    - "Der Ersatztext für den YAML-Kommentar in deploy-harp.yml ist wortwörtlich einsetzbar, für Zweig a und für Zweig b getrennt"
  artifacts:
    - path: ".planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md"
      provides: "Beleglage, beide Optionen, beide fertigen YAML-Ersatztexte, Vollzugs-Checkliste für den 16.09."
      contains: "Option a"
      min_lines: 90
  key_links:
    - from: ".planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md"
      to: ".github/workflows/deploy-harp.yml"
      via: "wortwörtlicher Ersatztext für den RE-CHECK-Absatz und das tolerate-failure-Flag"
      pattern: "tolerate-failure"
    - from: ".planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md"
      to: "backend/tests/test_lockstep_versions.py"
      via: "Leitplanke: der stable35-Matrixeintrag darf nicht entfernt werden, nur sein Flag darf fallen"
      pattern: "test_lockstep_versions"
---

<objective>
Beide Zweige des fristgebundenen stable35-Entscheids (HART-03) werden VOR dem
16.09.2026 vollständig ausformuliert, inklusive der fertigen Ersatztexte für
`.github/workflows/deploy-harp.yml`. Am 16.09. wird dann nur noch gelesen,
welcher Zweig greift, und der fertige Text eingesetzt (Plan 12-02).

Purpose: Die Frist liegt zwei Tage nach Phasenbeginn. Pitfall 6 der Recherche
benennt den Ausfallmodus namentlich: am 16.09. steht der Releasestand fest,
aber Text, Kommentar und Notiz sind noch nicht geschrieben, und der Tag geht
vorbei. Ein Plan, in dem "Entscheid treffen" eine einzige Aufgabe ist, ist das
Warnzeichen für genau diesen Ausfall.

Output: `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md`
mit Beleglage, Option a (D-01/D-02), Option b (D-03), beiden YAML-Ersatztexten
und einer Vollzugs-Checkliste für den 16.09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-PATTERNS.md

<interfaces>
Stand am 2026-09-14, aus dem eigenen Baum gelesen. Der Executor braucht diese
Werte, um die Notiz ohne eigene Erkundung zu schreiben.

Versionsfenster, bereits auf 35, es ist NICHTS zu heben:
- `php/appinfo/info.xml:219`     `<nextcloud min-version="33" max-version="35"/>`
- `backend/appinfo/info.xml:225` `<nextcloud min-version="33" max-version="35"/>`

Offener Punkt, ausschliesslich in `.github/workflows/deploy-harp.yml`:
- Matrixeintrag `- server-version: stable35` mit `php-version: '8.3'`,
  `tolerate-failure: true`, `runner: ubuntu-24.04` (Zeilen 217 bis 272)
- Der RE-CHECK-Kommentarblock darüber, Zeilen 211 bis 271, mit der Zeile
  `# RE-CHECK DATE: 2026-09-16.` und dem Satz
  `On that day this flag and this whole paragraph come out, the leg becomes must-be-green, and a red run is a finding rather than a reason to put the flag back.`
- Der jüngste Fortschreibe-Eintrag nennt als Nachfolge-Adresse Plan 11-11.

Leitplanke, gilt für BEIDE Optionen:
- `backend/tests/test_lockstep_versions.py:387` prüft, dass die CI-Matrix jede
  Version des deklarierten Fensters abdeckt
  (`_window_findings(("33","35"), ("33","35"), ["33","34","35"]) == []`).
  Der `stable35`-Eintrag darf also NICHT entfernt werden, nur sein Flag darf
  fallen.

Releasestand am 2026-09-14, über `gh api repos/nextcloud/server/releases`:
- `v35.0.0rc4`  prerelease=true   2026-09-10T12:53:25Z
- `v34.0.4`     prerelease=false  2026-09-10T13:16:15Z
- `v33.0.9`     prerelease=false  2026-09-10T13:29:56Z
NC 35 ist am 14.09.2026 NICHT final. Der offizielle Zeitplan nennt 2026-09-16
mit dem Zusatz "date not final, but will be pre-conf".

Beide Zweige `stable35` existieren (`gh api repos/nextcloud/server/branches/stable35`
und `.../app_api/branches/stable35`), der CI-Ast fällt also nicht auf `master`
zurück.

Formvorlage der Notiz:
`.planning/milestones/v1.1-phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md`
Abschnittsfolge je Frage: `### Was festgestellt ist`, `### Was unklar ist`,
`### Die Leitplanke, die für beide Optionen gilt`, `### Option a: ...`,
`### Option b: ...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entscheidungsnotiz mit Beleglage und beiden Optionen anlegen</name>
  <files>.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md</files>
  <read_first>
    - .planning/milestones/v1.1-phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md (Formvorlage: Kopf-Muster und Abschnittsfolge je Frage, wörtlich übernehmen)
    - .github/workflows/deploy-harp.yml (Zeilen 200 bis 280: der stable35-Matrixeintrag und der RE-CHECK-Kommentarblock im Ist-Zustand)
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md (Befund 5, Abschnitte 5.1 bis 5.5)
  </read_first>
  <action>
    Lege die Datei mit dem Kopf-Muster aus `11-VORENTSCHEIDE.md` an: Titel
    `# Phase 12: stable35-Fenster-Entscheid (HART-03)`, dann `**Angelegt:**`
    mit Datum und Planbezug (Plan 12-01, Task 1) und `**Zweck:**` mit dem Satz,
    dass die Frist der 16.09.2026 ist, beide Zweige deshalb VORHER ausformuliert
    werden und am Stichtag nur noch gelesen wird, welcher greift. Der Zweck-Absatz
    sagt ausserdem, dass Folgepläne die Optionskennung (a oder b) zitieren und
    keine Zusammenfassung.

    Danach die Abschnittsfolge der Vorlage, angewandt auf die eine Frage:

    `## Die Frage` in einem Halbsatz: fällt am 16.09.2026 das
    `tolerate-failure: true` am stable35-Ast von `deploy-harp.yml`, oder wird der
    RE-CHECK mit Begründung verlängert.

    `### Was festgestellt ist` mit Datei-und-Zeile-Belegen und Zahlen:
    - Das deklarierte Versionsfenster steht BEREITS auf `min-version="33"
      max-version="35"` in `php/appinfo/info.xml:219` und
      `backend/appinfo/info.xml:225`. Schreibe ausdrücklich hin, dass die
      Formulierung "auf max NC 35 heben" aus D-01 in den `info.xml` nichts zu
      tun lässt, damit kein Folgeplan eine bereits erledigte Aufgabe aufnimmt.
    - Offen ist ausschliesslich `.github/workflows/deploy-harp.yml`: der
      Matrixeintrag `server-version: stable35` mit `tolerate-failure: true` und
      der RE-CHECK-Kommentarblock darüber, der seinen eigenen Abgang beschreibt.
    - Releasestand am 14.09.2026 als Tabelle (v35.0.0rc4 prerelease,
      v34.0.4 release, v33.0.9 release, jeweils mit Datum), Quelle
      `gh api repos/nextcloud/server/releases`.
    - Beide `stable35`-Zweige existieren in `nextcloud/server` und
      `nextcloud/app_api`, der CI-Ast fällt nicht auf `master` zurück.
    - Der offizielle Zeitplan nennt 2026-09-16 als Final-Datum mit dem
      wörtlichen Zusatz "date not final".

    `### Was unklar ist`: ob NC 35 am 16.09. tatsächlich final ist. Nenne die
    Kadenz rc3 am 03.09., rc4 am 10.09. als Beobachtung und kennzeichne die
    Vermutung eines rc5 am 17.09. ausdrücklich als Extrapolation ohne Quelle.

    `### Die Leitplanke, die für beide Optionen gilt`: der stable35-Matrixeintrag
    darf NICHT entfernt werden, nur sein Flag darf fallen, weil
    `backend/tests/test_lockstep_versions.py:387` prüft, dass die Matrix jede
    Version des deklarierten Fensters abdeckt. Zweite Leitplanke: ein roter Lauf
    am Stichtag ist nach dem eigenen Wortlaut des Kommentars ein Befund und kein
    Grund, das Flag zurückzusetzen. Dritte Leitplanke: die bekannte Warnung vor
    den Uninstall-Gate-Schwellen derselben Strecke (Befund A aus Plan 11-11)
    steht daneben, damit ein roter Lauf richtig gelesen wird.

    `### Option a: NC 35 ist final (D-01 und D-02)` mit der Beweisgrundlage
    wörtlich aus D-02: NC-35-final-Check PLUS die bestehende deploy-harp-
    Fremdinstallations- und Upgrade-Strecke grün gegen stable35. Der
    Release-Status allein reicht nicht. Halte fest, was das Fenster in v1.2.0
    heisst: max NC 35 gilt bereits, ein separates 1.1.x nur für das Fenster
    entfällt.

    `### Option b: NC 35 ist nicht final (D-03)` mit: Entscheid dokumentieren,
    Fenster bleibt, neuer RE-CHECK-Termin wird gesetzt und spätestens vor der
    Phase-16-Einreichung erneut geprüft, Phase 12 gilt damit als erfüllt.

    Keine Em-Dashes, keine Emojis, deutsche Prosa mit echten Umlauten. Der
    Abschnitt für den Vollzug am 16.09. wird in Task 2 ergänzt.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md && grep -q '### Option a' "$f" && grep -q '### Option b' "$f" && grep -q 'max-version="35"' "$f" && grep -q 'test_lockstep_versions.py' "$f" && grep -q 'v35.0.0rc4' "$f" && ! grep -q $'—\|–' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei existiert und enthält die Überschriften `### Was festgestellt ist`, `### Was unklar ist`, `### Die Leitplanke, die für beide Optionen gilt`, `### Option a`, `### Option b`
    - Die Zeichenketten `php/appinfo/info.xml:219`, `backend/appinfo/info.xml:225` und `max-version="35"` stehen in der Datei
    - Die Zeichenkette `test_lockstep_versions.py` steht in der Datei, im Leitplanken-Abschnitt
    - Die Zeichenketten `v35.0.0rc4`, `v34.0.4` und `date not final` stehen in der Datei
    - Die Datei enthält weder U+2014 noch U+2013
    - Die Datei hat mindestens 60 Zeilen
  </acceptance_criteria>
  <done>Beleglage und beide Optionen stehen in der Notiz, mit Belegen an Datei und Zeile.</done>
</task>

<task type="auto">
  <name>Task 2: Beide YAML-Ersatztexte und die Vollzugs-Checkliste für den 16.09. ergänzen</name>
  <files>.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md</files>
  <read_first>
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md (der in Task 1 geschriebene Stand)
    - .github/workflows/deploy-harp.yml (Zeilen 200 bis 280: der exakte Ist-Wortlaut des Kommentarblocks, dessen Fortschreibemuster übernommen wird: Datum, entscheidender Plan, Aktenzeichen der Notiz, gelesener Beleg, neue Nachfolge-Adresse)
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md (Abschnitt 5.5: empfohlene Form des Entscheids)
  </read_first>
  <action>
    Ergänze die Notiz um zwei Abschnitte.

    `## Die fertigen Ersatztexte für deploy-harp.yml`, mit zwei Unterabschnitten
    `### Ersatztext Option a` und `### Ersatztext Option b`. Jeder Unterabschnitt
    enthält den vollständigen, wortwörtlich einsetzbaren YAML-Kommentarblock in
    einem Codeblock, so dass Plan 12-02 nur noch kopiert. Sprache der
    YAML-Kommentare bleibt Englisch, wie der bestehende Block. Vorgaben:

    Option a: Der gesamte RE-CHECK-Absatz entfällt und wird durch einen kurzen
    Vermerk ersetzt, der Datum des Vollzugs, den Plan (12-02), das Aktenzeichen
    dieser Notiz, die gelesene Belegzeile (Tag und Datum der finalen
    35er-Freigabe aus `gh api repos/nextcloud/server/releases`) und die Laufnummer
    des grünen `deploy-harp`-Beweislaufs nennt. Die Zeile `tolerate-failure: true`
    wird zu `tolerate-failure: false`. Lasse in dem Text die drei Stellen, die
    erst am Stichtag bekannt sind, als klar erkennbare Platzhalter stehen:
    `<FINAL-TAG>`, `<FINAL-DATUM>`, `<RUN-ID>`. Der Matrixeintrag selbst bleibt
    stehen, `php-version: '8.3'` und `runner: ubuntu-24.04` bleiben unverändert.

    Option b: `tolerate-failure: true` bleibt. Der bestehende RE-CHECK-Absatz
    wird nach dem vorhandenen Fortschreibemuster um einen weiteren Eintrag
    ergänzt, der Datum, Plan 12-02, das Aktenzeichen dieser Notiz, den gelesenen
    Beleg (neueste 35er-Marke mit Prerelease-Kennzeichen und neueste echte
    Freigabe, Platzhalter `<NEUESTE-35-MARKE>` und `<NEUESTE-FREIGABE>`), den
    NEUEN RE-CHECK-Termin und die neue Nachfolge-Adresse nennt. Die neue
    Nachfolge-Adresse ist die Store-Einreichung der Phase 16; der neue
    RE-CHECK-Termin ist spätestens der Tag vor dem Beginn der Phase-16-Einreichung,
    als Platzhalter `<NEUER-RE-CHECK>`. Die Zeile `# RE-CHECK DATE: 2026-09-16.`
    wird auf den neuen Termin umgeschrieben statt gelöscht, damit der Absatz
    seine Wirkung behält.

    `## Vollzug am 16.09.2026` als leerer, vorstrukturierter Abschnitt, den Plan
    12-02 füllt: Datumsfeld, das Kommando `gh api repos/nextcloud/server/releases`
    mit der Stelle für die Antwort, das Feld "greifender Zweig (a oder b)", das
    Feld "Beleg", das Feld "Laufnummer des Beweislaufs (nur Option a)" und das
    Feld "vollzogen am / durch Plan". Schreibe darunter die Vollzugs-Checkliste
    als nummerierte Liste in genau dieser Reihenfolge: Datum prüfen, Releasestand
    lesen, Zweig bestimmen, bei Option a den Beweislauf starten und abwarten,
    Ersatztext einsetzen, Notiz füllen, Owner-Bestätigung einholen.

    Keine Em-Dashes. Die YAML-Codeblöcke selbst enthalten keine Umlaute
    (Kommentare im Workflow sind Englisch), die deutsche Prosa drumherum schon.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md && grep -q '### Ersatztext Option a' "$f" && grep -q '### Ersatztext Option b' "$f" && grep -q 'tolerate-failure: false' "$f" && grep -q '<RUN-ID>' "$f" && grep -q '<NEUER-RE-CHECK>' "$f" && grep -q '## Vollzug am 16.09.2026' "$f" && ! grep -q $'—\|–' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `### Ersatztext Option a` und `### Ersatztext Option b` existieren, jeder mit mindestens einem eingerückten YAML-Codeblock
    - Der Ersatztext von Option a enthält `tolerate-failure: false` und die Platzhalter `<FINAL-TAG>`, `<FINAL-DATUM>`, `<RUN-ID>`
    - Der Ersatztext von Option b enthält `tolerate-failure: true`, eine umgeschriebene `RE-CHECK DATE`-Zeile und die Platzhalter `<NEUER-RE-CHECK>`, `<NEUESTE-35-MARKE>`, `<NEUESTE-FREIGABE>`
    - Der Ersatztext von Option b nennt die Store-Einreichung der Phase 16 als neue Nachfolge-Adresse
    - `## Vollzug am 16.09.2026` existiert mit den sechs benannten Feldern und einer nummerierten Checkliste mit genau sieben Schritten
    - In keinem Ersatztext wird der Matrixeintrag `- server-version: stable35` entfernt
    - Die Datei enthält weder U+2014 noch U+2013
  </acceptance_criteria>
  <done>Am 16.09. ist kein Entwurf mehr nötig: beide Ersatztexte sind kopierfertig, die Checkliste ist abarbeitbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositorium (öffentlich) ← Entscheidungsnotiz | Die Notiz liegt in einem öffentlichen Repositorium; alles, was hineingeschrieben wird, ist veröffentlicht |
| GitHub-API → Entwicklungsmaschine | `gh api`-Antworten werden gelesen und zitiert (in diesem Plan nur als bereits erhobener Stand) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | 12-STABLE35-ENTSCHEID.md | mitigate | Die Notiz nennt ausschliesslich öffentliche Tatsachen (NC-Releasetags, Dateipfade des eigenen Repositoriums, CI-Laufnummern). Keine Tokens, keine `gh`-Anmeldedaten, keine Konto-Kennungen; Acceptance-Criteria prüfen die vorgesehenen Zeichenketten und keine Wertfelder |
| T-12-02 | Tampering | deploy-harp.yml (später, via Ersatztext) | mitigate | Der Ersatztext darf den Matrixeintrag `- server-version: stable35` nicht entfernen, weil `backend/tests/test_lockstep_versions.py:387` sonst rot wird; das ist als Leitplanke in der Notiz und als Acceptance-Criterion in Task 2 festgeschrieben |
| T-12-03 | Repudiation | Entscheid ohne Beleg | mitigate | Beide Ersatztexte tragen Pflichtfelder für Datum, Plan, Aktenzeichen, gelesenen Beleg und (Option a) Laufnummer; ein Entscheid ohne diese Felder ist am Format erkennbar unvollständig |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket. Die Recherche hält fest: Package Legitimacy Audit nicht anwendbar, die Phase installiert keine externen Pakete |
</threat_model>

<verification>
- `12-STABLE35-ENTSCHEID.md` existiert und trägt beide Optionen mit vollständigen YAML-Ersatztexten
- Die Datei nennt ausdrücklich, dass an den `info.xml` nichts zu tun ist
- Die Datei nennt die Leitplanke aus `test_lockstep_versions.py:387`
- Keine Em-Dashes, keine Emojis
- Repo-Regeln: Git-Identität NICHT per `-c` übersteuern; sequenziell auf main, Basis-SHA gepinnt
</verification>

<success_criteria>
Ein Leser kann am 16.09.2026 ohne jede Entwurfsarbeit entscheiden, welcher Zweig
greift, und den zugehörigen Text kopieren. Kein Satz des Entscheids muss am
Stichtag noch formuliert werden; offen sind nur die als Platzhalter markierten
Tatsachen `<FINAL-TAG>`, `<FINAL-DATUM>`, `<RUN-ID>` beziehungsweise
`<NEUESTE-35-MARKE>`, `<NEUESTE-FREIGABE>`, `<NEUER-RE-CHECK>`.
</success_criteria>

<output>
Create `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-01-SUMMARY.md` when done
</output>
