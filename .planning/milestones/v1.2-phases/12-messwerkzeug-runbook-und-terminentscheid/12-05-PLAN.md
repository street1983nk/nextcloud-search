---
phase: 12-messwerkzeug-runbook-und-terminentscheid
plan: 05
type: execute
wave: 2
depends_on: [12-04]
files_modified:
  - docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh
autonomous: true
requirements: [MESS-04]

must_haves:
  truths:
    - "Die Vorprüfung des Fremdbestands misst nicht mehr die gedeckelte OCS-Route, sondern Bestand, Fensterbelegung und Rang aus dem Container"
    - "Ein Fall gilt als NICHT MESSBAR, wenn die eigene Datei in beiden Ranglisten ausserhalb der 64 Rechecks liegt; die gemessenen Zahlen können dabei Werte oberhalb der alten Deckelung 26 annehmen"
    - "Die gefahrene Fassung 98b bleibt unberührt: die Nachfolge ist eine neue Datei in einem neuen Laufverzeichnis"
    - "Jeder Abbruch geschieht nach der tee-Pipeline, damit die Verweigerung nicht als Zeile in einer Rohdatei verschwindet"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh"
      provides: "Nachfolgefassung der zehn Sprachfälle mit der neuen Messgrösse und dem Rangabschnitt 3b"
      contains: "73-bestand-sonde.py"
      min_lines: 400
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh"
      to: "docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py"
      via: "docker cp plus docker exec, Ausgabe nach WORK"
      pattern: "73-bestand-sonde.py"
    - from: "docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh"
      to: "Nextcloud WebDAV"
      via: "OC-FileId aus dem PUT-Antwortkopf, Grundlage des Rangabschnitts"
      pattern: "OC-FileId"
---

<objective>
`98c-sprachfaelle.sh` entsteht als Nachfolgefassung von `98b-sprachfaelle.sh` in
dem in Plan 12-04 angelegten Laufverzeichnis. Das dreiwertige Urteil bleibt, nur
die Messgrösse hinter der Messbarkeitsprüfung wechselt: statt der Trefferzahl
der gedeckelten OCS-Route entscheidet der Rang der eigenen Datei in den beiden
Ranglisten, gemessen durch die In-Container-Sonde.

Purpose: Erfolgskriterium 1 der Phase. `98b` ist am 10.09.2026 gefahren worden
und trägt Rohdaten; die Hausregel `DRIVEN_FASSUNG_RULE` verlangt deshalb eine
neue Datei in einem neuen Laufverzeichnis statt einer Korrektur an Ort und
Stelle.

Output: `docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-PATTERNS.md

<interfaces>
Struktur der Vorlage `docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh`
(664 Zeilen), an den Stellen, die `98c` übernimmt oder ändert:

| Stelle | Zeilen in 98b | In 98c |
|---|---|---|
| Kopf mit der Begründung der Nachfolge | 1 bis 13 | dieselbe Konstruktion, angewandt auf `98b` |
| Exit-Code-Katalog im Kopf | 84 bis 88 | fortsetzen, nicht verschieben |
| Variablenkopf ohne Maschinenpfad | 93 bis 130 | übernehmen, um `SONDE` und `CONTAINER` erweitert |
| Pflichteingabe `CI_LAUF`, Exit 22 | 145 bis 155 | unverändert |
| Arbeitsverzeichnis und leere Urteilsdateien | 167 bis 177 | unverändert |
| `occ`-Wrapper über zwei Schichten | 179 bis 190 | unverändert |
| `vorrat_von` aus EINEM Aufruf | 192 bis 198 | unverändert |
| dreiwertiges Urteil: `messbar`, `urteil`, `fail` | 243 bis 279 | Grundmuster bleibt, Inhalt von `$WORK/fremd/$FALL` wechselt |
| Block in `tee` | 281 bis 618 | unverändert als Konstruktion |
| Abschnitt 0, Vorprüfung über die OCS-Route | 287 bis 331 | ERSETZT durch die Sonde |
| Abschnitt 1, Konto | ab 333 | übernehmen |
| Abschnitt 2, Korpus über WebDAV | ab 358 | übernehmen, um die Erfassung von `OC-FileId` erweitert |
| Abschnitt 3, Indexierung gegen die langsamste Uhr | ab 391 | übernehmen |
| Abschnitt 3b, Rang der eigenen Datei | existiert nicht | NEU, zwischen Indexierung und Fällen |
| Abschnitt 4, die zehn Fälle | ab 418 | übernehmen, mit der neuen `messbar`-Semantik |
| Bilanz mit zwei Zahlen | 573 bis 580 | übernehmen |
| Alle Abbrüche NACH der Pipeline | 618 bis 664 | übernehmen, um die neuen Codes erweitert |

Der Exit-Code-Katalog von `98b`, wörtlich zu übernehmen und fortzusetzen:

```
15 der Upload hat nicht 39 Dateien geliefert
16 der Arbeitsvorrat war am Rundendeckel noch nicht leer
17 mindestens ein MESSBARER Fall war rot
18 jq liegt nicht auf dieser Box
19 die Vorpruefung des Fremdbestands konnte nicht gefahren werden
22 CI_LAUF traegt keine Laufnummer
23 kein einziger Fall war messbar
```

Neu in `98c`: `24` fuer "Abschnitt 3b konnte keine Datei-Kennungen erheben".

Die drei Funktionen des dreiwertigen Urteils, Grundmuster aus `98b:243-279`:

```sh
messbar() {
    fremd=$(cat "$WORK/fremd/$1" 2>/dev/null || echo '')
    if [ -z "$fremd" ]; then
        printf 'sprachfall %s NICHT MESSBAR (Fremdbestand nicht erhoben)\n' "$1"
        printf 'fall %s\n' "$1" >>"$WORK/nichtmessbar"
        return 1
    fi
    ...
}
```

Die zehn Fälle und ihre Suchzeilen, `98b:429-563`:

```
1 Genehmigung | 2 Frist | 3 Mueller | 4 Vertrag | 5 "drei Monate"
6 bescheid (und zusaetzlich 'bescheid -frist') | 7 type:pdf bescheid
8 Belehrung | 9 Auszug | 10 Erinnerung
```

Die Sonde aus Plan 12-04,
`docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py`:
- ohne `DATEI_IDS`: eine Zeile je Begriff mit `bestand`, `index=`, `fenster_lex=`,
  `fenster_sem=`, `rang_lex=ausserhalb`, `rang_sem=ausserhalb`
- mit `DATEI_IDS` als kommagetrennter Liste `<begriff>=<file_id>`: dieselben
  Zeilen, `rang_lex` und `rang_sem` als 1-basierte Zahlen
- Schwelle `64` aus `MAX_RECHECKS_ABSOLUTE` in `php/lib/Search/Provider.php:90`

Fahrermuster, `94-grundlast.sh:49` und `:102-113`:

```sh
CONTAINER="${CONTAINER:-nc_app_findling_backend}"
sudo docker cp "$SONDE" "$CONTAINER:/tmp/73-bestand-sonde.py"
sudo docker exec "$CONTAINER" /app/.venv/bin/python /tmp/73-bestand-sonde.py
```

Gates, die ab Plan 12-04 über dieses Verzeichnis laufen: Shebang exakt
`#!/bin/sh` mit LF, kein Wagenrücklauf, kein Halbgeviert- und kein
Geviertstrich, kein Maschinenpfad im Code, kein Passwort auf einer
Kommandozeile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kopf, Exit-Codes, Variablen, Hilfsfunktionen und Abschnitt 0 über die Sonde</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh</files>
  <read_first>
    - docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh (Zeilen 1 bis 340: Kopf, Exit-Code-Katalog, Variablenkopf, CI_LAUF-Pflichteingabe, Arbeitsverzeichnis, occ-Wrapper, vorrat_von, die drei Urteilsfunktionen und der alte Abschnitt 0)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/94-grundlast.sh (Zeilen 38 bis 122: Variablenkopf mit Vorgaben und das Transportmuster docker cp plus docker exec)
    - docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py (aus Plan 12-04: Ausgabeformat und die Umgebungsvariable DATEI_IDS)
  </read_first>
  <action>
    Lege `docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh` an.
    Shebang exakt `#!/bin/sh`, LF-Zeilenenden.

    Kopf nach dem Muster von `98b:1-13`, mit der Konstruktion "wo die gefahrene
    Fassung liegt und dass sie liegen bleibt", diesmal angewandt auf `98b`:
    `98b` wurde am 10.09.2026 gefahren (Plan 11-03, Werkzeugfixe) und trägt
    Rohdaten unter `docs/measurements/2026-09-werkzeugfixe/rohdaten/`; die Datei
    bleibt byteweise identisch, weil ein nachträglich bearbeitetes Messskript
    jede Zahl neben sich unbelegt macht. Nenne im selben Absatz die Änderung
    dieser Fassung: die Messgrösse hinter der Messbarkeitsprüfung wechselt von
    der Trefferzahl der gedeckelten OCS-Route auf den Rang der eigenen Datei in
    den beiden Ranglisten, gemessen im Container.

    Exit-Code-Katalog wörtlich aus `98b:84-88` übernehmen und um `24` ergänzen:
    Abschnitt 3b konnte keine Datei-Kennungen erheben. Danach `set -eu`.

    Variablenkopf nach `98b:93-130`, ohne jeden Maschinenpfad, jede Grösse als
    Variable mit Vorgabe: `SKRIPTE`, `REPO`, `OUT`, `ZIEL` (Vorgabe
    `$OUT/05-sprachfaelle.txt` innerhalb des neuen Laufverzeichnisses),
    `NEXTCLOUD`, `KONTO`, `LASTKONTO`, `PWFILE`, `CI_LAUF`. Neu hinzu:
    `CONTAINER` mit der Vorgabe `nc_app_findling_backend`, `SONDE` mit der
    Vorgabe `$SKRIPTE/73-bestand-sonde.py` und `RANG_SCHWELLE` mit der Vorgabe
    `64` und dem Kommentar, dass 64 die `MAX_RECHECKS_ABSOLUTE` aus
    `php/lib/Search/Provider.php:90` ist. Die alte Variable `FREMD_SCHWELLE`
    entfällt, weil sie eine Trefferzahl gemeint hat.

    Pflichteingabe `CI_LAUF` mit Exit 22 vor allem anderen, unverändert aus
    `98b:145-155`. Arbeitsverzeichnis mit `mktemp -d`, `chmod 700`,
    `trap` auf Aufräumen bei EXIT, leere Urteilsdateien `fehler`,
    `fehlertexte`, `nichtmessbar` und ein Verzeichnis `fremd`, unverändert aus
    `98b:167-177`.

    Hilfsfunktionen unverändert aus `98b` übernehmen: der `occ`-Wrapper mit
    `sudo --preserve-env=OC_PASS docker exec -e OC_PASS --user www-data`, die
    Funktion `vorrat_von` mit dem `awk`-Block, der beide Blöcke aus EINEM Aufruf
    liest, sowie `search`, `fail` und `urteil`.

    `messbar()` wird umgebaut, Grundmuster und Meldungsform bleiben. Der Inhalt
    von `$WORK/fremd/$FALL` ist jetzt eine Zeile mit dem kleineren der beiden
    Ränge oder das Wort `ausserhalb`. Regeln:
    - Datei leer oder fehlt: NICHT MESSBAR mit dem Grund "Rang nicht erhoben"
    - Inhalt `ausserhalb`: NICHT MESSBAR mit dem Grund "eigene Datei in keiner
      der beiden Listen"
    - Zahl grösser als `RANG_SCHWELLE`: NICHT MESSBAR mit Rang und Schwelle in
      der Meldung
    - sonst Rückgabe 0
    Jeder Nichtmessbarkeitsfall schreibt weiterhin eine Zeile nach
    `$WORK/nichtmessbar`, wie im Analog. Schreibe die Begründung als Kommentar
    über die Funktion: bei einer Fusion nach RRF stehen alle Dokumente, die in
    einer Liste vor der eigenen Datei liegen, auch in der fusionierten Liste vor
    ihr, ein Rang schlechter als 64 in beiden Listen kann deshalb durch keine
    Fusion in die Reichweite der 64 Rechecks kommen.

    Öffne danach den Block, der in `tee` mündet (die öffnende Klammer wie in
    `98b:281`), mit den vier Kopfzeilen (Startzeitstempel, Konto, Korpus,
    CI-Beleg), und schreibe Abschnitt 0 neu:

    Überschrift `=== Abschnitt 0: der Bestand im Index, im Prozess gemessen (DI-10-02) ===`
    mit zwei Erklärzeilen, dass hier keine Route und kein Konto gefragt wird,
    weil eine Trefferzahl über die Prozessgrenze das Zähl-Orakel aus T-02-93
    wäre, und dass die alte Fassung für jeden Begriff und jede Tiefe 26 gemessen
    hat. Dann wird `vorpruefung-gefahren nein` nach `$WORK/vorpruefung-urteil`
    geschrieben, danach der Fahrer: `sudo docker cp` der Sonde in den Container
    nach `/tmp/73-bestand-sonde.py` und `sudo docker exec` mit
    `/app/.venv/bin/python` auf diese Datei, Ausgabe nach
    `$WORK/bestand-vorlauf.txt`. Schlägt einer der beiden Schritte fehl, wird die
    Ursache gedruckt und das Urteil bleibt auf `nein`; der Abbruch mit Code 19
    geschieht NACH der Pipeline und nicht hier. Bei Erfolg wird die Datei
    ausgegeben und das Urteil auf `vorpruefung-gefahren ja` gesetzt.

    Kein Passwort auf einer Kommandozeile, keine Maschinenpfade, keine
    Em-Dashes, keine Wagenrückläufe, ASCII-Bezeichner im Code.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh && head -c 9 "$f" | grep -q '#!/bin/sh' && sh -n "$f" && grep -q 'exit 22' "$f" && grep -q 'RANG_SCHWELLE' "$f" && grep -q 'docker cp' "$f" && ! grep -q 'FREMD_SCHWELLE' "$f" && ! grep -q $'\r' "$f" && ! grep -q $'—' "$f" && ! grep -q $'–' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei beginnt exakt mit `#!/bin/sh` und einem LF; `sh -n` meldet keinen Syntaxfehler
    - Der Kopf nennt `98b-sprachfaelle.sh` als gefahrene Fassung, die byteweise identisch bleibt, und begründet die Nachfolge
    - Der Exit-Code-Katalog nennt 15, 16, 17, 18, 19, 22, 23 mit unveränderter Bedeutung und zusätzlich 24
    - `CI_LAUF` wird vor allem anderen geprüft und führt bei fehlender Laufnummer zu `exit 22`
    - `CONTAINER`, `SONDE` und `RANG_SCHWELLE` existieren als Variablen mit Vorgabe; `FREMD_SCHWELLE` kommt nicht mehr vor
    - `messbar()` unterscheidet die drei Nichtmessbarkeitsgründe (nicht erhoben, `ausserhalb`, Rang über der Schwelle), schreibt in jedem Fall nach `$WORK/nichtmessbar` und trägt die RRF-Begründung als Kommentar
    - Abschnitt 0 fährt die Sonde über `docker cp` und `docker exec` und setzt das Urteil nur bei Erfolg auf `vorpruefung-gefahren ja`
    - Kein Passwort auf einer Kommandozeile, kein `/home/` im Code
    - Keine Wagenrückläufe, kein U+2014, kein U+2013
  </acceptance_criteria>
  <done>Die Vorprüfung misst den Bestand im Prozess statt den Deckel der Route, und das dreiwertige Urteil hat seine neue Grundlage.</done>
</task>

<task type="auto">
  <name>Task 2: Abschnitte 1 bis 3, mit Erfassung der eigenen Datei-Kennungen</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh (der in Task 1 geschriebene Stand)
    - docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh (Zeilen 333 bis 418: Abschnitt 1 Konto, Abschnitt 2 Korpus über WebDAV inklusive Upload-Bilanz und files:scan, Abschnitt 3 Indexierung gegen die langsamste Uhr)
  </read_first>
  <action>
    Setze die Abschnitte 1 bis 3 aus `98b` in `98c` fort. Sie bleiben inhaltlich
    unverändert; eine einzige Erweiterung kommt hinzu.

    Abschnitt 1, das Konto: `skeletondirectory` vorher lesen, auf leer setzen,
    am Ende zurückschreiben; Konto anlegen oder Passwort über
    `--password-from-env` mit `OC_PASS` aus der Umgebung zurücksetzen, nie über
    ein Argument; `occ user:info` ausgeben.

    Abschnitt 2, der Korpus über WebDAV: Zielordner anlegen, jede Datei mit
    `curl -T` hochladen, Antwortcodes zählen, `dateien-hochgeladen` gegen
    `dateien-erwartet` stellen und das Urteil nach `$WORK/upload-urteil`
    schreiben, danach `occ files:scan --path=` auf den Ordner.

    ERWEITERUNG in Abschnitt 2: beim Hochladen wird zusätzlich der Antwortkopf
    gelesen und der Wert des Kopffeldes `OC-FileId` je Datei erfasst. Schreibe
    die Paare als Dateiname und Kennung nach `$WORK/dateiids.txt`. Lies die
    Kopfzeilen mit `curl` in eine Datei des Arbeitsverzeichnisses und schneide
    das Feld mit `sed` heraus; beachte, dass Nextcloud den Wert mit einem
    Instanz-Suffix ausgibt und nur der führende Zifferblock die Datei-Kennung
    ist. Schreibe als Kommentar dazu, warum das hier und nicht später geschieht:
    der Rang einer Datei ist erst messbar, wenn die Datei existiert, und ihre
    Kennung ist beim Hochladen ohne zweite Abfrage zu haben.

    Abschnitt 3, die Indexierung gegen die langsamste Uhr: erst die Frist
    abwarten, mit der ausgeschriebenen Begründung (bis zu 300 s Poller-Backoff
    plus zwei Runden des Fünf-Minuten-Systemcron), dann die Runden mit
    `occ findling:index` und `vorrat_von`, Abbruch der Schleife bei Vorrat 0,
    Urteil nach `$WORK/index-urteil`, und erst DANACH die Verdikte ausgeben,
    weil `skipped:no_text_layer` ein vorläufiges Verdikt ist.

    Weiterhin: kein Passwort auf einer Kommandozeile, keine Maschinenpfade,
    keine Em-Dashes, keine Wagenrückläufe.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh && sh -n "$f" && grep -q 'OC-FileId' "$f" && grep -q 'dateiids.txt' "$f" && grep -q 'password-from-env' "$f" && grep -q 'index-urteil' "$f" && ! grep -q $'\r' "$f" && ! grep -q $'—' "$f" && ! grep -q $'–' "$f" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n` meldet keinen Syntaxfehler
    - Abschnitt 1 liest `skeletondirectory` vorher, setzt es leer und schreibt es am Ende zurück; Passwörter laufen ausschliesslich über `--password-from-env` mit `OC_PASS`
    - Abschnitt 2 schreibt `$WORK/upload-urteil` mit `upload-vollstaendig ja` oder `nein` und ruft `occ files:scan --path=`
    - Abschnitt 2 erfasst `OC-FileId` je hochgeladener Datei und schreibt `$WORK/dateiids.txt` als Paare aus Dateiname und rein numerischer Kennung
    - Abschnitt 3 wartet zuerst die Frist ab, mit ausgeschriebener Begründung, läuft dann in Runden und schreibt `$WORK/index-urteil`
    - Die Verdikte werden erst nach dem Erreichen von Vorrat 0 beziehungsweise dem Rundendeckel ausgegeben
    - Die Datei enthält kein Passwort auf einer Kommandozeile, keinen Wagenrücklauf, kein U+2014 und kein U+2013
  </acceptance_criteria>
  <done>Der Lauf stellt den Korpus her, indexiert ihn und kennt die Kennung jeder eigenen Datei, bevor gemessen wird.</done>
</task>

<task type="auto">
  <name>Task 3: Abschnitt 3b Rangmessung, die zehn Fälle, Bilanz und Abbrüche</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh (der in Task 2 geschriebene Stand, besonders `messbar`, `$WORK/dateiids.txt` und das Verzeichnis `$WORK/fremd`)
    - docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh (Zeilen 418 bis 664: die zehn Fälle, die Bilanz mit zwei Zahlen und der gesamte Bereich unterhalb der tee-Pipeline)
    - docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py (aus Plan 12-04: Format der Ausgabezeile und die Bedeutung von `rang_lex`, `rang_sem` und `ausserhalb`)
  </read_first>
  <action>
    Ergänze `98c` um den neuen Abschnitt 3b, die zehn Fälle, die Bilanz und den
    Bereich unterhalb der Pipeline.

    Überschrift `=== Abschnitt 3b: der Rang der eigenen Datei, im Prozess gemessen ===`
    steht ZWISCHEN Abschnitt 3 und den Fällen und nicht in Abschnitt 0. Schreibe
    den Grund als Kommentar hin: der Rang ist erst messbar, wenn die eigene Datei
    existiert und indexiert ist; das ist die einzige strukturelle Änderung
    gegenüber `98b`.

    Ablauf des Abschnitts:
    1. Eine feste Zuordnung Fall zu Begriff zu erwartetem Dateinamen, als
       Here-Document in derselben Form wie die Begriffsliste in `98b:330`, damit
       sie an einer Stelle steht und lesbar bleibt.
    2. Aus `$WORK/dateiids.txt` und dieser Zuordnung wird die Umgebungsvariable
       `DATEI_IDS` als kommagetrennte Liste von Paaren `begriff=file_id` gebaut.
       Fehlt für einen Fall die Kennung, wird das ausgegeben und der Fall bekommt
       `ausserhalb`, statt den ganzen Lauf abzubrechen.
    3. Ist `$WORK/dateiids.txt` leer oder fehlt sie ganz, wird `rang-erhoben nein`
       nach `$WORK/rang-urteil` geschrieben; der Abbruch mit Code 24 geschieht
       nach der Pipeline. Sonst `rang-erhoben ja`.
    4. Die Sonde wird ein zweites Mal gefahren, diesmal mit `DATEI_IDS` in der
       Umgebung: `sudo --preserve-env=DATEI_IDS docker exec -e DATEI_IDS` auf den
       Container, `/app/.venv/bin/python` auf `/tmp/73-bestand-sonde.py`, Ausgabe
       nach `$WORK/bestand-rang.txt`. Die Datei wird ausgegeben.
    5. Für jeden Fall wird aus der zugehörigen Zeile der kleinere der beiden
       Ränge ermittelt und nach `$WORK/fremd/$FALL` geschrieben; stehen beide auf
       `ausserhalb`, wird das Wort `ausserhalb` geschrieben. Das ist genau der
       Inhalt, den `messbar()` aus Task 1 liest.

    Danach `=== Abschnitt 4: die zehn Faelle, dreiwertig beurteilt ===` mit den
    zehn Fallblöcken aus `98b:428-563`, unverändert in ihren Zusicherungen und
    ihren `jq`-Ausdrücken, jeweils eingeleitet durch den Aufruf von `messbar`
    mit der Fallnummer. Fall 6 behält seine zweite Suche mit dem
    Ausschluss-Operator.

    Bilanz mit ZWEI Zahlen in einer Zeile, wörtlich nach `98b:573-580`:
    bestandene Fälle von zehn, davon nicht messbar.

    Schliesse den Block mit der Pipeline nach `tee "$ZIEL"` und setze ALLE
    Abbrüche darunter, mit dem ausgeschriebenen Grund, dass der Rückgabewert
    einer Pipeline zu `tee` gehört und ein Abbruch innerhalb des Blocks nur die
    Subshell verliesse. Die Reihenfolge der Prüfungen unterhalb der Pipeline:
    `vorpruefung-urteil` (Exit 19), `rang-urteil` (Exit 24), `upload-urteil`
    (Exit 15), `index-urteil` (Exit 16), kein einziger Fall messbar (Exit 23),
    mindestens ein messbarer Fall rot (Exit 17). Am Ende die Fertigzeile
    `98C-SPRACHFAELLE-FERTIG`.

    Weiterhin: kein Passwort auf einer Kommandozeile, keine Maschinenpfade,
    keine Em-Dashes, keine Wagenrückläufe.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/98c-sprachfaelle.sh && sh -n "$f" && grep -q 'Abschnitt 3b' "$f" && grep -q 'DATEI_IDS' "$f" && grep -q '98C-SPRACHFAELLE-FERTIG' "$f" && grep -q 'exit 24' "$f" && test "$(grep -c 'if messbar ' "$f")" -eq 10 && test "$(grep -n 'tee \"\$ZIEL\"' "$f" | cut -d: -f1)" -lt "$(grep -n 'exit 19' "$f" | tail -1 | cut -d: -f1)" && cd "$(git rev-parse --show-toplevel)/backend" && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n` meldet keinen Syntaxfehler
    - Abschnitt 3b steht zwischen Abschnitt 3 und Abschnitt 4 und trägt den Kommentar, warum er dort steht
    - Die Zuordnung Fall zu Begriff zu Dateiname steht als ein einziges Here-Document mit zehn Zeilen
    - `DATEI_IDS` wird ausschliesslich über die Umgebung an die Sonde übergeben, mit `--preserve-env=DATEI_IDS` und `-e DATEI_IDS`
    - Für jeden der zehn Fälle wird eine Datei unter `$WORK/fremd/` geschrieben, deren Inhalt entweder eine Zahl oder das Wort `ausserhalb` ist
    - Abschnitt 4 enthält genau zehn Aufrufe von `messbar` und die unveränderten `jq`-Zusicherungen aus `98b`
    - Die Bilanzzeile nennt bestandene Fälle von zehn und davon nicht messbare
    - Jeder `exit`-Aufruf mit den Codes 15, 16, 17, 19, 23 und 24 steht NACH der Zeile mit `tee "$ZIEL"`
    - Die letzte ausgegebene Zeile ist `98C-SPRACHFAELLE-FERTIG`
    - `pytest tests/test_measurement_scripts.py` läuft grün (Shebang-, Wagenrücklauf-, Gedankenstrich-, Maschinenpfad- und Passwort-Gates greifen über dieses Verzeichnis)
  </acceptance_criteria>
  <done>Die zehn Sprachfälle werden dreiwertig beurteilt auf einer Messgrösse, die die Schwelle 64 erreichen kann, und kein Abbruch verschwindet in einer Rohdatei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Box (Messinstanz) → Container | `docker cp` und `docker exec` tragen Werkzeug in den Prozessraum der ExApp |
| Umgebung (Passwörter) → Prozessliste und Protokolle | `sudo` räumt unter `env_reset` die Umgebung, `docker exec` gibt von sich aus nichts weiter |
| Container-Prozessgrenze | Eine Trefferzahl über diese Grenze wäre das Zähl-Orakel aus T-02-93 |
| Repositorium (öffentlich) ← Messskript und Rohdatei | Beide werden committet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | Information Disclosure | Passwort der Konten `sprachfall` und `lasttest` | mitigate | `occ`-Wrapper mit `sudo --preserve-env=OC_PASS docker exec -e OC_PASS`, Passwortsetzung ausschliesslich über `--password-from-env`; durchgesetzt von `passwords_on_a_command_line` in `test_measurement_scripts.py`, das ab Plan 12-04 über dieses Verzeichnis läuft |
| T-12-20 | Information Disclosure | Zähl-Orakel über die Prozessgrenze | mitigate | Die Zahlen entstehen in der Sonde im Container; `98c` fragt keine Route nach einer Trefferzahl und berührt keinen Produktionscode |
| T-12-21 | Information Disclosure | Rohdatei `05-sprachfaelle.txt` im öffentlichen Repositorium | mitigate | Das Skript druckt Datei-Kennungen und Ränge, keine Dokumentinhalte, keine Passwörter und keine Adressen; alle Maschinenangaben sind Variablen mit Vorgabe |
| T-12-22 | Tampering | gefahrene Messfassung `98b-sprachfaelle.sh` | mitigate | `98c` ist eine neue Datei in einem neuen Laufverzeichnis; der Wächter über `98b` aus Plan 12-04 wird rot, sobald jemand `98b` anfasst |
| T-12-23 | Repudiation | Abbruch verschwindet als Zeile in einer Rohdatei | mitigate | Alle Abbrüche stehen unterhalb der `tee`-Pipeline; als Acceptance-Criterion und im automatisierten `<verify>` geprüft |
| T-12-24 | Elevation of Privilege | `sudo docker exec` in den Container | accept | Der Lauf findet auf einer Wegwerf-Messinstanz statt, die nach der Messung zerstört wird; das Muster ist seit Phase 10 etabliert und unverändert |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket. Vorbedingungen auf der Box sind `jq` (Exit 18), `curl` und `docker`, alle bereits vorhanden |
</threat_model>

<verification>
- Erfolgskriterium 1 der Phase: die Vorprüfung misst über den Prozess statt über die gedeckelte Route, die Schwelle 64 wird prüfbar
- `sh -n` grün, `pytest tests/test_measurement_scripts.py` grün
- `98b-sprachfaelle.sh` und `98-sprachfaelle.sh` sind byteweise unverändert
- Keine Em-Dashes, keine Wagenrückläufe, kein Passwort auf einer Kommandozeile
- Repo-Regeln: Git-Identität NICHT per `-c` übersteuern; sequenziell auf main, Basis-SHA gepinnt
</verification>

<success_criteria>
`98c-sprachfaelle.sh` ist fertig und wird in Phase 15 gefahren, ohne dass
während des Laufs noch etwas daran geändert werden muss. Das dreiwertige Urteil
steht auf einer Zahl, die die Schwelle 64 erreichen kann.
</success_criteria>

<output>
Create `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-05-SUMMARY.md` when done
</output>
