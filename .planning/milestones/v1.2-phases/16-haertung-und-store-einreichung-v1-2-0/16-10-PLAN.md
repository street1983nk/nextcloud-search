---
phase: 16-haertung-und-store-einreichung-v1-2-0
plan: 10
type: execute
wave: 4
depends_on: [16-07]
files_modified:
  - backend/Dockerfile
  - backend/src/findling/config.py
  - backend/tests/test_ocr_languages.py
  - THIRD-PARTY.md
  - backend/tests/test_measurement_scripts.py
  - .planning/BACKLOG.md
autonomous: false
requirements: [BL-F02]
auflagen: []

must_haves:
  truths:
    - "Sechs weitere OCR-Sprachen sind im Abbild vorhanden und ihre Anwesenheit ist beim Bau geprueft"
    - "Der Standard bleibt bei drei Sprachen, damit keine OCR-Seite langsamer wird"
    - "Die Lizenzlage der sechs Pakete steht in THIRD-PARTY.md"
    - "Der Abbruchpfad ist vor dem Bau benannt und wird vor dem Textblock gezogen, nicht danach"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "Sechs apt-Zeilen mit Pin plus sechs Pruefungen nach der Installation"
      contains: "tesseract-ocr-spa"
    - path: "backend/src/findling/config.py"
      provides: "OCR_LANGUAGE_ALLOWLIST mit neun Eintraegen, OCR_DEFAULT_LANGUAGES unveraendert"
      contains: "OCR_LANGUAGE_ALLOWLIST"
    - path: "backend/tests/test_ocr_languages.py"
      provides: "Gate ueber Dockerfile-Text und Liste, nach dem Muster von test_ocr_french.py"
      contains: "list-langs"
  key_links:
    - from: "backend/src/findling/config.py"
      to: "backend/Dockerfile"
      via: "jede Sprache der Liste ist im Abbild installiert und wird dort geprueft"
      pattern: "tesseract-ocr-"
---

<objective>
Backlog-Kandidat BL-F02 Baustein 1: die sechs weiteren OCR-Sprachen (spa, ita,
nld, por, dan, est). Entscheid E4 der 16-CONTEXT.md: **nur ohne Terminrisiko**,
eigener Plan in der letzten Welle vor der Einreichung, und wenn die Abgabe in
Verzug geraet, faellt er ersatzlos aufs Folgerelease.

Purpose: Die sechs Store-Texte sind in dieser Phase ohnehin offen (Messzahl,
Connector-Gate). Ein zweiter Durchgang durch dieselben sechs Texte in einem
Folgerelease waere derselbe Aufwand noch einmal. Deshalb jetzt, und deshalb VOR
dem Textblock.

Output: sechs Sprachpakete, eine erweiterte Liste, ein Gate, ein Lizenzeintrag.

**Der Textteil gehoert NICHT in diesen Plan.** Alle sechs Store-Texte nennen
heute "German, English, French"; neun Sprachen heisst neun Sprachen im Text,
dreisprachig, in zwei `info.xml` und in `docs/store-listing.md`. Diese Aenderung
macht geschlossen der Plan 16-11, und er erfaehrt aus der SUMMARY dieses Plans,
ob es drei oder neun sind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md
@.planning/BACKLOG.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Das Tor, vor dem Bau</name>
  <decision>Faehrt Baustein 1 in v1.2.0 mit, oder faellt er aufs Folgerelease?</decision>
  <context>
Entscheid E4 bindet diesen Plan an eine Bedingung, und die Bedingung wird VOR
dem Bau geprueft und nicht danach.

Vorzulegen sind vier Zeilen:

1. **Der Stand der Phase**: welche Plaene abgeschlossen sind, welche offen, und
   ob der Upgrade-Beweis (16-09) gruen ist. Ein offener Befund aus 16-09 wiegt
   schwerer als sechs Sprachpakete.
2. **Der Umfang**, ehrlich: sechs apt-Zeilen mit Pin, sechs Pruefungen beim Bau,
   sechs Lizenzdateien, `OCR_LANGUAGE_ALLOWLIST` von drei auf neun,
   `THIRD-PARTY.md`, ein Gate, `PACKAGE_TREE_HASH_TODAY`, und die Abbildgroesse,
   die `docker.yml` prueft.
3. **Die Annahme A7 der Recherche**: dass die sechs Pakete in Debian trixie mit
   `1:4.1.0-2` und `Architecture: all` existieren, ist aus dem Backlog
   uebernommen und nicht gegen die Paketquelle nachgesehen. Das wird VOR dem Bau
   einmal nachgesehen; faellt die Pruefung anders aus, ist das allein schon ein
   Grund fuer den Abbruchpfad.
4. **Der Abbruchpfad**: wenn der Bau ins Rutschen kommt oder ein Paket fehlt,
   faellt Baustein 1 heraus, die Texte bleiben bei drei Sprachen, und der
   Backlog-Eintrag wird fortgeschrieben. Kein halber Einbau.
  </context>
  <options>
    <option id="mitfahren">
      <name>Baustein 1 faehrt mit</name>
      <pros>Die Store-Texte sind ohnehin offen; ein zweiter Durchgang entfaellt</pros>
      <cons>Sechs Pakete wachsen das Abbild, und der Bau muss vor dem Textblock fertig sein</cons>
    </option>
    <option id="folgerelease">
      <name>Baustein 1 faellt aufs Folgerelease</name>
      <pros>Kein Terminrisiko fuer die Abgabe, die Bedingung des Owners ist gewahrt</pros>
      <cons>Die sechs Texte werden im Folgerelease ein zweites Mal angefasst</cons>
    </option>
  </options>
  <read_first>
    - .planning/BACKLOG.md, Eintrag BL-F02 im Wortlaut, besonders die Nebenbedingung und die drei Bausteine
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md, Entscheid E4
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md, Abschnitt "BL-F02 Baustein 1" mit den sieben Arbeitsschritten und Annahme A7
    - backend/Dockerfile, der apt-Block mit den drei Sprachen und der Pruefung danach
    - Die Paketquelle von Debian trixie fuer `tesseract-lang` (Version und Architektur der sechs Pakete)
  </read_first>
  <acceptance_criteria>
    - Die Existenz der sechs Pakete mit Version `1:4.1.0-2` und `Architecture: all` ist gegen die Paketquelle nachgesehen, und das Ergebnis liegt dem Owner vor.
    - Der Stand der Phase liegt in vier Zeilen vor, einschliesslich des Zustands von 16-09.
    - Die Antwort des Owners steht im Wortlaut in der SUMMARY.
    - Bei `folgerelease` endet der Plan hier, BL-F02 wird in `.planning/BACKLOG.md` mit dem Datum und dem Grund fortgeschrieben, und Plan 16-11 erfaehrt "drei Sprachen".
  </acceptance_criteria>
  <resume-signal>Antwort des Owners: "mitfahren" oder "folgerelease"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Sechs Sprachpakete im Abbild, sechs Pruefungen, eine Liste</name>
  <files>backend/Dockerfile, backend/src/findling/config.py, THIRD-PARTY.md</files>
  <read_first>
    - backend/Dockerfile, der apt-Block mit `tesseract-ocr`, `-deu`, `-eng`, `-fra`, `-osd`, je mit `=1:4.1.0-2`, und die Pruefung `tesseract --list-langs` danach; dazu die Stelle, an der die Lizenzdateien abgelegt werden
    - backend/src/findling/config.py, `OCR_DEFAULT_LANGUAGES` und `OCR_LANGUAGE_ALLOWLIST` samt der Behandlung von `FINDLING_OCR_LANGUAGES` bei unbekannten Werten
    - THIRD-PARTY.md, die Eintraege der drei bestehenden Sprachpakete (Form, Lizenz, Quelle)
    - backend/tests/test_ocr_french.py, vollstaendig (das Vorbild eines Gates, das den Dockerfile als Text liest)
  </read_first>
  <action>
**Der Dockerfile.** Sechs weitere Zeilen im bestehenden apt-Block:
`tesseract-ocr-spa`, `-ita`, `-nld`, `-por`, `-dan`, `-est`, je mit `=1:4.1.0-2`
wie die drei bestehenden, aus derselben Quelle und mit derselben Pin-Logik. Die
Pruefung nach der Installation wird um dieselben sechs Sprachcodes erweitert;
eine Installation ohne Pruefung ist fuer die neuen Sprachen stumm, und genau das
prueft das bestehende Muster ab. Die Lizenzdateien werden nach dem Muster der
bestehenden mitgenommen.

**Die Liste.** `OCR_LANGUAGE_ALLOWLIST` waechst von drei auf neun Eintraege.
`OCR_DEFAULT_LANGUAGES` bleibt bei **drei**: ein Standard mit neun Sprachen
macht jede OCR-Seite langsamer, und das waere eine Verhaltensaenderung fuer jede
bestehende Installation, die niemand bestellt hat. Der Kommentar sagt beides mit
seinem Grund. Die Behandlung unbekannter Werte in `FINDLING_OCR_LANGUAGES`
bleibt unveraendert (Warnung und Rueckfall).

**THIRD-PARTY.md.** Sechs Eintraege in der Form der drei bestehenden: Paket,
Version, Lizenz, Quelle. Kein Eintrag ohne Lizenzangabe.

Nicht anfassen: der OCR-Pfad selbst, die Seitendeckel, die DPI, das Zeitlimit je
Seite. Dieser Plan macht Sprachen verfuegbar und aendert kein Verhalten.
  </action>
  <verify>
    <automated>grep -c "tesseract-ocr-spa" backend/Dockerfile && grep -c "est" backend/src/findling/config.py && grep -c "tesseract" THIRD-PARTY.md</automated>
  </verify>
  <acceptance_criteria>
    - Sechs neue apt-Zeilen, je mit `=1:4.1.0-2`, im bestehenden Block.
    - Die `--list-langs`-Pruefung nennt alle neun Sprachcodes.
    - `OCR_LANGUAGE_ALLOWLIST` hat neun Eintraege, `OCR_DEFAULT_LANGUAGES` unveraendert drei, und der Kommentar begruendet beides.
    - `THIRD-PARTY.md` traegt sechs neue Eintraege, je mit Lizenz und Quelle.
    - Der Abbildbau ist in CI gelaufen (`docker.yml`), und die SUMMARY nennt Laufnummer sowie die Abbildgroesse vorher und nachher.
    - Keine Aenderung am OCR-Pfad, an Deckeln oder Zeitlimits.
  </acceptance_criteria>
  <done>Neun Sprachen sind im Abbild, drei sind der Standard, und jede ist beim Bau geprueft.</done>
</task>

<task type="auto">
  <name>Task 3: Das Gate und der Paket-Baumhash</name>
  <files>backend/tests/test_ocr_languages.py, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/tests/test_ocr_french.py, vollstaendig (wie dieses Repositorium den Dockerfile als Text prueft und was es dabei behauptet)
    - backend/src/findling/config.py nach Task 2
    - backend/tests/test_measurement_scripts.py, Zeilen 360 bis 380 und 570 bis 590 (PACKAGE_FILES, PACKAGE_TREE_HASH_TODAY und ihre Kommentarkette)
  </read_first>
  <action>
**Das Gate.** `backend/tests/test_ocr_languages.py`, nach dem Muster von
`test_ocr_french.py`, mit drei Zusagen:

1. Jede Sprache aus `OCR_LANGUAGE_ALLOWLIST` hat im Dockerfile eine
   apt-Zeile mit Pin. Die Liste wird aus `config.py` gelesen und nicht im Test
   wiederholt: eine im Test wiederholte Liste ist sich selbst einig an dem Tag,
   an dem jemand eine zehnte Sprache aufnimmt.
2. Jede Sprache der Liste kommt in der `--list-langs`-Pruefung vor.
3. `OCR_DEFAULT_LANGUAGES` ist eine echte Teilmenge der Liste und hat genau drei
   Eintraege; der Docstring sagt, warum der Standard nicht mitwaechst.

**Der Paket-Baumhash.** `config.py` liegt unter `backend/src/findling`, also
zieht `PACKAGE_TREE_HASH_TODAY` mit, im **selben Commit**. `PACKAGE_FILES`
bleibt bei 54, weil keine Datei dazukommt; liefert die Rezeptur eine andere
Zahl, ist das ein Befund. Die Kommentarkette bekommt ihren Eintrag mit Datum,
Plan (16-10) und Grund. Die historischen Zwillinge bleiben unberuehrt.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_ocr_languages.py tests/test_measurement_scripts.py -q && uv run ruff check tests && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_ocr_languages.py` hat drei Faelle, alle gruen, und liest die Liste aus `config.py`.
    - Ein Fall wuerde rot, wenn eine Sprache in der Liste stuende, aber nicht im Dockerfile; das ist mit einer gestagten Mutation im Testtext nachgewiesen.
    - `PACKAGE_TREE_HASH_TODAY` ist nachgezogen, `PACKAGE_FILES` steht bei 54, historische Zwillinge unveraendert.
    - Aenderung an `config.py` und Hash-Nachzug liegen im selben Commit.
    - Volle Suite gruen, Skipzahl unveraendert gegen den Stand vor diesem Plan.
  </acceptance_criteria>
  <done>Die Liste und das Abbild koennen nicht mehr auseinanderlaufen, ohne rot zu werden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Distributionsquelle zu Abbild | Sechs weitere Pakete aus derselben Signaturkette wie das Basisabbild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-36 | Tampering | Ein Sprachpaket fehlt und die Installation ist dafuer stumm | mitigate | `tesseract --list-langs` prueft alle neun nach der Installation, und ein Gate haelt Liste und Dockerfile zusammen |
| T-16-37 | Denial of Service | Ein Standard mit neun Sprachen macht jede OCR-Seite langsamer | mitigate | `OCR_DEFAULT_LANGUAGES` bleibt bei drei; ein Fall haelt die Zahl fest |
| T-16-38 | Tampering | Eine Paketversion driftet vom Pin | mitigate | `=1:4.1.0-2` je Zeile, wie bei den drei bestehenden |
| T-16-39 | Repudiation | Die Lizenzlage der neuen Pakete ist nirgends festgehalten | mitigate | Sechs Eintraege in `THIRD-PARTY.md`, je mit Lizenz und Quelle |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Kein Paket aus npm, PyPI oder crates.io; die sechs stammen aus `tesseract-lang` in Debian trixie, `Architecture: all`, gepinnt, und ihre Existenz wird in Task 1 gegen die Paketquelle nachgesehen |
</threat_model>

<verification>
1. Der Owner hat vor dem Bau entschieden, und die Paketexistenz war dabei nachgesehen.
2. Neun Sprachen im Abbild, drei als Standard, alle neun beim Bau geprueft.
3. Gate haelt Liste und Dockerfile zusammen; Paket-Baumhash nachgezogen.
4. Abbildgroesse vorher und nachher in der SUMMARY.
5. Die SUMMARY sagt in einem Wort, ob Plan 16-11 drei oder neun Sprachen in die Texte schreibt.
</verification>

<success_criteria>
Entweder sind neun OCR-Sprachen im Abbild und der Textblock weiss es, oder
Baustein 1 ist mit Datum und Grund aufs Folgerelease verschoben.
</success_criteria>

<output>
Create `.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-10-SUMMARY.md` when done
</output>
