---
phase: 11-haertung-und-store-einreichung-v1-1
plan: 11
type: execute
wave: 7
depends_on: ["11-09", "11-10"]
files_modified:
  - php/appinfo/info.xml
  - backend/appinfo/info.xml
autonomous: false
requirements: [REL-01]

must_haves:
  truths:
    - "Beide Apps tragen dieselbe Version 1.1.0, und der image-tag nennt dieselbe Zahl"
    - "Der Owner hat die Einreichung freigegeben, nachdem alle Gates gruen und die Texte abgenommen waren"
    - "Das Abbild ghcr.io/street1983nk/findling_backend:1.1.0 liegt als Manifestindex mit amd64 und arm64 in ghcr, geprueft und nicht angenommen"
    - "Beide Haelften sind im Store, mit HTTP 201 im Log belegt"
    - "Die eingereichten Assets sind genau die, die release.yml signiert hat; es gibt keinen zweiten Bau"
  artifacts:
    - path: "php/appinfo/info.xml"
      provides: "version 1.1.0"
      contains: "1.1.0"
    - path: "backend/appinfo/info.xml"
      provides: "version 1.1.0 und image-tag 1.1.0"
      contains: "1.1.0"
  key_links:
    - from: "git tag v1.1.0"
      to: "release.yml und docker.yml"
      via: "beide haengen am selben Tag-Muster"
      pattern: "v1.1.0"
    - from: "release.yml Assets"
      to: "store-submit.yml"
      via: "dieselbe Asset-URL, kein zweiter Bau"
      pattern: "releases/download"
---

<objective>
Die Abgabe. Drei Zahlen in einem Commit, ein Tag, ein Release mit vier Assets,
eine Pruefung in ghcr und zwei Antworten mit HTTP 201.

Purpose: Der Weg ist fuenfmal gefahren (v1.0.0 bis v1.0.3, Laeufe 34143894059
bis 34172689400), und Erfolgskriterium 5 ist mit dieser Kette automatisch
erfuellt, weil store-submit.yml genau die URL des Assets uebergibt, das
release.yml signiert und hochgeladen hat. Es gibt keinen zweiten Bau, und das
ist Absicht: ein tar.gz ist nicht byte-reproduzierbar.

D-10: eingereicht wird so frueh wie moeglich, sobald alle Gates gruen sind und
der Owner Texte und FR-Gate abgenommen hat. Das Zielfenster 16. bis 22.09. ist
die Obergrenze und kein Startsignal, und die Einreichung wartet nicht auf den
ISV-Call und nicht auf den stable35-RE-CHECK.

Output: v1.1.0 im Nextcloud App Store, beide Haelften, mit Belegen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-CONTEXT.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der Versionsbump, drei Stellen, ein Commit</name>
  <files>php/appinfo/info.xml, backend/appinfo/info.xml</files>
  <read_first>
    - php/appinfo/info.xml Zeile 113 (version) und der Kommentar bei Zeile 107, der die Kopplung an die andere Haelfte und den image-tag beschreibt
    - backend/appinfo/info.xml Zeile 136 (version) und Zeile 230 (image-tag)
    - backend/tests/test_lockstep_versions.py: was genau die drei Werte gegeneinander haelt
    - .github/workflows/release.yml, das Version-Gate (Tag gleich beide version-Angaben)
    - .github/workflows/docker.yml, das Version-Gate und der Pfadfilter auf backend/**
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 3.3 mit der neunzeiligen Handlungstabelle und Pitfall 10
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md, Entscheid V-2 (bei V-2b aendert sich hier zusaetzlich das Versionsfenster)
  </read_first>
  <action>
Setze in einem einzigen Commit alle drei Werte auf `1.1.0`: die version in
php/appinfo/info.xml, die version in backend/appinfo/info.xml und den
image-tag in backend/appinfo/info.xml. Drei Gates lesen diesen einen Wert
(test_lockstep_versions.py, docker.yml und release.yml), und ein Bump auf einer
Seite allein macht docker.yml rot, bevor es baut.

Wurde in V-2 die Option b gewaehlt, gehoert die Fenstersenkung auf
max-version 34 in denselben Commit, weil sie dieselben zwei Dateien beruehrt
und weil ein Fenster, das nach dem Tag korrigiert wird, ein zweites Release
ist.

Fahre danach die volle Gate-Kette lokal, nicht nur pytest: ruff check, ruff
format --check, pyright, vulture, dazu die PHP-Seite ueber php.yml, soweit sie
lokal faehrt. Erst wenn alles gruen ist, wird committet. Die Owner-Regel
verlangt lokal gruen vor dem Commit.

Kein Tag in diesem Task. Der Tag folgt nach der Freigabe.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest tests/test_lockstep_versions.py tests/test_store_metadata.py -q &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<version>1.1.0</version>" php/appinfo/info.xml` ergibt 1.
    - `grep -c "<version>1.1.0</version>" backend/appinfo/info.xml` ergibt 1.
    - `grep -c "<image-tag>1.1.0</image-tag>" backend/appinfo/info.xml` ergibt 1.
    - `grep -c "1.0.3" php/appinfo/info.xml backend/appinfo/info.xml` findet die alte Zahl nur noch in Kommentaren, die sie ausdruecklich als Vorgaenger bezeichnen.
    - Alle drei Werte stehen in genau einem Commit; `git show --stat HEAD` nennt genau die beiden info.xml (bei V-2b dieselben zwei Dateien).
    - Die gesamte Gate-Kette laeuft gruen.
  </acceptance_criteria>
  <done>Beide Haelften tragen dieselbe Version, und der image-tag nennt dieselbe Zahl, in einem Commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Einreichungs-Freigabe</name>
  <files>docs/store-listing.md</files>
  <read_first>
    - docs/store-listing.md, die Abnahmezeile aus Plan 11-09
    - docs/l10n-french.md, die FR-Gate-Zeile aus Plan 11-05
    - docs/audits/2026-09-phase-11/README.md, das Frontmatter mit findings und still_open
    - die Laufnummern der gruenen Laeufe aus 11-04 und 11-07
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-CONTEXT.md, D-10 und D-11
  </read_first>
  <action>
Stelle die sechs Voraussetzungen mit ihren Belegen zusammen (Laufnummern,
Abnahmezeilen, Auditfrontmatter) und lege sie dem Owner vor. Setze keinen Tag,
bevor die Freigabe da ist; git tag -l v1.1.0 muss bis dahin leer sein. Nach der
Antwort traegst du in docs/store-listing.md die Zeile
Einreichung freigegeben: Datum (D-10) ein. Weist das Auditfrontmatter einen
Befund der Schwere critical oder high aus, nennt die Freigabezeile
ausdruecklich, welcher Befund trotzdem mitgeht und warum.
  </action>
  <what-built>
Alles, was die Abgabe voraussetzt, steht:

1. **Fremdinstallation, amd64 und arm64** aus Store-Archiven, gruen, mit
   Laufnummer (Plan 11-04, Erfolgskriterium 1).
2. **Upgrade 1.0.3 auf 1.1.0** Ende zu Ende, gruen, mit Laufnummer, alle sechs
   Nachher-Zusicherungen bestanden (Plan 11-07, Erfolgskriterium 2).
3. **Drei Audits** gefahren, jeder Befund mit Verdikt, `still_open` im
   Frontmatter (Plan 11-10, Erfolgskriterium 3).
4. **Texte abgenommen** mit Datum und Fassung, **FR-Gate in beiden Teilen
   durch** (Plaene 11-05 und 11-09, Erfolgskriterium 4, D-06, D-07, D-08).
5. **Beide Versionen auf 1.1.0**, image-tag mit, in einem Commit, alle Gates
   lokal gruen (Task 1).
6. **Werkzeug-Fixe** DI-10-01 und DI-10-02 gegen den echten Lastkorpus belegt,
   Box angehalten, Kosten unter dem Deckel (Plan 11-06).

Was nach diesem Checkpoint passiert und nicht mehr rueckgaengig zu machen ist:
der Tag v1.1.0 loest release.yml und docker.yml aus, das GitHub-Release
entsteht mit vier Assets, und die Store-Registrierung ist danach nicht mehr
editierbar.

Noch offen und ausdruecklich nicht blockierend (D-11): der stable35-RE-CHECK
vom 16.09.2026. Nextcloud 35 ist Vorabversion, das Versionsfenster steht nach
Entscheid V-2, und die Einreichung wartet nicht darauf.
  </what-built>
  <how-to-verify>
1. Lies das Frontmatter von `docs/audits/2026-09-phase-11/README.md`: sind
   `findings.critical` und `findings.high` null, und ist `still_open` leer?
2. Pruefe die beiden Abnahmezeilen (Textabnahme in docs/store-listing.md,
   FR-Gate in docs/l10n-french.md) auf Datum und Vollstaendigkeit.
3. Sieh dir die genannten Laufnummern an, wenn du magst; beide Laeufe stehen
   auf success.
4. Gib den Tag frei oder nenne, was noch fehlt.
  </how-to-verify>
  <verify>
    <human-check>Der Owner hat die Abgabe freigegeben; die Freigabe steht mit Datum in docs/store-listing.md.</human-check>
    <automated>grep -c "Einreichung freigegeben:" docs/store-listing.md</automated>
  </verify>
  <acceptance_criteria>
    - In docs/store-listing.md steht eine Zeile `Einreichung freigegeben: <Datum> (D-10)`.
    - Das Auditfrontmatter weist `critical: 0` und `high: 0` aus; andernfalls nennt die Freigabezeile ausdruecklich, welcher Befund trotzdem mitgeht und warum.
    - Kein Tag ist vor dieser Zeile gepusht (`git tag -l v1.1.0` ist leer).
  </acceptance_criteria>
  <resume-signal>Antworte mit "freigegeben, taggen" oder nenne, was noch fehlt.</resume-signal>
  <done>Die Abgabe hat eine datierte Freigabe, und der Owner kennt die Unumkehrbarkeit.</done>
</task>

<task type="auto">
  <name>Task 3: Tag, Release, ghcr-Pruefung, Store-Submission</name>
  <files>docs/store-listing.md</files>
  <read_first>
    - .github/workflows/release.yml vollstaendig: das Version-Gate, die Zertifikatspruefung ueber den DER, die Reihenfolge stage, Codesignatur, packen, Release-Signatur, und `assert-contents`
    - .github/workflows/store-submit.yml: die Eingaben tag, register, direct_download, direct_signature, und dass 200 und 201 akzeptiert werden
    - .github/workflows/docker.yml: der Tag-Trigger, der Pfadfilter auf backend/**, das Version-Gate und der Hinweis, dass das ghcr-Paket oeffentlich sein muss
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 3.2 und 3.3 und Pitfall 10 und Pitfall 11
    - docs/certificates.md, die beiden Fingerabdruecke
  </read_first>
  <action>
1. Tag `v1.1.0` auf main setzen und pushen. Beide Workflows haengen daran,
   docker.yml zusaetzlich am Pfadfilter `backend/**`; der Releasecommit
   beruehrt backend/appinfo/info.xml, also greift der Filter.
2. `release.yml` abwarten. Am Ende steht ein GitHub-Release mit genau vier
   Assets: findling.tar.gz, findling.tar.gz.sig, findling_backend.tar.gz,
   findling_backend.tar.gz.sig. Pruefe die Zahl und die Namen mit
   `gh release view v1.1.0`.
3. **Vor der Store-Submission**: pruefen, nicht annehmen, dass
   `ghcr.io/street1983nk/findling_backend:1.1.0` als Manifestindex mit
   linux/amd64 und linux/arm64 in ghcr liegt. Das ist Pitfall 10: steht v1.1.0
   im Store ohne das Abbild, scheitert jede Nutzerinstallation am Pull. Nutze
   eine Abfrage, die die Plattformen des Manifestindex nennt, und schreibe die
   Ausgabe in das SUMMARY.
4. `store-submit.yml` mit `tag=v1.1.0` und `register=false` anstossen. Die
   Kennungen sind seit dem 07.09. registriert; `register` bleibt aus.
5. Das Log lesen und beide Antworten belegen. Erwartet sind zwei Zeilen mit
   HTTP 201, je eine Haelfte, gefolgt von der Bestaetigungszeile.
6. Von aussen gegenpruefen:
   `curl https://apps.nextcloud.com/api/v1/apps.json?version=34.0.0` listet fuer
   findling jetzt auch 1.1.0.
7. Die Belege in docs/store-listing.md unter der Freigabezeile festhalten:
   Tag, Release-Laufnummer, Submission-Laufnummer, die beiden HTTP-Codes und
   das Datum.

Baue das Archiv unter keinen Umstaenden ein zweites Mal. Es wird genau einmal
gebaut, und die Signatur gehoert zu genau diesen Bytes; ein zweiter Bau
erzeugte eine Signatur zu einer Datei, die niemand hat. Und wenn eine Signatur
fehlschlaegt: `tr -d '\r'` steht an beiden Schluesseln in release.yml, mit
Laufnummer 33895245084 als Beleg, und wird nicht wegoptimiert.
  </action>
  <verify>
    <automated>gh release view v1.1.0 --json assets --jq '.assets | length' | grep -c 4 &amp;&amp; gh run list --workflow=store-submit.yml --limit 1 --json conclusion --jq '.[0].conclusion' | grep -c success</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v1.1.0` nennt den Tag, und er zeigt auf den Releasecommit.
    - Das GitHub-Release v1.1.0 traegt genau vier Assets mit den erwarteten Namen.
    - Die ghcr-Abfrage belegt einen Manifestindex fuer 1.1.0 mit linux/amd64 UND linux/arm64; die Ausgabe steht im SUMMARY.
    - Der Lauf von store-submit.yml steht auf success, und das Log zeigt fuer beide Haelften HTTP 201.
    - Die Store-API listet fuer findling die Fassung 1.1.0.
    - docs/store-listing.md traegt die Belegzeile mit Tag, beiden Laufnummern, beiden HTTP-Codes und Datum.
    - Es gibt genau einen Bau der Archive; kein Schritt hat sie nach der Signatur neu erzeugt.
  </acceptance_criteria>
  <done>v1.1.0 steht als signiertes App-Paar im Nextcloud App Store, und jeder Schritt hat einen Beleg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub-Secret-Store zu CI-Job | APPSTORE_TOKEN, APP_PRIVATE_KEY und BACKEND_PRIVATE_KEY leben nur im Job, der taggetriggert laeuft |
| Repository zu apps.nextcloud.com | Die Registrierung ist nach der Abgabe nicht mehr editierbar |
| ghcr zu Nutzerinstanz | Jede Installation zieht das Abbild anonym; ein fehlender Tag ist ein Totalausfall beim Nutzer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-46 | Elevation of Privilege | Signierschluessel in einem Fork | mitigate | release.yml hat keinen pull_request-Trigger; die Abwesenheit ist die Hauptverteidigung und wird in diesem Plan nicht angetastet |
| T-11-47 | Tampering | Archiv nach der Signatur neu gebaut | mitigate | Genau ein Bau, `assert-contents` ueber beide Archive, Verifikation der Release-Signatur gegen das Zertifikat statt Vertrauen |
| T-11-48 | Denial of Service | Tag im Store ohne Abbild in ghcr | mitigate | Pflichtpruefung des Manifestindex fuer 1.1.0 mit beiden Plattformen VOR der Store-Submission, Ausgabe im SUMMARY |
| T-11-49 | Spoofing | falsches Zertifikat | mitigate | Subject mit RFC2253 und SHA-256 ueber den DER des Public Key gegen die Fingerabdruecke in docs/certificates.md, unveraendert aus release.yml |
| T-11-50 | Information Disclosure | Schluesseldateien im Runner | mitigate | release.yml loescht sie mit `if: always()`; der Plan aendert daran nichts |
| T-11-51 | Repudiation | Abgabe ohne Freigabe | mitigate | Blockierender Checkpoint vor dem Tag, Freigabezeile mit Datum in docs/store-listing.md, `git tag -l` als Gegenprobe |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Keiner. Die Abgabe installiert kein Paket; die Abhaengigkeiten sind gegen v1.0.3 nur Patch-Bumps exakt gepinnter Zeilen, das Audit stellt es fest |
</threat_model>

<verification>
- `gh release view v1.1.0` nennt vier Assets
- Manifestindex 1.1.0 in ghcr mit linux/amd64 und linux/arm64
- store-submit.yml auf success, zwei Antworten mit HTTP 201 im Log
- `curl https://apps.nextcloud.com/api/v1/apps.json?version=34.0.0` listet 1.1.0
- Belegzeile in docs/store-listing.md
</verification>

<success_criteria>
Erfolgskriterium 5 ist erfuellt: v1.1 ist eingereicht, und die Release-Artefakte
sind genau die, die eingereicht wurden, weil store-submit.yml die URL des
signierten Assets uebergibt und es keinen zweiten Bau gibt. REL-01 ist damit
abgehakt.
</success_criteria>

<output>
Create `.planning/phases/11-haertung-und-store-einreichung-v1-1/11-11-SUMMARY.md` when done
</output>
