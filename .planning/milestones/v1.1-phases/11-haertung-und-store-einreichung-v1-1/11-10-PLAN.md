---
phase: 11-haertung-und-store-einreichung-v1-1
plan: 10
type: execute
wave: 6
depends_on: ["11-04", "11-06", "11-07", "11-08", "11-09", "11-13"]
files_modified:
  - docs/audits/2026-09-phase-11/README.md
  - .planning/phases/11-haertung-und-store-einreichung-v1-1/deferred-items.md
  - .github/workflows/measure.yml
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "Security-, Bug- und Performance-Audit sind ueber den Diff der Phase 11 gefahren, und dieser Diff enthaelt Produktionscode"
    - "Jeder Befund ab MEDIUM ist vor dem Phasenabschluss geschlossen, jeder LOW ist dokumentiert entschieden"
    - "Die drei geerbten Befunde DI-07-02, DI-07-03 und T-09-29 sind im Audit entschieden, keiner bleibt offen"
    - "DI-10-05 ist entschieden, DI-10-04 ist dokumentiert weitergereicht"
  artifacts:
    - path: "docs/audits/2026-09-phase-11/README.md"
      provides: "der Auditbericht der Phase mit Frontmatter nach dem Schema der Phase 10"
      contains: "still_open"
      min_lines: 150
    - path: ".planning/phases/11-haertung-und-store-einreichung-v1-1/deferred-items.md"
      provides: "die Befunde, die diese Phase weiterreicht, mit Ziel und Begruendung"
    - path: ".github/workflows/measure.yml"
      provides: "die Praezisierung im Kopf, die DI-10-05 vollzieht"
      contains: "baumhash"
  key_links:
    - from: "docs/audits/2026-09-phase-11/README.md"
      to: ".planning/phases/07-gemeinsame-embedding-engine/deferred-items.md"
      via: "DI-07-02 und DI-07-03, beide mit Verdikt"
      pattern: "DI-07-0"
    - from: "docs/audits/2026-09-phase-11/README.md"
      to: "php/l10n/fr.json"
      via: "ASVS V5, die 173 beziehungsweise 174 neuen Werte auf Markup durchgesehen"
      pattern: "V5"
---

<objective>
Das Audit-Gate der Owner-Regel vom 15.08.2026, zum letzten Mal vor der Abgabe.

Purpose: Anders als Phase 10 aendert diese Phase Produktionscode: `php/l10n/fr.*`
liegen im Companion-Archiv und gehen an jeden Nutzer, dazu kommen
`scripts/ops/search_load.py`, neue Tests und geaenderte Workflows. Das
Phase-10-Audit konnte mit einem Satz sagen "diese Phase misst, sie baut
nicht"; dieses kann es nicht.

Dazu kommen vier Befunde, die frueheren Phasen gehoeren und hier entschieden
werden muessen, weil sie hier hingereicht wurden.

Output: Ein Auditbericht nach dem Muster der Phase 10, mit einem Verdikt je
Befund und ohne offene Zeile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Das Security-Audit ueber den Diff der Phase</name>
  <files>docs/audits/2026-09-phase-11/README.md</files>
  <read_first>
    - docs/audits/2026-09-phase-10/README.md vollstaendig: das Frontmatter-Schema (phase, audited, diff, files_reviewed, findings mit critical/high/medium/low/total, status, fix_run, fix_commits, fixed, still_open), der Aufbau der Abschnitte, die Diff-Tabelle nach Verzeichnis
    - docs/audits/2026-09-phase-09/README.md als das Muster, auf das der Phase-10-Bericht selbst zeigt
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 5.4 (die vorgeschlagenen ASVS-Kategorien und die Begruendung je Kategorie) und der Abschnitt Security Domain mit der Tabelle bekannter Bedrohungsmuster
    - der Basis-SHA der Phase (der Commit vor dem ersten Plan dieser Phase) und `git diff --stat <basis>..HEAD`
    - php/l10n/fr.json und php/l10n/fr.js (die Werte, die unter V5 durchgesehen werden)
    - backend/tests/test_workflow_pins.py und die geaenderten Workflows aus 11-04 und 11-07
  </read_first>
  <action>
Lege docs/audits/2026-09-phase-11/README.md an, mit demselben Frontmatter-Schema
wie der Phase-10-Bericht. `diff` traegt den Bereich `<basis>..HEAD` mit dem
echten Basis-SHA, `files_reviewed` die aus dem Diff gezaehlte Zahl, nicht eine
geschaetzte.

Beginne wie das Vorbild mit der einen Frage und ihrer Antwort: aendert diese
Phase Produktionscode? Die Antwort ist ja, und die Belegzeile ist die
Diff-Tabelle nach Verzeichnis. Nenne ausdruecklich, welche geaenderten Dateien
im Companion-Archiv liegen und damit an jeden Nutzer gehen.

Fahre die ASVS-Kategorien einzeln, jede mit Beleg statt mit einer Meinung:
    V2 Authentication und V3 Session Management: nicht beruehrt. Ausdruecklich
    benennen und nicht weglassen, so wie Phase 10 es haelt.
    V4 Access Control: die Kette aus SQLite-Vorfilter im Container und finalem
    PHP-Recheck ist unveraendert. Belege es am Diff: bei V-1b keine Aenderung
    unter php/lib/Search/ und php/lib/Service/Search*, bei V-1a genau die
    Aenderungen aus Plan 11-13 und die Feststellung, dass sie einen Zustand und
    einen Text hinzufuegen und keine zweite Grenze. Die drei Belegzeilen dafuer:
    MAX_ROUNDS ist unveraendert 3, isReadable() ist die einzige
    Berechtigungsfrage geblieben, und reduceIds ist nicht angefasst.
    V5 Input Validation und Output Encoding: die neuen Katalogwerte laufen
    durch die Uebersetzungsfunktion ins Template. Sieh alle FR-Werte einmal auf
    spitze Klammern, Ampersand und Anfuehrungszeichen durch, maschinell und mit
    der Zahl im Bericht. Nenne, dass das Escaping-Gate das Template prueft und
    nicht den Katalog, und dass das Dash-Gate seit 11-08 die Kataloge mitliest.
    V6 Cryptography: die Signaturkette. Belege, dass release.yml keinen
    pull_request-Trigger hat, dass der Fingerabdruck ueber den DER des Public
    Key laeuft und dass `tr -d '\r'` an beiden Schluesseln steht.
    V14 Configuration und Build: der neue arm64-Runner, der Release-Asset-Modus,
    die Workflow-Pins, der HaRP-Digest als Manifestindex, und der Versionsbump
    an drei Stellen als kommender Vorgang.

Jeder Befund bekommt eine Kennung, eine Schwere, eine Belegstelle und eine
Disposition. Kein Befund bleibt ohne Verdikt.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; python -c "import re,sys; t=open('../docs/audits/2026-09-phase-11/README.md',encoding='utf-8').read(); [sys.exit('fehlt: '+k) for k in ('phase:','audited:','diff:','files_reviewed:','findings:','status:','still_open:') if k not in t]; print('frontmatter vollstaendig')"</automated>
  </verify>
  <acceptance_criteria>
    - Das Frontmatter traegt alle Felder des Phase-10-Schemas, und `diff` nennt einen echten SHA-Bereich.
    - `files_reviewed` stimmt mit der Zahl aus `git diff --stat <basis>..HEAD` ueberein.
    - Alle sechs ASVS-Kategorien (V2, V3, V4, V5, V6, V14) kommen vor, auch die beiden nicht beruehrten.
    - Unter V5 steht die Zahl der geprueften FR-Werte und das Ergebnis der maschinellen Durchsicht auf Markup.
    - Unter V4 steht die Feststellung zur Berechtigungskette mit Diff-Beleg.
    - Jeder Befund traegt Kennung, Schwere und Disposition.
  </acceptance_criteria>
  <done>Der Sicherheitsteil steht mit Belegen statt mit Meinungen, und die beiden nicht beruehrten Kategorien sind benannt.</done>
</task>

<task type="auto">
  <name>Task 2: Bug- und Performance-Audit, und die vier geerbten Befunde entschieden</name>
  <files>docs/audits/2026-09-phase-11/README.md, .github/workflows/measure.yml</files>
  <read_first>
    - .planning/phases/07-gemeinsame-embedding-engine/deferred-items.md, DI-07-02 vollstaendig (Kaltstart 1.838,4 ms gegen 1.500 ms, Marge minus 338,4 ms, der eine belegte Abbruch um 14:05:17Z, und die Methodik-Korrektur: gemessen ist die ganze OCS-Anfrage, gedeckelt nur der innere Aufruf) und DI-07-03 vollstaendig
    - .planning/phases/10-vergleichsmessung-auf-der-aws-box/deferred-items.md, DI-10-04 und DI-10-05
    - .planning/phases/10-vergleichsmessung-auf-der-aws-box/10-07-SUMMARY.md, die Zeilen zu T-09-29 (tiefe Seite 0,333 s gegen erste Seite 0,332 s, der Vektorscan laeuft einmal je Anfrage und nicht je Seite)
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md, Entscheid V-1
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-13-SUMMARY.md, der bedingte Plan zu V-1a: gebaut oder als SKIPPED dokumentiert
    - docs/measurements/2026-09-vergleichsmessung-m7g/README.md, Abschnitte 9.2, 9.3, 12 und 19
    - docs/measurements/2026-09-werkzeugfixe/README.md aus Plan 11-06
    - .github/workflows/measure.yml, der Kopfkommentar und die Stelle, an der gegen den Tag dev geprueft wird
    - php/lib/Service/ExAppService.php, die Konstante fuer das Aufrufzeitlimit, und php/lib/Search/Provider.php, das Gruppenbudget
  </read_first>
  <action>
Der Bug- und der Performance-Teil folgen dem Muster der Phase 10: Befunde aus
dem Diff, dann die geerbten Befunde, jeder mit Verdikt.

**DI-07-02, das Aufrufzeitlimit.** Das ist eine Entscheidung im Audit und keine
Messung. Lege die Optionen aus dem Befund nebeneinander: Decke heben (trifft
jeden Aufruf jedes Nutzers, und die Unified Search wartet auf jeden Provider),
Gewichte beim Containerstart vorwaermen, oder ein eigener Weg fuer den ersten
Aufruf; die letzten beiden sind ungemessen. Nenne die Methodik-Korrektur aus
Abschnitt 9.2 des Berichts, die der Zahl ihre Schaerfe nimmt: gemessen ist die
ganze OCS-Anfrage, gedeckelt ist nur der innere Containeraufruf, und der eine
belegte Abbruch trat genau einmal je Containerstart bei kaltem Wirtscache auf.
Entscheide mit Begruendung und einer Wiedervorlagebedingung; kommt eine andere
Schwere als LOW heraus, folgt daraus ein Fix in dieser Phase, und das ist dann
zu benennen statt zu umgehen.

**DI-07-03.** Uebernimm den Entscheid V-1 aus 11-VORENTSCHEIDE.md woertlich,
mit Datum und Optionskennung. Bei V-1a: der Fix ist in dieser Phase gebaut,
und zwar von Plan 11-13; nenne dessen SUMMARY als Belegstelle, nimm seine
Pruefung in den V4- und V5-Teil des Berichts auf, und der Befund steht als
MEDIUM mit `fixed`. Ein `fixed` ohne diese Belegstelle ist eine Behauptung.
Bei V-1b: 11-13 traegt den SKIPPED-Vermerk, und der Befund steht als LOW mit
seinen Zahlen, seiner Wirkung auf den Nutzer und der Bedingung, unter der er
wiedervorgelegt wird.

**T-09-29.** Bereits in Phase 10 entschieden, hier nur uebernommen: accept
bleibt, mit den Zahlen (tiefe Seite 0,333 s gegen erste Seite 0,332 s, der
Scan laeuft einmal je Anfrage und nicht je Seite), Wiedervorlage bei deutlich
groesserem Vektorbestand als 146.171 Chunks, Verweis auf 10-07-SUMMARY.md. Eine
Zeile, kein Umfang.

**DI-10-05.** Entscheide und vollziehe: der Baumhash ist der Beweis, der
aufgeloeste Digest ist die Notiz, ein Messlauf darf gegen den wandernden Tag
dev pruefen, und der aufgeloeste Digest wird mitgeschrieben statt vorher
aufgeschrieben. Der Auslieferungspfad ist ohnehin unbeweglich, weil docker.yml
auf einem Tag prueft, dass Tag, beide Versionsangaben und der image-tag
uebereinstimmen. Praezisiere den Kopf von .github/workflows/measure.yml um
genau diese drei Saetze. Kein Produktionscode.

**DI-10-04.** Dokumentiert weiterreichen, mit Begruendung: die Frage entscheidet
nur eine Messung, die einen neuen Volllauf braucht (26 Stunden, rund 3 USD),
und der Deckel dieser Phase war 4 Stunden und 0,50 USD. Der Befund beruehrt
kein Erfolgskriterium und ist in Abschnitt 19.2 des Berichts als
Verschlechterung benannt. Ziel: die v1.2-Messplanung, zusammen mit dem
Box-Wiederaufbau-Runbook.

Der Performance-Teil nennt ausserdem die vier regressiven Laststufen aus dem
Vergleichslauf und entscheidet ausdruecklich, ob sie hingenommen oder
untersucht werden; die ROADMAP weist diese Entscheidung Phase 11 zu.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/measure.yml'))" &amp;&amp; grep -c "DI-07-02\|DI-07-03\|T-09-29\|DI-10-05\|DI-10-04" docs/audits/2026-09-phase-11/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Alle fuenf Kennungen DI-07-02, DI-07-03, T-09-29, DI-10-05 und DI-10-04 kommen im Bericht vor, jede mit einem Verdikt in einem eigenen Abschnitt.
    - DI-07-03 traegt woertlich die Optionskennung aus 11-VORENTSCHEIDE.md und dasselbe Datum, dazu die Belegstelle 11-13-SUMMARY.md (bei V-1a der gebaute Fix, bei V-1b der SKIPPED-Vermerk).
    - Der Bericht entscheidet ausdruecklich ueber die vier regressiven Laststufen.
    - `.github/workflows/measure.yml` ist gueltiges YAML und traegt im Kopf die drei Saetze zu Baumhash, Digest und dev.
    - Kein Abschnitt endet mit einer offenen Frage ohne Zieladresse.
  </acceptance_criteria>
  <done>Vier geerbte Befunde haben ein Verdikt, und einer davon ist im Workflow vollzogen statt nur beschlossen.</done>
</task>

<task type="auto">
  <name>Task 3: Befunde ab MEDIUM schliessen, deferred-items schreiben, Bericht nachziehen</name>
  <files>docs/audits/2026-09-phase-11/README.md, .planning/phases/11-haertung-und-store-einreichung-v1-1/deferred-items.md</files>
  <read_first>
    - docs/audits/2026-09-phase-11/README.md nach den Tasks 1 und 2
    - docs/audits/2026-09-phase-10/README.md, der Fix-Lauf-Teil mit fix_run, fix_commits, fixed und still_open
    - .planning/phases/10-vergleichsmessung-auf-der-aws-box/deferred-items.md als Formvorbild (Befund, warum nicht hier behoben, wohin es gehoert, und der Abschnitt "Geprueft und ausdruecklich kein Befund")
  </read_first>
  <action>
Schliesse jeden Befund ab MEDIUM, den die Tasks 1 und 2 gefunden haben, und
ziehe das Frontmatter nach: `fix_run` mit Datum, `fix_commits` mit den SHAs,
`fixed` mit den Kennungen, `still_open` mit den Kennungen, die offen bleiben,
und `status`. Bleibt `still_open` nicht leer, gehoert in den Bericht ein Satz,
warum ein Befund ab MEDIUM den Phasenabschluss nicht aufhaelt; die Owner-Regel
vom 15.08.2026 laesst das nicht ohne Begruendung zu.

Schreibe .planning/phases/11-haertung-und-store-einreichung-v1-1/deferred-items.md
in der Form der Phase 10: je Eintrag der Befund, wo er aufgefallen ist, warum er
nicht hier behoben wurde und wohin er gehoert. Mindestens ein Eintrag ist
sicher: DI-10-04 mit Ziel v1.2-Messplanung. Dazu der Abschnitt "Geprueft und
ausdruecklich kein Befund", der T-09-29 und DI-10-05 als anderswo entschieden
benennt, damit kein zweiter Eintrag an zweiter Stelle entsteht.

Halte im Bericht die vier Archive im Repositoriumswurzelverzeichnis fest, damit
Erfolgskriterium 5 eine Feststellung hat statt einer Vermutung: `findling.tar.gz`,
`findling_backend.tar.gz`, `findling.crt` und `findling_backend.crt` sind von git
nicht verfolgt und von `.gitignore` gedeckt (`*.tar.gz` und `*.crt`), also sind
sie Ueberbleibsel einer Handprobe und nicht "die Release-Artefakte im Repo".
Die Artefakte, die das Kriterium meint, sind die vier Assets am GitHub-Release,
und `store-submit.yml` reicht genau deren URLs ein. Pruefe das nach statt es zu
uebernehmen, mit `git ls-files` und `git log` ueber die vier Namen.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - Das Frontmatter traegt `fix_run`, `fix_commits`, `fixed` und `still_open`, jedes mit einem Wert oder einer ausdruecklich leeren Liste.
    - Ist `still_open` nicht leer, steht im Bericht die Begruendung dafuer.
    - `deferred-items.md` existiert, fuehrt DI-10-04 mit Ziel und Begruendung und traegt den Abschnitt "Geprueft und ausdruecklich kein Befund".
    - Der Bericht stellt zu den vier Wurzeldateien fest, dass `git ls-files` sie nicht kennt und `.gitignore` sie deckt, mit den beiden Musterzeilen als Beleg.
    - Die gesamte Gate-Kette laeuft gruen.
  </acceptance_criteria>
  <done>Das Audit-Gate der Owner-Regel ist gefahren, jeder Befund hat ein Verdikt, und die Phase reicht nur weiter, was sie benannt hat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Companion-Archiv zu Nutzerinstanz | Die neuen Katalogdateien gehen an jeden Nutzer und werden serverseitig ins Template gerendert |
| Signaturkette zu Store | Der Bericht prueft die Kette, bevor sie in 11-11 zum fuenften Mal benutzt wird |
| Audit zu Phasenabschluss | Ein Bericht, der einen Befund ohne Verdikt laesst, ist kein Gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-40 | Tampering | php/l10n/fr.json und fr.js | mitigate | ASVS V5: alle Werte maschinell auf spitze Klammern, Ampersand und Anfuehrungszeichen durchgesehen, Zahl im Bericht; das Dash-Gate liest die Kataloge seit 11-08 mit |
| T-11-41 | Elevation of Privilege | Berechtigungskette | mitigate | ASVS V4 mit Diff-Beleg: keine zweite Grenze; bei V-1a die ausdrueckliche Feststellung, dass der Fix aus Plan 11-13 einen Zustand und einen Text hinzufuegt und MAX_ROUNDS, isReadable und reduceIds unveraendert laesst |
| T-11-42 | Spoofing | Signierschluessel und Zertifikate | mitigate | ASVS V6: kein pull_request-Trigger in release.yml, Fingerabdruck ueber den DER, Verifikation gegen das Zertifikat statt Vertrauen |
| T-11-43 | Tampering | verschobener Action-Tag | mitigate | ASVS V14: alle uses-Zeilen auf Commit-SHA, test_workflow_pins.py haelt die Form, HaRP als Digest |
| T-11-44 | Repudiation | Befund ohne Verdikt | mitigate | Jeder Befund traegt Kennung, Schwere und Disposition; `still_open` ist ein Frontmatter-Feld und keine Auslassung |
| T-11-45 | Denial of Service | DI-07-02, leere Ergebnisgruppe bei kaltem Wirtscache | accept oder mitigate nach Auditverdikt | Das Verdikt faellt in Task 2 mit Zahlen und Wiedervorlagebedingung; eine hoehere Decke traefe jeden Aufruf jedes Nutzers |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Keiner. Der Diff der Phase fuehrt keine neue Abhaengigkeit ein; das Audit prueft das an backend/pyproject.toml gegen v1.0.3 nach |
</threat_model>

<verification>
- Aus `backend/`: `uv run python -m pytest -q && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80`
- Frontmatter des Auditberichts vollstaendig nach dem Phase-10-Schema
- Fuenf geerbte Kennungen im Bericht, jede mit Verdikt
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/measure.yml'))"`
</verification>

<success_criteria>
Erfolgskriterium 3 ist erfuellt: alle drei Audits sind erneut gefahren, jeder
Befund ab MEDIUM ist geschlossen, jeder LOW ist dokumentiert entschieden, und
kein geerbter Befund geht ohne Verdikt in die Abgabe.
</success_criteria>

<output>
Create `.planning/phases/11-haertung-und-store-einreichung-v1-1/11-10-SUMMARY.md` when done
</output>
