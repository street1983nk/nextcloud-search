---
phase: 08-deutsche-komposita-ohne-behelf
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/tests/test_index_open.py
  - backend/tests/test_analyzer.py
  - backend/src/findling/tools/index_status.py
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "Ein Test wird rot, wenn jemand die Zerlegung durch eine Prefix-, Wildcard- oder Fuzzy-Query ersetzt"
    - "Ein Test belegt, dass Index- und Query-Seite dieselben Terme erzeugen, statt nur dass ein Treffer kam"
    - "Ein Test wird rot, wenn die Filterreihenfolge der deutschen Kette umgestellt wird, nicht erst wenn split_compound verschwindet"
    - "Der einzige Aufrufer, der einen Index mit leerer Konstituentenliste oeffnet, kann nachweislich keine Frage stellen"
  artifacts:
    - path: "backend/tests/test_index_open.py"
      provides: "Negativkontrolle ohne Splitter, Praefix-Gegenprobe, Termgleichheit beider Seiten, Waechter ueber index_status"
      contains: "split_compound"
    - path: "backend/tests/test_analyzer.py"
      provides: "Strukturwaechter ueber die Filterkette im Syntaxbaum, mit Rotbeweisen"
      contains: "german_analyzer"
  key_links:
    - from: "backend/tests/test_analyzer.py"
      to: "backend/src/findling/index/analyzer.py"
      via: "ast.parse ueber die Quelldatei, Filterfolge als Liste"
      pattern: "ast\\.parse"
    - from: "backend/tests/test_index_open.py"
      to: "findling.index.open.open_index"
      via: "Index oeffnen und denselben Analyzer-Namen mit einer splitterlosen Kette ueberschreiben"
      pattern: "register_tokenizer"
---

<objective>
Erfolgskriterium 2 der Phase beweisen: Die Zerlegung laeuft im ausgelieferten Suchweg ueber
`split_compound` mit der mitgelieferten Wortliste, und ein Test belegt den Weg statt nur das
Ergebnis.

Der bestehende Test `test_the_registered_german_chain_splits_compounds` schreibt
"Kuendigungsfrist" und fragt "frist" ab. Er wuerde gruen bleiben, wenn jemand die Zerlegung
morgen durch eine Prefix-Query ersetzt. Genau diese Luecke schliesst dieser Plan, mit drei
Bausteinen aus Muster 1 der Recherche plus einem Strukturwaechter ueber die Filterreihenfolge.

Purpose: Der Behelf, den die Roadmap zurueckbauen wollte, existiert nicht (Recherche,
Bestandsaufnahme 2). Was fehlt, ist der Riegel, der ihn auch morgen nicht entstehen laesst.
Output: Vier neue Tests, ein geschaerfter Docstring, keine Produktionslogik.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-deutsche-komposita-ohne-behelf/08-RESEARCH.md

@backend/src/findling/index/analyzer.py
@backend/src/findling/index/open.py
@backend/src/findling/query/rewrite.py

<interfaces>
Vorhandene Bausteine, die die neuen Tests benutzen, keine neuen noetig:

backend/tests/test_index_open.py:
  FIXTURE / CONSTITUENTS  (die gemessene Teilmenge der echten Liste)
  DIGEST = "0" * 64
  OPENING_MODULE = "index/open.py"
  PACKAGE_ROOT (backend/src/findling)
  def _write(index: Index, *, body: str = ..., name: str = ..., ...) -> None
  def _hits(index: Index, query: str, fields: list[str] | None = None) -> int
  fixture index_dir(tmp_path) -> Path

backend/src/findling/index/analyzer.py:
  TOKENIZER_DE, TOKENIZER_EN, TOKENIZER_NAME, MAX_TOKEN_CHARS, ANALYZER_VERSION
  def german_analyzer(constituents: Sequence[str]) -> TextAnalyzer
  def cached_german_analyzer(digest: str, constituents: Sequence[str]) -> TextAnalyzer
  Die ausgelieferte Kette in dieser Reihenfolge:
    Tokenizer.simple -> lowercase -> split_compound -> custom_stopword(FUGEN)
    -> stopword("german") -> remove_long(MAX_TOKEN_CHARS) -> stemmer("german")

tantivy 0.26.0:
  Index.register_tokenizer(name: str, analyzer: TextAnalyzer) -> None
  TextAnalyzerBuilder(Tokenizer.simple()).filter(Filter...).build() -> TextAnalyzer
  TextAnalyzer.analyze(text: str) -> list[str]

Vorhandenes Muster fuer einen Waechter ueber den Syntaxbaum, als Vorlage:
  backend/tests/test_analyzer.py::normalize_callers plus die drei Rotbeweise daneben
  backend/tests/test_index_open.py::test_only_the_opening_module_opens_an_index
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Der Wegbeweis, drei Bausteine in test_index_open.py</name>
  <files>backend/tests/test_index_open.py</files>
  <behavior>
    - Negativkontrolle: dieselbe Kette ohne `Filter.split_compound`, sonst Filter fuer Filter identisch, findet das Dokument "Die Kuendigungsfrist betraegt drei Monate." unter der Frage "frist" nicht (0 Treffer), waehrend die ausgelieferte Kette 1 Treffer liefert
    - Praefix-Gegenprobe: gegen dasselbe Dokument liefert "kündigungsf" 0 Treffer und "frist" 1 Treffer; eine Prefix-Query wuerde hier treffen, split_compound nicht
    - Termgleichheit: `cached_german_analyzer(DIGEST, CONSTITUENTS).analyze("Kündigungsfrist")` ergibt `["kundig", "frist"]` und `analyze("Frist")` ergibt `["frist"]`, also erzeugt die Frageseite genau die Terme der Indexseite und keine weiteren
  </behavior>
  <read_first>
    - backend/tests/test_index_open.py, vollstaendig bis Zeile 250 (Helfer _write und _hits, Fixture index_dir, der bestehende Test test_the_registered_german_chain_splits_compounds ab Zeile 218)
    - backend/src/findling/index/analyzer.py, Zeilen 140 bis 215 (die Kette und cached_german_analyzer)
    - backend/src/findling/index/open.py, Zeilen 80 bis 145 (register_tokenizer und expected_versions)
    - backend/src/findling/query/rewrite.py, Zeilen 300 bis 400 (build_query, allow_regexes=False, conjunction_by_default=True)
    - .planning/phases/08-deutsche-komposita-ohne-behelf/08-RESEARCH.md, Abschnitt "Muster 1"
  </read_first>
  <action>
    Drei Tests direkt hinter `test_the_registered_german_chain_splits_compounds` einfuegen,
    mit einem Kommentarblock darueber, der in zwei Saetzen sagt, warum ein Trefferzaehler
    allein den Weg nicht beweist.

    Die Negativkontrolle baut die splitterlose Kette im Test selbst
    (`TextAnalyzerBuilder(Tokenizer.simple())` mit `Filter.lowercase`,
    `Filter.custom_stopword(list(FUGEN))`, `Filter.stopword("german")`,
    `Filter.remove_long(MAX_TOKEN_CHARS)`, `Filter.stemmer("german")`, also die ausgelieferte
    Folge minus `Filter.split_compound`), oeffnet den Index ueber `open_index(index_dir,
    CONSTITUENTS)` und ueberschreibt danach `TOKENIZER_DE` mit
    `index.register_tokenizer(TOKENIZER_DE, splitterless)`. Erst danach wird geschrieben und
    gefragt, damit Schreib- und Frageseite dieselbe splitterlose Kette benutzen. Erwartung: 0
    Treffer fuer "frist". Zur Gegenprobe im selben Test ein zweiter, frischer Index ueber
    `open_index` ohne Ueberschreiben, derselbe Text, dieselbe Frage, Erwartung 1 Treffer.
    Der Name der Konstante MAX_TOKEN_CHARS und FUGEN werden importiert, nicht abgeschrieben.

    Die Praefix-Gegenprobe schreibt denselben Satz in einen Index aus `open_index` und stellt
    zwei Fragen: "kündigungsf" muss 0 ergeben, "frist" muss 1 ergeben. Der Kommentar sagt
    ausdruecklich, dass genau dieser Test rot wird, wenn jemand eine Prefix-, Wildcard- oder
    Fuzzy-Query in `query/rewrite.py` einbaut, und nennt `allow_regexes=False` als die zweite
    Haelfte derselben Zusage.

    Der Termgleichheitstest ruft `cached_german_analyzer(DIGEST, CONSTITUENTS)` und vergleicht
    `analyze` fuer "Kündigungsfrist" und "Frist" gegen die gemessenen Listen. Er begruendet im
    Kommentar, dass beide Seiten durch `open_index` gehen und deshalb dieselbe Objektidentitaet
    benutzen.

    Umlaute stehen nur in Zeichenketten, Bezeichner bleiben ASCII. Testnamen und Kommentare
    auf Englisch wie der Bestand.
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q tests/test_index_open.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - Drei neue Testfunktionen in `backend/tests/test_index_open.py`, deren Namen `splitter`, `prefix` und `terms` enthalten
    - `grep -c "register_tokenizer" backend/tests/test_index_open.py` ist mindestens 1
    - Die Negativkontrolle behauptet beide Haelften in einem Test: 0 Treffer ohne Splitter und 1 Treffer mit
    - Der Praefixtest enthaelt die Zusicherung `== 0` fuer eine Zeichenkette, die ein echtes Praefix von "Kündigungsfrist" und kein Konstituent ist
    - `cd backend && uv run pytest -q tests/test_index_open.py` ist gruen
    - Rotbeweis von Hand gefahren und im Summary festgehalten: mit `Filter.split_compound` aus `german_analyzer` entfernt fallen die Negativkontrolle und der Termgleichheitstest, danach ist die Aenderung zurueckgenommen
  </acceptance_criteria>
  <done>Ein Prefix-Behelf im Suchweg macht die Suite rot, statt unbemerkt zu bleiben.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Der Strukturwaechter ueber die Filterreihenfolge</name>
  <files>backend/tests/test_analyzer.py</files>
  <behavior>
    - Der Waechter liest `backend/src/findling/index/analyzer.py` mit `ast` und gibt fuer die Funktion `german_analyzer` die Folge der Filternamen zurueck: `["lowercase", "split_compound", "custom_stopword", "stopword", "remove_long", "stemmer"]`
    - Ein synthetischer Quelltext, in dem `split_compound` fehlt, liefert eine Folge ohne `split_compound` und der Test darauf faellt
    - Ein synthetischer Quelltext, in dem `remove_long` vor `split_compound` steht, liefert eine andere Folge und der Test darauf faellt
    - Ein Kommentar, der `split_compound` nur nennt, hebt die Folge nicht (dieselbe Grep-Hygiene wie bei `normalize_callers`)
  </behavior>
  <read_first>
    - backend/tests/test_analyzer.py, Zeilen 1 bis 130 und 375 bis 431 (Docstring, COMPOUNDS, UNSPLIT, normalize_callers und seine drei Rotbeweise als Vorlage)
    - backend/src/findling/index/analyzer.py, Zeilen 140 bis 195 (german_analyzer, english_analyzer, name_analyzer)
    - .planning/phases/08-deutsche-komposita-ohne-behelf/08-RESEARCH.md, Pitfall 4 (ohne lowercase greift der Splitter gar nicht) und Anti-Muster "remove_long vor den Splitter ziehen"
  </read_first>
  <action>
    Eine Hilfsfunktion `filter_chain(source: str, function: str) -> list[str]` nach dem Muster
    von `normalize_callers` anlegen: sie parst den Quelltext, sucht die Funktionsdefinition mit
    dem gegebenen Namen, laeuft die Kette der `.filter(...)`-Aufrufe von innen nach aussen ab
    und gibt die Attributnamen der Filterfabriken (`Filter.lowercase` ergibt `"lowercase"`) in
    Aufrufreihenfolge zurueck. Kommentare und Docstrings duerfen die Liste nicht beeinflussen,
    weil ausschliesslich der Syntaxbaum gelesen wird.

    Darauf vier Tests:
    einer, der `filter_chain` gegen die echte Datei `index/analyzer.py` fuer `german_analyzer`
    genau gegen `["lowercase", "split_compound", "custom_stopword", "stopword", "remove_long",
    "stemmer"]` haelt, mit einem Kommentar, der die drei Zwangsbedingungen benennt: `lowercase`
    muss vor dem Splitter stehen, sonst greift er wegen bytegenauem Vergleich gar nicht;
    `remove_long` muss nach dem Splitter stehen, sonst wird das 63 Zeichen lange Kompositum zur
    leeren Tokenliste; `stemmer` muss zuletzt stehen, sonst trifft ein gestemmtes Kompositum
    keinen Listeneintrag mehr;
    drei Rotbeweise gegen synthetische Quelltexte (Splitter fehlt, `remove_long` vorgezogen,
    Nennung nur im Kommentar), analog zu den drei Rotbeweisen unter `normalize_callers`.

    Zusaetzlich einen Test, der `filter_chain` fuer `name_analyzer` gegen
    `["lowercase", "ascii_fold", "remove_long"]` haelt, damit der Waechter belegt, dass er
    Ketten unterscheidet und nicht immer dieselbe Liste zurueckgibt.
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q tests/test_analyzer.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_analyzer.py` enthaelt eine Funktion `filter_chain` und mindestens vier Tests, die sie benutzen
    - Der Test gegen die echte Datei vergleicht mit `==` gegen eine vollstaendige Liste, nicht mit `in` gegen einen Namen
    - Der Rotbeweis mit vorgezogenem `remove_long` faellt, wenn man ihn gegen die echte Kette laufen laesst (im Summary festgehalten)
    - `cd backend && uv run pytest -q tests/test_analyzer.py` ist gruen
  </acceptance_criteria>
  <done>Eine Umstellung der Filterreihenfolge wird rot, nicht nur ihr Entfernen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Der eine Aufrufer mit leerer Liste, festgenagelt</name>
  <files>backend/src/findling/tools/index_status.py, backend/tests/test_index_open.py</files>
  <behavior>
    - Ein Waechter ueber den Syntaxbaum von `tools/index_status.py` findet keinen Aufruf von `parse_query`, `parse_query_lenient`, `build_query` oder `search`
    - Der Waechter findet genau einen `open_index`-Aufruf mit einem leeren Tupel als zweitem Argument, und `tools/index_status.py` ist die einzige Datei des Pakets, in der ein solcher Aufruf steht
  </behavior>
  <read_first>
    - backend/src/findling/tools/index_status.py, Zeilen 1 bis 130 (Modul-Docstring und die Zaehlfunktion mit `open_index(directory, ())`)
    - backend/tests/test_index_open.py, Zeilen 305 bis 342 (`test_only_the_opening_module_opens_an_index` als Vorlage fuer den AST-Lauf ueber PACKAGE_ROOT)
    - .planning/phases/08-deutsche-komposita-ohne-behelf/08-RESEARCH.md, Bestandsaufnahme 1 (der eine Aufrufer mit leerer Liste) und Annahme A6
  </read_first>
  <action>
    Den vorhandenen Begruendungskommentar an der Aufrufstelle in `tools/index_status.py` um
    einen Satz erweitern, der die Zusage benennbar macht: dieses Werkzeug zaehlt und fragt
    nie, deshalb ist die leere Konstituentenliste unschaedlich, und der Waechter in
    `backend/tests/test_index_open.py` haelt das fest. Kein Verhalten aendern.

    In `backend/tests/test_index_open.py` einen Test neben
    `test_only_the_opening_module_opens_an_index` einfuegen, der mit `ast` ueber
    `PACKAGE_ROOT` laeuft und zwei Dinge behauptet: erstens, dass in
    `tools/index_status.py` kein Aufruf mit den Namen `parse_query`,
    `parse_query_lenient`, `build_query` oder `search` steht; zweitens, dass genau eine Datei
    des Pakets `open_index` mit einer leeren Sammlung als zweitem Argument ruft, und dass es
    diese Datei ist. Die Fehlermeldung nennt die gefundenen Stellen mit Datei und Zeile, wie
    der Waechter daneben es tut.
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q tests/test_index_open.py --no-header && uv run ruff check . && uv run pyright && uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parse_query" backend/src/findling/tools/index_status.py` ergibt 0
    - Der neue Test nennt in seiner Zusicherung den Pfad `tools/index_status.py` woertlich
    - Der Test faellt, wenn man in `tools/index_status.py` versuchsweise ein `index.parse_query("x", [FIELD_BODY_DE])` einfuegt (im Summary festgehalten, Aenderung danach zurueckgenommen)
    - ruff, ruff format, pyright und vulture sind im Verzeichnis `backend` gruen
  </acceptance_criteria>
  <done>Der einzige Index, der ohne Wortliste geoeffnet wird, kann nachweislich keine Frage beantworten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suchzeile des Nutzers zu Tantivy-Parser | Ungeprueft eintreffender Text, aufbereitet in query/rewrite.py |
| Analyzerkette zu Index | Was hier zerlegt wird, entscheidet jeden Term im Index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | index/analyzer.py Filterreihenfolge | mitigate | Der Strukturwaechter aus Task 2 haelt `remove_long` hinter dem Splitter fest; ein Vorziehen liesse lange Tokens entweder unbegrenzt in den Index oder loeschte deutsche Langwoerter vollstaendig |
| T-08-05 | Tampering | query/rewrite.py | mitigate | Die Praefix-Gegenprobe aus Task 1 wird rot, sobald eine Prefix-, Wildcard- oder Fuzzy-Umschreibung in den Suchweg gelangt; `allow_regexes=False` bleibt unangetastet |
| T-08-06 | Information Disclosure | tools/index_status.py | mitigate | Der Waechter aus Task 3 haelt fest, dass das Werkzeug zaehlt und nie fragt; es gibt weiterhin nur Zahlen aus und keinen Dokumentinhalt |
| T-08-07 | Elevation of Privilege | Kandidatenmenge der Suche | accept | Diese Aenderungen sind reine Tests und ein Kommentar; die Sicherheitsgrenze bleibt der finale PHP-Recheck, die Paritaetstests (test_parity_diff.py, test_guest_parity.py) bleiben unberuehrt |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` ist vollstaendig gruen
- ruff, ruff format --check, pyright und vulture sind gruen
- Die drei Rotbeweise (Splitter entfernt, remove_long vorgezogen, parse_query in index_status) sind von Hand gefahren und im Summary mit ihrem Ergebnis notiert
</verification>

<success_criteria>
- Erfolgskriterium 2 der Phase ist mit einem Test belegt, der den Weg festnagelt und nicht nur das Ergebnis
- Kein Produktionsverhalten geaendert: `index/analyzer.py`, `index/open.py` und `query/rewrite.py` bleiben inhaltlich unveraendert
</success_criteria>

<output>
Create `.planning/phases/08-deutsche-komposita-ohne-behelf/08-02-SUMMARY.md` when done.
</output>
