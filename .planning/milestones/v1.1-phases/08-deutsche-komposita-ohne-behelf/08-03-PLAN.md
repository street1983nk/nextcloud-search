---
phase: 08-deutsche-komposita-ohne-behelf
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .github/workflows/docker.yml
  - THIRD-PARTY.md
  - backend/tests/test_status_endpoint.py
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Ein veroeffentlichtes Abbild ohne Lizenztext oder mit einer anderen Wortlistenfassung faellt in CI auf, nicht erst bei einer Store-Pruefung"
    - "Ein Wechsel von FINDLING_COMPOUND_DICT erzeugt sichtbar reindexRequired auf der Statusseite, und die Marke, die widerspricht, heisst wordlist_hash"
  artifacts:
    - path: ".github/workflows/docker.yml"
      provides: "Schritt, der Paketfassung, Zeilenzahl und Lizenztext im gepushten Abbild prueft"
      contains: "COPYING.wngerman"
    - path: "backend/tests/test_status_endpoint.py"
      provides: "Ende-zu-Ende-Beleg fuer Erfolgskriterium 4 ueber den Variantenwechsel"
      contains: "FINDLING_COMPOUND_DICT"
    - path: "THIRD-PARTY.md"
      provides: "Der Hinweis, dass die Handpruefung jetzt eine CI-Pruefung hat"
  key_links:
    - from: ".github/workflows/docker.yml"
      to: "/usr/local/share/findling/COPYING.wngerman im Abbild"
      via: "docker run gegen den gepushten Digest"
      pattern: "COPYING.wngerman"
    - from: "backend/tests/test_status_endpoint.py"
      to: "findling.api.resources.version_drift"
      via: "Statusroute meldet reindexRequired"
      pattern: "reindexRequired"
---

<objective>
Die zwei Erfolgskriterien belegen, die laut Recherche bereits gebaut sind, und dabei genau
die zwei Luecken schliessen, die der Beleg sichtbar macht.

Erfolgskriterium 3 (Lizenz, Herkunft und Fassung der Wortliste im Repo und im Abbild) ist
inhaltlich vollstaendig erfuellt. Was fehlt, ist die Maschine: `backend/Dockerfile` prueft
zur Bauzeit mit `test -s`, aber kein CI-Schritt sieht im **veroeffentlichten** Abbild nach,
und niemand vergleicht Paketfassung und Zeilenzahl gegen die dokumentierten Werte
(Offene Frage 3 der Recherche, mit `grep -rn "COPYING" .github/workflows/` als leer belegt).

Erfolgskriterium 4 (ein Wechsel der Wortliste erzwingt sichtbar einen Reindex) ist gebaut und
an sieben Stellen getestet, aber kein Test faehrt den Weg, den ein Admin wirklich geht: die
Umgebungsvariable `FINDLING_COMPOUND_DICT` umstellen und danach auf die Statusseite sehen
(Offene Frage 4).

Purpose: Aus zwei "sieht richtig aus" werden zwei rotwerdende Pruefungen.
Output: Ein CI-Schritt im Abbildbau, ein Ende-zu-Ende-Test, ein Satz in THIRD-PARTY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-deutsche-komposita-ohne-behelf/08-RESEARCH.md

@backend/Dockerfile
@backend/src/findling/index/wordlist.py
@backend/src/findling/api/resources.py

<interfaces>
Bausteine, die der Ende-zu-Ende-Test benutzt:

backend/tests/conftest.py:
  CONSTITUENTS (Fixture-Liste)
  def write_wordlist(root: Path) -> str        # schreibt dict/<artifact_path().name> plus .sha256
  fixture volume(tmp_path, monkeypatch) -> Path        # setzt APP_PERSISTENT_STORAGE, leert settings-Cache
  fixture indexed_volume(volume) -> Corpus             # Wortliste, Index, state.db, Vektoren
  class Corpus(root: Path, digest: str, documents: int)

backend/src/findling/index/wordlist.py:
  def artifact_path(variant: str | None = None) -> Path     # Name traegt die Variante
  def wordlist_hash(entries: Sequence[str]) -> str
  DIGEST_SUFFIX, ENCODING

backend/src/findling/api/resources.py:
  UNPROVEN_WORDLIST = "wordlist_hash"
  _MARKS: tuple[Path, dict[str, str]] | None                # Prozess-Cache, auf dict_dir geschluesselt
  def expected_marks() -> dict[str, str] | None
  def version_drift(store: Store) -> list[str]

backend/src/findling/store/repo.py:
  Store.version_mismatch(expected: dict[str, str]) -> list[str]
  Store.write_meta(key: str, value: str) -> None

Statusroute: die Antwort traegt `reindexRequired` und `wordlistHash`
(vorhandener Test: test_status_endpoint.py::test_a_version_drift_is_reported_as_reindex_required).

Dokumentierte Werte der Wortliste (THIRD-PARTY.md, docs/german-analyzer.md):
  Paket `wngerman` = 20161207-15, Architecture: all
  /usr/share/dict/ngerman = 356010 Zeilen, 4725887 Bytes
  Lizenztext im Abbild: /usr/local/share/findling/COPYING.wngerman
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Wortliste und ihre Lizenz im veroeffentlichten Abbild pruefen</name>
  <files>.github/workflows/docker.yml, THIRD-PARTY.md</files>
  <read_first>
    - .github/workflows/docker.yml, Zeilen 150 bis 300 (Bau, Zurueckziehen des Digests, der Schritt "Answer A12 and A13 inside this image" als Formvorlage, der Smoke-Test)
    - backend/Dockerfile, Zeilen 185 bis 215 (die apt-Zeile mit dem Pin, das install der Lizenz nach /usr/local/share/findling/COPYING.wngerman und die GPL-2+-Begruendung darueber)
    - THIRD-PARTY.md, Abschnitt "The word list" und Abschnitt "How to check this file against reality"
    - .planning/phases/08-deutsche-komposita-ohne-behelf/08-RESEARCH.md, Bestandsaufnahme 6 und Offene Frage 3
  </read_first>
  <action>
    In `.github/workflows/docker.yml` einen Schritt direkt hinter "Answer A12 and A13 inside
    this image" einfuegen, Name: "The word list, its version and its licence in this image".
    Er laeuft in beiden Matrixaesten gegen `"${IMAGE}@${DIGEST}"` mit
    `docker run --rm --network none --entrypoint sh` und prueft vier Dinge, jedes mit einer
    eigenen Fehlermeldung, weil vier Fehlschlaege vier verschiedene Dinge bedeuten:

    1. `dpkg-query -W -f='${Version}' wngerman` ergibt genau `20161207-15`. Ein Wechsel der
       Fassung aendert die Tokenisierung jedes deutschen Dokuments und muss ein Bauereignis
       sein, kein stiller apt-Nebeneffekt.
    2. `wc -l < /usr/share/dict/ngerman` ergibt genau `356010` und `wc -c` genau `4725887`,
       die beiden Zahlen aus THIRD-PARTY.md.
    3. `/usr/local/share/findling/COPYING.wngerman` existiert, ist nicht leer und traegt die
       Lizenzkennzeichnung. Welche Zeichenkette dafuer geprueft wird, wird einmal im gebauten
       Abbild nachgesehen (`head -20` auf die Datei) und dann woertlich in den Schritt
       geschrieben, mit einem Kommentar, an welchem Datum sie gemessen wurde. Keine
       geratene Zeichenkette.
    4. Die Datei ist `-r--r--r--` (Modus 0444 aus dem Dockerfile), damit ein spaeterer
       Bauschritt sie nicht unbemerkt beschreibbar macht.

    Der Kommentarblock darueber sagt in drei Saetzen, warum der Schritt neben dem `test -s`
    des Dockerfiles steht und es nicht ersetzt: das Dockerfile prueft den Bau, dieser Schritt
    prueft das Artefakt, das wirklich veroeffentlicht wird, und die GPL-2+-Pflicht haengt am
    verteilten Abbild und nicht am Bauplan.

    In `THIRD-PARTY.md`, Abschnitt "How to check this file against reality", ueber dem ersten
    Codeblock einen Satz ergaenzen: die erste Handpruefung faehrt seit dieser Phase in
    `.github/workflows/docker.yml` als Schritt "The word list, its version and its licence in
    this image" bei jedem Abbildbau automatisch mit, die Handfassung bleibt fuer Leser stehen.
    Deutsche Prosa dieser Datei ist Englisch, also folgt der Satz der dortigen Sprache.
  </action>
  <verify>
    <automated>grep -c "COPYING.wngerman" .github/workflows/docker.yml | grep -qv '^0$' && grep -q "20161207-15" .github/workflows/docker.yml && grep -q "356010" .github/workflows/docker.yml && python -c "import sys,yaml;yaml.safe_load(open('.github/workflows/docker.yml',encoding='utf-8'));print('workflow parses')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/docker.yml` enthaelt genau einen Schritt mit `COPYING.wngerman`, und er steht im Job, der den gepushten Digest kennt (`steps.push.outputs.digest`)
    - Der Schritt enthaelt woertlich `20161207-15`, `356010`, `4725887` und `0444` oder `-r--r--r--`
    - Die gepruefte Lizenz-Zeichenkette steht mit einem Kommentar daneben, der das Messdatum nennt
    - Die Workflow-Datei ist gueltiges YAML (Verifikationskommando)
    - Ein lokaler Trockenlauf der vier Pruefungen gegen ein selbst gebautes Abbild ist gefahren und sein Ergebnis steht im Summary; falls kein lokaler Abbildbau moeglich war, steht das ausdruecklich im Summary als offener Punkt fuer den ersten CI-Lauf auf main
    - `THIRD-PARTY.md` nennt den neuen Schrittnamen woertlich
  </acceptance_criteria>
  <done>Ein Abbild ohne Lizenztext, mit anderer Paketfassung oder anderer Wortliste wird beim Bau rot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Der Variantenwechsel, Ende zu Ende bis zur Statusseite</name>
  <files>backend/tests/test_status_endpoint.py</files>
  <behavior>
    - Ein Datentraeger, dessen Index mit der Variante `full` gebaut wurde, meldet `reindexRequired` gleich `false`
    - Wird `FINDLING_COMPOUND_DICT` auf `nouns` gestellt, liegt fuer diese Variante ein eigenes Artefakt mit einem anderen Digest auf dem Datentraeger und wird der Prozess-Cache der Marken wie bei einem Containerneustart geleert, meldet dieselbe Route `reindexRequired` gleich `true`
    - Die widersprechende Marke heisst `wordlist_hash`, und der Nachweis unterscheidet sie von der Ersatzantwort UNPROVEN_WORDLIST: `expected_marks()` liefert nicht None, sondern echte Marken mit dem Digest der nouns-Liste
    - `wordlistHash` in der Antwort traegt den Digest der neu eingestellten Variante und nicht den des Index
  </behavior>
  <read_first>
    - backend/tests/test_status_endpoint.py, Zeilen 40 bis 80 und 340 bis 400 (Importe, _status-Helfer, der vorhandene Drifttest test_a_version_drift_is_reported_as_reindex_required)
    - backend/tests/conftest.py, Zeilen 40 bis 250 (CONSTITUENTS, write_wordlist, write_index, write_state, die Fixtures volume und indexed_volume)
    - backend/src/findling/index/wordlist.py, Zeilen 95 bis 300 (load_constituents, artifact_path, build_artifact und der Fail-closed-Vergleich)
    - backend/src/findling/api/resources.py, Zeilen 95 bis 175 (_MARKS, expected_marks, version_drift, UNPROVEN_WORDLIST)
    - backend/tests/test_wordlist.py, Zeilen 240 bis 300 (die drei Variantentests aus Bug-Audit H2, damit der neue Test sie nicht wiederholt)
  </read_first>
  <action>
    Einen Test hinter `test_a_version_drift_is_reported_as_reindex_required` einfuegen, Name in
    der Sprache des Bestands, etwa `test_switching_the_dictionary_variant_asks_for_a_reindex`.

    Ablauf, in dieser Reihenfolge und mit Kommentaren, die jeden Schritt begruenden:
    Der Datentraeger kommt aus der Fixture `indexed_volume`, ist also mit `full` gebaut, und
    der erste Abruf der Statusroute behauptet `reindexRequired is False`; ohne diese Haelfte
    waere die zweite ohne Aussage.
    Danach `monkeypatch.setenv("FINDLING_COMPOUND_DICT", "nouns")` und `settings.cache_clear()`.
    Danach ein Artefakt fuer die neue Variante auf denselben Datentraeger schreiben, unter dem
    Namen aus `artifact_path()` (der die Variante traegt) mit einer Konstituentenliste, die
    sich von der Fixture-Liste unterscheidet, plus der Digestdatei mit `wordlist_hash` genau
    dieser Liste, nach dem Muster von `write_wordlist`. Der Kommentar sagt, warum das noetig
    ist: ohne Artefakt liefe `build_artifact` gegen `/usr/share/dict/ngerman`, das auf einer
    Entwicklungsmaschine nicht existiert, und die Route meldete dann die Ersatzantwort
    UNPROVEN_WORDLIST, also die richtige Meldung aus dem falschen Grund.
    Danach den Prozess-Cache der Marken leeren
    (`monkeypatch.setattr(resources, "_MARKS", None)`) mit einem Kommentar, dass ein
    Variantenwechsel in Wirklichkeit ein Containerneustart ist und der Cache genau dessen
    Lebensdauer hat.
    Danach die Statusroute erneut abrufen und drei Dinge behaupten: `reindexRequired is True`,
    `wordlistHash` gleich dem Digest der nouns-Liste, und ueber `open_store` plus
    `version_mismatch(expected_marks())` beziehungsweise `version_drift`, dass genau
    `wordlist_hash` unter den widersprechenden Marken steht und `expected_marks()` nicht None
    ist.

    Falls sich zeigt, dass ein anderer Weg noetig ist, um die Marken neu zu bilden, wird der
    tatsaechlich benutzte Weg im Test kommentiert und im Summary genannt; ein zweiter
    Produktionspfad wird dafuer nicht gebaut.
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q tests/test_status_endpoint.py --no-header && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - Ein neuer Test in `backend/tests/test_status_endpoint.py` setzt `FINDLING_COMPOUND_DICT` auf `nouns` und behauptet beide Haelften, vorher `False` und nachher `True`
    - Der Test behauptet ausdruecklich, dass `expected_marks()` nicht None ist, also die Drift echt und nicht die Ersatzantwort UNPROVEN_WORDLIST ist
    - Der Test vergleicht `wordlistHash` gegen einen im Test berechneten `wordlist_hash` und nicht gegen eine hart geschriebene Zeichenkette
    - `cd backend && uv run pytest -q` ist vollstaendig gruen, ruff, pyright und vulture ebenso
    - Rotbeweis im Summary: mit unveraendertem `FINDLING_COMPOUND_DICT` faellt der zweite Teil des Tests
  </acceptance_criteria>
  <done>Erfolgskriterium 4 ist auf dem Weg belegt, den ein Admin wirklich geht, nicht nur ueber eine von Hand verbogene Marke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debian-Archiv zu Abbild | Die Wortliste kommt aus einem Paket und wird mit dem Abbild verteilt |
| Abbild zu Nutzer | Was verteilt wird, traegt eine GPL-2+-Pflicht |
| Admin-Einstellung zu Index | FINDLING_COMPOUND_DICT entscheidet ueber die Tokenisierung des ganzen Bestands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | /usr/share/dict/ngerman im Abbild | mitigate | Der CI-Schritt aus Task 1 haelt Paketfassung, Zeilenzahl und Byteanzahl gegen die dokumentierten Werte; eine getauschte oder gedrehte Liste faellt beim Abbildbau auf |
| T-08-09 | Repudiation | Lizenzpflicht GPL-2+ | mitigate | Anwesenheit, Nichtleere und Leserechte von `/usr/local/share/findling/COPYING.wngerman` werden im veroeffentlichten Abbild geprueft, nicht nur im Bauplan |
| T-08-10 | Tampering | stille Divergenz von Index und Query-Parser | mitigate | Der Test aus Task 2 belegt, dass ein Variantenwechsel `reindexRequired` setzt und die Marke `wordlist_hash` benennt, statt Suche und Index leise auseinanderlaufen zu lassen |
| T-08-11 | Information Disclosure | neuer CI-Schritt | accept | Der Schritt gibt nur Zahlen und einen Lizenzkopf aus, keinen Nutzerinhalt; er laeuft mit `--network none` |
| T-08-SC | Tampering | apt/pip in diesem Plan | mitigate | Keine neuen Pakete. Der Pin `wngerman=20161207-15` wird zusaetzlich geprueft statt neu gesetzt |
</threat_model>

<verification>
- `cd backend && uv run pytest -q` gruen, ruff, ruff format --check, pyright, vulture gruen
- Die Workflow-Datei parst als YAML und der neue Schritt steht im Job mit dem gepushten Digest
- Im Summary steht, ob der Abbildschritt lokal gefahren werden konnte, und wenn nein, warum nicht
</verification>

<success_criteria>
- Erfolgskriterium 3 ist nicht nur erfuellt, sondern maschinell geprueft
- Erfolgskriterium 4 ist Ende zu Ende belegt, vom Umschalten der Einstellung bis zur Meldung der Statusseite
</success_criteria>

<output>
Create `.planning/phases/08-deutsche-komposita-ohne-behelf/08-03-SUMMARY.md` when done.
</output>
