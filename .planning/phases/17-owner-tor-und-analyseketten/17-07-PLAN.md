---
phase: 17-owner-tor-und-analyseketten
plan: 07
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - backend/src/findling/store/repo.py
  - backend/tests/test_upgrade_compatibility.py
autonomous: true
requirements: [LEX-07]

must_haves:
  truths:
    - "Eine tantivy-Patchnummer bewegt die Marke `tantivy_version` nicht mehr, solange die `index_format`-Haelfte gleich bleibt"
    - "Ein geaendertes Indexformat wird weiterhin als Abweichung gemeldet"
    - "Der volle Banner wird weiter gespeichert und bleibt fuer die Diagnose lesbar"
    - "Die Lockerung hat einen roten Zustand, der nachweislich erzeugt werden kann"
  artifacts:
    - path: "backend/src/findling/store/repo.py"
      provides: "zweite Ausnahme in Store.version_mismatch nach dem Muster der index_version-Ausnahme"
      contains: "index_format"
    - path: "backend/tests/test_upgrade_compatibility.py"
      provides: "Gold-Wert auf die index_format-Haelfte umgestellt, vierte Selbstprobe ergaenzt"
      contains: "GOLD_INDEX_FORMAT"
  key_links:
    - from: "backend/src/findling/store/repo.py"
      to: "backend/src/findling/index/open.py"
      via: "expected_versions() liefert weiterhin den vollen Banner als tantivy_version"
      pattern: "tantivy_version"
---

<objective>
Die Vergleichsregel fuer `tantivy_version` wird gelockert: gespeichert wird weiter der volle Banner, verglichen wird nur noch die `index_format`-Haelfte.

Purpose: LEX-07 und Erfolgskriterium 4 vertragen sich nur mit dieser Lockerung. Ohne sie loest der Pin-Sprung 0.26.0 auf 0.26.2 auf jeder Bestandsinstallation einen vollen Reindex aus, gemessen 19 h 20 min fuer 52.137 Dokumente. Die Praezedenz ist die `index_version`-Ausnahme, die an derselben Stelle schon steht.
Output: zweite Ausnahme in `Store.version_mismatch`, angepasste Gold-Werte mit vierter Selbstprobe
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Gelesen am 2026-09-23.

`backend/src/findling/index/open.py`:
- Zeilen 61-67: `TANTIVY_VERSION: Final[str] = tantivy.__version__  # pyright: ignore[reportAttributeAccessIssue]`, Wert `"tantivy v0.26.0, index_format v7"`
- Zeilen 123-141: `expected_versions()` liefert `{"schema_version", _LOCAL_GENERATION, "analyzer_version", "wordlist_hash", "tantivy_version"}`

`backend/src/findling/store/repo.py`:
- Zeilen 631-669: `Store.version_mismatch(expected)`; Vergleichsschleife Zeilen 659-669 mit der einen Ausnahme `if key == "index_version" and _generation_at_least(current, value): continue`
- Zeile 637: "A mark that was never written counts as diverging" , bleibt unveraendert
- Zeile 1299: `def _generation_at_least(stored: str | None, expected: str) -> bool` , das Formvorbild fuer den neuen Hilfsvergleicher

`backend/tests/test_upgrade_compatibility.py`:
- Zeilen 15-24, Modulkopf: "A red test here is not a repair, it is a question for the owner." , bleibt woertlich stehen
- Zeilen 42-82: `GOLD_V1_0_AND_V1_1` mit `"tantivy_version": "0.26.0"`, `TANTIVY_MARK = "tantivy_version"`, `GOLD_INDEX_FORMAT = "index_format v7"`, `TANTIVY_PIN = "tantivy==0.26.0"`
- Zeilen 85-100: `drift_findings(marks)` mit der Zeile `held = gold in value if mark == TANTIVY_MARK else value == gold`
- Zeilen 103-127: drei gestellte Datensaetze als Selbsttests (einer haelt, einer hat eine bewegte Marke, einem fehlt eine)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Die zweite Ausnahme in Store.version_mismatch</name>
  <files>backend/src/findling/store/repo.py</files>
  <read_first>
    - `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md`, Abschnitte `## Vollzug am ...` (welcher Zweig von E-17-7 greift) und `## Die fertigen Ersatztexte`, Unterabschnitt `backend/src/findling/store/repo.py`
    - `backend/src/findling/store/repo.py`, Zeilen 625-680 (`version_mismatch` samt Docstring) und Zeilen 1290-1310 (`_generation_at_least`)
    - `backend/src/findling/index/open.py`, Zeilen 55-145 (`TANTIVY_VERSION`, `expected_versions`, Zustaendigkeits-Docstring)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 4.2 (die vier Messbelege)
  </read_first>
  <behavior>
    - `version_mismatch` meldet `tantivy_version` NICHT, wenn gespeicherter und erwarteter Wert dieselbe `index_format`-Haelfte tragen und sich nur in der Fassungsnummer unterscheiden.
    - `version_mismatch` meldet `tantivy_version` weiterhin, wenn die `index_format`-Haelfte abweicht.
    - `version_mismatch` meldet `tantivy_version` weiterhin, wenn die Marke fehlt oder leer ist.
    - `version_mismatch` meldet `tantivy_version` weiterhin, wenn der gespeicherte Wert keine `index_format`-Haelfte enthaelt (unlesbarer Banner).
    - Alle vier anderen Marken verhalten sich unveraendert.
  </behavior>
  <action>
**Vorbedingung, zuerst pruefen:** Lies im Vollzugsabschnitt von `17-GRUNDSATZ-ENTSCHEID.md`, welcher Zweig fuer E-17-7 greift. Steht dort Option b ("Pin bleibt auf 0.26.0"), wird dieser Plan NICHT ausgefuehrt: schreibe stattdessen ein SUMMARY, das den Zweig zitiert, festhaelt, dass `repo.py` und `test_upgrade_compatibility.py` unveraendert bleiben, und LEX-07 als umzuformulieren markiert. Alles Weitere gilt nur fuer Option a.

Ergaenze `backend/src/findling/store/repo.py`.

**a) Hilfsvergleicher** `_index_format_matches(stored: str | None, expected: str) -> bool`, platziert neben `_generation_at_least` (Zeile 1299) und in derselben Form: private Modulfunktion, kurzer Docstring, keine Ausnahmen nach aussen. Er schneidet aus beiden Zeichenketten die Teilzeichenkette ab `index_format` heraus und vergleicht sie. Gibt False zurueck, wenn `stored` None oder leer ist, wenn `index_format` in einer der beiden Zeichenketten fehlt, oder wenn die beiden Haelften ungleich sind. Der Docstring sagt, warum "faellt geschlossen": ein Banner, der die Formathaelfte gar nicht traegt, ist kein Beweis fuer Vertraeglichkeit, sondern ein unbekannter Zustand, und ein unbekannter Zustand ist eine Abweichung.

**b) Die Ausnahme** als dritte `if`-Zeile in der Vergleichsschleife von `version_mismatch`, direkt hinter der `index_version`-Ausnahme:
`if key == "tantivy_version" and _index_format_matches(current, value): continue`

**c) Der Docstring-Absatz**, im Ton des vorhandenen `index_version`-Absatzes, in englischer Prosa. Er muss vier Dinge sagen: der volle Banner wird weiter gespeichert, weil er fuer die Diagnose wertvoll ist, und nur der Vergleich lockert; entscheidend ist allein die `index_format`-Haelfte, weil sie sagt, ob die Dateien auf der Platte ueberhaupt noch zu oeffnen sind; die aufgegebene Absicherung ist der Fall, dass tantivy die Tokenisierung aendert, ohne das Format zu aendern, und sie wird durch die Tabellen- und Waechtertests in `backend/tests/test_analyzer.py` und `backend/tests/test_language_analyzers.py` ersetzt; die Belege sind die Messung vom 2026-09-23 (Format v7 in 0.26.0 und 0.26.2, Index in beide Richtungen les- und durchsuchbar, Tokenisierung ueber 224 Zeilen identisch) und der Owner-Entscheid E-17-7 Option a vom Vollzugsdatum.

Der Satz in Zeile 637 ("A mark that was never written counts as diverging") bleibt woertlich stehen; die neue Ausnahme darf ihn nicht aushebeln, und genau das prueft die dritte Behauptung oben.

`backend/src/findling/index/open.py` wird NICHT angefasst: der Banner wird weiter gespeichert.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "
from findling.store.repo import _index_format_matches as m
old = 'tantivy v0.26.0, index_format v7'
new = 'tantivy v0.26.2, index_format v7'
assert m(old, new) is True
assert m(new, old) is True
assert m('tantivy v0.27.0, index_format v8', new) is False
assert m(None, new) is False
assert m('', new) is False
assert m('0.26.2', new) is False
print('GRUEN')
" && uv run python -m pytest tests/test_upgrade_compatibility.py tests/test_lockstep_versions.py tests/test_index_open.py -q && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture</automated>
  </verify>
  <acceptance_criteria>
    - `_index_format_matches` existiert in `backend/src/findling/store/repo.py` neben `_generation_at_least` und liefert die fuenf oben geprueften Ergebnisse.
    - `version_mismatch` enthaelt genau eine neue `if`-Zeile mit `key == "tantivy_version"`; die `index_version`-Ausnahme und Zeile 637 sind unveraendert.
    - Der Docstring-Absatz nennt alle vier geforderten Punkte, darunter die Messung vom 2026-09-23 und `E-17-7 Option a`.
    - `git diff --quiet backend/src/findling/index/open.py`: der Banner wird weiter unveraendert gespeichert.
    - `tests/test_upgrade_compatibility.py`, `tests/test_lockstep_versions.py` und `tests/test_index_open.py` sind mit dem noch unveraenderten Pin 0.26.0 gruen.
    - ruff, ruff format, pyright und vulture aus `backend/` sind gruen.
  </acceptance_criteria>
  <done>Die Vergleichsregel ist gelockert, faellt geschlossen und hebelt die Fehlt-gleich-Abweichung nicht aus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gold-Werte auf die Formathaelfte umstellen, mit vierter Selbstprobe</name>
  <files>backend/tests/test_upgrade_compatibility.py</files>
  <read_first>
    - `backend/tests/test_upgrade_compatibility.py` (vollstaendig)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md`, Unterabschnitt `backend/tests/test_upgrade_compatibility.py` in `## Die fertigen Ersatztexte`
    - `backend/src/findling/store/repo.py` (der Stand aus Task 1)
  </read_first>
  <behavior>
    - Selbstprobe 1 (alle Marken passen) bleibt gruen.
    - Selbstprobe 2 (eine bewegte Marke) bleibt rot und meldet dieselbe Zeile wie bisher.
    - Selbstprobe 3 (fehlende Marke) bleibt rot.
    - Selbstprobe 4, NEU: derselbe `index_format`, andere Patchnummer, muss gruen sein.
    - Selbstprobe 5, NEU: anderes `index_format`, muss rot sein.
  </behavior>
  <action>
Aendere `backend/tests/test_upgrade_compatibility.py` genau an drei Stellen. Der Modulkopf bleibt woertlich stehen, insbesondere der Satz "A red test here is not a repair, it is a question for the owner."; dass er stehen bleibt, ist Teil der Abnahme.

**a) `GOLD_V1_0_AND_V1_1["tantivy_version"]`** wechselt von `"0.26.0"` auf `GOLD_INDEX_FORMAT`, also auf `"index_format v7"`. Da `drift_findings` fuer `TANTIVY_MARK` schon heute `gold in value` statt `value == gold` benutzt (Zeile mit `held = ...`), traegt die vorhandene Vergleichszeile die Lockerung ohne weitere Aenderung; das gehoert als Kommentar an die Stelle, damit niemand die Zeile "vereinheitlicht".

**b) Der begruendende Kommentarblock** ueber der Gold-Tabelle. Er nennt: den Owner-Entscheid `E-17-7 Option a` mit Vollzugsdatum; die vier Messbelege vom 2026-09-23 (Format v7 in beiden Fassungen, Kreuzlesetest in beide Richtungen, 224 Zeilen identische Tokenisierung, cp313-Raeder fuer aarch64 und x86_64 vorhanden); was die Zusicherung jetzt noch behauptet (die Dateien auf der Platte bleiben lesbar) und was sie nicht mehr behauptet (dieselbe Fassung); und wo der aufgegebene Teil jetzt gehalten wird (`backend/tests/test_analyzer.py` und `backend/tests/test_language_analyzers.py`).

**c) Zwei neue Selbstproben** nach dem Muster der drei vorhandenen (Zeilen 103-127), als gestellte Markensaetze:
- `test_a_patch_bump_with_the_same_index_format_is_no_drift`: Marken wie im haltenden Satz, nur `tantivy_version` auf `"tantivy v0.26.2, index_format v7"`; `drift_findings` liefert die leere Liste.
- `test_a_changed_index_format_is_still_drift`: derselbe Satz mit `"tantivy v0.27.0, index_format v8"`; `drift_findings` liefert genau einen Befund, und dessen Text nennt `tantivy_version`.

`TANTIVY_PIN` bleibt in diesem Plan auf `"tantivy==0.26.0"`. Der Pin wandert erst in Plan 17-08; dieser Plan bewegt keine Fassung, sondern nur die Vergleichsregel. Dieser Satz gehoert als Kommentar neben `TANTIVY_PIN`, damit die Reihenfolge nicht verlorengeht.
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/test_upgrade_compatibility.py -q && uv run python -m pytest -q && grep -q 'TANTIVY_PIN = "tantivy==0.26.0"' tests/test_upgrade_compatibility.py && grep -qF "A red test here is not a repair" tests/test_upgrade_compatibility.py && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture</automated>
  </verify>
  <acceptance_criteria>
    - `GOLD_V1_0_AND_V1_1["tantivy_version"]` ist `GOLD_INDEX_FORMAT` beziehungsweise `"index_format v7"`; die Zeichenkette `"0.26.0"` steht in der Gold-Tabelle nicht mehr.
    - `TANTIVY_PIN` ist unveraendert `"tantivy==0.26.0"` und traegt den Kommentar, dass der Pin erst in Plan 17-08 wandert.
    - Der Modulkopfsatz "A red test here is not a repair, it is a question for the owner." steht woertlich unveraendert in der Datei.
    - Die beiden neuen Selbstproben existieren und sind gruen; `uv run python -m pytest tests/test_upgrade_compatibility.py -q` meldet mindestens zwei Tests mehr als vor dieser Aenderung.
    - Der Kommentarblock ueber der Gold-Tabelle nennt `E-17-7 Option a`, das Vollzugsdatum und die vier Messbelege.
    - Die volle Suite `uv run python -m pytest -q` aus `backend/` ist gruen; ruff, ruff format, pyright und vulture sind gruen.
  </acceptance_criteria>
  <done>Die Ratsche prueft jetzt die Formathaelfte, beweist ihren roten Zustand an zwei neuen Proben und verbietet weiterhin das Gruenreparieren.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code zu Bestandsindex auf der Platte | `version_mismatch` entscheidet, ob ein installierter Index als fremd gilt; eine falsche Lockerung liest einen unlesbaren Index als vertraeglich |
| Test-Ratsche zu Owner-Entscheid | Ein gruen repariertes Gate nimmt dem Owner die Frage weg, die es stellen soll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-27 | Tampering | `_index_format_matches` liest einen unlesbaren Banner als vertraeglich | mitigate | Faellt geschlossen: None, leer, fehlendes `index_format` und ungleiche Haelfte liefern alle False; fuenf Faelle sind im Verify-Kommando behauptet |
| T-17-28 | Denial of Service | Bestandsinstallationen reindizieren wegen einer Patchnummer | mitigate | Genau diese Lockerung; belegt durch Kreuzlesetest in beide Richtungen und 224 Zeilen identische Tokenisierung |
| T-17-29 | Tampering | Die aufgegebene Absicherung (geaenderte Tokenisierung bei gleichem Format) bleibt ungedeckt | mitigate | Ersatz sind die AST-Waechter und Tabellentests aus den Plaenen 17-03 und 17-06; der Docstring benennt den Ersatz namentlich |
| T-17-30 | Repudiation | Die Gold-Werte werden ohne Entscheid bewegt | mitigate | Vorbedingung liest den Vollzugsabschnitt; der Kommentarblock zitiert `E-17-7 Option a` mit Datum; der Modulkopfsatz bleibt stehen |
| T-17-31 | Elevation of Privilege | , | accept | Kein Berechtigungspfad beruehrt; `version_mismatch` entscheidet nichts, es meldet nur |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket und bewegt keinen Pin; `backend/pyproject.toml` und `backend/uv.lock` bleiben unveraendert |
</threat_model>

<verification>
- `uv run python -m pytest -q` aus `backend/` ist gruen (volle Suite), und zwar noch mit tantivy 0.26.0.
- `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture` aus `backend/` sind gruen.
- `git diff --quiet backend/pyproject.toml backend/uv.lock backend/src/findling/index/open.py`: kein Pin bewegt, kein Banner geaendert.
- Der Vollzugsabschnitt von `17-GRUNDSATZ-ENTSCHEID.md` fuehrt E-17-7 auf Option a; andernfalls ist dieser Plan als entfallen dokumentiert.
</verification>

<success_criteria>
Die Vergleichsregel entscheidet ueber die `index_format`-Haelfte, faellt bei allem Unbekannten geschlossen, und die Suite ist mit dem noch unbewegten Pin gruen.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-07-SUMMARY.md` when done
</output>
