---
phase: 17-owner-tor-und-analyseketten
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dev/stopword_supplement.py
  - backend/src/findling/index/stopwords.py
  - backend/src/findling/index/analyzer.py
  - backend/tests/test_language_analyzers.py
autonomous: true
requirements: [LEX-01]

must_haves:
  truths:
    - "Eine Fabrik baut die Kette fuer en, es, it, nl und pt, und die englische Tokenisierung bewegt sich dabei um kein Byte"
    - "Die Faltung steht vor der Stoppwortliste und damit vor dem Stemmer, und ein Waechter liest das aus dem Syntaxbaum statt es zu erraten"
    - "Die gefaltete Ergaenzungsliste ist maschinell erzeugt, im Code ausgeliefert und per Hash gegen stilles Driften gehalten"
    - "`ANALYZER_VERSION` bleibt auf 1, weil keine bestehende Kette sich bewegt und keine neue registriert ist"
  artifacts:
    - path: "backend/src/findling/index/stopwords.py"
      provides: "FOLDED_STOPWORDS je Snowball-Sprache plus folded_stopwords_hash()"
      exports: ["FOLDED_STOPWORDS", "folded_stopwords_hash"]
    - path: "backend/src/findling/index/analyzer.py"
      provides: "snowball_analyzer() als eine Fabrik fuer fuenf Sprachen"
      contains: "def snowball_analyzer"
    - path: "scripts/dev/stopword_supplement.py"
      provides: "Erzeugung der Ergaenzungsliste aus stopwords.rs des gepinnten Tags"
    - path: "backend/tests/test_language_analyzers.py"
      provides: "Kettenwaechter ueber den AST, No-op-Beweis, Integritaet der Ergaenzungsliste"
      min_lines: 120
  key_links:
    - from: "backend/src/findling/index/analyzer.py"
      to: "backend/src/findling/index/stopwords.py"
      via: "Import von FOLDED_STOPWORDS"
      pattern: "from findling.index.stopwords import"
    - from: "backend/tests/test_language_analyzers.py"
      to: "backend/src/findling/index/analyzer.py"
      via: "AST-Waechter liest die Quelldatei, nicht das gebaute Objekt"
      pattern: "filter_chain"
---

<objective>
Die gemeinsame Kettenfabrik `snowball_analyzer()` entsteht, zusammen mit der maschinell erzeugten, gefalteten Ergaenzungs-Stoppwortliste und dem Waechter, der die Filterposition aus dem Syntaxbaum liest.

Purpose: LEX-01. Die Kette ist `lowercase -> ascii_fold -> stopword(lang) -> custom_stopword(gefaltet) -> remove_long -> stemmer(lang)` (Rezept A+, gemessen 467 von 573 Paaren, 0 Stoppwort-Lecks in beiden Schreibweisen). Die Arbeit beruehrt D-04 nicht: die vier neuen Ketten werden angelegt und nirgends registriert, `ANALYZER_VERSION` bleibt auf 1, der Hash der Ergaenzungsliste wird kein Versionsmerker (RESEARCH 6.3, Pitfall 4, PATTERNS "Marken-Falle").
Output: `scripts/dev/stopword_supplement.py`, `backend/src/findling/index/stopwords.py`, `snowball_analyzer()` in `analyzer.py`, `backend/tests/test_language_analyzers.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@CLAUDE.md
@docs/testing.md
</context>

<interfaces>
Was der Executor ohne Suche braucht. Gelesen am 2026-09-23.

Aus `backend/src/findling/index/analyzer.py`:
- Zeilen 51-53, Modulkopf: "Any change to a chain below has to raise ANALYZER_VERSION. Tokenisation is part of the data: an index written with one chain and queried with another disagrees with itself, and the only correct answer to that is a visible reindex."
- Zeile 73: `ANALYZER_VERSION = 1`
- Zeilen 79-81: `TOKENIZER_DE = "de"`, `TOKENIZER_EN = "en"`, `TOKENIZER_NAME = "name"`
- Zeile 86: `MAX_TOKEN_CHARS = 48`
- Zeilen 92-102 und 211-224: `_CACHED_GERMAN` plus `cached_german_analyzer()` , wird NICHT nachgeahmt
- Zeilen 182-192: `def english_analyzer() -> TextAnalyzer` , `TextAnalyzerBuilder(Tokenizer.simple()).filter(Filter.lowercase()).filter(Filter.ascii_fold()).filter(Filter.stopword("english")).filter(Filter.remove_long(MAX_TOKEN_CHARS)).filter(Filter.stemmer("english")).build()`
- Importzeile: `from tantivy import Filter, TextAnalyzer, TextAnalyzerBuilder, Tokenizer`

Aus `backend/src/findling/index/open.py`:
- Zeilen 98-101 registrieren genau vier Tokenizer: `TOKENIZER_DE`, `TOKENIZER_EN`, `TOKENIZER_NAME`, `TOKENIZER_STORED_ONLY`. Diese Datei wird in Phase 17 NICHT angefasst.

Aus `backend/src/findling/index/wordlist.py`:
- Zeile 95: `FUGEN = ("s", "es", "n", "en", "er", "ns")`
- Zeilen 143-153: `def wordlist_hash(entries: Sequence[str]) -> str` , SHA-256 ueber `"\n".join(entries)`, mit dem Docstring, der erklaert, warum die gefilterte Liste gehasht wird und nicht die Quelldatei

Aus `backend/tests/test_analyzer.py`:
- Zeile 58: `PACKAGE_ROOT = Path(__file__).resolve().parents[1] / "src" / "findling"`
- Zeile 509: `ANALYZER_SOURCE = PACKAGE_ROOT / "index" / "analyzer.py"`
- Zeilen 513-523: `EXPECTED_GERMAN_CHAIN` und `EXPECTED_NAME_CHAIN`
- Zeilen 545-560: `def filter_chain(source: str, function: str) -> list[str]` , liest nur den Syntaxbaum, gibt bei unbekannter Funktion die leere Liste
- Zeilen 578-630: die drei Waechter-Selbsttests
- `backend/tests/` hat KEIN `__init__.py`, pytest laeuft im prepend-Importmodus, `from test_analyzer import ANALYZER_SOURCE, filter_chain` funktioniert deshalb aus einem Geschwistermodul

Gemessene Zielwerte (2026-09-23, tantivy-Tag 0.26.2):
- Eingebaute Listen: SPANISH 308 (84 akzentuiert), ITALIAN 279 (11), PORTUGUESE 203 (36), DUTCH 101 (0), ENGLISH ohne Akzente
- Gefaltete Ergaenzung: 77 (es), 10 (it), 0 (nl), 30 (pt), 0 (en), zusammen 117
- `Filter.custom_stopword([])` ist ein No-op (13 Testwoerter, identische Tokens mit und ohne)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Erzeugungswerkzeug und die ausgelieferte Ergaenzungsliste</name>
  <files>scripts/dev/stopword_supplement.py, backend/src/findling/index/stopwords.py</files>
  <read_first>
    - `scripts/dev/compound_probe.py` (vollstaendig; Sicherheitsregel im Docstring Zeilen 26-38, Argumentmuster Zeilen 137-158 und 195-196, Rot-Muster Zeilen 125-134 und 175-189)
    - `backend/src/findling/index/wordlist.py`, Zeilen 80-160 (`FUGEN`, `wordlist_hash`, `ENCODING`)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 3.2 (`parse_stopwords`, `fold`, `folded_supplement`), Abschnitt 10 ("Die Ergaenzungsliste erzeugen"), Pitfall 5
    - `docs/testing.md`, Zeilen 18-36 (welche Gates fuer `scripts/dev/*` gelten)
  </read_first>
  <action>
**a) `scripts/dev/stopword_supplement.py`** , das Werkzeug, das die Liste erzeugt.

Englische Docstrings, handgeschriebener Argumentparser (kein argparse), `main(argv) -> int`, `raise SystemExit(main(sys.argv[1:]))`, genau wie `compound_probe.py`. Aufruf: `stopword_supplement.py [--tag TAG] [--source PATH] OUTPUT`.

Funktionen, aus RESEARCH 3.2 uebernommen und ruff-konform gemacht:
- `parse_stopwords(path) -> dict[str, list[str]]` , parst `pub const <NAME>: ... &[...]` aus `src/tokenizer/stop_word_filter/stopwords.rs`.
- `fold(word) -> str` , faltet mit `TextAnalyzerBuilder(Tokenizer.raw()).filter(Filter.ascii_fold()).build()`, also mit tantivys eigenem Filter und nicht mit `unicodedata`. Der Docstring sagt, warum: eine nachgebaute Faltung erzeugt eine Ergaenzungsliste, die nicht zur Kette passt (tantivy faltet auch `ss` und `n`-Formen).
- `folded_supplement(words) -> list[str]` , liefert die gefalteten Formen, die die eingebaute Liste nicht schon selbst traegt, in Erstauftrittsreihenfolge.

`--source` nimmt einen lokalen Pfad auf `stopwords.rs`; ohne `--source` laedt das Werkzeug die Datei vom gepinnten Tag (Vorgabe `--tag 0.26.2`) ueber die GitHub-raw-Adresse von `quickwit-oss/tantivy`. Ohne Netz bricht es mit Rueckgabecode 1 und einer Meldung ab, die den `--source`-Ausweg nennt.

Ausgabe: das Werkzeug schreibt den fertigen Python-Literalblock fuer `FOLDED_STOPWORDS` nach OUTPUT und gibt auf stdout ausschliesslich Zahlen und Digests aus, im Muster von `_numbers()` in `compound_probe.py`: `tag=`, `spanish_builtin=`, `spanish_supplement=`, dasselbe fuer italian, dutch, portuguese, english, dann `supplement_total=` und `supplement_hash=`.

Sicherheitsregel als eigener Absatz im Modulkopf, uebertragen von T-02-14: das Werkzeug liest ausschliesslich veroeffentlichte Snowball-Stoppwoerter aus dem tantivy-Quelltext, nie `state.db`, nie einen Index, nie eine Nutzerdatei. Was es druckt, ist eine veroeffentlichte Wortliste.

Erwartungswerte als Kopfkommentar, im Muster von `measure_compounds.sh` Zeilen 13-36, inklusive des Satzes "Eine Abweichung ist ein Befund fuer den Messbericht und kein Grund, eine Datei von Hand zu bearbeiten": `spanish_supplement=77 italian_supplement=10 dutch_supplement=0 portuguese_supplement=30 english_supplement=0 supplement_total=117`.

**b) `backend/src/findling/index/stopwords.py`** , der ausgelieferte Code.

Neues Modul, weil `wordlist.py` die deutsche Zerlegungsliste traegt und die beiden Anliegen sich nicht vermischen sollen. Inhalt:
- Modulkopf (englisch): woher die Liste stammt (tantivy-Tag 0.26.2, `src/tokenizer/stop_word_filter/stopwords.rs`), womit sie erzeugt wurde (`scripts/dev/stopword_supplement.py`), warum sie ueberhaupt existiert (die eingebauten Listen vergleichen exakt und tragen echte Akzente; sobald `ascii_fold` vor ihnen laeuft, greifen sie fuer die flache Schreibweise nicht mehr, und dann lecken 77 spanische, 10 italienische und 30 portugiesische Stoppwoerter als Terme in den Index), und der Satz, dass sie von Hand nicht angefasst wird.
- `FOLDED_STOPWORDS: Final[dict[str, tuple[str, ...]]]` mit den fuenf Schluesseln `"english"`, `"spanish"`, `"italian"`, `"dutch"`, `"portuguese"`. Werte: die vom Werkzeug erzeugten Tupel; `"english"` und `"dutch"` sind das leere Tupel. Die Akzente stehen ausschliesslich in String-Literalen; ein Satz im Modulkopf begruendet das nach dem Vorbild von `test_analyzer.py:21-23`.
- `def folded_stopwords_hash(entries: Sequence[str]) -> str` , SHA-256 ueber `"\n".join(entries)`, woertlich nach dem Muster von `wordlist_hash` (`wordlist.py:143-153`), mit demselben Docstring-Ton.
- **Marken-Falle, als fetter Warnabsatz in den Modulkopf:** dieser Hash darf NICHT nach `expected_versions()` (`index/open.py:123-141`). Er waere dort eine sechste Versionsmarke, die auf keiner Bestandsinstallation existiert, und `Store.version_mismatch` liest eine fehlende Marke als Abweichung. Das Ergebnis waere ein voller Reindex auf jeder Installation im Feld, gemessen 19 h 20 min. Der Hash gehoert in einen Test und nirgendwo sonst hin.

Fuehre das Werkzeug einmal aus und uebernimm sein Ergebnis in `stopwords.py`. Notiere den ausgegebenen `supplement_hash` im Modulkopf von `stopwords.py` und verwende denselben Wert in Task 3 als Testkonstante.

Gates: `backend/` durch ruff, ruff format, pyright, vulture. `scripts/dev/stopword_supplement.py` nur durch `uv run ruff check --config backend/pyproject.toml scripts/dev/stopword_supplement.py` und `uv run ruff format --check --config backend/pyproject.toml scripts/dev/stopword_supplement.py`, kein pyright, kein vulture (`docs/testing.md` Zeilen 20-36).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "
from findling.index.stopwords import FOLDED_STOPWORDS, folded_stopwords_hash
sizes = {k: len(v) for k, v in FOLDED_STOPWORDS.items()}
assert sizes == {'english': 0, 'spanish': 77, 'italian': 10, 'dutch': 0, 'portuguese': 30}, sizes
flat = [w for lang in sorted(FOLDED_STOPWORDS) for w in FOLDED_STOPWORDS[lang]]
assert len(flat) == 117, len(flat)
assert folded_stopwords_hash(flat)
print('GRUEN')
" && uv run ruff check && uv run ruff format --check && uv run pyright && cd .. && cd backend && uv run ruff check --config pyproject.toml ../scripts/dev/stopword_supplement.py && uv run ruff format --check --config pyproject.toml ../scripts/dev/stopword_supplement.py && ! grep -n "folded_stopwords_hash" src/findling/index/open.py && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dev/stopword_supplement.py` existiert, hat `main(argv) -> int` und `raise SystemExit(main(sys.argv[1:]))`, keinen `argparse`-Import, und seine stdout-Ausgabe besteht ausschliesslich aus `name=wert`-Zeilen mit Zahlen und Digests.
    - Der Modulkopf des Werkzeugs traegt die Sicherheitsregel (liest nur veroeffentlichte Snowball-Listen, nie `state.db`, nie einen Index, nie eine Nutzerdatei).
    - `FOLDED_STOPWORDS` hat genau fuenf Schluessel mit den Laengen 0 (english), 77 (spanish), 10 (italian), 0 (dutch), 30 (portuguese), zusammen 117 Eintraege.
    - Jeder Eintrag von `FOLDED_STOPWORDS` besteht nur aus ASCII-Zeichen (er ist die gefaltete Form) und kommt in seiner Sprachliste genau einmal vor.
    - `folded_stopwords_hash` ist eine SHA-256 ueber `"\n".join(entries)` und steht als Wert im Modulkopf von `stopwords.py`.
    - `grep -n "folded_stopwords_hash\|FOLDED_STOPWORDS" backend/src/findling/index/open.py` findet nichts: der Hash ist keine Versionsmarke.
    - ruff, ruff format und pyright aus `backend/` sind gruen; das Dev-Skript ist ruff-gruen mit `--config backend/pyproject.toml`.
  </acceptance_criteria>
  <done>Die Ergaenzungsliste ist maschinell erzeugt, im Code ausgeliefert, per Hash beschreibbar und beweisbar keine Versionsmarke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Die gemeinsame Kettenfabrik snowball_analyzer()</name>
  <files>backend/src/findling/index/analyzer.py</files>
  <read_first>
    - `backend/src/findling/index/analyzer.py` (vollstaendig; Modulkopftabelle Zeilen 3-22, Kopfsatz Zeilen 51-53, Konstanten Zeilen 70-90, `german_analyzer` 146-179, `english_analyzer` 182-192, `name_analyzer` 195-208, Cache 92-102 und 211-224)
    - `backend/src/findling/index/stopwords.py` (der Stand aus Task 1)
    - `backend/src/findling/index/open.py`, Zeilen 90-105 (die Registrierungsstelle, die NICHT angefasst wird)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 10 ("Die empfohlene Kette"), Abschnitt 2.4, Abschnitt 6.3, Pitfall 4
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitt "`backend/src/findling/index/analyzer.py`"
  </read_first>
  <behavior>
    - `snowball_analyzer("spanish", FOLDED_STOPWORDS["spanish"])` liefert fuer `"informacion"` und fuer die akzentuierte Schreibweise denselben Term.
    - `snowball_analyzer("italian", ...)` liefert fuer das italienische Stoppwort "warum" in beiden Schreibweisen die leere Tokenliste.
    - `snowball_analyzer("dutch", ())` verwirft den niederlaendischen Betonungsakzent-Fall vollstaendig und erzeugt kein Muelltoken.
    - `english_analyzer()` liefert fuer jede Eingabe exakt dieselbe Tokenliste wie vor dieser Aenderung.
  </behavior>
  <action>
Aendere `backend/src/findling/index/analyzer.py` an genau drei Stellen.

**a) Modulkopf praezisieren.** Der Satz in Zeile 51-53 bleibt stehen und bekommt einen Zusatz, ohne den der naechste Leser `ANALYZER_VERSION` aus Vorsicht hebt und damit Erfolgskriterium 4 der Phase reisst (RESEARCH Pitfall 4). Der Zusatz sagt in englischer Prosa: eine Kette, die hinzugefuegt, aber nirgends registriert wird, aendert keine Tokenisierung, also auch nicht die Marke; die Marke steigt in Phase 18 zusammen mit dem Schema. Ergaenze die Modulkopftabelle (Zeilen 3-22) um eine Zeile je Filterposition der neuen Kette, in derselben Form wie die vorhandenen Zeilen, mit der Spalte "Why exactly here": `lowercase` (ab hier vergleicht alles exakt), `ascii_fold` (vor der Stoppwortliste und damit vor dem Stemmer, weil das die einzige Position ist, die die akzentuierte und die flache Schreibweise auf einen Term haelt; gemessen 2026-09-23, 467 von 573 geordneten Paaren gegen 463 bei spaeter Faltung), `stopword(lang)` (die eingebaute Snowball-Liste), `custom_stopword(folded)` (direkt dahinter, weil die eingebaute Liste exakt vergleicht und echte Akzente traegt; ohne sie lecken 77 es, 10 it und 30 pt Stoppwoerter in beiden Schreibweisen), `remove_long(48)`, `stemmer(lang)` (zuletzt, wie in allen anderen Ketten).

**b) `snowball_analyzer(language: str, folded_stopwords: Sequence[str]) -> TextAnalyzer`** neu anlegen, platziert zwischen `english_analyzer()` und `name_analyzer()`. Kette exakt in dieser Reihenfolge: `TextAnalyzerBuilder(Tokenizer.simple())`, `Filter.lowercase()`, `Filter.ascii_fold()`, `Filter.stopword(language)`, `Filter.custom_stopword(list(folded_stopwords))`, `Filter.remove_long(MAX_TOKEN_CHARS)`, `Filter.stemmer(language)`, `build()`. Der Docstring uebernimmt sinngemaess den aus RESEARCH Abschnitt 10: die Faltposition wurde gemessen und nicht erschlossen; der Preis ist die Numerusklasse der Woerter, deren Snowball-Suffix einen Akzent traegt, und dieser Preis steht in `docs/language-analyzers.md`; `folded_stopwords` ist die Ergaenzung, die die Faltung in die eingebaute Liste reisst, stammt aus derselben Liste und kann deshalb nicht von ihr wegdriften; fuer Englisch und Niederlaendisch ist sie leer, und ein leerer `custom_stopword`-Filter ist gemessen ein No-op, weshalb Englisch diese Fabrik benutzen kann, ohne dass sich seine Tokenisierung um ein Byte bewegt.

Importiere `FOLDED_STOPWORDS` aus `findling.index.stopwords` in der bestehenden Importgruppe, direkt neben dem `wordlist`-Import.

Das Singleton-Cache-Muster (`_CACHED_GERMAN`, `cached_german_analyzer`, `build_count`) wird NICHT nachgeahmt. Begruende das in einem Satz im Docstring: das deutsche Automaton kostet gemessen 0,44 s und 23 MB, die vier Snowball-Ketten sind einkompiliert und kosten nichts, und ein Cache dafuer waere Ballast, den `vulture` zu Recht anmeckert.

**c) `english_analyzer()` in die Fabrik ueberfuehren.** Der Rumpf wird `return snowball_analyzer("english", FOLDED_STOPWORDS["english"])`. Der Docstring behaelt seine Aussage und bekommt den Satz, dass die Ueberfuehrung tokenidentisch ist und dass `backend/tests/test_language_analyzers.py` genau das beweist. Die Signatur bleibt `def english_analyzer() -> TextAnalyzer`, damit `index/open.py:99` unveraendert bleibt.

Was NICHT geschieht: `ANALYZER_VERSION` bleibt auf `1`. `german_analyzer` und `name_analyzer` bleiben Zeile fuer Zeile unveraendert. `index/open.py` wird nicht angefasst, es wird kein Tokenizer registriert, und es entsteht keine `TOKENIZER_ES`-artige Konstante; die Registrierung ist Zusage von Phase 18.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "
from findling.index.analyzer import ANALYZER_VERSION, english_analyzer, snowball_analyzer
from findling.index.stopwords import FOLDED_STOPWORDS
assert ANALYZER_VERSION == 1, ANALYZER_VERSION
es = snowball_analyzer('spanish', FOLDED_STOPWORDS['spanish'])
assert es.analyze('información') == es.analyze('informacion'), (es.analyze('información'), es.analyze('informacion'))
it = snowball_analyzer('italian', FOLDED_STOPWORDS['italian'])
assert it.analyze('perché') == [] and it.analyze('perche') == []
nl = snowball_analyzer('dutch', FOLDED_STOPWORDS['dutch'])
assert nl.analyze('één') == [] and nl.analyze('een') == []
print('GRUEN')
" && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture && uv run python -m pytest tests/test_analyzer.py tests/test_index_open.py tests/test_lockstep_versions.py tests/test_upgrade_compatibility.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `snowball_analyzer` existiert mit der Signatur `(language: str, folded_stopwords: Sequence[str]) -> TextAnalyzer` und baut die sechs Filter in der Reihenfolge lowercase, ascii_fold, stopword, custom_stopword, remove_long, stemmer.
    - `english_analyzer()` delegiert an `snowball_analyzer("english", FOLDED_STOPWORDS["english"])` und behaelt Signatur und Namen; `backend/src/findling/index/open.py` ist unveraendert (`git diff --quiet backend/src/findling/index/open.py`).
    - `ANALYZER_VERSION` ist weiterhin `1`; `grep -n "ANALYZER_VERSION = " backend/src/findling/index/analyzer.py` zeigt `= 1`.
    - Der Modulkopf enthaelt den Satz, dass eine hinzugefuegte, nirgends registrierte Kette keine Tokenisierung aendert.
    - Die Modulkopftabelle hat fuer die neue Kette sechs Zeilen mit je einer Begruendung in der Spalte "Why exactly here".
    - `tests/test_analyzer.py`, `tests/test_index_open.py`, `tests/test_lockstep_versions.py` und `tests/test_upgrade_compatibility.py` sind unveraendert gruen.
    - ruff, ruff format, pyright und vulture aus `backend/` sind gruen.
  </acceptance_criteria>
  <done>Eine Fabrik bedient fuenf Sprachen, die englische Tokenisierung ist unbewegt, und keine Marke hat sich geruehrt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Kettenwaechter, No-op-Beweis und Integritaet der Ergaenzungsliste</name>
  <files>backend/tests/test_language_analyzers.py</files>
  <read_first>
    - `backend/tests/test_analyzer.py`, Zeilen 1-60 (Modulkopf samt Umlautbegruendung, `PACKAGE_ROOT`), Zeilen 495-643 (`ANALYZER_SOURCE`, `EXPECTED_GERMAN_CHAIN`, `EXPECTED_NAME_CHAIN`, `_factory_names`, `filter_chain`, die drei Waechter-Selbsttests)
    - `backend/src/findling/index/analyzer.py` und `backend/src/findling/index/stopwords.py` (die Staende aus Task 1 und 2)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitt "`backend/tests/test_language_analyzers.py`" (EXPECTED_SNOWBALL_CHAIN, die drei Waechter-Selbsttests, das dritte Gate ohne Vorbild)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 2.4 (No-op-Messung), Assumptions A6
  </read_first>
  <behavior>
    - Test 1: `filter_chain(ANALYZER_SOURCE, "snowball_analyzer")` ist exakt `["lowercase", "ascii_fold", "stopword", "custom_stopword", "remove_long", "stemmer"]`.
    - Test 2: Der Waechter unterscheidet zwei Ketten (`german_analyzer` ergibt etwas anderes).
    - Test 3: Der Waechter sieht eine nach hinten geschobene Faltung (gestellte Quelle mit `ascii_fold` hinter `stemmer` ergibt nicht `EXPECTED_SNOWBALL_CHAIN`).
    - Test 4: Ein Kommentar oder Docstring, der einen Filternamen nennt, landet nicht in der Kette.
    - Test 5: `Filter.custom_stopword([])` ist ein No-op: dieselben Eingaben liefern mit und ohne den leeren Filter dieselben Tokenlisten.
    - Test 6: `FOLDED_STOPWORDS` hat die Laengen 0/77/10/0/30 und den festgeschriebenen SHA-256.
    - Test 7: Jeder Eintrag der Ergaenzungsliste ist noetig: `lowercase -> stopword(lang)` allein laesst ihn als Term stehen, und er ist bereits seine eigene gefaltete Form.
  </behavior>
  <action>
Lege `backend/tests/test_language_analyzers.py` an. Englische Docstrings und Bezeichner; Akzente ausschliesslich in String-Literalen, mit dem begruendenden Satz im Modulkopf nach dem Vorbild von `test_analyzer.py:21-23`. Keine Em-Dashes.

Der AST-Leser wird NICHT kopiert: importiere ihn aus dem Geschwistermodul mit `from test_analyzer import ANALYZER_SOURCE, filter_chain`. Ein Satz im Modulkopf begruendet den Import (ein zweiter Leser waere ein zweites Gate, das am Tag der Aenderung mit sich selbst uebereinstimmt).

Modulkonstante `EXPECTED_SNOWBALL_CHAIN = ["lowercase", "ascii_fold", "stopword", "custom_stopword", "remove_long", "stemmer"]`, darueber der Kommentar, der jede der sechs Positionen mit ihrem Messgrund benennt, im Ton der Kommentare ueber `EXPECTED_GERMAN_CHAIN` und ueber `test_the_german_chain_stands_in_the_measured_order`.

Die Waechter-Selbsttests (Tests 2 bis 4) folgen den Vorbildern `test_the_guard_tells_two_chains_apart`, `test_the_guard_sees_a_missing_splitter` und `test_the_guard_sees_remove_long_pulled_in_front_of_the_splitter`: eine gestellte Quelle als String-Literal im Test, durch `filter_chain` geschickt, Ergebnis ungleich der Erwartung. Der zu diesem Plan passende Name fuer Test 3 ist `test_the_guard_sees_the_fold_pushed_behind_the_stemmer`.

Test 5, der No-op-Beweis, hat kein Vorbild im Baum und ist trotzdem Pflicht, weil er die Bedingung dafuer ist, dass `english_analyzer()` ueberhaupt in die gemeinsame Fabrik durfte (RESEARCH 6.3 und A6). Form: zwei von Hand gebaute Ketten, identisch bis auf `Filter.custom_stopword([])`, gefahren ueber mindestens 13 Woerter, darunter englische Stoppwoerter, ein langes Wort und ein akzentuiertes; Behauptung ist Tokenliste gleich Tokenliste, keine Prosa. Zusaetzlich die Behauptung, dass `english_analyzer()` fuer dieselben Woerter dieselbe Liste liefert wie die handgebaute Kette ohne den leeren Filter.

Test 6 haelt den Hash aus Task 1 als Modulkonstante `FOLDED_SUPPLEMENT_SHA256` mit dem Kommentar, warum er hier und nicht in `expected_versions()` steht (eine sechste Marke waere ein Reindex auf jeder Bestandsinstallation) und was zu tun ist, wenn er faellt (`scripts/dev/stopword_supplement.py` erneut fahren, Differenz in den Messbericht schreiben, dann den Wert hier nachziehen, nie umgekehrt).

Test 7 beweist offline, dass jeder Eintrag noetig ist: fuer jede Sprache eine Kette `Tokenizer.simple() -> lowercase -> stopword(lang)` bauen, jedes Wort der Ergaenzungsliste hindurchschicken und eine nichtleere Tokenliste verlangen (sonst stuende das Wort schon in der eingebauten Liste und die Ergaenzung waere Ballast). Dazu die Behauptung, dass jeder Eintrag gleich seiner eigenen `ascii_fold`-Form ist. Die vollstaendige Dichtheitsmessung ueber alle 891 eingebauten Stoppwoerter braucht `stopwords.rs` und ist Zusage der Plaene 17-05 und 17-06; dieser Satz gehoert als Verweis in den Modulkopf.

Fehlermeldungen nennen den Namen UND die Seite, auf der er fehlt (Muster `test_allowlist_parity.py:54-64`).
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/test_language_analyzers.py -q && uv run python -m pytest -q && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_language_analyzers.py -q` meldet mindestens 7 bestandene Tests.
    - `EXPECTED_SNOWBALL_CHAIN` steht genau einmal in der Datei und hat genau sechs Eintraege in der Reihenfolge lowercase, ascii_fold, stopword, custom_stopword, remove_long, stemmer.
    - Die Datei enthaelt `from test_analyzer import` und definiert selbst keine Funktion namens `filter_chain` (`grep -c "def filter_chain" backend/tests/test_language_analyzers.py` ergibt 0).
    - Der No-op-Test faehrt mindestens 13 Woerter und behauptet Gleichheit der Tokenlisten, nicht nur gleiche Laenge.
    - `FOLDED_SUPPLEMENT_SHA256` steht in der Testdatei und nicht in `backend/src/findling/index/open.py`.
    - Die volle Suite `uv run python -m pytest -q` aus `backend/` ist gruen, ebenso ruff, ruff format, pyright und vulture.
    - `git status --porcelain` listet genau die vier Pfade dieses Plans.
  </acceptance_criteria>
  <done>Die Filterposition ist aus dem Syntaxbaum bewiesen, die englische Tokenisierung nachweislich unbewegt, die Ergaenzungsliste gegen stilles Driften gehalten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tantivy-Quelltext zu ausgelieferter Konstante | Die Ergaenzungsliste wird aus einer fremden Quelle abgeleitet und im Produkt ausgeliefert |
| Analysekette zu Indexdaten | Die Tokenisierung ist Teil der Daten; eine unbemerkte Aenderung macht Bestandsindizes unauffindbar |
| Dev-Werkzeug zu Nutzerdaten | Ein Messwerkzeug, das druckt, was es tokenisiert, ist der billigste Weg, Inhalte zu lecken (T-02-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Tampering | `FOLDED_STOPWORDS` driftet unbemerkt beim naechsten tantivy-Sprung | mitigate | Erzeugungswerkzeug `scripts/dev/stopword_supplement.py` plus `FOLDED_SUPPLEMENT_SHA256` als Testkonstante; Notwendigkeitstest je Eintrag gegen `Filter.stopword(lang)` |
| T-17-10 | Denial of Service | Bestandsinstallationen im Feld | mitigate | `ANALYZER_VERSION` bleibt 1, keine Registrierung in `index/open.py`, der Supplement-Hash wird ausdruecklich keine sechste Marke; Modulkopfsatz verhindert das vorsorgliche Heben der Marke |
| T-17-11 | Tampering | Stille Aenderung der englischen Tokenisierung durch die Ueberfuehrung | mitigate | No-op-Beweis fuer `Filter.custom_stopword([])` ueber mindestens 13 Woerter plus Vergleich gegen die handgebaute Kette |
| T-17-12 | Information Disclosure | `scripts/dev/stopword_supplement.py` | mitigate | Sicherheitsregel T-02-14 im Modulkopf; das Werkzeug liest ausschliesslich veroeffentlichte Snowball-Listen, stdout traegt nur Zahlen und Digests |
| T-17-13 | Tampering | Der AST-Waechter wird umgangen, indem ein Filter in einem Kommentar steht | mitigate | `filter_chain` liest den Syntaxbaum; Selbsttest 4 stellt genau diesen Fall |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket; `stopword_supplement.py` laedt nur eine Textdatei vom gepinnten tantivy-Tag und schreibt kein Abhaengigkeitsmanifest |
</threat_model>

<verification>
- `uv run python -m pytest -q` aus `backend/` ist gruen (volle Suite).
- `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture` aus `backend/` sind gruen.
- Das Dev-Skript ist ruff-gruen mit `--config backend/pyproject.toml`; pyright und vulture laufen nicht darauf.
- `git diff --quiet backend/src/findling/index/open.py` und `git diff --quiet backend/pyproject.toml`: keine Registrierung, kein bewegter Pin.
- `uv run python -m pytest tests/test_upgrade_compatibility.py tests/test_lockstep_versions.py tests/test_search_fields_lockstep.py tests/test_index_open.py -q` ist gruen: Erfolgskriterium 4 haelt.
</verification>

<success_criteria>
Eine Fabrik baut die Kette fuer fuenf Sprachen in der gemessenen Reihenfolge, ein Waechter liest diese Reihenfolge aus dem Syntaxbaum, die Ergaenzungsliste ist maschinell erzeugt und gehashed, und eine Bestandsinstallation merkt von alldem nichts.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-03-SUMMARY.md` when done
</output>
