---
phase: 17-owner-tor-und-analyseketten
plan: 06
type: execute
wave: 3
depends_on: ["17-05"]
files_modified:
  - backend/tests/test_language_analyzers.py
  - docs/language-analyzers.md
autonomous: true
requirements: [LEX-01]

must_haves:
  truths:
    - "Die zusammengefuehrte Testfall-Tabelle aus STACK, FEATURES und PITFALLS ist je Sprache als Test gruen oder als dokumentierter Verlust festgeschrieben"
    - "Kein Stoppwort der eingebauten Listen erzeugt einen Term, weder akzentuiert noch flach"
    - "Ein Verlust, der dazukommt ODER verschwindet, macht die Suite rot"
    - "Das Verdikt je Sprache steht datiert in der Produktdokumentation"
  artifacts:
    - path: "backend/tests/test_language_analyzers.py"
      provides: "Formfamilien-Tabelle, Dichtheitsgate, Verlustlisten in beide Richtungen, die 15 Faelle"
      min_lines: 280
    - path: "docs/language-analyzers.md"
      provides: "Produktdoku im Aufbau von german-analyzer.md mit Kettenordnung, Messzahlen und bekannten Grenzen"
      min_lines: 120
  key_links:
    - from: "backend/tests/test_language_analyzers.py"
      to: "backend/tests/fixtures/chain_cases_es.txt"
      via: "geteilter Fixture-Leser aus scripts/dev/chain_probe.py"
      pattern: "read_families"
    - from: "backend/tests/test_language_analyzers.py"
      to: "backend/tests/fixtures/snowball_stopwords_0_26_2.txt"
      via: "Dichtheitsmessung offline"
      pattern: "snowball_stopwords"
    - from: "docs/language-analyzers.md"
      to: "docs/measurements/2026-09-analyseketten/README.md"
      via: "jede Zahl der Doku zitiert den Messbericht"
      pattern: "2026-09-analyseketten"
---

<objective>
Die Messung wird zur Abnahme: Formfamilien-Tabelle, Stoppwort-Dichtheit und Verlustlisten werden Tests, und das Verdikt je Sprache wird datierte Produktdokumentation.

Purpose: Erfolgskriterium 2 der Phase. LEX-01 macht die zusammengefuehrte Testfall-Tabelle zum Abnahmekriterium, und das Verdikt je Sprache muss datiert in der Doku stehen. Ohne diesen Plan ist die Messung ein einmaliger Lauf statt eines Gates.
Output: erweitertes `backend/tests/test_language_analyzers.py`, `docs/language-analyzers.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@docs/measurements/2026-09-analyseketten/README.md
@CLAUDE.md
@docs/testing.md
</context>

<interfaces>
Was der Executor ohne Suche braucht.

Aus Plan 17-03 (`backend/tests/test_language_analyzers.py`, bereits vorhanden):
- `from test_analyzer import ANALYZER_SOURCE, filter_chain`
- `EXPECTED_SNOWBALL_CHAIN`
- `FOLDED_SUPPLEMENT_SHA256`
- die Waechter-Selbsttests und der No-op-Beweis

Aus Plan 17-03 (Produkt):
- `findling.index.analyzer.snowball_analyzer(language, folded_stopwords)`
- `findling.index.stopwords.FOLDED_STOPWORDS` mit den Schluesseln `english`, `spanish`, `italian`, `dutch`, `portuguese`

Aus Plan 17-05:
- `scripts/dev/chain_probe.py` mit `read_families(path) -> list[list[str]]` (der EINZIGE Ausleger des Fixture-Formats)
- `backend/tests/fixtures/chain_cases_{es,it,nl,pt}.txt` mit 20, 14, 13 und 18 Familien
- `backend/tests/fixtures/chain_known_losses_{es,it,nl,pt}.txt`
- `backend/tests/fixtures/snowball_stopwords_0_26_2.txt`, Zeilen `sprache<TAB>wort`, 308 spanish, 279 italian, 203 portuguese, 101 dutch
- `docs/measurements/2026-09-analyseketten/` mit Bericht und Rohdaten

Aus `backend/tests/test_analyzer.py` (Vorbilder):
- Zeilen 51-56 und 138-148: Pfadkonstanten oben, Modul-Fixtures, die einmal lesen
- Zeilen 237-250: `test_every_asserted_word_stands_in_the_measured_case_list`, das Gate ueber dem Gate
- Zeilen 203-235: dokumentierte Grenzen als Doppelrichtungs-Behauptung

Zielzahlen der Abnahme (RESEARCH 2.3): es 174 von 208, it 56 von 56, nl 57 von 81, pt 180 von 228, zusammen 467 von 573.

Aus `docs/german-analyzer.md` (Aufbauvorbild): Kopfsatz, "The constituent list", "Measured numbers", "The filter order" mit Textdiagramm plus Tabelle "Why exactly here", "Licence and provenance", "Memory", "Known limits" (fette Kennung, ein Absatz je Grenze, jede nennt die Alternative und warum sie teurer ist).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formfamilien-Abnahme und Verlustlisten in beide Richtungen</name>
  <files>backend/tests/test_language_analyzers.py</files>
  <read_first>
    - `backend/tests/test_language_analyzers.py` (der Stand aus Plan 17-03)
    - `backend/tests/test_analyzer.py`, Zeilen 51-56, 130-250 (Fixture-Muster, Tabellenmuster, das Gate ueber dem Gate)
    - `scripts/dev/chain_probe.py` (`read_families`, `family_score`)
    - `backend/tests/fixtures/chain_cases_es.txt` und `backend/tests/fixtures/chain_known_losses_es.txt`
    - `docs/measurements/2026-09-analyseketten/rohdaten/kennzahlen.txt`
  </read_first>
  <behavior>
    - Test: Je Sprache erreicht die ausgelieferte Kette exakt die gemessene Trefferzahl der Formfamilien-Metrik (es 174/208, it 56/56, nl 57/81, pt 180/228). Nicht "mindestens", sondern exakt, damit auch ein unerwarteter Gewinn rot wird.
    - Test: Die Menge der Formpaare, die auseinanderfallen, ist exakt die Menge aus `chain_known_losses_<lang>.txt`. Ein neuer Verlust ist rot, ein verschwundener Verlust ist genauso rot.
    - Test: Jedes Wort, ueber das dieses Modul eine Behauptung aufstellt, steht in der zugehoerigen `chain_cases_<lang>.txt`.
  </behavior>
  <action>
Erweitere `backend/tests/test_language_analyzers.py`. Englische Docstrings und Bezeichner, Akzente ausschliesslich in String-Literalen, keine Em-Dashes.

Den Fixture-Leser NICHT nachbauen. Lade `read_families` aus `scripts/dev/chain_probe.py` ueber `importlib.util.spec_from_file_location`, so wie `backend/tests/conftest.py` es fuer `corpus_generator` vorfuehrt (`conftest.py`, Zeilen 60-80). Ein Satz im Docstring der Ladefunktion begruendet die Richtung: `scripts/` ist kein Paket, der Import fuehrt nur vom Test zum Werkzeug, und ein zweiter Ausleger des Formats wuerde Messung und Abnahme auseinanderlaufen lassen.

Pfadkonstanten oben, im Muster von `test_analyzer.py:51-56`:
`FIXTURES = Path(__file__).resolve().parent / "fixtures"`, `CASES = {lang: FIXTURES / f"chain_cases_{lang}.txt" ...}`, `LOSSES = {...}`.

Eine Modul-Fixture je Sprache, die die Familien einmal liest, plus eine, die den Analysator einmal baut. Die Zuordnung Kuerzel zu Snowball-Name steht nicht doppelt im Test, sondern kommt aus `findling.config.SNOWBALL_NAME` (Plan 17-02).

`EXPECTED_FAMILY_SCORES = {"es": (174, 208), "it": (56, 56), "nl": (57, 81), "pt": (180, 228)}`, darueber der Kommentar, woher die Zahlen stammen (`docs/measurements/2026-09-analyseketten/rohdaten/kennzahlen.txt`, Lauf vom Datum X) und was zu tun ist, wenn eine faellt: erst `scripts/dev/measure_chains.sh` fahren und den Befund in den Messbericht schreiben, dann hier nachziehen, nie umgekehrt.

Der Verlustlisten-Test vergleicht zwei Mengen und meldet beide Richtungen getrennt, im Muster von `test_allowlist_parity.py:54-64`: `new losses that nobody measured: {...}` und `losses that disappeared, which means the chain moved: {...}`. Die Meldung nennt die Formpaare, nicht die Mengen.

Das Gate ueber dem Gate nach `test_analyzer.py:237-250`: sammle alle Woerter, ueber die dieses Modul eine Behauptung aufstellt (die Faelle aus Task 2, die Verlustlisten), und verlange, dass jedes in der Fixture seiner Sprache steht; die Fehlermeldung nennt die Datei und das Kommando `scripts/dev/measure_chains.sh`.
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/test_language_analyzers.py -q && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture</automated>
  </verify>
  <acceptance_criteria>
    - Der Formfamilien-Test behauptet Gleichheit mit `(174, 208)`, `(56, 56)`, `(57, 81)` und `(180, 228)`, nicht Ungleichungen.
    - `grep -c "def read_families" backend/tests/test_language_analyzers.py` ergibt 0: der Leser wird geladen, nicht nachgebaut.
    - `grep -c "SNOWBALL_NAME" backend/tests/test_language_analyzers.py` ist mindestens 1, und die Zuordnung Kuerzel zu Snowball-Name steht nicht als Literal im Test.
    - Der Verlustlisten-Test meldet beide Richtungen mit getrennten Saetzen und nennt die betroffenen Formpaare.
    - Das Gate ueber dem Gate existiert und nennt in seiner Fehlermeldung die Fixture-Datei und `scripts/dev/measure_chains.sh`.
    - `uv run python -m pytest tests/test_language_analyzers.py -q` ist gruen; ruff, ruff format, pyright und vulture aus `backend/` sind gruen.
  </acceptance_criteria>
  <done>Die gemessene Trefferzahl und die bekannten Verluste sind Gates, die in beide Richtungen zuschlagen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stoppwort-Dichtheit und die 15 Faelle der zusammengefuehrten Tabelle</name>
  <files>backend/tests/test_language_analyzers.py</files>
  <read_first>
    - `backend/tests/test_language_analyzers.py` (Stand aus Task 1)
    - `backend/tests/fixtures/snowball_stopwords_0_26_2.txt` (Kopfkommentar und Format)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 2.3 (Leck-Tabelle) und Abschnitt 3.4 (die 15 Faelle mit Ergebnis unter A+)
    - `backend/tests/test_analyzer.py`, Zeilen 203-235 (dokumentierte Grenzen als Doppelrichtungs-Behauptung)
  </read_first>
  <behavior>
    - Test: Kein Wort der eingebauten Snowball-Liste erzeugt unter der ausgelieferten Kette einen Term, weder in seiner Originalschreibweise noch in seiner gefalteten Form. Zielzahl 0 Lecks je Sprache und je Schreibweise.
    - Test: Faelle 1, 3, 5, 6, 13, 14 und 15 aus RESEARCH 3.4 liefern je gleichen Term fuer beide Schreibweisen.
    - Test: Faelle 7, 8, 9 liefern beidseitig die leere Tokenliste, ohne Muelltoken.
    - Test: Faelle 2 und 4 fallen auseinander, und zwar mit genau den gemessenen Termen. Das ist eine Behauptung, kein `xfail`.
  </behavior>
  <action>
Erweitere `backend/tests/test_language_analyzers.py` um zwei Bloecke.

**a) Dichtheitsgate, offline.** Lies `backend/tests/fixtures/snowball_stopwords_0_26_2.txt` in einer Modul-Fixture (Format `sprache<TAB>wort`, `#`-Zeilen ueberspringen). Fuer jede der vier Sprachen: jedes Wort und seine `ascii_fold`-Form durch `snowball_analyzer(name, FOLDED_STOPWORDS[name])` schicken und eine leere Tokenliste verlangen. Die Fehlermeldung nennt Sprache, Wort, Schreibweise (akzentuiert oder flach) und den erzeugten Term, nie nur eine Zahl. Zusaetzlich die Zahlenbehauptung aus dem Kopfkommentar der Fixture: 308 spanish, 279 italian, 203 portuguese, 101 dutch; eine abweichende Zeilenzahl heisst, dass die Fixture nicht mehr zum Tag gehoert, und die Meldung sagt das.

Die Faltung im Test wird mit `Filter.ascii_fold()` gefahren, nie mit `unicodedata`: eine nachgebaute Faltung prueft eine andere Kette als die ausgelieferte. Ein Satz im Docstring sagt das.

**b) Die 15 Faelle.** Eine Modultabelle `MERGED_CASES` mit je Zeile: Fallnummer, Sprache, die beiden Formen, Erwartung (`same`, `both_empty` oder `apart`) und bei `apart` die beiden erwarteten Terme. Die Quelle jeder Zeile (STACK, FEATURES, PITFALLS, LEX-01 oder "neu aus dieser Messung") steht als Kommentarspalte, damit ein Leser sieht, welche Recherche der Fall abnimmt.

Drei parametrisierte Tests, einer je Erwartungsart. Der `apart`-Test traegt den ausfuehrlichen Kommentar: diese zwei Faelle sind nicht reparierbar, ohne einen anderen Fall rot zu machen; der spanische Snowball-Algorithmus fuehrt die Endung mit Akzent in seiner Suffixliste und die Pluralendung ohne; sie stehen als bekannte Grenze in `docs/language-analyzers.md` und als Zeile in den Verlustlisten. Kein `pytest.mark.xfail`, weil ein `xfail` still gruen bleibt, wenn sich das Ergebnis aendert.

Die Faelle 10, 11 und 12 (alle akzentuierten Stoppwoerter es, pt, it) sind durch das Dichtheitsgate aus (a) abgedeckt; das gehoert als Kommentar an die Tabelle, damit niemand sie doppelt baut.
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/test_language_analyzers.py -q && uv run python -m pytest -q && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture</automated>
  </verify>
  <acceptance_criteria>
    - Das Dichtheitsgate prueft 891 Woerter in zwei Schreibweisen und verlangt je 0 Lecks; die Fehlermeldung nennt Sprache, Wort, Schreibweise und Term.
    - Die Zeilenzahlen 308, 279, 203 und 101 werden behauptet, mit einer Meldung, die auf den Tag der Fixture verweist.
    - `MERGED_CASES` hat 12 Eintraege (die Faelle 1 bis 9 und 13 bis 15); die Faelle 10 bis 12 sind per Kommentar auf das Dichtheitsgate verwiesen.
    - Die Faelle 2 und 4 werden als `apart` mit den exakt gemessenen Termen behauptet; `grep -c "xfail" backend/tests/test_language_analyzers.py` ergibt 0.
    - Der Test faltet mit `Filter.ascii_fold()`; `grep -c "unicodedata" backend/tests/test_language_analyzers.py` ergibt 0.
    - Die volle Suite `uv run python -m pytest -q` aus `backend/` ist gruen; ruff, ruff format, pyright und vulture sind gruen.
  </acceptance_criteria>
  <done>Die zusammengefuehrte Testfall-Tabelle ist ein Gate, und kein eingebautes Stoppwort leckt mehr in den Index.</done>
</task>

<task type="auto">
  <name>Task 3: docs/language-analyzers.md</name>
  <files>docs/language-analyzers.md</files>
  <read_first>
    - `docs/german-analyzer.md` (vollstaendig; Abschnittsfolge, Kettenabschnitt Zeilen 85-106, Grenzen-Abschnitt ab Zeile 256)
    - `docs/measurements/2026-09-analyseketten/README.md` (der Bericht aus Plan 17-05)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitte 2.4, 3.4 und 3.5
    - `backend/src/findling/index/analyzer.py` und `backend/src/findling/index/stopwords.py` (die ausgelieferte Kette und die Herkunft der Ergaenzungsliste)
  </read_first>
  <action>
Lege `docs/language-analyzers.md` an, im Aufbau eins zu eins nach `docs/german-analyzer.md`. Sprache: dieselbe wie im Vorbild. Keine Em-Dashes, keine Emojis.

Abschnitte:
1. Kopfsatz: welche Sprachen diese Datei beschreibt (es, it, nl, pt und, weil es dieselbe Fabrik ist, en), und dass Deutsch die begruendete Ausnahme in `docs/german-analyzer.md` bleibt.
2. "The supplement list": Herkunft (tantivy-Tag 0.26.2, `src/tokenizer/stop_word_filter/stopwords.rs`, BSD-3-Clause), Erzeugung (`scripts/dev/stopword_supplement.py`), Groesse je Sprache (77 es, 10 it, 0 nl, 30 pt, 0 en, zusammen 117), der SHA-256, und der Satz, warum dieser Hash keine Versionsmarke ist.
3. "Measured numbers": die Zahlen aus dem Messbericht mit Datum und Verweis auf `docs/measurements/2026-09-analyseketten/`, 65 Familien, 573 Paare, A+ 467 gegen C+ 463, sowie die Leckzahlen.
4. "The filter order": Textdiagramm `simple -> lowercase -> ascii_fold -> stopword(lang) -> custom_stopword(folded) -> remove_long(48) -> stemmer(lang)`, danach die Tabelle mit einer Zeile je Position und der Spalte "Why exactly here". Danach der Absatz, warum diese vier Ketten eine Faltung haben und warum vorn, im Gegenstueck zu dem Absatz im deutschen Dokument, der erklaert, warum die deutsche keine hat. Der Absatz nennt auch die Alternative (`fold spaet`) und den gemessenen Abstand, damit ein spaeterer Leser die Abwaegung nicht neu fuehren muss.
5. "The merged test table": die 15 Zeilen aus RESEARCH 3.4 mit Datum und dem Verweis auf den Test, der sie haelt (`backend/tests/test_language_analyzers.py`). Das ist das datierte Verdikt je Sprache, das Erfolgskriterium 2 verlangt.
6. "Known limits", im Formmuster des Vorbilds (fette Kennung, ein Absatz je Grenze, jede Grenze nennt die Alternative und warum sie teurer ist). Pflichteintraege: die Numerusklasse mit Akzentsuffix in es und pt; `año` gleich `ano` als bewusster Recall-Kauf; `ciudad`/`ciudades`, `joven`/`jóvenes`, `imagen`/`imágenes`, `examen`/`exámenes`; `huis`/`huizen`, `bedrijf`/`bedrijven`, `gemeente`/`gemeentes`; `café`/`cafés`; keine portugiesische Rechtschreibvereinheitlichung; Komposita nur de (und nl erst ab Phase 21); die fuenf Woerter, die in keiner Snowball-Liste stehen und deshalb Terme erzeugen (it `già`, `però`, `così`; es `aún`, `sólo`) samt der Begruendung, warum dafuer keine eigene Liste gepflegt wird.
7. "Licence and provenance": Snowball-Stoppwortlisten und -Stemmer sind in tantivy einkompiliert, BSD-3-Clause, AGPL-vertraeglich; es entsteht kein neues Paket und keine eigene Wortliste.

Owner-Regel vom 07.09.2026 beachten: dies ist `docs/` und darf ausfuehrlich sein; die Kurzfassung fuer HART-05 entsteht in Phase 23 und wird dem Owner vorgelegt. Dieser Satz gehoert als Fussnote in den Grenzen-Abschnitt.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search && test -f docs/language-analyzers.md && for s in "The supplement list" "Measured numbers" "The filter order" "Known limits" "Licence and provenance"; do grep -qF "$s" docs/language-analyzers.md || { echo "Abschnitt fehlt: $s"; exit 1; }; done && grep -qF "custom_stopword" docs/language-analyzers.md && grep -qF "2026-09-analyseketten" docs/language-analyzers.md && grep -qF "test_language_analyzers.py" docs/language-analyzers.md && test "$(grep -c . docs/language-analyzers.md)" -ge 120 && python -c "import sys,pathlib;t=pathlib.Path('docs/language-analyzers.md').read_text(encoding='utf-8');sys.exit(1 if (chr(8211) in t or chr(8212) in t) else 0)" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `docs/language-analyzers.md` existiert mit den sieben Abschnitten in der vorgegebenen Reihenfolge und mindestens 120 nicht leeren Zeilen.
    - Der Kettenabschnitt zeigt das Textdiagramm und eine Tabelle mit sechs Positionszeilen, jede mit einer Begruendung in "Why exactly here".
    - Die Testfall-Tabelle hat 15 Zeilen, traegt ein Datum und verweist auf `backend/tests/test_language_analyzers.py`.
    - Der Grenzen-Abschnitt enthaelt alle in `<action>` aufgezaehlten Pflichteintraege, jeder mit Alternative und Kostenbegruendung.
    - Der Hash der Ergaenzungsliste steht im Dokument, zusammen mit dem Satz, warum er keine Versionsmarke ist.
    - Keine Em-Dashes; die Fussnote zur Owner-Regel vom 07.09.2026 steht im Grenzen-Abschnitt.
  </acceptance_criteria>
  <done>Das Verdikt je Sprache steht datiert in der Produktdokumentation, samt Preis und Herkunft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture zu Abnahme | Eine Fixture, die nicht mehr zum tantivy-Tag gehoert, macht ein Gate gruen, das nichts mehr prueft |
| Analysekette zu Suchergebnis | Ein leckendes Stoppwort ist ein stiller Qualitaetsverlust ohne Fehlermeldung |
| Dokumentation zu Nutzererwartung | Eine nicht dokumentierte Grenze wird als Fehler gemeldet und kostet Supportzeit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-22 | Tampering | Stiller Recall-Verlust durch verschobene Filterposition | mitigate | Formfamilien-Test mit exakten Zahlen (174/208, 56/56, 57/81, 180/228) plus AST-Waechter aus Plan 17-03; ein Gewinn ist genauso rot wie ein Verlust |
| T-17-23 | Tampering | Verlustliste driftet unbemerkt | mitigate | Doppelrichtungs-Vergleich der Verlustmengen; ein verschwundener Verlust meldet "the chain moved" |
| T-17-24 | Tampering | Stoppwort-Fixture passt nicht mehr zum tantivy-Tag | mitigate | Zeilenzahlbehauptung 308/279/203/101 mit einer Meldung, die auf den Tag verweist |
| T-17-25 | Information Disclosure | , | accept | Die Tests lesen ausschliesslich Repo-Fixtures und veroeffentlichte Snowball-Woerter; kein Pfad beruehrt Nutzerinhalte |
| T-17-26 | Denial of Service | Bestandsinstallationen | mitigate | Dieser Plan aendert keinen Produktionspfad; die volle Suite inklusive `test_upgrade_compatibility.py` bleibt gruen |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket und fasst `backend/uv.lock` nicht an |
</threat_model>

<verification>
- `uv run python -m pytest -q` aus `backend/` ist gruen; `tests/test_language_analyzers.py` allein meldet mindestens 20 bestandene Tests.
- `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture` aus `backend/` sind gruen.
- `uv run python -m pytest tests/test_upgrade_compatibility.py tests/test_lockstep_versions.py tests/test_search_fields_lockstep.py tests/test_index_open.py -q` ist gruen: Erfolgskriterium 4 haelt.
- `git diff --quiet backend/src/`: kein Produktionspfad ist angefasst.
</verification>

<success_criteria>
Erfolgskriterium 2 ist belegt: die zusammengefuehrte Testfall-Tabelle ist je Sprache gruen oder als dokumentierter Verlust festgeschrieben, kein eingebautes Stoppwort leckt, und das Verdikt steht datiert in `docs/language-analyzers.md`.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-06-SUMMARY.md` when done
</output>
