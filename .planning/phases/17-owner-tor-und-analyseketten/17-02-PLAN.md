---
phase: 17-owner-tor-und-analyseketten
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/findling/config.py
  - backend/tests/test_language_allowlist.py
autonomous: true
requirements: [LEX-07]

must_haves:
  truths:
    - "Ein Sprachname, den tantivy nicht in beiden Haelften (Stemmer UND eingebaute Stoppwortliste) bedient, kann die Kettenfabrik nie erreichen"
    - "Die Positivliste und das laufende tantivy widersprechen sich nicht, und zwar in beide Richtungen geprueft"
    - "Die sechs Produktsprachen sind eine echte Teilmenge der Positivliste"
    - "Eine Bestandsinstallation verhaelt sich unveraendert: keine Marke bewegt, kein Schemafeld, `_languages()` und `DEFAULT_LANGUAGES` unangetastet"
  artifacts:
    - path: "backend/src/findling/config.py"
      provides: "LANGUAGE_ALLOWLIST, SUPPORTED_LANGUAGES, SNOWBALL_NAME"
      contains: "LANGUAGE_ALLOWLIST"
    - path: "backend/tests/test_language_allowlist.py"
      provides: "Doppelrichtungs-Paritaetsgate gegen das laufende tantivy, mit gestellter Mutation"
      min_lines: 80
  key_links:
    - from: "backend/tests/test_language_allowlist.py"
      to: "tantivy.Filter.stopword / Filter.stemmer"
      via: "Aufbau bis zum build() je Sprache der Positivliste"
      pattern: "\\.build\\(\\)"
    - from: "backend/tests/test_language_allowlist.py"
      to: "backend/src/findling/config.py"
      via: "Import der Konstanten, keine zweite Liste im Test"
      pattern: "from findling.config import"
---

<objective>
Die geschlossene Sprach-Positivliste entsteht in `config.py`, zusammen mit dem Paritaetsgate, das sie gegen das laufende tantivy in beide Richtungen prueft.

Purpose: Erfolgskriterium 3 der Phase. Ein unbekannter Sprachname darf den Container nie per Rust-Panic zu Fall bringen. Die Panic ist auf dem heutigen Pfad noch gar nicht erreichbar (`_languages()` filtert gegen `DEFAULT_LANGUAGES`); die Liste ist die Voraussetzung dafuer, dass Phase 18 die Kettenfabrik ueberhaupt an die Konfiguration haengen darf. Die Arbeit beruehrt D-04 nicht und laeuft deshalb vor dem Owner-Tor (RESEARCH 6.3).
Output: `LANGUAGE_ALLOWLIST`, `SUPPORTED_LANGUAGES`, `SNOWBALL_NAME` in `config.py` und `backend/tests/test_language_allowlist.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@CLAUDE.md
@docs/testing.md
</context>

<interfaces>
Was der Executor ohne Suche braucht. Gelesen am 2026-09-23.

Aus `backend/src/findling/config.py`:
- Zeile 80: `DEFAULT_LANGUAGES = ("de", "en")` mit dem Kommentar "Field order of the schema, not the order somebody types into the environment."
- Zeile 292: `OCR_LANGUAGE_ALLOWLIST = frozenset({...})`, davor der Begruendungskommentar Zeilen 265-291
- Zeile 268: `OCR_DEFAULT_LANGUAGES = ("deu", "eng", "fra")`
- Zeilen 1002-1015: `def _languages() -> tuple[str, ...]` , filtert gegen `DEFAULT_LANGUAGES`, normalisiert auf Schemafeldreihenfolge, faellt auf `DEFAULT_LANGUAGES` zurueck
- Zeilen 1028-1064: `def _ocr_languages() -> tuple[str, ...]` , das Lesermuster mit `kept`/`dropped` und zwei getrennten Warnungen

Aus `backend/tests/test_ocr_languages.py`:
- Zeilen 94-122: `test_every_offered_language_has_a_pinned_apt_line` , beide Richtungen plus gestellte Mutation, mit dem Satz "A gate whose red state is never produced is a gate nobody has tested."
- Zeilen 125-139: `test_the_default_stays_at_three_and_is_a_true_subset_of_the_offer`

Aus tantivy 0.26.0 (gemessen):
- `Filter.stemmer(name)` kennt 18 Sprachen, `Filter.stopword(name)` kennt 13
- Die Differenz ist genau `arabic, greek, romanian, tamil, turkish`
- Der Fehler tritt beim `build()` des `TextAnalyzerBuilder` auf, nicht bei der Filterkonstruktion
- Die 13er-Schnittmenge: danish, dutch, english, finnish, french, german, hungarian, italian, norwegian, portuguese, russian, spanish, swedish
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Die drei Konstanten in config.py</name>
  <files>backend/src/findling/config.py</files>
  <read_first>
    - `backend/src/findling/config.py`, Zeilen 70-100 (`DEFAULT_LANGUAGES` und Nachbarschaft), Zeilen 250-300 (`OCR_DEFAULT_LANGUAGES`, `OCR_LANGUAGE_ALLOWLIST` samt Begruendungskommentar), Zeilen 995-1070 (`_languages`, `_ocr_languages`)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 5 (Panic-Klasse, Entwurf der Konstante, "Was die Positivliste nicht loest")
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitt "`backend/src/findling/config.py` (config)"
  </read_first>
  <behavior>
    - `LANGUAGE_ALLOWLIST` ist ein `frozenset` mit genau 13 Eintraegen: danish, dutch, english, finnish, french, german, hungarian, italian, norwegian, portuguese, russian, spanish, swedish
    - `SUPPORTED_LANGUAGES` ist das Tupel `("de", "en", "es", "it", "nl", "pt")` in Schemafeldreihenfolge
    - `SNOWBALL_NAME` bildet jeden Eintrag von `SUPPORTED_LANGUAGES` auf einen Eintrag von `LANGUAGE_ALLOWLIST` ab
    - `DEFAULT_LANGUAGES` bleibt `("de", "en")` und `_languages()` bleibt Zeile fuer Zeile unveraendert
  </behavior>
  <action>
Ergaenze `backend/src/findling/config.py` um genau drei neue Konstanten. Platzierung: unmittelbar hinter `DEFAULT_LANGUAGES` (Zeile 80), damit ein Leser die Werkseinstellung und die groessere Menge nebeneinander sieht.

`LANGUAGE_ALLOWLIST` als `frozenset` mit den 13 Namen oben. Der Kommentar darueber folgt dem Muster von `OCR_LANGUAGE_ALLOWLIST` (`config.py:265-291`): er sagt nicht, was der Wert ist, sondern warum er dort steht und was passiert, wenn jemand ihn allein aendert. Inhalt in englischer Prosa: die Menge ist die Schnittmenge der beiden Mengen, die tantivy bedient (18 Sprachen mit Stemmer, 13 mit eingebauter Stoppwortliste); die fuenf in der Differenz sind arabic, greek, romanian, tamil und turkish; sie brachten in tantivy 0.26.0 die Rust-Seite per Panic zu Fall und werfen in 0.26.2 einen ValueError; beides ist keine annehmbare Antwort auf einen Tippfehler in einer Umgebungsvariablen, also wird keines von beidem je erreicht; die Gegenstelle ist das laufende tantivy und nicht eine zweite Liste, und `backend/tests/test_language_allowlist.py` vergleicht beide Seiten in beide Richtungen; gemessen am 2026-09-23.

`SUPPORTED_LANGUAGES = ("de", "en", "es", "it", "nl", "pt")`. Der Kommentar muss in einem Satz den Unterschied zu `DEFAULT_LANGUAGES` sagen: `DEFAULT_LANGUAGES` ist die Werkseinstellung, `SUPPORTED_LANGUAGES` ist die Menge, fuer die dieser Build ueberhaupt ein Koerperfeld tragen kann. Die Reihenfolge ist Schemafeldreihenfolge, nicht die Reihenfolge, die jemand in die Umgebung tippt, weil eine umsortierte Umgebungsvariable sonst spaeter (ab Phase 18, E-17-4) eine Versionsmarke bewegen wuerde.

`SNOWBALL_NAME` als `dict[str, str]`: de->german, en->english, es->spanish, it->italian, nl->dutch, pt->portuguese. Kommentar: die Abbildung ist der einzige Ort, an dem aus einem Feldkuerzel ein tantivy-Sprachname wird; ein Eintrag hier ohne Eintrag in `LANGUAGE_ALLOWLIST` ist eine wartende Panic, und der Paritaetstest faellt darauf rot.

Aenderungen, die AUSDRUECKLICH NICHT stattfinden, weil sie Erfolgskriterium 4 treffen wuerden: `DEFAULT_LANGUAGES` bleibt `("de", "en")`; `_languages()` (Zeilen 1002-1015) wird nicht angefasst, es filtert weiter gegen `DEFAULT_LANGUAGES`; `Settings.languages` (`config.py:745`, befuellt Zeile 1116) bleibt unveraendert. Die drei neuen Konstanten werden in dieser Phase von keinem Produktionspfad gelesen; das ist Absicht und gehoert als Satz in den Kommentar von `SUPPORTED_LANGUAGES` (die Fabrik haengt erst in Phase 18 daran).

Gates aus `backend/`: `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture`. `vulture` liest `src` und `tests`, die Konstanten gelten also durch den Test aus Task 2 als benutzt; bis dahin darf `vulture` rot sein, am Planende nicht mehr.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from findling.config import LANGUAGE_ALLOWLIST, SUPPORTED_LANGUAGES, SNOWBALL_NAME, DEFAULT_LANGUAGES; assert len(LANGUAGE_ALLOWLIST)==13, LANGUAGE_ALLOWLIST; assert SUPPORTED_LANGUAGES==('de','en','es','it','nl','pt'); assert DEFAULT_LANGUAGES==('de','en'); assert set(SNOWBALL_NAME)==set(SUPPORTED_LANGUAGES); assert set(SNOWBALL_NAME.values()) <= set(LANGUAGE_ALLOWLIST); print('GRUEN')" && uv run ruff check && uv run ruff format --check && uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `from findling.config import LANGUAGE_ALLOWLIST, SUPPORTED_LANGUAGES, SNOWBALL_NAME` gelingt.
    - `len(LANGUAGE_ALLOWLIST) == 13`; die Menge enthaelt keinen der fuenf Namen arabic, greek, romanian, tamil, turkish.
    - `SUPPORTED_LANGUAGES == ("de", "en", "es", "it", "nl", "pt")` und `DEFAULT_LANGUAGES == ("de", "en")`.
    - `set(SNOWBALL_NAME.values())` ist eine echte Teilmenge von `LANGUAGE_ALLOWLIST` (6 von 13).
    - `git diff backend/src/findling/config.py` zeigt ausschliesslich Zeilen hinzugefuegt, keine geloeschte oder geaenderte Zeile in `_languages()` und keine an `DEFAULT_LANGUAGES`.
    - Der Kommentar ueber `LANGUAGE_ALLOWLIST` nennt die Zahlen 18 und 13, die fuenf Namen der Differenz, das Messdatum 2026-09-23 und die Gegenstelle `backend/tests/test_language_allowlist.py`.
    - `uv run ruff check`, `uv run ruff format --check` und `uv run pyright` aus `backend/` sind gruen.
  </acceptance_criteria>
  <done>Die drei Konstanten stehen in `config.py`, von keinem Produktionspfad gelesen, mit begruendendem Kommentar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Das Doppelrichtungs-Paritaetsgate gegen das laufende tantivy</name>
  <files>backend/tests/test_language_allowlist.py</files>
  <read_first>
    - `backend/tests/test_ocr_languages.py` (vollstaendig; Zeilen 15-19 "die Gegenstelle wird gelesen", Zeilen 94-122 Doppelrichtung plus gestellte Mutation, Zeilen 125-139 die Zahlenbehauptung)
    - `backend/tests/test_allowlist_parity.py`, Zeilen 25-90 (Muster fuer Fehlermeldungen: Name UND Seite, nie zwei Mengen zum Vergleichen)
    - `backend/src/findling/config.py` (der Stand aus Task 1)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 5.1 und 5.2
  </read_first>
  <behavior>
    - Test 1: Jeder Name in `LANGUAGE_ALLOWLIST` laesst sich mit `Filter.stopword(name)` UND `Filter.stemmer(name)` bis zum `build()` durchbauen. Ein Name, bei dem das nicht gelingt, ist eine falsche Zusage und wird einzeln genannt.
    - Test 2: Jeder Wert von `SNOWBALL_NAME` steht in `LANGUAGE_ALLOWLIST`. Ein fehlender ist eine wartende Panic und wird mit Kuerzel und Snowball-Namen genannt.
    - Test 3: Die gestellte Mutation. Fuer einen der fuenf Differenznamen (`romanian`) gilt: `Filter.stemmer("romanian")` baut durch, der Aufbau mit `Filter.stopword("romanian")` nicht. Damit beweist das Gate seinen eigenen roten Zustand.
    - Test 4: Die Zahlenbehauptung. `len(LANGUAGE_ALLOWLIST) == 13`, `len(SUPPORTED_LANGUAGES) == 6`, und die Bildmenge von `SNOWBALL_NAME` ist eine ECHTE Teilmenge der Positivliste.
  </behavior>
  <action>
Lege `backend/tests/test_language_allowlist.py` an. Englische Docstrings und Bezeichner, deutsche Prosa gibt es hier nicht. Keine Em-Dashes.

Modulkopf nach dem Vorbild von `test_ocr_languages.py`: er sagt, warum die Gegenstelle das laufende tantivy ist und nicht eine zweite Liste im Test ("a test that repeats the set it guards agrees with itself on the very day somebody adds a fourteenth language, which is the only day it matters"), und er nennt den gemessenen Befund, dass der Fehler erst beim `build()` auftritt und ein Test, der nur `Filter.stopword(...)` konstruiert, gruen ist und nichts beweist.

Eine Hilfsfunktion `builds(language: str, *, with_stopword: bool, with_stemmer: bool) -> bool`: baut einen `TextAnalyzerBuilder(Tokenizer.simple())`, haengt die verlangten Filter an und ruft `build()`. Faengt genau `ValueError` und die Panic-Klasse ab und gibt False zurueck; alles andere laeuft durch. Die Panic aus tantivy 0.26.0 ist eine `BaseException`-Unterklasse namens `PanicException` und keine `Exception`; fange sie ueber `except BaseException` mit anschliessender Namenspruefung ab oder ueber `pytest.raises`-freie Breitfassung, und begruende im Docstring, warum hier ausnahmsweise breit gefangen wird (in 0.26.0 ist der Fehler keine `Exception`). `ruff` verlangt dafuer eine `# noqa`-Begruendung; schreibe sie mit dem Regelcode und einem Satz, nicht nackt.

Die vier Tests aus `<behavior>`, Namen im Stil des Baums, also ganze Saetze:
`test_every_offered_language_is_carried_by_the_running_tantivy`,
`test_every_product_language_stands_in_the_allowlist`,
`test_the_guard_sees_a_language_with_a_stemmer_but_no_stop_word_list`,
`test_the_thirteen_hold_and_the_six_are_a_true_subset`.

Die Fehlermeldungen nennen den Namen UND die Seite, auf der er fehlt, nach dem Muster von `test_allowlist_parity.py:54-64`. Beispiel fuer Test 2: `f"{code} maps to {name}, and {name} is not in LANGUAGE_ALLOWLIST: that is a panic waiting for phase 18"`.

Test 3 bekommt den Kommentar aus dem Vorbild sinngemaess: "A gate whose red state is never produced is a gate nobody has tested." Der Name `romanian` steht als Modulkonstante `MUTATED_LANGUAGE = "romanian"` oben, mit dem Satz, warum gerade dieser (Stemmer ja, Stoppwortliste nein, gemessen 2026-09-23).

Wichtig: Test 3 muss unter beiden tantivy-Fassungen halten. Unter 0.26.0 ist der Fehler eine Panic, unter 0.26.2 ein `ValueError`; `builds()` gibt in beiden Faellen False zurueck, der Test behauptet also nur False und nie einen Ausnahmetyp.

Gates aus `backend/`: `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture`, `uv run python -m pytest -q`.
  </action>
  <verify>
    <automated>cd backend && uv run python -m pytest tests/test_language_allowlist.py -q && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture && uv run python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_language_allowlist.py -q` meldet 4 bestandene Tests.
    - Die Datei importiert `LANGUAGE_ALLOWLIST`, `SUPPORTED_LANGUAGES` und `SNOWBALL_NAME` aus `findling.config` und schreibt keine der drei Mengen ein zweites Mal hin (`grep -c` auf ein Sprachwort wie `"finnish"` ergibt hoechstens 1, und diese eine Stelle liegt in einer Fehlermeldung oder einem Kommentar, nicht in einem Mengenliteral).
    - Der Mutationstest prueft `builds(MUTATED_LANGUAGE, with_stemmer=True, with_stopword=False) is True` und `builds(MUTATED_LANGUAGE, with_stemmer=True, with_stopword=True) is False`.
    - Alle vier Gates aus `backend/` (ruff, ruff format, pyright, vulture) sind gruen, und die volle Suite `uv run python -m pytest -q` ist gruen.
    - Keine Datei ausser `config.py` und `test_language_allowlist.py` ist geaendert: `git status --porcelain` listet genau diese zwei Pfade.
  </acceptance_criteria>
  <done>Die Positivliste ist gegen das laufende tantivy in beide Richtungen abgesichert und beweist ihren eigenen roten Zustand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Umgebungsvariable zu FFI | `FINDLING_LANGUAGES` ist Admin-Eingabe; ab Phase 18 erreicht der Wert `Filter.stopword` und damit die Rust-Seite |
| Positivliste zu tantivy | Die Konstante behauptet etwas ueber eine fremde Bibliothek; eine falsche Zusage ist ein Startabbruch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Denial of Service | `Filter.stopword` ueber `FINDLING_LANGUAGES` | mitigate | Geschlossenes `frozenset` `LANGUAGE_ALLOWLIST` als Schnittmenge beider von tantivy bedienten Mengen (ASVS V5, Muster T-03-502 von `OCR_LANGUAGE_ALLOWLIST`) |
| T-17-06 | Tampering | `LANGUAGE_ALLOWLIST` driftet gegen tantivy | mitigate | Doppelrichtungs-Paritaetstest gegen das laufende tantivy bis zum `build()`, plus gestellte Mutation mit `romanian` |
| T-17-07 | Spoofing | , | accept | Kein Authentifizierungspfad beruehrt; die Konstante ist Bauzeit-Wissen ohne Netzbezug |
| T-17-08 | Denial of Service | Bestandsinstallationen | mitigate | `DEFAULT_LANGUAGES`, `_languages()` und `Settings.languages` bleiben unveraendert; Abnahmekriterium prueft den Diff auf reine Ergaenzung |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket; RESEARCH `## Package Legitimacy Audit` fuehrt nur `tantivy` als Bestandsabhaengigkeit mit Disposition Approved |
</threat_model>

<verification>
- `uv run python -m pytest -q` aus `backend/` ist gruen (volle Suite, nicht nur die neue Datei).
- `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture` aus `backend/` sind gruen.
- `git status --porcelain` listet genau `backend/src/findling/config.py` und `backend/tests/test_language_allowlist.py`.
- Keine der fuenf Versionsmarken wurde beruehrt: `uv run python -m pytest tests/test_upgrade_compatibility.py tests/test_lockstep_versions.py -q` ist gruen.
</verification>

<success_criteria>
Die Positivliste existiert, ist gegen tantivy in beide Richtungen bewiesen, und eine Bestandsinstallation sieht davon nichts.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-02-SUMMARY.md` when done
</output>
