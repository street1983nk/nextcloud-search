---
phase: 17-owner-tor-und-analyseketten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md
autonomous: true
requirements: [LEX-01, LEX-07]

must_haves:
  truths:
    - "Der Owner kann am Vorlagetag jeden der acht Entscheide lesen und einen Zweig waehlen, ohne dass ein Satz neu entworfen werden muss"
    - "Ein Planpruefer kann an einer geschriebenen Definition ablesen, ob ein Plan D-04 beruehrt oder nicht"
    - "Die sieben Stellen, die der tantivy-Pin-Sprung rot macht, stehen mit Ist- und Zielzustand im Dokument"
    - "Folgeplaene koennen eine Optionskennung zitieren (E-17-7 Option a) statt einer Zusammenfassung"
  artifacts:
    - path: ".planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md"
      provides: "Owner-Tor mit acht ausformulierten Entscheiden, Leitplanke und fertigen Ersatztexten"
      min_lines: 200
      contains: "E-17-7"
  key_links:
    - from: ".planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md"
      to: "backend/tests/test_upgrade_compatibility.py"
      via: "Ist/Soll-Tabelle plus fertiger Ersatztext fuer GOLD_V1_0_AND_V1_1"
      pattern: "test_upgrade_compatibility"
    - from: ".planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md"
      to: ".github/workflows/deploy-harp.yml"
      via: "Zusicherung 2 des Jobs Store upgrade 5"
      pattern: "deploy-harp"
---

<objective>
Das Owner-Tor der Phase entsteht als vollstaendig ausformuliertes Entscheiddokument: acht Entscheide E-17-1 bis E-17-8, je mit Feststellung, Optionen und fertigem Ersatztext fuer jede betroffene Datei.

Purpose: Erfolgskriterium 1 der Phase verlangt einen datierten Owner-Entscheid, bevor Code geschrieben wird, der D-04 beruehrt. Das Muster aus Phase 12 (12-STABLE35-ENTSCHEID.md) verlangt, dass am Vorlagetag nur noch gelesen wird, welcher Zweig greift. Dieser Plan erzeugt das Dokument; Plan 17-04 legt es vor.
Output: `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@.planning/milestones/v1.2-phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Geruest, Feststellungen und die Leitplanke "D-04 beruehren"</name>
  <files>.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md</files>
  <read_first>
    - `.planning/milestones/v1.2-phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md` (vollstaendig; das ist die Formvorlage: Kopfmuster Zeilen 1-9, Ist/Soll-Tabelle Zeilen 34-40, Vollzugsmuster Zeilen 233-281, Checkliste Zeilen 282-299)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitte 4.1, 6.1, 6.2, 6.3 und 7
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitt "`17-GRUNDSATZ-ENTSCHEID.md` (planning, Owner-Tor)"
    - `.planning/ROADMAP.md`, Abschnitt "Phase 17" (die vier Erfolgskriterien woertlich)
  </read_first>
  <action>
Lege `17-GRUNDSATZ-ENTSCHEID.md` an. Deutsche Prosa mit echten Umlauten, keine Em-Dashes, keine Emojis.

Kopfblock nach dem Muster von `12-STABLE35-ENTSCHEID.md`: Titel `# Phase 17: Grundsatzentscheide des Milestones v1.3 (LEX-01, LEX-07)`, dann `**Angelegt:** 2026-09-23 (Plan 17-01, Task 1)` und ein `**Zweck:**`-Absatz, der sinngemaess sagt: alle acht Entscheide werden VOR der Vorlage vollstaendig ausformuliert, inklusive der fertigen Ersatztexte fuer die betroffenen Dateien; am Vorlagetag wird nur gelesen, welcher Zweig greift, und der zugehoerige Text eingesetzt; Folgeplaene zitieren die Optionskennung (Beispiel `E-17-7 Option a`) und nie eine Zusammenfassung.

Abschnittsfolge, exakt in dieser Reihenfolge anlegen (Inhalt der Entscheide kommt in Task 2, der Ersatztexte in Task 3):
1. `## Die Frage` , ein Satz: Duerfen die sechs Grundsatzentscheide des Milestones v1.3 so fallen, wie die Recherche sie empfiehlt, und darf die Vergleichsregel fuer `tantivy_version` gelockert werden.
2. `## Was festgestellt ist`
3. `## Was unklar ist`
4. `## Die Leitplanke, die fuer alle Optionen gilt`
5. `## Die acht Entscheide` (in Task 2 gefuellt)
6. `## Die fertigen Ersatztexte` (in Task 3 gefuellt)
7. `## Vollzug am <Datum>` (in Task 3 leer angelegt)
8. `### Vollzugs-Checkliste` (in Task 3 gefuellt)

In `## Was festgestellt ist` gehoeren, jeweils mit gelesener Datei und Zeilennummer plus Lesedatum 2026-09-23:
- `backend/src/findling/index/open.py:61-67` speichert `TANTIVY_VERSION = tantivy.__version__`, also den vollen Banner `"tantivy v0.26.0, index_format v7"`, und `open.py:123-141` schreibt ihn als Marke `tantivy_version`.
- `backend/src/findling/store/repo.py:631-669` (`Store.version_mismatch`) vergleicht auf Gleichheit; die einzige heutige Ausnahme ist `index_version` ueber `_generation_at_least` (`repo.py:1299`).
- Gemessen am 2026-09-23: `index_format v7` in 0.26.0 UND 0.26.2; ein mit 0.26.0 geschriebener Index ist mit 0.26.2 les- und durchsuchbar und umgekehrt; die Tokenisierung von sieben Ketten ueber 32 Woerter (224 Zeilen) ist zwischen beiden Fassungen `diff`-gleich.
- Die Panic-Klasse ist genau `arabic, greek, romanian, tamil, turkish` (Stemmer vorhanden, eingebaute Stoppwortliste fehlt); der Fehler tritt beim `build()` auf, nicht bei der Filterkonstruktion.
- Die Kettenmessung: 65 Formfamilien, 573 geordnete Paare, `fold frueh` (A+) 467, `fold spaet` (C+) 463; Stoppwort-Lecks ohne gefaltete Ergaenzungsliste 77 es / 10 it / 0 nl / 30 pt.
- `.github/dependabot.yml` traegt den `ignore`-Eintrag fuer tantivy BEREITS (Dateiende, mit Owner-Datum 2026-09-21). Seine Begruendung "tantivy carries index format v7 from 0.26.2 on, which means a reindex" ist durch die eigene Messung widerlegt und muss mit dem Entscheid berichtigt werden. Ausdruecklich festhalten, dass RESEARCH 4.1 und 4.4 an dieser Stelle irren.
- Der "Union-Scorer-Bugfix in 0.26.2" ist in den Release Notes nicht auffindbar und wird aus jeder Begruendung gestrichen.

Danach die Ist/Soll-Tabelle der sieben Stellen aus RESEARCH 4.1, Spalten `Ort | Ist-Zustand am 23.09.2026 | Zielzustand bei E-17-7 Option a (lockern) | Zielzustand bei Option b (Pin bleibt)`. Zeilen: `backend/tests/test_upgrade_compatibility.py` GOLD_V1_0_AND_V1_1["tantivy_version"]; dieselbe Datei TANTIVY_PIN; dieselbe Datei GOLD_INDEX_FORMAT (bleibt gruen); `.github/workflows/deploy-harp.yml` Job "Store upgrade 5" Zusicherung 2 (Zeilen 3288-3291); dieselbe Stelle Zusicherung 4 (kein Reindex-Banner); dieselbe Stelle Zusicherung 5 (keine Zeile "built by different code"); `.github/dependabot.yml` Begruendungskommentar.

In `## Was unklar ist`: allein die Risikoneigung des Owners gegenueber einer gelockerten Marke (E-17-7) und der Widerspruch gegen die einheitliche Kettenreihenfolge (E-17-8). Alles andere ist gemessen.

In `## Die Leitplanke, die fuer alle Optionen gilt` steht die scharfe Definition, ohne die das Tor die ganze Phase blockiert (RESEARCH Pitfall 8):
"D-04 beruehren heisst genau dreierlei: eine der fuenf Versionsmarken (`schema_version`, `index_version`, `analyzer_version`, `wordlist_hash`, `tantivy_version`) bewegen, ein Schemafeld anlegen, oder die Query-Feldliste erweitern. Nicht mehr und nicht weniger."
Direkt darunter die Tabelle aus RESEARCH 6.3 woertlich, Spalten `Arbeit | Vor dem Tor? | Grund`, mit allen sieben Zeilen (Kettenmessung; `docs/language-analyzers.md`; `LANGUAGE_ALLOWLIST` plus Paritaetstest; die vier Analyse-Funktionen angelegt und nirgends registriert; tantivy-Pin bewegen = nein; `english_analyzer()` in die gemeinsame Fabrik nur mit dem No-op-Beweis). Ergaenze eine achte Zeile: "Hash der gefalteten Ergaenzungsliste | ja, aber NUR als Testwert | ein Hash in `expected_versions()` waere eine sechste Marke und damit ein Reindex auf jeder Bestandsinstallation".
  </action>
  <verify>
    <automated>cd .planning/phases/17-owner-tor-und-analyseketten && test -f 17-GRUNDSATZ-ENTSCHEID.md && for s in "## Die Frage" "## Was festgestellt ist" "## Was unklar ist" "## Die Leitplanke" "## Die acht Entscheide"; do grep -qF "$s" 17-GRUNDSATZ-ENTSCHEID.md || { echo "FEHLT: $s"; exit 1; }; done && grep -qF "test_upgrade_compatibility.py" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "deploy-harp.yml" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "dependabot.yml" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "index_format v7" 17-GRUNDSATZ-ENTSCHEID.md && python -c "import sys,pathlib;t=pathlib.Path('17-GRUNDSATZ-ENTSCHEID.md').read_text(encoding='utf-8');sys.exit(1 if (chr(8211) in t or chr(8212) in t) else 0)" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Datei `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md` existiert und traegt die acht Ueberschriften der Abschnittsfolge in der vorgegebenen Reihenfolge.
    - Der Satz mit der Definition enthaelt woertlich alle fuenf Markennamen `schema_version`, `index_version`, `analyzer_version`, `wordlist_hash`, `tantivy_version`.
    - Die Ist/Soll-Tabelle hat genau sieben Datenzeilen und nennt in jeder Zeile eine Datei aus dem Baum.
    - Die Tabelle "was vor dem Tor laufen darf" hat acht Datenzeilen; die Zeile zum tantivy-Pin steht auf "nein".
    - Der Text stellt ausdruecklich fest, dass der `ignore`-Eintrag in `.github/dependabot.yml` bereits existiert und sein Begruendungskommentar sachlich falsch ist.
    - Keine Em-Dashes (U+2013, U+2014) in der Datei; deutsche Prosa mit echten Umlauten.
  </acceptance_criteria>
  <done>Geruest, Feststellungen und Leitplanke stehen; das Dokument ist zitierfaehig, aber noch ohne Entscheide.</done>
</task>

<task type="auto">
  <name>Task 2: Die acht Entscheide E-17-1 bis E-17-8 ausformulieren</name>
  <files>.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md</files>
  <read_first>
    - `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md` (der Stand aus Task 1)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 6.2 (Tabelle der acht Entscheide mit Empfehlung und Beleg), Abschnitt 2.4 (Verdikt je Sprache), Abschnitt 4.2 (Belege fuer E-17-7), Abschnitt 9 Pitfall 3 (Auflage zu E-17-4)
    - `.planning/milestones/v1.2-phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md`, die Optionsbloecke (Felder "Greift, wenn", "Beweisgrundlage", "Vollzug")
  </read_first>
  <action>
Fuelle `## Die acht Entscheide`. Je Entscheid ein `### E-17-N: <Frage in einem Satz>` mit dieser festen Feldfolge:
`**Empfehlung:**` , `**Beleg:**` (Datei plus Zeilennummer oder Messzahl, nie eine Meinung) , `#### Option a: <Name>` und `#### Option b: <Name>`, jede Option mit den drei Feldern `Greift, wenn`, `Beweisgrundlage`, `Vollzug` (welche Plaene der Phase 17 oder 18 den Zweig ausfuehren).

Inhalte, aus RESEARCH 6.2 uebernommen und ausformuliert:
- E-17-1 Umbauweg: Re-Analyse aus den gespeicherten Feldern (Option a, Empfehlung) gegen Vollreindex ueber Nextcloud (Option b). Beleg: gemessene 19 h 20 min fuer 52.137 Dokumente gegen geschaetzte 1 bis 3 h; `vectors.db` und `state.db` bleiben bei a unberuehrt; MESS-08 misst nach.
- E-17-2 Feldmodell: sechs Koerperfelder immer im Schema, Befuellung nach `FINDLING_LANGUAGES` (Option a, Empfehlung, Modell A) gegen Index je Sprache (Option b). Beleg: leere Felder kosten gemessen null Byte.
- E-17-3 Darf `FINDLING_LANGUAGES` Deutsch und Englisch abschalten: ja, erlauben (Option a, Empfehlung) gegen `de` und `en` als erzwungener Boden (Option b). Beleg: `config.py:_languages()` erlaubt heute schon `FINDLING_LANGUAGES=de`; Werkseinstellung bleibt `DEFAULT_LANGUAGES = ("de", "en")`.
- E-17-4 Sprachmenge als sechster Versionsmerker in `expected_versions()`: ja, als nach Schemafeldreihenfolge normalisierte Zeichenkette (Option a, Empfehlung) gegen kein Merker (Option b). Die Auflage aus Pitfall 3 steht als eigener fetter Absatz unter Option a: eine Marke, die auf keiner Bestandsinstallation existiert, liest `Store.version_mismatch` als Abweichung ("A mark that was never written counts as diverging", `repo.py:637`) und loest damit selbst den Reindex aus, den sie verhindern soll; sie muss also gesaet werden (Muster `_DEFAULT_META`) oder ihr Fehlen als `"de,en"` gelesen werden. Ausfuehrung in Phase 18.
- E-17-5 Katalogprozess: maschinell plus Community-Review mit datiertem Vorbehalt, ohne Muttersprachler-Gate (Option a, Empfehlung, FR-Muster aus `docs/l10n-french.md`) gegen Muttersprachler-Gate vor Auslieferung (Option b).
- E-17-6 Go/No-Go niederlaendische Komposita: im Scope behalten mit endgueltigem Tor in Phase 21 (Option a, Empfehlung) gegen jetzt streichen (Option b). Sturzkriterium ist der Termin, und sie faellt als Ganzes, nicht halb.
- E-17-7 Lockerung der Vergleichsregel fuer `tantivy_version`: Option a "lockern" (Empfehlung) , gespeichert wird weiter der volle Banner, verglichen wird nur noch die `index_format`-Haelfte, Praezedenz ist die `index_version`-Ausnahme in `Store.version_mismatch` (`repo.py:665-667`); Option b "Pin bleibt auf 0.26.0" , dann wird LEX-07 umformuliert und die Panic-Frage allein ueber die Positivliste geschlossen. Beweisgrundlage von Option a sind die vier Messungen aus RESEARCH 4.2 (Format v7 beidseitig, Kreuzlesetest in beide Richtungen, 224 Zeilen identische Tokenisierung, Raedermatrix cp313 aarch64 und x86_64 vorhanden). Unter Option a ausdruecklich benennen, welche Absicherung aufgegeben wird ("tantivy koennte die Tokenisierung aendern, ohne das Format zu aendern") und was sie ersetzt (die Tabellen- und Waechtertests aus den Plaenen 17-03, 17-05 und 17-06). Explizit dazuschreiben: was NICHT geht, ist den Pin bewegen und die Marke unveraendert behaupten.
- E-17-8 Einheitliche Kettenreihenfolge `fold frueh` fuer alle vier Sprachen. Als **Kenntnisnahme mit Widerspruchsmoeglichkeit** kennzeichnen, nicht als offene Frage. Option a: Kenntnis genommen, einheitlich `fold frueh`. Option b: Widerspruch, dann Spanisch auf `fold spaet` und zwei Kettenformen im Modul. Beleg: 573 Paare, A+ 467 gegen C+ 463; es allein +4 fuer C+, it +2 und pt +6 fuer A+, nl unentschieden; vier Begruendungen aus RESEARCH 2.4 (kleiner Abstand auf einer Wortklasse; A+ ist die Form der schon ausgelieferten englischen Kette und `Filter.custom_stopword([])` ist gemessen ein No-op; das Produkt liest OCR und verliert dort Akzente; `fold spaet` erzeugt unerreichbare Terme). Die zwei dokumentierten Verluste (`informacion`/`informaciones` und `informacao`/`informacoes`) stehen als Preis unter Option a, damit der Owner sie mit unterschreibt.

Setze ganz oben in den Abschnitt den Satz, dass E-17-7 die erste Frage der Vorlage ist, weil alle Plaene mit tantivy-Bezug daran haengen.
  </action>
  <verify>
    <automated>cd .planning/phases/17-owner-tor-und-analyseketten && for n in 1 2 3 4 5 6 7 8; do grep -qE "^### E-17-$n:" 17-GRUNDSATZ-ENTSCHEID.md || { echo "FEHLT: E-17-$n"; exit 1; }; done && a=$(grep -cE "^#### Option a:" 17-GRUNDSATZ-ENTSCHEID.md) && b=$(grep -cE "^#### Option b:" 17-GRUNDSATZ-ENTSCHEID.md) && test "$a" = 8 && test "$b" = 8 && g=$(grep -c "Greift, wenn" 17-GRUNDSATZ-ENTSCHEID.md) && test "$g" -ge 16 && grep -qF "A mark that was never written counts as diverging" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "custom_stopword" 17-GRUNDSATZ-ENTSCHEID.md && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Genau acht Ueberschriften `### E-17-N:` mit N von 1 bis 8, jede mit genau einer `#### Option a:` und einer `#### Option b:`.
    - Jede der 16 Optionen traegt die drei Felder `Greift, wenn`, `Beweisgrundlage`, `Vollzug`.
    - E-17-4 Option a enthaelt die Auflage mit dem woertlichen Zitat "A mark that was never written counts as diverging".
    - E-17-7 Option a nennt die vier Messbelege aus RESEARCH 4.2 und den Satz, dass Pin bewegen plus Marke unveraendert behaupten kein gangbarer Weg ist.
    - E-17-8 ist als Kenntnisnahme mit Widerspruchsmoeglichkeit gekennzeichnet und nennt die Zahlen 573, 467 und 463 sowie beide dokumentierten Verluste.
    - Der Abschnitt beginnt mit dem Hinweis, dass E-17-7 zuerst gestellt wird.
  </acceptance_criteria>
  <done>Alle acht Entscheide sind so ausformuliert, dass am Vorlagetag kein Satz mehr entworfen werden muss.</done>
</task>

<task type="auto">
  <name>Task 3: Fertige Ersatztexte, leerer Vollzugsabschnitt und Checkliste</name>
  <files>.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md</files>
  <read_first>
    - `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md` (Stand aus Task 2)
    - `backend/tests/test_upgrade_compatibility.py`, Zeilen 1-130 (Modulkopf, `GOLD_V1_0_AND_V1_1`, `TANTIVY_MARK`, `GOLD_INDEX_FORMAT`, `TANTIVY_PIN`, `drift_findings`, die drei Selbsttests)
    - `backend/src/findling/store/repo.py`, Zeilen 631-676 und 1290-1310 (`version_mismatch`, `_generation_at_least`)
    - `.github/workflows/deploy-harp.yml`, Zeilen 3260-3365 (Job "Store upgrade 5", Vergleicher `unchanged`, Zusicherungen 2, 4, 5, 6 und der Zusammenfassungsblock)
    - `.github/dependabot.yml` (der `ignore`-Block am Dateiende samt Begruendungskommentar)
    - `THIRD-PARTY.md`, Zeile 139 und der Begruendungsabsatz Zeilen 150-155
    - `backend/pyproject.toml`, Zeile 13 (der Pin)
    - `.planning/milestones/v1.2-phases/12-messwerkzeug-runbook-und-terminentscheid/12-STABLE35-ENTSCHEID.md`, Zeilen 233-299 (Vollzugsmuster und Checkliste)
  </read_first>
  <action>
Fuelle die drei letzten Abschnitte.

`## Die fertigen Ersatztexte` bekommt je betroffener Datei einen Unterabschnitt `### <Dateipfad>` mit dem heutigen Text und dem Ersatztext fuer den greifenden Zweig, wortwoertlich einsetzbar. Pflichtstellen:
1. `backend/pyproject.toml` , `"tantivy==0.26.0"` wird `"tantivy==0.26.2"` (nur unter E-17-7 Option a).
2. `backend/tests/test_upgrade_compatibility.py` , `GOLD_V1_0_AND_V1_1["tantivy_version"]` wechselt von `"0.26.0"` auf `GOLD_INDEX_FORMAT`, also auf `"index_format v7"`; `TANTIVY_PIN` wechselt auf `"tantivy==0.26.2"`. Dazu der Kommentarblock, der die Lockerung begruendet und den Messbeleg vom 2026-09-23 nennt, und die geforderte vierte Selbstprobe: derselbe `index_format`, andere Patchnummer, muss gruen sein. Der Modulkopfsatz "A red test here is not a repair, it is a question for the owner" bleibt unveraendert; dass er stehen bleibt, gehoert als Satz in den Ersatztext-Block.
3. `backend/src/findling/store/repo.py` , die dritte `if`-Zeile in `version_mismatch` nach dem Vorbild der `index_version`-Ausnahme, mit einem Hilfsvergleicher nach dem Muster von `_generation_at_least` (Arbeitsname `_index_format_matches`), plus der neue Docstring-Absatz im selben Ton wie der vorhandene `index_version`-Absatz.
4. `.github/workflows/deploy-harp.yml` , Zusicherung 2 verliert `tantivyVersion` aus der `for`-Schleife der unveraenderten Marken und bekommt daneben eine eigene Pruefung nach dem Muster von Zusicherung 6 ("diese Marke DARF sich bewegen, und zwar genau von X nach Y"). Der Zusammenfassungsblock (Zeilen 3350-3362) wird mitgeaendert. Die Zusicherungen 4 und 5 bleiben woertlich stehen und muessen gruen bleiben; das gehoert als Satz dazu.
5. `.github/dependabot.yml` , berichtigter Begruendungskommentar. Der neue Text sagt: `index_format v7` gilt in 0.26.0 UND 0.26.2, gemessen am 2026-09-23; die Behauptung "index format v7 from 0.26.2 on, which means a reindex" war falsch; der Pin bewegt sich weiterhin nur mit Absicht; der Owner-Entscheid vom 2026-09-21 wird durch E-17-7 vom Vorlagetag abgeloest, mit beiden Daten im Kommentar. Der `ignore`-Block selbst bleibt unveraendert; ausdruecklich dazuschreiben, dass er nicht neu angelegt wird.
6. `THIRD-PARTY.md` , Tabellenzeile 139 auf 0.26.2 und der Tag im Begruendungsabsatz (Zeilen 150-155) auf `0.26.2`. Dazu der neue Satz, dass die Snowball-Stoppwortlisten BSD-3-Clause sind, einkompiliert vorliegen und die im Repo ausgelieferte gefaltete Ergaenzungsliste aus ihnen abgeleitet ist.

Fuer E-17-7 Option b ("Pin bleibt") steht je Stelle explizit "keine Aenderung" plus der Satz, dass LEX-07 dann umformuliert werden muss.

`## Vollzug am <Datum>` wird leer angelegt, mit den festen Feldern aus dem Muster: `Datum`, `Gelesener Stand` (Tabelle), `Greifender Zweig je Entscheid` (Tabelle E-17-1 bis E-17-8 mit Spalte Option und Spalte Begruendung des Owners), `Beleg`, `Vollzogen am / durch Plan`. Die Felder bleiben mit `, ` gefuellt, bis Plan 17-04 sie schreibt.

`### Vollzugs-Checkliste` , nummerierte Schritte in dieser Reihenfolge: 1. Entscheiddokument vorlegen (Plan 17-04). 2. Freigabe oder benannte Aenderungen eintragen. 3. Vollzugsabschnitt datiert fuellen. 4. Erst danach `backend/src/findling/store/repo.py` und `backend/tests/test_upgrade_compatibility.py` anfassen (Plan 17-07). 5. Danach `uv lock --upgrade-package tantivy` und `uv sync` aus `backend/`, nie eine Handkante an `uv.lock` (Plan 17-08). 6. Volle Suite und alle Gates fahren; ein roter Lauf ist ein Befund und wird gelesen, bevor irgendetwas geaendert wird. 7. CI-Zusicherung, dependabot-Kommentar und `THIRD-PARTY.md` mit dem Messbeleg im Kommentar nachziehen.
  </action>
  <verify>
    <automated>cd .planning/phases/17-owner-tor-und-analyseketten && for f in "backend/pyproject.toml" "backend/tests/test_upgrade_compatibility.py" "backend/src/findling/store/repo.py" ".github/workflows/deploy-harp.yml" ".github/dependabot.yml" "THIRD-PARTY.md"; do grep -qF "$f" 17-GRUNDSATZ-ENTSCHEID.md || { echo "Ersatztext fehlt fuer $f"; exit 1; }; done && grep -qF "## Vollzug am" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "### Vollzugs-Checkliste" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "ein roter Lauf ist ein Befund" 17-GRUNDSATZ-ENTSCHEID.md && grep -qF "uv lock --upgrade-package tantivy" 17-GRUNDSATZ-ENTSCHEID.md && test "$(wc -l < 17-GRUNDSATZ-ENTSCHEID.md)" -ge 200 && python -c "import sys,pathlib;t=pathlib.Path('17-GRUNDSATZ-ENTSCHEID.md').read_text(encoding='utf-8');sys.exit(1 if (chr(8211) in t or chr(8212) in t) else 0)" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Sechs Unterabschnitte mit Ersatztexten, je einer pro betroffener Datei; jeder nennt den heutigen Text und den Zieltext.
    - Der Ersatztext fuer `.github/dependabot.yml` sagt ausdruecklich, dass der `ignore`-Block nicht neu angelegt, sondern nur sein Kommentar berichtigt wird, und traegt beide Daten (2026-09-21 und den Vorlagetag).
    - Der Ersatztext fuer `deploy-harp.yml` nennt `tantivyVersion`, das Muster von Zusicherung 6 und den Zusammenfassungsblock, und sagt, dass Zusicherung 4 und 5 unveraendert gruen bleiben muessen.
    - `## Vollzug am` existiert mit allen fuenf Feldern und ist noch ungefuellt.
    - `### Vollzugs-Checkliste` hat sieben nummerierte Schritte; Schritt 6 enthaelt woertlich "ein roter Lauf ist ein Befund und wird gelesen, bevor irgendetwas geaendert wird".
    - Die Datei hat mindestens 200 Zeilen und keine Em-Dashes.
  </acceptance_criteria>
  <done>Das Entscheiddokument ist vorlagereif: am Vorlagetag wird nur noch gelesen, welcher Zweig greift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner-Entscheid zu Code | Ein schriftlicher Entscheid wird spaeter von Plaenen zitiert und in Code umgesetzt; eine unscharfe Formulierung wird zur falschen Aenderung |
| Planungsdokument zu CI-Beweisstrecke | Die Ersatztexte beruehren Gates, deren roter Zustand die Bestandsinstallationen schuetzt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Repudiation | `17-GRUNDSATZ-ENTSCHEID.md` | mitigate | Vollzugsabschnitt mit Datum, greifendem Zweig je Entscheid und Plannummer; Folgeplaene zitieren die Optionskennung statt einer Zusammenfassung |
| T-17-02 | Tampering | `backend/tests/test_upgrade_compatibility.py` (spaeter) | mitigate | Der Ersatztext haelt fest, dass der Modulkopfsatz "A red test here is not a repair" stehen bleibt und dass eine vierte Selbstprobe (gleicher `index_format`, andere Patchnummer) hinzukommt |
| T-17-03 | Denial of Service | Bestandsinstallationen im Feld | mitigate | Die Leitplanke definiert "D-04 beruehren" scharf; die Tabelle "was vor dem Tor laufen darf" verhindert, dass ein Plan den Pin vor dem Entscheid bewegt und 19 h 20 min Reindex ausloest |
| T-17-04 | Information Disclosure | , | accept | Das Dokument enthaelt keine Geheimnisse, keine Nutzerdaten und keine Zugangsdaten |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket. RESEARCH `## Package Legitimacy Audit` fuehrt `tantivy` als Bestandsabhaengigkeit mit Hash-Pin in `uv.lock`, Disposition Approved; der Pin-Sprung selbst laeuft in Plan 17-08 hinter dem Owner-Tor |
</threat_model>

<verification>
- `17-GRUNDSATZ-ENTSCHEID.md` existiert, traegt acht Entscheide, sechs Ersatztextbloecke, einen leeren Vollzugsabschnitt und eine siebenschrittige Checkliste.
- Kein Quelltext des Backends wurde angefasst: `git status --porcelain backend/ .github/ THIRD-PARTY.md` ist leer.
- Keine Em-Dashes, keine Emojis, echte Umlaute in der deutschen Prosa.
</verification>

<success_criteria>
Ein Leser, der nur dieses Dokument kennt, kann jeden der acht Entscheide treffen, ohne eine Recherche zu oeffnen, und weiss fuer jede der sechs betroffenen Dateien, welcher Text danach dort steht.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-01-SUMMARY.md` when done
</output>
