---
phase: 17-owner-tor-und-analyseketten
plan: 05
type: execute
wave: 2
depends_on: ["17-03"]
files_modified:
  - scripts/dev/chain_probe.py
  - scripts/dev/measure_chains.sh
  - backend/tests/fixtures/chain_cases_es.txt
  - backend/tests/fixtures/chain_cases_it.txt
  - backend/tests/fixtures/chain_cases_nl.txt
  - backend/tests/fixtures/chain_cases_pt.txt
  - backend/tests/fixtures/chain_known_losses_es.txt
  - backend/tests/fixtures/chain_known_losses_it.txt
  - backend/tests/fixtures/chain_known_losses_nl.txt
  - backend/tests/fixtures/chain_known_losses_pt.txt
  - backend/tests/fixtures/snowball_stopwords_0_26_2.txt
  - docs/measurements/2026-09-analyseketten/README.md
autonomous: true
requirements: [LEX-01]

must_haves:
  truths:
    - "Die Messung, die die Kettenreihenfolge entschieden hat, laesst sich auf jeder Entwicklermaschine ohne Container wiederholen"
    - "Die Sonde misst das Produkt: die Siegerkette wird Token fuer Token gegen `snowball_analyzer()` gegengeprueft"
    - "Ein Messbericht mit Datum, Umgebungstabelle und Rohdaten belegt jede Zahl, die diese Phase behauptet"
    - "Die Sonde sieht nie Nutzertext"
  artifacts:
    - path: "scripts/dev/chain_probe.py"
      provides: "Kandidatenketten, Formfamilien-Metrik, Stoppwort-Dichtheit, Gegenprobe gegen das Produkt"
      min_lines: 200
    - path: "scripts/dev/measure_chains.sh"
      provides: "Treiber ohne Container, mit Erwartungswerten im Kopfkommentar"
    - path: "backend/tests/fixtures/chain_cases_es.txt"
      provides: "Formfamilien Spanisch, eine je Zeile"
    - path: "docs/measurements/2026-09-analyseketten/README.md"
      provides: "Messbericht mit Umgebungstabelle, Kandidatentabelle und Verdikt je Sprache"
      min_lines: 80
  key_links:
    - from: "scripts/dev/chain_probe.py"
      to: "backend/src/findling/index/analyzer.py"
      via: "Gegenprobe der Siegerkette gegen snowball_analyzer()"
      pattern: "snowball_analyzer"
    - from: "docs/measurements/2026-09-analyseketten/README.md"
      to: "docs/measurements/2026-09-analyseketten/rohdaten/"
      via: "jede Zahl des Berichts zitiert eine Zeile der Rohdaten"
      pattern: "rohdaten"
---

<objective>
Die Kettenmessung wird als wiederholbares Werkzeug ins Repo geholt: Sonde, Treiber, Fixtures, Rohdaten und ein datierter Messbericht.

Purpose: LEX-01 macht die Messung zum Abnahmeinstrument. Die Zahlen aus der Recherche (65 Formfamilien, 573 geordnete Paare, A+ 467 gegen C+ 463, Ergaenzungsliste 77/10/0/30) sind heute einmalig gemessen; ab diesem Plan sind sie auf jeder Maschine nachfahrbar und damit ein Befund und keine Behauptung. Die Arbeit beruehrt D-04 nicht und laeuft unabhaengig vom Owner-Tor (RESEARCH 6.3).
Output: `scripts/dev/chain_probe.py`, `scripts/dev/measure_chains.sh`, neun Fixtures, `docs/measurements/2026-09-analyseketten/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@CLAUDE.md
@docs/testing.md
</context>

<interfaces>
Was der Executor ohne Suche braucht. Gelesen am 2026-09-23.

Aus dem Produkt (Plan 17-03):
- `findling.index.analyzer.snowball_analyzer(language: str, folded_stopwords: Sequence[str]) -> TextAnalyzer`
- `findling.index.stopwords.FOLDED_STOPWORDS: dict[str, tuple[str, ...]]`, Schluessel `english`, `spanish`, `italian`, `dutch`, `portuguese`
- `findling.index.stopwords.folded_stopwords_hash(entries: Sequence[str]) -> str`

Aus `scripts/dev/compound_probe.py` (die Formvorlage):
- Sicherheitsregel im Docstring, Zeilen 26-38
- `_tokenise()`, Zeilen 63-71: die Sonde baut keine eigene Kette, sie ruft die ausgelieferte
- Ausgabekonstanten, Zeilen 52-56: `TOKENS_FILE`, `SUBSET_FILE`, `NUMBERS_FILE`, `HEADER`
- `_numbers()`, Zeilen 105-122: ausschliesslich Zahlen und Digests
- `_differing()` und `_report()`, Zeilen 125-134 und 175-189: Rueckgabecode 1 bei Differenz, jede abweichende Stelle einzeln auf stderr
- Argumentmuster, Zeilen 137-158 und 195-196: handgeschriebener Parser, `main(argv) -> int`, `raise SystemExit(main(sys.argv[1:]))`

Aus `scripts/dev/measure_compounds.sh`:
- Kopfkommentar mit Erwartungswerten, Zeilen 13-36, samt dem Satz, wie mit einer Abweichung umzugehen ist
- `set -eu`, Pins als vollstaendige Argumente, `CDPATH='' cd --`, Zeilen 38-47 und 67-80
- Der `docker run`-Block, Zeilen 77-80 und 104-123, der hier ERSATZLOS entfaellt

Die sieben Kandidatenketten (RESEARCH 2.2), `CSTOP` ist `Filter.custom_stopword(<gefaltete Ergaenzung>)`:
A `low, fold, stop, long, stem` | A+ `low, fold, stop, CSTOP, long, stem` | B `low, stop, fold, long, stem` | B+ `low, stop, CSTOP, fold, long, stem` | C `low, stop, long, stem, fold` | C+ `low, stop, CSTOP, long, stem, fold` | D+ `low, fold, stop, CSTOP, long, stem, fold`

Zielzahlen (RESEARCH 2.3), gemessen am 2026-09-23 gegen tantivy 0.26.2:

| Sprache | Familien | Paare | A+ | C+ |
|---|---:|---:|---:|---:|
| es | 20 | 208 | 174 | 178 |
| it | 14 | 56 | 56 | 54 |
| nl | 13 | 81 | 57 | 57 |
| pt | 18 | 228 | 180 | 174 |
| Summe | 65 | 573 | 467 | 463 |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Die Sonde chain_probe.py</name>
  <files>scripts/dev/chain_probe.py</files>
  <read_first>
    - `scripts/dev/compound_probe.py` (vollstaendig)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitte 3.1, 3.2 (der abgedruckte Quelltext), 3.3, 2.2, 2.3
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitt "`scripts/dev/chain_probe.py`" und "Die Messung misst das Produkt, nicht sich selbst"
    - `backend/src/findling/index/analyzer.py` und `backend/src/findling/index/stopwords.py` (der Stand aus Plan 17-03)
    - `docs/testing.md`, Zeilen 18-36
  </read_first>
  <action>
Lege `scripts/dev/chain_probe.py` an. Englische Docstrings und Bezeichner; handgeschriebener Argumentparser, `main(argv) -> int`, `raise SystemExit(main(sys.argv[1:]))`. Aufruf: `chain_probe.py [--stopwords PATH] FIXTURE_DIR OUTPUT_DIR`.

Der Quelltext aus RESEARCH 3.2 ist die Vorlage und wird uebernommen, aber ruff-konform gemacht und um vier Dinge ergaenzt.

**a) Der geteilte Fixture-Leser.** `read_families(path) -> list[list[str]]`: Zeile fuer Zeile, alles ab `#` abschneiden, Rest mit `.split()` zerlegen, leere Zeilen ueberspringen. Dieser Leser ist die einzige Stelle, an der das Format ausgelegt wird, und er wird spaeter vom Test importiert, nicht nachgebaut. Ein Satz im Docstring sagt, warum er hier und nicht im Paket lebt (`scripts/` ist kein Paket, der Import fuehrt nur in dieser Richtung).

**b) Die sieben Kandidatenketten.** `chain(language, recipe, supplement) -> TextAnalyzer` genau wie in RESEARCH 3.2, mit `MAX_TOKEN_CHARS = 48`. Der Docstring erklaert, warum die Sonde hier ausnahmsweise eigene Ketten baut, obwohl die Regel des Baums lautet, dass die Sonde das Produkt misst: sechs der sieben Kandidaten gibt es im Produkt nicht.

**c) Die Gegenprobe, die diese Ausnahme wieder schliesst.** `verify_against_product(language, families, supplement) -> list[str]`: faehrt jede Form jeder Familie durch die selbstgebaute Kette A+ UND durch `snowball_analyzer(language, supplement)` aus dem Paket und liefert die Formen, deren Tokenlisten nicht identisch sind. Eine nichtleere Liste ist Rueckgabecode 1 und wird Fall fuer Fall auf stderr gemeldet, im Muster von `_differing()` und `_report()`.

**d) Die beiden Messungen.**
- `family_score(analyzer, forms) -> tuple[int, int, dict[str, list[str]]]` woertlich aus RESEARCH 3.2: geordnete Paare (getippte Form, Form im Dokument), die mindestens einen Term teilen.
- `stopword_leaks(analyzer, builtin, folder) -> tuple[list[str], list[str]]`: jedes Wort der eingebauten Liste und jede gefaltete Form davon durch die Kette schicken; alles, was einen nichtleeren Tokenstrom liefert, ist ein Leck. Die eingebaute Liste kommt aus `--stopwords` (Pfad auf `stopwords.rs` oder auf die in Task 3 erzeugte Fixture); fehlt sie, ueberspringt die Sonde diesen Teil mit einer Zeile auf stderr statt abzubrechen.

**Ausgabe**, drei Dateien mit festen Namen in OUTPUT_DIR, im Muster von `compound_probe.py`:
- `familien.tsv` mit der Kopfzeile `language`, `lemma`, `form`, `recipe`, `tokens`
- `kennzahlen.txt` mit ausschliesslich `name=wert`-Zeilen: je Sprache `<lang>_families`, `<lang>_pairs` und je Kandidat `<lang>_<recipe>_hits`, dann `<lang>_supplement`, `<lang>_leaks_accented`, `<lang>_leaks_flat`, am Ende `total_families`, `total_pairs`, `total_Aplus_hits`, `total_Cplus_hits`, `supplement_hash`
- `verluste.tsv` mit den Formpaaren, die unter A+ nicht zueinander finden, Kopfzeile `language`, `lemma`, `left`, `right`, `left_tokens`, `right_tokens`

**Sicherheitsregel** als eigener Absatz im Modulkopf, uebertragen von T-02-14: die Sonde liest die Fixtures dieses Repos und veroeffentlichte Snowball-Stoppwoerter, nie `state.db`, nie einen Index, nie eine Nutzerdatei; was sie druckt, ist entweder eine Zahl oder ein Wort aus einer dieser beiden Quellen.

Gates: nur `uv run ruff check --config backend/pyproject.toml` und `uv run ruff format --check --config backend/pyproject.toml` auf diese Datei. Kein pyright, kein vulture (`docs/testing.md`, Zeilen 20-36).
  </action>
  <verify>
    <automated>cd backend && uv run ruff check --config pyproject.toml ../scripts/dev/chain_probe.py && uv run ruff format --check --config pyproject.toml ../scripts/dev/chain_probe.py && uv run python -c "
import importlib.util, pathlib, sys
p = pathlib.Path('../scripts/dev/chain_probe.py').resolve()
spec = importlib.util.spec_from_file_location('chain_probe', p)
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
for name in ('read_families', 'chain', 'family_score', 'stopword_leaks', 'verify_against_product', 'main'):
    assert hasattr(m, name), name
assert m.read_families.__doc__
print('GRUEN')
" && ! grep -q "argparse" ../scripts/dev/chain_probe.py && grep -q "snowball_analyzer" ../scripts/dev/chain_probe.py && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dev/chain_probe.py` definiert `read_families`, `chain`, `family_score`, `stopword_leaks`, `verify_against_product` und `main(argv) -> int`, endet mit `raise SystemExit(main(sys.argv[1:]))` und importiert kein `argparse`.
    - `chain()` kennt genau die sieben Rezeptnamen A, Aplus, B, Bplus, C, Cplus, Dplus und setzt `ascii_fold` und `custom_stopword` je Rezept an die in RESEARCH 2.2 genannte Position.
    - `verify_against_product` ruft `snowball_analyzer` aus `findling.index.analyzer` auf und gibt die abweichenden Formen zurueck.
    - Der Modulkopf traegt die Sicherheitsregel T-02-14 mit der Aufzaehlung, was die Sonde nicht liest (`state.db`, Index, Nutzerdatei).
    - `uv run ruff check --config backend/pyproject.toml scripts/dev/chain_probe.py` und `ruff format --check` sind gruen.
  </acceptance_criteria>
  <done>Die Sonde steht, misst sieben Kandidaten und schliesst ihre eigene Ausnahme mit einer Gegenprobe gegen das Produkt.</done>
</task>

<task type="auto">
  <name>Task 2: Formfamilien-Fixtures und der Treiber ohne Container</name>
  <files>backend/tests/fixtures/chain_cases_es.txt, backend/tests/fixtures/chain_cases_it.txt, backend/tests/fixtures/chain_cases_nl.txt, backend/tests/fixtures/chain_cases_pt.txt, scripts/dev/measure_chains.sh</files>
  <read_first>
    - `backend/tests/fixtures/compound_cases_de.txt` (das heutige Fixture-Format)
    - `scripts/dev/measure_compounds.sh` (vollstaendig; der Kopfkommentar Zeilen 13-36 und der Docker-Block, der hier entfaellt)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 3.3 (Fixture-Form mit Beispiel), Abschnitt 2.3 (welche Woerter gemessen wurden), Abschnitt 3.4 (die 15 Faelle der zusammengefuehrten Tabelle)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitte "`backend/tests/fixtures/chain_cases_es.txt` und Geschwister" und "`scripts/dev/measure_chains.sh`"
  </read_first>
  <action>
**a) Die vier Formfamilien-Fixtures.** Eine Zeile je Formfamilie, Formen durch Leerzeichen getrennt, Kommentare mit `#`. Kopfkommentar je Datei: woher die Woerter stammen (Messung 2026-09-23), was eine Zeile bedeutet (alle Schreibweisen, die ein Mensch tippt oder ein Dokument traegt: akzentuiert und flach, Singular und Plural), und der Satz, dass ein Wort, ueber das ein Test eine Behauptung aufstellt, hier stehen muss.

Anzahl der Familien je Datei, exakt wie gemessen: `chain_cases_es.txt` 20, `chain_cases_it.txt` 14, `chain_cases_nl.txt` 13, `chain_cases_pt.txt` 18. Zusammen 65 Familien und 573 geordnete Paare (die Paarzahl ergibt sich als Summe der Quadrate der Formenzahl je Familie; die Fixtures muessen diese Summen treffen: es 208, it 56, nl 81, pt 228).

Pflichtfamilien, die in RESEARCH 3.4 als Abnahmefaelle stehen und deshalb enthalten sein MUESSEN:
- es: `informacion` mit Akzent, flach und Plural; `administracion`-Familie; `alemán aleman alemanes alemana alemanas`; `año ano años anos`; `ciudad ciudades`; `joven jóvenes`; `imagen imágenes`; `examen exámenes`
- it: `qualità qualita`; `città citta`; `società societa`; `università universita`
- nl: `coördinatie coordinatie`; `financiën financien`; `huis huizen`; `bedrijf bedrijven`; `gemeente gemeentes`
- pt: `informação informacao informações informacoes`; `café cafés`

Die Akzente stehen hier als Daten in einer Textdatei; das ist der gleiche Fall wie die Umlaute in `compound_cases_de.txt` und keine Verletzung der Code-Regel.

**b) `scripts/dev/measure_chains.sh`.** Uebernimm von `measure_compounds.sh`: `set -eu`, Pin als vollstaendiges Argument (`TANTIVY="tantivy==0.26.0"`, mit dem Hinweis, dass der Wert dem Pin in `backend/pyproject.toml` folgt), Pfadaufloesung ueber `CDPATH='' cd --`, Vorbedingungen mit eigener Fehlermeldung und Rueckgabecode.

Der gesamte `docker run`-Block entfaellt ersatzlos. Die Begruendung gehoert als Satz in den Kopfkommentar, weil sie der einzige sichtbare Unterschied zum Vorbild ist: alle vier Stoppwortlisten und alle vier Stemmer sind in tantivy einkompiliert, also genuegt `uv run` aus `backend/`, und ein Container wuerde nur Laufzeit kosten. Auch die MSYS-Warnung des Vorbilds entfaellt.

Kopfkommentar mit Erwartungswerten im Muster der Zeilen 13-36 des Vorbilds:
`total_families=65  total_pairs=573  total_Aplus_hits=467  total_Cplus_hits=463`
`es_supplement=77  it_supplement=10  nl_supplement=0  pt_supplement=30`
plus der Satz "Eine Abweichung ist ein Befund fuer den Messbericht und kein Grund, eine Datei von Hand zu bearbeiten."

Das Skript ruft `uv run python ../scripts/dev/chain_probe.py backend/tests/fixtures docs/measurements/2026-09-analyseketten/rohdaten` aus `backend/` (Pfade relativ zur aufgeloesten Repo-Wurzel) und reicht den Rueckgabecode der Sonde durch.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search && for l in es it nl pt; do test -f backend/tests/fixtures/chain_cases_$l.txt || { echo "fehlt: $l"; exit 1; }; done && python - <<'PY'
import pathlib
want = {"es": (20, 208), "it": (14, 56), "nl": (13, 81), "pt": (18, 228)}
for lang, (families, pairs) in want.items():
    lines = pathlib.Path(f"backend/tests/fixtures/chain_cases_{lang}.txt").read_text(encoding="utf-8").splitlines()
    rows = [l.split("#", 1)[0].split() for l in lines]
    rows = [r for r in rows if r]
    assert len(rows) == families, (lang, "families", len(rows), families)
    assert sum(len(r) ** 2 for r in rows) == pairs, (lang, "pairs", sum(len(r) ** 2 for r in rows), pairs)
print("GRUEN")
PY
test -f scripts/dev/measure_chains.sh && sh -n scripts/dev/measure_chains.sh && grep -q "total_pairs=573" scripts/dev/measure_chains.sh && ! grep -q "docker" scripts/dev/measure_chains.sh && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Vier Fixtures existieren mit genau 20, 14, 13 und 18 Familienzeilen (Kommentar- und Leerzeilen zaehlen nicht mit).
    - Die Summe der Quadrate der Formenzahl je Familie ergibt je Sprache 208, 56, 81 und 228, zusammen 573.
    - Alle in `<action>` aufgezaehlten Pflichtfamilien stehen in der jeweiligen Datei.
    - `scripts/dev/measure_chains.sh` ist syntaktisch gueltig (`sh -n`), enthaelt `set -eu`, die vier Erwartungswertzeilen und den Satz zur Abweichung, und enthaelt nirgends das Wort `docker`.
    - Der Kopfkommentar des Treibers begruendet in einem Satz, warum es keinen Container gibt.
  </acceptance_criteria>
  <done>Die Messgrundlage liegt im Repo und der Treiber laeuft ohne Container.</done>
</task>

<task type="auto">
  <name>Task 3: Messlauf, Rohdaten, bekannte Verluste und Messbericht</name>
  <files>backend/tests/fixtures/chain_known_losses_es.txt, backend/tests/fixtures/chain_known_losses_it.txt, backend/tests/fixtures/chain_known_losses_nl.txt, backend/tests/fixtures/chain_known_losses_pt.txt, backend/tests/fixtures/snowball_stopwords_0_26_2.txt, docs/measurements/2026-09-analyseketten/README.md</files>
  <read_first>
    - `docs/measurements/2026-09-komposita-rezept-a/README.md` (vollstaendig; Kopf Zeilen 1-13, Umgebungstabelle Zeilen 17-32)
    - `scripts/dev/chain_probe.py` und `scripts/dev/measure_chains.sh` (die Staende aus Task 1 und 2)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitte 2.1 bis 2.4 und 3.4
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitt "`docs/measurements/2026-09-analyseketten/`"
  </read_first>
  <action>
**a) Messlauf.** Fahre `scripts/dev/measure_chains.sh`. Die drei Ausgabedateien landen unter `docs/measurements/2026-09-analyseketten/rohdaten/`. Weicht eine Zahl von den Erwartungswerten ab, ist das ein Befund fuer den Bericht: die Abweichung wird im Bericht benannt und begruendet, und keine Fixture wird nachtraeglich passend gemacht.

**b) `backend/tests/fixtures/snowball_stopwords_0_26_2.txt`.** Damit die Dichtheitsmessung spaeter offline als Test laufen kann (Plan 17-06), wird die geparste eingebaute Liste als Fixture abgelegt: eine Zeile je Wort im Format `<snowball-sprache>\t<wort>`, sortiert nach Sprache und dann nach Wort. Kopfkommentar mit `#`: Herkunft (`quickwit-oss/tantivy`, Tag 0.26.2, `src/tokenizer/stop_word_filter/stopwords.rs`), Ladedatum, Lizenz BSD-3-Clause, Wortzahlen je Sprache (spanish 308, italian 279, portuguese 203, dutch 101) und der Satz, dass die Datei maschinell entsteht und von Hand nicht angefasst wird.

**c) Die vier Verlustlisten `chain_known_losses_<lang>.txt`.** Inhalt aus `rohdaten/verluste.tsv`: je Zeile ein Formpaar, das unter der ausgelieferten Kette A+ nicht zueinander findet, im Format `<form_links> <form_rechts>`. Kopfkommentar je Datei: diese Liste ist eine Behauptung in beide Richtungen, der zugehoerige Test in Plan 17-06 wird rot, sobald ein Verlust dazukommt ODER verschwindet; ein verschwundener Verlust ist genauso ein Befund wie ein neuer, weil er bedeutet, dass sich die Kette bewegt hat. Fuer eine Sprache ohne Verluste bleibt die Datei bis auf den Kopfkommentar leer.

**d) `docs/measurements/2026-09-analyseketten/README.md`.** Deutsche Prosa mit ECHTEN Umlauten (die Nachbarverzeichnisse nutzen ASCII-Ersatz; das ist Bestand und wird nicht angefasst, neue Texte folgen der globalen Regel). Keine Em-Dashes. Aufbau nach dem Vorbild:
1. Kopfsatz: Dieser Bericht ist die Messgrundlage der Phase 17. Jede spaetere Aussage dieser Phase ueber Token soll eine Zeile in `rohdaten/familien.tsv` zitieren koennen statt geraten zu werden. Dazu der Satz, womit gemessen wurde (`scripts/dev/measure_chains.sh` faehrt `scripts/dev/chain_probe.py`, ohne Container, weil alle Listen und Stemmer in tantivy einkompiliert sind) und dass die Siegerkette Token fuer Token gegen `snowball_analyzer()` aus dem Paket gegengeprueft wird, damit der Bericht das Produkt misst und nicht sich selbst.
2. Umgebungstabelle: Datum, tantivy-Fassung des Laufs, Tag von `stopwords.rs`, Wortzahlen 308/279/203/101, Zahl und Hash der Ergaenzungsliste, Python-Fassung, Rechner.
3. Die Kandidatentabelle aus RESEARCH 2.3 mit den selbst gemessenen Zahlen, Spalten wie dort.
4. Die Stoppwort-Leck-Tabelle aus RESEARCH 2.3.
5. Verdikt je Sprache (RESEARCH 2.4) mit den vier Begruendungen fuer die einheitliche Kette und dem Verweis, dass die Entscheidung als E-17-8 im Owner-Tor liegt.
6. Die zusammengefuehrte Testfall-Tabelle aus RESEARCH 3.4, alle 15 Zeilen, mit Datum. Die beiden roten Zeilen bleiben rot und werden als dokumentierter Verlust gekennzeichnet.
7. Nebenrechnung: die gerichtete Variante der Metrik (Dokumente korrekt akzentuiert, Anfragen gemischt) wurde fuer zwei Familien von Hand nachgerechnet und ergab fuer es einen Vorteil von `fold spaet`, fuer pt ein Unentschieden. Sie steht als Nebenrechnung im Bericht, damit ein spaeterer Leser die Abwaegung nachvollziehen kann; Abnahme ist die ungerichtete Metrik.
8. Abweichungen dieses Laufs von den Erwartungswerten, falls es welche gab, sonst der Satz, dass es keine gab.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search && test -f docs/measurements/2026-09-analyseketten/README.md && for f in familien.tsv kennzahlen.txt verluste.tsv; do test -s docs/measurements/2026-09-analyseketten/rohdaten/$f || { echo "Rohdaten fehlen: $f"; exit 1; }; done && grep -qE "^total_pairs=573$" docs/measurements/2026-09-analyseketten/rohdaten/kennzahlen.txt && grep -qE "^total_Aplus_hits=467$" docs/measurements/2026-09-analyseketten/rohdaten/kennzahlen.txt && for l in es it nl pt; do test -f backend/tests/fixtures/chain_known_losses_$l.txt || { echo "fehlt: $l"; exit 1; }; done && test -s backend/tests/fixtures/snowball_stopwords_0_26_2.txt && awk -F'\t' '!/^#/ && NF==2 {c[$1]++} END {exit !(c["spanish"]==308 && c["italian"]==279 && c["portuguese"]==203 && c["dutch"]==101)}' backend/tests/fixtures/snowball_stopwords_0_26_2.txt && test "$(grep -v '^#' docs/measurements/2026-09-analyseketten/README.md | grep -c .)" -ge 60 && python -c "import sys,pathlib;t=pathlib.Path('docs/measurements/2026-09-analyseketten/README.md').read_text(encoding='utf-8');sys.exit(1 if (chr(8211) in t or chr(8212) in t) else 0)" && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `docs/measurements/2026-09-analyseketten/rohdaten/` enthaelt `familien.tsv`, `kennzahlen.txt` und `verluste.tsv`, alle nicht leer.
    - `kennzahlen.txt` traegt `total_families=65`, `total_pairs=573`, `total_Aplus_hits=467`, `total_Cplus_hits=463` sowie `es_supplement=77`, `it_supplement=10`, `nl_supplement=0`, `pt_supplement=30`. Jede Abweichung ist im Bericht unter Punkt 8 benannt und begruendet.
    - Die Gegenprobe der Sonde gegen `snowball_analyzer()` meldet null abweichende Formen (Rueckgabecode 0).
    - `backend/tests/fixtures/snowball_stopwords_0_26_2.txt` enthaelt 308 spanische, 279 italienische, 203 portugiesische und 101 niederlaendische Zeilen im Format `sprache<TAB>wort`.
    - Die vier Verlustlisten existieren; ihr Inhalt stammt aus `verluste.tsv` und nicht aus der Recherche.
    - Der Bericht hat alle acht Abschnitte, echte Umlaute, keine Em-Dashes, und die Tabelle aus RESEARCH 3.4 mit allen 15 Zeilen samt Datum.
  </acceptance_criteria>
  <done>Jede Zahl dieser Phase hat eine Zeile in den Rohdaten, und die Messung ist wiederholbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Messsonde zu Nutzerdaten | Ein Werkzeug, das druckt, was es tokenisiert, ist der billigste Weg, Inhalte zu lecken (T-02-14) |
| Fremde Quelle zu Repo-Fixture | `stopwords.rs` wird aus einem fremden Repository geladen und als Fixture ausgeliefert |
| Messbericht zu Produktentscheid | Eine falsche Zahl im Bericht traegt eine Kettenentscheidung, die den ganzen Index formt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Information Disclosure | `scripts/dev/chain_probe.py` | mitigate | Sicherheitsregel T-02-14 im Modulkopf; die Sonde liest ausschliesslich Repo-Fixtures und veroeffentlichte Snowball-Woerter, nie `state.db`, nie einen Index, nie eine Nutzerdatei |
| T-17-18 | Tampering | Messergebnis weicht ab und wird stillschweigend passend gemacht | mitigate | Erwartungswerte im Kopfkommentar des Treibers plus der Satz, dass eine Abweichung ein Befund ist; Abweichungen sind im Bericht als eigener Abschnitt zu fuehren |
| T-17-19 | Spoofing | `stopwords.rs` aus fremder Quelle | mitigate | Laden ausschliesslich vom gepinnten Tag 0.26.2 ueber die offizielle Adresse; die abgelegte Fixture traegt Herkunft, Datum, Lizenz und Wortzahlen im Kopf, und die Zahlen 308/279/203/101 sind eine Pruefbedingung |
| T-17-20 | Tampering | Sonde misst sich selbst statt des Produkts | mitigate | `verify_against_product` vergleicht die Siegerkette Token fuer Token gegen `snowball_analyzer()`; eine Differenz ist Rueckgabecode 1 |
| T-17-21 | Denial of Service | Bestandsinstallationen | accept | Dieser Plan aendert keinen Produktionspfad; er legt Werkzeug, Fixtures und Dokumentation an |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket; `uv sync` wird nicht aufgerufen und `backend/uv.lock` nicht angefasst |
</threat_model>

<verification>
- `scripts/dev/measure_chains.sh` laeuft mit Rueckgabecode 0 und erzeugt drei nicht leere Rohdatendateien.
- `uv run python -m pytest -q` aus `backend/` ist weiterhin gruen (die neuen Fixtures werden in diesem Plan von keinem Test gelesen).
- Die Dev-Skripte sind ruff-gruen mit `--config backend/pyproject.toml`.
- `git diff --quiet backend/src/` : kein Produktionspfad ist angefasst.
</verification>

<success_criteria>
Die Messung, die die Kettenreihenfolge traegt, ist im Repo, laeuft ohne Container, misst das Produkt und ist in einem datierten Bericht mit Rohdaten belegt.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-05-SUMMARY.md` when done
</output>
