---
phase: 17-owner-tor-und-analyseketten
plan: 08
type: execute
wave: 4
depends_on: ["17-07"]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/tests/test_upgrade_compatibility.py
  - .github/workflows/deploy-harp.yml
  - .github/dependabot.yml
  - THIRD-PARTY.md
autonomous: true
requirements: [LEX-07]

must_haves:
  truths:
    - "`tantivy` steht auf 0.26.2, und `index_format v7` ist unveraendert"
    - "Ein unbekannter Sprachname erzeugt in dieser Fassung einen ValueError statt einer Rust-Panic"
    - "Die CI-Zusicherung sagt weiterhin die Wahrheit: vier Marken unveraendert, die fuenfte bewegt sich genau von X nach Y"
    - "Die Begruendung im dependabot-Kommentar stimmt mit der Messung ueberein"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Pin tantivy==0.26.2"
      contains: "tantivy==0.26.2"
    - path: ".github/workflows/deploy-harp.yml"
      provides: "Zusicherung 2 ohne tantivyVersion plus eigene Muss-sich-bewegen-Pruefung"
    - path: "THIRD-PARTY.md"
      provides: "Lizenzzeile und Provenienzabsatz auf 0.26.2, Snowball-Listen erwaehnt"
  key_links:
    - from: "backend/pyproject.toml"
      to: "backend/uv.lock"
      via: "uv lock --upgrade-package tantivy"
      pattern: "tantivy"
    - from: ".github/workflows/deploy-harp.yml"
      to: "backend/src/findling/store/repo.py"
      via: "die gelockerte Vergleichsregel macht die bewegte Marke folgenlos"
      pattern: "tantivyVersion"
---

<objective>
Der Pin springt auf `tantivy==0.26.2`, und alle Stellen, die davon rot werden, werden mit dem Messbeleg im Kommentar nachgezogen.

Purpose: LEX-07. Der Sprung bringt die saubere Ausnahme statt der Rust-Panic (PR #734) und `Index.is_compatible` fuer Phase 18. Er laeuft hinter dem Owner-Tor und hinter der gelockerten Vergleichsregel aus Plan 17-07, damit Erfolgskriterium 4 haelt: keine Bestandsinstallation reindiziert.
Output: bewegter Pin, aktualisierter Lock, angepasste CI-Zusicherung, berichtigter dependabot-Kommentar, aktualisierte Lizenzliste
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md
@.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md
@.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Gelesen am 2026-09-23.

Wo der Pin ueberall steht (RESEARCH 4.4):
- `backend/pyproject.toml`, Zeile 13: `"tantivy==0.26.0"` , von Hand
- `backend/uv.lock`: `name = "tantivy"`, `version = "0.26.0"`, Rad-URLs mit sha256, `{ name = "tantivy", specifier = "==0.26.0" }` , NUR ueber `uv lock --upgrade-package tantivy`, nie von Hand
- `backend/tests/test_upgrade_compatibility.py`: `TANTIVY_PIN` , von Hand
- `backend/tests/test_index_open.py`, Zeilen 413 und 418: prueft gegen `TANTIVY_VERSION`, bleibt gruen
- `.github/dependabot.yml`: der `ignore`-Block EXISTIERT bereits am Dateiende; nur sein Kommentar wird berichtigt
- `THIRD-PARTY.md`, Zeile 139 (Tabellenzeile) und Zeilen 150-155 (Provenienzabsatz mit Tag)

`.github/workflows/deploy-harp.yml`, Job "Store upgrade 5":
- Zeilen 3270-3281: der Vergleicher `unchanged()`, der jede Zusicherung laufen laesst statt beim ersten Unterschied abzubrechen
- Zeilen 3288-3291, Zusicherung 2: `for mark in schemaVersion indexVersion analyzerVersion wordlistHash tantivyVersion; do unchanged ".marks.${mark}" "..."; done`
- Zeilen 3303-3329, Zusicherungen 4 und 5: kein Reindex-Banner, keine Zeile "built by different code" , bleiben woertlich
- Zeilen 3331-3342, Zusicherung 6: das Muster fuer eine Marke, die sich bewegen MUSS ("was X before the upgrade and is Y after it")
- Zeilen 3350-3362: der Zusammenfassungsblock, der die Marken im Klartext aufzaehlt
- `UPGRADE_FROM_TAG` steht auf `v1.1.0`; LEX-08 hebt ihn erst in Phase 18 auf `v1.2.0`

Messbelege (RESEARCH 4.2, 4.3, 5.1), die in die Kommentare gehoeren:
- `tantivy v0.26.0, index_format v7` und `tantivy v0.26.2, index_format v7`
- Index mit 0.26.0 geschrieben, mit 0.26.2 gelesen: `num_docs=1 hits=1`; und umgekehrt ebenso
- Tokenisierung ueber 7 Ketten und 32 Woerter, 224 Zeilen: `diff` leer
- Raedermatrix: cp313 manylinux_2_17 aarch64 und x86_64 vorhanden; die `cp313t`-Raeder sind entfallen und werden nicht genutzt; keine musllinux-Raeder in beiden Fassungen, das Image ist glibc-basiert
- `Filter.stopword("romanian")`: Panic in 0.26.0, `ValueError: No builtin stop word list for language: romanian` in 0.26.2
- Der behauptete "Union-Scorer-Bugfix" existiert nicht und wird aus jeder Begruendung gestrichen
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pin bewegen, Lock erneuern, volle Suite fahren</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/tests/test_upgrade_compatibility.py</files>
  <read_first>
    - `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md`, Vollzugsabschnitt (greifender Zweig E-17-7) und der Ersatztext-Unterabschnitt zu `backend/pyproject.toml`
    - `backend/pyproject.toml`, Zeilen 1-30 (die Abhaengigkeitsliste mit dem Pin)
    - `backend/tests/test_upgrade_compatibility.py` (Stand aus Plan 17-07, insbesondere `TANTIVY_PIN` und der Kommentar daneben)
    - `backend/tests/test_index_open.py`, Zeilen 405-425 (die Marke und die `index_format`-Zusicherung, die gruen bleiben muessen)
    - `.planning/phases/17-owner-tor-und-analyseketten/17-RESEARCH.md`, Abschnitt 4.4 (Ablauf ohne Ueberraschung)
  </read_first>
  <action>
**Vorbedingung:** Der Vollzugsabschnitt von `17-GRUNDSATZ-ENTSCHEID.md` fuehrt E-17-7 auf Option a, und Plan 17-07 ist abgeschlossen (die Ausnahme `_index_format_matches` steht in `repo.py`). Steht dort Option b, entfaellt dieser Plan vollstaendig; schreibe dann ein SUMMARY, das den Zweig zitiert und LEX-07 als umzuformulieren markiert.

Reihenfolge, genau so und nicht anders:
1. In `backend/pyproject.toml` `"tantivy==0.26.0"` auf `"tantivy==0.26.2"` aendern. Keine andere Zeile anfassen.
2. Aus `backend/`: `uv lock --upgrade-package tantivy`, danach `uv sync`. Keine Handkante an `uv.lock`.
3. `git diff backend/uv.lock` lesen, BEVOR irgendetwas weitergeht. Der Diff darf ausschliesslich den `tantivy`-Block betreffen: `version`, die Rad-URLs samt sha256 und die `specifier`-Zeile. Bewegt der Resolver mehr, ist das ein Befund und gehoert ins SUMMARY, nicht stillschweigend in den Commit.
4. Volle Suite: `uv run python -m pytest -q` aus `backend/`.
5. In `backend/tests/test_upgrade_compatibility.py` `TANTIVY_PIN` auf `"tantivy==0.26.2"` setzen und den in Plan 17-07 gesetzten Kommentar ("der Pin wandert erst in Plan 17-08") durch den Vollzugshinweis ersetzen: Datum, `E-17-7 Option a`, und die Feststellung, dass `GOLD_V1_0_AND_V1_1["tantivy_version"]` weiterhin `"index_format v7"` ist und deshalb nicht mitwandert.
6. Volle Suite erneut, dazu alle Gates: `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture`.

Zwei Dinge, die dabei zu pruefen und gegebenenfalls zu bereinigen sind:
- Der Kommentar `# pyright: ignore[reportAttributeAccessIssue]` an `TANTIVY_VERSION` (`index/open.py`, Zeile 66) wird unnoetig, falls 0.26.2 das Attribut im Typstub deklariert. Meldet `pyright` oder `ruff` den Kommentar als ueberfluessig, entferne ihn samt der Haelfte des Begruendungstexts, die sich auf den fehlenden Stub bezieht, und halte das im SUMMARY fest. Meldet niemand etwas, bleibt alles stehen.
- Der Panic-Beweis: pruefe an der neuen Fassung, dass `Filter.stopword("romanian")` bis zum `build()` einen `ValueError` wirft und keine Panic mehr. Das ist die Begruendung des Sprungs und gehoert als Zahl ins SUMMARY.

Ein roter Lauf ist ein Befund und wird gelesen, bevor irgendetwas geaendert wird. Insbesondere: wird `test_upgrade_compatibility.py` rot, ist die Lockerung aus Plan 17-07 unvollstaendig, und die Antwort ist eine Korrektur dort, kein Anpassen der Gold-Werte hier.
  </action>
  <verify>
    <automated>cd backend && grep -q 'tantivy==0.26.2' pyproject.toml && grep -q 'tantivy==0.26.2' tests/test_upgrade_compatibility.py && uv run python -c "
import tantivy
from tantivy import Filter, TextAnalyzerBuilder, Tokenizer
assert tantivy.__version__ == 'tantivy v0.26.2, index_format v7', tantivy.__version__
try:
    TextAnalyzerBuilder(Tokenizer.simple()).filter(Filter.stopword('romanian')).build()
except ValueError as exc:
    print('ValueError ok:', exc)
else:
    raise SystemExit('romanian built without an error')
print('GRUEN')
" && uv run python -m pytest -q && uv run ruff check && uv run ruff format --check && uv run pyright && uv run vulture && cd .. && test "$(git diff --name-only -- backend/uv.lock | wc -l)" = 1 && test "$(git diff -U0 -- backend/uv.lock | grep -c '^[+-][^+-]')" -le 80 && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` und `backend/tests/test_upgrade_compatibility.py` tragen `tantivy==0.26.2`.
    - `tantivy.__version__` meldet `"tantivy v0.26.2, index_format v7"`.
    - `Filter.stopword("romanian")` wirft beim `build()` einen `ValueError` und keine Panic.
    - Der Diff von `backend/uv.lock` betrifft ausschliesslich den `tantivy`-Block; jede weitere bewegte Abhaengigkeit ist im SUMMARY benannt und begruendet.
    - `GOLD_V1_0_AND_V1_1["tantivy_version"]` ist unveraendert `"index_format v7"` und wurde in diesem Plan nicht angefasst.
    - Die volle Suite `uv run python -m pytest -q` aus `backend/` ist gruen, insbesondere `tests/test_upgrade_compatibility.py`, `tests/test_index_open.py` und `tests/test_lockstep_versions.py`.
    - ruff, ruff format, pyright und vulture aus `backend/` sind gruen; ein ueberfluessig gewordener `pyright: ignore`-Kommentar ist entweder entfernt oder es meldet ihn niemand.
  </acceptance_criteria>
  <done>Der Pin steht auf 0.26.2, der Lock ist sauber, die Suite ist gruen, und keine Marke ist unbemerkt gewandert.</done>
</task>

<task type="auto">
  <name>Task 2: CI-Zusicherung, dependabot-Kommentar und Lizenzliste nachziehen</name>
  <files>.github/workflows/deploy-harp.yml, .github/dependabot.yml, THIRD-PARTY.md</files>
  <read_first>
    - `.github/workflows/deploy-harp.yml`, Zeilen 3256-3365 (Vergleicher, Zusicherungen 1 bis 6, Zusammenfassungsblock)
    - `.github/dependabot.yml` (vollstaendig, insbesondere der `ignore`-Block am Dateiende samt Kommentar)
    - `THIRD-PARTY.md`, Zeilen 130-160
    - `.planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md`, die Ersatztexte zu diesen drei Dateien
    - `.planning/phases/17-owner-tor-und-analyseketten/17-PATTERNS.md`, Abschnitte "`.github/workflows/deploy-harp.yml`" und "`.github/dependabot.yml`"
  </read_first>
  <action>
**a) `.github/workflows/deploy-harp.yml`.** Nimm `tantivyVersion` aus der `for`-Schleife der Zusicherung 2 heraus; dort bleiben die vier Marken `schemaVersion indexVersion analyzerVersion wordlistHash` stehen, jede weiterhin ueber `unchanged`. Setze daneben eine eigene Pruefung fuer `tantivyVersion` nach dem Muster von Zusicherung 6 (Zeilen 3331-3342): sie liest `.marks.tantivyVersion` vor und nach dem Upgrade und verlangt, dass die `index_format`-Haelfte gleich geblieben ist, waehrend die Fassungsnummer sich bewegen darf. Die Meldung sagt in einem Satz, warum diese eine Marke anders behandelt wird, und nennt `E-17-7 Option a` mit Datum sowie den Messbeleg (Format v7 in beiden Fassungen, Index in beide Richtungen lesbar, Tokenisierung ueber 224 Zeilen identisch).

Die Zusicherungen 4 und 5 (kein Reindex-Banner, keine Zeile "built by different code") bleiben woertlich unveraendert; sie sind der eigentliche Beweis von Erfolgskriterium 4, waehrend Zusicherung 2 nur der erste Anzeiger war. Der Zusammenfassungsblock (Zeilen 3350-3362) zaehlt die Marken im Klartext auf und wird mitgeaendert, sonst behauptet die Job-Zusammenfassung etwas, das der Schritt darueber nicht mehr prueft. Der Vergleicher `unchanged()` und das Prinzip "jede Zusicherung laeuft, keine bricht ab" bleiben.

`UPGRADE_FROM_TAG` bleibt auf `v1.1.0`; die Hebung auf `v1.2.0` ist LEX-08 und Zusage von Phase 18.

**b) `.github/dependabot.yml`.** Der `ignore`-Block bleibt unveraendert; er wird NICHT neu angelegt, er existiert seit dem 21.09.2026. Ersetze allein den Begruendungskommentar darueber. Der neue Text sagt: `index_format v7` gilt in 0.26.0 UND in 0.26.2, gemessen am 2026-09-23; die frueher hier stehende Behauptung "index format v7 from 0.26.2 on, which means a reindex" war sachlich falsch und wird hiermit berichtigt; der Pin bewegt sich weiterhin nur mit Absicht und zusammen mit einer Pruefung der Vergleichsregel; der Owner-Entscheid vom 21.09.2026 ist durch `E-17-7` vom Vollzugsdatum abgeloest, beide Daten stehen im Kommentar. Der Absatz ueber die Antwort des Dienstes auf Pull Request #10 bleibt stehen, weil er weiterhin gilt.

**c) `THIRD-PARTY.md`.** Tabellenzeile 139 auf `0.26.2`. Im Provenienzabsatz (Zeilen 150-155) wandert der gelesene Tag von `0.26.0` auf `0.26.2` mit, zusammen mit dem Lesedatum; die Aussage, dass die Lizenz von tantivy als einzige nicht aus PyPI ablesbar ist, bleibt. Ergaenze einen Satz: die Snowball-Stoppwortlisten sind BSD-3-Clause, in tantivy einkompiliert und damit keine eigene Zeile wert; die im Repo ausgelieferte gefaltete Ergaenzungsliste (`backend/src/findling/index/stopwords.py`) ist aus ihnen abgeleitet, und deshalb steht ihre Herkunft hier.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search && python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy-harp.yml',encoding='utf-8')); yaml.safe_load(open('.github/dependabot.yml',encoding='utf-8')); print('YAML ok')" && grep -q "schemaVersion indexVersion analyzerVersion wordlistHash;" .github/workflows/deploy-harp.yml && grep -q "tantivyVersion" .github/workflows/deploy-harp.yml && grep -q "built by different code" .github/workflows/deploy-harp.yml && grep -q "UPGRADE_FROM_TAG" .github/workflows/deploy-harp.yml && ! grep -q "index format v7 from 0.26.2 on" .github/dependabot.yml && grep -q 'dependency-name: "tantivy"' .github/dependabot.yml && grep -q "E-17-7" .github/dependabot.yml && grep -q "| \`tantivy\` | 0.26.2 |" THIRD-PARTY.md && grep -q "BSD-3" THIRD-PARTY.md && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Beide YAML-Dateien parsen fehlerfrei.
    - In Zusicherung 2 laeuft die `for`-Schleife ueber genau vier Marken; `tantivyVersion` steht nicht mehr darin, sondern in einer eigenen Pruefung, die die `index_format`-Haelfte vergleicht und die Fassungsnummer freigibt.
    - Die Zusicherungen 4 und 5 sind unveraendert (`git diff` zeigt in diesem Bereich keine Zeilen).
    - Der Zusammenfassungsblock nennt dieselben Marken wie der Schritt darueber.
    - `UPGRADE_FROM_TAG` ist unveraendert `v1.1.0`.
    - Der `ignore`-Block in `.github/dependabot.yml` ist unveraendert (Eintrag `dependency-name: "tantivy"` mit drei `update-types`); nur der Kommentar darueber ist neu und enthaelt beide Daten sowie `E-17-7`. Die falsche Behauptung "index format v7 from 0.26.2 on" kommt in der Datei nicht mehr vor.
    - `THIRD-PARTY.md` fuehrt `tantivy` mit `0.26.2`, der Provenienzabsatz nennt Tag `0.26.2` mit Lesedatum, und die Snowball-Listen sind mit BSD-3-Clause erwaehnt.
    - Der Begriff "Union-Scorer" kommt in keiner der drei Dateien vor.
  </acceptance_criteria>
  <done>Die Beweisstrecke sagt wieder die Wahrheit, und keine Begruendung im Baum widerspricht dem Messbericht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paketregistry zu Laufzeit | Ein bewegter Pin zieht neue Raeder mit neuen Hashes in das Image |
| CI-Zusicherung zu Bestandsinstallation | Die Beweisstrecke ist der letzte Schutz vor einem Feld-Reindex; eine aufgeweichte Zusicherung schuetzt nichts mehr |
| Kommentar im Baum zu spaeterem Leser | Eine falsche Begruendung im Repo wird beim naechsten Mal als Tatsache gelesen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-32 | Tampering | `backend/uv.lock` bewegt mehr als tantivy | mitigate | Der Lock wird ausschliesslich ueber `uv lock --upgrade-package tantivy` erneuert, der Diff wird vor dem Weitergehen gelesen, und das Verify-Kommando begrenzt die geaenderten Zeilen |
| T-17-33 | Denial of Service | Feld-Reindex durch bewegte Marke | mitigate | Plan 17-07 liegt davor; Zusicherungen 4 und 5 in `deploy-harp.yml` bleiben woertlich und sind der eigentliche Beweis |
| T-17-34 | Tampering | Aufgeweichte CI-Zusicherung | mitigate | `tantivyVersion` wird nicht geloescht, sondern durch eine schaerfere Pruefung ersetzt (Formathaelfte gleich, Fassung darf wandern), Muster von Zusicherung 6; der Zusammenfassungsblock wird mitgefuehrt |
| T-17-35 | Repudiation | Berichtigter dependabot-Kommentar loescht den frueheren Entscheid | mitigate | Beide Daten (21.09.2026 und Vollzugsdatum) stehen im neuen Kommentar; der Block selbst bleibt unangetastet |
| T-17-36 | Information Disclosure | , | accept | Der Sprung beruehrt keinen Datenpfad und keine Ausgabe |
| T-17-SC | Tampering | pip/uv installs | mitigate | Kein neuer Paketname; `tantivy` ist Bestandsabhaengigkeit mit Hash-Pin in `uv.lock`, Herkunft ueber die offiziellen GitHub-Releases verifiziert, Disposition **Approved** laut RESEARCH `## Package Legitimacy Audit`. Der Sprung laeuft ueber `uv lock --upgrade-package` und nicht ueber einen frei getippten Namen; der Hash-Diff wird gelesen |
</threat_model>

<verification>
- `uv run python -m pytest -q` aus `backend/` ist gruen, mit tantivy 0.26.2.
- `uv run ruff check`, `uv run ruff format --check`, `uv run pyright`, `uv run vulture` aus `backend/` sind gruen.
- `uv run python -m pytest tests/test_upgrade_compatibility.py tests/test_index_open.py tests/test_lockstep_versions.py tests/test_search_fields_lockstep.py -q` ist gruen: Erfolgskriterium 4 haelt trotz bewegtem Pin.
- Beide YAML-Dateien parsen; der `ignore`-Block ist unveraendert, sein Kommentar berichtigt.
- `git diff --stat` listet genau die sechs Dateien dieses Plans.
- Nach dem Merge: der Lauf von `deploy-harp.yml` ist gruen, und der Job "Store upgrade 5" meldet kein Reindex-Banner und keine Zeile "built by different code". Die Laufnummer gehoert ins SUMMARY und in den Vollzugsabschnitt von `17-GRUNDSATZ-ENTSCHEID.md`.
</verification>

<success_criteria>
`tantivy` steht auf 0.26.2, die Panic ist eine Ausnahme geworden, keine Bestandsinstallation reindiziert, und jede Begruendung im Baum deckt sich mit dem Messbericht.
</success_criteria>

<output>
Create `.planning/phases/17-owner-tor-und-analyseketten/17-08-SUMMARY.md` when done
</output>
