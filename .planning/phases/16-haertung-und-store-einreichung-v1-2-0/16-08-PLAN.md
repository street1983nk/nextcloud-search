---
phase: 16-haertung-und-store-einreichung-v1-2-0
plan: 08
type: execute
wave: 3 # bewusst hinter Welle 2: Bauordnung A1 vor A4, keine technische Abhaengigkeit
depends_on: [16-01]
files_modified:
  - .github/workflows/integration.yml
  - docs/measurements/2026-09-a4-sprachfaelle-ci/README.md
  - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/deferred-items.md
autonomous: false
requirements: [A4]
auflagen: [A4]

must_haves:
  truths:
    - "Die zehn Sprachfaelle laufen auf arm64 gegen eine frische Instanz ohne Fremdbestand"
    - "Der Beleg nennt Laufnummer, Maschine, Korpusgroesse und je Fall das Ergebnis"
    - "Wenn der CI-Weg die Messung nicht traegt, liegt der Fehlschlag als Beleg vor, bevor ueber eine Box gesprochen wird"
    - "Die Box startet nur nach Owner-Freigabe, und das Rechenblatt liegt dabei"
  artifacts:
    - path: ".github/workflows/integration.yml"
      provides: "arm64-Ast des Auftrags index-search-e2e"
      contains: "ubuntu-24.04-arm"
    - path: "docs/measurements/2026-09-a4-sprachfaelle-ci/README.md"
      provides: "Der Beleg der zehn Faelle ohne Fremdbestand, mit Laufnummer"
      contains: "ohne Fremdbestand"
  key_links:
    - from: "docs/measurements/2026-09-a4-sprachfaelle-ci/README.md"
      to: ".github/workflows/integration.yml"
      via: "Der Beleg nennt den Auftrag und die Laufnummer, aus der er stammt"
      pattern: "index-search-e2e"
---

<objective>
Auflage A4 und Erfolgskriterium 4 der Phase 15: die Sprachfall-Messung ohne
Fremdbestand. Entscheid E3 der 16-CONTEXT.md gibt den Weg vor: **CI zuerst, Box
als Fallback.**

Purpose: Fuenf von zehn Faellen hiessen in Phase 15 "nicht messbar", weil die
eigene Datei ausserhalb der 64 Rechecks lag, verduennt durch rund 52.000
Fremddokumente mit denselben Woertern. Der Korpus-Snapshot IST der Fremdbestand.
Eine frische Instanz hat ihn nicht.

Output: ein arm64-Ast, ein Beleg, und ein Owner-Checkpoint mit zwei Zweigen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md
@docs/audits/2026-09-phase-15/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der arm64-Ast des Auftrags index-search-e2e</name>
  <files>.github/workflows/integration.yml</files>
  <read_first>
    - .github/workflows/integration.yml, Zeilen 952 bis 1060 (der Auftrag `index-search-e2e`, seine Matrix ueber `database`, `server-version` und `php-version`, das feste `runs-on: ubuntu-24.04`, die Dienstcontainer und der Zeitdeckel von 45 Minuten) und Zeilen 1408 bis 1560 (der Schritt "The ten German language cases, as the owner" mit allen zehn Faellen)
    - .github/actions/setup-test-nc/action.yml (die zusammengesetzte Aktion; was sie an Architektur voraussetzt)
    - .github/workflows/docker.yml und .github/workflows/python.yml, je die Stelle, an der `ubuntu-24.04-arm` benutzt wird (die Hausform des ARM-Asts in diesem Repositorium, einschliesslich `role: target`)
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md, Abschnitt "A4: die kleine Sprachfaelle-Anfahrt", Weg A mit seinem Vorbehalt aus 14-02
  </read_first>
  <action>
Erweitere den Auftrag `index-search-e2e` um genau einen arm64-Ast.

Bauform:

- `runs-on` wird zu `${{ matrix.runner }}`, und die Matrix bekommt die Achse
  `runner: ['ubuntu-24.04']`, damit die drei bestehenden Kombinationen
  unveraendert laufen.
- Ein `include`-Eintrag ergaenzt genau eine Kombination:
  `database: sqlite`, `server-version: stable34`, `php-version: '8.2'`,
  `runner: ubuntu-24.04-arm`. Nur eine, und mit sqlite: was dieser Ast
  beantwortet, ist eine Frage nach Suchqualitaet auf ARM und keine nach
  Datenbankdialekten.
- `name:` wird um den Runner erweitert, damit die vier Kombinationen in der
  Laufuebersicht unterscheidbar sind.
- Ueber der neuen Zeile steht ein Kommentar, der drei Dinge sagt: warum der Ast
  existiert (Auflage A4, Erfolgskriterium 4, Befund L-09), was er beweist (die
  zehn Faelle auf ARM64 gegen eine frische Instanz ohne Fremdbestand,
  wiederholbar und dauerhaft), und **was er nicht beweist** (keine echte
  Nextcloud mit AppAPI und HaRP auf einer m7g.large, keine harte Speichergrenze;
  der Runner ist eine Vier-Kern-Maschine derselben Flotte, Vorbehalt aus 14-02).

**Vor dem Push**, nach der Lehre aus 11-07: der geaenderte Matrixblock wird
lokal gegen den nachgebauten Zustand gefahren, also mindestens als
YAML-Auswertung mit einer Ausgabe der vier erzeugten Kombinationen. Eine
Workflow-Aenderung, die zum ersten Mal in CI ausgewertet wird, kostet einen Lauf
je Tippfehler.

**Achtung, eine Nebenwirkung, die zu pruefen ist:** der geaenderte `name:`
aendert die Namen der Pruefungen. Wenn im Repositorium erforderliche Pruefungen
auf die alten Namen zeigen, wird das in der SUMMARY benannt und dem Owner
gemeldet, statt es stillschweigend zu lassen.
  </action>
  <verify>
    <automated>grep -c "ubuntu-24.04-arm" .github/workflows/integration.yml && python -c "import yaml; d=yaml.safe_load(open('.github/workflows/integration.yml',encoding='utf-8')); m=d['jobs']['index-search-e2e']['strategy']['matrix']; print(m); assert any('arm' in str(v) for v in str(m)), 'kein arm-Eintrag'; print('matrix ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei parst als YAML und der Befehl oben endet mit `matrix ok`.
    - Die Matrix erzeugt genau vier Kombinationen: drei auf `ubuntu-24.04`, eine auf `ubuntu-24.04-arm` mit sqlite.
    - `runs-on` liest den Runner aus der Matrix, und keine bestehende Kombination hat ihren Runner gewechselt.
    - Der Kommentar nennt A4, L-09 und den Vorbehalt aus 14-02 einschliesslich des Satzes, was der Ast nicht beweist.
    - Der Zeitdeckel des Auftrags ist geprueft und, falls der ARM-Ast langsamer ist, mit Begruendung angehoben; eine Aenderung ohne Begruendung ist keine.
    - Keine Umlaute, keine Em-Dashes in der YAML-Datei.
  </acceptance_criteria>
  <done>Die zehn Faelle haben einen ARM-Ast, und was er nicht beweist, steht neben ihm.</done>
</task>

<task type="auto">
  <name>Task 2: Den Ast fahren und den Beleg schreiben</name>
  <files>docs/measurements/2026-09-a4-sprachfaelle-ci/README.md</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/rohdaten/05-sprachfaelle.txt (die Bilanz der Phase 15: 5 von 10 bei 5 nicht messbaren, und warum)
    - docs/measurements/2026-09-werkzeugfixe/README.md (die Hausform eines kurzen Messberichts dieses Projekts)
    - docs/audits/2026-09-phase-15/README.md, Befund L-09
    - Die Ausgabe des tatsaechlichen CI-Laufs (`gh run view <nummer> --log`), Schritt "The ten German language cases, as the owner"
  </read_first>
  <action>
Stosse den Lauf an (Push auf den Arbeitszweig genuegt, der Auftrag laeuft auf
Pull Requests und auf main) und lies den arm64-Ast aus.

**Die Regel der Phase fuer einen roten Ast gilt auch hier:** die erste Handlung
bei rot ist die Wiederholung. Erst wenn sie Zeile fuer Zeile dasselbe liefert,
ist es ein Befund. Beide Laufnummern wandern in den Beleg.

Schreibe `docs/measurements/2026-09-a4-sprachfaelle-ci/README.md`. Inhalt, in
dieser Reihenfolge:

1. **Was gemessen wurde und warum**: Auflage A4, Erfolgskriterium 4 der Phase
   15, Befund L-09; die zehn deutschen Sprachfaelle gegen eine frische Instanz,
   deren einziger Bestand der Korpus ist.
2. **Die Bedingungen**: Laufnummer, Datum, Runner (`ubuntu-24.04-arm`),
   Architektur, Serverfassung, Datenbank, Abbild beziehungsweise Baustand des
   Containers, Korpus (`testdata/corpus`, 39 Dateien, rund 500 KB) und der Satz,
   dass kein Fremdbestand auf dieser Instanz liegt.
3. **Die zehn Faelle**, je mit Begriff, erwartetem Titel, Trefferzahl und
   Urteil. Kein Fall bekommt ein weicheres Wort, als sein Ergebnis hergibt.
4. **Die Gegenueberstellung zur Phase 15**: welche der fuenf damals nicht
   messbaren Faelle jetzt eine Aussage tragen und welche nicht.
5. **Was dieser Lauf NICHT beweist**, als eigener Abschnitt und Pflicht: keine
   echte Nextcloud mit AppAPI und HaRP auf einer m7g.large, keine harte
   Speichergrenze, keine Zeit- oder Speicheraussage; der Runner ist eine
   Vier-Kern-Maschine derselben Flotte (Vorbehalt aus 14-02). Und: er misst die
   Suchqualitaet, und genau das sind die Sprachfaelle.
6. **Das Urteil zum Weg**, in einem Wort: `traegt` oder `traegt nicht`. Bei
   `traegt nicht` steht darunter, WORAN es liegt, mit der Protokollzeile und der
   Laufnummer, denn das ist der Beleg, den Entscheid E3 fuer die Box verlangt.

Deutsche Prosa in ASCII-Umschrift, Ueberschriften ohne Umlaute, keine
Em-Dashes, kein gesperrtes Wort, keine Kennung, keine Adresse. Das neue
Verzeichnis liegt unter `docs/` und wird deshalb vom Gate aus Plan 16-02
mitgeprueft.
  </action>
  <verify>
    <automated>test -f docs/measurements/2026-09-a4-sprachfaelle-ci/README.md && grep -c "ohne Fremdbestand" docs/measurements/2026-09-a4-sprachfaelle-ci/README.md && grep -cE "traegt( nicht)?" docs/measurements/2026-09-a4-sprachfaelle-ci/README.md && cd backend && uv run pytest tests/test_public_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Bericht existiert und nennt Laufnummer, Runner, Datum, Korpusgroesse und die Abwesenheit von Fremdbestand.
    - Zehn Faelle mit je Trefferzahl und Urteil; die Zahlen stammen aus der Laufausgabe und nicht aus einer Erwartung.
    - Der Abschnitt "Was dieser Lauf nicht beweist" existiert und nennt den Vorbehalt aus 14-02.
    - Das Urteil zum Weg steht als ein Wort da; bei `traegt nicht` liegt die Protokollzeile mit Laufnummer daneben.
    - Bei einem roten Ast sind zwei Laufnummern genannt (Erstlauf und Wiederholung).
    - Das Gate aus Plan 16-02 ist ueber dem neuen Verzeichnis gruen.
  </acceptance_criteria>
  <done>Die zehn Faelle haben eine Zahl von einer Instanz ohne Fremdbestand, oder es liegt schwarz auf weiss vor, warum nicht.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Owner-Checkpoint, zwei Zweige (A4)</name>
  <decision>Ist Auflage A4 mit dem CI-Weg erfuellt, oder wird die kleine Box-Anfahrt gestartet?</decision>
  <context>
Entscheid E3 der 16-CONTEXT.md lautet: CI zuerst, Box als Fallback, **Start nur
bei belegtem CI-Fehlschlag**; der Deckel 7 Stunden und 0,90 USD ist bereits
freigegeben.

Vorzulegen ist, je nach Ausgang von Task 2, genau einer der beiden Zweige.

**Zweig a, der Ast traegt.** Vorgelegt werden: die Bilanz der zehn Faelle mit
Laufnummer, die Gegenueberstellung zu den fuenf in Phase 15 nicht messbaren
Faellen, der Abschnitt "Was dieser Lauf nicht beweist", und der Vorschlag,
Erfolgskriterium 4 der Phase 15 (Befund L-09) damit als erfuellt zu fuehren.
Gefragt wird nach der Kenntnisnahme und der Zustimmung zu diesem Verdikt. Es
wird KEINE Box angeboten und kein Deckel abgerufen.

**Zweig b, der Ast traegt nicht.** Vorgelegt werden: der Fehlschlag mit
Protokollzeile und Laufnummer (beide Laeufe, Erstlauf und Wiederholung), das
Rechenblatt der Box-Anfahrt in der Fassung des Recherchedokuments (sieben
Posten, Summe 5 h 15 min Planwerte, mal 1,15 gerundet auf 7 Stunden, mal
0,115841 USD je Stunde gerundet auf 0,90 USD netto), ausdruecklich als
Schaetzung gekennzeichnet fuer die drei Posten ohne Messung (frische Nextcloud,
Korpus, Abbau), dazu die zwei Hinweise des Rechenblatts (der Stundensatz
enthaelt 100 GB Datentraeger, den eine Box fuer 39 Dateien nicht braucht; die
Snapshotkosten gehoeren nicht in diesen Deckel, sie sind Q5). Gefragt wird nach
der Freigabe des Starts.
  </context>
  <options>
    <option id="zweig-a-erfuellt">
      <name>A4 ueber den CI-Weg erfuellt</name>
      <pros>Kostenlos, dauerhaft, wiederholbar; das Gate bleibt und faengt den naechsten Fall</pros>
      <cons>Keine echte Nextcloud mit AppAPI und HaRP, keine m7g.large, keine harte Speichergrenze</cons>
    </option>
    <option id="zweig-b-box">
      <name>Box-Anfahrt starten, Deckel 7 h / 0,90 USD</name>
      <pros>Genau das, was die Auflage woertlich verlangt</pros>
      <cons>Kosten, Zeit, und nichts davon ist dauerhaft: die naechste Frage kostet wieder. Die Anfahrt bekommt dann eigene Plaene und faehrt NIE zwischen Tag und Einreichung</cons>
    </option>
  </options>
  <read_first>
    - docs/measurements/2026-09-a4-sprachfaelle-ci/README.md (der Beleg aus Task 2)
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md, Entscheid E3
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md, der Rechenblatt-Entwurf fuer Weg B
    - docs/runbook-messbox.md, Abschnitt 2 (Rechenweg, Kostensaetze, die Regel, dass die Ist-Werte der Phase 15 die Untergrenze je Posten sind)
  </read_first>
  <acceptance_criteria>
    - Dem Owner ist genau ein Zweig vorgelegt worden, und welcher, entscheidet der Beleg aus Task 2 und nicht die Bequemlichkeit.
    - In Zweig b liegt der CI-Fehlschlag mit Laufnummer und Protokollzeile vor der Frage, nicht danach.
    - Die Antwort des Owners steht im Wortlaut in der SUMMARY.
    - Bei Zweig b wird KEINE Box in diesem Plan gestartet: die Anfahrt bekommt eigene Plaene, und der Merker steht in der SUMMARY, dass sie nie zwischen Tag und Einreichung faehrt.
    - Bei Zweig a ist L-09 in `deferred-items.md` als geschlossen vermerkt, mit Verweis auf Laufnummer und Bericht.
  </acceptance_criteria>
  <resume-signal>Antwort des Owners: "zweig a" (erfuellt, keine Box) oder "zweig b" (Box freigegeben) oder ein eigener Wortlaut</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Runner zu Testinstanz | Ein neuer Ast installiert eine ganze Nextcloud und faehrt einen OCR-Durchlauf |
| Owner-Freigabe zu Anbieter-Rechnung | Ohne Freigabe hat eine Anfahrt nie stattgefunden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-28 | Denial of Wallet | Eine Box startet ohne Beleg, dass der kostenlose Weg nicht traegt | mitigate | Blockierender Checkpoint, Zweig b nur mit Laufnummer und Protokollzeile des Fehlschlags |
| T-16-29 | Denial of Service | Der neue Ast sprengt den Zeitdeckel und haelt einen Runner | mitigate | Zeitdeckel des Auftrags geprueft und, wenn angehoben, begruendet |
| T-16-30 | Repudiation | Ein Ergebnis auf einem fremden Maschinentyp wird fuer die Zielhardware gehalten | mitigate | Der Abschnitt "Was dieser Lauf nicht beweist" ist Pflichtbestandteil des Berichts |
| T-16-31 | Information Disclosure | Kennung oder Adresse im neuen Messverzeichnis | mitigate | Das Gate aus Plan 16-02 laeuft ueber `docs/` und damit ueber dieses Verzeichnis |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neue Abhaengigkeit |
</threat_model>

<verification>
1. Vier Matrixkombinationen, eine davon auf arm64.
2. Der Lauf ist gefahren und ausgelesen, bei rot mit Wiederholung.
3. Der Bericht traegt Bedingungen, zehn Urteile, die Gegenueberstellung und den Nicht-Beweis-Abschnitt.
4. Der Owner hat genau einen Zweig vorgelegt bekommen und geantwortet; die Antwort steht im Wortlaut.
</verification>

<success_criteria>
Erfolgskriterium 4 der Phase 15 hat entweder seine Zahl von einer Instanz ohne
Fremdbestand, oder der Owner hat auf belegter Grundlage ueber die Box
entschieden.
</success_criteria>

<output>
Create `.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-08-SUMMARY.md` when done
</output>
