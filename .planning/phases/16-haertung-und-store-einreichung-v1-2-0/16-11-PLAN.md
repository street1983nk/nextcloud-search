---
phase: 16-haertung-und-store-einreichung-v1-2-0
plan: 11
type: execute
wave: 5
depends_on: [16-07, 16-10]
files_modified:
  - docs/store-listing.md
autonomous: false
requirements: [HART-02, REL-02]
auflagen: []

must_haves:
  truths:
    - "Die sechs Store-Texte liegen dem Owner vor dem Release als Entwurf vor, an einer Stelle und nebeneinander"
    - "Die neue Messzahl 731,9 MB steht mit ihrer Messgroesse da und nicht als blosse Zahl"
    - "Der alte Wert bleibt fuer die Bedingungen gueltig, unter denen er entstand, und bekommt einen datierten Eintrag"
    - "Die Kurztext-Regel ist eingehalten: hoechstens eine Messzahl je Store-Text"
  artifacts:
    - path: "docs/store-listing.md"
      provides: "Die sechs Texte im Entwurf, das Aenderungsprotokoll und die Fundstellenliste der Messzahl"
      contains: "731,9"
  key_links:
    - from: "docs/store-listing.md"
      to: "php/appinfo/info.xml"
      via: "die Quelle, aus der beide info.xml woertlich beziehen (Uebernahme in Plan 16-12)"
      pattern: "RAM"
---

<objective>
Der Textentwurf. Die neue Messzahl aus Entscheid E1 geht durch
`docs/store-listing.md`, weil dort alle sechs Texte nebeneinander stehen, und
von dort in einem zweiten Plan woertlich in beide `info.xml`. Am Ende dieses
Plans steht der Owner-Checkpoint.

Purpose: Owner-Regel vom 07.09.2026: der Store-Text reist mit dem Release und
ist danach nicht mehr editierbar, also wird der Entwurf VORHER gezeigt. Und
Entscheid E1 ist gesperrt: **731,9 MB nach Nutzung ersetzt die alte 103,2 MB an
allen neun Stellen.**

Output: ein Entwurf, ein Aenderungsprotokoll, eine Owner-Entscheidung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der Entwurf der sechs Texte und der drei README-Zeilen</name>
  <files>docs/store-listing.md</files>
  <read_first>
    - docs/store-listing.md, vollstaendig (die Regeltabelle oben, die sechs Texte beider Haelften, die Messzahl-Tabelle und das Aenderungsprotokoll am Ende)
    - php/appinfo/info.xml, Zeilen 40 bis 100, und backend/appinfo/info.xml, Zeilen 85 bis 140 (die sechs Texte, wie sie heute ausgeliefert sind, einschliesslich der RAM-Zeile mit 103,2 MB und des Connector-Satzes)
    - README.en.md Zeilen 30 bis 38, README.md Zeilen 31 bis 39, README.fr.md Zeilen 35 bis 45 (die Spitzen-Zeile und die Grundlast-Zeile, je dreisprachig)
    - backend/tests/test_store_metadata.py, Zeilen 294 bis 316 (MEASURED_SENTENCE, MEASURED_SENTENCE_DE, MEASURED_SENTENCE_FR) und der Kommentar darueber
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md, Entscheid E1 im Wortlaut
    - .planning/REQUIREMENTS.md, MEM-02 in der Traceability-Tabelle (die Zahl, ihre Messgroesse, ihre Rohdatei, ihr Datum, ihre Maschine)
    - .planning/BACKLOG.md, BL-F01 (der Stand-Absatz, der seit 07.09.2026 ueberholt ist)
    - Die SUMMARY von Plan 16-10 (drei oder neun OCR-Sprachen)
  </read_first>
  <action>
Schreibe den Entwurf in `docs/store-listing.md`. Geaendert wird an vier Stellen,
und jede hat ihre eigene Regel.

**1. Die RAM-Zeile der sechs Store-Texte.** Heute:
`- RAM: 4 GB is enough, 103.2 MB idle, under a hard 2 GB limit (measured)` und
die zwei Uebersetzungen. Neu: dieselbe Zeile mit **731,9 MB** und mit der
Messgroesse statt des Wortes "idle". Die Zahl ist der residente Stand des
Containers, nachdem einmal eingebettet und wieder entladen wurde (Rohdatei 94b,
21.09.2026, m7g.large/aarch64, ausgeliefertes v1.2-Abbild). Der Text nennt die
Messgroesse in drei bis vier Woertern, nicht in einem Satz: der Store-Text ist
eine Faktenliste, keine Messgeschichte.

**Die Kurztext-Regel ist bindend**: hoechstens EINE Messzahl je Store-Text. Die
4 GB und die 2-GB-Grenze sind Anforderungen und keine Messzahlen, sie bleiben.
Eine zweite Messzahl kommt nicht dazu, auch nicht die 103,2.

**2. Die Grundlast-Zeile der drei READMEs.** Heute steht dort ein Vergleich
(691,8 MB in v1.0 auf 103,2 MB in v1.1, minus 85,1 Prozent). **Dieser Vergleich
traegt die neue Zahl nicht**, weil 731,9 MB eine ANDERE Messgroesse ist: 103,2
war die Grundlast im Leerlauf, 731,9 ist der residente Stand nach einem
Indexlauf mit entladenem Modell. Eine Prozentzahl zwischen zwei verschiedenen
Messgroessen waere falsch, und zwar in die guenstige Richtung falsch.

Der Entwurf ersetzt die Zeile deshalb durch eine, die die neue Zahl mit ihrer
Messgroesse, ihrem Datum, ihrer Maschine und dem Verweis auf
`docs/performance.md` nennt, dreisprachig, in der Zahlenschreibweise jeder
Sprache (731,9 MB deutsch, 731.9 MB englisch, 731,9 Mo franzoesisch). Der alte
Vergleich verschwindet nicht aus der Welt: er steht weiter in
`docs/performance.md` mit den Bedingungen, unter denen er entstand, und im
Aenderungsprotokoll dieser Datei.

**3. Der Spitzen-Satz der drei READMEs bleibt unveraendert.** Er nennt 52.111
Dokumente und 1.764 MB aus der v1.1-Anfahrt. Der v1.2-Lauf hat die Spitze eines
Volllaufs NICHT neu gemessen; wer die Dokumentzahl nachzoege, ohne die Spitze
mitzunehmen, mischte zwei Laeufe in einem Satz. Das steht als Satz im
Aenderungsprotokoll, damit der naechste Leser nicht denselben Griff versucht.

**4. Die OCR-Sprachen**, falls Plan 16-10 mit "mitfahren" geendet hat: die sechs
Texte nennen dann neun Sprachen statt drei, dreisprachig. Hat 16-10 mit
"folgerelease" geendet, bleibt es bei drei und dieser Punkt entfaellt.

**Das Aenderungsprotokoll** am Ende der Datei bekommt seinen Eintrag: Datum,
Plan, was sich aendert, welche Zahl von welcher abgeloest wird, unter welcher
Messgroesse die alte gueltig bleibt, und der Verweis auf Entscheid E1.

**Die Textregeln der Datei gelten unveraendert**: Laengengrenze 128 fuer `name`
und `summary`, kein leeres Element, Sprachcodes `de` und `fr` (nie `de_DE`), je
Elementart jede Sprache genau einmal, keine Gedankenstriche, keine Emojis, keine
Backticks und keine Tabellen in einer Beschreibung, genau EIN Satz Querverweis
auf den Connector, kein gesperrtes Wort.
  </action>
  <verify>
    <automated>grep -c "731,9" docs/store-listing.md && grep -c "731.9" docs/store-listing.md && cd backend && uv run pytest tests/test_store_metadata.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/store-listing.md` traegt die neue Zahl in allen sechs Store-Texten und in den drei README-Zeilen, je in der Zahlenschreibweise der Sprache.
    - Je Store-Text steht genau eine Messzahl; die 103,2 kommt in keinem der sechs Texte mehr vor.
    - Die RAM-Zeile nennt die Messgroesse und nicht mehr das Wort "idle" beziehungsweise "im Leerlauf".
    - Der Spitzen-Satz (52.111 Dokumente, 1.764 MB) ist zeichengleich unveraendert.
    - Das Aenderungsprotokoll traegt den neuen Eintrag mit Datum, Plan, Messgroesse und Verweis auf E1.
    - Keine Backticks und keine Tabelle in einer Beschreibung, kein Gedankenstrich, kein Emoji, kein gesperrtes Wort.
    - `uv run pytest tests/test_store_metadata.py -q` ist gruen (die Datei ist Teil des Katalog-Gates).
  </acceptance_criteria>
  <done>Die sechs Texte und die drei README-Zeilen stehen im Entwurf an einer Stelle, mit der Zahl, die ein Selfhoster auf seiner 4-GB-Box wirklich sieht.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner-Checkpoint, Textentwurf und die Store-Bilder</name>
  <what-built>
Der Entwurf der sechs Store-Texte und der drei README-Zeilen liegt in
`docs/store-listing.md`. Ausgeliefert ist noch nichts: beide `info.xml` und die
drei README-Dateien traegt erst Plan 16-12, nach dieser Abnahme.
  </what-built>
  <how-to-verify>
1. `docs/store-listing.md` oeffnen und die sechs Texte lesen, in allen drei
   Sprachen, beide Haelften. Der Store-Text reist mit dem Release und ist danach
   NICHT editierbar.
2. Die neue Zahl pruefen: **731,9 MB**, residenter Stand nach einem Indexlauf
   mit entladenem Modell, gemessen am 21.09.2026 auf m7g.large/aarch64 gegen das
   ausgelieferte v1.2-Abbild, Rohdatei 94b. Sie ersetzt 103,2 MB an allen neun
   Stellen (Entscheid E1).
3. Den Punkt lesen, der KEINE Entscheidung mehr ist, aber Folgen hat: die alte
   Zeile der READMEs verglich 691,8 MB mit 103,2 MB und nannte minus 85,1
   Prozent. Dieser Vergleich faellt weg, weil 731,9 MB eine andere Messgroesse
   ist. Der alte Wert bleibt in `docs/performance.md` gueltig fuer die
   Bedingungen, unter denen er entstand.
4. Den Spitzen-Satz stehen lassen oder nicht: er nennt 52.111 Dokumente und
   1.764 MB aus der v1.1-Anfahrt. Der Vorschlag ist, ihn unveraendert zu lassen,
   weil der v1.2-Lauf die Spitze nicht neu gemessen hat.
5. Die OCR-Sprachen im Text gegen den Ausgang von Plan 16-10 halten: drei oder
   neun.

**Zusaetzlich zu entscheiden, offene Frage Q-6 der Recherche: die Store-Bilder.**
Die drei Bilder in `store/media/` stammen vom 07.09.2026, also aus der
v1.0.0-Zeit. Seitdem hat das Produkt drei sichtbare Aenderungen bekommen: die
eigene Ergebnisseite mit Navigationseintrag (Phase 9), die Filter- und
Sortierzeile (Phase 13) und den sechsten Engine-Zustand auf der Admin-Seite
(Phase 14). `screenshot-admin.png` zeigt die Admin-Seite in ihrem damaligen
Stand. Neue Bilder sind aufwendig (Wegwerf-Aufbau, eigener erzeugter Bestand,
Playwright, Sichtprobe) und sind kein Erfolgskriterium dieser Phase.

Zur Wahl stehen:
- **a) Bilder bleiben.** Kein Aufwand, aber die Admin-Seite im Store zeigt einen
  Stand, den v1.2 so nicht mehr hat.
- **b) Bilder werden erneuert.** Eigener Plan in Welle 6, vor der Abgabe.

Unabhaengig davon wird in jedem Fall die Groessentabelle in
`store/media/README.md` nachgezogen: sie nennt bis heute die Groessen der
abgeloesten Dateien, und diese Wiedervorlage steht seit dem 07.09.2026 offen.
  </how-to-verify>
  <read_first>
    - docs/store-listing.md nach Task 1
    - store/media/README.md, Abschnitt "Die Live-Bestaetigung der Adressen, 07.09.2026" mit dem Vermerk zur Wiederholung
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md, Abschnitte "Die Store-Bilder" und "Pitfall 6"
  </read_first>
  <acceptance_criteria>
    - Der Owner hat die sechs Texte und die drei README-Zeilen gelesen und ihre Abnahme im Wortlaut erteilt oder Aenderungen benannt.
    - Die Entscheidung zu den Store-Bildern (a oder b) steht im Wortlaut in der SUMMARY.
    - Bei b ist ein eigener Plan fuer die Bilder angekuendigt und in der SUMMARY als Auftrag an Welle 6 vermerkt.
    - Wird eine Textaenderung gewuenscht, wird sie in `docs/store-listing.md` eingearbeitet und erneut vorgelegt; erst danach gilt die Abnahme.
    - Die Abnahme liegt VOR jeder Aenderung an einer `info.xml` oder einer README-Datei.
  </acceptance_criteria>
  <resume-signal>Antwort des Owners: Abnahme der Texte im Wortlaut, plus "bilder bleiben" oder "bilder neu"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositorium zu Store | Der Store-Text reist mit dem Release und ist danach nicht mehr editierbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-40 | Spoofing | Eine Prozentzahl zwischen zwei verschiedenen Messgroessen behauptet eine Verbesserung, die es nicht gibt | mitigate | Der Vergleich faellt weg; die neue Zahl steht mit ihrer Messgroesse da, und der alte Wert bleibt fuer seine Bedingungen gueltig |
| T-16-41 | Repudiation | Eine ersetzte Zahl ohne Nachtrag laesst nicht mehr erkennen, was frueher gemessen wurde | mitigate | Aenderungsprotokoll in `docs/store-listing.md` plus die datierten Nachtraege in `docs/performance.md` |
| T-16-42 | Tampering | Ein Text laeuft in einer Sprache oder in einer Haelfte auseinander | mitigate | Alle sechs Texte stehen in einer Datei nebeneinander; die Uebernahme in Plan 16-12 ist woertlich, und Gates halten sie |
| T-16-43 | Denial of Service | Ein leeres Element beendet den Upload mit einem Serverfehler | mitigate | Die Regeltabelle der Datei und `test_store_metadata.py` prueft es vor der Abgabe |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Abhaengigkeit |
</threat_model>

<verification>
1. Die neue Zahl steht an allen neun Stellen des Entwurfs, je in der Schreibweise der Sprache.
2. Je Store-Text genau eine Messzahl.
3. Der Spitzen-Satz ist unveraendert.
4. Das Aenderungsprotokoll traegt den Eintrag.
5. Die Owner-Antwort liegt im Wortlaut vor, einschliesslich der Bilder-Entscheidung.
</verification>

<success_criteria>
Der Owner hat die Texte gesehen, bevor sie unwiderruflich werden, und die Zahl
im Store ist die, die ein Selfhoster auf einer 4-GB-Box wirklich sieht.
</success_criteria>

<output>
Create `.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-11-SUMMARY.md` when done
</output>
