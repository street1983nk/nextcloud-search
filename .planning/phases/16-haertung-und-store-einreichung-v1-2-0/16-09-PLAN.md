---
phase: 16-haertung-und-store-einreichung-v1-2-0
plan: 09
type: execute
wave: 4
depends_on: [16-07]
files_modified:
  - .github/workflows/deploy-harp.yml
  - backend/tests/test_upgrade_compatibility.py
  - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/deferred-items.md
autonomous: true
requirements: [REL-02]
auflagen: []

must_haves:
  truths:
    - "Der Upgrade-Beweis springt von v1.1.0 auf den Baum und laeuft nicht in ERROR_UP_TO_DATE"
    - "Keine Zusicherung behauptet mehr eine Eigenschaft von 1.0.3"
    - "Zusicherung 6 misst einen sichtbaren Unterschied, den v1.2 gegenueber v1.1 macht"
    - "Die Suche ist nach dem Sprung nicht stumm: gleiche Treffer, gleiche Marken, kein Reindex-Banner"
  artifacts:
    - path: ".github/workflows/deploy-harp.yml"
      provides: "Upgrade-Beweis 1.1.0 auf 1.2.0 mit neuer sechster Zusicherung"
      contains: "v1.1.0"
    - path: "backend/tests/test_upgrade_compatibility.py"
      provides: "Die Ratsche der fuenf Marken, benannt fuer 1.0.x und 1.1.x"
      contains: "GOLD"
  key_links:
    - from: ".github/workflows/deploy-harp.yml"
      to: "php/lib/Migration/"
      via: "occ upgrade fuehrt die Migration des Minor-Sprungs wirklich aus"
      pattern: "Version001200"
---

<objective>
Der Upgrade-Beweis erzaehlt heute an drei Stellen von 1.0.3. Er wird auf den
Sprung 1.1.0 auf 1.2.0 umgestellt, und seine sechste Zusicherung bekommt einen
neuen Gegenstand, weil ihr alter (der Navigationseintrag) ab 1.1.0 schon
vorhanden ist.

Purpose: Erfolgskriterium 3 von REL-02. Und die Lehre aus 11-11: ein Beweis, der
zum ersten Mal wirklich greift, findet Fehler. Dieser Plan faehrt den zweiten
echten Minor-Sprung, diesmal mit geaenderter Ausgangsversion und neu zu bauender
Zusicherung. **Er plant Zeit fuer Befunde ein und nicht nur fuer die
Umstellung.**

Output: ein Beweis, der einen echten Sprung faehrt, und eine Ratsche, deren
Namen die Wahrheit sagen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-CONTEXT.md
@.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nachsehen, was Zusicherung 6 tragen kann (Q-5)</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml, Zeilen 2600 bis 2800 (die Hilfsfunktionen index_report, term_hits, navigation_ids, reindex_banner, die Funktion snapshot mit allen Feldern, die sie schreibt, und die zwei Vorbedingungen des Vorher-Zustands)
    - .github/workflows/deploy-harp.yml, das eingebettete Skript overview.sh, das reindex_banner ruft (welche Route es abfragt und welche Felder die Antwort traegt)
    - php/lib/Service/AdminViewService.php und php/templates/admin.php (der sechste Engine-Zustand unloaded aus Plan 14-09: wo er entsteht und ob er in der Antwort steht, die overview.sh liest)
    - Die Route des Containers, die engineState liefert, und die Frage, ob 1.1.0 dieses Feld ueberhaupt kannte (git show v1.1.0 auf die betroffene Datei)
  </read_first>
  <action>
Beantworte die offene Frage Q-5 der Recherche **durch Nachsehen und nicht durch
Raten**: welcher sichtbare Unterschied zwischen 1.1.0 und 1.2.0 ist im
Zustandsabbild ablesbar, das `snapshot()` heute schreibt?

Zwei Kandidaten, in dieser Reihenfolge pruefen:

1. **Der sechste Engine-Zustand** (`unloaded`, Phase 14). Er kommt ueber die
   Admin-Antwort, die `overview.sh` ohnehin abfragt. Zu pruefen ist zweierlei:
   ob das Feld in dieser Antwort steht, und ob die alte Haelfte (1.1.0, mit dem
   Container 1.1.0) es sicher NICHT fuehrt. Nur dann ist der Unterschied eine
   Aussage.
2. **Die Filter- und Sortierzeile der Ergebnisseite** (Phase 13). Sie lebt in
   `php/templates/search.php` und im `PageController`; ein Abbild muesste die
   Seite rendern, und das ist der teurere Weg.

Halte das Ergebnis als Kommentar im Workflow fest, an der Stelle, an der
`snapshot()` erweitert wird: welcher Kandidat es wird, WARUM, und was am anderen
nicht getragen haette. Erweitere `snapshot()` um genau das eine Feld, das den
Unterschied traegt, in derselben Bauform wie die uebrigen Felder, also mit einer
Hilfsfunktion, die bei fehlender Antwort mit einer Meldung zurueckkehrt statt
einen Wert zu raten.

Dieser Task aendert noch keine Zusicherung; er stellt die Zutat bereit und
schreibt auf, warum sie die richtige ist.
  </action>
  <verify>
    <automated>grep -c "Q-5" .github/workflows/deploy-harp.yml</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei parst weiterhin als YAML (Pruefung mit einem YAML-Leser vor dem Commit, Ergebnis in der SUMMARY).
    - `snapshot()` schreibt ein zusaetzliches Feld, das den v1.2-eigenen Unterschied traegt.
    - Ein Kommentar nennt Q-5, den gewaehlten Kandidaten, die Begruendung und den Grund gegen den anderen.
    - Das neue Feld hat eine Hilfsfunktion, die bei fehlender Antwort mit Meldung zurueckkehrt statt einen Wert zu raten.
    - Es ist an der Quelle nachgesehen, dass 1.1.0 das Feld nicht fuehrt; der Beleg steht als Dateiname und Zeile beziehungsweise als git-Befehl im Kommentar.
  </acceptance_criteria>
  <done>Die sechste Zusicherung hat einen Gegenstand, und er ist nachgesehen und nicht geraten.</done>
</task>

<task type="auto">
  <name>Task 2: Die vier Stellen, die von 1.0.3 erzaehlen</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml, Zeilen 80 bis 95 (UPGRADE_FROM_TAG mit seinem Begruendungsabsatz), 2440 bis 2475 (die Vorbedingung im Schritt "Store upgrade 1", die den fehlenden Navigationsblock verlangt), 2776 bis 2800 (die zweite Vorbedingung des Vorher-Zustands) und 3190 bis 3225 (Zusicherung 6)
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md, Abschnitte "Der Upgrade-Beweis" und "Pitfall 2"
    - .planning/milestones/v1.1-phases/11-haertung-und-store-einreichung-v1-1/11-11-SUMMARY.md (was der Beweis beim ersten echten Sprung gefunden hat)
  </read_first>
  <action>
Stelle den Beweis auf den Sprung `v1.1.0` auf den Baum um. Vier Stellen, in
einem Commit:

1. **`UPGRADE_FROM_TAG: v1.1.0`.** Der Begruendungsabsatz darueber wird
   fortgeschrieben und nicht ueberschrieben: er sagt, warum v1.1.0 der richtige
   Ausgangspunkt ist (es ist die neueste Veroeffentlichung, es sind die Bytes,
   die bestehende Installationen wirklich halten, und `store-submit.yml` hat
   genau sie eingereicht). Der alte Absatz zu v1.0.3 bleibt als datierter
   Vorgaenger darunter stehen.
2. **Die Vorbedingung in "Store upgrade 1"** behauptet heute, die alte
   Begleit-App trage keinen `<navigations>`-Block, und bricht sonst ab. Das ist
   ab v1.1.0 falsch herum, denn v1.1.0 traegt ihn. Die Pruefung wird umgedreht
   und behauptet, was jetzt wahr sein muss; ihre Fehlermeldung sagt, was daraus
   folgt. Der alte Wortlaut wird im Kommentar mit Datum als ueberholt vermerkt.
3. **Die zweite Vorbedingung im Vorher-Zustand** verlangt heute, dass
   `navigation.findling` abwesend ist. An ihre Stelle tritt die Abwesenheit des
   NEUEN Feldes aus Task 1. Ohne diesen Tausch waere Zusicherung 6 gruen aus dem
   falschen Grund.
4. **Zusicherung 6** misst ab jetzt das Feld aus Task 1: vorher abwesend
   beziehungsweise alter Wert, nachher vorhanden. Ihre Fehlermeldung nennt den
   Plan, dessen Aenderung auf einer bestehenden Installation ankommen muss, und
   nicht mehr 09-06.

Ausserdem, ohne Verhaltensaenderung: die Kommentare, die 11-11 und 1.1.0 als
Zukunft nennen, werden nachgezogen. Ein Leser soll keine Anleitung fuer ein
Release von vorgestern finden.

**Unberuehrt bleiben** die Zusicherungen 1 bis 5: gleiche Treffer, gleiche fuenf
Marken nach D-04, gleiche Dokumentzahlen, kein Reindex-Banner, keine
`start_rebuild_on_drift`-Zeile. Sie sind versionsunabhaengig formuliert und
gelten unveraendert. Ebenso unberuehrt bleibt der Versionsschritt in "Store
upgrade 4" mit seinen zwei Zweigen aus 11-11.

**Vor dem Push**, Lehre aus 11-07: die geaenderten Schritte werden gegen einen
lokal nachgebauten Zustand gefahren, mindestens die `jq`-Ausdruecke gegen ein
selbst gebautes Vorher- und Nachher-Abbild, je einmal im erwarteten Fall und
einmal im Fehlerfall.
  </action>
  <verify>
    <automated>grep -c "UPGRADE_FROM_TAG: v1.1.0" .github/workflows/deploy-harp.yml</automated>
  </verify>
  <acceptance_criteria>
    - `UPGRADE_FROM_TAG` steht auf `v1.1.0`, und die Datei parst als YAML.
    - Keine aktive Pruefung behauptet mehr eine Eigenschaft von 1.0.3; wo 1.0.3 noch vorkommt, steht es in einem Kommentar mit Datum als Vorgaenger.
    - Die Vorbedingung des Vorher-Zustands prueft die Abwesenheit des neuen Feldes und nicht mehr die des Navigationseintrags.
    - Zusicherung 6 vergleicht das neue Feld vorher und nachher, und ihre Fehlermeldung nennt den richtigen Plan.
    - Die Zusicherungen 1 bis 5 sind zeichengleich unveraendert (`git diff` zeigt in ihrem Block keine Zeile).
    - Die beiden lokal gefahrenen Probefaelle (erwartet und Fehlerfall) sind in der SUMMARY mit ihrer Ausgabe genannt.
  </acceptance_criteria>
  <done>Der Beweis springt von der Version, die die Leute wirklich installiert haben, und seine sechste Zusicherung misst etwas, das es vorher nicht gab.</done>
</task>

<task type="auto">
  <name>Task 3: Die Ratsche nachbenennen und den Lauf auslesen</name>
  <files>backend/tests/test_upgrade_compatibility.py</files>
  <read_first>
    - backend/tests/test_upgrade_compatibility.py, vollstaendig (die fuenf Marken gegen GOLD_V1_0_3, die Literale schema_version, index_version, analyzer_version, das tantivy-Banner, und die Meldungstexte)
    - .planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-RESEARCH.md, Abschnitt "Die Ratsche daneben"
    - Die Ausgabe des tatsaechlichen deploy-harp-Laufs nach Task 2 (gh run view auf den Auftrag deploy-harp, Schritte "Store upgrade 0" bis "Store upgrade 5")
  </read_first>
  <action>
**Teil 1, die Ratsche.** Die fuenf Marken haben sich seit 1.0.3 nicht bewegt,
auch nicht durch die Filter- und Sortierarbeit der Phase 13, und fuer den Sprung
1.1.0 auf 1.2.0 gelten dieselben Werte. Inhaltlich aendert sich also nichts. Was
sich aendert, sind Name und Meldungstexte: die Konstante und die Meldungen sagen
heute "1.0.x" und sollen "1.0.x und 1.1.x" sagen. Der Kommentar bekommt den
Satz, dass ein Wert, der ueber zwei Minor-Spruenge steht, staerker ist als
einer, der ueber einen steht.

**Teil 2, den Lauf auslesen.** Fahre den Auftrag `deploy-harp` und lies alle
sieben Schritte. **Die erste Handlung bei rot ist die Wiederholung**; erst wenn
sie Zeile fuer Zeile dasselbe liefert, ist es ein Befund.

Fuer jeden Befund gilt die Lehre aus 11-11: er wird als Befund behandelt und
nicht als Formalie. Ein Befund im Erzeugnis (nicht im Beweis) bekommt seinen
eigenen Eintrag in `.planning/phases/16-.../deferred-items.md` oder, wenn er
nutzerwirksam ist, einen eigenen Fix in dieser Phase. Die SUMMARY nennt je
Befund: was rot war, was die Wiederholung sagte, was die Ursache war, und wo er
jetzt steht.

Die SUMMARY nennt ausserdem die Laufnummer des gruenen Durchgangs und die sechs
Zusicherungen mit je einem Wort.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_upgrade_compatibility.py -q && uv run ruff check tests && uv run ruff format --check tests && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_upgrade_compatibility.py -q` ist gruen und waehlt dieselbe Zahl Faelle aus wie vorher.
    - Die Konstante und die Meldungstexte nennen 1.0.x und 1.1.x; die fuenf Werte sind unveraendert.
    - Der Auftrag `deploy-harp` ist gefahren, und die SUMMARY nennt die Laufnummer sowie je Zusicherung ein Wort.
    - Bei einem roten Lauf sind zwei Laufnummern genannt und die Ursache benannt; kein roter Lauf ist als Flake abgelegt worden, ohne dass die Wiederholung ihn so ausgewiesen haette.
    - Jeder gefundene Befund hat eine Adresse: Fix in dieser Phase oder Eintrag in `deferred-items.md`.
  </acceptance_criteria>
  <done>Der Sprung 1.1.0 auf 1.2.0 ist Ende zu Ende gefahren, mit sechs Zusicherungen und ohne stumme Suche.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Veroeffentlichtes Release zu bestehender Installation | Was hier gruen ist, faehrt spaeter auf fremden Instanzen mit echten Daten |
| CI-Runner zu Registry | Der Beweis zieht das ausgelieferte Abbild und die ausgelieferten Paketdateien |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-32 | Spoofing | Ein Beweis, der nichts tut, sieht aus wie ein bestandener Beweis (ERROR_UP_TO_DATE) | mitigate | Der Baum steht auf 1.2.0 (Plan 16-07), bevor dieser Beweis faehrt; der Versionsschritt in "Store upgrade 4" nimmt den richtigen Zweig |
| T-16-33 | Tampering | Zusicherung 6 wird gruen aus dem falschen Grund | mitigate | Die Vorbedingung des Vorher-Zustands prueft die Abwesenheit desselben Feldes, das die Zusicherung danach verlangt |
| T-16-34 | Denial of Service | Ein Upgrade laesst die Suche stumm | mitigate | Zusicherungen 1 bis 5 unveraendert: gleiche Treffer, gleiche Marken, kein Reindex-Banner, keine Drift-Zeile |
| T-16-35 | Repudiation | Ein roter Lauf wird als Flake abgelegt, ohne einer zu sein | mitigate | Erst wiederholen, dann suchen; beide Laufnummern in der SUMMARY; das Flake-Register aus Plan 16-01 daneben |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neue Abhaengigkeit |
</threat_model>

<verification>
1. `snapshot()` traegt das neue Feld, und der Kommentar sagt, warum dieses.
2. Vier Stellen umgestellt, Zusicherungen 1 bis 5 zeichengleich.
3. Die Ratsche nennt beide Minor-Reihen, ihre fuenf Werte sind unveraendert.
4. Der Auftrag `deploy-harp` ist gruen gefahren; jeder Befund hat eine Adresse.
</verification>

<success_criteria>
Erfolgskriterium 3 von REL-02 ist belegt: das Upgrade 1.1.0 auf 1.2.0 ist Ende
zu Ende in CI gefahren, mit Migration, und die Suche ist nach dem Sprung nicht
stumm.
</success_criteria>

<output>
Create `.planning/phases/16-haertung-und-store-einreichung-v1-2-0/16-09-SUMMARY.md` when done
</output>
