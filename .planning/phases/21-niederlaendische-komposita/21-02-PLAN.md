---
phase: 21-niederlaendische-komposita
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - .github/workflows/docker.yml
  - THIRD-PARTY.md
  - backend/tests/test_upgrade_compatibility.py
autonomous: true
requirements: [KOMP-01]

must_haves:
  truths:
    - "Das Abbild traegt /usr/share/dict/dutch aus wdutch=1:2.20.19+1-3 und laesst sich ohne Liste oder ohne Lizenztext nicht bauen"
    - "Der Lizenztext der Wortliste reist im Abbild als /usr/local/share/findling/COPYING.wdutch mit Modus 0444"
    - "THIRD-PARTY.md nennt fuer die OpenTaal-Liste CC-BY-3.0 (Bezug ueber Debian wdutch), die Upstream-Wahl BSD-3-Clause und/oder CC BY 3.0 als Anmerkung, Namensnennung OpenTaal und die Kennzeichnung des Artefakts als Bearbeitung (D-04)"
    - "CI prueft am gepushten Abbild Version, Zeilen, Bytes, Lizenzzeile und Modus der niederlaendischen Liste"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "wdutch-Block mit exaktem Pin und fail-closed Tests"
      contains: "wdutch=1:2.20.19+1-3"
    - path: ".github/workflows/docker.yml"
      provides: "Abbild-Gate fuer wdutch"
      contains: "5096240"
    - path: "THIRD-PARTY.md"
      provides: "Abschnitt The Dutch word list"
      contains: "## The Dutch word list"
    - path: "backend/tests/test_upgrade_compatibility.py"
      provides: "Pin-Test fuer wdutch"
      contains: "WDUTCH_PIN"
  key_links:
    - from: "backend/Dockerfile"
      to: "/usr/local/share/findling/COPYING.wdutch"
      via: "install -D -m 0444 aus /usr/share/doc/wdutch/copyright"
      pattern: "COPYING\\.wdutch"
---

<objective>
Das Abbild bekommt die niederlaendische Wortliste als gepinntes Debian-Paket, samt
Lizenztext, CI-Gate und THIRD-PARTY-Abschnitt (Erfolgskriterium 3, Lizenzhaelfte; D-04).

Purpose: Ohne `/usr/share/dict/dutch` im Container kann die Verdrahtung ab Plan 21-05 bei
aktivem nl kein Artefakt bauen und faellt geschlossen. Deshalb kommt das Abbild zuerst, parallel
zu den Vorfragen. Keine Datei unter `backend/src/findling` wird beruehrt, der Baumhash bleibt.

Output: Dockerfile-Block, docker.yml-Gate, THIRD-PARTY.md-Abschnitt, Pin-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-niederlaendische-komposita/21-CONTEXT.md
@.planning/phases/21-niederlaendische-komposita/21-RESEARCH.md
@.planning/phases/21-niederlaendische-komposita/21-PATTERNS.md
@CLAUDE.md

Gemessene Referenzwerte (Research 25.09.2026, amd64 und arm64 byteidentisch):
Paket `wdutch` `1:2.20.19+1-3`, `Architecture: all`, Quelle `dutch` (OpenTaal),
`/usr/share/dict/dutch` 413288 Zeilen, 5096240 Byte, SHA-256
`2e5128e8e7f9a5bdfc427c784c839986b0df1386cc53aef90ed2df71644f3987`, Installed-Size 5021 kB,
`/usr/share/doc/wdutch/copyright` 21777 Byte, `Files: wordlist/*` mit `License: CC-BY-3.0`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: wdutch ins Abbild, fail closed, mit Lizenztext</name>
  <files>backend/Dockerfile, backend/tests/test_upgrade_compatibility.py</files>
  <read_first>
    - backend/Dockerfile (Z. 165-215, der wngerman-Block mit Kommentar)
    - backend/tests/test_upgrade_compatibility.py (Z. 89-120 Konstanten inkl. WNGERMAN_PIN, Z. 348-366 Pin-Tests)
    - .planning/phases/21-niederlaendische-komposita/21-RESEARCH.md (Abschnitte "Installation", "Lizenzlage", Pitfall 6)
  </read_first>
  <action>
    Direkt unter dem wngerman-RUN in `backend/Dockerfile` einen zweiten Block mit eigenem
    Kommentar im Aufbau des deutschen (englische Prosa): gemessene Zahlen (413288 Zeilen,
    5096240 Byte, Architecture all), Pin-Begruendung (die Liste entscheidet die Zerlegung,
    stille Paketaenderung waere stille Aenderung jedes niederlaendischen Treffers), kein
    Laufzeit-Download, Artefakt `dict/nl-full.txt` entsteht beim Start durch
    `findling.index.wordlist_nl` nur bei aktivem nl, Lizenzpflicht CC-BY-3.0 (Hinweis +
    Lizenztext + Namensnennung reisen mit dem Abbild). Der RUN setzt `DEBIAN_FRONTEND=noninteractive`
    (Pitfall 6: dictionaries-common fragt bei zwei Wortlisten per debconf), installiert
    `wdutch=1:2.20.19+1-3` mit `--no-install-recommends`, entfernt `/var/lib/apt/lists/*`, prueft
    `test -s /usr/share/dict/dutch` und `test -s /usr/share/doc/wdutch/copyright` und kopiert per
    `install -D -m 0444 /usr/share/doc/wdutch/copyright /usr/local/share/findling/COPYING.wdutch`.
    Die Epoche `1:` bleibt im Pin (so in der Research gebaut). Keine Aenderung am wngerman-Block.

    In `backend/tests/test_upgrade_compatibility.py` die Konstante
    `WDUTCH_PIN = "wdutch=1:2.20.19+1-3"` neben `WNGERMAN_PIN` und einen Test
    `test_the_dutch_word_list_is_held_through_its_debian_pin` nach dem Muster von
    `test_the_word_list_is_held_through_its_debian_pin`; Docstring nennt die eigene Marke
    `wordlist_hash_nl` als das, was an diesem Paket haengt.

    Pruefung am echten Paket in einem Wegwerf-Container (kein voller Abbild-Bau noetig): ein
    `docker run --rm python:3.13-slim-trixie sh -c '...'` mit genau der RUN-Zeile aus dem
    Dockerfile plus `wc -lc /usr/share/dict/dutch`, `stat -c %a /usr/local/share/findling/COPYING.wdutch`
    und `grep -n "License:" /usr/local/share/findling/COPYING.wdutch | head`. Unter Git-Bash
    `MSYS_NO_PATHCONV=1` voranstellen. Die Ausgabe in die SUMMARY; die Lizenzzeilen braucht Task 2.
  </action>
  <acceptance_criteria>
    - `grep -c "wdutch=1:2.20.19+1-3" backend/Dockerfile` gibt 1
    - `grep -c "COPYING.wdutch" backend/Dockerfile` gibt mindestens 1
    - `grep -c "DEBIAN_FRONTEND=noninteractive" backend/Dockerfile` ist mindestens 1 und steht im wdutch-RUN
    - Der Wegwerf-Container meldet `413288` Zeilen, `5096240` Byte und Modus `444`
    - `cd backend && uv run pytest tests/test_upgrade_compatibility.py -q` gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_upgrade_compatibility.py -q && grep -c "wdutch=1:2.20.19+1-3" Dockerfile</automated>
  </verify>
  <done>Ein Abbild ohne niederlaendische Liste oder ohne ihren Lizenztext ist nicht baubar, und der Pin ist per Test gehalten.</done>
</task>

<task type="auto">
  <name>Task 2: CI-Gate am gepushten Abbild und THIRD-PARTY.md</name>
  <files>.github/workflows/docker.yml, THIRD-PARTY.md</files>
  <read_first>
    - .github/workflows/docker.yml (Z. 215-295, Schritt "The word list, its version and its licence in this image")
    - THIRD-PARTY.md (Z. 1-52 Vorspann und "## The word list", Z. 317-340 Pruefkommandos)
    - SUMMARY-Notizen aus Task 1 (die gelesenen License-Zeilen)
  </read_first>
  <action>
    `.github/workflows/docker.yml`: im bestehenden Schritt "The word list, its version and its
    licence in this image" einen zweiten Pruefblock fuer wdutch anhaengen (gleiche Architektur
    beider Aeste, gleicher Stil: vier Pruefungen, vier eigene `::error::`-Meldungen):
    `dpkg-query -W -f=\${Version} wdutch` gleich `1:2.20.19+1-3`; `wc -l` gleich `413288` und
    `wc -c` gleich `5096240` fuer `/usr/share/dict/dutch`; `grep -q "License: CC-BY-3.0"` in
    `/usr/local/share/findling/COPYING.wdutch` (Zeichenkette genau so, wie Task 1 sie im
    Container gelesen hat; weicht sie ab, die gelesene nehmen und in der SUMMARY nennen);
    `stat -c %a` gleich `444`. Den Schrittnamen NICHT aendern (THIRD-PARTY.md zitiert ihn). Die
    Kommentarzeile ueber dem Schritt um einen Satz zur zweiten Liste ergaenzen.

    `THIRD-PARTY.md`: neuer Abschnitt `## The Dutch word list` direkt nach `## The word list`,
    englisch, mit derselben Item/Value-Tabelle: Debian package `wdutch`, version `1:2.20.19+1-3`,
    `Architecture: all`; Source package `dutch`, upstream OpenTaal; Origin Debian trixie archive;
    File in the image `/usr/share/dict/dutch` (413288 lines, 5096240 bytes); Licence
    **CC-BY-3.0** (`debian/copyright`, `Files: wordlist/*`), mit der Anmerkung, dass upstream
    `wordlist/LICENSE.txt` die Wahl BSD-3-Clause und/oder CC BY 3.0 laesst (D-04); Attribution
    "OpenTaal, https://www.opentaal.org"; Licence text in the image
    `/usr/local/share/findling/COPYING.wdutch`; Derived artifact
    `$APP_PERSISTENT_STORAGE/dict/nl-full.txt`, produced at start up only when Dutch is active, a
    filtered (length 4 to 14, alphabetic) and ASCII folded adaptation of the list, SHA-256
    recorded as the meta mark `wordlist_hash_nl`. Einen Satz, dass `dictionaries-common` schon
    gelistet ist. Im Abschnitt "How to check this file against reality" das erste Kommando um
    `wc -lc /usr/share/dict/dutch; head -3 /usr/local/share/findling/COPYING.wdutch` ergaenzen
    und den Satz ueber den docker.yml-Schritt auf beide Listen beziehen. Kein Em-/En-Dash.
  </action>
  <acceptance_criteria>
    - `grep -c "1:2.20.19+1-3" .github/workflows/docker.yml` mindestens 1, `grep -c "413288" .github/workflows/docker.yml` mindestens 1, `grep -c "5096240" .github/workflows/docker.yml` mindestens 1
    - `grep -c "COPYING.wdutch" .github/workflows/docker.yml` mindestens 1
    - `grep -c "^## The Dutch word list" THIRD-PARTY.md` gibt 1
    - THIRD-PARTY.md enthaelt `CC-BY-3.0`, `BSD-3-Clause`, `https://www.opentaal.org`, `nl-full.txt`, `wordlist_hash_nl`
    - `python -c` Pruefung: keine Zeichen U+2013/U+2014 in THIRD-PARTY.md
    - Workflow bleibt gueltiges YAML: `cd backend && uv run python -c "import yaml,io;yaml.safe_load(io.open('../.github/workflows/docker.yml',encoding='utf-8'))"`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import io,yaml;yaml.safe_load(io.open('../.github/workflows/docker.yml',encoding='utf-8'));t=io.open('../THIRD-PARTY.md',encoding='utf-8').read();assert '## The Dutch word list' in t;assert all(s in t for s in ('CC-BY-3.0','BSD-3-Clause','https://www.opentaal.org','nl-full.txt','wordlist_hash_nl','COPYING.wdutch'));assert not [c for c in t if ord(c) in (0x2013,0x2014)];print('ok')"</automated>
  </verify>
  <done>Lizenzlage ist im Repo und im Abbild dokumentiert, und CI prueft sie am ausgelieferten Abbild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debian-Archiv -> Image-Build | Paketinhalt bestimmt die Tokenisierung jedes niederlaendischen Dokuments |
| Abbild -> Nutzer | Lizenzpflichten haengen am verteilten Abbild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-SC | Tampering | apt install wdutch | mitigate | exakter Pin mit Epoche, CI-Gate auf Version, Zeilen, Bytes am gepushten Abbild beider Architekturen; kein pip/npm-Paket (Package Legitimacy Audit: Approved) |
| T-21-02-01 | Tampering | stille Paketbewegung in trixie | mitigate | Build scheitert laut bei nicht mehr verfuegbarer Version; Pin-Test in test_upgrade_compatibility |
| T-21-02-02 | Repudiation (Compliance) | Lizenztext fehlt im Abbild | mitigate | `test -s` fail closed im RUN, `install -m 0444`, CI prueft Lizenzzeile und Modus |
| T-21-02-03 | Denial of Service | debconf-Rueckfrage beim Build | mitigate | DEBIAN_FRONTEND=noninteractive im RUN |
</threat_model>

<verification>
- Wegwerf-Container bestaetigt Zeilen, Bytes, Modus und Lizenzzeile
- Pin-Test gruen, YAML gueltig, THIRD-PARTY.md vollstaendig
</verification>

<success_criteria>
Die Lizenzlage (CC-BY-3.0 per D-04) ist dokumentiert und das Abbild traegt die Liste gepinnt
und fail closed.
</success_criteria>

<output>
Create `.planning/phases/21-niederlaendische-komposita/21-02-SUMMARY.md` when done
</output>
