---
phase: 21-niederlaendische-komposita
plan: 09
type: execute
wave: 7
depends_on: ["21-02", "21-04", "21-07", "21-08"]
files_modified:
  - backend/src/findling/index/schema.py
  - backend/tests/test_measurement_scripts.py
  - docs/dutch-analyzer.md
  - docs/language-analyzers.md
  - docs/performance.md
autonomous: false
requirements: [KOMP-01]

must_haves:
  truths:
    - "docs/language-analyzers.md sagt nicht mehr, Komposita gebe es nur fuer Deutsch, und verweist auf das niederlaendische Dokument"
    - "docs/performance.md fuehrt den gemessenen Posten des zweiten Automaten (Liste freigegeben) mit Budgetrechnung gegen 2000 MB und benennt die 23 MB als ueberholte Schaetzung des deutschen Automaten"
    - "docs/dutch-analyzer.md beschreibt Rezept, Kette, Marke, Umbauweg, die sieben benannten Grenzen, die D-08-Grenze des fullreindex-Auswegs und den Poller-Fix aus 21-01"
    - "Nach dem Push ist der CI-Lauf gruen: Language proof mit dem Fall nlc und Store upgrade 5 (Bestand ohne nl sieht keinen Hinweis und keinen Lauf, D-09)"
  artifacts:
    - path: "docs/dutch-analyzer.md"
      provides: "Referenzdokument der niederlaendischen Splitterkette"
      contains: "wordlist_hash_nl"
    - path: "docs/performance.md"
      provides: "RAM-Posten nl und Richtigstellung"
      contains: "wordlist_hash_nl"
    - path: "backend/src/findling/index/schema.py"
      provides: "aktualisierter Kommentar zu body_nl, keine Schemaaenderung"
      contains: "FIELD_BODY_NL"
  key_links:
    - from: "docs/language-analyzers.md"
      to: "docs/dutch-analyzer.md"
      via: "Verweis im Absatz ueber Komposita"
      pattern: "dutch-analyzer\\.md"
    - from: "docs/performance.md"
      to: "docs/measurements/2026-09-komposita-nl/README.md"
      via: "Quelle des RAM-Postens"
      pattern: "2026-09-komposita-nl"
---

<objective>
Die Phase wird dokumentiert, der letzte Kommentar im Code nachgezogen, alle Gates laufen ein
letztes Mal, und nach Owner-Freigabe beweist ein CI-Lauf den ganzen Weg: Kompositumfall gruen,
Bestand ohne nl unberuehrt.

Purpose: Erfolgskriterium 3 (Budget haelt, dokumentiert) und der End-to-End-Teil von 2 und 4
(D-09). Phase 23 zitiert Zahlen aus diesen Dokumenten; eine falsche Zahl hier ist eine falsche
Zahl im Store.

Output: drei Dokumente, ein Kommentar in schema.py, Baumhash, gruener CI-Lauf mit Laufnummer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-niederlaendische-komposita/21-CONTEXT.md
@.planning/phases/21-niederlaendische-komposita/21-RESEARCH.md
@.planning/phases/21-niederlaendische-komposita/21-01-SUMMARY.md
@.planning/phases/21-niederlaendische-komposita/21-04-SUMMARY.md
@.planning/phases/21-niederlaendische-komposita/21-07-SUMMARY.md
@.planning/phases/21-niederlaendische-komposita/21-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doku und der letzte Kommentar im Code</name>
  <files>docs/dutch-analyzer.md, docs/language-analyzers.md, docs/performance.md, backend/src/findling/index/schema.py, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - docs/german-analyzer.md (Strukturvorlage)
    - docs/language-analyzers.md (Z. 430-450, Absatz "Compounds are German only")
    - docs/performance.md (Abschnitt "Drei Zahlen, die nicht dasselbe sind" ab Z. 1387 und die RAM-Posten des deutschen Automaten)
    - docs/measurements/2026-09-komposita-nl/README.md und rohdaten/ram.txt (aus 21-04)
    - backend/src/findling/index/schema.py (Z. 160-175)
    - backend/tests/test_measurement_scripts.py (Baumhash)
    - die SUMMARYs 21-01 (D-08-Ergebnis, Poller-Fix), 21-04 (Zahlen), 21-06 (Marken-Aufrufer, AST-Gate), 21-07 (open_index-Aufrufer)
  </read_first>
  <action>
    `docs/dutch-analyzer.md` neu, englisch wie `docs/german-analyzer.md` und im selben Aufbau:
    Kette in Reihenfolge (lowercase, ascii_fold, split_compound, custom_stopword TUSSENKLANKEN,
    stopword dutch, Ergaenzung, remove_long 48, stemmer dutch) mit der Begruendung der Position
    hinter der Faltung (D-07); Rezept B 4-14 mit den gemessenen Zahlen aus
    `docs/measurements/2026-09-komposita-nl/` (316740 Eintraege, 21/28, 32/33, 0/28 ohne Splitter);
    die sieben benannten Grenzen (sechs Komposita stehen selbst in der Liste, `onroerendezaakbelasting`
    scheitert an `zaakbelasting`); die Marke `wordlist_hash_nl` (Wert off oder
    `<DUTCH_CHAIN_VERSION>:<digest>`, nicht gesaet, Fehlen bei off ist Altbestand, wann sie einen
    Umbau ausloest, dass ANALYZER_VERSION nicht steigt und warum, dass DUTCH_CHAIN_VERSION bei
    jeder Kettenaenderung steigen muss); der Umbauweg (Re-Analyse, kein Download, kein OCR, keine
    Neueinbettung; seit 21-01 hebt eine Band-Drift keine Generation mehr); die D-08-Grenze
    woertlich nach dem Ergebnis des Tests aus 21-01 (unter FINDLING_REBUILD_FALLBACK=fullreindex
    werden die Verzeichnismarken, auch wordlist_hash_nl, so behandelt wie dort festgeschrieben);
    RAM mit Verweis auf performance.md; Lizenz mit Verweis auf THIRD-PARTY.md; Vorbehalt A4
    (kein Muttersprachler, datiert 2026-09-25).

    `docs/language-analyzers.md`: den Absatz "**Compounds are German only.**" ersetzen durch
    "**Compounds are German and Dutch.**" mit zwei Saetzen zum Stand (eigene Liste, eigener
    Splitter nur bei aktivem nl, die deutsche Liste wird nie auf Niederlaendisch angewandt, weil
    sie falsche statt fehlende Zerlegungen liefert) und Verweis auf `docs/dutch-analyzer.md`;
    der Satz "scheduled for phase 21" faellt.

    `docs/performance.md`: im Abschnitt mit den RAM-Posten (dort, wo der deutsche Automat
    41,9 MB und die Liste 21,9 MB stehen; per grep finden) einen Posten "niederlaendischer
    Automat, Liste freigegeben" mit dem Wert aus `ram.txt` (Spanne der drei Laeufe), der Bedingung
    "nur bei aktivem nl", der Quelle `docs/measurements/2026-09-komposita-nl/`, und der
    Budgetrechnung 1.812,7 MB plus Posten gegen 2.000 MB mit Reserve. Ein Satz, dass die
    "rund 23 MB" aus Roadmap und Anforderung die Phase-2-Schaetzung des deutschen Automaten sind
    und fuer Niederlaendisch nicht gelten, und dass die native ARM-Zahl in Phase 22 nachgemessen
    wird (Annahme A1). Deutsche Prosa mit echten Umlauten wie im Rest der Datei.

    `backend/src/findling/index/schema.py` Z. 170-171: Kommentar zu `FIELD_BODY_NL` wird
    "Dutch. Its chain splits compounds only when Dutch is active (see findling.index.analyzer.dutch_chain_for);
    the field itself did not change for it." oder gleichwertig; KEINE Codeaenderung, kein
    SCHEMA_VERSION-Hub. Baumhash im selben Commit: `PACKAGE_TREE_HASH_TODAY` neu,
    `PACKAGE_FILES_TODAY` bleibt 57, Kommentar "Plan 21-09".

    Kein Em-Dash und kein En-Dash in allen drei Dokumenten; README.md, README.en.md,
    README.fr.md, info.xml und Store-Texte werden NICHT angefasst (Owner-Regel, Phase 23).
  </action>
  <acceptance_criteria>
    - `grep -c "Compounds are German only" docs/language-analyzers.md` gibt 0 und `grep -c "dutch-analyzer.md" docs/language-analyzers.md` mindestens 1
    - `grep -c "wordlist_hash_nl" docs/dutch-analyzer.md` mindestens 1 und `grep -c "316740\|316.740" docs/dutch-analyzer.md` mindestens 1
    - `grep -c "2026-09-komposita-nl" docs/performance.md` mindestens 1
    - `grep -c "23 MB" docs/performance.md` mindestens 1 (Richtigstellung)
    - `git diff --stat -- README.md README.en.md README.fr.md backend/appinfo/info.xml` leer
    - keine U+2013/U+2014 in den drei Dokumenten (python-Pruefung)
    - `cd backend && uv run pytest tests/test_measurement_scripts.py -q` gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_measurement_scripts.py -q && uv run python -c "import io,sys;bad=[p for p in ('../docs/dutch-analyzer.md','../docs/language-analyzers.md','../docs/performance.md') if any(ord(c) in (0x2013,0x2014) for c in io.open(p,encoding='utf-8').read())];print('dashes in',bad);sys.exit(1 if bad else 0)"</automated>
  </verify>
  <done>Die Phase ist so dokumentiert, dass Phase 23 die richtigen Zahlen zitiert.</done>
</task>

<task type="auto">
  <name>Task 2: Schlusspruefung lokal</name>
  <files>backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - .planning/phases/21-niederlaendische-komposita/21-CONTEXT.md (D-01 bis D-09, fuer die Checkliste)
    - alle SUMMARYs 21-01 bis 21-08
  </read_first>
  <action>
    Volle Suite und alle Gates in genau der CI-Form laufen lassen: `uv run pytest -q`,
    `uv run ruff check .`, `uv run ruff format --check .`,
    `PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright`, `uv run vulture src tests --min-confidence 80`,
    `uv run ruff check --config pyproject.toml ../scripts`, `uv run ruff format --config pyproject.toml --check ../scripts`.
    Danach die Entscheid-Checkliste in die SUMMARY, je Zeile mit Beleg (Datei und Zeile oder
    Testname): D-02 Liste freigegeben (Test aus 21-03), D-03 Fenster 4-14 (`MIN_LEN`/`MAX_LEN`),
    D-04 CC-BY-3.0 (THIRD-PARTY.md), D-05 `ANALYZER_VERSION = 1`, D-06 Marke nicht gesaet und
    Legacy-Regel (Tests aus 21-05), D-07 Splitter hinter der Faltung (Kettentest), D-08 (Test aus
    21-01), D-09 (Store upgrade 5 unveraendert: `git diff` der Phase zeigt keine Zeile in dem
    Schritt). `test_measurement_scripts.py` wird nur angefasst, wenn der Baumhash nicht passt
    (dann ist ein vorheriger Commit ohne Hash der Befund, in der SUMMARY nennen).
  </action>
  <acceptance_criteria>
    - Alle sieben Kommandos gruen, Zahl passed/skipped in der SUMMARY
    - Checkliste D-02 bis D-09 vollstaendig mit Beleg je Zeile
    - `git status --short` zeigt nach dem Commit keine offenen Aenderungen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest -q && uv run ruff check . && uv run ruff format --check . && uv run vulture src tests --min-confidence 80 && uv run ruff check --config pyproject.toml ../scripts</automated>
  </verify>
  <done>Lokal ist alles gruen und jede gesperrte Entscheidung hat einen benannten Beleg.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner-Freigabe fuer den Push und der CI-Beweis</name>
  <files>.planning/phases/21-niederlaendische-komposita/21-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/21-niederlaendische-komposita/21-09-SUMMARY.md (Checkliste aus Task 2)
    - .github/workflows/deploy-harp.yml (Schritte "Language proof, ..." und "Store upgrade 5, ...")
  </read_first>
  <action>
    Dem Owner die Checkliste und die Commitliste der Phase (`git log --oneline` seit dem letzten
    Push) vorlegen und auf Freigabe warten. Erst nach "approved": `git push`, dann
    `gh run list --limit 10` und `gh run watch` fuer jeden ausgeloesten Lauf. Belege in die
    SUMMARY: Laufnummern, Ergebnis von "Language proof, the four new chains answer on the
    ordinary search route" (Fall nlc gruen, erste Datei `language-proof-nlc.txt` fuer `belasting`)
    und von "Store upgrade 5, the seven assurances after the upgrade" (gruen: keine bewegte Marke,
    kein Banner, keine Logzeile start_rebuild_on_drift). Ein roter Lauf wird analysiert, nicht
    durch Aenderung einer Zusicherung gruen gemacht; Befund an den Owner.
  </action>
  <what-built>
    Niederlaendischer Splitter hinter der Faltung (nur bei aktivem nl), Wortliste wdutch im
    Abbild mit Lizenz, siebte Marke wordlist_hash_nl mit Umbau nur bei nl, Poller-Fix gegen den
    Generationshub bei Band-Drift, Mess- und Pruefwerkzeug, CI-Kompositumfall, Doku.
  </what-built>
  <how-to-verify>
    1. SUMMARY von 21-09 lesen: Checkliste D-02 bis D-09 mit Belegen, Suite- und Gate-Zahlen.
    2. Entscheiden, ob gepusht wird. Antwort "approved" loest Push und CI-Beobachtung aus.
    3. Nach dem Lauf in der SUMMARY pruefen: Language proof gruen mit Fall nlc, Store upgrade 5 gruen, Laufnummern genannt.
    4. Optional an findling-nextcloud: nach dem Umbau mit nl die Suche `belasting` gegen ein hochgeladenes Dokument mit `gemeentebelastingen` ansehen.
  </how-to-verify>
  <acceptance_criteria>
    - Owner-Antwort "approved" in der SUMMARY datiert
    - CI-Laufnummern und Ergebnis beider Schritte in der SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>gh run list --limit 10 zeigt die nach dem Push ausgeloesten Laeufe; beide benannten Schritte enden gruen</automated>
    <human-check>Owner liest die 21-09-SUMMARY (Checkliste D-02 bis D-09 mit Belegen, Suite- und Gate-Zahlen) und gibt mit "approved" den Push frei; nach dem Lauf stehen Laufnummern und beide Schrittergebnisse in der SUMMARY</human-check>
  </verify>
  <done>Owner-Freigabe datiert in der SUMMARY, Push erfolgt, Language proof (Fall nlc) und Store upgrade 5 gruen mit Laufnummern belegt</done>
  <resume-signal>Type "approved" (Push und CI-Beobachtung) oder beschreibe, was vorher anders sein soll</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lokales Repo -> GitHub/CI | Push loest Abbild-Bau und Beweislaeufe aus |
| Doku -> Store-Texte (Phase 23) | Zahlen reisen in eine nicht editierbare Veroeffentlichung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09-01 | Repudiation | falsche RAM-Zahl in der Doku | mitigate | Wert aus rohdaten/ram.txt, 23-MB-Richtigstellung, Quelle verlinkt |
| T-21-09-02 | Tampering | roter CI-Lauf durch geaenderte Zusicherung gruen gemacht | mitigate | Checkpoint: Analyse statt Aenderung, Befund an den Owner |
| T-21-09-03 | Elevation of Privilege | Push ohne Freigabe | mitigate | blockierender Checkpoint vor git push |
</threat_model>

<verification>
- Doku vollstaendig, keine Owner-Texte angefasst, Baumhash passend
- lokale Gates gruen, Entscheid-Checkliste belegt
- CI-Lauf nach Freigabe gruen mit Laufnummern
</verification>

<success_criteria>
KOMP-01 ist gebaut, dokumentiert und in CI bewiesen: Kompositumfall gruen, Bestand ohne nl
unberuehrt, Budget dokumentiert.
</success_criteria>

<output>
Create `.planning/phases/21-niederlaendische-komposita/21-09-SUMMARY.md` when done
</output>
