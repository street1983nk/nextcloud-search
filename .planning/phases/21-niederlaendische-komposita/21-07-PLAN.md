---
phase: 21-niederlaendische-komposita
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy-harp.yml
  - backend/tests/test_language_proof_steps.py
autonomous: true
requirements: [KOMP-01]

must_haves:
  truths:
    - "Der CI-Schritt Language proof laedt ein fuenftes Dokument mit gemeentebelastingen hoch und verlangt, dass die Frage belasting genau diese Datei als ersten Treffer bringt, ueber die OCS-Route und ueber die Ergebnisseite"
    - "Der Fall ist ohne Splitter rot: die Frage ist ein einzelnes Wort und wird deshalb nur aus dem Wortindex beantwortet (one term rule), und keine ausgelieferte Kette ohne Splitter trifft belast in diesem Dokument (Querprobe 25.09.2026)"
    - "Der Schritt prueft entries | length und nie die Subline (Pitfall 7)"
    - "Store upgrade 5 bleibt unveraendert und ist der End-to-End-Beweis von Erfolgskriterium 4 (D-09)"
  artifacts:
    - path: ".github/workflows/deploy-harp.yml"
      provides: "fuenftes Dokument nlc im Schritt Language proof"
      contains: "language-proof-nlc.txt"
    - path: "backend/tests/test_language_proof_steps.py"
      provides: "Textgate kennt den fuenften Fall"
      contains: "language-proof-nlc.txt"
  key_links:
    - from: ".github/workflows/deploy-harp.yml"
      to: "backend/tests/test_language_proof_steps.py"
      via: "DOCUMENTS, QUESTIONS, PAGE_CALLS_EXPECTED lesen den Schritt als Text"
      pattern: "PAGE_CALLS_EXPECTED = 5"
---

<objective>
Der CI-Sprachbeweis bekommt den Kompositumfall (Erfolgskriterium 2, zweite Haelfte): ein
fuenftes Dokument im bestehenden Schritt, kein fuenfter Schritt. Die Vorbedingung
`languagesActive == "de,en,es,it,nl,pt"` steht dort bereits.

Purpose: Die Python-Suite beweist die Kette (21-06); nur dieser Schritt beweist den ganzen Weg
Upload, Crawl, Index, OCS-Route, Ergebnisseite auf einer echten Nextcloud. Der Plan beruehrt
keine Datei unter `backend/src` oder `php/`, er laeuft deshalb parallel zu 21-01 und 21-02.
Der Schritt ist bis zur Verdrahtung (21-06) erwartbar rot; der Push erfolgt erst in Plan 21-08
nach der vollstaendigen Verdrahtung.

Output: erweiterter Schritt, erweitertes Textgate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-niederlaendische-komposita/21-CONTEXT.md
@.planning/phases/21-niederlaendische-komposita/21-RESEARCH.md
@.planning/phases/21-niederlaendische-komposita/21-PATTERNS.md
@CLAUDE.md

Querprobe (21-RESEARCH "Code Examples", gemessen 25.09.2026), Dokument
"De gemeentebelastingen voor dit jaar zijn verhoogd.", Frage "belasting":
de doc [de, gemeentebelasting, voor, dit, jaar, zijn, verhoogd] q [belasting] kein Treffer;
en doc [..., gemeentebelastingen, ...] q [belast] kein Treffer;
nl ohne doc [gemeentebelast, jar, verhoogd] q [belast] kein Treffer;
nl mit doc [gemeent, belast, jar, verhoogd] q [belast] TREFFER.

Warum kein Vektortreffer den Fall gruen machen kann: deploy-harp.yml Z. 3083-3092 zitiert die
one term rule aus Plan 06.1-20: `api/search.py` beantwortet eine Ein-Wort-Frage allein aus dem
Wortindex, die Vektorhaelfte wird fuer sie nicht gebaut.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fuenftes Dokument im Schritt Language proof</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml (Z. 829-1165, der ganze Schritt; Z. 3075-3095 one term rule; Z. 4085-4160 Store upgrade 5, nur lesen)
    - backend/tests/test_language_proof_steps.py (was der Textgate verlangt)
  </read_first>
  <action>
    Im Schritt "Language proof, the four new chains answer on the ordinary search route" (Name
    NICHT aendern, `PROOF_STEP` und andere Stellen zitieren ihn) den Fall `nlc` an jeder Stelle
    ergaenzen, an der die vier Sprachen stehen:
    Dokumente: `printf 'De gemeentebelastingen voor dit jaar zijn verhoogd.\n' > "${RUNNER_TEMP}/language-proof-nlc.txt"`
    (reines ASCII, keine Oktal-Escapes). Upload-Schleife `for lang in es it nl pt nlc`. Poll:
    `missing="es it nl pt nlc"` und im `case` die Zeile `nlc) term=belasting ;;`. Die
    Treffer-Zuordnung (erster Treffer ist die eigene Datei) gilt unveraendert fuer nlc. Ergebnisseite:
    ein fuenfter literaler `curl`-Aufruf nach dem Muster der vier, Ausgabe
    `language-page-nlc.html`/`.code`, URL `.../index.php/apps/findling/?query=belasting`; der
    Kommentar "Four literal calls and no loop" wird "Five literal calls and no loop" mit dem
    Hinweis, dass nl zwei Faelle hat (Kette und Kompositum). Die Pruefschleife der Ergebnisseite,
    die Loeschschleife und die Verschwinden-Messung laufen ueber `es it nl pt nlc`.

    Kommentarblock unter "-- the four documents --" um einen Absatz ergaenzen (englisch): der
    fuenfte Fall ist der Kompositumfall von Phase 21, "red without split_compound, measured
    2026-09-25", mit den vier Zeilen der Querprobe (de, en, nl without, nl with) und dem Satz, dass
    die Frage ein einzelnes Wort ist und die one term rule (Verweis auf den Kommentar zur
    Vektorhaelfte weiter unten) einen Vektortreffer ausschliesst. Nachweisen, dass keines der
    Woerter des neuen Satzes ein Term ist, den ein anderer Beweis des Workflows zaehlt:
    `grep -n -i "gemeente\|belasting\|verhoogd" .github/workflows/deploy-harp.yml` vor der
    Aenderung, Ergebnis in die SUMMARY. Findet sich ein Treffer ausserhalb dieses Schritts,
    anhalten und berichten.

    "Store upgrade 5" und "Store upgrade 6" bleiben byteidentisch (D-09: der bestehende Schritt
    ist der Beweis, dass eine Installation ohne nl keinen Hinweis und keinen Lauf sieht).
  </action>
  <acceptance_criteria>
    - `grep -c "language-proof-nlc.txt" .github/workflows/deploy-harp.yml` mindestens 1
    - `grep -c "nlc) term=belasting ;;" .github/workflows/deploy-harp.yml` gibt 1
    - `grep -c "apps/findling/?query=belasting" .github/workflows/deploy-harp.yml` gibt 1
    - `grep -c "red without split_compound, measured 2026-09-25" .github/workflows/deploy-harp.yml` gibt 1
    - `git diff .github/workflows/deploy-harp.yml` zeigt keine Zeile innerhalb der Schritte "Store upgrade 5" und "Store upgrade 6"
    - YAML gueltig: `cd backend && uv run python -c "import io,yaml;yaml.safe_load(io.open('../.github/workflows/deploy-harp.yml',encoding='utf-8'))"`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import io,yaml;t=io.open('../.github/workflows/deploy-harp.yml',encoding='utf-8').read();yaml.safe_load(t);assert 'nlc) term=belasting ;;' in t;assert t.count('apps/findling/?query=belasting')==1;print('ok')"</automated>
  </verify>
  <done>CI verlangt den Kompositumfall ueber beide Wege, mit dokumentierter Rot-Begruendung.</done>
</task>

<task type="auto">
  <name>Task 2: Textgate kennt den fuenften Fall</name>
  <files>backend/tests/test_language_proof_steps.py</files>
  <read_first>
    - backend/tests/test_language_proof_steps.py (komplett: Konstanten Z. 85-133, Tests ab Z. 404, `_CLEAN`-Beispiel und `scan_proof_step`)
    - .github/workflows/deploy-harp.yml (Stand nach Task 1)
  </read_first>
  <action>
    `DOCUMENTS` um `"language-proof-nlc.txt"`, `QUESTIONS` um `"belasting"`,
    `PAGE_CALLS_EXPECTED = 5`. Die Kommentare ueber den drei Konstanten auf fuenf Faelle ziehen
    und nennen, dass nl zweimal vorkommt (Kette, Kompositum). Testnamen, die "four" tragen
    (`test_the_step_names_all_four_documents_and_all_four_questions`), bewusst umbenennen in
    `test_the_step_names_every_document_and_every_question`, und `test_the_step_calls_the_result_page_once_per_language`
    in `test_the_step_calls_the_result_page_once_per_case`; Docstrings entsprechend. Das
    `_CLEAN`-Beispiel und `scan_proof_step` pruefen, ob sie die Listen lesen oder eigene Zahlen
    tragen, und mitziehen, damit die Gegenproben weiter rot werden koennen (jede Gegenprobe
    einmal von Hand gegen einen absichtlich entfernten nlc-Eintrag laufen lassen und das Rot in
    der SUMMARY belegen, danach zurueck).
  </action>
  <acceptance_criteria>
    - `grep -c "PAGE_CALLS_EXPECTED = 5" backend/tests/test_language_proof_steps.py` gibt 1
    - `grep -c '"language-proof-nlc.txt"' backend/tests/test_language_proof_steps.py` gibt 1
    - `grep -c '"belasting"' backend/tests/test_language_proof_steps.py` gibt 1
    - `cd backend && uv run pytest tests/test_language_proof_steps.py -q` gruen
    - SUMMARY belegt ein Rot bei entferntem nlc-Aufruf
    - `cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80` gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_language_proof_steps.py -q</automated>
  </verify>
  <done>Der Textgate haelt fuenf Faelle und wird rot, wenn der Kompositumfall aus dem Schritt faellt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Runner -> Test-Nextcloud | Testkonto laedt Dokumente per WebDAV hoch |
| Workflow-Text -> Textgate | Gate liest den Schritt als Text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Repudiation | Beweis gruen aus falschem Grund | mitigate | Ein-Wort-Frage (nur Wortindex), erster Treffer muss die eigene Datei sein, Querprobe dokumentiert, Kollisionspruefung der Woerter |
| T-21-07-02 | Information Disclosure | Zugangsdaten im Schritt | accept | unveraenderter Umgang des bestehenden Schritts (nur Laenge ausgegeben), kein neuer Geheimwert |
| T-21-07-03 | Tampering | stilles Entfernen des Falls | mitigate | Textgate mit PAGE_CALLS_EXPECTED = 5 und Namenslisten, Rot per Gegenprobe belegt |
</threat_model>

<verification>
- Workflow gueltig, fuenf Faelle an allen Stellen, Store upgrade 5/6 unberuehrt
- Textgate gruen, Rot bei entferntem Fall belegt
</verification>

<success_criteria>
Der CI-Sprachfall `gemeentebelastingen` ueber `belasting` steht und ist ohne Splitter
nachweislich rot.
</success_criteria>

<output>
Create `.planning/phases/21-niederlaendische-komposita/21-07-SUMMARY.md` when done
</output>
