---
phase: 21-niederlaendische-komposita
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - backend/src/findling/index/wordlist_nl.py
  - backend/src/findling/index/analyzer.py
  - backend/tests/test_wordlist_nl.py
  - backend/tests/test_dutch_analyzer.py
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [KOMP-01]

must_haves:
  truths:
    - "Rezept B 4-14 (D-03, D-07) baut aus einer Quelle zeilenweise eine mit tantivys ascii_fold gefaltete Liste plus Tussenklanken s, e, en"
    - "Bei inaktivem nl liefert dutch_mark den Wert off und dutch_digest_for None, ohne Quelle oder Artefakt anzufassen"
    - "Die niederlaendische Liste wird nach dem Automatenbau freigegeben; gecacht wird nur der Digest (D-02)"
    - "Die Splitterkette steht HINTER der Faltung und eine Kette ohne nl bleibt die unveraenderte snowball_analyzer(dutch)"
    - "ANALYZER_VERSION bleibt 1 (D-05), und der Docstring sagt warum"
    - "wordlist.py und german_analyzer bleiben byteidentisch (deutscher Digest b1f64012...dde0 unberuehrt)"
  artifacts:
    - path: "backend/src/findling/index/wordlist_nl.py"
      provides: "SYSTEM_WORDLIST_NL, MIN_LEN, MAX_LEN, TUSSENKLANKEN, DUTCH_LIST_OFF, DUTCH_CHAIN_VERSION, load_constituents_nl, artifact_path_nl, build_artifact_nl, read_count_nl, dutch_digest_for, dutch_mark, measure"
      contains: "TUSSENKLANKEN"
    - path: "backend/src/findling/index/analyzer.py"
      provides: "dutch_analyzer, cached_dutch_analyzer, dutch_build_count, dutch_chain_for"
      contains: "def dutch_chain_for"
    - path: "backend/tests/test_wordlist_nl.py"
      provides: "Rezept-, Artefakt-, Cache- und Sprachgate-Tests"
      min_lines: 120
    - path: "backend/tests/test_dutch_analyzer.py"
      provides: "Kettenreihenfolge, Singleton, Tussenklank-Test, Versionspin"
      min_lines: 80
  key_links:
    - from: "backend/src/findling/index/analyzer.py"
      to: "findling.index.wordlist_nl"
      via: "import TUSSENKLANKEN und build_artifact_nl"
      pattern: "from findling.index.wordlist_nl import"
    - from: "backend/src/findling/index/wordlist_nl.py"
      to: "findling.index.wordlist"
      via: "Wiederverwendung wordlist_hash, Artifact, DIGEST_SUFFIX, ENCODING, rss_bytes (Import, keine Kopie)"
      pattern: "from findling.index.wordlist import"
---

<objective>
Das niederlaendische Rezept und die Splitterkette entstehen als eigene, isolierte Bausteine,
ohne Wirkung auf den laufenden Container: kein Aufrufer in `open.py`, `rebuild.py`, `poller.py`
oder `resources.py` benutzt sie in diesem Plan.

Purpose: Research Pattern 1 (eigenes Modul, deutsches Modul unberuehrt) und Pattern 5
(Abbruch vor der Verdrahtung ist ein Dateiloeschen). Die Messwerkzeuge in Plan 21-04 rufen
genau diese ausgelieferten Funktionen auf (Gegenprobe nie mit dem Muster der Umsetzung).

Output: `wordlist_nl.py`, vier neue Funktionen in `analyzer.py`, zwei Testdateien, Baumhash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-niederlaendische-komposita/21-CONTEXT.md
@.planning/phases/21-niederlaendische-komposita/21-RESEARCH.md
@.planning/phases/21-niederlaendische-komposita/21-PATTERNS.md
@.planning/phases/21-niederlaendische-komposita/21-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Aus backend/src/findling/index/wordlist.py (wiederverwenden, nicht kopieren, Datei NICHT aendern):
- `DIGEST_SUFFIX = ".sha256"`, `ENCODING = "utf-8"`, `def wordlist_hash(entries: Sequence[str]) -> str`,
  `def rss_bytes() -> int`, dataclass `Artifact(entries, digest, rebuilt)`.
- Muster fuer Artefakt fail closed: `_digest_path`, `_read_artifact`, `_artifact_key` (Schluessel `(path, digest, size, mtime_ns)`), `_CACHE_LOCK`, `_READ_COUNT`/`read_count()`, `build_artifact`, `_load_artifact` (Z. 156-335), `measure()` (Z. 370-399).

Aus backend/src/findling/index/analyzer.py:
- `_CACHED_GERMAN: dict[str, TextAnalyzer]`, `_BUILD_COUNT`, `build_count()` (Z. 142-152)
- `german_analyzer(constituents)` mit Logging (Z. 191-224), `cached_german_analyzer(digest, constituents)` (Z. 331-344)
- `snowball_analyzer(language: str) -> TextAnalyzer` (Z. 242) bleibt UNVERAENDERT
- `MAX_TOKEN_CHARS = 48`, `FOLDED_STOPWORDS` aus findling.index.stopwords, `SNOWBALL_NAME` aus findling.config (`SNOWBALL_NAME["nl"] == "dutch"`)
- Docstring Z. 77-87 mit dem Satz "Any change to a chain below has to raise ANALYZER_VERSION"

Neue Vertraege dieses Plans (von 21-04, 21-05, 21-06 benutzt):
- `wordlist_nl.DUTCH_LIST_OFF: Final = "off"`
- `wordlist_nl.DUTCH_CHAIN_VERSION: Final = 1`
- `wordlist_nl.dutch_digest_for(languages: Sequence[str]) -> str | None`
- `wordlist_nl.dutch_mark(languages: Sequence[str]) -> str`  (off oder f"{DUTCH_CHAIN_VERSION}:{digest}")
- `wordlist_nl.build_artifact_nl(source: Path = SYSTEM_WORDLIST_NL, target: Path | None = None) -> Artifact`
- `wordlist_nl.load_constituents_nl(source: Path = SYSTEM_WORDLIST_NL, *, window: tuple[int, int] = (MIN_LEN, MAX_LEN)) -> list[str]`
- `analyzer.dutch_analyzer(constituents: Sequence[str]) -> TextAnalyzer`
- `analyzer.cached_dutch_analyzer(digest: str, constituents: Sequence[str]) -> TextAnalyzer`
- `analyzer.dutch_build_count() -> int`
- `analyzer.dutch_chain_for(digest: str | None) -> TextAnalyzer`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wordlist_nl.py, Rezept B 4-14 mit Artefakt fail closed und Sprachgate</name>
  <files>backend/src/findling/index/wordlist_nl.py, backend/tests/test_wordlist_nl.py</files>
  <read_first>
    - backend/src/findling/index/wordlist.py (komplett, 399 Zeilen: Aufbau, Docstrings, Cache, fail closed)
    - backend/tests/test_wordlist.py (Z. 1-120: Docstring-Stil, Miniatur-Quelle, Fixtures source und storage, Testnamen)
    - backend/src/findling/config.py (settings().dict_dir, settings().languages)
    - .planning/phases/21-niederlaendische-komposita/21-RESEARCH.md (Rezepttabelle, "Rezept B 4-14 (Prototyp)", Warnung zu split("\n"), Open Question 2)
  </read_first>
  <behavior>
    - `test_the_window_and_the_alphabetic_test_decide_what_stays`: Miniatur-Quelle mit Woertern der Laenge 3, 4, 14, 15, einer Ziffernzeile; nur 4 bis 14, nur isalpha, Fenster auf UNGEFALTETE Laenge
    - `test_the_source_is_read_line_by_line`: eine Quellzeile mit Leerzeichen ("in het bijzonder") ergibt KEINE Einzelwoerter
    - `test_the_list_is_folded_by_the_engine`: "coördinatie" landet als "coordinatie", "Belasting" als "belasting"
    - `test_the_linking_elements_are_entries_of_their_own`: `TUSSENKLANKEN == ("s", "e", "en")` und alle drei in der Liste
    - `test_a_tampered_artifact_is_rebuilt_from_the_source`, `test_the_cache_never_answers_for_a_missing_artifact` (Muster test_wordlist.py)
    - `test_the_digest_is_read_once_per_process`: zweimal `dutch_digest_for(("de","en","nl"))` ergibt einen Anstieg von `read_count_nl()` um genau 1
    - `test_the_list_is_not_held_after_the_digest_is_known`: nach `dutch_digest_for` haelt das Modul keine Liste (kein Modulglobal vom Typ list mit mehr als 3 Eintraegen; der Digest-Cache haelt nur str)
    - `test_without_dutch_nothing_is_read`: Quelle zeigt auf einen nicht existierenden Pfad, `dutch_digest_for(("de","en"))` ist None, `dutch_mark(("de","en")) == "off"`, `read_count_nl()` unveraendert, kein Fehler
    - `test_the_mark_carries_the_chain_version`: `dutch_mark(("de","en","nl")) == f"1:{digest}"`
  </behavior>
  <action>
    Neues Modul `backend/src/findling/index/wordlist_nl.py` als Geschwister von `wordlist.py`
    (Pattern 1; `wordlist.py` bleibt byteidentisch). Englischer Modul-Docstring mit der
    Rezepttabelle aus 21-RESEARCH "Rezepte" (A/B mit Fenstern, Eintraegen, 21/28 und 32/33),
    Rezept B 4-14 als gewaehlt per Owner-Entscheid 25.09.2026 (D-03), Begruendung gegen 4-12
    (`onderhandel, ing`), Quelle `wdutch 1:2.20.19+1-3`, Lizenz CC-BY-3.0 (D-04).

    Konstanten: `SYSTEM_WORDLIST_NL = Path("/usr/share/dict/dutch")`, `MIN_LEN = 4`,
    `MAX_LEN = 14`, `TUSSENKLANKEN: Final = ("s", "e", "en")` mit dem "used twice on purpose"-Kommentar
    (Liste hier, custom_stopword in analyzer), `DUTCH_LIST_OFF: Final = "off"`,
    `DUTCH_CHAIN_VERSION: Final = 1` mit Kommentar: raise it when `analyzer.dutch_analyzer`
    changes its filters or their order, because the list digest alone would not move
    (Research Pattern 4, offene Luecke; Wahl des Planers). `ARTIFACT_NAME_NL = "nl-full.txt"` einmal buchstabiert.

    Faltung der Liste ausschliesslich mit tantivy: ein modulweiter `TextAnalyzer`
    `simple -> lowercase -> ascii_fold`, je Eintrag genau ein Token, sonst verwerfen (Anti-Pattern:
    kein unicodedata). `load_constituents_nl(source=SYSTEM_WORDLIST_NL, *, window=(MIN_LEN, MAX_LEN))`
    liest `read_text(encoding=ENCODING, errors="replace").split("\n")`, `strip()`, `isalpha()`,
    Fenster auf ungefaltete Laenge, faltet, fuegt `TUSSENKLANKEN` hinzu, gibt `sorted(set)` zurueck.
    Import von `wordlist_hash`, `Artifact`, `DIGEST_SUFFIX`, `ENCODING`, `rss_bytes` aus
    `findling.index.wordlist`. `wordlist_nl` importiert `findling.index.analyzer` NIE (Importgrenze,
    Muster wordlist.measure).

    Artefakt: `artifact_path_nl() -> settings().dict_dir / ARTIFACT_NAME_NL`; `build_artifact_nl(source,
    target=None) -> Artifact` fail closed nach dem Muster `_load_artifact` (Digestdatei neben dem
    Artefakt muss den Inhalt beschreiben, sonst Rezept neu fahren und atomar schreiben). Cache nach
    D-02: `_CACHED_DIGESTS: dict[_CacheKey, str]` unter `_CACHE_LOCK`, Schluessel
    `(path, digest, size, mtime_ns)`; die Eintraege werden NICHT gecacht. `read_count_nl()` zaehlt
    jedes tatsaechliche Lesen/Bauen. `dutch_digest_for(languages)`: `"nl" not in languages` ->
    `None` ohne Dateizugriff; sonst Cache-Treffer ueber den Schluessel -> Digest ohne Lesen; sonst
    `build_artifact_nl()` und Digest merken. `dutch_mark(languages)` -> `DUTCH_LIST_OFF` oder
    `f"{DUTCH_CHAIN_VERSION}:{digest}"`. `measure()` gibt nur Zahlen aus (T-02-14), mit
    `if __name__ == "__main__":  # pragma: no cover` und `key=value`-Zeilen.

    Tests zuerst (RED), dann Implementierung (GREEN). Testdatei nach dem Stil von
    `test_wordlist.py`: Docstring "asserted rather than trusted", Miniatur-Quelle als Tupel mit
    Kommentar je Klasse, Fixture `storage` mit `APP_PERSISTENT_STORAGE` und `settings.cache_clear()`.
    Testdateien mit `newline="\n"` schreiben.
  </action>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_wordlist_nl.py -q` gruen, alle neun Testnamen aus `<behavior>` vorhanden (`grep -c "^def test_" backend/tests/test_wordlist_nl.py` mindestens 9)
    - `grep -cF 'split("\n")' backend/src/findling/index/wordlist_nl.py` mindestens 1
    - `grep -c "unicodedata" backend/src/findling/index/wordlist_nl.py` gibt 0
    - `grep -c "findling.index.analyzer" backend/src/findling/index/wordlist_nl.py` gibt 0
    - `git diff --quiet -- backend/src/findling/index/wordlist.py` (unveraendert)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_wordlist_nl.py tests/test_wordlist.py -q</automated>
  </verify>
  <done>Das niederlaendische Rezept existiert isoliert, faellt geschlossen und liest nichts, solange nl nicht aktiv ist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Splitterkette hinter der Faltung, Singleton, Auswahlhilfe, Versionsdocstring</name>
  <files>backend/src/findling/index/analyzer.py, backend/tests/test_dutch_analyzer.py, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/src/findling/index/analyzer.py (Z. 1-160 Docstring und Singletons, Z. 191-345 Fabriken)
    - backend/tests/test_analyzer.py (Z. 91-150 Tabellen und Fixtures, Z. 168-174 Fugen-Test, Z. 269-305 Singleton- und Versionstests, Z. 520-640 Ketten-AST-Waechter `test_the_german_chain_stands_in_the_measured_order`)
    - backend/tests/test_language_analyzers.py (Z. 247-300: `test_the_analyzer_version_did_not_move`, `test_the_snowball_chain_stands_in_the_measured_order`)
    - backend/tests/test_sandbox.py (Z. 361-368: Extraktionskind laedt kein findling.index.*; deckt wordlist_nl automatisch ab)
    - backend/tests/test_measurement_scripts.py (Baumhash-Konstanten und Kommentarhistorie)
  </read_first>
  <behavior>
    - `test_the_dutch_chain_stands_in_the_measured_order`: AST-Waechter nach Muster des deutschen: Filterfolge lowercase, ascii_fold, split_compound, custom_stopword(TUSSENKLANKEN), stopword, custom_stopword(FOLDED_STOPWORDS), remove_long, stemmer
    - `test_the_splitter_stands_behind_the_fold`: mit Miniliste ("gemeente", "belasting", Tussenklanken) zerlegt die Kette "gemeentebelastingen" in "gemeent", "belast" UND "Gemeentebelastingen" genauso
    - `test_no_bare_linking_element_reaches_the_index`: kein Token der Laenge 1 aus "waterschapsbelasting" mit Miniliste ("waterschap", "belasting", Tussenklanken)
    - `test_the_dutch_automaton_is_built_once`: zwei Aufrufe `cached_dutch_analyzer(digest, liste)` geben dasselbe Objekt, `dutch_build_count()` +1
    - `test_without_dutch_the_chain_is_the_snowball_one`: `dutch_chain_for(None)` tokenisiert "gemeentebelastingen" wie `snowball_analyzer("dutch")` (ein Token) und `dutch_build_count()` bleibt unveraendert
    - `test_a_chain_for_a_digest_the_volume_does_not_hold_fails_closed`: `dutch_chain_for("falscher-digest")` bei Artefakt mit anderem Digest wirft `ValueError`
    - `test_the_analyzer_version_stays_where_it_was`: `ANALYZER_VERSION == 1` und `DUTCH_CHAIN_VERSION == 1`
  </behavior>
  <action>
    In `backend/src/findling/index/analyzer.py` neben den deutschen Singletons:
    `_CACHED_DUTCH: dict[str, TextAnalyzer] = {}`, `_DUTCH_BUILD_COUNT = 0`, `dutch_build_count()`.
    `dutch_analyzer(constituents)` baut per D-07 und Pitfall 3:
    `TextAnalyzerBuilder(Tokenizer.simple())` -> `Filter.lowercase()` -> `Filter.ascii_fold()`
    (Position 2 wie Phase 17) -> `Filter.split_compound(list(constituents))` ->
    `Filter.custom_stopword(list(TUSSENKLANKEN))` -> `Filter.stopword(SNOWBALL_NAME["nl"])` ->
    `Filter.custom_stopword(list(FOLDED_STOPWORDS[SNOWBALL_NAME["nl"]]))` (Schluesselform so wie
    `snowball_analyzer` sie liest; vorher nachsehen) -> `Filter.remove_long(MAX_TOKEN_CHARS)` ->
    `Filter.stemmer(SNOWBALL_NAME["nl"])`, Logzeile mit Eintragszahl, Bauzeit und Zaehler wie beim
    deutschen. `cached_dutch_analyzer(digest, constituents)` nach Muster `cached_german_analyzer`
    (ein Eintrag, `clear()` vor dem neuen). `dutch_chain_for(digest: str | None)`: None ->
    `snowball_analyzer(SNOWBALL_NAME["nl"])`; Digest im Cache -> Rueckgabe ohne Lesen; sonst
    `artifact = build_artifact_nl()`, bei `artifact.digest != digest` `ValueError` mit Meldung
    ohne Pfad (fail closed: das Volume traegt eine andere Liste als die Marke sagt), sonst
    `cached_dutch_analyzer(digest, artifact.entries)`; die lokale Liste wird danach nicht
    gehalten (D-02). Diese Hilfe existiert, damit `open_index` in Plan 21-06 die Variante ohne
    `ast.If` um `register_tokenizer` waehlen kann (Registrierungs-Gate).
    Import `from findling.index.wordlist_nl import TUSSENKLANKEN, build_artifact_nl`.

    Modul-Docstring: den Absatz zu "Any change to a chain below has to raise ANALYZER_VERSION" um
    die benannte Ausnahme ergaenzen, im Stil des Absatzes "The other half of that sentence": die
    niederlaendische Splitterkette wird von der eigenen Marke `wordlist_hash_nl` getragen (Digest der
    Liste plus `DUTCH_CHAIN_VERSION`), ein Hub von ANALYZER_VERSION waere ein Vollreindex von rund
    19 h auf jeder Installation, auch ohne nl (D-05). `snowball_analyzer` und `german_analyzer`
    bleiben unveraendert.

    Tests in neuer Datei `backend/tests/test_dutch_analyzer.py` zuerst (RED), dann Code.
    Miniaturlisten inline; die gemessenen Tabellen kommen in Plan 21-04 mit der Fixture.

    Baumhash im selben Commit wie der letzte src-Eingriff dieses Plans:
    `PACKAGE_TREE_HASH_TODAY` neu, `PACKAGE_FILES_TODAY` von 56 auf 57 (neue Datei
    `wordlist_nl.py`), datierter Kommentarabsatz "Plan 21-03 ...". Falls die Datei vorher schon
    committet wird (Task 1), den Hash dort bereits nachziehen: jeder Commit mit src-Aenderung
    traegt den passenden Hash.
  </action>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_dutch_analyzer.py tests/test_analyzer.py tests/test_language_analyzers.py tests/test_sandbox.py tests/test_measurement_scripts.py -q` gruen
    - `grep -c "ANALYZER_VERSION = 1" backend/src/findling/index/analyzer.py` gibt 1
    - `grep -c "wordlist_hash_nl" backend/src/findling/index/analyzer.py` mindestens 1 (Docstring)
    - `grep -c "PACKAGE_FILES_TODAY = 57" backend/tests/test_measurement_scripts.py` gibt 1
    - `cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80` gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_dutch_analyzer.py tests/test_wordlist_nl.py tests/test_analyzer.py tests/test_language_analyzers.py tests/test_sandbox.py tests/test_measurement_scripts.py -q</automated>
  </verify>
  <done>Die Splitterkette existiert, ist gegen Reihenfolge, Tussenklank-Reste und Mehrfachbau abgesichert, und kein Aufrufer im Container benutzt sie noch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Volume `dict/nl-full.txt` -> Automat | Datei auf beschreibbarem Volume speist die Tokenisierung |
| Paketdatei /usr/share/dict/dutch -> Rezept | Eingabedaten aus dem Abbild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | build_artifact_nl | mitigate | Digestdatei muss den Inhalt beschreiben, sonst Rezept neu; Identitaetsschluessel inkl. Groesse und mtime; Test a_tampered_artifact |
| T-21-03-02 | Tampering | dutch_chain_for | mitigate | ValueError, wenn der Digest des Volumes nicht der erwartete ist (fail closed) |
| T-21-03-03 | Denial of Service | zweiter Automat | mitigate | nur bei aktivem nl, Singleton je Digest, Liste nach Bau freigegeben (D-02), Zaehler als Testhebel |
| T-21-03-04 | Information Disclosure | measure() | mitigate | nur Zahlen, keine Token (T-02-14) |
</threat_model>

<verification>
- Beide neuen Testdateien gruen, bestehende Analyzer-, Sprach- und Sandbox-Tests gruen
- wordlist.py unveraendert, ANALYZER_VERSION 1, Baumhash passend
</verification>

<success_criteria>
Rezept und Kette sind gebaut und getestet, ohne dass eine Installation davon etwas merkt.
</success_criteria>

<output>
Create `.planning/phases/21-niederlaendische-komposita/21-03-SUMMARY.md` when done
</output>
