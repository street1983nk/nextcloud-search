---
phase: 21-niederlaendische-komposita
plan: 06
type: execute
wave: 5
depends_on: ["21-05"]
files_modified:
  - backend/src/findling/index/open.py
  - backend/src/findling/index/rebuild.py
  - backend/src/findling/worker/poller.py
  - backend/src/findling/api/resources.py
  - backend/src/findling/tools/one_load.py
  - backend/src/findling/tools/index_status.py
  - backend/src/findling/index/bench.py
  - backend/tests/test_index_open.py
  - backend/tests/test_index_rebuild.py
  - backend/tests/test_read_side.py
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [KOMP-01]

must_haves:
  truths:
    - "Bei aktivem nl findet die Frage belasting ein Dokument mit gemeentebelastingen, und zwar ueber body_nl"
    - "Ohne Splitter findet dieselbe Frage dasselbe Dokument nicht (Gegenprobe am selben Index-Aufbau)"
    - "Ohne nl wird kein niederlaendischer Automat gebaut und die nl-Liste nicht gelesen (0 MB Zusatz)"
    - "Unter TOKENIZER_NL steht immer eine Kette; open_index registriert weiter genau 8 Ketten und keine unter einem if"
    - "Der Band-Umbau schreibt body_nl eines Bestandsdokuments mit der Splitterkette neu, ohne Vollreindex"
    - "Jeder Aufrufer von open_index in src nennt die nl-Wahl ausdruecklich"
  artifacts:
    - path: "backend/src/findling/index/open.py"
      provides: "open_index(path, constituents, *, dutch) mit freier Registrierung ueber dutch_chain_for"
      contains: "register_tokenizer(TOKENIZER_NL, dutch_chain_for(dutch))"
    - path: "backend/tests/test_index_open.py"
      provides: "Verhaltenstests nl an/aus, Gegenprobe ohne Splitter, Aufruf-Gate"
      contains: "gemeentebelastingen"
  key_links:
    - from: "backend/src/findling/index/open.py"
      to: "findling.index.analyzer.dutch_chain_for"
      via: "Auswahl der Variante in der Hilfsfunktion, nicht unter ast.If"
      pattern: "dutch_chain_for\\(dutch\\)"
    - from: "backend/src/findling/worker/poller.py, api/resources.py, index/rebuild.py, tools/one_load.py"
      to: "findling.index.wordlist_nl.dutch_digest_for"
      via: "dutch=dutch_digest_for(languages) an jeder open_index-Stelle"
      pattern: "dutch=dutch_digest_for\\("
---

<objective>
Die Splitterkette wird unter dem Namen `nl` registriert, aber nur bei aktivem Niederlaendisch;
ohne nl bleibt die heutige Snowball-Kette (Research Pattern 2). Damit wird Erfolgskriterium 2
im Container wahr: `gemeentebelastingen` wird ueber `belasting` gefunden.

Purpose: Letzter Verdrahtungsschritt. Nach diesem Plan ist ein Abbruch nur noch ein Revert der
Commit-Reihe 21-05/21-06 vor Phase 23 (Pattern 5); Phase 23 startet nicht mit halber Verdrahtung.

Output: neue Signatur von `open_index`, alle Aufrufer, Verhaltenstests, Aufruf-Gate, Baumhash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-niederlaendische-komposita/21-CONTEXT.md
@.planning/phases/21-niederlaendische-komposita/21-RESEARCH.md
@.planning/phases/21-niederlaendische-komposita/21-PATTERNS.md
@.planning/phases/21-niederlaendische-komposita/21-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Aus 21-03:
- `analyzer.dutch_chain_for(digest: str | None) -> TextAnalyzer` (None -> snowball_analyzer("dutch"), Digest -> Singleton, fail closed bei fremdem Artefakt)
- `analyzer.cached_dutch_analyzer(digest, constituents)`, `analyzer.dutch_build_count()`
- `wordlist_nl.dutch_digest_for(languages: Sequence[str]) -> str | None`, `wordlist_nl.read_count_nl()`
Aus 21-05: `conftest.write_wordlist_nl(root) -> str`, `open.DUTCH_MARK`

Heute backend/src/findling/index/open.py:
- `def open_index(path: Path, constituents: Sequence[str]) -> Index` (Z. 123), Registrierung Z. 137-159, nl: `index.register_tokenizer(TOKENIZER_NL, snowball_analyzer(SNOWBALL_NAME["nl"]))`
Gate backend/tests/test_index_open.py: `EXPECTED_REGISTRATIONS = 8`, `registrations_of_open_index` (Z. 581) zaehlt register_tokenizer-Aufrufe unter einem `ast.If` als Befund; Test `test_open_index_registers_eight_chains_and_hangs_none_of_them_on_a_condition` (Z. 662).

open_index-Aufrufer in src: poller.py Z. 373; resources.py Z. 688; rebuild.py Z. 551, 552 (Bandfunktion mit Parameter `languages`, `active = tuple(...)`), Z. 988, 995 (`_make_the_target_fit_this_code(target, constituents, wanted)`), one_load.py Z. 232; bench.py Z. 287; tools/index_status.py Z. 125 (`open_index(directory, ())`, wird nie gefragt).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gated Registrierung und Verhaltensbeweis im Index</name>
  <files>backend/src/findling/index/open.py, backend/tests/test_index_open.py</files>
  <read_first>
    - backend/src/findling/index/open.py (Z. 100-180 open_index mit Kommentaren)
    - backend/tests/test_index_open.py (Z. 300-370 Splitter-Gegenprobe mit `_splitterless_german`, `_write`, `_hits`; Z. 575-725 Registrierungs-Gate und Verhaltenstests unter FINDLING_LANGUAGES)
    - backend/src/findling/index/analyzer.py (dutch_chain_for aus 21-03)
    - backend/src/findling/api/resources.py (field_plan_for: body_nl wird nur gefragt, wenn nl in der languages-Marke steht)
  </read_first>
  <behavior>
    - `test_with_dutch_the_constituent_finds_the_compound`: Volume mit `write_wordlist_nl`, FINDLING_LANGUAGES=de,en,nl, `open_index(..., dutch=dutch_digest_for(("de","en","nl")))`, Dokument "De gemeentebelastingen voor dit jaar zijn verhoogd." in body_de und body_nl geschrieben (Muster `_write`), Frage `belasting` ueber body_nl ergibt 1 Treffer
    - `test_without_the_dutch_splitter_the_constituent_finds_nothing`: derselbe Aufbau mit `dutch=None`, dieselbe Frage ueber body_nl ergibt 0 (Gegenprobe des CI-Falls, beide Haelften in einem Test wie das deutsche Vorbild)
    - `test_without_dutch_no_dutch_automaton_is_built`: `dutch=None` -> `dutch_build_count()` und `read_count_nl()` unveraendert
    - `test_two_openings_with_dutch_build_one_automaton`: zweimal open_index mit demselben Digest -> `dutch_build_count()` +1 hoechstens
    - bestehendes `test_open_index_registers_eight_chains_and_hangs_none_of_them_on_a_condition` bleibt unveraendert gruen
    - `test_every_caller_in_src_names_the_dutch_choice` (AST ueber backend/src/findling: jeder Call auf open_index traegt Keyword `dutch`) plus Gegenprobe `test_the_dutch_choice_gate_sees_a_caller_without_it`
  </behavior>
  <action>
    Tests zuerst (RED). Dann `open_index(path: Path, constituents: Sequence[str], *, dutch: str | None = None) -> Index`.
    Discretion-Entscheid (dokumentiert im Docstring und in der SUMMARY): Default `None` statt
    Pflichtparameter, weil gut 100 Testaufrufe sonst mechanisch umgeschrieben wuerden; die
    Absicht der Research ("kein Aufrufer trifft die Entscheidung stillschweigend") haelt das
    AST-Gate `test_every_caller_in_src_names_the_dutch_choice` fuer src. Die Registrierung wird
    `index.register_tokenizer(TOKENIZER_NL, dutch_chain_for(dutch))`, frei und ohne `if`
    (Registrierungs-Gate, Phase-18-Pitfall 2: nie abwesend). Kommentar ueber der Zeile: ohne nl
    ist body_nl leer und wird von `field_plan_for` nicht gefragt, die Variante ist dort
    tokenisierungsneutral und spart 17,6 MB; mit nl steht die Splitterkette hinter der Faltung
    (D-07). Docstring von `open_index` um den Parameter. `snowball_analyzer` bleibt fuer es, it,
    pt. `EXPECTED_REGISTRATIONS` bleibt 8.
  </action>
  <acceptance_criteria>
    - `grep -c "register_tokenizer(TOKENIZER_NL, dutch_chain_for(dutch))" backend/src/findling/index/open.py` gibt 1
    - `grep -c "EXPECTED_REGISTRATIONS = 8" backend/tests/test_index_open.py` gibt 1
    - `cd backend && uv run pytest tests/test_index_open.py -q` gruen, die sechs Testnamen aus `<behavior>` existieren
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_index_open.py -q</automated>
  </verify>
  <done>Die Splitterkette antwortet bei aktivem nl, und ohne nl ist sie weder gebaut noch gelesen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alle Aufrufer, Re-Analyse-Beweis, Baumhash, volle Suite</name>
  <files>backend/src/findling/index/rebuild.py, backend/src/findling/worker/poller.py, backend/src/findling/api/resources.py, backend/src/findling/tools/one_load.py, backend/src/findling/tools/index_status.py, backend/src/findling/index/bench.py, backend/tests/test_index_rebuild.py, backend/tests/test_read_side.py, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/src/findling/index/rebuild.py (Z. 500-560 Bandfunktion, Z. 940-1000 _make_the_target_fit_this_code, Z. 1100-1200)
    - backend/src/findling/worker/poller.py (Z. 355-375 _open_writer)
    - backend/src/findling/api/resources.py (Z. 670-700 Leseseite)
    - backend/src/findling/tools/one_load.py (Z. 225-270), backend/src/findling/index/bench.py (Z. 275-295), backend/src/findling/tools/index_status.py (Z. 115-130)
    - backend/tests/test_index_rebuild.py (Test `test_the_text_reaches_the_chain_of_every_active_language` Z. 297 als Vorlage)
    - backend/tests/test_read_side.py (Test `test_a_fresh_container_searches_the_chains_it_was_switched_on_with`)
  </read_first>
  <behavior>
    - `test_the_rebuild_splits_the_dutch_compounds_of_an_existing_index` in test_index_rebuild.py: Bestandsindex unter de,en,nl ohne Splitter (dutch=None) mit dem Dokument aus Task 1, dann Band-Umbau unter aktivem nl mit Fixture-Artefakt: danach findet `belasting` das Dokument ueber body_nl, die Generation ist unveraendert, wordlist_hash_nl gestempelt
    - `test_a_fresh_container_finds_a_dutch_compound_through_its_constituent` in test_read_side.py: frischer Container unter de,en,es,it,nl,pt, das Dokument gecrawlt bzw. geschrieben, die Suchfunktion der Leseseite findet es mit `belasting`
  </behavior>
  <action>
    Jede src-Stelle nennt die Wahl ausdruecklich: poller.py `_open_writer`:
    `open_index(resolved.index_dir, artifact.entries, dutch=dutch_digest_for(resolved.languages))`;
    resources.py Leseseite `dutch=dutch_digest_for(settings().languages)`; rebuild.py Bandfunktion
    `dutch = dutch_digest_for(active)` einmal, beide `open_index(..., dutch=dutch)`;
    `_make_the_target_fit_this_code(target, constituents, wanted, *, dutch: str | None)` reicht es an
    beide `open_index`-Aufrufe durch, Aufrufer in `rebuild_the_index` uebergibt
    `dutch_digest_for(resolved.languages)`; one_load.py aus `resolved.languages`; bench.py und
    index_status.py `dutch=None` mit je einem Satz Begruendung (bench misst die deutsche Kette,
    index_status fragt den Index nie). Import `from findling.index.wordlist_nl import dutch_digest_for`.

    Tests aus `<behavior>` zuerst (RED ist fuer den Umbau-Test vor dem rebuild.py-Eingriff zu
    belegen), dann Code. Das Extraktionskind: `tests/test_sandbox.py::test_child_does_not_import_findling_index`
    muss gruen bleiben (deckt wordlist_nl ab, keine Aenderung).

    Baumhash im selben Commit wie der letzte src-Eingriff: `PACKAGE_TREE_HASH_TODAY` neu,
    `PACKAGE_FILES_TODAY` bleibt 57, Kommentar "Plan 21-06". Danach volle Suite und alle Gates;
    Tests, die jetzt an einem fehlenden nl-Artefakt scheitern, bekommen `write_wordlist_nl` in
    ihrer Vorbereitung (Dateien in der SUMMARY auflisten, keine Erwartung lockern). Lokaler
    Laufzeittest des Containers ist nicht Teil dieses Plans; den End-to-End-Beweis traegt CI
    (Plan 21-07 und 21-08).
  </action>
  <acceptance_criteria>
    - `grep -rn "open_index(" backend/src/findling --include=*.py | grep -v "def open_index" | grep -vc "dutch"` gibt 0 (mehrzeilige Aufrufe deckt das AST-Gate aus Task 1)
    - `cd backend && uv run pytest tests/test_index_rebuild.py tests/test_read_side.py tests/test_sandbox.py tests/test_poller.py tests/test_main_lifespan.py -q` gruen
    - `cd backend && uv run pytest -q` gruen (volle Suite), Zahlen in der SUMMARY
    - `cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80` gruen
    - `grep -c "ANALYZER_VERSION = 1" backend/src/findling/index/analyzer.py` gibt 1
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest -q && uv run ruff check . && uv run ruff format --check . && uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <done>Der Container registriert bei aktivem nl die Splitterkette an jeder Oeffnung, und ein Bestandsindex bekommt sie per Re-Analyse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Volume-Artefakt -> Registrierung | die nl-Liste auf dem Volume speist den Automaten jeder Oeffnung |
| Suchfrage -> body_nl | Nutzerfrage trifft die neue Kette |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06-01 | Tampering | dutch_chain_for bei fremdem Artefakt | mitigate | ValueError fail closed (21-03), Digest aus derselben Quelle wie die Marke |
| T-21-06-02 | Denial of Service | fehlende Registrierung | mitigate | Registrierung immer frei, Gate EXPECTED_REGISTRATIONS = 8 ohne if |
| T-21-06-03 | Denial of Service | RAM ohne nl | mitigate | dutch=None ohne nl, Test auf dutch_build_count und read_count_nl |
| T-21-06-04 | Elevation of Privilege | Treffer ueber body_nl | accept | kein neues Feld, PHP-Nachpruefung bleibt einzige Autoritaet (ASVS V4 unveraendert) |
</threat_model>

<verification>
- Treffer mit Splitter, kein Treffer ohne, kein Automat ohne nl
- Re-Analyse schreibt body_nl neu ohne Generationshub
- volle Suite, alle Gates, Baumhash
</verification>

<success_criteria>
Nutzer mit aktivem Niederlaendisch finden `gemeentebelastingen` ueber `belasting`; Installationen
ohne nl merken nichts.
</success_criteria>

<output>
Create `.planning/phases/21-niederlaendische-komposita/21-06-SUMMARY.md` when done
</output>
