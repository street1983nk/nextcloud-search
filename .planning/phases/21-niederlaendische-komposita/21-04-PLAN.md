---
phase: 21-niederlaendische-komposita
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - scripts/dev/measure_compounds_nl.sh
  - scripts/dev/compound_probe_nl.py
  - backend/tests/fixtures/compound_cases_nl.txt
  - backend/tests/fixtures/constituents_nl.txt
  - docs/measurements/2026-09-komposita-nl/README.md
  - docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt
  - docs/measurements/2026-09-komposita-nl/rohdaten/tokens-rezept-b.tsv
  - docs/measurements/2026-09-komposita-nl/rohdaten/fixture-subset.txt
  - docs/measurements/2026-09-komposita-nl/rohdaten/ram.txt
  - backend/tests/test_dutch_analyzer.py
  - backend/tests/test_language_analyzers.py
  - backend/tests/test_upgrade_compatibility.py
autonomous: true
requirements: [KOMP-01]

must_haves:
  truths:
    - "Das Repo-Messskript reproduziert im Wegwerf-Container die Research-Zahlen: 413288 Quellzeilen, 316740 Eintraege, Digest-Praefix ee7f3b8380c75283, 21/28 Komposita ueber ein Glied, 32/33 Waechter einteilig, 0/28 ohne Splitter"
    - "Das RAM-Budget des zweiten Automaten ist VOR der Verdrahtung produktnah im Repo gemessen (Liste freigegeben, Zielwert 17,5 bis 17,7 MB), und die 23 MB sind im Bericht als ueberholte Schaetzung benannt"
    - "Die Fixture constituents_nl.txt ist von der Sonde erzeugt, und ihre Tokenisierung ist gegen die Vollliste fuer jeden Fall gleich"
    - "Die gemessenen Token stehen als Tabellen-Tests, und jedes behauptete Wort steht in der gemessenen Fallliste"
    - "Die Phase-17-Tabellen fuer nl laufen gegen beide nl-Ketten, die Differenz ist benannt statt eine Kette still zu ersetzen"
  artifacts:
    - path: "scripts/dev/measure_compounds_nl.sh"
      provides: "Wegwerf-Container-Messung mit tantivy==0.26.2 und wdutch=1:2.20.19+1-3"
      contains: "tantivy==0.26.2"
    - path: "scripts/dev/compound_probe_nl.py"
      provides: "Sonde ueber die ausgelieferten Fabriken, Fixture-Erzeugung, RAM-Modus"
      contains: "dutch_analyzer"
    - path: "backend/tests/fixtures/compound_cases_nl.txt"
      provides: "28 Komposita mit Glied, 33 Waechter, datierter Vorbehalt A4"
    - path: "backend/tests/fixtures/constituents_nl.txt"
      provides: "Fixture-Teilmenge der Vollliste"
    - path: "docs/measurements/2026-09-komposita-nl/README.md"
      provides: "Messbericht Rezept, Fall fuer Fall, RAM, Budget"
      contains: "17,"
  key_links:
    - from: "scripts/dev/compound_probe_nl.py"
      to: "findling.index.analyzer.dutch_analyzer und findling.index.wordlist_nl.load_constituents_nl"
      via: "Import der ausgelieferten Funktionen, keine nachgebaute Kette"
      pattern: "from findling.index.(analyzer|wordlist_nl) import"
    - from: "backend/tests/test_dutch_analyzer.py"
      to: "backend/tests/fixtures/compound_cases_nl.txt"
      via: "guard over the guard"
      pattern: "compound_cases_nl"
---

<objective>
Die Rezeptmessung der Research wird zum wiederholbaren Werkzeug im Repo, erzeugt die
Test-Fixture und misst das RAM des zweiten Automaten produktnah (Erfolgskriterium 3, "RAM vor
dem Bau gemessen"; D-02, D-03, D-07). Keine Datei unter `backend/src/findling` wird beruehrt.

Purpose: Die Research-Zahlen sind im Scratchpad entstanden; eine Zahl, die nur dort existiert,
ist keine Abnahme. Die Sonde ruft die ausgelieferten Funktionen aus Plan 21-03 (Memory-Regel:
Gegenprobe nie mit dem Muster der Umsetzung). Eine Abweichung von den Research-Zahlen ist ein
BEFUND, kein Anlass, Code oder Zahlen nachzubessern: dann anhalten und berichten.

Output: Messskript, Sonde, zwei Fixtures, Rohdaten, Messbericht, gemessene Tabellen-Tests,
Pin-Test fuer das Messskript.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-niederlaendische-komposita/21-CONTEXT.md
@.planning/phases/21-niederlaendische-komposita/21-RESEARCH.md
@.planning/phases/21-niederlaendische-komposita/21-PATTERNS.md
@.planning/phases/21-niederlaendische-komposita/21-03-SUMMARY.md
@CLAUDE.md

Referenzzahlen (21-RESEARCH "Die Rezeptmessung" und "RAM"): Quelle 413288 Zeilen, 5096240 Byte,
4380 Zeilen mit Leerzeichen; Rezept B 4-14: 316740 Eintraege, SHA-256-Anfang `ee7f3b8380c75283`,
21/28, 32/33; ohne Splitter 0/28; RAM produktnah, Liste freigegeben 17,53 bis 17,66 MB, Bau
0,73 bis 0,80 s; Grundlast der Sonde mit deutscher Liste und deutschem Automat 94,2 bis 95,8 MB.
Die Fall-fuer-Fall-Tabelle "Rezept B 4-14, Fall fuer Fall" und die Waechterliste stehen dort
woertlich und sind die Quelle fuer `compound_cases_nl.txt`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fallliste, Sonde und Messskript</name>
  <files>backend/tests/fixtures/compound_cases_nl.txt, scripts/dev/compound_probe_nl.py, scripts/dev/measure_compounds_nl.sh, backend/tests/test_upgrade_compatibility.py</files>
  <read_first>
    - scripts/dev/compound_probe.py (komplett, 196 Zeilen)
    - scripts/dev/measure_compounds.sh (komplett, 123 Zeilen)
    - backend/tests/fixtures/chain_cases_nl.txt (Kommentarformat mit #, datierte Herkunft)
    - backend/src/findling/index/wordlist_nl.py und backend/src/findling/index/analyzer.py (die Vertraege aus 21-03)
    - backend/tests/test_upgrade_compatibility.py (Z. 40-50 Pfadkonstanten, Z. 368-381 `test_the_measurement_script_names_the_pinned_engine`)
  </read_first>
  <action>
    `backend/tests/fixtures/compound_cases_nl.txt`: Kopfkommentar mit `#` (Herkunft Research
    25.09.2026, Bedeutung einer Zeile, erwartete Kennzahlen, datierter Vorbehalt A4: "kein
    Muttersprachler hat die Faelle gelesen, 2026-09-25"). Eine Zeile je Fall: Komposita als
    `kompositum glied` (zwei Woerter, Leerzeichen), Waechter als ein Wort. Inhalt exakt die 28
    Zeilen der Research-Tabelle "Rezept B 4-14, Fall fuer Fall" (inklusive `coördinatiecentrum
    centrum` und `coordinatiecentrum centrum`) und die 33 Waechter (die 32 gelisteten plus
    `belastingplichtige`). Mit Python und `newline="\n"` schreiben.

    `scripts/dev/compound_probe_nl.py` nach Muster `compound_probe.py`, gleicher Docstring-Aufbau
    inklusive Sicherheitsregel T-02-14. Importiert `dutch_analyzer`, `snowball_analyzer` aus
    `findling.index.analyzer` und `SYSTEM_WORDLIST_NL`, `load_constituents_nl`, `ENCODING` aus
    `findling.index.wordlist_nl` bzw. `findling.index.wordlist`; baut KEINE eigene Kette.
    Usage `compound_probe_nl.py [--against LIST] CASES CHAIN_CASES OUTPUT_DIR` und
    `compound_probe_nl.py --ram`. Messmodus schreibt nach OUTPUT_DIR: `tokens-rezept-b.tsv`
    (Spalten word, chars, in_dutch, entry_in_list, tokens_without_splitter, tokens), 
    `fixture-subset.txt` (Eintraege der Vollliste, die als Teilstring eines gefalteten,
    kleingeschriebenen Wortes aus CASES oder CHAIN_CASES vorkommen, plus Tussenklanken, sortiert),
    `kennzahlen.txt` mit `key=value`: `source_lines`, `entries`, `digest`, `subset_entries`,
    `compounds_found_via_constituent` (Treffer = das einzige Token von `analyze(glied)` steht in
    `analyze(kompositum)`), `compounds_found_without_splitter` (gleiche Frage an
    `snowball_analyzer("dutch")`), `sentinels_whole` (genau ein Token), `sentinels_total`,
    `chain_cases_differing` (Zahl der CHAIN_CASES-Woerter, deren Token sich zwischen Snowball-
    und Splitterkette unterscheiden) und je solches Wort eine Zeile `chain_case_differs=<wort>`.
    Token-Gleichheit Teilmenge gegen Vollliste fuer JEDES Wort beider Dateien, sonst Exit 1 mit
    Namen der Faelle; `--against` wie im Original. RAM-Modus `--ram` (nur Zahlen): VmRSS ueber
    `rss_bytes()`; zuerst deutsche Liste per `findling.index.wordlist.load_constituents()` halten
    und `cached_german_analyzer` bauen (Reihenfolge des laufenden Containers), Messpunkt, dann
    niederlaendische Liste laden, `cached_dutch_analyzer` bauen, Liste loeschen plus `gc.collect()`,
    Messpunkt; Ausgabe `ram_base_mb`, `ram_nl_released_mb`, `nl_build_seconds`.

    `scripts/dev/measure_compounds_nl.sh` nach Muster `measure_compounds.sh`: `IMAGE="python:3.13-slim-trixie"`,
    `WDUTCH="wdutch=1:2.20.19+1-3"`, `WNGERMAN="wngerman=20161207-15"` (fuer den RAM-Modus),
    `TANTIVY="tantivy==0.26.2"` (NICHT 0.26.0 wie das deutsche Skript), Kopfkommentar mit den
    erwarteten Kennzahlen und dem Satz "A deviation is a finding, not a reason to edit a file by
    hand", read-only Mounts fuer `backend/src`, Sonde, beide Falllisten, Ausgabe nach
    `docs/measurements/2026-09-komposita-nl/rohdaten`, `--against` wie im Original,
    `DEBIAN_FRONTEND=noninteractive`, danach drei getrennte `docker run --rm` mit `--ram`, deren
    Ausgabe als `lauf=1..3` in `rohdaten/ram.txt` landet. Datei ausfuehrbar (`git update-index --chmod=+x`).

    `backend/tests/test_upgrade_compatibility.py`: Konstante `MEASURE_COMPOUNDS_NL` neben
    `MEASURE_CHAINS`, Test `test_the_dutch_measurement_script_names_the_pinned_engine`, der
    `TANTIVY_PIN` und `WDUTCH_PIN` im Skripttext findet (Muster des bestehenden Tests Z. 368).
  </action>
  <acceptance_criteria>
    - `grep -c "tantivy==0.26.2" scripts/dev/measure_compounds_nl.sh` gibt 1 und `grep -c "tantivy==0.26.0" scripts/dev/measure_compounds_nl.sh` gibt 0
    - `grep -cE "^from findling.index.(analyzer|wordlist_nl) import" scripts/dev/compound_probe_nl.py` mindestens 2
    - `grep -c "TextAnalyzerBuilder" scripts/dev/compound_probe_nl.py` gibt 0
    - `grep -vcE "^(#|$)" backend/tests/fixtures/compound_cases_nl.txt` gibt 61 (28 Komposita + 33 Waechter)
    - `cd backend && uv run pytest tests/test_upgrade_compatibility.py -q` gruen
    - `cd backend && uv run ruff check --config pyproject.toml ../scripts && uv run ruff format --config pyproject.toml --check ../scripts` gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_upgrade_compatibility.py -q && uv run ruff check --config pyproject.toml ../scripts && uv run ruff format --config pyproject.toml --check ../scripts</automated>
  </verify>
  <done>Werkzeug und Fallliste stehen; die Messung ist ein Kommando.</done>
</task>

<task type="auto">
  <name>Task 2: Messung fahren, Fixture erzeugen, Messbericht</name>
  <files>docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt, docs/measurements/2026-09-komposita-nl/rohdaten/tokens-rezept-b.tsv, docs/measurements/2026-09-komposita-nl/rohdaten/fixture-subset.txt, docs/measurements/2026-09-komposita-nl/rohdaten/ram.txt, backend/tests/fixtures/constituents_nl.txt, docs/measurements/2026-09-komposita-nl/README.md</files>
  <read_first>
    - docs/measurements/2026-09-komposita-rezept-a/README.md (Aufbau des deutschen Berichts)
    - docs/measurements/2026-09-grundlast-fein/README.md (amd64 gegen arm64 nativ, Praezedenz fuer A1)
    - docs/performance.md (Abschnitt "Drei Zahlen", Spitzenwert 1812,7 MB, Grenzwert 2000 MB)
    - .planning/phases/21-niederlaendische-komposita/21-RESEARCH.md (Abschnitte "Die Rezeptmessung", "RAM", "Budgetrechnung")
  </read_first>
  <action>
    `bash scripts/dev/measure_compounds_nl.sh` ausfuehren (Git-Bash, Docker Desktop laeuft;
    bei Pfadproblemen `MSYS_NO_PATHCONV=1`). Pruefen gegen die Referenzzahlen: `source_lines=413288`,
    `entries=316740`, `digest` beginnt mit `ee7f3b8380c75283`, `compounds_found_via_constituent=21`,
    `compounds_found_without_splitter=0`, `sentinels_whole=32`, `sentinels_total=33`, Sonde endet
    mit Exit 0 (Teilmenge gleich Vollliste). RAM: `ram_nl_released_mb` in drei Laeufen im Bereich
    der Research (17,5 bis 17,7 MB; Streuung bis 1 MB ist Messrauschen und wird so benannt). Weicht
    eine Rezeptzahl ab: ANHALTEN, Abweichung in die SUMMARY, nichts nachbessern.

    Danach `fixture-subset.txt` nach `backend/tests/fixtures/constituents_nl.txt` kopieren
    (Byte-Kopie, LF) und das Skript ein zweites Mal mit
    `--against backend/tests/fixtures/constituents_nl.txt` fahren: Exit 0 ist der Beleg, dass die
    Fixture wie die Vollliste tokenisiert.

    `docs/measurements/2026-09-komposita-nl/README.md` (deutsche Prosa mit echten Umlauten, kein
    Em-/En-Dash) im Aufbau des deutschen Berichts: Umgebung (Datum, Abbild, Pins), Rezepttabelle
    der Research mit Verweis, dass B 4-14 per Owner-Entscheid 25.09.2026 gewaehlt ist (D-03),
    Fall-fuer-Fall-Tabelle aus `tokens-rezept-b.tsv`, die sieben Nicht-Treffer als benannte
    Grenzen, Waechterergebnis mit Erklaerung `belastingplichtige` und `openbaarheid`, die Zeile
    ohne Splitter (0/28), RAM-Tabelle aus `ram.txt` plus die Research-Werte zum Vergleich,
    Budgetrechnung (1812,7 MB + gemessener Wert gegen 2000 MB), der Satz, dass die "rund 23 MB"
    aus Roadmap und Anforderung die alte Schaetzung des DEUTSCHEN Automaten aus Phase 2 sind und
    fuer Niederlaendisch nicht gelten, die Uebertragung auf ARM als Annahme A1 mit Verweis auf
    grundlast-fein und auf die native Nachmessung in Phase 22, Vorbehalt A4, Kommando zum
    Wiederholen.
  </action>
  <acceptance_criteria>
    - `grep -c "^entries=316740$" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt` gibt 1
    - `grep -c "^compounds_found_via_constituent=21$" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt` gibt 1
    - `grep -c "^compounds_found_without_splitter=0$" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt` gibt 1
    - `grep -c "^sentinels_whole=32$" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt` gibt 1
    - `grep -c "^digest=ee7f3b8380c75283" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt` gibt 1
    - `grep -c "ram_nl_released_mb" docs/measurements/2026-09-komposita-nl/rohdaten/ram.txt` gibt 3
    - `cmp backend/tests/fixtures/constituents_nl.txt docs/measurements/2026-09-komposita-nl/rohdaten/fixture-subset.txt` ohne Ausgabe
    - README enthaelt "23 MB" im Satz der Richtigstellung und keine U+2013/U+2014
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search && grep -c "^entries=316740$" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt && grep -c "^compounds_found_via_constituent=21$" docs/measurements/2026-09-komposita-nl/rohdaten/kennzahlen.txt && cmp backend/tests/fixtures/constituents_nl.txt docs/measurements/2026-09-komposita-nl/rohdaten/fixture-subset.txt</automated>
  </verify>
  <done>Die Research-Zahlen sind im Repo reproduziert, das RAM des zweiten Automaten ist vor der Verdrahtung gemessen, die Fixture ist erzeugt und belegt.</done>
</task>

<task type="auto">
  <name>Task 3: Messung wird Abnahme, Tabellen-Tests und beide nl-Ketten</name>
  <files>backend/tests/test_dutch_analyzer.py, backend/tests/test_language_analyzers.py</files>
  <read_first>
    - backend/tests/test_dutch_analyzer.py (Stand aus 21-03)
    - backend/tests/test_analyzer.py (Z. 91-150 COMPOUNDS/UNSPLIT, Z. 237-249 guard over the guard)
    - backend/tests/test_language_analyzers.py (Z. 400-520: CODES, EXPECTED_FAMILY_SCORES, Fixture-Leser, Messung ueber chain_probe)
    - docs/measurements/2026-09-komposita-nl/rohdaten/tokens-rezept-b.tsv und kennzahlen.txt (die gemessenen Token)
  </read_first>
  <action>
    `backend/tests/test_dutch_analyzer.py` erweitern: Modul-Fixture liest
    `fixtures/constituents_nl.txt` und baut `dutch_analyzer` einmal. Parametrisierte Tabelle
    `COMPOUNDS_NL` (die 21 Treffer mit genau den Token aus `tokens-rezept-b.tsv`, z. B.
    `gemeentebelastingen -> ["gemeent", "belast"]`), Test
    `test_the_measured_compounds_produce_the_measured_tokens`; Tabelle `UNSPLIT_NL` der sieben
    Nicht-Treffer mit ihrem gemessenen Token, Test `test_the_named_limits_stay_as_measured`;
    `test_the_sentinels_stay_whole` (alle Waechter ausser `belastingplichtige` ein Token);
    `test_every_asserted_word_stands_in_the_measured_case_list` (guard over the guard gegen
    `compound_cases_nl.txt`, Meldung nennt `scripts/dev/measure_compounds_nl.sh`);
    `test_without_the_splitter_no_compound_is_found_via_its_constituent` (0/28 ueber
    `snowball_analyzer("dutch")`, die Gegenprobe des CI-Falls).

    `backend/tests/test_language_analyzers.py`: die nl-Formfamilien aus `chain_cases_nl.txt`
    zusaetzlich durch `dutch_analyzer(fixture)` schicken. Neuer Test
    `test_the_dutch_splitter_chain_differs_from_the_snowball_chain_only_where_measured`: die Menge
    der Woerter mit abweichenden Token ist GENAU die Liste der `chain_case_differs=`-Zeilen aus
    `kennzahlen.txt` (als Konstante `DUTCH_SPLITTER_DIFFERENCES` im Test mit Verweis auf den
    Bericht; leere Menge ist ein gueltiges Messergebnis). `EXPECTED_FAMILY_SCORES["nl"]` und die
    bestehenden Tests bleiben unveraendert, weil sie die Snowball-Kette messen (Pitfall 4).
  </action>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_dutch_analyzer.py tests/test_language_analyzers.py -q` gruen
    - `grep -c "def test_every_asserted_word_stands_in_the_measured_case_list" backend/tests/test_dutch_analyzer.py` gibt 1
    - `grep -c "DUTCH_SPLITTER_DIFFERENCES" backend/tests/test_language_analyzers.py` mindestens 2
    - `grep -c '"nl": (57, 81)' backend/tests/test_language_analyzers.py` gibt 1 (unveraendert)
    - `cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80` gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_dutch_analyzer.py tests/test_language_analyzers.py tests/test_measurement_scripts.py -q</automated>
  </verify>
  <done>Die gemessenen Zahlen sind Tests; eine Kettenaenderung faellt an einer benannten Stelle auf.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Hub / Debian / PyPI -> Messcontainer | Wegwerf-Container zieht Abbild, Paket und tantivy |
| Messausgabe -> Repo | Rohdaten werden committet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Tampering | Messcontainer-Pins | mitigate | exakte Pins wdutch und tantivy==0.26.2 im Skript, Pin-Test; Mounts read-only |
| T-21-04-02 | Information Disclosure | Sonde | mitigate | liest nur Repo-Fallliste und Paketliste, nie state.db, Index oder Nutzerdatei (T-02-14) |
| T-21-04-03 | Repudiation | Fixture statt Vollliste | mitigate | Token-Gleichheit Teilmenge gegen Vollliste fuer jedes Wort, zweiter Lauf mit --against |
</threat_model>

<verification>
- Kennzahlen reproduziert, RAM dreifach gemessen, Fixture per --against belegt
- Tabellen-Tests, guard over the guard und Doppelkettentest gruen
</verification>

<success_criteria>
Die Rezeptmessung und die RAM-Messung liegen im Repo, wiederholbar und als Tests abgenommen,
bevor eine Installation den Splitter bekommt.
</success_criteria>

<output>
Create `.planning/phases/21-niederlaendische-komposita/21-04-SUMMARY.md` when done
</output>
