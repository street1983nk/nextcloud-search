---
phase: 18-schema-marken-und-umbauweg
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_query_rewrite.py
  - backend/tests/test_schema_generations.py
autonomous: true
requirements: [LEX-04]

must_haves:
  truths:
    - "Die Feldliste der Anfrage nennt nur Felder, die in Schema 1 UND in Schema 2 stehen"
    - "build_query gegen einen echten Schema-1-Index wirft nicht und erzeugt keine leere degradierte Antwort"
    - "Ein body_-Feld, das nur in Schema 2 steht, laesst den Waechter rot werden"
  artifacts:
    - path: "backend/tests/test_schema_generations.py"
      provides: "Mengeninklusion beider Schemastaende plus AST-Waechter ueber die Feldlisten"
      contains: "FIELDS_SCHEMA_1"
    - path: "backend/tests/conftest.py"
      provides: "Fixture, die einen echten Schema-1-Index baut"
      contains: "schema_1"
  key_links:
    - from: "backend/tests/test_schema_generations.py"
      to: "backend/src/findling/query/rewrite.py"
      via: "Syntaxbaum statt Import, DEFAULT_FIELDS und FIELD_BOOSTS"
      pattern: "DEFAULT_FIELDS"
---

<objective>
Der Zwischenzustand wird beweisbar ungefaehrlich: die Feldliste der Anfrage bleibt in
Phase 18 bei den vier Feldern, die beide Schemastaende fuehren, und ein Waechter macht die
Phasengrenze zu einer Codegrenze.

Purpose: Erfolgskriterium 1 der Phase verlangt, dass in KEINEM Zwischenzustand eine Suche
leer laeuft. Der gefaehrliche Pfad ist `parse_query_lenient` mit einem schemafremden Feld:
gemessen 2026-09-24 auf tantivy 0.26.2 wirft er `ValueError`, und der Suchpfad verschluckt
das zu einer dauerhaft leeren degradierten Antwort. Dieser Plan beweist, dass der Pfad in
Phase 18 nicht erreichbar ist, und zwar ohne eine einzige Produktivzeile zu bewegen.

Output: Eine neue Gate-Datei, eine Schema-1-Fixture, kein Eingriff in `backend/src`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-schema-marken-und-umbauweg/18-RESEARCH.md
@.planning/phases/18-schema-marken-und-umbauweg/18-PATTERNS.md
@CLAUDE.md

Dieser Plan aendert bewusst KEINE Datei unter `backend/src/findling`. Die Ratsche
(`PACKAGE_TREE_HASH_TODAY`) bewegt sich deshalb nicht, und der Plan kann neben 18-02 in
derselben Welle laufen.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema-1-Fixture und Mengeninklusion</name>
  <files>backend/tests/conftest.py, backend/tests/test_schema_generations.py</files>
  <read_first>
    - backend/tests/conftest.py Zeilen 130-175 (`write_index`, Kommentar "Field by field, never through keyword arguments")
    - backend/src/findling/index/schema.py nach Plan 18-01
    - backend/src/findling/query/rewrite.py Zeilen 40-80 (`DEFAULT_FIELDS`, `TITLE_ONLY_FIELDS`, `FIELD_BOOSTS`)
    - 18-RESEARCH.md Pattern 5
  </read_first>
  <action>
    In der neuen Datei `backend/tests/test_schema_generations.py` die Feldliste von
    Schema 1 als festgehaltene Konstante `FIELDS_SCHEMA_1` niederschreiben: die neun
    Namen, die jede ausgelieferte Fassung bis 1.2.0 im Index hatte. Darueber ein Absatz,
    warum diese Liste nicht aus dem Code kommen darf: der Code baut ab 18-01 Schema 2,
    eine aus ihm abgeleitete Liste waere eine Tautologie.

    Den Inklusionstest schreiben:
    `set(DEFAULT_FIELDS) <= set(FIELDS_SCHEMA_1)`, `set(DEFAULT_FIELDS) <= set(FIELDS)`,
    `set(TITLE_ONLY_FIELDS) <= set(FIELDS_SCHEMA_1)` und dieselbe Inklusion fuer die
    Schluessel von `FIELD_BOOSTS`. Im Docstring der Satz, was die Inklusion beweist: es
    gibt in dieser Phase keine Feldliste, die ein Feld nennen koennte, das ein
    Bestandsindex nicht hat, also ist der `ValueError`-Pfad nicht erreichbar.

    In `conftest.py` eine zweite Fixture nach der Bauart von `write_index` anlegen, die
    einen Index gegen ein festgehaltenes Schema-1-`SchemaBuilder`-Ergebnis baut (nicht
    gegen `build_schema()`, das jetzt dreizehn Felder liefert) und Dokumente hineinlegt.
    Reihenfolge wie im Vorbild: Feld fuer Feld, dann `commit()`, `wait_merging_threads()`,
    `reload()`. Ein Index, der ueber `open_index` geoeffnet wird, bekommt damit alle acht
    Ketten registriert, obwohl sein Schema nur zwei Koerperfelder kennt; das ist genau der
    Zustand einer Bestandsinstallation nach dem Upgrade und der Grund, warum die Fixture
    existiert.
  </action>
  <acceptance_criteria>
    - `FIELDS_SCHEMA_1` ist ein Literal mit genau neun Namen und wird nicht aus `findling.index.schema` importiert.
    - Alle vier Inklusionen laufen als eigene Assertions mit eigener Fehlermeldung, die das verletzende Feld nennt.
    - Die Fixture liefert einen Index, dessen `searcher().schema`-Suche nach `body_es` kein Ergebnis hat, waehrend `body_de` Terme fuehrt.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_schema_generations.py</automated>
  </verify>
  <done>Die Mengeninklusion steht, die Schema-1-Fixture baut einen echten Altindex.</done>
</task>

<task type="auto">
  <name>Task 2: AST-Waechter ueber die Feldlisten der Anfrage</name>
  <files>backend/tests/test_schema_generations.py</files>
  <read_first>
    - backend/tests/test_analyzer.py Zeilen 430-480 (`normalize_callers`, "Not a tautology, a ratchet")
    - backend/tests/test_search_fields_lockstep.py Zeilen 1-40 (Form der Gate-Dateien)
    - backend/src/findling/query/rewrite.py Zeilen 40-80
  </read_first>
  <action>
    Einen Waechter schreiben, der `backend/src/findling/query/rewrite.py` als Text liest
    und parst (nie importiert), aus dem Syntaxbaum die Namen zieht, die in `DEFAULT_FIELDS`,
    `TITLE_ONLY_FIELDS` und den Schluesseln von `FIELD_BOOSTS` stehen, und jeden Namen
    meldet, der mit `body_` beginnt und nicht in `FIELDS_SCHEMA_1` steht.

    Der Waechter loest ueber `ast.Name` gegen die Feldkonstanten von
    `findling.index.schema` auf, damit `FIELD_BODY_ES` und `"body_es"` derselbe Befund
    sind. Er faellt geschlossen: kann eine der drei Listen nicht gelesen werden, ist das
    ein Befund und kein stilles Null.

    Zwei Selbstproben gegen gestellte Quelltexte: eine mit einem `body_es` in
    `DEFAULT_FIELDS` (muss einen Befund liefern) und eine ohne (muss null liefern). Ohne
    diese Proben koennte ein geloeschter Rumpf null Befunde ueber null Zeilen melden und
    gesund aussehen.

    Im Docstring der Datei der Satz, warum dieser Waechter in Phase 19 bewusst umgebaut
    wird: dort werden `DEFAULT_FIELDS` und `FIELD_BOOSTS` zu Funktionen des gespeicherten
    `schema_version`-Merkers, und ab diesem Moment ist eine Konstantenpruefung sinnlos.
    Genau deshalb muss er heute existieren: er macht die Phasengrenze zu einer Codegrenze.
  </action>
  <acceptance_criteria>
    - Die gestellte Probe mit `body_es` in der Feldliste liefert genau einen Befund, der den Feldnamen nennt.
    - Die gestellte saubere Probe liefert null Befunde.
    - Der Waechter liest die Quelle ueber `Path.read_text` und `ast.parse`; `grep -c "^from findling.query.rewrite import" backend/tests/test_schema_generations.py` meldet hoechstens 1 (die Konstanten fuer den Inklusionstest), und der Waechter selbst benutzt sie nicht.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_schema_generations.py</automated>
  </verify>
  <done>Ein neues Koerperfeld in der Feldliste der Anfrage laesst die Suite rot werden, mit Selbstproben abgesichert.</done>
</task>

<task type="auto">
  <name>Task 3: build_query gegen einen echten Altindex</name>
  <files>backend/tests/test_query_rewrite.py</files>
  <read_first>
    - backend/tests/test_query_rewrite.py (bestehende Faelle und Fixtures)
    - backend/src/findling/query/rewrite.py, `build_query` und `parse_query_lenient`-Aufruf
    - backend/src/findling/api/resources.py `degraded()`
    - 18-RESEARCH.md Pattern 5, letzter Absatz
  </read_first>
  <action>
    Einen Test schreiben, der die Schema-1-Fixture aus Task 1 benutzt, `build_query` mit
    einer gewoehnlichen Anfrage gegen diesen Index fuehrt und zusichert: kein Wurf, eine
    nicht leere Trefferliste fuer ein Wort, das in den Fixture-Dokumenten steht, und keine
    degradierte Antwort.

    Ein Nachbau des Schemas im Test genuegt nicht, und der Docstring sagt warum: ein
    handgebauter `SchemaBuilder` im Test koennte von dem abweichen, was ausgeliefert
    wurde, und der Beweis waere gegen ein Schema gefuehrt, das nie existiert hat. Die
    Fixture baut deshalb gegen die festgehaltene Neun-Felder-Liste.

    Ein zweiter Test fuehrt denselben Weg gegen einen Schema-2-Index (die vorhandene
    `write_index`-Fixture) und zeigt, dass dieselbe Anfrage dort dieselbe Trefferzahl
    liefert. Das ist die Zusicherung von Erfolgskriterium 1 auf Testebene: derselbe
    Bestand antwortet unter beiden Schemastaenden gleich.
  </action>
  <acceptance_criteria>
    - Der Schema-1-Lauf liefert Treffer &gt; 0 und `pytest.raises` wird nirgends gebraucht.
    - Beide Laeufe liefern dieselbe Trefferzahl fuer denselben Begriff.
    - Volle Suite und alle vier Gates gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check &amp;&amp; uv run ruff format --check &amp;&amp; uv run pyright &amp;&amp; uv run vulture</automated>
  </verify>
  <done>Die Anfrage laeuft gegen einen echten Altindex durch, mit gleicher Trefferzahl wie gegen den neuen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anfragezeile -> `parse_query_lenient` | Nutzereingabe trifft auf eine Feldliste; ein schemafremdes Feld beendet die Suche mit einer Ausnahme |
| Phasengrenze 18 -> 19 | Eine Codeaenderung der naechsten Phase darf den Bestand nicht ruecklings treffen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Denial of Service | `query/rewrite.build_query` | mitigate | Mengeninklusion plus AST-Waechter halten die Feldliste innerhalb der Schnittmenge beider Schemastaende, solange sie eine Konstante ist |
| T-18-03-02 | Repudiation | Gate ohne Rumpf | mitigate | Selbstproben gegen gestellte Quelltexte, damit null Befunde nicht mit null Pruefung verwechselt werden |
| T-18-03-03 | Tampering | Testfixture gegen ein nie ausgeliefertes Schema | mitigate | `FIELDS_SCHEMA_1` ist ein Literal und wird ausdruecklich nicht aus dem Produktivcode abgeleitet |
| T-18-03-SC | Tampering | npm/pip/cargo installs | accept | Kein Paket; dieser Plan aendert nur Testdateien |
</threat_model>

<verification>
- Ein Schema-1-Index beantwortet dieselbe Anfrage mit derselben Trefferzahl wie ein Schema-2-Index.
- Ein eingebautes `body_es` in `DEFAULT_FIELDS` macht die Suite rot.
- Keine Datei unter `backend/src/findling` ist geaendert (`git diff --name-only backend/src` ist leer).
</verification>

<success_criteria>
- `backend/tests/test_schema_generations.py` existiert mit Inklusionstest, AST-Waechter und zwei Selbstproben.
- `conftest.py` traegt eine Schema-1-Fixture nach der Bauart von `write_index`.
- `build_query` ist gegen einen echten Altindex bewiesen.
</success_criteria>

<output>
Create `.planning/phases/18-schema-marken-und-umbauweg/18-03-SUMMARY.md` when done
</output>
