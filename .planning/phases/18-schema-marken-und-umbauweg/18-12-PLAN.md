---
phase: 18-schema-marken-und-umbauweg
plan: 12
type: execute
wave: 9
depends_on: ["18-10"]
files_modified:
  - .github/workflows/deploy-harp.yml
  - backend/tests/test_workflow_pins.py
autonomous: true
requirements: [LEX-08]

must_haves:
  truths:
    - "Die bestehende Strecke Store upgrade 0 bis 5 bleibt Zeile fuer Zeile stehen und laeuft von v1.2.0 aus gruen"
    - "Eine zweite, umgedrehte Strecke beweist, dass der Umbau vollstaendig lief"
    - "Eine Suche mitten im Umbau antwortet mit Treffern und nie leer"
    - "vectors.db ist vor und nach dem Umbau bytegleich"
  artifacts:
    - path: ".github/workflows/deploy-harp.yml"
      provides: "UPGRADE_FROM_TAG auf v1.2.0, Ersatz fuer Zusicherung 6, neuer Schritt Store upgrade 6"
      contains: "Store upgrade 6"
  key_links:
    - from: ".github/workflows/deploy-harp.yml"
      to: "findling.tools.index_status"
      via: "marks.languages als neuer sichtbarer Unterschied zwischen 1.2.0 und 1.3.0"
      pattern: "marks.languages"
---

<objective>
Die Beweisstrecke wird umgedreht statt entschaerft: `UPGRADE_FROM_TAG` wandert auf
`v1.2.0`, die bestehenden sechs Zusicherungen bleiben stehen, und daneben kommt ein
neuer Schritt, der den Umbau selbst beweist.

Purpose: Erfolgskriterium 5. Der gefaehrlichste Reflex der ganzen Phase waere, "Store
upgrade 5" gruen zu machen, indem man Zeilen daraus entfernt; danach haette das Projekt fuer
den einen Milestone, in dem der Upgrade-Pfad wirklich gefaehrlich ist, keinen
Upgrade-Waechter mehr.

Output: Ein bewegter Tag, ein Nachfolger fuer die gestorbene Zusicherung 6, ein neuer
Schritt mit neun Zusicherungen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-schema-marken-und-umbauweg/18-RESEARCH.md
@.planning/phases/18-schema-marken-und-umbauweg/18-PATTERNS.md
@CLAUDE.md

Dieser Plan aendert keine Datei unter `backend/src/findling` und keine unter `php/`, also
bewegt sich keiner der beiden Baumhashes. Er kann neben Plan 18-11 in derselben Welle
laufen.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tagwechsel und Nachfolger fuer die gestorbene Zusicherung</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml Zeilen 80-115 (`UPGRADE_FROM_TAG` und die uebrigen `env`-Werte)
    - .github/workflows/deploy-harp.yml Zeilen 2470-2500 (Vorpruefung "die Companion von UPGRADE_FROM_TAG erklaert den Navigationseintrag")
    - .github/workflows/deploy-harp.yml Zeilen 2690-2790 (Kommentarblock der Kandidatenpruefung fuer die heutige Zusicherung 6, und `snapshot()`)
    - .github/workflows/deploy-harp.yml ab Zeile 3256 ("Store upgrade 5", `unchanged()`-Helfer, der `tantivyVersion`-Block mit seinen drei Aesten)
    - 18-RESEARCH.md, Abschnitt "Die umgedrehte Beweisstrecke in deploy-harp.yml"
  </read_first>
  <action>
    `UPGRADE_FROM_TAG` von `v1.1.0` auf `v1.2.0` heben. Der bestehende Block "Store
    upgrade 0" bis "Store upgrade 5" bleibt Zeile fuer Zeile stehen; ein Diff, der dort
    Zeilen entfernt, ist der Fehler, vor dem die Recherche warnt. Dass dieser Block von
    v1.2.0 aus gruen bleibt, ist der Beweis fuer Erfolgskriterium 1: ein Upgrade 1.2.0 auf
    1.3.0 mit Werkseinstellung `de,en` bewegt keine der fuenf Marken, weil `schema_version`
    erst nach einem Umbau gestempelt wird und der Umbau ohne Sprachwechsel gar nicht
    startet.

    Zwei Stellen brauchen wegen des Tagwechsels eine Anpassung:
    Erstens die Vorpruefung um Zeile 2479: sie bleibt inhaltlich gueltig, weil auch 1.2.0
    den Navigationseintrag erklaert, aber ihr Kommentar begruendet das ueber 1.1.x und muss
    die Begruendung nachziehen.
    Zweitens Zusicherung 6, die beiden Datumsgrenzen des Providers: sie faellt um, weil die
    Grenzen seit Plan 13-06 in 1.2.0 stehen, also vorher `true` und nachher `true`. Ohne
    Ersatz verliert die Strecke den Beweis, dass ueberhaupt etwas ausgetauscht wurde.

    Der Ersatz ist `marks.languages` aus dem Bericht von `findling.tools.index_status`: er
    existiert in 1.2.0 nicht und in 1.3.0 schon, kostet keine Renderzeit und wird ueber
    denselben `index_report()`-Helfer gelesen, den die Probe ohnehin hat. Die
    Kandidatenpruefung wird dokumentiert wie der Kommentarblock bei Zeile 2705 es vormacht:
    welche Kandidaten geprueft wurden, warum die anderen ausschieden, und warum dieser
    traegt.

    `snapshot()` waechst um zwei Felder, `marks.languages` und `container.rebuildState`,
    und zwar in der vorhandenen Reihenfolge: jeder Wert erst in eine Shellvariable, jeder
    Leerwert ein Abbruch mit Ausgabe des Rohtextes, und erst danach baut ein einziges
    `jq -n` das Objekt.
  </action>
  <acceptance_criteria>
    - `grep -n "UPGRADE_FROM_TAG" .github/workflows/deploy-harp.yml` zeigt `v1.2.0`.
    - `git diff .github/workflows/deploy-harp.yml` entfernt keine Zeile zwischen "Store upgrade 0" und dem Ende von "Store upgrade 5", ausser den Zeilen der ersetzten Zusicherung 6, und diese werden durch den Nachfolger ersetzt statt gestrichen.
    - Der neue Kommentarblock nennt mindestens zwei gepruefte Kandidaten und den Grund ihrer Ablehnung.
    - `snapshot()` liefert die beiden neuen Felder, und ein Leerwert bricht ab.
    - `cd backend && uv run python -m pytest -q tests/test_workflow_pins.py` gruen (der Workflow bleibt syntaktisch und inhaltlich gegen seine Gates gueltig).
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_workflow_pins.py tests/test_lockstep_versions.py</automated>
  </verify>
  <done>Der Tag steht auf v1.2.0, die alte Strecke ist unversehrt, die gestorbene Zusicherung hat einen dokumentierten Nachfolger.</done>
</task>

<task type="auto">
  <name>Task 2: Store upgrade 6, der Umbau</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml ab Zeile 3256, insbesondere der `unchanged()`-Helfer und der `tantivyVersion`-Block mit seinen drei Aesten (unchanged, moved on purpose, Fehler)
    - .github/workflows/deploy-harp.yml, der `term_hits`-Helfer und die drei Begriffe, deren Einzigartigkeit `backend/tests/test_corpus_terms.py` festhaelt
    - .github/workflows/deploy-harp.yml Zeile 3257 (die Schrittbedingung, die jeder Schritt einzeln traegt)
    - 18-RESEARCH.md, Tabelle "Was der neue Schritt behauptet" mit den neun Zusicherungen
  </read_first>
  <action>
    Einen neuen Schritt "Store upgrade 6" NEBEN dem bestehenden Block anlegen, der auf dem
    Zustand aufsetzt, den "Store upgrade 5" hinterlaesst. Er traegt dieselbe
    Schrittbedingung wie jeder andere Schritt des Blocks, einzeln ausgeschrieben, weil die
    Datei keine Block-Bedingung kennt.

    Der Umbau wird auf der bereits aufgeruesteten Instanz ausgeloest, indem
    `FINDLING_LANGUAGES` gesetzt und der Container neu gestartet wird. Keine zweite
    Registrierung.

    Die neun Zusicherungen aus der Recherche, jede mit einem Satz, was sie bedeutet, und
    jede laeuft, auch wenn eine vorher fehlschlug (das ist der Grund fuer den
    `fail=0`-Helfer im Vorbild):
    1. `schemaVersion` hat sich um GENAU eine Stufe bewegt, gerechnet auf den beiden
       Werten der Momentaufnahmen, mit den drei Aesten des `tantivyVersion`-Blocks als
       Vorbild.
    2. `marks.languages` steht danach auf der gesetzten Menge, normalisiert nach
       Schemafeldreihenfolge, also `de,en,es` und nicht `es,de,en`.
    3. Das Umbau-Banner erschien und ist danach weg: drei Momentaufnahmen, vor dem
       Neustart, mitten im Lauf, danach. Die mittlere ist die, die heute fehlt.
    4. Die Trefferzahlen fuer `Belehrung`, `Auszug` und `Erinnerung` sind nach dem Umbau
       wieder identisch mit den Werten davor, ueber denselben `term_hits`-Helfer.
    5. Eine Suche MITTEN im Umbau antwortet mit Treffern: Kanarienabfrage
       `term_hits Belehrung` waehrend des Laufs, erwartet mindestens 1, niemals 0 und
       niemals ein HTTP-Status ungleich 200.
    6. `container.docs` ist nach dem Umbau gleich.
    7. Kein Nextcloud-Aufruf und kein OCR-Prozess waehrend des Umbaus:
       `nextcloud.scheduled` und `nextcloud.running` sind vor und nach dem Umbau gleich,
       und im Containerlog steht keine Zeile des Extraktionspfads.
    8. Genau EINE Zeile `built by different code` im Log, nicht null und nicht zwei; zwei
       hiesse, der Neustart hat den Umbau neu angestossen, also greift `REBUILD_MARK` nicht.
    9. `vectors.db` ist bytegleich, gemessen per `sha256sum` im Container vor und nach dem
       Umbau. Diese Zusicherung ist in keiner Recherche vorher benannt und ist die
       billigste Absicherung der Zusage "keine Neu-Einbettung": sie beweist sie am Artefakt
       statt ueber ein abwesendes Logmuster.

    Der Schritt bekommt eine Wartelogik mit Obergrenze fuer das Ende des Umbaus (Abfrage
    des Fortschritts aus der Statusantwort) und bricht mit einer Fehlermeldung ab, wenn die
    Obergrenze reisst. Eine unbegrenzte Warteschleife in CI ist ein haengender Lauf, und
    ein haengender Lauf ist ein Waechter, den man abschaltet.
  </action>
  <acceptance_criteria>
    - Der neue Schritt traegt die Schrittbedingung ausgeschrieben, wie jeder andere Schritt des Blocks.
    - Alle neun Zusicherungen stehen als eigene Vergleiche mit eigener Bedeutungszeile, und keine bricht den Schritt vorzeitig ab (`fail=1` statt `exit 1` je Vergleich).
    - Die Kanarienabfrage laeuft nachweislich WAEHREND des Laufs, nicht davor oder danach (Fortschrittswert in derselben Momentaufnahme belegt das).
    - Die Wartelogik hat eine Obergrenze und eine Fehlermeldung.
    - `cd backend && uv run python -m pytest -q` und alle vier Gates gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check &amp;&amp; uv run ruff format --check &amp;&amp; uv run pyright &amp;&amp; uv run vulture</automated>
  </verify>
  <done>Ein zweiter Schritt beweist den Umbau mit neun Zusicherungen, ohne dem ersten eine Zeile zu nehmen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Lauf -> Release-Freigabe | Eine entschaerfte Strecke gibt frei, was sie nicht mehr prueft |
| Container im CI -> Momentaufnahme | Ein leerer Wert in der Probe wuerde als Gleichheit durchgehen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12-01 | Repudiation | Entschaerfte Upgrade-Strecke | mitigate | Der bestehende Block bleibt Zeile fuer Zeile stehen; die neue Strecke steht daneben; ein Diff, der Zeilen entfernt, ist im Plan als Fehler benannt |
| T-18-12-02 | Repudiation | Zusicherung, die nichts mehr prueft | mitigate | Die gestorbene Zusicherung 6 wird durch `marks.languages` ersetzt, mit dokumentierter Kandidatenpruefung |
| T-18-12-03 | Repudiation | Probe mit Leerwerten | mitigate | Jeder Wert erst in eine Shellvariable, jeder Leerwert bricht mit Ausgabe des Rohtextes ab, bevor `jq -n` das Objekt baut |
| T-18-12-04 | Denial of Service | Unbegrenzte Warteschleife | mitigate | Obergrenze mit Fehlermeldung statt endlosem Warten |
| T-18-12-05 | Tampering | Stille Neu-Einbettung | mitigate | `sha256sum` auf `vectors.db` vor und nach dem Umbau, Gleichheit als eigene Zusicherung |
| T-18-12-06 | Denial of Service | Suche laeuft waehrend des Umbaus leer | mitigate | Kanarienabfrage mitten im Lauf, erwartet Treffer und HTTP 200 |
| T-18-12-SC | Tampering | npm/pip/cargo installs | accept | Kein Paket; der Workflow installiert nichts Neues und die Actions-Pins bleiben unberuehrt |
</threat_model>

<verification>
- "Store upgrade 5" laeuft von v1.2.0 aus gruen, unveraendert.
- "Store upgrade 6" beweist Umbau, Schemasprung um eine Stufe, Banner-Erscheinen und -Verschwinden, Treffererhalt, Kanarienabfrage, Dokumentzahl, Ruhe von Nextcloud und OCR, genau eine Driftzeile und bytegleiche `vectors.db`.
- Die Sprachfaelle auf Feldebene liegen in Plan 18-04 und laufen mit der Suite in jedem Ast.
</verification>

<success_criteria>
- `UPGRADE_FROM_TAG` steht auf `v1.2.0`.
- Der bestehende Block hat keine entfernte Zeile ausser der ersetzten Zusicherung.
- Der neue Schritt traegt alle neun Zusicherungen mit Bedeutungszeile.
</success_criteria>

<output>
Create `.planning/phases/18-schema-marken-und-umbauweg/18-12-SUMMARY.md` when done
</output>
