---
phase: 18-schema-marken-und-umbauweg
plan: 11
type: execute
wave: 9
depends_on: ["18-10"]
files_modified:
  - php/lib/Migration/Version001300Date20260925000000.php
  - php/tests/Unit/Version001300Date20260925000000Test.php
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [LEX-04]

must_haves:
  truths:
    - "Beim Upgrade auf 1.3.0 wird die aufgezeichnete Backendversion verworfen, wie bei jedem Minor-Sprung"
    - "Die Migration fragt den Container nichts und wartet auf nichts"
    - "Ein zweiter Lauf derselben Migration tut nichts und faellt nicht um"
  artifacts:
    - path: "php/lib/Migration/Version001300Date20260925000000.php"
      provides: "Lockstep-Migration des Minor-Sprungs 1.2.0 auf 1.3.0"
      contains: "postSchemaChange"
    - path: "php/tests/Unit/Version001300Date20260925000000Test.php"
      provides: "vier Faelle des Vorgaengers plus Zweitlauf plus Konstruktorprobe"
  key_links:
    - from: "php/lib/Migration/Version001300Date20260925000000.php"
      to: "ExAppService::KEY_BACKEND_VERSION"
      via: "deleteKey auf der aufgezeichneten Backendversion"
      pattern: "KEY_BACKEND_VERSION"
---

<objective>
Die Lockstep-Migration des Minor-Sprungs, eine Kopie des Musters von 1.2.0 mit neuem
Datum und neuem Klassennamen.

Purpose: Erfolgskriterium 1 verlangt sie ausdruecklich. Der Klassenkommentar des
Vorgaengers sagt, dass jeder Minor-Schritt eine Migration dieser Form braucht, solange die
aufgezeichnete Version so gefuehrt wird wie heute. Zehn Zeilen, doppellaufsicher, und sie
ist ausdruecklich NICHT der Ort des Indexumbaus.

Output: Zwei neue PHP-Dateien, bewegte PHP-Ratsche von 66 auf 68.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-schema-marken-und-umbauweg/18-RESEARCH.md
@.planning/phases/18-schema-marken-und-umbauweg/18-PATTERNS.md
@CLAUDE.md

Die Versionsnummern in `php/appinfo/info.xml` und `backend/appinfo/info.xml` werden in
diesem Plan NICHT bewegt. Der Bump auf 1.3.0 gehoert zur Release-Phase 23; die Migration
liegt vorher bereit und laeuft erst, wenn die Version sie erreicht.
Der Dateiname im Frontmatter ist ein Vorschlag: das Datum im Namen wird beim Anlegen auf
das tatsaechliche Datum gesetzt, und Datei- und Klassenname muessen zeichengleich sein.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration nach der Kopiervorlage</name>
  <files>php/lib/Migration/Version001300Date20260925000000.php</files>
  <read_first>
    - php/lib/Migration/Version001200Date20260921000000.php (vollstaendig, die Kopiervorlage)
    - php/lib/Migration/Version001100Date20260911000000.php (der Vorgaenger, auf den der Klassenkommentar verweist)
    - 18-PATTERNS.md Abschnitt zur Migration mit den drei Absaetzen, die mitwandern muessen
  </read_first>
  <action>
    Die Datei `Version001200Date20260921000000.php` kopieren und genau drei Dinge aendern:
    Dateiname, Klassenname und das Datum im Namen. Der Rumpf bleibt Zeile fuer Zeile
    derselbe: `postSchemaChange` liest die aufgezeichnete Backendversion, gibt bei leerem
    Wert eine Zeile aus und kehrt zurueck, sonst wird der Schluessel verworfen und die
    verworfene Version in der Ausgabe genannt.

    Drei Absaetze des Klassenkommentars wandern woertlich mit, weil sie nicht ueber 1.2.0
    reden, sondern ueber die Bauart: dass jeder Minor-Schritt eine Migration dieser Form
    braucht; dass von dieser Migration nichts vom Container verlangt wird, weil sie im
    Wartungsmodus ohne angemeldeten Nutzer laeuft und AppAPI den Container gerade neu
    starten koennte; und dass Datei- und Klassenname zeichengleich sein muessen, weil
    Nextcloud Migrationen nach Dateinamen laedt und ein Unterschied bedeutet, dass die
    Migration stillschweigend nie ausgefuehrt wird.

    Ein vierter Absatz kommt hinzu, und er ist der Grund, warum dieser Plan ueberhaupt
    einen Kommentar braucht: der Re-Analyse-Umbau der Phase 18 liegt ausdruecklich NICHT
    hier. Ein ein- bis dreistuendiger Lauf innerhalb von `occ upgrade` verwandelt ein
    App-Update in einen Ausfall. Der Umbau laeuft im Container, als Lifespan-Aufgabe, und
    die Suche antwortet waehrenddessen weiter.

    Keine Em-Dashes, keine Umlaute im Code und in den Kommentaren.
  </action>
  <acceptance_criteria>
    - Klassenname und Dateiname sind zeichengleich (per Test belegt).
    - `git diff --stat` zeigt, dass ausser Name und Datum keine Zeile des Rumpfs vom Vorgaenger abweicht.
    - Der Klassenkommentar enthaelt den Absatz, dass der Umbau nicht hier liegt.
    - Keine `-` der Codepunkte U+2013 oder U+2014 in der Datei.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_lockstep_versions.py tests/test_ops_scripts.py</automated>
  </verify>
  <done>Die Migration steht als Kopie des Musters, mit dem Absatz, der den Umbau ausdruecklich woanders verortet.</done>
</task>

<task type="auto">
  <name>Task 2: Unittest der Migration und PHP-Ratsche auf 68</name>
  <files>php/tests/Unit/Version001300Date20260925000000Test.php, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - php/tests/Unit/Version001200Date20260921000000Test.php (vollstaendig, die vier Faelle plus Zweitlauf plus Konstruktorprobe)
    - backend/tests/test_measurement_scripts.py Zeilen 490-540 (PHP-Kommentarkette, insbesondere der Absatz zu Plan 16-07, der genau diesen Fall beschreibt: zwei ankommende Dateien, 64 wird 66)
  </read_first>
  <action>
    Den Unittest des Vorgaengers kopieren und auf die neue Klasse umstellen. Die Faelle
    bleiben dieselben: leere aufgezeichnete Version (eine Ausgabezeile, kein Loeschen),
    gesetzte Version (Loeschen, Version in der Ausgabe), zweiter Lauf nach dem ersten (tut
    nichts, faellt nicht um), und die Probe, dass dem Konstruktor nichts uebergeben wurde,
    was den Container erreichen koennte.

    Danach die PHP-Haelfte der Ratsche bewegen: das Rezept
    `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py` ueber `php`
    mit dem Muster `**/*.php` fahren, `PHP_FILES_TODAY` von 66 auf 68 heben und
    `PHP_TREE_HASH_TODAY` setzen. Die Kommentarkette bekommt einen Absatz nach dem Vorbild
    des Absatzes zu Plan 16-07: zwei Dateien kommen an, die Migration und ihr Unittest,
    keine Datei geht, kein anderer Rumpf der Haelfte bewegt sich.
  </action>
  <acceptance_criteria>
    - `PHP_FILES_TODAY == 68` und `cd backend && uv run python -m pytest -q tests/test_measurement_scripts.py` gruen.
    - Der neue Absatz der PHP-Kommentarkette nennt beide Dateinamen.
    - Volle Suite und alle vier Gates gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check &amp;&amp; uv run ruff format --check &amp;&amp; uv run pyright &amp;&amp; uv run vulture</automated>
  </verify>
  <done>Der Unittest deckt dieselben Faelle wie beim Vorgaenger, die PHP-Ratsche steht auf 68 mit neuem Hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `occ upgrade` -> Nextcloud-Datenbank | Die Migration schreibt in `appconfig`, im Wartungsmodus, ohne angemeldeten Nutzer |
| Migration -> Container | Jede Abhaengigkeit von einer Containerantwort waere ein Update, das haengen bleibt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11-01 | Denial of Service | Migration wartet auf den Container | mitigate | Der Rumpf ruft weder `adminGet` noch `proxyRequest`; der Konstruktor bekommt nur `IAppConfig`, und ein Test belegt das |
| T-18-11-02 | Repudiation | Migration laeuft nie, weil der Name abweicht | mitigate | Klassenname und Dateiname zeichengleich, per Test geprueft; der Absatz dazu wandert mit |
| T-18-11-03 | Tampering | Doppellauf verwirft einen frisch geschriebenen Wert | mitigate | Der Zweitlauf findet einen leeren Wert und tut nichts; eigener Testfall |
| T-18-11-04 | Denial of Service | Umbau in der Migration | mitigate | Ausdruecklich nicht hier; der Klassenkommentar sagt es, und ein Ein- bis Dreistundenlauf in `occ upgrade` waere ein Ausfall |
| T-18-11-SC | Tampering | npm/pip/cargo installs | accept | Kein Paket; `composer.json` unveraendert |
</threat_model>

<verification>
- Vier Testfaelle plus Zweitlauf plus Konstruktorprobe gruen.
- `PHP_FILES_TODAY` auf 68, Hash stimmt gegen den Baum.
- Versionsnummern in beiden `info.xml` unveraendert.
</verification>

<success_criteria>
- Migration und Unittest existieren als Kopie des 1.2.0-Musters mit neuem Datum.
- Der Klassenkommentar verortet den Umbau ausdruecklich im Container.
- PHP-Ratsche bewegt, Anzahl 68.
</success_criteria>

<output>
Create `.planning/phases/18-schema-marken-und-umbauweg/18-11-SUMMARY.md` when done
</output>
