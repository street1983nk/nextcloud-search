---
phase: 18-schema-marken-und-umbauweg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/findling/index/analyzer.py
  - backend/src/findling/index/schema.py
  - backend/src/findling/index/open.py
  - backend/src/findling/config.py
  - backend/tests/test_index_open.py
  - backend/tests/test_language_analyzers.py
  - backend/tests/test_upgrade_compatibility.py
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [LEX-02]

must_haves:
  truths:
    - "Das Schema fuehrt dreizehn Felder, sechs davon Koerperfelder, unabhaengig von FINDLING_LANGUAGES"
    - "Ein Schreibvorgang gegen das neue Schema wirft keine ValueError, auch wenn nur de,en aktiv ist"
    - "SCHEMA_VERSION steht auf 2, und die Sperrklinke haelt den Sprung als genau eine Stufe fest"
  artifacts:
    - path: "backend/src/findling/index/schema.py"
      provides: "dreizehn Felder, BODY_FIELD-Abbildung, fortgeschriebener Modulkopf"
      contains: "body_pt"
    - path: "backend/src/findling/index/open.py"
      provides: "acht Kettenregistrierungen, unbedingt"
      contains: "register_tokenizer"
    - path: "backend/tests/test_upgrade_compatibility.py"
      provides: "GOLD_V1_3 neben GOLD_V1_0_AND_V1_1 plus Ein-Stufen-Test"
      contains: "GOLD_V1_3"
  key_links:
    - from: "backend/src/findling/index/schema.py"
      to: "backend/src/findling/index/analyzer.py"
      via: "Tokenizernamen der vier neuen Ketten"
      pattern: "TOKENIZER_(ES|IT|NL|PT)"
    - from: "backend/src/findling/index/open.py"
      to: "findling.index.analyzer.snowball_analyzer"
      via: "Kettenfabrik statt Nachbau"
      pattern: "snowball_analyzer"
---

<objective>
Das Schema traegt ab sofort alle sechs Koerperfelder, `open_index` registriert alle
Ketten unbedingt, und die Schemamarke springt begruendet von 1 auf 2.

Purpose: Ohne die sechs Felder und die acht Registrierungen gibt es keinen Umbau und
keine neue Sprache. Die Marke muss im selben Plan springen, weil das Kommentargesetz in
`config.py` genau das verlangt ("Raised when a field is added") und weil die Sperrklinke
`test_upgrade_compatibility.py` sonst rot bleibt.

Output: Dreizehn Felder, acht Ketten, `SCHEMA_VERSION = 2`, zweite Goldtabelle mit
Begruendungsabsatz, bewegter Baumhash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-schema-marken-und-umbauweg/18-RESEARCH.md
@.planning/phases/18-schema-marken-und-umbauweg/18-PATTERNS.md
@CLAUDE.md

Bindende Vorgaben dieser Phase:
- Kein Paket wird installiert (18-RESEARCH.md, Package Legitimacy Audit: leere Liste).
- Der `tantivy`-Pin steht auf 0.26.2 und wird in dieser Phase NICHT bewegt.
- Code, Bezeichner, Kommentare und Logtexte englisch und ASCII; keine Em-Dashes.
- Lokal gruen vor dem Commit: `uv run python -m pytest -q`, `uv run ruff check`,
  `uv run ruff format --check`, `uv run pyright`, `uv run vulture` (aus `backend/`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vier Kettennamen und dreizehn Felder</name>
  <files>backend/src/findling/index/analyzer.py, backend/src/findling/index/schema.py</files>
  <read_first>
    - backend/src/findling/index/schema.py (ganz, 116 Zeilen)
    - backend/src/findling/index/analyzer.py (Modulkopf und `snowball_analyzer`, `english_analyzer`, die Tokenizernamen-Konstanten)
    - backend/src/findling/config.py Zeilen 108-150 (`SUPPORTED_LANGUAGES`, `SNOWBALL_NAME`)
    - 18-PATTERNS.md Abschnitt "index/schema.py"
  </read_first>
  <action>
    In `analyzer.py` neben `TOKENIZER_DE`, `TOKENIZER_EN` und `TOKENIZER_NAME` die vier
    Namen `TOKENIZER_ES`, `TOKENIZER_IT`, `TOKENIZER_NL`, `TOKENIZER_PT` mit denselben
    kurzen Werten anlegen, die die vorhandenen tragen (Sprachcode als Name). Keine neue
    Fabrik bauen: `snowball_analyzer(language)` existiert einarmig und ist die Quelle
    aller vier Ketten.

    In `schema.py` die Feldkonstanten `FIELD_BODY_ES`, `FIELD_BODY_IT`, `FIELD_BODY_NL`,
    `FIELD_BODY_PT` ergaenzen, `FIELDS` in Schemafeldreihenfolge auf dreizehn Eintraege
    heben (die vier neuen unmittelbar nach `FIELD_BODY_EN`, vor `FIELD_MTIME`), und in
    `build_schema()` vier `add_text_field(..., stored=False, tokenizer_name=...)`-Zeilen
    nach dem `body_en`-Vorbild anlegen, jede mit eigener Begruendungszeile.

    Eine geschlossene Abbildung `BODY_FIELD: Final = {"de": FIELD_BODY_DE, ...}` fuer alle
    sechs Codes in `schema.py` anlegen; sie ist die einzige Stelle, an der ein Sprachcode
    zu einem Feldnamen wird, und sie ist nie eine zusammengesetzte Zeichenkette. Ein Test
    stellt sie gegen `SNOWBALL_NAME` aus `config.py`, damit die beiden Abbildungen
    dieselben Schluessel in derselben Reihenfolge fuehren.

    Den Modulkopf fortschreiben: er sagt heute "nine fields" und ist damit nach diesem
    Task falsch. Die beiden gemessenen Zahlen aus 18-RESEARCH.md Pattern 6 gehoeren
    hinein, mit Datum: vier leere Felder kosten +0,40 Prozent Verzeichnisgroesse (gemessen
    2026-09-24 ueber 2000 Dokumente), vier befuellte Ketten kosten +150 Prozent, je
    befuellte Kette 0,086 mal die Textmenge. Der Satz aus E-17-2, leere Felder kosteten
    null Byte, wird dabei ausdruecklich als von dieser Messung korrigiert benannt.
  </action>
  <acceptance_criteria>
    - `grep -c "add_text_field\|add_unsigned_field\|add_integer_field" backend/src/findling/index/schema.py` meldet 13.
    - `python -c "from findling.index.schema import FIELDS, BODY_FIELD; assert len(FIELDS)==13; assert tuple(BODY_FIELD)==('de','en','es','it','nl','pt')"` laeuft aus `backend/` unter `uv run` fehlerfrei.
    - Der Modulkopf enthaelt weder das Wort "nine" noch die Behauptung "null Byte"; er nennt 0,40 und 0,086 mit dem Datum 2026-09-24.
    - `uv run ruff check` und `uv run pyright` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_index_open.py tests/test_language_analyzers.py</automated>
  </verify>
  <done>Dreizehn Felder stehen im Schema, BODY_FIELD ist die einzige Code-zu-Feld-Abbildung, der Modulkopf widerspricht sich nicht mehr.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Acht Ketten, unbedingt registriert</name>
  <files>backend/src/findling/index/open.py, backend/tests/test_index_open.py</files>
  <read_first>
    - backend/src/findling/index/open.py Zeilen 1-102
    - backend/src/findling/index/analyzer.py, Signatur von `snowball_analyzer`
    - 18-RESEARCH.md Pitfall 2 und die Messtabelle in Pattern 5
  </read_first>
  <behavior>
    - `open_index` registriert acht Ketten: sechs Koerperketten, `name`, `stored_only`.
    - Die Registrierung haengt NICHT an `settings().languages`: mit
      `FINDLING_LANGUAGES=de` laeuft ein `writer.add_document(...)` gegen das neue Schema
      ohne Ausnahme durch. Gemessen 2026-09-24: eine nicht registrierte Kette eines
      vorhandenen Textfeldes wirft `ValueError: Schema error: 'Error getting tokenizer
      for field: body_es'`, auch wenn das Dokument dieses Feld gar nicht traegt.
    - Ein Test stellt fest, dass keine `if`-Bedingung ueber einer der sechs
      `register_tokenizer`-Zeilen steht: gelesen ueber den Syntaxbaum von `open.py`, nach
      dem Muster `backend/tests/test_analyzer.py:440-470`, nicht ueber eine Textsuche.
  </behavior>
  <action>
    Die vier neuen `index.register_tokenizer(TOKENIZER_XX, snowball_analyzer("<name>"))`
    Zeilen unmittelbar hinter `TOKENIZER_EN` einfuegen; den Sprachnamen aus
    `SNOWBALL_NAME` nehmen und nie als Literal wiederholen. Die deutsche Kette bleibt
    `cached_german_analyzer`, weil nur sie den Zerlegungsautomaten traegt.

    Ueber die vier Zeilen einen Absatz setzen, der die gemessene Falle benennt und warum
    die Registrierung nichts kostet (Snowball-Ketten sind in tantivy einkompiliert, nur
    der deutsche Automat kostet die rund 23 MB und wird ohnehin immer gebaut).

    Den AST-Waechter in `test_index_open.py` ergaenzen: die Zahl der
    `register_tokenizer`-Aufrufe in `open_index` ist genau acht, und keiner von ihnen
    liegt unterhalb eines `ast.If`. Der Waechter traegt eine Selbstprobe gegen ein
    gestelltes Muster, damit ein geloeschter Rumpf nicht null Befunde ueber null Zeilen
    meldet.
  </action>
  <acceptance_criteria>
    - Neuer Test faellt rot, wenn eine der sechs Koerperketten-Registrierungen entfernt oder in ein `if` gestellt wird (durch eine gestellte Probe im Test selbst belegt).
    - Ein Test schreibt mit `FINDLING_LANGUAGES=de` ein Dokument in einen frischen Index und liest es zurueck, ohne Ausnahme.
    - `grep -v '^ *#' backend/src/findling/index/open.py | grep -c register_tokenizer` meldet 8.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_index_open.py</automated>
  </verify>
  <done>Acht Ketten sind immer registriert, ein Test haelt die Unbedingtheit fest, ein Schreibvorgang unter de,en laeuft durch.</done>
</task>

<task type="auto">
  <name>Task 3: SCHEMA_VERSION 2 und die umgedrehte Sperrklinke</name>
  <files>backend/src/findling/config.py, backend/tests/test_upgrade_compatibility.py, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/src/findling/config.py Zeilen 34-48
    - backend/tests/test_upgrade_compatibility.py Zeilen 1-140 (Kopf, GOLD_V1_0_AND_V1_1, ALL_MARKS, TANTIVY_PIN mit seinem Begruendungsabsatz, `drift_findings`)
    - .planning/phases/17-owner-tor-und-analyseketten/17-GRUNDSATZ-ENTSCHEID.md Abschnitt E-17-1 bis E-17-4 und der Vollzugsabschnitt
    - 18-RESEARCH.md Pitfall 5
    - backend/tests/test_measurement_scripts.py Zeilen 596-700 (Kommentarkette, PACKAGE_FILES_TODAY, PACKAGE_TREE_HASH_TODAY)
  </read_first>
  <action>
    `SCHEMA_VERSION = 2` in `config.py` setzen und den Kommentar darueber um den Satz
    ergaenzen, wann und unter welchem Entscheid die Marke gesprungen ist (23.09.2026,
    E-17-1 bis E-17-4 alle Option a, vier neue Koerperfelder).

    In `test_upgrade_compatibility.py` NICHTS an `GOLD_V1_0_AND_V1_1` aendern. Daneben
    eine zweite Goldtabelle `GOLD_V1_3` anlegen, mit `schema_version` auf `"2"` und den
    uebrigen Marken unveraendert. Darueber den Begruendungsabsatz nach der Bauart des
    `TANTIVY_PIN`-Absatzes: Datum, Entscheidnummern, was die Marke bedeutet und warum ein
    Sprung hier eine Freigabe hat und keine Reparatur ist.

    Einen Test ergaenzen, der den Sprung als GENAU eine Stufe festhaelt
    (`int(GOLD_V1_3["schema_version"]) - int(GOLD_V1_0_AND_V1_1["schema_version"]) == 1`),
    mit dem Satz im Docstring, warum zwei Stufen ein Befund waeren: eine uebersprungene
    Stufe ist ein Schema, das nie ausgeliefert wurde, und damit ein Umbauweg, den nichts
    beweist. `ALL_MARKS` bleibt in diesem Plan bei fuenf Eintraegen; der sechste kommt in
    Plan 18-04 und wird dort begruendet.

    Danach die Ratsche bewegen: das Rezept
    `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py` ueber
    `backend/src/findling` mit dem Muster `**/*.py` fahren, die beiden abgelesenen Zahlen
    nach `PACKAGE_FILES_TODAY` (bleibt 55, keine Datei kommt oder geht) und
    `PACKAGE_TREE_HASH_TODAY` schreiben und die Kommentarkette um einen Absatz
    fortschreiben, der die geaenderten Dateien einzeln nennt (analyzer.py, schema.py,
    open.py, config.py) und sagt, was sie bekommen haben.
  </action>
  <acceptance_criteria>
    - `grep -n "SCHEMA_VERSION = 2" backend/src/findling/config.py` trifft genau einmal.
    - `git diff backend/tests/test_upgrade_compatibility.py` entfernt keine Zeile aus `GOLD_V1_0_AND_V1_1` und keine Zeile des Dateikopfs.
    - Der neue Absatz in `test_upgrade_compatibility.py` nennt das Datum 23.09.2026 und mindestens eine Entscheidnummer der Form `E-17-`.
    - `cd backend && uv run python -m pytest -q tests/test_measurement_scripts.py` gruen, also stimmt der neue Baumhash gegen den Baum.
    - Volle Suite und alle vier Gates gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check &amp;&amp; uv run ruff format --check &amp;&amp; uv run pyright &amp;&amp; uv run vulture</automated>
  </verify>
  <done>Die Marke steht auf 2, die alte Goldtabelle ist unberuehrt, der Sprung ist als eine Stufe festgehalten, die Ratsche traegt den neuen Hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Umgebungsvariable -> Kettenfabrik | `FINDLING_LANGUAGES` waehlt Sprachnamen aus; ein Name ausserhalb der Liste erreicht sonst `Filter.stopword` |
| Code -> Bestandsindex auf Platte | Ein geaendertes `build_schema()` darf einen vorhandenen Index nicht umdeuten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | `index/schema.py` Feldnamen | mitigate | Feldnamen kommen aus `BODY_FIELD`, einer geschlossenen Abbildung; nie aus einer zusammengesetzten Zeichenkette (gemessen: ein unbekannter Name wird beim Schreiben spurlos verschluckt) |
| T-18-01-02 | Denial of Service | `open_index` | mitigate | Alle sechs Koerperketten immer registriert; eine bedingte Registrierung liesse jeden Schreibvorgang mit `ValueError` enden und den Indexer stehen |
| T-18-01-03 | Repudiation | `SCHEMA_VERSION` | mitigate | Sprung nur mit Begruendungsabsatz, Datum und Entscheidnummer in `test_upgrade_compatibility.py`; `GOLD_V1_0_AND_V1_1` bleibt als Zeuge stehen |
| T-18-01-04 | Injection | Sprachname -> `Filter.stopword` | transfer | Geschlossen durch `LANGUAGE_ALLOWLIST` aus Plan 17-02; dieser Plan liest die Liste und schreibt sie nicht |
| T-18-01-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket; `backend/pyproject.toml` und `backend/uv.lock` werden nicht angefasst, der `tantivy`-Pin bleibt auf 0.26.2 |
</threat_model>

<verification>
- Ein frisch angelegter Index traegt dreizehn Felder, ein Bestandsindex bleibt bei neun (weil `Index.open()` das persistierte Schema liest und `build_schema()` dann gar nicht gerufen wird).
- Ein Schreibvorgang unter jeder Sprachmenge laeuft ohne Ausnahme durch.
- Volle Suite, ruff, ruff format, pyright, vulture gruen.
</verification>

<success_criteria>
- `FIELDS` hat dreizehn Eintraege, `BODY_FIELD` sechs.
- `open_index` registriert acht Ketten, unbedingt, mit Test.
- `SCHEMA_VERSION == 2`, `GOLD_V1_3` steht neben `GOLD_V1_0_AND_V1_1`, Ein-Stufen-Test gruen.
- `PACKAGE_TREE_HASH_TODAY` stimmt gegen den Baum, `PACKAGE_FILES_TODAY` bleibt 55.
</success_criteria>

<output>
Create `.planning/phases/18-schema-marken-und-umbauweg/18-01-SUMMARY.md` when done
</output>
