---
phase: 18-schema-marken-und-umbauweg
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - backend/src/findling/config.py
  - backend/src/findling/index/writer.py
  - backend/appinfo/info.xml
  - backend/tests/test_config.py
  - backend/tests/test_index_writer.py
  - backend/tests/test_measurement_scripts.py
  - docs/language-analyzers.md
autonomous: true
requirements: [LEX-02]

must_haves:
  truths:
    - "FINDLING_LANGUAGES=de,en,es fuehrt zu einem Index, in dem body_es befuellt ist"
    - "body_de traegt den Text auch dann, wenn de nicht aktiv ist, weil es die einzige gespeicherte Kopie ist"
    - "DEFAULT_LANGUAGES bleibt ('de','en'), die Werkseinstellung bewegt sich nicht"
  artifacts:
    - path: "backend/src/findling/index/writer.py"
      provides: "Schleife ueber die aktive Sprachmenge statt des einzelnen Englisch-Flags"
      contains: "BODY_FIELD"
    - path: "backend/src/findling/config.py"
      provides: "_languages() filtert gegen SUPPORTED_LANGUAGES statt gegen DEFAULT_LANGUAGES"
      contains: "SUPPORTED_LANGUAGES"
  key_links:
    - from: "backend/src/findling/index/writer.py"
      to: "findling.index.schema.BODY_FIELD"
      via: "Feldname je aktivem Sprachcode"
      pattern: "BODY_FIELD\\["
    - from: "backend/src/findling/config.py"
      to: "settings().languages"
      via: "geordnete Konstante, nie die Eingabereihenfolge"
      pattern: "for language in SUPPORTED_LANGUAGES"
---

<objective>
Sechs Sprachen werden wirklich waehlbar: `_languages()` filtert gegen die Menge, fuer die
dieser Build ein Koerperfeld hat, und `IndexBatchWriter.add()` befuellt genau die aktiven
Felder.

Purpose: Nach Plan 18-01 existieren sechs Felder, aber `_languages()` filtert noch gegen
`DEFAULT_LANGUAGES`, also fiele jede neue Sprache stumm heraus. Das ist die eine Zeile
zwischen "sechs Felder existieren" und "sechs Sprachen sind moeglich".

Output: Aktive Sprachmenge aus `SUPPORTED_LANGUAGES`, Befuellungsschleife, nachgezogene
Beschreibung der Umgebungsvariable, Doku-Satz zum Neustart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-schema-marken-und-umbauweg/18-RESEARCH.md
@.planning/phases/18-schema-marken-und-umbauweg/18-PATTERNS.md
@CLAUDE.md

Bindende Vorgaben: kein neues Paket, `tantivy`-Pin unberuehrt, Code englisch und ASCII,
alle fuenf Gates lokal gruen vor dem Commit.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _languages() filtert gegen die Menge mit Koerperfeld</name>
  <files>backend/src/findling/config.py, backend/tests/test_config.py</files>
  <read_first>
    - backend/src/findling/config.py Zeilen 100-150 (`DEFAULT_LANGUAGES`, `SUPPORTED_LANGUAGES` mit seinem Kommentarblock, `SNOWBALL_NAME`)
    - backend/src/findling/config.py Zeilen 1068-1085 (`_languages`)
    - backend/src/findling/config.py Zeilen 1093-1131 (`_ocr_languages`, Vorlage fuer einen Allowlist-Filter mit Warnung)
    - 18-RESEARCH.md Anti-Patterns, Absatz "DEFAULT_LANGUAGES auf sechs Eintraege erweitern"
  </read_first>
  <behavior>
    - `FINDLING_LANGUAGES=es,de` liefert `("de", "es")`: die Iteration laeuft ueber die
      geordnete Konstante, nicht ueber die Eingabe, damit die spaetere Marke fuer `es,de`
      und `de,es` dieselbe ist.
    - `FINDLING_LANGUAGES=xx` liefert `DEFAULT_LANGUAGES` und eine Warnung, die den
      Variablennamen nennt und nie den Wert.
    - `FINDLING_LANGUAGES` ungesetzt oder leer liefert `("de", "en")`.
    - `FINDLING_LANGUAGES=es` liefert `("es",)`: Deutsch und Englisch duerfen abgeschaltet
      werden (E-17-3 Option a).
  </behavior>
  <action>
    In `_languages()` den Filter von `DEFAULT_LANGUAGES` auf `SUPPORTED_LANGUAGES`
    umstellen. `DEFAULT_LANGUAGES` bleibt unveraendert `("de", "en")`, das ist die
    Werkseinstellung und keine Faehigkeitsliste.

    Den Kommentarblock an `SUPPORTED_LANGUAGES` fortschreiben: der Satz "No production
    path reads this constant in phase 17, and that is deliberate" wird ab diesem Plan
    falsch und muss durch die Aussage ersetzt werden, dass `_languages()` jetzt gegen
    diese Konstante filtert und dass die Ordnung die Normalisierung ist, die den
    Sprachmerker traegt.

    Den Docstring von `_languages()` nachziehen: er sagt heute "keeps both fields", was
    bei sechs Faellen nicht mehr stimmt.
  </action>
  <acceptance_criteria>
    - Neue Tests in `test_config.py` decken die vier Faelle des `<behavior>`-Blocks ab, jeder mit `monkeypatch.setenv` und geleertem `settings`-Cache.
    - `grep -n "DEFAULT_LANGUAGES = " backend/src/findling/config.py` zeigt weiterhin genau `("de", "en")`.
    - `grep -v '^ *#' backend/src/findling/config.py | grep -c "for language in DEFAULT_LANGUAGES"` meldet 0.
    - Der Kommentarblock an `SUPPORTED_LANGUAGES` enthaelt den Satz "No production path reads this constant" nicht mehr.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_config.py</automated>
  </verify>
  <done>Sechs Sprachcodes sind waehlbar, die Ordnung ist die Schemafeldreihenfolge, die Werkseinstellung steht still.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Befuellung nach aktiver Sprachmenge</name>
  <files>backend/src/findling/index/writer.py, backend/tests/test_index_writer.py</files>
  <read_first>
    - backend/src/findling/index/writer.py Zeilen 1-60 (Modulkopf), 150-180 (Konstruktor mit `_index_english`), 250-300 (`add`, `drop_document`)
    - backend/src/findling/index/schema.py nach Plan 18-01 (`BODY_FIELD`)
    - 18-PATTERNS.md Abschnitt "index/writer.py"
  </read_first>
  <behavior>
    - Mit `FINDLING_LANGUAGES=de,en,es` traegt ein geschriebenes Dokument Terme in
      `body_de`, `body_en` und `body_es` und keine in `body_it`, `body_nl`, `body_pt`.
    - Mit `FINDLING_LANGUAGES=es` ist `body_de` trotzdem gespeichert und traegt den Text,
      weil es die einzige gespeicherte Textkopie im ganzen System ist und der
      Snippetgenerator daraus schneidet.
    - Der Konstruktorparameter, der die Sprachmenge fuer die Suite ueberschreibbar macht,
      folgt der Form von `index_english` heute: aus `settings()` aufgeloest, per Parameter
      ueberschreibbar.
  </behavior>
  <action>
    Den Konstruktor von `IndexBatchWriter` von `index_english: bool | None` auf eine
    Sprachmenge umstellen (`languages: Sequence[str] | None`, aufgeloest zu
    `resolved.languages`). Den `if self._index_english`-Zweig in `add()` durch eine
    Schleife ueber die aktive Menge ersetzen, die den Feldnamen aus
    `findling.index.schema.BODY_FIELD` holt und `body_de` dabei nicht doppelt schreibt.

    Den Absatz an der `body_de`-Zeile ergaenzen, den 18-RESEARCH.md fordert: der
    Unterschied zwischen "Feld wird gespeichert" und "Feld wird durch die deutsche Kette
    analysiert" ist die Stelle, an der der naechste Leser stolpert. `body_de` ist beides
    zugleich, und die Speicherung haengt nie an der Sprachmenge.

    Alle Aufrufstellen des alten Parameters im Baum mitziehen (Produktivcode und Suite);
    `grep -rn "index_english" backend/` muss danach leer sein.
  </action>
  <acceptance_criteria>
    - `grep -rn "index_english" backend/ --include=*.py` liefert keine Treffer.
    - Ein Test schreibt unter `FINDLING_LANGUAGES=es` ein Dokument und liest ueber `Searcher.doc(...).to_dict()` zurueck, dass `body_de` den Text traegt.
    - Ein Test prueft ueber `searcher.terms_with_prefix(field, "", limit=1)`, dass `body_es` befuellt und `body_it` leer ist.
    - `uv run vulture` meldet den entfernten Parameter nicht als toten Rest.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_index_writer.py tests/test_index_open.py</automated>
  </verify>
  <done>Die Befuellung folgt der aktiven Sprachmenge, body_de bleibt die gespeicherte Kopie, kein Rest des alten Flags im Baum.</done>
</task>

<task type="auto">
  <name>Task 3: Beschreibung der Umgebungsvariable, Doku und Ratsche</name>
  <files>backend/appinfo/info.xml, docs/language-analyzers.md, backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/appinfo/info.xml Zeilen 338-356 (`FINDLING_LANGUAGES`, `FINDLING_COMPOUND_DICT` als Vorbild fuer den Ton)
    - docs/language-analyzers.md (Stand aus Plan 17-06)
    - 18-RESEARCH.md Pitfall 7
    - backend/tests/test_measurement_scripts.py Zeilen 596-700
  </read_first>
  <action>
    Die Beschreibung von `FINDLING_LANGUAGES` in `backend/appinfo/info.xml` nachziehen:
    sie sagt heute "currently de and en" und ist damit falsch. Der neue Text nennt die
    sechs Codes, behaelt die Werkseinstellung `de,en` und den Satz, dass ein leerer oder
    unbekannter Wert auf `de,en` zurueckfaellt. Der Rat "the index has to be built again
    afterwards" wird durch die wahre Aussage ersetzt: Findling baut den Index nach einer
    Aenderung selbst um, waehrenddessen antwortet die Suche weiter; ein
    `occ findling:index --restart` ist dafuer ausdruecklich nicht noetig.

    In `docs/language-analyzers.md` einen kurzen Abschnitt ergaenzen, der die Grenze aus
    18-RESEARCH.md Pitfall 7 benennt: `settings()` ist zwischengespeichert und die
    Driftpruefung laeuft im Startpfad, also wirkt ein Sprachwechsel erst nach dem Neustart
    des Containers. Der Satz ist eine Zusage, keine Entschuldigung, und er verhindert
    einen Supportfall.

    Danach die Ratsche bewegen: Rezept ueber `backend/src/findling` fahren,
    `PACKAGE_TREE_HASH_TODAY` setzen (Anzahl bleibt bei 55, `info.xml` und `docs/` liegen
    ausserhalb des Musters `**/*.py`), Kommentarkette um einen Absatz fortschreiben, der
    `config.py` und `index/writer.py` einzeln nennt.
  </action>
  <acceptance_criteria>
    - `grep -c "currently de and en" backend/appinfo/info.xml` meldet 0.
    - Die neue Beschreibung nennt alle sechs Codes und enthaelt keinen `occ findling:index --restart`-Rat.
    - `cd backend && uv run python -m pytest -q tests/test_measurement_scripts.py` gruen.
    - Volle Suite und alle vier Gates gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check &amp;&amp; uv run ruff format --check &amp;&amp; uv run pyright &amp;&amp; uv run vulture</automated>
  </verify>
  <done>Die Adminbeschreibung stimmt, die Neustartgrenze steht in der Doku, der Baumhash traegt beide Codeaenderungen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Umgebungsvariable -> aktive Sprachmenge | `FINDLING_LANGUAGES` ist Admin-Eingabe und steuert Feldnamen und Kettenwahl |
| Aktive Sprachmenge -> Schreibpfad | Ein Feldname, der nicht aus der Abbildung kommt, wuerde beim Schreiben spurlos verschluckt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Injection | `_languages()` -> Kettenname | mitigate | Gefiltert gegen `SUPPORTED_LANGUAGES`, das seinerseits gegen `LANGUAGE_ALLOWLIST` paritaetsgeprueft ist (Plan 17-02); ein Tippfehler erreicht `Filter.stopword` nicht |
| T-18-02-02 | Information Disclosure | Warnung bei unbekannter Sprache | mitigate | Die Warnung nennt den Variablennamen und nie den Wert, Hausregel von `config.py` |
| T-18-02-03 | Tampering | Feldname im Schreibpfad | mitigate | Feldname ausschliesslich aus `BODY_FIELD`; gemessen 2026-09-24, dass ein unbekannter Name ohne Fehler verschwindet |
| T-18-02-04 | Denial of Service | Leere Sprachmenge | accept | Eine unbrauchbare Liste faellt auf `DEFAULT_LANGUAGES` zurueck statt den Start zu verweigern; Hausregel "eine unlesbare Einstellung ist eine Warnung, nie eine Startverweigerung" |
| T-18-02-SC | Tampering | npm/pip/cargo installs | accept | Kein Paket, keine Bewegung an `pyproject.toml` oder `uv.lock` |
</threat_model>

<verification>
- Ein Index unter `FINDLING_LANGUAGES=de,en` ist zeichengleich befuellt wie vor der Phase.
- Ein Index unter `FINDLING_LANGUAGES=es` traegt Terme in `body_es`, gespeicherten Text in `body_de` und keine Terme in den uebrigen vier Koerperfeldern.
- Volle Suite und alle Gates gruen.
</verification>

<success_criteria>
- `_languages()` filtert gegen `SUPPORTED_LANGUAGES`, Werkseinstellung unveraendert.
- `IndexBatchWriter.add()` befuellt genau die aktiven Felder, `body_de` immer gespeichert.
- `index_english` existiert nirgends mehr im Baum.
- `info.xml` und `docs/language-analyzers.md` sagen die Wahrheit ueber Wertebereich und Neustartgrenze.
</success_criteria>

<output>
Create `.planning/phases/18-schema-marken-und-umbauweg/18-02-SUMMARY.md` when done
</output>
