---
phase: 18-schema-marken-und-umbauweg
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - backend/tests/test_language_cases_field_level.py
autonomous: true
requirements: [LEX-08]

must_haves:
  truths:
    - "Ein spanisches Dokument wird ueber die Stammform seines Suchworts auf Feldebene gefunden"
    - "Derselbe Beweis steht je Sprache fuer it, nl und pt"
    - "Der Index des Sprachfalls traegt keinen Fremdbestand"
  artifacts:
    - path: "backend/tests/test_language_cases_field_level.py"
      provides: "Vier Sprachfaelle gegen body_es, body_it, body_nl, body_pt"
      contains: "parse_query_lenient"
  key_links:
    - from: "backend/tests/test_language_cases_field_level.py"
      to: "backend/tests/fixtures/chain_cases_*.txt"
      via: "Wortpaare aus der Messung 17-06, nicht neu erfunden"
      pattern: "chain_cases_"
---

<objective>
Die Sprachfaelle je neuer Sprache laufen auf Feldebene, gegen einen Index ohne
Fremdbestand, und zeigen, dass die Kette am Feld wirklich greift.

Purpose: LEX-08 fordert diesen Beweis in Phase 18, weil die Frageseite erst in Phase 19
geoeffnet wird. Auf Feldebene heisst: `parse_query_lenient(..., default_field_names=
["body_es"])` direkt gegen das Feld, nicht ueber `build_query`, denn `DEFAULT_FIELDS`
ruehrt diese Phase nicht an.

Output: Eine Testdatei mit vier Sprachfaellen, deren Wortpaare aus der Messung vom
23.09.2026 stammen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-schema-marken-und-umbauweg/18-RESEARCH.md
@.planning/phases/18-schema-marken-und-umbauweg/18-PATTERNS.md
@CLAUDE.md

Dieser Plan aendert keine Datei unter `backend/src/findling`; die Ratsche bewegt sich
nicht, und er laeuft neben 18-02 und 18-03 in derselben Welle.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wortpaare auswaehlen, die nur die Zielkette zusammenfuehrt</name>
  <files>backend/tests/test_language_cases_field_level.py</files>
  <read_first>
    - backend/tests/fixtures/chain_cases_es.txt, chain_cases_it.txt, chain_cases_nl.txt, chain_cases_pt.txt
    - backend/tests/fixtures/chain_known_losses_es.txt und die drei Geschwister
    - backend/tests/test_language_analyzers.py, die Stelle, an der die Fixtures gelesen werden
    - docs/language-analyzers.md (Verdikt je Sprache aus Plan 17-06)
    - 18-RESEARCH.md, Abschnitt "Die Sprachfaelle auf Feldebene, ohne Fremdbestand", inklusive der Warnung zu `arrendamientos`
  </read_first>
  <action>
    Je Sprache genau ein Paar (Grundform, Beugungsform) aus den vorhandenen
    Fixture-Dateien waehlen und die Wahl im Kommentar begruenden. Das Auswahlkriterium ist
    hart und steht in der Recherche: der Stamm der beiden Formen darf NUR vom
    Snowball-Stemmer der Zielsprache zusammengefuehrt werden. Ein Paar, das die englische
    Kette ebenfalls zusammenfuehrt (etwa ein reines Plural-s), beweist nichts, und genau
    dieser Fehler ist der Sonde vom 2026-09-24 unterlaufen.

    Die Pruefung der Eignung gehoert in den Test selbst und nicht in einen Kommentar: fuer
    jedes gewaehlte Paar wird zusaetzlich gezeigt, dass die ENGLISCHE Kette die beiden
    Formen NICHT auf einen Term legt. Damit ist die Aussagekraft des Falls Teil der Suite
    und nicht Teil des Vertrauens in den Autor.

    Das Paar wird aus der Fixture gelesen, nicht als Literal in die Testdatei geschrieben,
    damit ein spaeterer Kettenumbau die Datei an einer Stelle bewegt.
  </action>
  <acceptance_criteria>
    - Vier Paare, je eines fuer es, it, nl, pt, jedes mit einer Begruendungszeile im Code.
    - Fuer jedes Paar ist per Assertion gezeigt, dass die englische Kette die beiden Formen auf zwei verschiedene Terme legt.
    - Kein Wortpaar steht als Literal in der Testdatei; alle kommen aus `backend/tests/fixtures/chain_cases_*.txt`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q tests/test_language_cases_field_level.py</automated>
  </verify>
  <done>Vier belastbare Paare stehen fest, ihre Aussagekraft ist in der Suite bewiesen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vier Sprachfaelle gegen das Feld, ohne Fremdbestand</name>
  <files>backend/tests/test_language_cases_field_level.py</files>
  <read_first>
    - backend/tests/conftest.py `write_index` (Dokumentaufbau Feld fuer Feld)
    - backend/src/findling/index/open.py `open_index`, `open_reader`
    - backend/src/findling/index/schema.py `BODY_FIELD` (nach 18-01)
    - .planning/milestones/v1.1-phases/10-*/deferred-items.md, Abschnitt zur Lehre A4/2026-09 (Rangbeweise bei Fremdbestand)
  </read_first>
  <behavior>
    - Fuer jede der vier Sprachen: ein Index mit AUSSCHLIESSLICH den Dokumenten dieser
      Sprache, geschrieben in `BODY_FIELD[code]`, hoechstens eine Handvoll Dokumente.
    - Eine Anfrage mit der Grundform ueber
      `parse_query_lenient(..., default_field_names=[BODY_FIELD[code]])` findet das
      Dokument, das nur die Beugungsform traegt, und umgekehrt.
    - Dieselbe Anfrage gegen `body_en` desselben Index findet nichts, weil dieses Feld
      leer ist. Das ist die Gegenprobe, die zeigt, dass der Treffer aus der Zielkette kam.
    - Kein Fremdbestand: der Index enthaelt keine Dokumente einer anderen Sprache. Die
      Lehre A4 aus v1.1 lautet, dass bei 52.111 fremden Dokumenten mit denselben Tokens
      ein Beweis nichts mehr traegt.
  </behavior>
  <action>
    Die vier Faelle als parametrisierten Test bauen (ein `pytest.mark.parametrize` ueber
    die vier Codes), damit die Strecke nicht viermal kopiert ist. Der Index wird je Fall
    frisch in `tmp_path` angelegt und ueber `open_index` geoeffnet, niemals ueber
    `Index(...)` direkt, weil der Waechter in `test_index_open.py` ueber das ganze Paket
    laeuft und ein Testmodul sich an dieselbe Disziplin haelt.

    Auf einen Rangbeweis wird ausdruecklich verzichtet: gefordert ist "gefunden", nicht
    "besser gerankt". Die Rangfrage liegt in Phase 19 und braucht die Frageseite.
  </action>
  <acceptance_criteria>
    - Vier Faelle laufen gruen, je Sprache einer.
    - Die Gegenprobe gegen `body_en` liefert null Treffer und ist eine eigene Assertion mit eigener Meldung.
    - `grep -c "Index(" backend/tests/test_language_cases_field_level.py` meldet 0 (der Index kommt ausschliesslich ueber `open_index`).
    - Volle Suite und alle vier Gates gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check &amp;&amp; uv run ruff format --check &amp;&amp; uv run pyright &amp;&amp; uv run vulture</automated>
  </verify>
  <done>Je Sprache ein gruener Sprachfall auf Feldebene, mit Gegenprobe und ohne Fremdbestand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testaussage -> Zusicherung im Release | Ein Sprachfall, der aus dem falschen Grund gruen ist, ist eine Zusicherung ohne Deckung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Repudiation | Sprachfall gruen aus dem falschen Grund | mitigate | Je Paar eine Assertion, dass die englische Kette es NICHT zusammenfuehrt, plus Gegenprobe gegen das leere `body_en` |
| T-18-04-02 | Tampering | Wortpaar als Literal | mitigate | Paare kommen aus den Messfixtures von Plan 17-06, nicht aus dem Gedaechtnis des Autors |
| T-18-04-SC | Tampering | npm/pip/cargo installs | accept | Kein Paket; nur Testdateien |
</threat_model>

<verification>
- Vier parametrisierte Sprachfaelle gruen.
- Ein absichtlich auf `body_en` umgestelltes Feld laesst den Fall rot werden.
- Keine Datei unter `backend/src/findling` geaendert.
</verification>

<success_criteria>
- `backend/tests/test_language_cases_field_level.py` deckt es, it, nl und pt ab.
- Jeder Fall laeuft auf einem Index ohne Fremdbestand.
- Die Eignung des Wortpaars ist Teil der Suite, nicht des Kommentars.
</success_criteria>

<output>
Create `.planning/phases/18-schema-marken-und-umbauweg/18-04-SUMMARY.md` when done
</output>
