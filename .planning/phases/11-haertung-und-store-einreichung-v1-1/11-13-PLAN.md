---
phase: 11-haertung-und-store-einreichung-v1-1
plan: 13
type: execute
wave: 2
depends_on: ["11-01"]
conditional: "nur bei Entscheid V-1a aus 11-VORENTSCHEIDE.md; bei V-1b dokumentiert uebersprungen"
files_modified:
  - php/lib/Service/SearchOutcome.php
  - php/lib/Service/SearchService.php
  - php/lib/Controller/PageController.php
  - php/lib/Search/Provider.php
  - php/templates/search.php
  - php/l10n/de.json
  - php/l10n/de.js
  - php/l10n/de_DE.json
  - php/l10n/de_DE.js
  - php/tests/Unit/SearchServiceTest.php
  - php/tests/Unit/PageControllerTest.php
  - php/tests/Unit/ProviderTest.php
  - backend/tests/test_admin_ui_contract.py
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "Bei V-1a: ein Lauf, der Kandidaten bekam und nach dem Recheck keinen einzigen behalten hat, ist ein eigener Zustand und nicht mehr dasselbe wie ein Lauf ohne Kandidaten"
    - "Bei V-1a: die Ergebnisseite sagt in diesem Zustand einen Satz, statt eine leere Liste ohne Erklaerung zu zeigen"
    - "Bei V-1a: der neue Zustand nimmt dem Nutzer die naechste Seite nicht weg, denn er ist eine Aussage ueber das Ergebnis und kein Abbruch des Laufs"
    - "Bei V-1a: die vier deutschen Katalogdateien tragen genau einen Schluessel mehr, und das Gate haelt die neue Zahl 174 statt der alten 173"
    - "Bei V-1a: die Berechtigungskette ist unveraendert, MAX_ROUNDS ist unveraendert, und es gibt keine zweite Grenze"
    - "Bei V-1b: dieser Plan ist ohne eine einzige Codeaenderung als SKIPPED dokumentiert, und die SUMMARY nennt den Entscheid mit Datum"
  artifacts:
    - path: "php/lib/Service/SearchOutcome.php"
      provides: "die fuenfte Konstante FAILURE_ALL_CANDIDATES_REJECTED mit ihrem Docblock"
      contains: "FAILURE_ALL_CANDIDATES_REJECTED"
    - path: "php/templates/search.php"
      provides: "der Satz des neuen Zustands im Block 4, ohne Aenderung an der showEmpty-Entscheidung"
      contains: "FAILURE_ALL_CANDIDATES_REJECTED"
    - path: "php/l10n/de.json"
      provides: "der 174. Schluessel mit deutschem Wortlaut"
      contains: "Other files contain this word"
  key_links:
    - from: "php/lib/Service/SearchService.php"
      to: "php/templates/search.php"
      via: "SearchOutcome::failure, durch PageController unveraendert durchgereicht"
      pattern: "FAILURE_ALL_CANDIDATES_REJECTED"
    - from: "php/l10n/de.json"
      to: "docs/l10n-french.md (Plan 11-05)"
      via: "der 174. Schluessel, den die FR-Tabelle aus de.json zaehlt"
      pattern: "Other files contain this word"
    - from: "backend/tests/test_admin_ui_contract.py"
      to: "php/l10n/de.json"
      via: "die harte Katalogzahl, von 173 auf 174 gehoben"
      pattern: "174"
---

<objective>
Der umsetzende Plan zu Option V-1a: der Nutzer, dessen Begriffe im Fremdbestand
haeufig sind, bekommt statt einer leeren Liste einen Satz.

Purpose: 11-01 legt dem Owner V-1a als echte Wahl vor. Eine Wahl ohne einen
Plan, der sie baut, ist keine Wahl. Dieser Plan ist dieser Plan. Er baut genau
das, was das Dossier unter V-1a beschreibt: EINEN zusaetzlichen Zustand im
Ergebnisobjekt und EINEN neuen Katalogschluessel. Er fasst die Berechtigungskette
nicht an, er aendert MAX_ROUNDS nicht, und er macht keine zweite Grenze auf.

Output: Bei V-1a ein gebauter, getesteter und begatteter Zustand, den 11-05
uebersetzen, 11-08 giessen und 11-10 als `fixed` verbuchen kann. Bei V-1b eine
SUMMARY, die den Entscheid nennt und sonst nichts.
</objective>

<precondition>
**Dieser Plan laeuft nur bei Entscheid V-1a.**

Erster Handgriff des Executors, vor jedem Lesen von Code: lies
`.planning/phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md`
und suche unter `## V-1` die Zeile, die mit `**Entscheid` beginnt.

- Steht dort `v1-a`: fahre den Plan wie beschrieben.
- Steht dort `v1-b`: aendere KEINE Datei. Schreibe ausschliesslich
  `11-13-SUMMARY.md` mit der Ueberschrift `SKIPPED per Owner-Entscheid V-1b`,
  darin der woertliche Entscheid mit Datum, die Feststellung, dass kein Task
  gelaufen ist, und der Verweis, dass 11-10 den Befund DI-07-03 als LOW
  dokumentiert entscheidet. Danach ist der Plan fertig.
- Steht dort gar nichts oder etwas Drittes: brich ab und melde es. Ein Plan,
  der PHP-Code kurz vor der Abgabe anfasst, laeuft nicht auf Verdacht.

Jedes Kriterium jedes Tasks unten gilt unter dieser Vorbedingung: bei V-1b ist
es durch den SKIPPED-Vermerk in der SUMMARY erfuellt.
</precondition>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-CONTEXT.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md
</context>

<interfaces>
Die Vertraege, gegen die dieser Plan arbeitet, stehen hier, damit niemand sie
im Baum suchen muss.

Aus `php/lib/Service/SearchOutcome.php` (heute vier Konstanten):

    public const FAILURE_BACKEND_SILENT = 'backend_silent';
    public const FAILURE_VERSION_DRIFT = 'version_drift';
    public const FAILURE_NO_HOME_FOLDER = 'no_home_folder';
    public const FAILURE_OFFSET_CEILING = 'offset_ceiling';

    public function __construct(
        public readonly array $hits,
        public readonly array $excerpts,
        public readonly int $nextCursor,
        public readonly bool $hasMore,
        public readonly bool $degraded,
        public readonly ?string $failure,
    )

Aus `php/lib/Service/SearchService.php`, die Stelle, an der der Zustand
entsteht (heute rund Zeile 359 bis 369): der Zweig `if ($approved === [])`
setzt `$failure` auf `FAILURE_OFFSET_CEILING`, sonst auf
`FAILURE_BACKEND_SILENT`, sonst auf `null`. Die Kandidaten einer Seite stehen
davor in `$candidates = $page['candidates']`, die Zahl der in dieser Seite
entschiedenen Kandidaten in `$consumed`.

Aus `php/lib/Controller/PageController.php`: Zeile 213 reicht
`'failure' => $outcome->failure` unveraendert an das Template. Zeile 427
entscheidet, ob es eine naechste Seite gibt, und liest dafuer heute
`$outcome->failure !== null`.

Aus `php/templates/search.php`: die vier `$silent/$drift/$noHome/$ceiling`
Zeilen ab Zeile 73, daraus `$hasError` und `$hasHint`, daraus
`$showEmpty = $hits === [] && (!$hasQuery || (!$hasError && !$hasHint));`.
Block 4 ist der Leerzustand mit `class="findling-empty"`, Ueberschrift
`No file contains "%s"` und Text `Try another word, a part of a compound word,
or check the spelling.`

Aus `backend/tests/test_admin_ui_contract.py`: das Gate
`scan_page_template_for_an_unguarded_empty_state` prueft, dass der Leerzustand
hinter `elseif ($showEmpty)` steht und dass die `$showEmpty`-Entscheidung
`$hasError`, `$hasHint` und `$hasQuery` liest. Zeile 955 traegt
`assert len(keys_of["de.json"]) == 173`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Der fuenfte Zustand entsteht im Dienst, dort wo er entsteht</name>
  <files>php/lib/Service/SearchOutcome.php, php/lib/Service/SearchService.php, php/tests/Unit/SearchServiceTest.php</files>
  <read_first>
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md, Abschnitt V-1 mit dem Entscheid und dem englischen Quellstring (die Vorbedingung dieses Plans)
    - php/lib/Service/SearchOutcome.php vollstaendig (85 Zeilen, die vier Konstanten mit ihren Docblocks sind das Muster fuer die fuenfte)
    - php/lib/Service/SearchService.php, Zeilen 230 bis 405: die Kandidatenschleife, $consumed, $stopped, der Zweig `if ($approved === [])` und die Methode nothing()
    - php/tests/Unit/SearchServiceTest.php, die Testfaelle um den leeren Ausgang (Muster fuer den Aufbau des Containermocks)
    - .planning/phases/07-gemeinsame-embedding-engine/deferred-items.md, DI-07-03, Abschnitt "Was offen bleibt und Phase 11 gehoert"
  </read_first>
  <behavior>
    - Ein Lauf, dessen Container gar keine Kandidaten lieferte und der ohne Treffer endet, traegt weiterhin `failure === null` (oder `backend_silent`, wenn der Container schwieg). Nichts an diesem Fall aendert sich.
    - Ein Lauf, dessen Container mindestens einen Kandidaten lieferte, der ueber diesen Kandidaten entschieden hat und der ohne einen einzigen Treffer endet, traegt `failure === SearchOutcome::FAILURE_ALL_CANDIDATES_REJECTED`.
    - Traf der Lauf die Offset-Decke, gewinnt `FAILURE_OFFSET_CEILING`: die Decke ist die aeltere und die genauere Aussage.
    - Schwieg der Container, gewinnt `FAILURE_BACKEND_SILENT`. Ein Schweigen ist kein Verwerfen.
    - Ein Lauf mit mindestens einem Treffer traegt den neuen Zustand nie, auch dann nicht, wenn andere Kandidaten derselben Seite verworfen wurden.
    - `hasMore` und `nextCursor` sind im neuen Zustand genau dieselben Werte wie heute im selben Fall mit `failure === null`.
  </behavior>
  <action>
Zuerst die Vorbedingung aus dem Abschnitt `<precondition>` pruefen. Bei V-1b
endet der Plan hier.

Fuege in `SearchOutcome.php` die fuenfte Konstante hinzu:
`FAILURE_ALL_CANDIDATES_REJECTED = 'all_candidates_rejected'`. Der Docblock
folgt dem Ton der vier vorhandenen und sagt, was diese Konstante von den
anderen unterscheidet: die anderen vier sagen, dass der Lauf nicht oder nicht
zu Ende stattgefunden hat, diese eine sagt, dass er stattgefunden hat und dass
seine Antwort fuer diesen Nutzer leer ist. Nenne DI-07-03 und das Datum des
Entscheids V-1a als Herkunft.

In `SearchService.php` zaehlst du mit, ob die Schleife ueberhaupt Kandidaten zu
entscheiden hatte. Eine Zaehlvariable neben `$approved`, vor der Rundenschleife
angelegt, die je Seite um die Zahl der tatsaechlich entschiedenen Kandidaten
waechst; `$consumed` ist die Zahl, die es dafuer schon gibt, und sie zaehlt
genau die entschiedenen und keine uebersprungenen. Ein Name in der Sprache der
Datei, nicht `$count`.

Im Zweig `if ($approved === [])` bekommt die bestehende Kette einen dritten
Zweig, in dieser Reihenfolge: Decke, dann Schweigen, dann der neue Zustand,
wenn die Zaehlvariable groesser als null ist, sonst weiterhin `null`. Die
Reihenfolge ist die Aussage: eine Decke und ein Schweigen sind Feststellungen
ueber den Lauf, der neue Zustand ist eine Feststellung ueber sein Ergebnis, und
eine Feststellung ueber den Lauf ist die dringendere. Schreibe diesen Satz als
Kommentar dazu, so wie es die Datei mit ihren anderen Entscheidungen haelt.

Was ausdruecklich NICHT passiert, und was als Kommentar an der Schleife stehen
bleibt: `MAX_ROUNDS` bleibt bei 3, das Recheck-Budget bleibt, wo es ist, die
Frage `isReadable()` bleibt die einzige Berechtigungsentscheidung dieses
Produkts, und es kommt kein zweiter Filter dazu. Dieser Plan macht den
bestehenden Zustand sichtbar; er aendert ihn nicht.

Die Tests kommen vor der Aenderung und stehen in
`php/tests/Unit/SearchServiceTest.php`: je ein Fall fuer die sechs Punkte der
Verhaltensliste, mit dem Containermock der Datei aufgebaut, nicht mit einem
zweiten Muster.
  </action>
  <verify>
    <automated>cd php &amp;&amp; find lib -name '*.php' -print0 | xargs -0 -n1 php -l &amp;&amp; grep -c "FAILURE_ALL_CANDIDATES_REJECTED" lib/Service/SearchOutcome.php lib/Service/SearchService.php tests/Unit/SearchServiceTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Bei V-1b: keine der drei Dateien ist angefasst, und die SUMMARY traegt den SKIPPED-Vermerk. Alle folgenden Kriterien entfallen damit.
    - `php -l` laeuft ueber alle Dateien unter `php/lib` fehlerfrei.
    - `grep -c "FAILURE_ALL_CANDIDATES_REJECTED"` findet die Konstante in allen drei Dateien mindestens einmal.
    - `grep -c "MAX_ROUNDS = 3" php/lib/Search/Provider.php` ist unveraendert 1, und `git diff php/lib/Service/SearchService.php` enthaelt keine Zeile, die `isReadable`, `reduceIds` oder das Recheck-Budget aendert.
    - Sechs neue Testfaelle in SearchServiceTest.php, je einer je Punkt der Verhaltensliste, jeder mit einem Docstring-Kommentar, der die Frage nennt.
    - Die PHP-Unit-Suite laeuft nur in CI (php.yml, Job mit `vendor/bin/phpunit`, weil sie einen nextcloud/server-Checkout braucht). Die Laufnummer des gruenen Laufs steht in der SUMMARY.
  </acceptance_criteria>
  <done>Der Dienst unterscheidet "es gab keine Kandidaten" von "es gab Kandidaten, und keiner blieb uebrig", und sechs Tests halten die Unterscheidung fest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Die Ergebnisseite sagt den Satz, der Dialog bleibt stumm, und die naechste Seite bleibt erreichbar</name>
  <files>php/templates/search.php, php/lib/Controller/PageController.php, php/lib/Search/Provider.php, php/tests/Unit/PageControllerTest.php, php/tests/Unit/ProviderTest.php</files>
  <read_first>
    - php/templates/search.php, Zeilen 56 bis 105 (die Zustandsvariablen und die showEmpty-Entscheidung) und der Block 4 ab Zeile 239 (der Leerzustand mit Ueberschrift und Text)
    - php/lib/Controller/PageController.php, Zeilen 185 bis 215 (Bannerzustand und die Parameterliste) und Zeile 415 bis 430 (die Entscheidung ueber die naechste Seite, die heute jeden failure als Abbruch liest)
    - php/lib/Search/Provider.php, Zeilen 170 bis 190 (der Debug-Eintrag mit dem Satz "The four reasons are a closed set of constants")
    - php/tests/Unit/PageControllerTest.php, testEveryFailureReasonReachesTheTemplateUnchanged (Zeile 350) und die Faelle um nextUrl
    - php/tests/Unit/ProviderTest.php, testEveryFailureOfTheServiceBecomesTheSameEmptyGroup (Zeile 223) mit seiner Begruendung, warum der Dialog keinen eigenen Satz hat
    - backend/tests/test_admin_ui_contract.py, scan_page_template_for_an_unguarded_empty_state (ab Zeile 373) und seine drei Marker $hasError, $hasHint, $hasQuery
  </read_first>
  <behavior>
    - Der neue Zustand erzeugt KEIN Banner: weder das Fehlerbanner noch den Hinweis. `$hasError` und `$hasHint` sind in diesem Zustand unveraendert falsch, und `$showEmpty` bleibt Zeichen fuer Zeichen dieselbe Entscheidung wie heute.
    - Im Leerzustand mit Suchbegriff steht im neuen Zustand der neue Satz anstelle von `Try another word, a part of a compound word, or check the spelling.`; die Ueberschrift bleibt `No file contains "%s"`.
    - Ohne Suchbegriff ist der Leerzustand unveraendert die Einladung.
    - Der neue Zustand nimmt die naechste Seite nicht weg: bei `hasMore === true` traegt die Seite weiterhin einen `nextUrl`. Die vier alten Zustaende verlieren sie weiterhin.
    - Der Suchdialog liefert auch im neuen Zustand eine leere Gruppe ohne Eintrag und schreibt den neuen Grund in seine Ablaufspur. Ein Eintrag im Dialog waere ein Treffer, der keiner ist.
  </behavior>
  <action>
Zuerst die Vorbedingung aus dem Abschnitt `<precondition>` pruefen. Bei V-1b
endet der Plan hier.

**Template.** Eine fuenfte Zustandszeile neben die vier vorhandenen, nach
demselben Muster, und sie geht ausdruecklich weder in `$hasError` noch in
`$hasHint` ein. Schreibe den Grund als Kommentar dazu: ein Banner ueber diesem
Zustand wuerde dem Nutzer sagen, er solle warten, und der Satz darunter sagt
ihm, dass die Suche fertig ist. Das ist genau der Widerspruch, den das Gate von
Phase 9 verbietet. In Block 4 waehlt eine Bedingung im Textabsatz zwischen dem
bestehenden Satz und dem neuen; die Ueberschrift bleibt unveraendert, denn sie
ist auch im neuen Zustand wahr. Jede Ausgabe laeuft durch `p($l->t(...))`, wie
jede andere Zeile dieser Datei.

**Der Wortlaut.** Der englische Quellstring ist der, den der Owner unter V-1 in
`11-VORENTSCHEIDE.md` eingetragen hat, byteweise uebernommen. Hat er dort
keinen genannt, gilt der Vorschlag des Dossiers:
`Other files contain this word, but none that you may open.`
Er traegt keine Prozent-Direktive, damit das Platzhalter-Gate G3 von 11-08
nichts Neues zu pruefen bekommt.

**PageController.** Die Entscheidung ueber die naechste Seite liest heute
`$outcome->failure !== null` und wuerde dem Nutzer damit ausgerechnet in dem
Zustand die naechste Seite nehmen, in dem seine eigenen Dateien womoeglich
dahinter liegen. Nimm den neuen Grund von dieser Regel aus, mit genau diesem
Satz als Kommentar. Sonst aendert der Controller nichts: er reicht den Grund
weiter wie die anderen vier.

**Provider.** Nur der Kommentar am Debug-Eintrag: aus vier Gruenden werden
fuenf, und der Satz, warum der Dialog trotzdem keinen eigenen Satz bekommt,
bleibt stehen. Er steht schon im Docstring von
`testEveryFailureOfTheServiceBecomesTheSameEmptyGroup` und ist damit die
bestehende Linie dieses Produkts und keine neue Entscheidung dieses Plans.

**Tests.** Die beiden Aufzaehlungen der vier Konstanten in PageControllerTest
und ProviderTest bekommen die fuenfte, und ihre Docstrings sagen fuenf statt
vier. Dazu zwei neue Faelle: die naechste Seite bleibt im neuen Zustand
erhalten, und der Dialog liefert auch fuer ihn eine leere Gruppe ohne Eintrag.
Die Tests kommen vor der Aenderung.
  </action>
  <verify>
    <automated>cd php &amp;&amp; find lib templates -name '*.php' -print0 | xargs -0 -n1 php -l &amp;&amp; cd ../backend &amp;&amp; uv run python -m pytest tests/test_admin_ui_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Bei V-1b: keine der fuenf Dateien ist angefasst, und die SUMMARY traegt den SKIPPED-Vermerk. Alle folgenden Kriterien entfallen damit.
    - `php -l` laeuft ueber `php/lib` und `php/templates` fehlerfrei.
    - `git diff php/templates/search.php` enthaelt keine geaenderte Zeile, in der `$showEmpty`, `$hasError` oder `$hasHint` definiert wird; die drei Zeilen sind byteweise unveraendert.
    - `grep -c "print_unescaped" php/templates/search.php` ist 0, und jede neue Ausgabezeile nutzt `p($l->t(`.
    - Der englische Quellstring im Template ist byteweise derselbe wie in 11-VORENTSCHEIDE.md unter V-1, beziehungsweise der Vorschlag oben, wenn dort keiner steht. Mit einem Python-Einzeiler geprueft, nicht per Augenmass.
    - Die fuenf Konstanten stehen in beiden Testaufzaehlungen: `grep -c "FAILURE_ALL_CANDIDATES_REJECTED" php/tests/Unit/PageControllerTest.php php/tests/Unit/ProviderTest.php` findet sie in beiden Dateien.
    - `cd backend && uv run python -m pytest tests/test_admin_ui_contract.py -q` ist gruen, das Gate gegen den ungeschuetzten Leerzustand also weiterhin zufrieden.
    - Die PHP-Unit-Suite ist in CI gruen, Laufnummer in der SUMMARY.
  </acceptance_criteria>
  <done>Der Nutzer liest einen Satz statt einer leeren Seite, er kann weiterblaettern, und der Dialog erfindet keinen Treffer.</done>
</task>

<task type="auto">
  <name>Task 3: Der 174. Schluessel in den vier deutschen Katalogen und die harte Zahl im Gate</name>
  <files>php/l10n/de.json, php/l10n/de.js, php/l10n/de_DE.json, php/l10n/de_DE.js, backend/tests/test_admin_ui_contract.py</files>
  <read_first>
    - php/l10n/de.json, die Eintraege der Ergebnisseite (`No file contains "%s"`, `Try another word, a part of a compound word, or check the spelling.`) als Vorbild fuer Ton, Anrede und Anfuehrungszeichen
    - php/l10n/de.js, Kopf und Fuss der Datei (OC.L10N.register und die pluralForm-Zeile), damit der Zwilling byteweise dieselbe Textmenge traegt
    - backend/tests/test_admin_ui_contract.py, Zeile 940 bis 956: der Vergleich der vier Katalogdateien und `assert len(keys_of["de.json"]) == 173`
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-VORENTSCHEIDE.md, V-1 mit dem englischen Quellstring
  </read_first>
  <action>
Zuerst die Vorbedingung aus dem Abschnitt `<precondition>` pruefen. Bei V-1b
endet der Plan hier.

Trage den neuen Schluessel in alle vier deutschen Katalogdateien ein, an
derselben Stelle in der Reihenfolge, an der er im Template steht, also direkt
neben `Try another word, a part of a compound word, or check the spelling.`
Die vier Dateien sind zwei Formate mal zwei Sprachcodes; der bestehende Test
haelt sie textgleich, also ist ein Eintrag in dreien ein roter Baum.

Der deutsche Wortlaut folgt der Sie-Anrede des Bestands und traegt echte
Umlaute, so wie jeder andere Wert dieser Kataloge:
`Andere Dateien enthalten dieses Wort, aber keine, die Sie öffnen dürfen.`
Kein Gedankenstrich, kein Emoji, keine Prozent-Direktive.

Hebe in `backend/tests/test_admin_ui_contract.py` die harte Zahl von 173 auf
174 und schreibe in den Docstring des Tests, warum sie steigt: Entscheid V-1a
vom Datum aus 11-VORENTSCHEIDE.md, ein Schluessel fuer den Zustand aus
DI-07-03. Ohne diesen Satz liest der naechste Leser die Zahl als Schlamperei.
Die Zahl steigt hier und nicht erst in 11-08, weil der Baum zwischen diesem
Plan und 11-08 sonst rot stuende; 11-08 prueft sie danach nur noch.

Der Kommentar in Zeile 101 derselben Datei nennt "24 of 173 strings". Zieh ihn
mit, sonst widersprechen sich Kommentar und Zusicherung in einer Datei.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest tests/test_admin_ui_contract.py -q &amp;&amp; cd .. &amp;&amp; python -c "import json,sys; d=json.load(open('php/l10n/de.json',encoding='utf-8'))['translations']; k='Other files contain this word, but none that you may open.'; print('Schluessel:',len(d)); sys.exit(0 if len(d)==174 and k in d else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Bei V-1b: keine der fuenf Dateien ist angefasst, und die SUMMARY traegt den SKIPPED-Vermerk. Alle folgenden Kriterien entfallen damit.
    - `php/l10n/de.json` traegt 174 Schluessel, und der Test `test_the_german_catalogue_covers_both_german_language_codes` ist gruen, prueft also gleichzeitig, dass alle vier Dateien dieselbe Schluesselmenge tragen.
    - Der neue Schluessel ist derselbe englische Quellstring, den Task 2 ins Template geschrieben hat; ein Python-Einzeiler vergleicht Template und Katalog und meldet eine Abweichung.
    - Der deutsche Wortlaut traegt echte Umlaute und keinen Gedankenstrich; das Dash-Gate der Datei bleibt gruen.
    - Der Docstring des Tests nennt den Entscheid V-1a mit Datum, und der Kommentar in Zeile 101 spricht nicht mehr von 173.
    - `cd backend && uv run python -m pytest -q && uv run ruff check . && uv run ruff format --check .` laeuft gruen.
  </acceptance_criteria>
  <done>Die deutsche Seite ist vollstaendig, die franzoesische Tabelle von 11-05 zaehlt 174 aus der Datei, und kein Gate steht rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container zu PHP | Die Kandidatenliste kommt aus dem Container; die Zaehlung dieses Plans liest sie, entscheidet aber nichts ueber Sichtbarkeit |
| PHP zu Browser | Ein neuer Satz geht ins Template und damit an jeden Nutzer |
| Katalog zu Template | Ein neuer Katalogwert wird uebersetzt ausgegeben und laeuft durch dieselbe Escaping-Kette wie die 173 anderen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-50 | Information Disclosure | Der neue Satz auf der Ergebnisseite | mitigate | Der Satz nennt keine Zahl, keinen Dateinamen und keinen Pfad. Er sagt nur, dass es andere Dateien gibt, und das weiss jeder Nutzer einer geteilten Instanz ohnehin. Ein Satz mit der Zahl der verworfenen Kandidaten waere ein Zaehlorakel ueber den Fremdbestand und ist deshalb ausdruecklich nicht Teil dieses Plans |
| T-11-51 | Elevation of Privilege | SearchService, Kandidatenschleife | mitigate | Die Zaehlvariable liest nur, sie entscheidet nichts. `isReadable()` bleibt die einzige Berechtigungsfrage, `MAX_ROUNDS` bleibt 3, `reduceIds` bleibt unveraendert; das Kriterium in Task 1 belegt es am Diff, und 11-10 prueft es unter ASVS V4 nach |
| T-11-52 | Tampering | php/templates/search.php | mitigate | Jede neue Ausgabe laeuft durch `p($l->t(...))`; das Escaping-Gate und das Gate gegen den ungeschuetzten Leerzustand laufen in Task 2 als Verifikation mit |
| T-11-53 | Repudiation | Der bedingte Charakter dieses Plans | mitigate | Bei V-1b entsteht eine SUMMARY mit dem woertlichen Entscheid und dem SKIPPED-Vermerk; ein uebersprungener Plan ohne Spur waere von einem vergessenen nicht zu unterscheiden |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Keiner. Dieser Plan installiert kein Paket; RESEARCH.md haelt fest, dass die Phase kein externes Paket einfuehrt |
</threat_model>

<verification>
- Bei V-1b: `git diff --stat` nennt keine Datei, und `11-13-SUMMARY.md` traegt `SKIPPED per Owner-Entscheid V-1b`.
- Bei V-1a: `php -l` ueber `php/lib` und `php/templates` fehlerfrei.
- Bei V-1a: `cd backend && uv run python -m pytest -q` gruen, die harte Katalogzahl also 174 und die vier deutschen Dateien deckungsgleich.
- Bei V-1a: die PHP-Unit-Suite in php.yml gruen, Laufnummer in der SUMMARY.
- Bei V-1a: `git diff` enthaelt keine Aenderung an `MAX_ROUNDS`, an `isReadable`, an `reduceIds` oder am Recheck-Budget.
</verification>

<success_criteria>
Die Option, die 11-01 dem Owner anbietet, ist gebaut, sobald er sie waehlt: der
Zustand steht im Ergebnisobjekt, der Satz auf der Ergebnisseite, der Schluessel
in den vier deutschen Katalogen, und die Berechtigungskette ist beweisbar
unveraendert. Waehlt er die andere Option, steht dieser Plan als dokumentiert
uebersprungen in der Phasenhistorie.
</success_criteria>

<output>
Create `.planning/phases/11-haertung-und-store-einreichung-v1-1/11-13-SUMMARY.md` when done
</output>
