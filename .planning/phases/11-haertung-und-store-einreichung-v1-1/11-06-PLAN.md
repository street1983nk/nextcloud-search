---
phase: 11-haertung-und-store-einreichung-v1-1
plan: 06
type: execute
wave: 2
depends_on: ["11-02", "11-03"]
files_modified:
  - docs/measurements/2026-09-werkzeugfixe/rohdaten/01-lastwerkzeug-stufe16.json
  - docs/measurements/2026-09-werkzeugfixe/rohdaten/02-lastwerkzeug-stufe08.json
  - docs/measurements/2026-09-werkzeugfixe/rohdaten/03-nc-protokoll-abbrueche.txt
  - docs/measurements/2026-09-werkzeugfixe/rohdaten/04-bestand-vor-der-messung.txt
  - docs/measurements/2026-09-werkzeugfixe/rohdaten/05-sprachfaelle.txt
  - docs/measurements/2026-09-werkzeugfixe/rohdaten/06-kosten.txt
  - docs/measurements/2026-09-werkzeugfixe/README.md
autonomous: false
requirements: [REL-01]

must_haves:
  truths:
    - "Der Owner hat die Anfahrt mit Kostendeckel vor dem Start freigegeben"
    - "Der neue failures-Zaehler des Lastwerkzeugs stimmt mit einer unabhaengigen Zaehlung der Abbrueche im Nextcloud-Protokoll ueberein"
    - "Das Sprachfall-Skript meldet die nicht messbaren Faelle als NICHT MESSBAR und nicht als ROT"
    - "Die Box ist nach der Messung angehalten, und die Kosten der Anfahrt stehen dem Deckel gegenuebergestellt"
  artifacts:
    - path: "docs/measurements/2026-09-werkzeugfixe/rohdaten/01-lastwerkzeug-stufe16.json"
      provides: "Stufe 16 mit dem neuen failures-Zaehler, min_hits und hits_per_request"
      contains: "hits_per_request"
    - path: "docs/measurements/2026-09-werkzeugfixe/rohdaten/03-nc-protokoll-abbrueche.txt"
      provides: "die unabhaengige Zaehlung der cURL-error-28-Zeilen im Zeitfenster des Laufs"
    - path: "docs/measurements/2026-09-werkzeugfixe/rohdaten/05-sprachfaelle.txt"
      provides: "Vorpruefung Fremdbestand, dreiwertige Urteile, Bilanzzeile mit zwei Zahlen, ci-beleg mit Laufnummer"
      contains: "NICHT MESSBAR"
    - path: "docs/measurements/2026-09-werkzeugfixe/README.md"
      provides: "der kurze Bericht der Anfahrt mit Kostenbilanz gegen den Deckel"
  key_links:
    - from: "01-lastwerkzeug-stufe16.json failures"
      to: "03-nc-protokoll-abbrueche.txt"
      via: "zwei unabhaengige Zaehlungen derselben Sache"
      pattern: "cURL error 28"
    - from: "05-sprachfaelle.txt"
      to: "integration.yml Laufnummer"
      via: "ci-beleg als Pflichteingabe"
      pattern: "ci-beleg"
---

<objective>
Die EINE Box-Anfahrt der Phase (D-01). Sie beweist zwei Werkzeug-Fixe gegen den
echten Lastkorpus und sonst nichts.

Purpose: Ein Fix an einem Messwerkzeug, der nur lokal gruen ist, ist eine
Behauptung ueber ein Werkzeug und keine ueber die Messung, die es macht. Der
Index auf der Box ist intakt, also braucht der Beweis keinen Neuaufbau und
passt unter vier Stunden.

Ausdruecklich NICHT Gegenstand dieser Anfahrt: der Upgrade-Beweis. Er braucht
eine Nextcloud mit 1.0.x, die Box traegt den v1.1-Stand mit dem Messindex, und
eine zweite Nextcloud am selben Docker-Dienst verbietet D-02 mit einer teuren
Begruendung (07.09.: `--rm-data` loeschte das Messvolumen der ersten Instanz).
Der Upgrade-Beweis liegt in 11-07, in CI.

Output: Vier Rohdateien und ein kurzer Bericht, danach eine angehaltene Box.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-CONTEXT.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Anfahrt-Freigabe mit Kostendeckel, vor dem Start</name>
  <files>docs/measurements/2026-09-werkzeugfixe/README.md</files>
  <read_first>
    - C:/Users/Student/.findling-loadtest/box.env (Instanz, Volume, BOX_STOPPED_ISO, BOX_LAST_UPTIME_HOURS, BOX_LAST_UPTIME_COST_USD, BOX_PARKED_COST_USD_PER_DAY, der Schadensbericht vom 07.09., der DI-05-36-Befund)
    - scripts/ops/aws_box.sh, `cmd_prices` und `cmd_status`, die gepinnten Saetze und die Laufzeitrechnung
    - docs/measurements/2026-09-werkzeugfixe/skripte/00-ablauf.md aus Plan 11-03
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-CONTEXT.md, D-01
  </read_first>
  <action>
Lies box.env und aws_box.sh cmd_prices, rechne den Kostenrahmen aus den
gepinnten Saetzen aus, und lege ihn dem Owner in der Form der Liste unter
how-to-verify vor. Starte nichts, bevor die Freigabe da ist. Nach der Antwort
traegst du in docs/measurements/2026-09-werkzeugfixe/README.md die Zeile
Anfahrt freigegeben: Datum, Deckel Stunden h / USD USD (D-01) ein, mit dem vom
Owner bestaetigten Deckel und nicht mit dem vorgeschlagenen, falls beide
abweichen. Der Abbau der Box gehoert ausdruecklich nicht in diese Freigabe.
  </action>
  <what-built>
Die beiden Werkzeug-Fixe sind gebaut und lokal gruen: `scripts/ops/search_load.py`
zaehlt eine Ergebnisgruppe ohne Containerteil als Fehlschlag und meldet
`hits_per_request` (Plan 11-02), und
`docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh` prueft den
Fremdbestand vor und urteilt dreiwertig (Plan 11-03). Beide brauchen einen
Beweis gegen den echten Bestand.
  </what-built>
  <how-to-verify>
Der Kostenrahmen, zur Freigabe vorgelegt:

1. Deckel dieser Anfahrt nach D-01: **rund 4 Stunden und 0,50 USD**.
2. Laufender Satz: **0,1158 USD je Stunde** (0,0978 Instanz plus 0,0130
   Speicher plus 0,0050 oeffentliche Adresse), also 0,46 USD fuer vier
   Stunden. Die Saetze sind in `aws_box.sh` gepinnt.
3. Geparkt kostet die Box weiter **0,3130 USD je Tag**; die Anfahrt ersetzt
   diese Kosten nicht, sie kommt dazu.
4. Letzter Lauf laut `box.env`: 31,05 Stunden fuer 3,5969 USD, angehalten am
   2026-09-10T16:22:50Z.
5. Was gefahren wird: Messblock A (Lastwerkzeug, Stufe 16 und Stufe 8, rund 10
   bis 15 Minuten) und Messblock B (Sprachfaelle mit `FRIST=60 RUNDEN=10`,
   rund 15 bis 30 Minuten). Der Rest der vier Stunden ist Ruestzeit und
   Reserve.
6. Abbruchbedingung, die vor dem Start feststeht: zeigt `occ findling:index
   --status` nicht **52.111 indexiert, 37 uebersprungen, 0 fehlgeschlagen**,
   endet die Anfahrt und der Owner entscheidet neu. Unter einem
   Vier-Stunden-Deckel wird kein Index neu aufgebaut.

Der Abbau der Box ist NICHT Teil dieser Freigabe. Er ist Plan 11-12 mit eigener
Bestaetigung (D-03).
  </how-to-verify>
  <verify>
    <human-check>Der Owner hat den Deckel bestaetigt oder einen anderen genannt; die Freigabe steht mit Datum und Deckel im Bericht.</human-check>
    <automated>grep -c "Anfahrt freigegeben" docs/measurements/2026-09-werkzeugfixe/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/measurements/2026-09-werkzeugfixe/README.md` traegt eine Zeile `Anfahrt freigegeben: <Datum>, Deckel <Stunden> h / <USD> USD (D-01)`.
    - Der genannte Deckel ist der vom Owner bestaetigte, nicht der vorgeschlagene, falls beide abweichen.
    - Kein `aws_box.sh start` ist vor dieser Zeile gelaufen (`box.env` traegt keinen neuen Startzeitpunkt).
  </acceptance_criteria>
  <resume-signal>Antworte mit "freigegeben, Deckel 4 h / 0,50 USD" oder nenne einen anderen Deckel.</resume-signal>
  <done>Die Anfahrt hat eine datierte Freigabe mit Deckel, bevor die erste Minute laeuft.</done>
</task>

<task type="auto">
  <name>Task 2: Anfahrt, Zustandspruefung und Messblock A</name>
  <files>docs/measurements/2026-09-werkzeugfixe/rohdaten/04-bestand-vor-der-messung.txt, docs/measurements/2026-09-werkzeugfixe/rohdaten/01-lastwerkzeug-stufe16.json, docs/measurements/2026-09-werkzeugfixe/rohdaten/02-lastwerkzeug-stufe08.json, docs/measurements/2026-09-werkzeugfixe/rohdaten/03-nc-protokoll-abbrueche.txt</files>
  <read_first>
    - scripts/ops/aws_box.sh, `cmd_start` vollstaendig, insbesondere die Nachlese mit ihren fuenf Punkten (neue Adresse, SSH-Regel, /etc/hosts, DI-05-36, harte Speichergrenze)
    - C:/Users/Student/.findling-loadtest/box.env, der Schadensbericht vom 07.09. mit dem Wechsel der Apache-Adresse von 172.18.0.6 auf 172.18.0.4
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 4.4 (die Anfahrtsliste mit Punkt 0 bis 7 und Messblock A) sowie Pitfalls 3, 4 und 5
    - scripts/ops/search_load.py nach Plan 11-02
    - docs/measurements/2026-09-vergleichsmessung-m7g/rohdaten/97-stufe-16.json als Vergleichsdatei
  </read_first>
  <action>
Die Anfahrtsliste in dieser Reihenfolge, jeder Punkt eine bekannte Falle:

1. `AWS_ACCESS_KEY_ID` und `AWS_SECRET_ACCESS_KEY` aus
   `C:/Users/Student/.findling-aws.env` in die Umgebung, nie in ein Argument.
2. `scripts/ops/aws_box.sh start`. Die oeffentliche Adresse wechselt bei jedem
   Start; das Skript schreibt sie und zieht die SSH-Regel nach (revoke vor
   authorize).
3. Den `/etc/hosts`-Eintrag der Box auf die aktuelle Adresse des
   Apache-Containers pinnen. Der Pruefbefehl steht als Kommentar ueber dem
   Eintrag. Diese Adresse wechselt bei JEDEM Maschinenstart und hat am 07.09.
   genau einmal zugeschlagen.
4. `occ app_api:app:disable findling_backend && occ app_api:app:enable
   findling_backend` (DI-05-36). Ohne das indexiert der Container stumm nicht.
5. Falls irgendwo `app_api:app:register` lief: `docker update --memory=2g
   --memory-swap=2g` und den Wert aus der cgroup zurueckgelesen.
6. `free -h` gleich 3.9Gi, `nproc` gleich 2, `uname -m` gleich aarch64.
7. `occ findling:index --status`. Zeigt es nicht 52.111 / 37 / 0, halte an,
   schreibe den Ist-Stand nach `04-bestand-vor-der-messung.txt` und beende die
   Anfahrt (Abbruchbedingung der Freigabe).

Alle Ausgaben der Punkte 6 und 7 gehen beschriftet nach
`04-bestand-vor-der-messung.txt`, mit Zeitstempeln.

**Messblock A.** Notiere den Startzeitstempel, fahre Stufe 16 nach:
`search_load.py --concurrency 16 --rounds 10 --limit 5 --container
nc_app_findling_backend --json .../01-lastwerkzeug-stufe16.json`, das sind 160
Anfragen wie am 10.09. Danach die Kontrollstufe 8 mit denselben Parametern nach
`02-lastwerkzeug-stufe08.json`. Notiere den Endzeitstempel.

Der Beweis ist die Uebereinstimmung zweier unabhaengiger Zaehlungen: schreibe
den Ausschnitt des Nextcloud-Protokolls im Zeitfenster des Laufs nach
`03-nc-protokoll-abbrueche.txt`, mit Start- und Endzeitstempel als Kopfzeilen,
und zaehle darin die `cURL error 28`-Zeilen. Diese Zahl wird gegen den
`failures`-Zaehler des Werkzeugs gestellt.

Die Erwartung steht in `00-ablauf.md` und wird nicht nachtraeglich angepasst:
Stufe 16 zeigt `failures > 0` und ein `hits_per_request` deutlich unter dem der
Stufe 8, Stufe 8 zeigt `failures == 0`. Trifft die Erwartung nicht, ist das ein
Ergebnis und kein Grund fuer eine zweite Anfahrt: die 17 Abbrueche vom 10.09.
hingen an der Kaltheit des Wirtscaches, und der ist nach dem Stillstand anders
kalt.
  </action>
  <verify>
    <automated>python -c "import json; a=json.load(open('docs/measurements/2026-09-werkzeugfixe/rohdaten/01-lastwerkzeug-stufe16.json')); b=json.load(open('docs/measurements/2026-09-werkzeugfixe/rohdaten/02-lastwerkzeug-stufe08.json')); assert a['requests']==160 and b['requests']==80; assert 'hits_per_request' in a and 'min_hits' in a; print(a['failures'], a['hits_per_request'], b['failures'], b['hits_per_request'])"</automated>
  </verify>
  <acceptance_criteria>
    - `04-bestand-vor-der-messung.txt` nennt `52111`, `37` und `0` aus `occ findling:index --status` sowie `3.9Gi`, `2` und `aarch64`.
    - Beide JSON-Rohdateien tragen die Schluessel `min_hits`, `hits_per_request`, `failures` und `failure_kinds`.
    - `01-lastwerkzeug-stufe16.json` meldet `requests` gleich 160, `02-lastwerkzeug-stufe08.json` meldet 80.
    - `03-nc-protokoll-abbrueche.txt` traegt Start- und Endzeitstempel des Laufs als Kopfzeilen und eine Zeile mit der gezaehlten Zahl der `cURL error 28`-Vorkommen.
    - Die gezaehlte Zahl aus dem Protokoll und `failure_kinds["EmptyResultGroup"]` der Stufe 16 stehen im Bericht nebeneinander; eine Abweichung ist benannt und nicht geglaettet.
  </acceptance_criteria>
  <done>Messblock A liegt als drei Rohdateien vor, und der neue Zaehler steht neben einer unabhaengigen Zaehlung derselben Sache.</done>
</task>

<task type="auto">
  <name>Task 3: Messblock B und die Box anhalten</name>
  <files>docs/measurements/2026-09-werkzeugfixe/rohdaten/05-sprachfaelle.txt, docs/measurements/2026-09-werkzeugfixe/rohdaten/06-kosten.txt</files>
  <read_first>
    - docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh aus Plan 11-03, insbesondere die Vorpruefung, die Schwelle und die Bilanzzeile
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 4.4, Messblock B, und Annahme A5 (das Konto sprachfall und seine 39 Dateien liegen noch auf der Box)
    - scripts/ops/aws_box.sh, `cmd_stop` (schreibt Laufzeit und Kosten selbst nach box.env)
    - docs/measurements/2026-09-vergleichsmessung-m7g/rohdaten/98b-sprachfaelle-diagnose.txt als Vergleich
  </read_first>
  <action>
Hole die Laufnummer des letzten gruenen `integration.yml`-Laufs mit `gh run
list --workflow=integration.yml` und uebergib sie als `CI_LAUF`. Ohne sie
bricht das Skript ab, und das ist Absicht.

Fahre `98b-sprachfaelle.sh` mit `FRIST=60 RUNDEN=10 CI_LAUF=<nummer>` und
schreibe die Ausgabe nach `05-sprachfaelle.txt`. `FRIST=60` statt der Vorgabe
360, weil die Vorgabe fuer einen erst aufzubauenden Bestand gilt; der Bestand
steht schon.

Existiert das Konto `sprachfall` mit seinen 39 Dateien nicht mehr, legt das
Skript sie an; das kostet rund 20 bis 30 zusaetzliche Minuten (OCR von 21
Seiten plus Indexierung) und passt in den Deckel. Halte im Bericht fest, welcher
der beiden Faelle eingetreten ist.

Danach `scripts/ops/aws_box.sh stop`. Lies `box.env` zurueck und schreibe
`BOX_LAST_UPTIME_HOURS` und `BOX_LAST_UPTIME_COST_USD` dieser Anfahrt nach
`06-kosten.txt`, zusammen mit dem freigegebenen Deckel und der Differenz. Der
`status`-Aufruf nennt die Zeit seit dem letzten Start nur bei laufender Instanz
eine Laufzeit; das ist seit Plan 10-05 richtig und darf nicht umgangen werden.
  </action>
  <verify>
    <automated>grep -c "NICHT MESSBAR" docs/measurements/2026-09-werkzeugfixe/rohdaten/05-sprachfaelle.txt &amp;&amp; grep -c "ci-beleg: integration.yml Lauf [0-9]" docs/measurements/2026-09-werkzeugfixe/rohdaten/05-sprachfaelle.txt &amp;&amp; grep -c "sprachfaelle bestanden" docs/measurements/2026-09-werkzeugfixe/rohdaten/05-sprachfaelle.txt</automated>
  </verify>
  <acceptance_criteria>
    - `05-sprachfaelle.txt` traegt fuer jeden der zehn Begriffe eine Zeile `fremdbestand <begriff> <treffer>`.
    - Mindestens ein Fall traegt `NICHT MESSBAR` mit der Trefferzahl in Klammern.
    - Die Bilanzzeile nennt beide Zahlen in einer Zeile (`bestanden n von 10, davon m nicht messbar`).
    - `ci-beleg:` traegt eine Laufnummer und nicht das Wort `unbekannt`.
    - `06-kosten.txt` nennt Laufzeit, Kosten dieser Anfahrt, den freigegebenen Deckel und die Differenz; die Kosten liegen unter dem Deckel oder die Ueberschreitung ist beziffert.
    - `aws_box.sh status` meldet danach `stopped`, unabhaengig aus der API gelesen.
  </acceptance_criteria>
  <done>Messblock B liegt vor, die Box ist angehalten, und die Kosten stehen dem Deckel gegenueber.</done>
</task>

<task type="auto">
  <name>Task 4: Der kurze Bericht der Anfahrt</name>
  <files>docs/measurements/2026-09-werkzeugfixe/README.md</files>
  <read_first>
    - alle sechs Rohdateien aus den Tasks 2 und 3
    - docs/measurements/2026-09-werkzeugfixe/skripte/00-ablauf.md (die vorher aufgeschriebene Erwartung)
    - docs/measurements/2026-09-vergleichsmessung-m7g/README.md, Abschnitt 9.3, als Anschlussstelle
    - .planning/phases/10-vergleichsmessung-auf-der-aws-box/deferred-items.md, DI-10-01 und DI-10-02
  </read_first>
  <action>
Schreibe einen kurzen Bericht, kein zweiter Messbericht: Anlass (DI-10-01 und
DI-10-02), Freigabe mit Deckel und Datum, was gefahren wurde, was gemessen
wurde, und ob die vorher aufgeschriebene Erwartung eingetreten ist. Jede Zahl
mit ihrer Rohdatei daneben, in der Form, die der Phase-10-Bericht fuehrt.

Zwei Abschnitte tragen den Beweis:
    **DI-10-01 geschlossen oder nicht.** Die beiden Zaehlungen nebeneinander
    (`failure_kinds["EmptyResultGroup"]` der Stufe 16 gegen die Zahl der
    `cURL error 28`-Zeilen im selben Fenster), dazu `hits_per_request` beider
    Stufen gegen die Werte vom 10.09. (5,25 auf Stufe 8, 4,16 auf Stufe 16).
    Weichen die Zaehlungen ab, steht die Abweichung mit einer Vermutung ueber
    den Grund da und nicht ohne.
    **DI-10-02 geschlossen oder nicht.** Die Vorpruefungszahlen je Begriff, die
    Zahl der nicht messbaren Faelle, die Bilanzzeile, und der Satz, dass der
    Beweis mit eigenem Index in `integration.yml` liegt, mit der Laufnummer.

Ein Abschnitt "Was diese Anfahrt nicht besser gemacht hat" gehoert dazu, nach
dem Muster des Phase-10-Berichts. Er nennt mindestens: DI-10-04 bleibt
ungeklaert und braucht einen 26-Stunden-Volllauf, und der Upgrade-Beweis lag
nicht in dieser Anfahrt.

Das Wort "archiv" darf in keiner nach aussen sichtbaren Datei stehen; fahre das
Vokabular-Gate lokal, bevor du fertig meldest.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest tests/test_store_metadata.py tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Bericht nennt jede der sechs Rohdateien mindestens einmal mit ihrem Dateinamen.
    - Beide Zaehlungen zu DI-10-01 stehen in einer Tabelle nebeneinander.
    - Der Abschnitt "Was diese Anfahrt nicht besser gemacht hat" existiert und nennt DI-10-04.
    - `grep -ci "arch""iv" docs/measurements/2026-09-werkzeugfixe/README.md` ergibt 0.
    - Der Bericht enthaelt keinen Gedankenstrich.
    - `docs/measurements/2026-09-werkzeugfixe/` enthaelt keine Datei mit einem Passwort und keinen Maschinenpfad.
  </acceptance_criteria>
  <done>Die Anfahrt hat einen Bericht, der sagt, welcher der beiden Befunde geschlossen ist und welcher nicht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsplatz zu AWS-Konto | Zugangsdaten und SSH-Schluessel liegen ausserhalb des Repositoriums und duerfen nie in ein Argument oder eine Rohdatei geraten |
| Messbestand zu Messaussage | Der 52.111er-Index ist die Vergleichsgrundlage; ein Eingriff daran macht die Phase-10-Zahlen unvergleichbar |
| Kostendeckel zu Ausfuehrung | Eine Anfahrt ohne Deckel ist eine offene Rechnung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-20 | Information Disclosure | AWS-Zugangsdaten, Lasttest-Passwort | mitigate | Zugangsdaten aus der Umgebung, Passwort per Umgebungsvariable, nie als Argument; das Gate `test_the_script_of_this_run_puts_no_password_on_a_command_line` liest die Skripte, und die Rohdateien werden vor dem Commit auf Zugangsdaten durchgesehen |
| T-11-21 | Denial of Service | Kosten der Box | mitigate | Blockierende Freigabe mit Deckel VOR dem Start, `stop` unmittelbar nach Messblock B, Kostenbilanz in `06-kosten.txt` |
| T-11-22 | Tampering | Messvolumen vol-04c5b59fe9417babd | mitigate | Keine zweite Nextcloud am selben Docker-Dienst (D-02), kein `--rm-data`, kein Indexneuaufbau; die Abbruchbedingung 52.111/37/0 steht vor der ersten Messung |
| T-11-23 | Spoofing | SSH-Zugang zur Box | mitigate | `aws_box.sh start` zieht die SSH-Regel auf die aktuelle Owner-Adresse nach, revoke vor authorize; kein dauerhaft offener Port 22 |
| T-11-24 | Repudiation | Ergebnis gegen Erwartung | mitigate | Die Erwartung steht vorher in `00-ablauf.md`; eine verfehlte Erwartung wird als Ergebnis berichtet und nicht nachtraeglich umgeschrieben |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Keiner. Auf der Box wird kein Paket installiert; der Container bleibt das veroeffentlichte Abbild |
</threat_model>

<verification>
- Sechs Rohdateien unter `docs/measurements/2026-09-werkzeugfixe/rohdaten/` vorhanden
- `aws_box.sh status` meldet `stopped`, unabhaengig aus der API bestaetigt
- Kosten dieser Anfahrt unter dem freigegebenen Deckel, oder die Ueberschreitung beziffert
- Kein Diff unter `docs/measurements/2026-09-vergleichsmessung-m7g/`
</verification>

<success_criteria>
Die zwei Werkzeug-Fixe sind gegen den echten Lastkorpus belegt, in einer
Anfahrt, unter dem freigegebenen Deckel, und die Box ist wieder angehalten.
</success_criteria>

<output>
Create `.planning/phases/11-haertung-und-store-einreichung-v1-1/11-06-SUMMARY.md` when done
</output>
