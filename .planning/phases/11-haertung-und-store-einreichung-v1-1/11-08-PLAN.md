---
phase: 11-haertung-und-store-einreichung-v1-1
plan: 08
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - php/l10n/fr.json
  - php/l10n/fr.js
  - backend/tests/test_admin_ui_contract.py
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "Die App liefert einen vollstaendigen franzoesischen Katalog in beiden Dateiformaten"
    - "Sechs Katalogdateien tragen dieselbe Schluesselmenge, und ein Test macht jede Abweichung rot"
    - "Kein franzoesischer Wert verliert oder erfindet einen Platzhalter"
    - "fr.json und fr.js tragen die franzoesische Pluralregel und nicht die deutsche"
  artifacts:
    - path: "php/l10n/fr.json"
      provides: "der franzoesische Katalog fuer die Serverseite"
      contains: "pluralForm"
    - path: "php/l10n/fr.js"
      provides: "der franzoesische Katalog fuer den Browser"
      contains: "OC.L10N.register"
    - path: "backend/tests/test_admin_ui_contract.py"
      provides: "die vier Gates G1 bis G4 und das auf die Kataloge ausgedehnte Dash-Gate"
      contains: "fr.json"
  key_links:
    - from: "php/l10n/fr.json"
      to: "docs/l10n-french.md"
      via: "mechanischer Guss aus der abgenommenen Tabelle"
      pattern: "translations"
    - from: "backend/tests/test_admin_ui_contract.py"
      to: "php/l10n/fr.js"
      via: "Schluesselvergleich ueber alle sechs Dateien"
      pattern: "fr.js"
---

<objective>
Der Guss: aus der abgenommenen Tabelle werden zwei Katalogdateien, und vier
Gates halten sie dauerhaft mit den vier deutschen zusammen.

Purpose: `docs/l10n-french.md` verlangt vollstaendig oder gar nicht, in beiden
Dateiformaten, mit erweitertem Schluesselvergleich. Anders als bei Deutsch ist
es dabei ausdruecklich KEINE Gleichheit der Dateien: de_DE gegen de ist
Textgleichheit, fr gegen de waere sinnlos. An die Stelle der Textgleichheit
tritt die Platzhalter-Paritaet je Schluessel, die Franzoesisch tatsaechlich
halten kann.

Output: fr.json, fr.js und vier Zusicherungen, die rot werden koennen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fr.json aus der abgenommenen Tabelle giessen</name>
  <files>php/l10n/fr.json</files>
  <read_first>
    - docs/l10n-french.md nach der Abnahme in Plan 11-05, insbesondere die dreispaltige Tabelle, die Ausnahmenliste fuer G2 und die Pluralzeilen
    - php/l10n/de.json vollstaendig: Aufbau der Datei (translations und pluralForm), die Reihenfolge der Schluessel, die Form der Pluralwerte als Liste
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 1.3 und Pitfall 8
  </read_first>
  <action>
Erzeuge php/l10n/fr.json mechanisch aus der Tabelle, nicht von Hand
abgeschrieben: ein kurzes Einwegskript liest docs/l10n-french.md, zieht die
Tabellenzeilen und schreibt die Datei. Das Skript wird nicht eingecheckt, das
Erzeugnis wird geprueft. Ein Guss von Hand waere genau die Fehlerquelle, gegen
die die Tabelle gebaut wurde.

Aufbau exakt wie de.json: das Objekt translations mit derselben
Schluesselreihenfolge wie de.json, daneben pluralForm mit dem Wert
nplurals=2; plural=(n greater than 1); in der Schreibweise, die de.json
verwendet, also mit dem Zeichen fuer groesser als. Die fuenf Pluralschluessel
bekommen Listen mit genau zwei Formen, in der Reihenfolge Singular, Plural.

Die deutsche Regel mit ungleich 1 darf nirgends stehen: bei n gleich 0 verlangt
Franzoesisch den Singular, die deutsche Regel liefert dort den Plural.

Kodierung UTF-8 ohne BOM, Zeilenende LF, Einrueckung und Dateiendezeichen wie
in de.json. Echte Akzente und Guillemets, ASCII-Apostroph, kein
Gedankenstrich.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -c "import json; d=json.load(open('../php/l10n/de.json',encoding='utf-8')); f=json.load(open('../php/l10n/fr.json',encoding='utf-8')); assert set(d['translations'])==set(f['translations']); assert f['pluralForm'].startswith('nplurals=2'); print(len(f['translations']))"</automated>
  </verify>
  <acceptance_criteria>
    - php/l10n/fr.json ist gueltiges JSON und traegt genau die Schluesselmenge von de.json.
    - pluralForm lautet zeichengleich `nplurals=2; plural=(n > 1);`.
    - Genau fuenf Werte sind Listen, und jede Liste hat genau zwei Eintraege.
    - Die Zeichenkette `plural=(n != 1)` kommt in der Datei nicht vor.
    - Die Datei enthaelt kein U+2019, kein U+2014 und kein U+2013.
    - Kein Wert ist leer.
  </acceptance_criteria>
  <done>Der serverseitige Katalog liegt vor, mechanisch erzeugt und maschinell gegen den deutschen gehalten.</done>
</task>

<task type="auto">
  <name>Task 2: fr.js aus derselben Quelle</name>
  <files>php/l10n/fr.js</files>
  <read_first>
    - php/l10n/de.js, die ersten und die letzten Zeilen: die Aufrufform OC.L10N.register mit der Anwendungskennung findling, dem Objekt und der Pluralregel als letztem Parameter, dazu Einrueckung und Dateiende
    - php/l10n/fr.json aus Task 1
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Pitfall 8 (ein fr.js aus Copy-Paste von de.js traegt die falsche Pluralregel)
  </read_first>
  <action>
Erzeuge php/l10n/fr.js aus derselben Quelle wie fr.json, mit demselben
Einwegskript, und nicht aus de.js kopiert. Die Aufrufform ist die von de.js:
OC.L10N.register mit der Kennung findling, dem Katalogobjekt und der
Pluralregel als letztem Parameter, mit derselben Schluesselreihenfolge und
derselben Einrueckung wie die deutsche Fassung.

Die Pluralregel steht hier ein zweites Mal. Sie muss zeichengleich mit der aus
fr.json sein, das ist Gate G4.

Kodierung UTF-8 ohne BOM, Zeilenende LF.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -c "import json; s=open('../php/l10n/fr.js',encoding='utf-8').read(); o=json.loads(s[s.index('{'):s.rindex('}')+1]); f=json.load(open('../php/l10n/fr.json',encoding='utf-8')); assert set(o)==set(f['translations']); assert f['pluralForm'] in s; assert 'findling' in s; print(len(o))"</automated>
  </verify>
  <acceptance_criteria>
    - Das Objekt zwischen der ersten geschweiften Klammer und der letzten ist gueltiges JSON und traegt dieselbe Schluesselmenge wie fr.json.
    - Der Wert von pluralForm aus fr.json kommt zeichengleich in fr.js vor.
    - Die Anwendungskennung findling steht im Aufruf, wie in de.js.
    - Die Datei enthaelt kein U+2019, kein U+2014 und kein U+2013 und keinen Wagenruecklauf.
  </acceptance_criteria>
  <done>Der Browserteil spricht dieselbe Sprache wie der Serverteil, aus derselben Quelle erzeugt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Die vier Gates und das ausgedehnte Dash-Gate</name>
  <files>backend/tests/test_admin_ui_contract.py</files>
  <read_first>
    - backend/tests/test_admin_ui_contract.py, Zeilen 83 bis 120 (die Pfadkonstanten L10N_JSON, L10N_JS, L10N_DE_DE_JSON, L10N_DE_DE_JS), Zeile 896 (test_the_two_translation_files_carry_the_same_keys), Zeile 918 (test_the_german_catalogue_covers_both_german_language_codes mit der harten Zahl 173 und der Textgleichheit der Zwillinge) und Zeile 486 (test_no_file_of_the_page_carries_a_dash_or_an_emoji, das heute sechs Dateien der beiden Seiten liest und keine Katalogdatei)
    - docs/l10n-french.md, die Ausnahmenliste fuer G2 und der Grund, warum Findling kein Uebersetzungsschluessel ist
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-13-SUMMARY.md: bei V-1a steht die harte Zahl schon auf 174, bei V-1b traegt die Datei den SKIPPED-Vermerk
    - .planning/phases/11-haertung-und-store-einreichung-v1-1/11-RESEARCH.md, Abschnitt 1.3 mit der Gate-Tabelle G1 bis G4 und der Code-Example-Block "Der Schluesselvergleich fuer zwei Sprachpaare"
  </read_first>
  <behavior>
    - G1: die sechs Dateien de.json, de.js, de_DE.json, de_DE.js, fr.json, fr.js tragen dieselbe Schluesselmenge; eine fehlende Datei ist ein Fehlschlag mit Namen und kein stilles Weniger-Pruefen.
    - G2: kein FR-Wert ist leer, und keiner ist mit seinem englischen Schluessel identisch, ausser bei den namentlich gelisteten Ausnahmen.
    - G3: fuer jeden Schluessel ist die Multimenge der Prozent-Direktiven im FR-Wert dieselbe wie im Schluessel, ueber alle Formen eines Pluralwerts.
    - G4: fr.json traegt die franzoesische Pluralregel, fr.js denselben String, und die fuenf Pluralschluessel tragen genau zwei Formen.
    - Dash-Gate: keine der sechs Katalogdateien traegt einen Gedankenstrich oder ein Emoji.
  </behavior>
  <action>
Alle Gates kommen nach backend/tests/test_admin_ui_contract.py und nicht in
eine neue Datei: der de/de_DE-Vergleich lebt dort, und zwei Orte fuer eine
Frage sind genau der Widerspruch, den die Audits dieses Projekts sonst
aufschreiben.

Lege zwei Pfadkonstanten fuer fr.json und fr.js neben die vier bestehenden.
Nutze den bestehenden Ausschneider fuer das Objekt der .js-Datei (zwischen der
ersten geschweiften Klammer und der letzten) statt eines zweiten.

G1 ersetzt die Schluesselmengenpruefung in
test_the_german_catalogue_covers_both_german_language_codes nicht, sondern
steht daneben und deckt alle sechs. Die Textgleichheit der deutschen Zwillinge
bleibt unveraendert und gilt ausdruecklich nicht fuer Franzoesisch; schreibe
diesen Satz in den Docstring, mit dem Verweis auf docs/l10n-french.md Punkt 3.

Die harte Zahl ruehrst du nicht an: der FR-Katalog fuegt keine Schluessel
hinzu, er uebersetzt die vorhandenen. Bei V-1b steht dort 173, bei V-1a steht
dort 174, weil Plan 11-13 sie in Welle 2 mitsamt Docstring gehoben hat. Pruefe
nur, dass die Zahl zum Entscheid passt und dass der Docstring ihn bei 174
nennt. Musst du sie hier selbst aendern, ist etwas anderes schiefgegangen, und
das gehoert in die SUMMARY statt in einen stillen Griff: jede Aenderung dieser
Zahl ohne Entscheid dahinter heisst, jemand hat einen Schluessel erfunden.

G2 arbeitet mit einer benannten Ausnahmenliste als Modulkonstante, gefuellt aus
docs/l10n-french.md. Keine Toleranzschwelle: eine Zahl, die sagt "so viele
duerfen gleich sein", nagelt die falsche Invariante fest.

G3 nutzt einen Regulaerausdruck ueber die Prozent-Direktiven, der die
nummerierten Formen mit Dollarzeichen, die einfachen und die Pluralform mit n
erfasst, dazu das verdoppelte Prozentzeichen. Vergleiche sortierte Listen, also
Multimengen und nicht Mengen: zwei gleiche Direktiven sind nicht eine.

Dehne test_no_file_of_the_page_carries_a_dash_or_an_emoji auf die sechs
Katalogdateien aus. Die Begruendung gehoert in den Docstring: franzoesische
Typographie bringt Guillemets und Apostrophe mit, die harmlos sind, aber der
Gedankenstrich ist in franzoesischer Zeitungstypographie gebraeuchlich und
wuerde sich ohne Gate einschleichen.

Jedes Gate braucht seinen Selbsttest nach dem Muster der Datei: eine mutierte
Eingabe, die gemeldet wird. Mindestens fuer G3 (ein verlorener nummerierter
Platzhalter) und fuer G4 (die deutsche Pluralregel in fr.json).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -m pytest tests/test_admin_ui_contract.py -q &amp;&amp; uv run python -m pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright &amp;&amp; uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - Vier neue Tests existieren, je einer fuer G1 bis G4, und jeder nennt in seinem Docstring, welche Frage er beantwortet.
    - G1 prueft namentlich alle sechs Dateien und meldet eine fehlende Datei als Fehlschlag mit Dateinamen.
    - G2 nutzt eine Modulkonstante als Ausnahmenliste, und keine Zahl als Schwelle.
    - G3 vergleicht sortierte Listen der Direktiven und laeuft ueber alle Formen eines Pluralwerts.
    - Zwei Selbsttests belegen, dass G3 und G4 rot werden koennen.
    - test_no_file_of_the_page_carries_a_dash_or_an_emoji liest jetzt zusaetzlich die sechs Katalogdateien.
    - Die harte Zahl in test_the_german_catalogue_covers_both_german_language_codes passt zum Entscheid: 173 bei V-1b, 174 bei V-1a mit dem Docstring, den Plan 11-13 dort hinterlassen hat. Dieser Plan aendert sie nicht.
    - Die gesamte Gate-Kette laeuft gruen: pytest, ruff check, ruff format --check, pyright, vulture.
  </acceptance_criteria>
  <done>Was keine Maschine haelt, laeuft auseinander; ab jetzt haelt eine Maschine sechs Katalogdateien zusammen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Katalogwert zu Templateausgabe | Jeder FR-Wert laeuft durch die Uebersetzungsfunktion und wird im Template ausgegeben |
| Katalogwert zu Formatierungsaufruf | Die Werte werden mit Platzhaltern gefuellt; ein verlorener Platzhalter ist ein Aufruf, der seine Datei nicht mehr nennt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-30 | Tampering | FR-Werte mit spitzen Klammern oder Ampersand | mitigate | Das Escaping-Gate prueft das Template; zusaetzlich sieht das Phase-11-Audit die 173 beziehungsweise 174 Werte unter ASVS V5 einmal durch, und das Dash-Gate liest die Katalogdateien jetzt mit |
| T-11-31 | Denial of Service | verlorener oder erfundener Platzhalter | mitigate | Gate G3, Platzhalter-Paritaet je Schluessel ueber alle Pluralformen, mit Selbsttest |
| T-11-32 | Repudiation | halbe Oberflaeche durch einseitige Schluessel | mitigate | Gate G1 ueber alle sechs Dateien; eine fehlende Datei ist ein benannter Fehlschlag statt eines stillen Weniger-Pruefens |
| T-11-33 | Tampering | falsche Pluralregel aus Copy-Paste | mitigate | Gate G4 prueft beide Dateien und den Ausschluss der deutschen Regel, mit Selbsttest |
| T-11-34 | Elevation of Privilege | keine | accept | Der Katalog fuehrt keine Berechtigungsentscheidung und beruehrt die Kette aus Vorfilter und PHP-Recheck nicht |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | Keiner. Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- Aus `backend/`: `uv run python -m pytest -q && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80`
- Sechs Katalogdateien unter `php/l10n/` mit einer Schluesselmenge
- Kein Gedankenstrich und kein typographischer Apostroph in den Katalogen
</verification>

<success_criteria>
Die Vorbedingung aus Phase 9 ist eingeloest: ein vollstaendiger franzoesischer
Katalog in beiden Dateiformaten, mit einem Schluesselvergleich, der auf zwei
Sprachpaare erweitert ist, und mit einer Invariante, die Franzoesisch halten
kann.
</success_criteria>

<output>
Create `.planning/phases/11-haertung-und-store-einreichung-v1-1/11-08-SUMMARY.md` when done
</output>
