---
phase: 06.1-launch-haertung-vor-der-store-abgabe
plan: 19
subsystem: testing
tags: [d-h5, sichtprobe, abnahme, release-rehearsal, code-signatur, openblas, rlimit-as, ooxml, gastnutzer, appstore-token, roadmap]

# Dependency graph
requires:
  - phase: 06.1-launch-haertung-vor-der-store-abgabe
    provides: "Plan 06.1-13 die Store-Formalien und die netzfreien Bildpruefungen, 06.1-15 scripts/dev/guest_parity.sh, 06.1-16 scripts/dev/aio_install_check.sh und docs/install-check.md, 06.1-17 die drei Audits und die uebergebenen Reste DI-06.1-05 und DI-06.1-13, 06.1-18 die Nachmessung und den arm64-Lauf"
provides:
  - "Die Live-Bestaetigung aller fuenf screenshot-Adressen beider info.xml, mit Status, Inhaltstyp, Groesse, PNG-Signatur, Massen und SHA-256 gegen die Dateien im Repository"
  - "Die Entscheidung des NC-35-tolerate-failure-Schalters samt Wiedervorlagedatum, Nachfolgeplan und der Messung, dass die Zeile heute schon gruen ist"
  - "Kriterium 3 der ROADMAP in der Fassung nach E-H5 und E-H1, mit benannter Nichtabdeckung all-in-one und der alten Fassung durchgestrichen darunter"
  - "Die vollstaendige Planliste der Phase, 23 statt 20 Eintraege"
  - "Die Lizenz AGPL-3.0-or-later und die verankerte Routenform ^/<name>$ in beiden info.xml, im Lockstep (DI-06.1-05, DI-06.1-13)"
  - "Der Vorab-Vergleich des Eigentuemers in der Gastnutzer-Probe, mit eigenem Exitcode 2 (DI-06.1-17)"
  - "Das Ergebnis der Gastnutzer-Probe in docs/testing.md, vier Vergleiche bestanden, guests 4.9.0"
  - "docs/install-check.md Abschnitt 6: das Protokoll der Sichtprobe samt Archivpruefsummen, Zertifikatsherkunft, Zero-Config-Zahlenreihe und Befund 8"
  - "Der Fix des Befundes 8: _pin_native_thread_pools() in der Extraktions-Sandbox, mit drei Tests"
  - "Der dritte Verbleib der ARM-Box in docs/performance.md, als dokumentierte Abweichung zum Plan, mit Parkkosten und Abbaukriterium"
  - "Eine laufende frische Nextcloud auf http://localhost:8097, beide Haelften aus den Release-Archiven, fuer die Sichtprobe des Owners"
affects: [06-12, v1.1, store-abgabe]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Die Sichtprobe laeuft auf zwei nacheinander erzeugten Instanzen: eine fuer den vollstaendigen Fahrplan samt Deinstallations-Zusagen, eine unberuehrte fuer den Owner"
    - "Native Threadpools werden im Extraktions-Kindprozess auf einen Thread festgenagelt, nach der Adressraumgrenze und vor dem Import der Parser"
    - "Ein Probelauf-Archiv wird an seiner Zertifikatsherkunft gemessen und nicht nur an seiner Signatur"

key-files:
  created:
    - ".dev/sichtprobe/stack/compose.yaml (nicht ausgeliefert, Wegwerf-Stack der Sichtprobe)"
    - ".dev/sichtprobe/stack/prepare.sh, install-for-owner.sh, run-install-check.sh, run-guest-parity.sh, upload-docs.sh, drive-cron.sh, tables.php"
    - ".dev/sichtprobe/stack/build_sichtprobe_docs.py (die fuenf deutschen Dokumente)"
  modified:
    - "backend/appinfo/info.xml (Lizenz, fuenf Routen-URLs, zwei Begruendungsabsaetze)"
    - "php/appinfo/info.xml (Lizenz, im Lockstep)"
    - "backend/src/findling/extract/sandbox.py (_pin_native_thread_pools)"
    - "backend/tests/test_sandbox.py (drei Tests zum Fix)"
    - "backend/tests/test_store_metadata.py (media_files ohne Dotfiles, Lizenzkommentar)"
    - "backend/tests/test_diagnose_endpoint.py (verankerte Routenform)"
    - "scripts/dev/guest_parity.sh (Vorab-Vergleich des Eigentuemers)"
    - ".github/workflows/deploy-harp.yml (NC-35-Schalter entschieden)"
    - ".planning/ROADMAP.md (Kriterium 3, Planliste)"
    - "store/media/README.md (Live-Bestaetigung)"
    - "docs/install-check.md (Abschnitt 6, das Protokoll)"
    - "docs/testing.md (Gastnutzer-Probe bestanden statt ausstehend)"
    - "docs/performance.md (dritter Verbleib der Box)"
    - "docs/certificates.md (die Routenaussage nachgezogen)"

key-decisions:
  - "Der NC-35-tolerate-failure-Schalter bleibt: Nextcloud 35 stand am 07.09. bei v35.0.0rc3 als prerelease, die Bedingung des Schalters ist erst am 16.09. erfuellt. Wiedervorlagedatum, Nachfolgeplan 06-12 und die Regel, dass ein roter Lauf danach ein Befund ist, stehen neben ihm im Workflow"
  - "Kriterium 3 verliert all-in-one als Installationsweg und NC 32, behaelt amd64 und arm64; die alte Fassung bleibt durchgestrichen sichtbar"
  - "Die Sichtprobe laeuft auf dem unveraenderten veroeffentlichten Abbild und nicht auf einem von Hand gepatchten Container, auch um den Preis, dass der Owner Befund 8 sieht"
  - "Die sechs Deinstallations-Zusagen werden auf der Instanz des Owners nicht wiederholt, weil eine abgerissene und wieder aufgebaute Instanz nicht die Instanz ist, nach der D-H5 fragt"
  - "Die ARM-Box wird nicht abgebaut (Betreiberentscheid 07.09.); Abbaukriterium ist die v1.1-Messung"

patterns-established:
  - "Zweistufige Sichtprobe: Fahrplan-Instanz fuer die Maschinenaussage, unberuehrte Instanz fuer die Menschenaussage"
  - "Ein Suchwort einer Sichtprobe steht nur im Inhalt und in keinem Dateinamen, sonst prueft es die Dateiliste"
  - "MSYS2_ARG_CONV_EXCL nennt die Pfade des Containers und den Optionswert, nie *"

requirements-completed: [PKG-05]

# Metrics
duration: 100min
completed: 2026-09-07
---

# Phase 06.1 Plan 19: Owner-Sichtprobe, Abnahme und die drei Reste Summary

**Die drei Reste sind geschlossen, die Sichtprobe steht bereit, und sie hat auf dem Weg dorthin einen Fehler gefunden, den fuenf Phasen Tests und Messungen nicht sehen konnten: jedes Office-Dokument meldet sich ab genuegend CPU-Kernen als beschaedigt, Nextclouds eigenes Willkommensdokument eingeschlossen.**

## Performance

- **Duration:** rund 100 min bis zum Checkpoint
- **Started:** 2026-09-07T11:15:00Z
- **Completed:** noch offen, Checkpoint erreicht
- **Tasks:** 1 von 3 abgeschlossen, Task 2 ist der Checkpoint
- **Files modified:** 14 im Repository, 9 Hilfsskripte unter `.dev/` (nicht ausgeliefert)

## Accomplishments

### Task 1: die drei Reste und die Vorbereitung der Sichtprobe

**Erstens die Bildadressen.** Alle drei Dateien hinter den fuenf
`screenshot`-Adressen sind am 07.09.2026 um 11:19 UTC von Hand abgerufen: Status
200, Inhaltstyp `image/png`, Groesse gleich der Zahl in der Tabelle daneben. Zwei
Pruefungen mehr als der Plan verlangte, weil ein Statuscode allein nur sagt, dass
etwas geantwortet hat: die PNG-Signatur samt `IHDR`-Massen, und die SHA-256-Summe
der heruntergeladenen Bytes gegen die Dateien im Verzeichnis. Damit ist nicht nur
belegt, dass die Adresse antwortet, sondern dass sie genau dieses Bild
ausliefert. `test_store_metadata.py` ist in seinen Zusicherungen unveraendert und
braucht weiterhin kein Netz.

**Zweitens der NC-35-Rest.** Entschieden, nicht offen gelassen, und die
Entscheidung ruht auf einer Ablesung: `gh api repos/nextcloud/server/releases`
antwortete am 07.09. mit `v35.0.0rc3` vom 03.09. als neuestem 35er-Tag, als
`prerelease` markiert, und mit `v34.0.3` als neuestem Nicht-Prerelease. Nextcloud
35 ist am Abnahmetag also noch in Entwicklung, die Aufhebungsbedingung des
Schalters ist nicht erfuellt, und der Schalter bleibt. Was jetzt neben ihm steht:
das Wiedervorlagedatum 16.09.2026, der Plan, der die Nachverfolgung haelt (06-12,
der naechste Plan nach diesem Datum), die Regel dass ein roter Lauf nach dem
Umlegen ein Befund ist und kein Grund zum Zuruecknehmen, und die eine Messung,
die den Tag schon vorwegnimmt: im Lauf `34114937751` vom 07.09. war die
`stable35`-Zeile von sich aus gruen, alle drei Zeilen `success`. Das Umlegen
traegt damit kein bekanntes Risiko.

**Drittens Kriterium 3 der ROADMAP.** Es versprach AIO und docker-compose, NC 32
bis 35, amd64 und arm64. Gefahren wurde der compose-Weg auf beiden Architekturen
auf NC 34.0.3, und die CI-Matrix fuehrt ihn auf stable33 bis stable35. Nicht
gefahren wurde all-in-one als Installationsweg: beide Handlaeufe gingen ueber
`--flavour compose`, der arm64-Lauf aus drei in `docs/install-check.md`
Abschnitt 5 benannten Gruenden. NC 32 fiel mit E-H1 aus dem Versionsfenster. Das
Kriterium sagt das jetzt, mit Verweis auf die Abschnitte 1, 3 und 5, und die
urspruengliche Fassung steht durchgestrichen darunter, damit sichtbar bleibt,
dass hier etwas kleiner geworden ist und nicht immer schon so stand (T-06.1-83).

**Viertens die Vorbereitung.** Der Probelauf von `release.yml` ist ueber
`workflow_dispatch` gefahren (Lauf `34116531030`, Zweig `main` bei `94420f7`),
ohne ein Release zu erzeugen. Beide Archive samt Pruefsummen und beide
Signaturdateien stehen im Protokoll. Eine frische Nextcloud 34.0.3 laeuft auf
Port 8097 hinter einem Frontproxy, beide Haelften sind aus diesen Archiven
installiert, fuenf deutsche Dokumente liegen im Nutzerordner, darunter ein Scan
ohne Textschicht.

### Was die Sichtprobe neu belegt, und es ist die Haelfte, die keine CI belegen kann

Die Archive dieses Probelaufs tragen die **echte** Code-Signatur:
`CN=findling`, ausgestellt von der `Nextcloud Code Signing Intermediate
Authority`. Die frische Instanz hat sie gegen die `resources/codesigning/root.crt`
geprueft, die Nextcloud 34.0.3 selbst mitbringt, **ohne dass an dieser Datei
etwas angehaengt wurde**. Befund 2 des amd64-Laufs (Ersatzidentitaet) und
Befund 3 (die verklebte `root.crt`) entfallen damit fuer diesen Lauf. Die
Faelschungsprobe kippt das Urteil weiterhin auf `INVALID_HASH`, und nach dem
Zuruecklegen ist es wieder sauber.

Dazu: alle sechs Deinstallations-Zusagen halten auf der Fahrplan-Instanz
(`SUMMARY: all six uninstall promises hold on this instance`), und der
Zero-Config-Nachweis traf nach **einer** Cron-Runde und 42 s Wanduhr, mit
`occ calls between the installation and the hit: none`.

### Die Gastnutzer-Probe: gefahren und bestanden, mit ihrer Voraussetzung

Vier Vergleiche, alle bestanden, Nextcloud 34.0.3, `guests` 4.9.0. Der erste ist
neu und schliesst DI-06.1-17: der Eigentuemer wird nach seinem eigenen
freigegebenen Marker gefragt, **bevor** die erste Gastfrage gestellt wird, und
die Probe bricht mit Exitcode 2 ab, wenn er ihn nicht findet. Ohne diesen Schritt
konnte die Probe ihre zwei Fehlerarten nicht unterscheiden: eine Instanz mit
stillgelegtem Container hat ebenfalls eine leere Warteschlange, der Vergleich
meldete dann `missing`, und das liest sich als Rechtefehler, wo in Wahrheit die
Probe nicht stattgefunden hatte. `docs/testing.md` fuehrt das Ergebnis statt
"State: outstanding".

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Jedes Office-Dokument meldete sich als beschaedigt, ab genuegend CPU-Kernen**

- **Found during:** Task 1, beim Indexieren der fuenf Dokumente der Sichtprobe
- **Issue:** Auf einer frischen Nextcloud melden DOCX, XLSX (und PPTX) den
  Endzustand `failed`, Grund `corrupt`, Beschriftung "File damaged". Betroffen
  waren nicht nur die beiden Dokumente der Probe, sondern auch
  `Documents/Welcome to Nextcloud Hub.docx`, das Nextcloud selbst mitbringt: das
  Erste, was ein Selfhoster nach der Installation auf der Verwaltungsseite sieht,
  war eine Fehlergruppe ueber sein eigenes Willkommensdokument.
  Die Kette, im laufenden Container gemessen: `office.py` importiert `openpyxl`
  auf Modulebene, `openpyxl.compat.numbers` importiert `numpy` bedingungslos,
  `numpy` laedt OpenBLAS, OpenBLAS startet einen Arbeitsthread je CPU (hier
  zwoelf), jeder braucht einen Stapel im Adressraum, den `_limit_address_space`
  unmittelbar davor per `RLIMIT_AS` auf 512 MB begrenzt hat, `pthread_create`
  scheitert, der Import bricht mitten drin ab, und `dispatch` bildet die
  unbekannte Ausnahme auf `failed(corrupt)` ab. OpenBLAS hat seine eigene Abhilfe
  ins Containerprotokoll geschrieben.
  Warum es fuenf Phasen ueberlebt hat: die Threadzahl folgt der CPU-Zahl. Auf der
  Messbox mit zwei vCPU und auf einem Runner mit vier passen die Stapel unter die
  Grenze. Kein Gate war falsch; die Maschinen waren zu klein, um die Grenze zu
  erreichen.
- **Fix:** `_pin_native_thread_pools()` in `backend/src/findling/extract/sandbox.py`
  setzt `OPENBLAS_NUM_THREADS`, `OMP_NUM_THREADS`, `MKL_NUM_THREADS` und
  `NUMEXPR_NUM_THREADS` auf 1, nach der Adressraumgrenze und vor dem Import des
  Dispatchers. Drei Tests halten ihn: die Variablen, die Reihenfolge, und ein
  DOCX ueber den echten Kindprozess. Der dritte nennt seine eigene Grenze im
  Text, weil er nur auf einer Maschine mit genuegend Kernen eine Ratsche ist.
  In drei Stufen im laufenden Container nachgestellt und mit der gesetzten
  Variablen aufgeloest.
- **Files modified:** `backend/src/findling/extract/sandbox.py`, `backend/tests/test_sandbox.py`
- **Commit:** debb395

**2. [Rule 1 - Bug] Das Bildgate meldete einen git-ignorierten Fremdling als Befund**

- **Found during:** Task 1, beim ersten Lauf von `test_store_metadata.py`
- **Issue:** `media_files()` lief ueber jede Datei in `store/media` und verlangte,
  dass die README sie nennt. Das Worktree-Werkzeug dieses Projekts legte dort eine
  git-ignorierte `.claude-active` ab, und das Gate wurde rot. Ein Gate, das ueber
  das Arbeitsverzeichnis einer Maschine berichtet statt ueber den Inhalt des
  Repositories, faellt beim naechsten Editor-Swapfile genauso um.
- **Fix:** `media_files()` ueberspringt Dotfiles. Der Schutzwert bleibt
  vollstaendig: die Adressen beider `info.xml` nennen `.png`-Dateien, und nichts
  Verstecktes kann ein Store-Screenshot sein. Die Begruendung steht in der
  Docstring.
- **Files modified:** `backend/tests/test_store_metadata.py`
- **Commit:** 71cfee7

**3. [Rule 3 - Blocking] Der Diagnose-Test suchte eine Routenform, die es nicht mehr gibt**

- **Found during:** Task 1, direkt nach der Umstellung der Routen
- **Issue:** `test_diagnose_endpoint.py` schnitt die `info.xml` an
  `<url>diagnose</url>` auf und lief nach der Umstellung in einen `ValueError`.
- **Fix:** Auf das Literal umgestellt, das die Datei wirklich traegt, mit dem
  Grund daneben. Ausdruecklich kein Rueckfall auf den nackten Namen: eine Suche,
  die beides annimmt, laeuft still weiter ueber einen Eintrag, den es nicht gibt.
- **Files modified:** `backend/tests/test_diagnose_endpoint.py`
- **Commit:** 71cfee7

**4. [Rule 2 - Missing] Die Gastnutzer-Probe prueft jetzt ihre eigene Voraussetzung (DI-06.1-17)**

- **Found during:** Task 1, vor dem Lauf der Probe, aus der Uebergabe des Orchestrators
- **Issue:** siehe oben. Die Probe konnte "der Gast sieht zu wenig" nicht von "die
  Probe konnte nicht stattfinden" unterscheiden.
- **Fix:** Vorab-Vergleich des Eigentuemers ueber den freigegebenen Marker, mit
  eigenem Exitcode 2 und eigener Meldung, vor der ersten Gastfrage.
- **Files modified:** `scripts/dev/guest_parity.sh`, `docs/testing.md`
- **Commit:** 9b2efdb

**5. [Rule 1 - Bug] Der Tabellenleser der Deinstallations-Zusagen war tot und log dabei**

- **Found during:** Task 1, an Zusage 4 des Fahrplans
- **Issue:** Der erste `--db-tables-cmd`-Helfer rief
  `OC\Server::getDatabaseConnection()`, das es auf Nextcloud 34 nicht mehr gibt.
  Ein Helfer, der mit einem Fatal Error stirbt, gibt nichts aus, und der
  Installationscheck liest das als "keine Tabellen" und meldet einen Befund ueber
  die App. Ein toter Helfer sieht aus wie eine kaputte Installation.
- **Fix:** Verbindung ueber `\OCP\Server::get(\OCP\IDBConnection::class)`, Abfrage
  gegen `sqlite_master` statt gegen die deklarierte Migrationsschema-Auskunft.
  Danach meldeten alle drei Tabellen sich und die Zusagen 4 bis 6 liefen durch.
  Der Helfer liegt unter `.dev/` und wird nicht ausgeliefert.
- **Files modified:** `.dev/sichtprobe/stack/tables.php` (nicht im Repository)
- **Commit:** kein Repository-Commit, dokumentiert in `docs/install-check.md`

### Dokumentierte Abweichungen zum Plan

**A. Die beiden `info.xml` und ihre Nachbardateien stehen nicht in `files_modified`**

Aus 06.1-17 an diesen Plan uebergeben, vom Orchestrator ausdruecklich angeordnet:
die Lizenz `agpl` wird `AGPL-3.0-or-later` in **beiden** `info.xml` im Lockstep
(DI-06.1-05), und die fuenf Routen-URLs des Backends bekommen die kanonische Form
`^/<name>$` (DI-06.1-13). Beide Dateien und die vier Nachbardateien
(`test_store_metadata.py`, `test_diagnose_endpoint.py`, `docs/certificates.md`,
`test_sandbox.py`, `sandbox.py`, `guest_parity.sh`) stehen nicht in
`files_modified` dieses Plans. Lokal gemessen: `validate_info_xml.sh` laesst beide
Haelften durch den Store-Weg (Transform, dann Schema, am Pin `eda850ba`), und die
volle Backend-Suite ist gruen (1671 bestanden, 13 uebersprungen). Was lokal nicht
messbar ist und deshalb benannt wird: dass die verankerte Routenform an einer
echten HaRP greift. Der Beweisort ist der `deploy-harp`-Job des CI-Laufs nach dem
Merge.

**B. Die Planliste nennt 23 Plaene und nicht die 19, die der Plan nennt**

Die Zahl 19 stammt aus dem Planungsstand vom 06.09.; die Plaene 20 bis 23 sind
danach aus Befunden entstanden und standen in der ROADMAP nicht. Sie sind
eingetragen, mit einem Absatz darunter, der die Abweichung benennt. Der
Fortschrittszaehler `22/23` ist unberuehrt geblieben, er gehoert dem
Orchestrator.

**C. Der Haken an 06.1-19 ist nicht gesetzt**

Der Plan sah "abgehakt" vor. Der Plan ist am Checkpoint und nicht fertig, also
bleibt der Haken offen. Er gehoert ohnehin dem Orchestrator.

**D. Kein Abbau der Box, kein AWS-Kontakt (Betreiberentscheid 07.09.)**

Der Plan sah den Abbau mit Freigabe und Nichtexistenz-Pruefung vor. Der Betreiber
hat am 07.09. anders entschieden: die Box bleibt angehalten bis nach der
v1.1-Messung. `docs/performance.md` fuehrt das als dokumentierte Abweichung mit
Verbleib, Parkkosten (0,3130 USD je Tag), Abbaukriterium und Verweis auf
`box.env`. Der gestoppte Zustand (seit 06:56:42Z) stammt aus der API-Abfrage vom
Morgen; danach ist die Box nicht angefasst worden. Zweiter Grund, der den Abbau
ohnehin verschoben haette: der Index auf dem Datentraeger ist seit dem
Volumenvorfall weg, ein Start faehrt rund 19 h Neuaufbau an.

**E. Zwei Instanzen statt einer, und die Deinstallations-Zusagen nur auf der ersten**

Der Plan sagt "`aio_install_check.sh` gegen diese Instanz fahren" und "die Instanz
bleibt laufen, bis der Owner sie gesehen hat". Beides zusammen geht nicht: Zusage
5 des Fahrplans entfernt die App mit Absicht, und Zusage 6 laesst einen Container
ohne Companion stehen. Am Ende eines vollstaendigen Fahrplans ist nichts mehr
installiert. Aufgeloest mit zwei Instanzen, jede aus einem leeren Docker-Zustand:
die erste traegt den vollstaendigen Fahrplan samt aller sechs Zusagen, die zweite
nur die Schritte 2 und 3 und ist die, die der Owner sieht. Eine abgerissene und
wieder aufgebaute Instanz ist nicht die Instanz, nach der D-H5 fragt.

**F. Port 8097 statt 8090**

8090 traegt den Alltagsstack dieser Maschine (`findling-nextcloud`, seit vier
Tagen), 8080 gehoert einer anderen Sitzung. Beide sind unberuehrt. 8097 ist
derselbe Port, den der amd64-Lauf am 06.09. aus demselben Grund benutzt hat.

**G. Der Kommentar zur Lizenz in `test_store_metadata.py` ist nachgezogen**

Der Plan verlangt `test_store_metadata.py` unveraendert. Die Zusicherungen und die
Netzfreiheit sind unveraendert; geaendert ist ein Kommentarabsatz, der behauptete,
dieser Eintrag liefere `agpl` aus. Nach der Umstellung waere das eine falsche
Aussage in einem Gate gewesen.

## Authentication Gates

Keine. `gh` war angemeldet, Docker lief, die Registry antwortete anonym.

## Verification

| Zusicherung des Plans | Stand |
|---|---|
| Fuenf Bildadressen mit Status, Inhaltstyp, Groesse | erfuellt, dazu PNG-Signatur, Masse und SHA-256 |
| `test_store_metadata.py` ohne Netz | erfuellt, Zusicherungen unveraendert |
| tolerate-failure-Schalter entschieden | erfuellt, bleibt stehen mit Begruendung und Wiedervorlage 16.09. |
| `deploy-harp.yml` gruen | erfuellt fuer den Codestand, Lauf 34114937751, alle drei Zeilen `success`; der Lauf ueber die Aenderungen dieses Plans folgt nach dem Merge |
| Kriterium 3 nach E-H5, Verweis, alte Fassung erkennbar | erfuellt |
| Planliste vollstaendig | erfuellt, 23 Plaene |
| Probelauf ohne Release, beide Pruefsummen | erfuellt, Lauf 34116531030 |
| Frische Instanz, beide Haelften aus den Archiven, 8080 unberuehrt | erfuellt, Port 8097 statt 8090 (Abweichung F) |
| Gastnutzer-Probe gefahren, Ergebnis in `docs/testing.md` | erfuellt, vier Vergleiche bestanden |
| Kein Em-Dash und kein En-Dash in den geaenderten Dateien | geprueft, keiner |
| Owner hat abgenommen | **offen, das ist der Checkpoint** |
| APPSTORE_TOKEN rotiert | erfuellt, 07.09.2026 11:01:21Z, an `gh secret list` nachgeprueft |
| Box abgebaut | **nicht ausgefuehrt, ersetzt durch Betreiberentscheid D** |

Python-Gates lokal gruen vor jedem Commit: `ruff check`, `ruff format --check`,
`pyright` (0 Fehler), `vulture --min-confidence 80`, und die volle Suite mit 1671
bestandenen Tests.

## Known Stubs

Keine.

## Threat Flags

Keine neue Angriffsflaeche. Die Routenumstellung verkleinert die unsignierte
Flaeche nicht und vergroessert sie nicht: sie bringt den erklaerten Block zur
Deckung mit dem, was er nennt, und der Weg war vorher fail closed.

## Deferred Items (neu, nur hier)

- **DI-06.1-30 (Plan 06.1-19): Der Fix des Befundes 8 ist noch nicht im veroeffentlichten Abbild.**
  `_pin_native_thread_pools()` liegt im Quellcode und ist im laufenden Container
  bewiesen, aber `ghcr.io/street1983nk/findling_backend:dev` entsteht in
  `docker.yml` beim Push auf `main`. Bis dahin melden Office-Dokumente auf jeder
  Installation aus dem heutigen Abbild "File damaged", sobald die Maschine genug
  Kerne hat. Wohin: der CI-Lauf nach dem Merge, danach eine Wiederholung der
  Suchproben 1 und 4 auf der Instanz der Sichtprobe.
- **DI-06.1-31 (Plan 06.1-19): Kein Integrationsjob fuehrt ein Office-Dokument ueber den bewachten Pfad auf einer Maschine mit vielen Kernen.**
  Das ist die Luecke, durch die Befund 8 fuenf Phasen gefallen ist. Der
  Referenzkorpus ist ueberwiegend PDF, und die Runner sind zu klein, um die
  Adressraumgrenze zu erreichen. Denkbare Schliessform: im Kindprozess die
  wirksame CPU-Zahl fuer den Threadpool erzwingen und den Fall zusaetzlich mit
  einem kuenstlich hochgesetzten `nproc` oder einem herabgesetzten `RLIMIT_AS`
  nachstellen, damit die Grenze auf jedem Runner erreichbar ist. Wohin: eigener
  Folgeplan.
- **DI-06.1-32 (Plan 06.1-19): `docs/dev-setup.md`, Abschnitt "Der Store-Installationsweg lokal", fuehrt weiter ohne Frontproxy.**
  Bekannt als DI-06.1-14 und dort als erledigt vermerkt; der Abschnitt beschreibt
  aber weiterhin `http://harp:8780` als `nextcloud_url` und keinen Frontproxy. Die
  Sichtprobe brauchte die Topologie mit Frontproxy, wie Befund 4 sie beschreibt.
  Wohin: Doku-Nachzug, klein.
- **DI-06.1-33 (Plan 06.1-19): Die Deckung meldet `indexable 0` waehrend des ersten Laufs, und das Protokoll druckt dann `unknown`.**
  Im Zero-Config-Nachweis stand `indexed 41 of 0 indexable`. Die Zahl ist im
  Endzustand richtig (85 von 100), waehrend des Laufs aber unbrauchbar, und ein
  Selfhoster liest in dieser Zeit einen Deckungsgrad, den die Seite nicht kennt.
  Wohin: klein, mit der naechsten Arbeit an der Verwaltungsseite.

## Self-Check: PASSED

Alle 15 genannten Dateien liegen auf der Platte, alle sieben Commits liegen im
Log dieses Worktrees. Nichts fehlt.

| Geprueft | Ergebnis |
|---|---|
| 15 Dateien (beide `info.xml`, vier Testdateien, `sandbox.py`, `guest_parity.sh`, `deploy-harp.yml`, ROADMAP, `store/media/README.md`, vier Doku-Dateien, diese Zusammenfassung) | 15 gefunden, 0 fehlend |
| 7 Commits (71cfee7, 2739a0b, 8f124c1, 11ec5aa, debb395, 9b2efdb, 720f4bc) | 7 gefunden, 0 fehlend |
| Loeschungen gegenueber 94420f7 | keine |
| Unversionierte Reste im Arbeitsbaum | keine ausserhalb von `.dev/`, das git-ignoriert ist |
