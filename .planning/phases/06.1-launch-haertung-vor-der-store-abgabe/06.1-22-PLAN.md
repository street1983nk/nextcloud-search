---
phase: 06.1-launch-haertung-vor-der-store-abgabe
plan: 22
type: execute
wave: 6
depends_on: []
files_modified:
  - docs/dev-setup.md
  - docs/ocr.md
  - docs/performance.md
  - scripts/dev/build_load_corpus.py
  - scripts/release/store-archive.sh
  - scripts/dev/guest_parity.sh
  - scripts/dev/aio_install_check.sh
  - backend/tests/test_guest_parity.py
autonomous: true
gap_closure: true
requirements: [PKG-05]
must_haves:
  truths:
    - "Keine normative Stelle im Repo nennt den Korpus mehr mit 33 Dateien; wer die Zahl braucht, wird auf testdata/CORPUS.md verwiesen oder liest 39"
    - "Ein auf einem CRLF-Arbeitsbaum gebautes Release-Paket bricht ab statt still ein byteweise anderes Paket zu erzeugen"
    - "Die beiden Dev-Skripte nehmen Passwoerter aus der Umgebung; ein Passwort in der Argumentliste wird abgelehnt statt in der Prozessliste zu landen"
    - "docs/dev-setup.md fuehrt beim lokalen Store-Weg nicht mehr in die haengende Registrierung"
---

## Ziel

Vier kleine offene Befunde schliessen, waehrend die Checkpoints 06.1-18/19 auf
den Owner warten. Kein Kontakt zur AWS-Box, keine Aenderung an info.xml
(agpl-Schreibweise und Routen-Form gehoeren zu 06.1-19), keine Aenderung an
Workflows.

## Tasks

### Task 1: DI-06.1-07 Rest, die Korpuszahl 33 in Prosa

Vier Stellen, alle verifiziert am 07.09.:
- docs/dev-setup.md Z. 238 ("33 files, the four image types ...")
- docs/ocr.md Z. 294 ("33 Dateien, davon 19 PDFs")
- docs/performance.md Z. 2223 ("Korpus von 33 Dateien")
- scripts/dev/build_load_corpus.py Z. 4, 11, 1167 (Docstrings/Kommentare)

Regel: NORMATIVE Aussagen (was der Korpus ist) verweisen auf testdata/CORPUS.md
statt eine Zahl zu fixieren, oder nennen 39 Dateien / 24 PDFs, wenn der Satz
ohne Zahl nicht traegt. HISTORISCHE Messaussagen (docs/performance.md
beschreibt einen konkreten Lauf; build_load_corpus.py Z. 1167 begruendet eine
damalige Beobachtung) bleiben wahr, wie sie sind, und bekommen hoechstens einen
klarstellenden Zusatz ("der Referenzkorpus von damals, 33 Dateien; heute 39,
siehe testdata/CORPUS.md"). Nichts umluegen. Z. 500/1068/1230 ("thirty pages")
meinen Seiten, nicht Dateien: NICHT anfassen.

Verify: grep ueber Repo (ohne .planning, ohne testdata/CORPUS.md-Historie)
findet keine normative 33-Dateien-Aussage mehr; die vier Stellen lesen sich
schluessig.

### Task 2: DI-06.1-15, CRLF-Wache im Packweg

scripts/release/store-archive.sh: im stage- oder pack-Schritt eine Pruefung,
die bei CR-Bytes in gestagten Textdateien (php, xml, js, css, md, sh, py) hart
abbricht und den Grund nennt (byteweise anderes Paket als aus release.yml).
Binaerdateien (onnx, Bilder, Fonts) ausnehmen. bash -n und sh -n sauber.

Verify: eine Probedatei mit CRLF in ein Stage-Verzeichnis gelegt laesst den
Schritt mit klarer Meldung scheitern; ohne CR laeuft er durch.

### Task 3: DI-06.1-18, Passwoerter raus aus der Argumentliste

scripts/dev/guest_parity.sh und scripts/dev/aio_install_check.sh: Passwoerter
kommen aus Umgebungsvariablen (z.B. FINDLING_GUEST_PASS, FINDLING_ADMIN_PASS;
Namen an die vorhandene Konvention der Skripte anpassen). Wird ein Passwort
als Argument uebergeben, bricht das Skript mit einer Meldung ab, die die
Umgebungsvariable nennt (Ablehnen statt stilles Weiterreichen, Schliessform
aus dem Security-Audit). --help nachziehen. backend/tests/test_guest_parity.py
haelt die neue Hausordnung fest (Argument-Ablehnung rotfaehig belegen);
pruefe, ob bestehende Zusicherungen die alte Signatur festschreiben, und ziehe
sie mit. Der Aufrufweg in .github/workflows/*.yml darf sich NICHT aendern
muessen; falls ein Workflow die Skripte mit Passwort-Argument ruft, stoppe und
schreibe den Befund in die SUMMARY statt den Workflow anzufassen.

Verify: Skriptaufruf mit Passwort-Argument = Exit != 0 mit Meldung; mit
Umgebungsvariable laeuft die Argumentpruefung durch; pytest
tests/test_guest_parity.py und tests/test_ops_scripts.py gruen; volle Gates.

### Task 4: DI-06.1-14, der lokale Store-Weg in docs/dev-setup.md

Abschnitt "Der Store-Installationsweg lokal": die compose-Topologie so
beschreiben, wie deploy-harp.yml sie seit 03.09. loest (nextcloud_url muss aus
Sicht des Containers UND aus Sicht des Servers aufloesbar sein; ohne
Frontproxy haengt PUT /ocs/v1.php/apps/app_api/ex-app/status auf 404). Den
Fallstrick ausdruecklich benennen, damit niemand den Fehler bei der App sucht.
Keine neuen Zusagen, nur die funktionierende Topologie.

Verify: der Abschnitt nennt die Zwei-Richtungen-Bedingung und den 404-Fallstrick;
kein Em-Dash, LF, Umlaute nur in deutscher Prosa.

## Abnahme

- Jeder Task ein Commit (docs/fix/test-Prefixe)
- SUMMARY.md nennt je Task, welches DI geschlossen ist; neue Befunde ab
  DI-06.1-19 NUR in der SUMMARY
- Volle lokale Gates (ruff, ruff format --check, pyright, vulture, pytest)
