---
phase: 15-messphase-eine-box-anfahrt
plan: 6
type: execute
wave: 5
depends_on: [15-02, 15-05]
files_modified:
  - docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [MESS-05]

must_haves:
  truths:
    - "Der Abbildwechsel hat ein Werkzeug, das per Digest zieht und den Baumhash als Beweis liest"
    - "Die Abhaengigkeitskette PHP-Haelfte, Registrierung, harte Grenze ist im Werkzeug erzwungen und nicht nur beschrieben"
    - "Kein --rm-data ohne vorherige Zaehlung der laufenden Nextcloud-Instanzen"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh"
      provides: "Der Abbildwechsel auf den v1.2-Stand, Rueckgabewerte 2, 36, 37, 38, 39"
      contains: "baumhash-gleich"
    - path: "backend/tests/test_measurement_scripts.py"
      provides: "Boxlose Tests des Abbildwechsels"
      contains: "92b-wechsel"
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh"
      to: "docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.sh"
      via: "der Baumhash als Identitaetsbeweis des Abbilds"
      pattern: "40b-baumhash"
---

<objective>
Der Snapshot traegt die lokale Registry mit dem Abbild vom 10.09.2026. Ohne
Abbildwechsel misst der Volllauf einen Container ohne Top-up-Route, ohne die
Filter aus Phase 13 und ohne den Entladeschalter aus Phase 14. Dieser Plan baut
das Werkzeug des Wechsels.

Purpose: Erfolgskriterium 2 verlangt den Volllauf "mit Top-up-Fix",
Erfolgskriterium 5 verlangt A/B ueber einen Schalter, den das alte Abbild gar
nicht kennt. Der Wechsel findet **einmal** statt, vor der ersten Messung (D-04),
und er ist genau der Vorgang, der `baumhash-gleich ja` herstellt.

Output: `92b-wechsel.sh`, fuenf Rueckgabewerte, boxlose Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md
@.planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md
@docs/runbook-messbox.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 92b-wechsel.sh, der Abbildwechsel per Digest</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh</files>
  <read_first>
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/92-wechsel.sh vollstaendig (die vier Aenderungen gegen 61-wechsel.sh, die Abhaengigkeitskette, die vier Exit-Codes, der Umgang mit der info.xml-Kopie ausserhalb des Arbeitsbaums)
    - docs/runbook-messbox.md, Block 13b aus Plan 15-02 und Block 8, 12, 13 (Registry aus dem Snapshot, harte Grenze, Zaehlung der Nextcloud-Instanzen)
    - docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.sh (der Aufrufvertrag des Baumhash-Beweises, der unveraendert bleibt)
    - backend/appinfo/info.xml (der Abschnitt `external-app`, der Bildtag und die Umgebungsvariablen, die bei jeder Registrierung neu gesetzt werden muessen)
  </read_first>
  <action>
Baue `docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh` als
**Nachfolgefassung** von `92-wechsel.sh`. Die Vorgaengerin wird nicht editiert
und nicht kopiert: sie ist eine gefahrene Fassung und bleibt, was sie ist.

Vier Unterschiede gegen die Vorgaengerin, jeder mit seinem Begruendungsabsatz im
Kopfkommentar:

1. **Das Abbild kommt per Digest** und nicht ueber den wandernden Zeiger `:dev`
   (D-04). Variable `ABBILD_DIGEST` ohne Vorgabewert: fehlt sie, endet das
   Werkzeug mit `exit 2` und der Benutzung auf stderr. Der Digest wird nach dem
   Pull zurueckgelesen (`docker image inspect --format '{{index .RepoDigests 0}}'`)
   und als Zeile `abbild-digest-ist` protokolliert. Der Digest ist die Notiz,
   der Baumhash ist der Beweis (Befund L-05 der Phase 11).
2. **Der Baumhash entscheidet.** `40b-baumhash.sh` wird aufgerufen, seine
   Rohdatei zurueckgelesen, und es muss `baumhash-gleich ja` dastehen, mit drei
   verankerten `baumhash:`-Zeilen. Alles andere endet mit **36**. Der Aufruf
   geht ueber das Skript und wird nicht als Heredoc nachgebaut.
3. **Die Abhaengigkeitskette ist erzwungen und nicht beschrieben**: erst die
   PHP-Haelfte in ein Verzeichnis, das `findling` heissen **muss** (mit jedem
   anderen Namen findet der Klassenlader nichts und es gibt nirgends eine
   Fehlermeldung), dann die Registrierung, dann die harte Grenze **aus der
   cgroup** gelesen mit 2147483648 in `memory.max` und `memory.swap.max`.
   Greift die Grenze nicht: **39**. Unmittelbar daneben wird die Stellung von
   `FINDLING_EMBED_IDLE_RELEASE_SECONDS` neu abgelesen und als
   `entladeschalter-ist` protokolliert, weil sie bei jeder Registrierung
   genauso verloren geht wie die Grenze.
4. **`unregister --rm-data` nur vor dem Indexaufbau und nie danach**, und nie
   ohne die Zaehlung der laufenden Nextcloud-Instanzen unmittelbar davor.
   Liefert die Zaehlung mehr als 1: **37**, und es wird nichts weiter getan. Am
   07.09.2026 hat eine zweite Nextcloud am selben Docker-Dienst das Messvolumen
   der ersten geloescht, weil der Volumenname einer ExApp allein aus ihrer
   App-Kennung folgt.

Dazu unveraendert aus der Vorgaengerin: die Kopie der `info.xml` entsteht
**ausserhalb** des Arbeitsbaums, und danach muss
`git status --porcelain backend/appinfo/info.xml` leer sein, sonst **38**. Die
Kopie bleibt als Rohdatei `$OUT/92b-info-box.xml` liegen, damit der Bericht
zeigen kann, womit registriert wurde.

Rohdatei `$OUT/92b-wechsel.txt`, Messblock in `{ ... } 2>&1 | tee "$ZIEL"`,
alle Abbrueche darunter, aus `$WORK` gelesen.

Der Kopfkommentar nennt ausserdem die Aufloesung des Widerspruchs: gewechselt
wird einmal, **vor** der ersten Messung, danach gilt `baumhash-gleich` wieder
als Haltebedingung des Laufs. Ein Abbildwechsel waehrend der Messreihe ist kein
Nachtrag, sondern ein zweiter Messgegenstand.

Keine Em-Dashes, keine Umlaute im Skript, kein Passwort in einem Argument, kein
Pfad einer Maschine ausserhalb eines kommentierten Vorgabewerts.
  </action>
  <verify>
    <automated>sh -n docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh && cd backend && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `sh -n` ist fehlerfrei.
    - Die Datei traegt `exit 2`, `exit 36`, `exit 37`, `exit 38`, `exit 39`, alle hinter der `tee`-Pipeline.
    - Die Datei ruft `40b-baumhash.sh` als Skript auf und baut seine Rechnung nicht als Heredoc nach.
    - Die Datei verlangt `ABBILD_DIGEST` ohne Vorgabewert und protokolliert `abbild-digest-ist`.
    - Die Datei liest die harte Grenze aus `/sys/fs/cgroup/memory.max` und `memory.swap.max` und erwartet `2147483648`.
    - Jedes `--rm-data` im Skript hat die Zaehlung der Nextcloud-Instanzen davor, in derselben Ausfuehrungsreihenfolge.
    - `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/92-wechsel.sh` ist unveraendert.
  </acceptance_criteria>
  <done>Der Abbildwechsel ist ein Werkzeug mit Abbruchpfaden und keine Absatzfolge, die jemand auf der Box aus dem Kopf nachbaut.</done>
</task>

<task type="auto">
  <name>Task 2: Boxlose Tests des Abbildwechsels</name>
  <files>backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/tests/test_measurement_scripts.py Zeilen 680 bis 745 (`RUNNING_IMAGES_OF_THE_BOX` und die drei Faelle zur Zaehlung der Nextcloud-Instanzen, die bereits fuer die Vorgaengerin existieren)
    - backend/tests/test_measurement_scripts.py Zeilen 1350 bis 1436 (`a_boxless_run`)
    - docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh (die eigene Datei aus Task 1)
  </read_first>
  <action>
Neue Konstante `V12_IMAGE_SWITCH = V12_RUN_DIR / "92b-wechsel.sh"`.

Faelle:

1. `test_the_image_switch_refuses_a_run_without_a_digest`, parametrisiert ueber
   `None` und `""` fuer `ABBILD_DIGEST`: Rueckgabewert 2, Benutzung auf stderr
   mit dem Variablennamen, `stdout` leer, `tmp_path` leer. Der Docstring nennt
   den Grund: `:dev` ist ein wandernder Zeiger, und der Pfadfilter von
   `docker.yml` greift nach `backend/**`, also verschiebt schon eine neue
   Testdatei den Digest.
2. `test_the_image_switch_counts_the_nextclouds_before_every_rm_data`: liest die
   Datei ohne Kommentarzeilen, findet jede Zeile mit `--rm-data` und verlangt,
   dass oberhalb davon die Zaehlung steht. Der Fall wuerde rot, wenn jemand ein
   zweites `--rm-data` ohne Zaehlung ergaenzt, und der Docstring nennt den
   Schaden vom 07.09.2026 als Grund.
3. `test_the_image_switch_reads_the_hard_limit_out_of_the_cgroup`: die Datei
   nennt `/sys/fs/cgroup/memory.max`, `memory.swap.max` und `2147483648` und
   liest die Grenze nicht ueber den Docker-Klienten.
4. `test_the_image_switch_calls_the_tree_hash_as_a_script`: die Datei nennt
   `40b-baumhash.sh` und traegt keine eigene Hashrechnung (kein `sha256sum` in
   einer Schleife ueber den Arbeitsbaum).
5. `test_the_driven_image_switch_of_the_predecessor_stays_byte_identical`: die
   Vorgaengerin `92-wechsel.sh` behaelt ihren sha256. Der Sollwert wird beim
   Schreiben des Tests aus der Datei abgelesen und als Modulkonstante
   festgehalten, nach dem Muster der bestehenden Waechter.
  </action>
  <verify>
    <automated>cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest tests/test_measurement_scripts.py -k "image_switch" -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_measurement_scripts.py -k "image_switch" -q` waehlt mindestens sechs Faelle aus.
    - Der `--rm-data`-Fall wuerde an einer gestellten Probe rot, in der die Zaehlung fehlt.
    - Der sha256-Sollwert der Vorgaengerin steht als Modulkonstante in der Datei.
    - Volle Suite gruen, Skipzahl unveraendert.
  </acceptance_criteria>
  <done>Die gefaehrlichste Zeile der ganzen Anfahrt, `--rm-data`, kann nicht mehr ohne ihre Zaehlung im Skript stehen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ghcr.io zu Box | Das Abbild kommt von aussen in die Messinstanz |
| Docker-Dienst der Box | Ein Volumenname folgt allein aus der App-Kennung, also teilen sich zwei Instanzen einen Namensraum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Tampering | Ein wanderndes `:dev` zieht ein anderes Abbild, als der Bericht nennt | mitigate | Pull per Digest, Digest zurueckgelesen, Baumhash als Beweis, Abbruch 36 |
| T-15-16 | Denial of Service | `--rm-data` loescht das Messvolumen einer zweiten Instanz | mitigate | Zaehlung unmittelbar vor jedem `--rm-data`, Abbruch 37, Gate ueber die Reihenfolge im Quelltext |
| T-15-17 | Repudiation | Messung gegen 4 GB, waehrend 2 GB gemeint waren | mitigate | Harte Grenze aus der cgroup zurueckgelesen, Abbruch 39 |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Abhaengigkeit; `onnxruntime==1.30.0` bleibt eingefroren, damit Speicher- und Ladezahlen vergleichbar bleiben |
</threat_model>

<verification>
1. `sh -n` fehlerfrei, fuenf Abbrueche unterhalb der Pipeline.
2. Die Vorgaengerin `92-wechsel.sh` ist byteweise unveraendert.
3. `backend/appinfo/info.xml` ist unveraendert (`git status --porcelain backend/appinfo/info.xml` leer).
4. Volle Suite aus `backend/` gruen, Skipzahl unveraendert.
</verification>

<success_criteria>
Die erste und teuerste Luecke der Recherche ist geschlossen: die Box misst den
v1.2-Stand, und dass sie es tut, haengt an einem Baumhash und nicht an einer
Erinnerung.
</success_criteria>

<output>
Create `.planning/phases/15-messphase-eine-box-anfahrt/15-06-SUMMARY.md` when done
</output>
