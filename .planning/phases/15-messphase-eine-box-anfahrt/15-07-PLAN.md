---
phase: 15-messphase-eine-box-anfahrt
plan: 7
type: execute
wave: 6
depends_on: [15-01, 15-02, 15-03, 15-04, 15-05, 15-06]
files_modified:
  - docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md
  - docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt
autonomous: true
requirements: [MESS-05]

must_haves:
  truths:
    - "Die Erwartung dieses Laufs steht mit Zahlen da, bevor die Box existiert, und der Commit-Zeitstempel belegt es"
    - "Die Ablaufdatei beschreibt alle zehn Messschritte und nicht nur die fuenf aus Phase 12"
    - "Kalt wird hergestellt und nicht bewahrt, und dieser Satz steht in der Ablaufdatei"
    - "Das Belegkriterium fuer den 900-s-Vorschlagswert steht VOR dem Lauf fest"
    - "Acht der neun Vorbedingungen sind abgearbeitet und protokolliert; die neunte ist der Owner-Checkpoint"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md"
      provides: "Schrittfolge, Erwartungen E1 bis E14, Abbruchkatalog 15 bis 39"
      contains: "kalt wird hergestellt"
    - path: "docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt"
      provides: "Die abgearbeiteten Vorbedingungen mit ihren tatsaechlichen Ausgaben"
      contains: "CI_LAUF"
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md"
      to: "docs/runbook-messbox.md Abschnitt 7"
      via: "dieselben Schrittnummern, dieselben Werkzeuge, dieselben Rueckgabewerte"
      pattern: "95b-wiederaufwaermen"
---

<objective>
Zwei Dinge, die beide vor der Box passieren muessen: die Ablaufdatei des Laufs
wird auf zehn Schritte fortgeschrieben, mit den Erwartungen E8 bis E14 **in
Zahlen und vorher**, und die neun Vorbedingungen aus Abschnitt 3 des Runbooks
werden abgearbeitet und protokolliert.

Purpose: "Die Erwartung steht weiter unten mit Zahlen da, damit sie nach der
Messung nicht zur Erklaerung des Ergebnisses werden kann" (`00-ablauf.md`, Kopf).
Der Beweis dafuer ist der Commit-Zeitstempel, also muss dieser Plan vor der
Anfahrt committet sein. Und jede Vorbedingung, die auf spaeter verschoben wird,
ist in die bezahlte Zeit verschoben.

Output: eine fortgeschriebene Ablaufdatei, eine Rohdatei mit acht abgearbeiteten
Vorbedingungen, eine offene Owner-Zeile fuer die neunte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md
@.planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md
@docs/runbook-messbox.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 00-ablauf.md auf zehn Schritte, mit den Erwartungen E8 bis E14</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md vollstaendig (Aufbau, Tonlage, die fuenf Schritte, E1 bis E7, der Abbruchkatalog 15 bis 28, der Satz zur Schreibweise)
    - docs/runbook-messbox.md Abschnitt 7 in der Fassung aus Plan 15-02 (die zehn Schritte, 6b und 8b, die Rueckgabewerte bis 39)
    - docs/measurements/2026-09-entladung-vorpruefung/skripte/00-ablauf.md (Praezedenz: eine Erwartung mit Zahlen, die vor den Rohdaten committet wurde)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md (D-02, D-04, D-05)
  </read_first>
  <action>
Schreibe `00-ablauf.md` fort. Die vorhandenen Abschnitte 1 bis 5 und die
Erwartungen E1 bis E7 bleiben **unveraendert im Wortlaut**: sie sind vor Phase 12
geschrieben worden und ihre Unveraendertheit ist Teil ihres Werts. Ergaenzt wird,
nicht umgeschrieben.

**Neuer Vorspann-Absatz.** Die zwei Saetze im Kopf ("Dieser Lauf beweist zwei
Messwerkzeuge und eine Messbedingung, und sonst nichts") stimmen fuer den v12-Lauf
in seiner urspruenglichen Fassung. Sie bekommen einen datierten Nachtrag vom
Planungstag: dieser Lauf ist seit der Planung der Phase 15 die Anfahrt selbst,
also beweist er zusaetzlich die vier offenen Messauftraege des Milestones. Der
alte Satz wird nicht geloescht (Muster 14-11: datierter Nachtrag statt Loeschung).

**Abschnitt 2 wird auf zehn Schritte fortgeschrieben.** Je Zeile: Nummer,
Schritt, Werkzeug, Rohdatei, die Aussage, an der der Schritt haengt. Die
Nummerierung folgt der des Runbooks, damit beide Dateien im Gleichschritt
stehen:

| Nr | Schritt | Werkzeug | Rohdatei |
|---|---|---|---|
| 0 | Abbildwechsel auf den v1.2-Stand | `92b-wechsel.sh` | `92b-wechsel.txt`, `40b-baumhash.txt` |
| 1 | Zustandspruefung und Nullstandsbeleg | `93-nullstand.sh`, `occ findling:index --restart -n` | `04-bestand-vor-der-messung.txt`, `93-nullstand.txt` |
| 2 | Cron-Konfigurationszweig | `97-cron-vorpruefung.sh vorher` | `97-cron-vorpruefung-vorher.txt` |
| 3 | Bestandsvorlauf der Sonde | Abschnitt 0 von `98c-sprachfaelle.sh` | `05-sprachfaelle.txt` |
| 4 | Volllauf, detached | `96-volllauf.sh`, `96b-waechter.sh` | `96-volllauf.csv`, `96b-waechter.txt` |
| 5 | Cron-Wirkungszweig | `97-cron-vorpruefung.sh waehrend` | `97-cron-vorpruefung-waehrend.txt` |
| 6 | Laststufen 1, 4, 8, 12, 16 | `95-spitze.sh`, `97-nebenlaeufigkeit.sh` | `95-*.json`, `97-nebenlaeufigkeit.txt` |
| 6b | Filter- und Sortierblock | `99c-filter-sortierung.sh` | `99c-filter-sortierung.txt` |
| 7 | Sprachfaelle mit Abschnitt 3b | `98c-sprachfaelle.sh` | `05-sprachfaelle.txt` |
| 8 | Wiederaufwaerm-A/B, vier Auspraegungen | `95b-wiederaufwaermen.sh` | `95b-wiederaufwaermen-1..4.txt` |
| 8b | MEM-02 | `94b-grundlast-rueckkehr.sh` | `94b-grundlast-rueckkehr.txt` |
| 9 | Endmessungen und Gegenproben | `90-bestand.sh`, Kostenzeilen aus `box.env` | `90-bestand.txt`, `96-vektorbestand.txt`, `93-kosten-und-verbleib.txt` |

**Der eine Satz, der die Reihenfolge traegt**, als eigener Absatz mit der
Ueberschrift "Kalt wird hergestellt, nicht bewahrt": Schritt 3 und Schritt 6
waermen den Container auf, Schritt 8 braucht kalte Auspraegungen. Die
Kaltmessungen laufen deshalb **nach** Schritt 6 und 7, und jede beginnt mit
einem Containerneustart, einer gefahrenen Ruhezeit und danach dem geleerten
Wirtscache (`sync`, dann der Wert `3` nach `/proc/sys/vm/drop_caches`, auf dem
Wirt). Die Nebenwirkung steht daneben: das Leeren verwirft auch den mmap-Cache
des Tantivy-Index, also misst die kalte Suche beide Haelften kalt. Diese Zeile
gehoert in die Ablaufdatei, **bevor** die Box steht, weil sie die Messung
definiert und nicht erklaert.

**Abschnitt 3 bekommt die Erwartungen E8 bis E14**, je mit Zahl und Herkunft:

- **E8, Abbildwechsel:** `baumhash-gleich ja` nach dem Wechsel und ein
  protokollierter Digest. Herkunft: Pitfall 1 der Recherche, Befund L-05.
- **E9, Volllauf:** die Laufzeit liegt **unter** 26 h 37 min. Herkunft: der
  v1.1-Lauf hatte 5,85 h von 26,6 h ohne Arbeitsvorrat, und die Top-up-Route ist
  genau dagegen gebaut. Die Erwartung ist ausdruecklich eine Erwartung und kein
  Planwert: der Deckel rechnet weiter mit 26 h 37 min.
- **E10, Laststufen:** die Stufen 1 und 4 halten das Budget von 2.500 ms, die
  Stufe 8 haelt es mit kleinerer Reserve als 374,5 ms, die Stufen 12 und 16
  reissen es weiterhin. Herkunft: die Reihe aus `docs/audits/2026-09-phase-11/`.
  Dazu der Satz, dass die neuen Zahlen schlechter aussehen **duerfen**, weil der
  Zaehler seit dem 10.09. abgebrochene Aufrufe nicht mehr als beantwortet zaehlt.
- **E11, Filter und Sortierung:** `newest` und `oldest` antworten auf dem
  Vollbestand langsamer als `relevance` nicht um mehr als den Faktor zwei, und
  drei geblaetterte Seiten melden die Seitenzahlen 1, 2, 3. Herkunft: der
  Sortierzweig ist rein lexikalisch ohne Vektorhaelfte (13-02), und der
  Cursor-Fingerabdruck haelt oder er haelt nicht (13-08). Erstmessung, keine
  Vergleichszeile.
- **E12, Wiederaufwaermen:** die erste Suche nach einer Entladung bleibt unter
  1,5 s. Herkunft: auf der Entwicklungsmaschine 1,37 bis 1,44 s gegen warm 0,41
  bis 0,48 s, und die Box ist langsamer. Dazu der Satz, dass ein Reissen hier
  ein **Ergebnis** ist und keine Stoerung: die Marge war schon bei der Abnahme
  der Phase 14 als duenn benannt.
- **E13, MEM-02:** die Rueckkehr zur Grundlast nach einem Indexlauf liegt ueber
  300 MB, und der Bodensatz liegt bei rund 16 MB. Herkunft: 376,3 MB Rueckgabe
  in der Sichtprobe aus 14-12 auf einer Maschine ohne `malloc_trim`, und 17,1 MB
  Zielast ueber fuenf Zyklen im Vorprueflauf auf aarch64.
- **E14, das Belegkriterium fuer den Vorschlagswert 900 s** (D-05): belegt
  werden die **Folgen** einer Frist und nie die Frist selbst. Der Vorschlagswert
  bleibt bei 900 s, wenn beide Bedingungen halten: die Rueckkehr zur Grundlast
  liegt ueber 300 MB (E13) **und** die erste Suche nach einer Entladung bleibt
  unter 1,5 s (E12). Reisst die zweite, wird der Vorschlagswert nicht korrigiert,
  sondern seine Empfehlung bekommt die Zahl der Box daneben und den Satz, fuer
  welche Instanzen er nicht taugt. Reisst die erste, ist die Frist die falsche
  Stellschraube und der Befund gehoert an die Entladung selbst. Die Messung mit
  verkuerzter Ruhezeit (60 bis 120 s, D-02) misst **denselben Mechanismus**; das
  ist die Begruendung, die im Protokoll steht, und die einzige Einschraenkung ist,
  dass sie ueber die Haeufigkeit von Entladungen im Alltag nichts sagt.

**Abschnitt 4 bekommt die neuen Abbruchbedingungen 29 bis 39** in derselben
Tabellenform wie die vorhandenen, je mit Bedingung, Ort und Folge. Der
Schlussabsatz zur `tee`-Pipeline gilt unveraendert weiter und bekommt den Satz,
dass auch die elf neuen Werte darunter stehen.

**Abschnitt 5 bekommt** die Liste der Werkzeuge, fuer die nach dem Lauf ein
Pruefsummen-Waechter nachgezogen wird: `98c-sprachfaelle.sh`,
`97-cron-vorpruefung.sh`, `95b-wiederaufwaermen.sh`,
`94b-grundlast-rueckkehr.sh`, `99c-filter-sortierung.sh`, `92b-wechsel.sh`.

Deutsche Prosa mit echten Umlauten, Ueberschriften ohne Umlaute, keine
Em-Dashes, keine Emojis, kein gesperrtes Wort.
  </action>
  <verify>
    <automated>grep -c "kalt wird hergestellt" -i docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md && grep -c "E14" docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md && python -c "import pathlib,sys; t=pathlib.Path('docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md').read_text(encoding='utf-8'); sys.exit(1 if (chr(8212) in t or chr(8211) in t or ('arch'+'iv') in t.lower()) else 0)" && cd backend && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 2 traegt zwoelf Zeilen (Schritte 0 bis 9 inklusive 6b und 8b), je mit Werkzeug und Rohdatei.
    - Die Erwartungen E1 bis E7 sind byteweise unveraendert; `git diff` zeigt in ihrem Bereich keine Aenderung.
    - E8 bis E14 stehen mit Zahlen da: `baumhash-gleich ja`, 26 h 37 min, 2.500 ms und 374,5 ms, Faktor zwei und Seitenzahlen 1/2/3, 1,5 s, 300 MB und 16 MB.
    - E14 nennt beide Bedingungen des Belegkriteriums und sagt, was bei jedem der beiden Reissen folgt.
    - Der Absatz "Kalt wird hergestellt, nicht bewahrt" nennt `sync`, `drop_caches`, den Wert `3` und die Nebenwirkung auf den mmap-Cache.
    - Abschnitt 4 traegt elf neue Zeilen fuer die Werte 29 bis 39.
    - Kein U+2014, kein U+2013, kein Emoji, kein gesperrtes Wort.
  </acceptance_criteria>
  <done>Die Erwartung dieses Laufs steht mit Zahlen da, bevor die Box existiert, und der Commit dieses Plans ist ihr Zeitstempel.</done>
</task>

<task type="auto">
  <name>Task 2: Die neun Vorbedingungen abarbeiten und protokollieren</name>
  <files>docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt</files>
  <read_first>
    - docs/runbook-messbox.md Abschnitt 3 vollstaendig (die neun Zeilen mit ihrer Pruefung und ihrer Begruendung)
    - docs/measurements/2026-09-v12-messung/rohdaten/01-aws-lesende-proben.txt (die Erwartung der Abschnitte 1 und 2, gegen die die Snapshot- und Tagprobe gehalten wird)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md, Abschnitt "Environment Availability" (was auf dieser Maschine vorhanden ist und was fehlt)
  </read_first>
  <action>
Arbeite die neun Vorbedingungen ab und schreibe ihre **tatsaechlichen Ausgaben**
nach `docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt`.
Jede Zeile nennt Nummer, Vorbedingung, den gefahrenen Befehl, die Ausgabe und
das Urteil `erfuellt` oder `offen`. Kein Befehl wird uebersprungen, weil sein
Ergebnis bekannt scheint.

1. **`CI_LAUF`**: `gh run list --workflow=integration.yml --status success --limit 1`,
   die Laufnummer **neu** holen, damit sie zum gemessenen Stand passt. Die Zahl
   35462611738 aus der Recherche ist der Stand vom 19.09. und wird ersetzt, wenn
   seither ein gruener Lauf dazugekommen ist.
2. **Sicherung der Systemplatte**: Datei vorhanden unter
   `${FINDLING_LOADTEST_DIR:-$HOME/.findling-loadtest}/systemplatte-2026-09/home-ubuntu-work.tar.gz`,
   Groesse und sha256 gegen `07-snapshot-und-abbau.txt` Abschnitt 4. Der Hash
   wandert als Wert in die Rohdatei, der Pfad als Platzhalter.
3. **AWS-Anmeldung**: nur die beiden Variablennamen und die Antwort auf die
   Frage, ob sie gesetzt sind. **Kein Wert, kein Teil eines Werts, keine
   Kontokennung.**
4. **Lesende Snapshot-Probe**:
   `aws ec2 describe-snapshots --region eu-central-1 --snapshot-ids snap-03f1d1d9ad9262704`,
   Erwartung `State completed`, `Progress 100%`, `VolumeSize 60`.
5. **Tag des Snapshots**: `purpose=findling-corpus-keep`, gelesen ueber
   `describe-tags`.
6. **Werkzeuge der Entwicklungsmaschine**: `aws --version`, `gh --version`,
   `python3 -V`, `uv --version`, dazu `jq --version` und `ssh -V`. Fuer `dig`
   wird der Fehlstand protokolliert und der Rueckfall benannt (`nslookup` oder
   `curl --resolve`).
7. **`jq` auf der Box**: noch nicht pruefbar, weil es die Box nicht gibt. Zeile
   `offen`, mit dem Satz, dass die Pruefung Vorbedingung des ersten Box-Blocks
   ist und `apt-get install jq` Sekunden kostet.
8. **Zugang zur DNS-Verwaltung** fuer `loadtest.infranode.dev`: Zeile `offen`,
   weil der Zugang beim Owner liegt. Sie wird im Checkpoint 15-08 vorgelegt.
9. **Deckel-Rechenblatt**: es ist in 15-02 neu gerechnet und steht auf
   46 h / 5,40 USD netto. Die Freigabezeile bleibt an dieser Stelle **leer** und
   traegt den Platzhalter
   `Anfahrt freigegeben: <Datum>, Deckel <h> h / <USD> USD`; gefuellt wird sie
   in 15-08 und nirgends sonst.

Zusaetzlich, weil es nichts kostet und der Deckel daran haengt:
`scripts/ops/aws_box.sh prices` fahren und die sechs gedruckten Saetze gegen die
in `15-02` verwendeten halten. Weicht einer ab, ist das eine Zeile fuer den
Checkpoint und kein Abbruch.

**Geheimnisregel.** Diese Datei wird committet und ist damit oeffentlich: keine
IP-Adresse, keine lebende Instanz- oder Volumekennung, kein Passwortinhalt, keine
Kontokennung. Ausgenommen ist allein `snap-03f1d1d9ad9262704`. Wo ein Wert
gebraucht wird, steht ein Platzhalter mit einem Satz, woher er kommt.

ASCII, keine Umlaute, keine Em-Dashes, keine Emojis.
  </action>
  <verify>
    <automated>test -f docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt && grep -c "CI_LAUF" docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt && grep -c "Anfahrt freigegeben" docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt && python -c "import pathlib,re,sys; t=pathlib.Path('docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt').read_text(encoding='utf-8'); bad=[m for m in re.findall(r'\b(?:i-[0-9a-f]{8,}|vol-[0-9a-f]{8,}|\d{1,3}(?:\.\d{1,3}){3})\b', t)]; print('gefunden:', bad); sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Die Rohdatei traegt neun nummerierte Zeilen plus den Preisabgleich.
    - Acht Zeilen tragen das Urteil `erfuellt` oder eine benannte Ausgabe; die Zeilen 7 und 8 tragen `offen` mit Begruendung, Zeile 9 traegt den leeren Platzhalter.
    - Die Laufnummer aus `integration.yml` ist am Tag dieses Plans neu geholt und nicht aus der Recherche uebernommen.
    - Keine Instanzkennung (`i-...`), keine Volumekennung (`vol-...`), keine IPv4-Adresse und kein Passwortinhalt in der Datei; `snap-03f1d1d9ad9262704` ist erlaubt.
    - Die sechs Preissaetze aus `aws_box.sh prices` stehen in der Datei und sind gegen die des Rechenblatts gehalten.
  </acceptance_criteria>
  <done>Keine der neun Zeilen ist in die bezahlte Zeit verschoben, und was offen bleibt, ist offen benannt statt vergessen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/` zu Oeffentlichkeit | Die Rohdatei der Vorbedingungen wird committet und ist damit veroeffentlicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-18 | Information Disclosure | Anmeldedaten oder Kennungen in der Vorbedingungs-Rohdatei | mitigate | Nur Variablennamen, Platzhalter statt Werte, ein Gate-Befehl sucht nach `i-`, `vol-` und IPv4 |
| T-15-19 | Repudiation | Eine Erwartung, die nach der Messung zur Erklaerung des Ergebnisses wird | mitigate | E8 bis E14 mit Zahlen, committet vor der Anfahrt; der Commit-Zeitstempel ist der Beleg |
| T-15-20 | Denial of Wallet | Eine vergessene Vorbedingung faellt in der bezahlten Zeit auf | mitigate | Alle neun abgearbeitet oder ausdruecklich als `offen` benannt, mit dem Ort ihrer Pruefung |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Abhaengigkeit; `jq` auf der Box kommt aus der Distributionsquelle und ist Vorbedingung, nicht Auslieferung |
</threat_model>

<verification>
1. Die Ablaufdatei traegt zwoelf Schrittzeilen und die Erwartungen E1 bis E14.
2. E1 bis E7 sind unveraendert; `git diff` belegt es.
3. Die Vorbedingungs-Rohdatei traegt kein Geheimnis, belegt durch den Suchbefehl.
4. Volle Suite aus `backend/` gruen (die Gates greifen auf `docs/measurements/**`).
</verification>

<success_criteria>
Alles, was ohne Box passieren kann, ist passiert: die Werkzeuge stehen, die
Erwartung steht mit Zahlen fest, die Vorbedingungen sind abgearbeitet. Was bleibt,
ist eine Freigabe und eine Anfahrt.
</success_criteria>

<output>
Create `.planning/phases/15-messphase-eine-box-anfahrt/15-07-SUMMARY.md` when done
</output>
