---
phase: 15-messphase-eine-box-anfahrt
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbook-messbox.md
autonomous: true
requirements: [MESS-05]

must_haves:
  truths:
    - "Das Deckel-Rechenblatt traegt alle zehn Posten dieser Anfahrt und kommt auf 46 h / 5,40 USD netto"
    - "Das Runbook hat einen Block fuer den Abbildwechsel, und er steht vor der ersten Messung"
    - "Der Widerspruch baumhash-gleich gegen Abbildwechsel ist aufgeloest und die Aufloesung steht als Satz da"
    - "Kalt ist eine Bedingung mit einem Befehl und kein Wort"
    - "Die verkuerzte Ruhezeit ist als begruendungspflichtige Abweichung vermerkt, nicht als stille Praxis"
  artifacts:
    - path: "docs/runbook-messbox.md"
      provides: "Abschnitt 2 neu gerechnet, Block 13b Abbildwechsel, drop_caches in 7.2, Schritt 6b und 8b"
      contains: "drop_caches"
      min_lines: 1150
  key_links:
    - from: "docs/runbook-messbox.md"
      to: "docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh"
      via: "Block 13b nennt das Werkzeug des Abbildwechsels beim Namen"
      pattern: "92b-wechsel"
---

<objective>
Das Runbook bekommt vier Nachtraege, alle vor der ersten bezahlten Minute: das neu
gerechnete Deckel-Rechenblatt mit zwei Nachtragsposten und dem vom Owner
bestellten Filter- und Sortierblock, ein Block fuer den Abbildwechsel samt
Aufloesung des Baumhash-Widerspruchs, ein Befehl fuer "kalt", und zwei neue
Messschritte in der Reihenfolge.

Purpose: Vier der fuenf Luecken der Recherche liegen im Runbook selbst. Sie
fallen sonst auf der Box auf, und dort kostet jede Minute 0,115841 USD und mehr
noch die Vergleichbarkeit: ein Volllauf gegen das Abbild vom 10.09. koennte
Erfolgskriterium 2 gar nicht erfuellen, weil jenes Abbild die Top-up-Route nicht
kennt.

Output: ein Runbook, das die Anfahrt dieser Phase vollstaendig beschreibt, und
eine Deckelzahl, die der Owner in 15-08 nur noch freigeben oder aendern muss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md
@.planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abschnitt 2, das Deckel-Rechenblatt neu gerechnet</name>
  <files>docs/runbook-messbox.md</files>
  <read_first>
    - docs/runbook-messbox.md Zeilen 101 bis 209 (Abschnitt 2 vollstaendig: Zeitposten, Kostensaetze, Deckel-Geschichte, Rechenweg, Abschnitt 2.6)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md Abschnitt "Deckel-Rechenblatt, neu gerechnet" (die beiden Nachtragsposten und die Pruefung der Kostensaetze)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md (Owner-Entscheide 1 und 2)
  </read_first>
  <action>
Schreibe Abschnitt 2.1 auf zehn Posten fort. Die acht vorhandenen Zeilen bleiben
im Wortlaut stehen, die Spalte "Ist (Phase 15)" bleibt leer. Neu dazwischen, je
mit Quelle und der Kennzeichnung **NEU**:

- **Abbildwechsel auf den v1.2-Stand** (Pull per Digest, PHP-Haelfte,
  Registrierung, harte Grenze, Baumhash): **1 h 00 min**, Quelle: Muster
  `92-wechsel.sh`, geschaetzt, Annahme A2 der Recherche. Die Zeile steht
  zwischen "Wiederaufbau aus dem Snapshot" und "Volllauf".
- **MEM-02-Block** (Grundlast vor und nach dem Indexlauf, Freigabe abwarten,
  `rss_sampler`): **0 h 45 min**, Quelle: MEM-02 ist in keiner Zeile des
  bisherigen Rechenblatts enthalten, Annahme A3. Die Zeile steht hinter dem
  Volllauf.
- **Filter- und Sortierblock** (Sortierung auf grossem Bestand, Blaettern unter
  Filter): **1 h 30 min**, Quelle: Owner-Entscheid vom 19.09.2026 (D-01,
  `15-CONTEXT.md`), der 1 bis 2 h nennt; als Planwert gilt die Mitte. Die Zeile
  steht hinter den Laststufen.

Die Summe der Planwerte wird neu addiert und lautet **39 h 22 min**
(2:30 + 1:30 + 1:00 + 26:37 + 0:45 + 1:30 + 2:00 + 1:30 + 1:00 + 1:00 = 2362
Minuten). Rechne es beim Schreiben nach, statt die Zahl zu uebernehmen.

Abschnitt 2.5 bekommt den neuen Rechenweg mit derselben Formel und demselben
Zuschlag von 15 Prozent:

    39,37 h x 1,15 = 45,27 h, aufgerundet 46 h
    46 h x 0,115841 USD/h = 5,3287 USD, aufgerundet 5,40 USD

Die Empfehlungszeile lautet danach **46 Stunden und 5,40 USD netto**, die
ausgewiesene Untergrenze bleibt unveraendert bei 31 h und rund 3,59 USD mit ihrer
bisherigen Begruendung. Die alte Empfehlung 42 h / 4,90 USD wird nicht geloescht,
sondern als Vorgaengerstand mit Datum (16.09.) daneben gefuehrt, damit sichtbar
bleibt, welche zwei Posten dazugekommen sind.

**Die verkuerzte Ruhezeit kuerzt den Deckel nicht** (D-02). Der Planwert der
Wiederaufwaerm-Messung bleibt bei 2 h 00 min: der Planwert nimmt keine
Verbesserung vorweg, genau dieser Fehler hat den v1.1-Deckel gerissen. Die
Verkuerzung von 900 s auf 60 bis 120 s ist ausgewiesene Reserve und steht als
eigener Absatz unter 2.1, zusammen mit der Begruendungspflicht: die Abweichung
vom Vorschlagswert wird im Protokoll mit ihrem Grund genannt (derselbe
Mechanismus, gesparte Box-Zeit), und der Vorschlagswert 900 s bleibt anderswo
eine gekennzeichnete Schaetzung.

Abschnitt 2.3 bekommt einen Satz zum Alter der Saetze: sie stammen vom
04.09.2026 und werden am Anfahrtstag ein zweites Mal ueber
`scripts/ops/aws_box.sh prices` gelesen, weil `describe-instance-types`
kostenlos ist (Annahme A1). Die gepinnten Werte selbst bleiben unveraendert.

Deutsche Prosa mit echten Umlauten, Abschnittsueberschriften weiter ohne Umlaute,
keine Em-Dashes, keine Emojis, das gesperrte Wort kommt nicht vor.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_measurement_scripts.py -q && cd .. && grep -c "46 h" docs/runbook-messbox.md && grep -c "5,40 USD" docs/runbook-messbox.md && python -c "import pathlib,sys; t=pathlib.Path('docs/runbook-messbox.md').read_text(encoding='utf-8'); sys.exit(1 if (chr(8212) in t or chr(8211) in t or ('arch'+'iv') in t.lower()) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 2.1 traegt genau zehn Postenzeilen plus die Summenzeile.
    - Die drei neuen Zeilen tragen die Werte 1 h 00 min, 0 h 45 min und 1 h 30 min und je eine Quellenangabe.
    - Die Summenzeile lautet 39 h 22 min.
    - Abschnitt 2.5 nennt 45,27 h, 46 h, 5,3287 USD und 5,40 USD.
    - Die Untergrenze 31 h / 3,59 USD steht unveraendert.
    - Der Planwert der Wiederaufwaerm-Messung steht unveraendert bei 2 h 00 min, und ein Absatz sagt, warum die verkuerzte Ruhezeit ihn nicht senkt.
    - Kein U+2014, kein U+2013, kein Emoji, kein gesperrtes Wort.
  </acceptance_criteria>
  <done>Der Owner bekommt in 15-08 eine belegte Zahlbasis statt einer Zahl, und die Rechnung ist nachrechenbar, ohne eine Quelle ausserhalb dieser Datei zu oeffnen.</done>
</task>

<task type="auto">
  <name>Task 2: Block 13b Abbildwechsel und die Aufloesung des Baumhash-Widerspruchs</name>
  <files>docs/runbook-messbox.md</files>
  <read_first>
    - docs/runbook-messbox.md Zeilen 443 bis 616 (Bloecke 8 bis 13 und die Zustandspruefung, damit der neue Block an die richtige Stelle faellt)
    - docs/runbook-messbox.md Zeilen 617 bis 723 (Abschnitt 6, insbesondere die Zeile zum Baumhash mit `baumhash-gleich`)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/92-wechsel.sh Kopf (die Abhaengigkeitskette: PHP-Haelfte, dann Registrierung, dann harte Grenze; die vier Exit-Codes)
  </read_first>
  <action>
Ergaenze Abschnitt 4 um einen **Block 13b: Abbildwechsel auf den v1.2-Stand**,
unmittelbar hinter Block 13 und vor Abschnitt 5.

Inhalt des Blocks, in dieser Reihenfolge:

1. **Warum er existiert.** Der Snapshot traegt die lokale Registry mit dem
   Abbild vom 10.09.2026. Ein Volllauf gegen dieses Abbild misst einen Container
   ohne Top-up-Route, ohne die Filter aus Phase 13 und ohne den Entladeschalter
   aus Phase 14; Erfolgskriterium 2 waere damit unerfuellbar und Erfolgskriterium
   5 ebenso. Block 8 prueft heute nur, dass die Registry aus dem Snapshot
   antwortet.
2. **Was gewechselt wird** (D-04, `15-CONTEXT.md`): das aktuelle
   Multi-arch-Release-Abbild des Phase-14-Abschlusses, **per Digest** und nicht
   ueber den wandernden Zeiger `:dev`. Der Digest wird im Protokoll genannt, der
   Beweis der Identitaet bleibt der Baumhash.
3. **Die Abhaengigkeitskette**, woertlich aus dem Kopf von `92-wechsel.sh`: erst
   die PHP-Haelfte, und ihr Verzeichnis **muss** `findling` heissen, sonst findet
   der Klassenlader nichts und es gibt nirgends eine Fehlermeldung; dann die
   Registrierung; dann die harte Grenze **aus der cgroup** gelesen, weil eine
   Registrierung sie wegwirft, mit 2147483648 in beiden Feldern. Direkt daneben
   die Stellung des Entladeschalters, die aus demselben Grund verloren geht.
4. **`unregister --rm-data` nur vor dem Indexaufbau und nie danach**, und nie
   ohne die Zaehlung der laufenden Nextcloud-Instanzen davor (Block 13). Am
   07.09.2026 hat eine zweite Nextcloud am selben Docker-Dienst das Messvolumen
   der ersten geloescht.
5. **Das Werkzeug**: `92b-wechsel.sh` im Laufverzeichnis
   `docs/measurements/2026-09-v12-messung/skripte/`, Rohdatei
   `rohdaten/92b-wechsel.txt`, Exit-Codes 36 bis 39 (Baumhash, mehr als eine
   Nextcloud, unsauberer Arbeitsbaum, harte Grenze hat nicht gegriffen).

Danach die **Aufloesung des Widerspruchs**, als eigener Absatz in Abschnitt 6 an
der Baumhash-Zeile: der Abbildwechsel findet **einmal** statt, vor der ersten
Messung, und er ist genau der Vorgang, der `baumhash-gleich ja` herstellt.
Ab der ersten Messung gilt die Bedingung wieder unveraendert: steht dort spaeter
`nein`, haelt der Lauf an. Die Reihenfolge ist damit: wechseln, Baumhash lesen,
messen. Ein Abbildwechsel waehrend der Messreihe ist kein Nachtrag, sondern ein
zweiter Messgegenstand.

Ergaenze in Abschnitt 6 die Werkzeugstand-Zeile um den Satz, dass der aufgeloeste
Digest die Notiz daneben ist und nicht der Beweis (Befund L-05 der Phase 11).
  </action>
  <verify>
    <automated>grep -c "Block 13b" docs/runbook-messbox.md && grep -c "92b-wechsel" docs/runbook-messbox.md && grep -v '^#' docs/runbook-messbox.md | grep -c "baumhash-gleich"</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 4 traegt eine Ueberschrift `### Block 13b: Abbildwechsel auf den v1.2-Stand`, und sie steht zwischen Block 13 und Abschnitt 5.
    - Der Block nennt `92b-wechsel.sh`, `rohdaten/92b-wechsel.txt` und die vier Exit-Codes 36, 37, 38, 39.
    - Der Block nennt das Wort `findling` als Pflichtnamen des PHP-Verzeichnisses und `2147483648` als erwarteten cgroup-Wert.
    - Abschnitt 6 traegt einen Absatz, der sagt: einmal gewechselt vor der ersten Messung, danach gilt `baumhash-gleich` wieder als Haltebedingung.
    - Der Block sagt ausdruecklich, dass das Abbild per Digest gezogen wird und nicht ueber `:dev`.
  </acceptance_criteria>
  <done>Wer das Runbook von oben nach unten abarbeitet, wechselt das Abbild vor der ersten Messung und stolpert nicht auf der Box ueber einen Widerspruch zwischen zwei Abschnitten.</done>
</task>

<task type="auto">
  <name>Task 3: Der Befehl fuer "kalt", und zwei neue Messschritte</name>
  <files>docs/runbook-messbox.md</files>
  <read_first>
    - docs/runbook-messbox.md Zeilen 724 bis 838 (Abschnitt 7 vollstaendig: Messreihenfolge, Rueckgabewerte, Abschnitt 7.2)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md Abschnitt "Bindende Reihenfolge auf der Box" (der Aufwaermkonflikt zwischen Schritt 3, 6 und 8)
  </read_first>
  <action>
Drei Ergaenzungen in Abschnitt 7.

**Erstens, der Befehl fuer "kalt".** Abschnitt 7.2 definiert zwei der vier
Auspraegungen als "kalt, Cache des Wirts vorher geleert", nennt aber keinen
Befehl. Schreibe ihn hin, als Block mit drei Zeilen: `sync`, danach der Wert `3`
nach `/proc/sys/vm/drop_caches`, **auf dem Wirt und nicht im Container**, danach
die Rueckleseprobe ueber `free -h`. Daneben die Nebenwirkung im Klartext: das
Leeren verwirft auch den mmap-Cache des Tantivy-Index, also misst die kalte
Suche beide Haelften kalt. Das ist die gewollte schlechtere Haelfte der Wahrheit,
und sie muss dastehen. Dazu die Warnschwelle aus der Recherche: unterscheiden
sich zwei Kaltmessungen um mehr als fuenf Prozent, ist die Bedingung nicht
hergestellt.

**Zweitens, kalt wird hergestellt und nicht bewahrt.** Schreibe die Aufloesung
des Aufwaermkonflikts als bindenden Satz in Abschnitt 7.2: die Bestandssonde
(Schritt 3) und die Laststufen (Schritt 6) waermen den Container auf, Schritt 8
braucht kalte Auspraegungen. Die Kaltmessungen des Schrittes 8 laufen deshalb
**nach** Schritt 6 und 7, und jede von ihnen beginnt mit einem Containerneustart
**und** dem geleerten Wirtscache. Die Reihenfolge innerhalb des Schrittes bleibt
unveraendert bindend: erst 1 und 3 kalt, danach 2 und 4 warm.

**Drittens, zwei neue Zeilen in der Messreihenfolge.** Die Tabelle in Abschnitt 7
bekommt:

- **Schritt 6b, Filter- und Sortierblock** (D-01): Werkzeug
  `99c-filter-sortierung.sh`, Rohdatei `rohdaten/99c-filter-sortierung.txt`,
  Abbruchpfade 34 und 35. Er laeuft hinter den Laststufen, weil beide gegen den
  vollen Bestand messen und er den Container ohnehin warm vorfindet.
- **Schritt 8b, MEM-02**: Werkzeug `94b-grundlast-rueckkehr.sh`, Rohdatei
  `rohdaten/94b-grundlast-rueckkehr.txt`, Abbruchpfade 31, 32 und 33 (31 ist derselbe
  Befund wie in 7.2: ein Ast ohne Entladung, also dieselbe Zahl), Messgroesse
  `Rueckkehr zur Grundlast nach einem Indexlauf` und ausdruecklich nicht
  "Grundlast minus X".

Ergaenze die Rueckgabewert-Tabelle in 7.1 um die acht neuen Werte 32 bis 39 mit
je einer Bedingungszeile, und schreibe Schritt 8 von "Muster
`95b-kaltstart-reproduktion`" auf das Werkzeug `95b-wiederaufwaermen.sh` um,
weil `95b-kaltstart-reproduktion` eine Rohdatei von Hand ist und kein Werkzeug.

Ergaenze ausserdem Block 10 um einen Nachtrag: `dig` liegt auf der
Entwicklungsmaschine nicht vor, die Rueckfaelle sind `nslookup` und
`curl --resolve`.
  </action>
  <verify>
    <automated>grep -c "drop_caches" docs/runbook-messbox.md && grep -c "95b-wiederaufwaermen" docs/runbook-messbox.md && grep -c "94b-grundlast-rueckkehr" docs/runbook-messbox.md && grep -c "99c-filter-sortierung" docs/runbook-messbox.md && cd backend && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Abschnitt 7.2 nennt `sync`, `/proc/sys/vm/drop_caches`, den Wert `3` und das Wort Wirt; der Container ist ausdruecklich ausgeschlossen.
    - Abschnitt 7.2 nennt die Nebenwirkung auf den mmap-Cache des Tantivy-Index und die Fuenf-Prozent-Warnschwelle.
    - Abschnitt 7.2 traegt den Satz, dass die Kaltmessungen nach Schritt 6 und 7 laufen und mit Containerneustart plus geleertem Wirtscache beginnen.
    - Die Tabelle in Abschnitt 7 traegt die Zeilen 6b und 8b mit Werkzeug, Rohdatei und Abbruchpfaden.
    - Abschnitt 7.1 traegt acht neue Zeilen fuer die Werte 32 bis 39.
    - Die Zeile zu Schritt 8 nennt `95b-wiederaufwaermen.sh`; `95b-kaltstart-reproduktion` steht nur noch als Herkunftshinweis da.
    - Block 10 traegt den `dig`-Nachtrag mit beiden Rueckfaellen.
  </acceptance_criteria>
  <done>"Kalt" ist eine Bedingung mit einem Befehl, die Reihenfolge loest ihren eigenen Konflikt, und jeder der zehn Messschritte hat ein Werkzeug und einen Abbruchpfad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/` zu Oeffentlichkeit | Das Runbook ist oeffentlich lesbar; Adressen und Kennungen duerfen darin nicht stehen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | Neue Runbook-Bloecke nennen Adressen, Volume- oder Kontokennungen | mitigate | Platzhalter mit einem Satz zur Herkunft, wie seit 12-07; ausgenommen bleibt allein `snap-03f1d1d9ad9262704` |
| T-15-04 | Denial of Wallet | Deckel kleiner als der Lauf | mitigate | Alle zehn Posten gerechnet, Planwert des Volllaufs ohne vorweggenommene Verbesserung, 15 Prozent Zuschlag |
| T-15-05 | Repudiation | Eine Kaltmessung ohne Befehl, also ohne Bedingung | mitigate | `sync` plus `drop_caches` mit Wert 3 als Befehl im Runbook, plus Rueckleseprobe |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Abhaengigkeit in dieser Phase; `onnxruntime==1.30.0` bleibt bis nach der Anfahrt eingefroren |
</threat_model>

<verification>
1. `grep -c "Block 13b" docs/runbook-messbox.md` liefert mindestens 1.
2. `grep -c "drop_caches" docs/runbook-messbox.md` liefert mindestens 1.
3. Die Summenzeile des Rechenblatts lautet 39 h 22 min, die Empfehlung 46 h / 5,40 USD.
4. Kein Em-Dash, kein Emoji, kein gesperrtes Wort in der Datei.
5. Volle Suite aus `backend/` gruen.
</verification>

<success_criteria>
Das Runbook beschreibt die Anfahrt dieser Phase vollstaendig: mit Abbildwechsel,
mit einem Befehl fuer kalt, mit zehn Messschritten und mit einer Deckelzahl, die
nachgerechnet und nicht erinnert ist.
</success_criteria>

<output>
Create `.planning/phases/15-messphase-eine-box-anfahrt/15-02-SUMMARY.md` when done
</output>
