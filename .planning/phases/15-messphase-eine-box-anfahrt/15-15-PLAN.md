---
phase: 15-messphase-eine-box-anfahrt
plan: 15
type: execute
wave: 14
depends_on: [15-14]
files_modified:
  - docs/measurements/2026-09-v12-messung/README.md
  - docs/runbook-messbox.md
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [MESS-05]

must_haves:
  truths:
    - "Jede der vierzehn Erwartungen hat ihr Urteil, auch die verfehlten"
    - "Der Bericht sagt, was dieser Lauf nicht besser gemacht hat"
    - "Das Runbook traegt die tatsaechlichen Ausgaben seines Erstvollzugs und jede Abweichung als eigene Zeile"
    - "Jede gefahrene Messfassung hat danach einen Pruefsummen-Waechter"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/README.md"
      provides: "Der Bericht des Laufs, Urteil je Erwartung, Verdikte, Bilanzzeilen"
      contains: "Was dieser Lauf nicht besser gemacht hat"
    - path: "docs/runbook-messbox.md"
      provides: "Die gefuellte Ist-Spalte und die Nachtraege des Erstvollzugs"
      contains: "in Phase 15 erstmals vollzogen"
    - path: "backend/tests/test_measurement_scripts.py"
      provides: "Pruefsummen-Waechter fuer die sechs gefahrenen Fassungen"
      contains: "DRIVEN_V12"
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/README.md"
      to: "docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md"
      via: "je Erwartung ein Urteil, gegen die vor dem Lauf notierte Fassung"
      pattern: "E1[0-4]"
---

<objective>
Die Box ist weg, die Zahlen liegen im Repositorium. Jetzt entsteht, wofuer sie
erhoben wurden: der Bericht mit einem Urteil je Erwartung, die Nachtraege ins
Runbook aus dem Erstvollzug, und die Pruefsummen-Waechter fuer die sechs
gefahrenen Messfassungen.

Purpose: Eine gefahrene Messfassung ist Teil des Belegs und wird danach nicht
mehr angefasst. Und ein Runbook, dessen Erstvollzug nicht zurueckfliesst, ist
beim naechsten Mal wieder so falsch wie beim ersten.

Output: ein Bericht, ein fortgeschriebenes Runbook, sechs Waechter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md
@docs/runbook-messbox.md
@docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der Bericht, mit einem Urteil je Erwartung</name>
  <files>docs/measurements/2026-09-v12-messung/README.md</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md, Abschnitt 3 (die Erwartungen E1 bis E14 in der Fassung, die vor dem Lauf committet wurde)
    - alle Rohdateien in docs/measurements/2026-09-v12-messung/rohdaten/ (die Zahlen, ueber die berichtet wird)
    - docs/measurements/2026-09-werkzeugfixe/README.md (die Hausform: kurze Abschnitte, Zahlen mit ihren Bedingungen, ein Abschnitt zu dem, was offen blieb)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-08-SUMMARY.md bis 15-14-SUMMARY.md (die Owner-Entscheide im Wortlaut)
  </read_first>
  <action>
Schreibe `docs/measurements/2026-09-v12-messung/README.md` zum Bericht aus. Die
Freigabezeile im Kopf bleibt unveraendert stehen.

Der Bericht enthaelt, in dieser Reihenfolge:

1. **Was dieser Lauf war**, in drei Saetzen: eine bezahlte Anfahrt, der
   Erstvollzug des Runbooks, die vier offenen Messauftraege des Milestones.
2. **Die Bedingungen**, als Block: die sechs Vergleichbarkeitsgroessen mit
   ihren gelesenen Werten, der Abbilddigest, der Baumhash, die Schalterstellung
   je Messblock.
3. **Urteil je Erwartung, E1 bis E14.** Je Zeile: die Erwartung im Wortlaut, die
   gemessene Zahl, das Urteil `gehalten`, `verfehlt` oder `nicht entschieden`.
   **Eine verfehlte Erwartung ist ein Ergebnis und kein Grund fuer eine zweite
   Anfahrt**; sie bekommt ihren Satz und keine Entschuldigung. Keine Erwartung
   wird nachtraeglich umformuliert.
4. **Der DI-10-04-Wirkungsbeleg**: die Laufzeit gegen 26 h 37 min, der
   Leerlaufanteil mit seiner Ablesereihe, und der Satz, ob die Top-up-Route
   gewirkt hat oder ob die Zahlen es nicht entscheiden. Wenn die Instanz aus
   einem Snapshot neu aufgebaut wurde und seither eine Route dazugekommen ist,
   sind beide Erklaerungen moeglich, und das gehoert dahin.
5. **Die vier regressiven Laststufen**, je mit p95, Trefferdichte, der
   Gegenrechnung aus dem Nextcloud-Protokoll und ihrem Verdikt.
6. **Die Sprachfaelle**: die Bilanzzeile mit zwei Zahlen, der `ci-beleg`, und
   die Raenge der vier am 10.09. roten Faelle.
7. **Filter und Sortierung**, als benannte **Erstmessung**.
8. **Wiederaufwaerm-Kosten und MEM-02**: die vier Auspraegungen nebeneinander,
   die Rueckkehrzahl, der Bodensatz, und der Owner-Entscheid zum Vorschlagswert
   im Wortlaut.
9. **Kosten**: Laufzeit, Kosten, freigegebener Deckel, Differenz. Hat der Deckel
   gehalten oder nicht, als Wort.
10. **Was dieser Lauf nicht besser gemacht hat.** Ein eigener Abschnitt, und er
    ist Pflicht: was offen blieb, was nicht entscheidbar war, welche Zahl eine
    Erstmessung ist und keine Vergleichszeile, und die fuenf offenen Fragen der
    Recherche, soweit sie offen geblieben sind (darunter die
    Snapshot-Wiedervorlage, falls der Korpus-Snapshot stehen geblieben ist).

**Kurze Produkttexte gelten hier nicht**: das ist ein Messbericht und kein
Store-Text. Zahlen tragen trotzdem immer ihre Maschine, ihr Datum und ihre
Bedingungen neben sich.

Deutsche Prosa mit echten Umlauten, Ueberschriften ohne Umlaute, keine
Em-Dashes, keine Emojis, kein gesperrtes Wort, keine Kennung und keine Adresse.
  </action>
  <verify>
    <automated>grep -c "Was dieser Lauf nicht besser gemacht hat" docs/measurements/2026-09-v12-messung/README.md && grep -cE "E1[0-4]" docs/measurements/2026-09-v12-messung/README.md && python -c "import pathlib,sys; t=pathlib.Path('docs/measurements/2026-09-v12-messung/README.md').read_text(encoding='utf-8'); sys.exit(1 if (chr(8212) in t or chr(8211) in t or ('arch'+'iv') in t.lower()) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Vierzehn Urteile, je eines aus `gehalten`, `verfehlt`, `nicht entschieden`.
    - Kein Wortlaut einer Erwartung weicht von `00-ablauf.md` ab; `git diff` auf jene Datei ist in Abschnitt 3 leer.
    - Vier Verdikte der Laststufen stehen im Bericht, wortgleich mit denen aus `97-nebenlaeufigkeit.txt`.
    - Der Abschnitt "Was dieser Lauf nicht besser gemacht hat" existiert und ist nicht leer.
    - Die Kostenzeile nennt Laufzeit, Kosten, Deckel und Differenz.
    - Kein Em-Dash, kein Emoji, kein gesperrtes Wort, keine Kennung.
  </acceptance_criteria>
  <done>Der Bericht sagt, was gemessen wurde, unter welchen Bedingungen, und was er nicht entscheidet.</done>
</task>

<task type="auto">
  <name>Task 2: Die Nachtraege ins Runbook, aus dem Erstvollzug</name>
  <files>docs/runbook-messbox.md</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt und 07-snapshot-und-abbau.txt (die tatsaechlichen Ausgaben der erstmals vollzogenen Bloecke)
    - alle SUMMARY-Dateien der Plaene 15-09 bis 15-14 (die benannten Abweichungen)
    - docs/runbook-messbox.md, Abschnitt 2.1 (die leere Spalte "Ist (Phase 15)")
  </read_first>
  <action>
Schreibe das Runbook fort, nach D-10: der Erstvollzug validiert es woertlich, und
**Abweichungen bleiben als eigene Zeile stehen** statt ueberschrieben zu werden.

1. **Die Spalte "Ist (Phase 15)" in Abschnitt 2.1 fuellen**, Zeile fuer Zeile,
   mit den tatsaechlich gebrauchten Zeiten. Die Planwerte bleiben daneben stehen.
   Der naechste Lauf rechnet gegen die gefuellte Spalte.
2. **Jeder Block, der in Phase 15 erstmals vollzogen wurde**, bekommt seine
   tatsaechliche Ausgabe als Nachtrag, mit der Marke
   "in Phase 15 erstmals vollzogen" und dem Datum. Was anders lief als
   beschrieben, bekommt eine eigene Zeile "Abweichung:" mit dem, was
   tatsaechlich noetig war.
3. **Die benannten Abweichungen aus den Werkzeugen** wandern hierher, darunter
   die aus 15-03: der Runbook-Wortlaut in Abschnitt 7.2 nennt einen
   "Entladezaehler", der ueber eine Prozessgrenze nicht lesbar ist; gelesen wird
   `engineState unloaded` und daneben die cgroup-Differenz. Der alte Wortlaut
   wird nicht geloescht, er bekommt den Nachtrag.
4. **Abschnitt 9.3, der Rueckfluss ins Deckel-Rechenblatt**, bekommt die
   Schlusszahlen dieser Anfahrt und den Satz, welche Annahme ihren
   Schaetzcharakter verloren hat (Annahme A8, der Handaufbau, war die erste, die
   nirgends gemessen war).
5. Wo eine Vorbedingung in der bezahlten Zeit aufgefallen ist, wandert sie in
   Abschnitt 3 als neue Zeile. Wo eine Zeile ueberfluessig war, bleibt sie
   stehen und bekommt den Vermerk.

Deutsche Prosa, Ueberschriften ohne Umlaute, keine Em-Dashes, keine Emojis, kein
gesperrtes Wort, keine Kennung und keine Adresse.
  </action>
  <verify>
    <automated>grep -c "in Phase 15 erstmals vollzogen" docs/runbook-messbox.md && python -c "import pathlib,sys; t=pathlib.Path('docs/runbook-messbox.md').read_text(encoding='utf-8'); sys.exit(1 if (chr(8212) in t or chr(8211) in t or ('arch'+'iv') in t.lower()) else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Die Spalte "Ist (Phase 15)" traegt in jeder der zehn Postenzeilen eine Zahl oder den Vermerk, warum keine entstand.
    - Mindestens jeder Block, der erstmals vollzogen wurde, traegt die Marke und sein Datum.
    - Jede Abweichung steht als eigene Zeile und hat keinen bestehenden Satz ersetzt.
    - Abschnitt 9.3 traegt die Schlusszahlen dieser Anfahrt.
  </acceptance_criteria>
  <done>Die naechste Anfahrt beginnt mit einem Runbook, das einmal gefahren worden ist.</done>
</task>

<task type="auto">
  <name>Task 3: Pruefsummen-Waechter fuer die sechs gefahrenen Fassungen</name>
  <files>backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/tests/test_measurement_scripts.py Zeilen 95 bis 125 und 1180 bis 1235 (die vorhandenen Waechter, `DRIVEN_FASSUNG_RULE` und die Mutationsprobe daneben)
    - docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md, Abschnitt 5 (die Liste der Werkzeuge, fuer die ein Waechter faellig ist)
  </read_first>
  <action>
Ziehe fuer die sechs in dieser Anfahrt gefahrenen Messfassungen einen
Pruefsummen-Waechter nach: `98c-sprachfaelle.sh`, `97-cron-vorpruefung.sh`,
`95b-wiederaufwaermen.sh`, `94b-grundlast-rueckkehr.sh`,
`99c-filter-sortierung.sh`, `92b-wechsel.sh`.

Neue Modulkonstante `DRIVEN_V12_FASSUNGEN` als Abbildung von Dateiname auf
sha256 und Byteanzahl, mit einem Begruendungsabsatz darueber: eine gefahrene
Messfassung ist Teil des Belegs und wird danach nicht mehr angefasst; wer sie
aendern will, legt eine Nachfolgefassung mit neuer Nummer an.

Ein parametrisierter Fall
`test_the_driven_v12_fassung_stays_byte_identical`, parametrisiert ueber
`DRIVEN_V12_FASSUNGEN`, mit `DRIVEN_FASSUNG_RULE` als Assertion-Kommentar, plus
eine Mutationsprobe nach dem vorhandenen Muster.

Die Sollwerte werden aus den Dateien im Zustand **nach** der Anfahrt abgelesen;
keines der sechs Skripte wurde waehrend der Anfahrt geaendert, also sind es
dieselben Bytes wie vor ihr. Weicht eines ab, ist das ein Befund fuer die
SUMMARY und nicht ein Anlass, den Sollwert anzupassen.

Der Kopie-Waechter `COPIED_TOOLS` aus 15-01 bleibt unberuehrt.
  </action>
  <verify>
    <automated>cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest tests/test_measurement_scripts.py -k "driven_v12_fassung" -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_measurement_scripts.py -k "driven_v12_fassung" -q` waehlt mindestens sechs Faelle aus.
    - `DRIVEN_V12_FASSUNGEN` traegt genau sechs Eintraege, je mit sha256 und Byteanzahl.
    - Die Mutationsprobe belegt, dass ein einziges zusaetzliches Byte den Waechter rot macht.
    - Die SUMMARY sagt fuer jedes der sechs Skripte, ob es seit dem Stand vor der Anfahrt byteweise unveraendert ist.
    - Volle Suite gruen, Skipzahl unveraendert.
  </acceptance_criteria>
  <done>Die sechs gefahrenen Fassungen sind eingefroren, so wie es 98 und 98b vor ihnen sind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/` zu Oeffentlichkeit | Bericht und Runbook sind oeffentlich lesbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-44 | Repudiation | Eine Erwartung wird nachtraeglich umformuliert, damit sie haelt | mitigate | Urteil gegen die committete Fassung; `git diff` auf `00-ablauf.md` Abschnitt 3 muss leer sein |
| T-15-45 | Tampering | Eine gefahrene Messfassung wird spaeter angefasst | mitigate | sha256-Waechter fuer alle sechs, plus Mutationsprobe |
| T-15-46 | Information Disclosure | Kennungen oder Adressen wandern aus den Rohdaten in den Bericht | mitigate | Geheimnisregel, Platzhalter, Pruefbefehl im `verify` |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Abhaengigkeit |
</threat_model>

<verification>
1. Vierzehn Urteile im Bericht, je eines aus drei erlaubten Woertern.
2. Der Abschnitt "Was dieser Lauf nicht besser gemacht hat" existiert.
3. Die Ist-Spalte des Rechenblatts ist gefuellt.
4. Sechs Pruefsummen-Waechter, volle Suite gruen.
5. Kein Em-Dash, kein Emoji, kein gesperrtes Wort, keine Kennung.
</verification>

<success_criteria>
Aus einer bezahlten Anfahrt ist ein Beleg geworden, der ohne die Box gelesen
werden kann, und das Runbook ist um seinen Erstvollzug klueger.
</success_criteria>

<output>
Create `.planning/phases/15-messphase-eine-box-anfahrt/15-15-SUMMARY.md` when done
</output>
