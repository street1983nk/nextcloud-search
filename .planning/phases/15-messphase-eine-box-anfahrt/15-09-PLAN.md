---
phase: 15-messphase-eine-box-anfahrt
plan: 9
type: execute
wave: 8
depends_on: [15-08]
gated_by: "15-08. Ohne die Freigabezeile im Bericht wird dieser Plan nicht ausgefuehrt."
files_modified:
  - docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt
  - docs/measurements/2026-09-v12-messung/rohdaten/92b-wechsel.txt
  - docs/measurements/2026-09-v12-messung/rohdaten/40b-baumhash.txt
  - docs/measurements/2026-09-v12-messung/rohdaten/92b-info-box.xml
autonomous: false
requirements: [MESS-05]

must_haves:
  truths:
    - "Die Box steht, aus dem Snapshot wiederaufgebaut, mit 4 GB harter Maschinengrenze und 2 GB harter Containergrenze"
    - "Die Bewaffnung ist gezaehlt und nicht abgelesen"
    - "Genau eine Nextcloud laeuft am Docker-Dienst"
    - "Das Abbild traegt den v1.2-Stand, belegt durch baumhash-gleich ja"
    - "Jede tatsaechliche Ausgabe steht in einer Rohdatei, und keine Kennung darin"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt"
      provides: "Die Ausgaben der Aufbaubloecke 1 bis 13, mit Platzhaltern statt Kennungen"
      contains: "nproc"
    - path: "docs/measurements/2026-09-v12-messung/rohdaten/40b-baumhash.txt"
      provides: "Der Identitaetsbeweis des Abbilds"
      contains: "baumhash-gleich"
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/rohdaten/40b-baumhash.txt"
      to: "docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh"
      via: "der Abbildwechsel liest seinen eigenen Beweis zurueck und bricht bei nein ab"
      pattern: "baumhash-gleich ja"
---

<objective>
Die Anfahrt beginnt. Dieser Plan fuehrt die Aufbaubloecke 1 bis 13 des Runbooks
und den neuen Block 13b (Abbildwechsel) aus, als **begleitete Sitzung**: Block
fuer Block, jede tatsaechliche Ausgabe sofort in die Rohdatei, jede Abweichung
als eigene Zeile stehen gelassen und nicht geglaettet (D-10 aus Phase 12).

Purpose: Dies ist der Erstvollzug eines Verfahrens von 1102 Zeilen. Der Vollzug
validiert es woertlich; was nicht stimmt, ist ein Befund des Runbooks und keine
Stoerung des Laufs.

Output: eine laufende Messinstanz mit dem v1.2-Abbild und drei Rohdateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md
@docs/runbook-messbox.md
@docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloecke 1 bis 9, Maschine und Datentraeger</name>
  <files>docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt</files>
  <read_first>
    - docs/runbook-messbox.md Zeilen 234 bis 484 (Bloecke 1 bis 9 mit allen Kommandobloecken, die wiederkehrenden Handgriffe, die `aws_box.sh start` nicht erledigt)
    - scripts/ops/aws_box.sh, die Unterbefehle `create`, `restore`, `status`, `prices` und `require_state` (was die Zustandsdatei braucht und was sie selbst anhaengt)
    - docs/measurements/2026-09-v12-messung/rohdaten/02-vorbedingungen.txt (der Stand der neun Vorbedingungen)
    - docs/measurements/2026-09-werkzeugfixe/rohdaten/07-snapshot-und-abbau.txt (die Hausform einer Rohdatei mit Platzhaltern)
  </read_first>
  <action>
Fahre die Bloecke 1 bis 9 des Runbooks in ihrer Reihenfolge und schreibe jede
tatsaechliche Ausgabe nach
`docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt`, mit einer
Abschnittsueberschrift je Block.

1. Security Group anlegen. SSH ausschliesslich von genau einer Adresse mit
   Praefixlaenge 32; offen sind sonst nur 80 und 443 TCP und 443 UDP.
2. Schluesselpaar neu erzeugen. Der private Teil verlaesst die erzeugende
   Maschine nie und gehoert in keine Sicherung, die irgendwohin synchronisiert.
3. Instanz erzeugen, `m7g.large`, `eu-central-1c` (D-06 aus Phase 12, kein
   Instanztypwechsel, auch nicht zum Sparen).
4. `box.env` von Hand neu schreiben: `BOX_INSTANCE_ID` und
   `BOX_SECURITY_GROUP`. Es gibt heute keine `box.env` mehr, und ohne sie
   brechen `volume`, `restore`, `stop`, `start` und `snapshot` in
   `require_state` ab. Alles Weitere haengt `aws_box.sh` selbst an.
5. `mem=4G` in die Kernel-Kommandozeile, Neustart, danach `free -h` als
   Rueckleseprobe.
6. `aws_box.sh restore`: Volume aus `snap-03f1d1d9ad9262704`. Der Unterbefehl
   taggt pflichtmaessig auf `purpose=findling-phase5` um und liest zurueck; er
   endet beim Anhaengen.
7. Datentraeger mounten, **ueber die UUID** und nicht ueber den Geraetenamen.
8. Docker-`data-root` setzen, **bevor** der Daemon zum ersten Mal startet.
   Danach die Registry aus dem Snapshot befragen, Erwartung
   `{"repositories":["findling_backend"]}`.
9. Systemplatte zurueckspielen aus `home-ubuntu-work.tar.gz`. Danach liegen die
   Passwortdateien beider Konten auf der Box, und sie gehoeren in kein
   Repositorium.

**Die Geheimnisregel gilt fuer jede Zeile, die in die Rohdatei wandert**: keine
IP-Adresse, keine lebende Instanz- oder Volumekennung, kein Passwortinhalt, keine
Kontokennung. `snap-03f1d1d9ad9262704` ist die einzige Ausnahme. Wo ein Wert
gebraucht wird, steht ein Platzhalter mit einem Satz, woher er kommt.

**Pfadumschreibung.** Git fuer Windows schreibt Pfadargumente um; aus `/dev/sdf`
wurde schon einmal ein Windows-Pfad unterhalb des Git-Verzeichnisses, und der
Aufruf lief durch und meinte etwas anderes, als er sagte. Fuer jeden Aufruf, der
von dieser Maschine gegen die Box oder die AWS-API laeuft, wird die
Umschreibung fuer den eigenen Prozess abgeschaltet, so wie `aws_box.sh` es tut.

**Jede Abweichung vom Runbook bleibt als eigene Zeile stehen** (D-10), mit der
Marke "in Phase 15 erstmals vollzogen" und der tatsaechlichen Ausgabe. Nichts
wird ueberschrieben, und das Runbook wird waehrend der Anfahrt nicht editiert;
die Nachtraege sammelt 15-15.
  </action>
  <verify>
    <automated>test -f docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt && python -c "import pathlib,re,sys; t=pathlib.Path('docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt').read_text(encoding='utf-8'); bad=re.findall(r'\b(?:i-[0-9a-f]{8,}|vol-[0-9a-f]{8,}|\d{1,3}(?:\.\d{1,3}){3})\b', t); print('gefunden:', bad); sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Die Rohdatei traegt neun Abschnitte, je mit der tatsaechlichen Ausgabe des Blocks.
    - `free -h` meldet nach dem Neustart 3.9Gi, `nproc` meldet 2, `uname -m` meldet `aarch64`.
    - Die Registry aus dem Snapshot antwortet mit `findling_backend`.
    - Das aus dem Snapshot erzeugte Volume traegt `purpose=findling-phase5`, zurueckgelesen ueber `describe-tags`; der geerbte Keep-Tag haengt nicht mehr daran.
    - Keine Instanzkennung, keine Volumekennung, keine IPv4-Adresse, kein Passwortinhalt in der Rohdatei.
  </acceptance_criteria>
  <done>Die Maschine steht mit dem Korpus des 10.09. auf ihrem Datentraeger, und die Rohdatei sagt, wie sie dahin kam.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Block 10, der A-Record</name>
  <files>docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt</files>
  <read_first>
    - docs/runbook-messbox.md Zeilen 484 bis 506 (Block 10 mit beiden Rueckfaellen)
    - .planning/phases/15-messphase-eine-box-anfahrt/15-08-SUMMARY.md (die Antwort des Owners auf Frage A)
  </read_first>
  <action>
`loadtest.infranode.dev` zeigt seit dem 11.09.2026 ins Leere. Die Zone liegt
beim externen Verwalter, und im Repositorium gibt es dafuer keinen Zugang. Die
oeffentliche Adresse der Instanz steht jetzt fest und wird dem Owner **ausserhalb
des Repositoriums** genannt, nie in einer committeten Datei.

Je nach Antwort des Owners aus 15-08, Frage A:

- **Owner setzt den Record:** anhalten, bis der Record gesetzt ist. Danach die
  Aufloesung pruefen. `dig` liegt auf dieser Maschine nicht vor; genutzt wird
  `nslookup`. In die Rohdatei wandert, dass der Name aufloest, und nicht, worauf.
- **Rueckfall:** `/etc/hosts`-Pin auf der Box, nach **jedem** Maschinenneustart
  neu zu bilden, oder `curl --resolve`. Der gewaehlte Rueckfall steht als Zeile
  in der Rohdatei, samt dem Satz, dass er nach jedem Neustart neu gebildet
  werden muss.
  </action>
  <what-built>
    Die Instanz laeuft, der Datentraeger ist gemountet, die Systemplatte ist
    zurueckgespielt. Was fehlt, ist ein Name, unter dem die Nextcloud erreichbar
    ist, und der liegt ausserhalb dieses Repositoriums.
  </what-built>
  <how-to-verify>
    1. Der Owner setzt den A-Record beim Verwalter der Zone auf die ihm genannte
       Adresse, oder bestaetigt den Rueckfall.
    2. Gegenprobe auf dieser Maschine: `nslookup loadtest.infranode.dev` loest
       auf, oder der Rueckfall antwortet.
    3. In der Rohdatei steht danach, welcher der drei Wege gewaehlt wurde, und
       keine Adresse.
  </how-to-verify>
  <verify>
    <human-check>Der Name loest auf, oder der gewaehlte Rueckfall ist in der Rohdatei benannt.</human-check>
  </verify>
  <acceptance_criteria>
    - Die Rohdatei traegt eine Zeile `namensaufloesung-ist=` mit einem der drei Werte `a-record`, `hosts-pin` oder `curl-resolve`.
    - Die Rohdatei traegt keine Adresse.
    - Beim `hosts-pin` steht der Satz daneben, dass er nach jedem Maschinenneustart neu gebildet werden muss.
  </acceptance_criteria>
  <resume-signal>Der Record ist gesetzt, oder der Rueckfall ist gewaehlt. Weiter mit Block 11.</resume-signal>
  <done>Die Nextcloud ist unter einem Namen erreichbar, und das Repositorium weiss nicht, unter welcher Adresse.</done>
</task>

<task type="auto">
  <name>Task 3: Bloecke 11 bis 13 und der Abbildwechsel</name>
  <files>docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt, docs/measurements/2026-09-v12-messung/rohdaten/92b-wechsel.txt, docs/measurements/2026-09-v12-messung/rohdaten/40b-baumhash.txt, docs/measurements/2026-09-v12-messung/rohdaten/92b-info-box.xml</files>
  <read_first>
    - docs/runbook-messbox.md Zeilen 507 bis 561 (Bloecke 11 bis 13) und Block 13b in der Fassung aus 15-02
    - docs/measurements/2026-09-v12-messung/skripte/92b-wechsel.sh (das Werkzeug aus 15-06, sein Aufrufvertrag und seine fuenf Abbrueche)
    - docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.sh (wie der Beweis erzeugt und zurueckgelesen wird)
  </read_first>
  <action>
Fahre die Bloecke 11 bis 13 und danach den Abbildwechsel.

**Block 11, die Bewaffnung nach DI-05-36.** `app_api:app:disable` und
`app_api:app:enable`, danach ein **gezaehlter** Pollerdurchgang aus dem
Containerprotokoll. `app_api:app:list` zaehlt hier ausdruecklich nicht: ein
abgelesener Zustand war schon einmal gruen, waehrend nichts lief. Die Zahl
wandert in die Rohdatei.

**Block 12, die harte Speichergrenze des Containers**, gelesen **aus der
cgroup** (`memory.max` und `memory.swap.max`), Erwartung `2147483648` in beiden
Feldern. Die Grenze geht bei jeder Registrierung verloren.

**Block 13, genau eine Nextcloud am Docker-Dienst.** Die Zaehlung wird
unmittelbar vor **jedem** `--rm-data` wiederholt. Liefert sie mehr als 1, wird
nichts weiter getan.

**Block 13b, der Abbildwechsel** ueber `92b-wechsel.sh` mit dem Digest des
aktuellen Multi-arch-Release-Abbilds (D-04). Das Werkzeug fuehrt selbst die
Kette PHP-Haelfte, Registrierung, harte Grenze aus der cgroup und bricht bei
36 bis 39 ab. Danach:

- `rohdaten/40b-baumhash.txt` traegt `baumhash-gleich ja` mit drei verankerten
  `baumhash:`-Zeilen.
- `rohdaten/92b-wechsel.txt` traegt `abbild-digest-ist` und
  `entladeschalter-ist`.
- `rohdaten/92b-info-box.xml` ist die Kopie, mit der registriert wurde.
- `git status --porcelain backend/appinfo/info.xml` ist leer.

**Ab hier gilt `baumhash-gleich` wieder als Haltebedingung** des Laufs: steht
dort spaeter `nein`, haelt der Lauf an. Der Wechsel hat einmal stattgefunden,
und ein zweiter waehrend der Messreihe waere ein zweiter Messgegenstand.

**Waehrend der bezahlten Anfahrt wird kein Werkzeug mehr geaendert.** Faellt
eines auf, wird der Befund notiert und der Lauf zu Ende gefahren oder
abgebrochen; die Korrektur gehoert in die Zeit nach dem Abbau.
  </action>
  <verify>
    <automated>grep -c "baumhash-gleich ja" docs/measurements/2026-09-v12-messung/rohdaten/40b-baumhash.txt && grep -c "abbild-digest-ist" docs/measurements/2026-09-v12-messung/rohdaten/92b-wechsel.txt && git status --porcelain backend/appinfo/info.xml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Der gezaehlte Pollerdurchgang liefert eine Zahl groesser null, und sie steht in der Rohdatei.
    - Beide cgroup-Felder melden `2147483648`.
    - Die Zaehlung der Nextcloud-Instanzen liefert genau 1, und sie steht vor jedem `--rm-data` in der Rohdatei.
    - `rohdaten/40b-baumhash.txt` traegt `baumhash-gleich ja`.
    - `git status --porcelain backend/appinfo/info.xml` ist leer (die Zeilenzahl ist 0).
    - Die Statusseite kennt das Wort `unloaded`; ein Abbild, das es nicht kennt, ist der Stand vom 10.09. und damit der falsche.
  </acceptance_criteria>
  <done>Die Box misst den v1.2-Stand, und dass sie es tut, haengt an einem Baumhash und nicht an einer Erinnerung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet zu Messinstanz | Eine oeffentlich erreichbare Nextcloud fuer die Dauer der Anfahrt |
| Box zu Repositorium | Rohdaten wandern aus einer lebenden Umgebung in ein oeffentliches Repositorium |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-23 | Elevation of Privilege | Offene Nextcloud im Internet waehrend 26 Stunden Volllauf | mitigate | Nur 80 und 443 offen, SSH von genau einer Adresse mit Praefixlaenge 32, Instanz existiert nur fuer die Dauer der Anfahrt |
| T-15-24 | Information Disclosure | Adresse, Instanz- oder Volumekennung in einer committeten Rohdatei | mitigate | Geheimnisregel, Platzhalter, ein Suchbefehl im `verify` sucht nach `i-`, `vol-` und IPv4 |
| T-15-25 | Information Disclosure | Privater Schluesselteil in einer Sicherung | mitigate | Der private Teil verlaesst die erzeugende Maschine nicht und gehoert in keine synchronisierte Sicherung |
| T-15-26 | Tampering | `--rm-data` loescht das Volume einer zweiten Nextcloud | mitigate | Zaehlung unmittelbar vor jedem Aufruf, Abbruch 37 im Werkzeug |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Keine Abhaengigkeit; auf der Box kommt nur `jq` aus der Distributionsquelle dazu |
</threat_model>

<verification>
1. `free -h` 3.9Gi, `nproc` 2, `uname -m` aarch64.
2. Beide cgroup-Felder auf 2147483648.
3. `baumhash-gleich ja` in der Rohdatei.
4. Genau eine Nextcloud am Docker-Dienst, gezaehlt.
5. Keine Kennung und keine Adresse in einer der vier Rohdateien.
</verification>

<success_criteria>
Die Messinstanz steht auf der Zielhardware, traegt den Korpus des 10.09. und das
Abbild des v1.2-Stands, und jede Ausgabe des Erstvollzugs ist protokolliert.
</success_criteria>

<output>
Create `.planning/phases/15-messphase-eine-box-anfahrt/15-09-SUMMARY.md` when done
</output>
