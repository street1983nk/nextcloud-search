---
phase: 15-messphase-eine-box-anfahrt
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.py
  - docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.sh
  - docs/measurements/2026-09-v12-messung/skripte/90-bestand.sh
  - docs/measurements/2026-09-v12-messung/skripte/91-korpus.sh
  - docs/measurements/2026-09-v12-messung/skripte/93-nullstand.sh
  - docs/measurements/2026-09-v12-messung/skripte/95-spitze.sh
  - docs/measurements/2026-09-v12-messung/skripte/96-volllauf.sh
  - docs/measurements/2026-09-v12-messung/skripte/96b-waechter.sh
  - docs/measurements/2026-09-v12-messung/skripte/96c-lesen.py
  - docs/measurements/2026-09-v12-messung/skripte/96d-statusbeobachter.py
  - docs/measurements/2026-09-v12-messung/skripte/97-nebenlaeufigkeit.sh
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [MESS-05]

must_haves:
  truths:
    - "Das Laufverzeichnis der Anfahrt traegt jedes Werkzeug, das die Messreihenfolge des Runbooks nennt"
    - "Keine Kopie weicht um ein Byte von ihrem Original ab, und ein Gate haelt das fest"
    - "Kein neues Laufverzeichnis, also bleibt NARROW_SCOPE_DIRS unveraendert"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/skripte/96-volllauf.sh"
      provides: "Der Anstoss des Volllaufs (Schritt 4 der Messreihenfolge)"
      contains: "#!/bin/sh"
    - path: "docs/measurements/2026-09-v12-messung/skripte/97-nebenlaeufigkeit.sh"
      provides: "Die fuenf Laststufen (Schritt 6)"
      contains: "STUFEN"
    - path: "backend/tests/test_measurement_scripts.py"
      provides: "Der Kopie-Waechter ueber alle elf uebernommenen Werkzeuge"
      contains: "COPIED_TOOLS"
  key_links:
    - from: "backend/tests/test_measurement_scripts.py"
      to: "docs/measurements/2026-09-vergleichsmessung-m7g/skripte/"
      via: "sha256 der Kopie gegen sha256 des Originals"
      pattern: "COPIED_TOOLS"
---

<objective>
Das Laufverzeichnis `docs/measurements/2026-09-v12-messung/skripte/` traegt heute
drei Werkzeuge, die Messreihenfolge des Runbooks verlangt aber neun Bloecke. Elf
Werkzeuge werden aus dem v1.1-Verzeichnis **kopiert**, nie editiert, und ein Gate
haelt fest, dass sie byteweise ihre Originale sind.

Purpose: Ein Werkzeug, das waehrend der bezahlten Anfahrt fehlt, kostet Box-Zeit
zum vollen Satz. Ein Werkzeug, das jemand beim Kopieren "nur schnell angepasst"
hat, macht die Stufenzahlen dieser Anfahrt gegen die von v1.1 unvergleichbar
(Regel C.3, `.planning/research/ARCHITECTURE.md`).

Output: elf Kopien, ein parametrisierter Kopie-Waechter, volle Suite gruen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-messphase-eine-box-anfahrt/15-CONTEXT.md
@.planning/phases/15-messphase-eine-box-anfahrt/15-RESEARCH.md
@docs/runbook-messbox.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elf Werkzeuge byteweise uebernehmen</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.py, docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.sh, docs/measurements/2026-09-v12-messung/skripte/90-bestand.sh, docs/measurements/2026-09-v12-messung/skripte/91-korpus.sh, docs/measurements/2026-09-v12-messung/skripte/93-nullstand.sh, docs/measurements/2026-09-v12-messung/skripte/95-spitze.sh, docs/measurements/2026-09-v12-messung/skripte/96-volllauf.sh, docs/measurements/2026-09-v12-messung/skripte/96b-waechter.sh, docs/measurements/2026-09-v12-messung/skripte/96c-lesen.py, docs/measurements/2026-09-v12-messung/skripte/96d-statusbeobachter.py, docs/measurements/2026-09-v12-messung/skripte/97-nebenlaeufigkeit.sh</files>
  <read_first>
    - docs/measurements/2026-09-v12-messung/skripte/00-ablauf.md (die Schrittfolge, in die diese Werkzeuge einsortiert werden)
    - docs/runbook-messbox.md Abschnitt 7, Zeilen 724 bis 749 (welcher Schritt welches Werkzeug ruft)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/96-volllauf.sh Kopf (die Variablen mit Vorgabewerten, die eine Kopie ohne Aenderung fahrbar machen)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/97-nebenlaeufigkeit.sh Kopf (Stufenliste, Passwortregel, das absichtliche Vorladen des Modells)
  </read_first>
  <action>
Kopiere genau diese elf Dateien aus
`docs/measurements/2026-09-vergleichsmessung-m7g/skripte/` nach
`docs/measurements/2026-09-v12-messung/skripte/`, byteweise unveraendert:
`40b-baumhash.py`, `40b-baumhash.sh`, `90-bestand.sh`, `91-korpus.sh`,
`93-nullstand.sh`, `95-spitze.sh`, `96-volllauf.sh`, `96b-waechter.sh`,
`96c-lesen.py`, `96d-statusbeobachter.py`, `97-nebenlaeufigkeit.sh`.

Kopieren heisst kopieren: kein Umbenennen einer Variablen, kein neuer Kommentar,
keine angepasste Jahreszahl, kein neues Zeilenende. Die Skripte sind ueber
Umgebungsvariablen mit Vorgabewerten parametrisiert (`SKRIPTE`, `REPO`, `OUT`,
`IMAGE`, `CONTAINER`, `NEXTCLOUD`, `APP_ID`), und `OUT` leitet sich aus dem Ort
des Skripts ab, also schreibt die Kopie von selbst nach
`2026-09-v12-messung/rohdaten/`.

Das Ausfuehrungsbit der neun Skripte bleibt gesetzt (`96c-lesen.py` und
`96d-statusbeobachter.py` sind ebenfalls ausfuehrbar).

**Nicht kopiert werden** `92-wechsel.sh` (bekommt in 15-06 eine Nachfolgefassung
`92b-wechsel.sh`, weil der Abbildwechsel dieser Anfahrt per Digest laeuft, D-04),
`94-grundlast.sh` (bekommt in 15-04 die Nachfolgefassung
`94b-grundlast-rueckkehr.sh`), `99-seitenroute.sh` und `99b-runden.sh` (dienen in
15-05 nur als Muster), `96e-ntfy-watch.sh`, `98-sprachfaelle.sh` und
`98b-sprachfaelle.sh` (eingefrorene Beweisstuecke, Nachfolger ist das bereits
vorhandene `98c-sprachfaelle.sh`).

`__pycache__` wird nicht mitkopiert.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Alle elf Dateien liegen in `docs/measurements/2026-09-v12-messung/skripte/`.
    - Fuer jede gilt: `sha256` der Kopie ist gleich `sha256` des Originals in `2026-09-vergleichsmessung-m7g/skripte/`.
    - `git status --porcelain docs/measurements/2026-09-vergleichsmessung-m7g/` ist leer, das Quellverzeichnis ist unberuehrt.
    - Kein `__pycache__` und keine `.claude-active` im Zielverzeichnis.
    - `test_the_measurement_script_carries_no_carriage_return`, `..._no_dash`, `..._no_path_of_one_machine` und `..._no_password_on_a_command_line` sind fuer die elf neuen Pfade gruen (sie werden ueber `NARROW_SCOPE_DIRS` automatisch eingesammelt).
  </acceptance_criteria>
  <done>Die Messreihenfolge des Runbooks hat fuer jeden ihrer neun Bloecke ein Werkzeug im Laufverzeichnis der Anfahrt.</done>
</task>

<task type="auto">
  <name>Task 2: Der Kopie-Waechter</name>
  <files>backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/tests/test_measurement_scripts.py Zeilen 60 bis 130 (Pfadkonstanten, NARROW_SCOPE_DIRS, die vorhandenen sha256-Waechter und DRIVEN_FASSUNG_RULE)
    - backend/tests/test_measurement_scripts.py Zeilen 1160 bis 1235 (Muster der bestehenden Waechter: byteweise Gleichheit plus eine Mutationsprobe daneben)
  </read_first>
  <action>
Ergaenze `backend/tests/test_measurement_scripts.py` um einen Waechter ueber die
elf Kopien.

Neue Modulkonstante `COPIED_TOOLS` als Tupel der elf Dateinamen, in der
Reihenfolge ihrer Nummern, mit einem Begruendungsabsatz darueber: eine Kopie ist
kein Fork, sie traegt die Zahlen von v1.1 mit, und eine stillschweigend
angepasste Kopie waere ein zweiter Messgegenstand.

Ein parametrisierter Fall
`test_the_copied_tools_of_the_v12_run_are_byte_identical_to_their_original`,
parametrisiert ueber `COPIED_TOOLS`, mit `ids` auf dem Dateinamen. Er liest beide
Dateien binaer, vergleicht die `sha256`-Hexdigests und nennt im
Assertion-Kommentar `DRIVEN_FASSUNG_RULE` als Grund. Er prueft ausserdem, dass
beide Dateien existieren, damit ein geloeschtes Original nicht als gruen
durchgeht.

Ein Fall `test_the_copy_watchman_fires_on_a_single_added_character`, der die
Schaerfe des Waechters an einer gestellten Probe zeigt, nach dem Muster von
`test_the_watchman_of_the_driven_fassung_fires_on_a_single_added_character`: das
gelesene Original plus ein Leerzeichen hat einen anderen Hexdigest.

Ein Fall
`test_the_run_directory_of_the_trip_carries_every_tool_the_measurement_order_names`,
der ueber eine Liste der vom Runbook genannten Werkzeugnamen laeuft und fuer jeden
prueft, dass er im v12-Laufverzeichnis liegt. Die Liste enthaelt die elf Kopien
und die drei bereits vorhandenen Werkzeuge (`73-bestand-sonde.py`,
`97-cron-vorpruefung.sh`, `98c-sprachfaelle.sh`). Die in den Plaenen 15-03 bis
15-06 entstehenden Werkzeuge stehen NICHT in dieser Liste; sie kommen mit ihren
eigenen Plaenen dazu.

`NARROW_SCOPE_DIRS` bleibt unveraendert, und
`test_the_narrow_scope_covers_the_three_run_directories_written_under_these_rules`
bleibt unberuehrt: es wird in `2026-09-v12-messung` gemessen und kein viertes
Laufverzeichnis angelegt (Annahme A6 der Recherche).

Docstrings auf Englisch nach der Hausform der Datei, Prosa ohne Em-Dashes.
  </action>
  <verify>
    <automated>cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest tests/test_measurement_scripts.py -k "byte_identical_to_their_original or copy_watchman_fires or every_tool_the_measurement_order_names" -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_measurement_scripts.py -k "byte_identical_to_their_original" -q` waehlt **elf** Faelle aus, nicht null und nicht einen.
    - `COPIED_TOOLS` traegt genau elf Eintraege.
    - Der Mutationsfall belegt, dass ein einziges zusaetzliches Byte den Waechter rot macht.
    - `NARROW_SCOPE_DIRS == (RUN_DIR, FIX_RUN_DIR, V12_RUN_DIR)` gilt unveraendert.
    - Volle Suite gruen, Skipzahl unveraendert gegen den Stand aus 14-12 (15 uebersprungen).
  </acceptance_criteria>
  <done>Eine spaeter geaenderte Kopie faellt an einem Gate auf und nicht erst beim Auswerten einer Messreihe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/` zu Oeffentlichkeit | Alles unter `docs/measurements/` ist oeffentlich lesbar, sobald es committet ist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Kopiertes Messwerkzeug weicht still vom Original ab | mitigate | `COPIED_TOOLS`-Waechter mit sha256 je Datei plus Mutationsprobe |
| T-15-02 | Information Disclosure | Eine Kopie traegt einen Pfad oder ein Passwort einer Maschine | mitigate | Die bestehenden Gates ueber `NARROW_SCOPE_DIRS` greifen automatisch auf die neuen Pfade |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Diese Phase installiert kein Paket (15-RESEARCH.md, Package Legitimacy Audit); `onnxruntime==1.30.0` bleibt eingefroren |
</threat_model>

<verification>
1. Elf Kopien vorhanden, je sha256-gleich mit dem Original.
2. Quellverzeichnis `2026-09-vergleichsmessung-m7g/` unveraendert.
3. Volle Suite aus `backend/` gruen, Skipzahl unveraendert.
4. Kein Em-Dash und kein Emoji in den geaenderten Dateien.
</verification>

<success_criteria>
Jeder Block der Messreihenfolge hat sein Werkzeug im Laufverzeichnis, und ein
Gate haelt fest, dass keines davon beim Kopieren angefasst wurde.
</success_criteria>

<output>
Create `.planning/phases/15-messphase-eine-box-anfahrt/15-01-SUMMARY.md` when done
</output>
