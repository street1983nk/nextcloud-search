---
phase: 13-filter-und-sortierung-auf-der-ergebnisseite
plan: 11
type: execute
wave: 10
depends_on: [13-10]
files_modified:
  - php/l10n/de.json
  - php/l10n/de.js
  - php/l10n/de_DE.json
  - php/l10n/de_DE.js
  - php/l10n/fr.json
  - php/l10n/fr.js
  - backend/tests/test_admin_ui_contract.py
  - docs/l10n-french.md
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, FILT-04]

must_haves:
  truths:
    - "Die Seite ist auf Englisch, Deutsch und Französisch vollständig übersetzt; keine der 23 neuen Zeichenketten bleibt unübersetzt"
    - "Alle sechs Katalogdateien tragen dieselbe Schlüsselmenge, und die beiden deutschen Zwillingspaare sind textgleich"
    - "Die harte Schlüsselzahl steht auf 197 und trägt einen Absatz, der die Erhöhung begründet"
    - "Die drei französischen Gleichwort-Ausnahmen stehen namentlich mit Begründung in der Ausnahmeliste, nicht als erhöhte Schwelle"
  artifacts:
    - path: "php/l10n/de.json"
      provides: "23 neue Schlüssel mit echten Umlauten"
      contains: "Dateityp"
    - path: "php/l10n/fr.js"
      provides: "23 neue Schlüssel mit echten Akzenten"
      contains: "Feuilles de calcul"
    - path: "backend/tests/test_admin_ui_contract.py"
      provides: "Schlüsselzahl 197 mit Begründungsabsatz, drei neue G2-Ausnahmen mit Begründung"
      contains: "197"
    - path: "docs/l10n-french.md"
      provides: "Zähltabelle auf 197, die drei neuen Ausnahmen, die neuen Zeilen der großen Tabelle"
      contains: "197"
  key_links:
    - from: "backend/tests/test_admin_ui_contract.py"
      to: "php/l10n/de.json"
      via: "harte Schlüsselzahl und Schlüsselmengen-Vergleich über alle sechs Dateien"
      pattern: "== 197"
    - from: "docs/l10n-french.md"
      to: "FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY"
      via: "die Ausnahmen stehen an beiden Stellen mit derselben Begründung"
      pattern: "Documents"
---

<objective>
Die 23 neuen Katalogschlüssel kommen in alle sechs Dateien, die harte Zahl im
Gate zieht von 174 auf 197 nach, die drei französischen Gleichwort-Ausnahmen
bekommen ihre Einträge mit Begründung, und `docs/l10n-french.md` zieht mit.

Purpose: 13-RESEARCH nennt die Kataloge den größten unterschätzten Posten dieser
Phase und ausdrücklich den wahrscheinlichsten Grund, aus dem ein sonst fertiger
Plan rot wird. Vier Gates hängen daran: Schlüsselmengen über sechs Dateien,
Textgleichheit der deutschen Zwillinge samt harter Zahl, französische
Vollständigkeit mit ihrer Gleichwort-Falle, und Platzhalter-Parität. Deshalb ist
das hier ein eigener Planschritt und kein Anhängsel.

Output: sechs gepflegte Katalogdateien, die neue Zahl mit ihrem
Begründungsabsatz, drei neue G2-Ausnahmen und die nachgezogene Dokumentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-CONTEXT.md
@.planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-RESEARCH.md
@.planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-PATTERNS.md
@.planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-UI-SPEC.md
@.planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-10-SUMMARY.md

<interfaces>
Die vier Gates in `backend/tests/test_admin_ui_contract.py` und was sie rot
macht:

| Gate | Was rot wird |
|---|---|
| G1 Schluesselmengen | ein Schluessel fehlt in einer der sechs Dateien |
| Deutsche Zwillinge | `de_DE.json` ist nicht TEXTGLEICH mit `de.json` (und `de_DE.js` nicht mit `de.js`), oder die harte Zahl stimmt nicht |
| G2 Franzoesische Vollstaendigkeit | ein franzoesischer Wert ist leer ODER gleich seinem englischen Schluessel, ausser er steht namentlich in `FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY` |
| G3 Platzhalter-Paritaet | ein Wert traegt andere `%s`/`%1$s`-Direktiven als sein Schluessel |
| Prosa-Gate | ein Gedankenstrich oder ein Emoji in einem Katalog |

Die Ausnahmeliste heute, woertlich:

```python
FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY = {
    "Findling": "the name of the app, the same word in all three languages",
    "Page %s": "Page is the same word in French, and a difference would be a loss",
}
```

Der Begruendungsabsatz der harten Zahl, woertlich (gekuerzt):

```
    The hard number below stands at 174 and stood at 173 until 10.09.2026. It
    rose by exactly one key, and the key is the sentence the result page says
    for a run that was handed candidates and kept none of them: ...
    Without this paragraph the next reader takes a raised number for sloppiness
    and lowers it again.
```

Das Dateiformat: `de.json` und `fr.json` sind JSON mit einem
`translations`-Objekt, `de.js` und `fr.js` rufen `OC.L10N.register`. Die Werte
tragen ECHTE Umlaute und ECHTE Akzente.

Die 23 Schluessel, woertlich aus 13-UI-SPEC "Copywriting Contract":

| # | EN (Quelle) | DE | FR |
|---|---|----|----|
| 1 | `File type` | Dateityp | Type de fichier |
| 2 | `PDF` | PDF | PDF |
| 3 | `Documents` | Dokumente | Documents |
| 4 | `Spreadsheets` | Tabellen | Feuilles de calcul |
| 5 | `Presentations` | Präsentationen | Présentations |
| 6 | `Images` | Bilder | Images |
| 7 | `Text` | Text | Texte |
| 8 | `Time range` | Zeitraum | Période |
| 9 | `Today` | Heute | Aujourd'hui |
| 10 | `Last 7 days` | Letzte 7 Tage | 7 derniers jours |
| 11 | `Last 30 days` | Letzte 30 Tage | 30 derniers jours |
| 12 | `This year` | Dieses Jahr | Cette année |
| 13 | `Remove filter %s` | Filter %s entfernen | Retirer le filtre %s |
| 14 | `Reset all filters` | Alle Filter zurücksetzen | Réinitialiser tous les filtres |
| 15 | `Sort by` | Sortieren nach | Trier par |
| 16 | `Relevance` | Relevanz | Pertinence |
| 17 | `Last modified` | Zuletzt geändert | Dernière modification |
| 18 | `Oldest first` | Älteste zuerst | Les plus anciens d'abord |
| 19 | `Modified on %s` | Geändert am %s | Modifié le %s |
| 20 | `%1$s in %2$s, modified on %3$s` | %1$s in %2$s, geändert am %3$s | %1$s dans %2$s, modifié le %3$s |
| 21 | `No results with the active filters` | Keine Treffer mit den aktiven Filtern | Aucun résultat avec les filtres actifs |
| 22 | `Remove a filter or widen the time range.` | Entfernen Sie einen Filter oder erweitern Sie den Zeitraum. | Retirez un filtre ou élargissez la période. |
| 23 | `Reset filters` | Filter zurücksetzen | Réinitialiser les filtres |

Drei davon sind im Franzoesischen wortgleich mit dem englischen Schluessel und
loesen G2 aus: `PDF`, `Documents`, `Images`.

Die Zusammensetzung der 23, fuer den Begruendungsabsatz: 10 Chips,
3 Zeilenbeschriftungen, 3 Sortierlinks, 2 Zuruecksetzen-Varianten, 2 Datumsformen,
3 Leerzustand.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die 23 Schlüssel in sechs Dateien</name>
  <files>php/l10n/de.json, php/l10n/de.js, php/l10n/de_DE.json, php/l10n/de_DE.js, php/l10n/fr.json, php/l10n/fr.js</files>
  <read_first>
    - php/l10n/de.json und php/l10n/de.js (Aufbau, Reihenfolge der bestehenden Schlüssel, Einrückung)
    - php/l10n/fr.js (die `OC.L10N.register`-Form)
    - php/templates/search.php (nach Plan 13-09: die englischen Quell-Strings, wie sie wirklich im Baum stehen)
    - .planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-UI-SPEC.md (Abschnitt "Copywriting Contract" vollständig)
  </read_first>
  <action>
    Trage die 23 Schlüssel aus der Tabelle in `<interfaces>` in alle sechs
    Dateien ein.

    Regeln, die aus den Gates folgen und ohne Ausnahme gelten:
    - Die englischen Schlüssel müssen ZEICHENGLEICH mit den `$l->t()`-Argumenten
      in `php/templates/search.php` sein. Nicht abschreiben, sondern aus dem
      Template übernehmen; ein abweichendes Leerzeichen ist ein fehlender
      Schlüssel.
    - `de_DE.json` ist textgleich mit `de.json`, `de_DE.js` textgleich mit
      `de.js`. Textgleich heißt textgleich, nicht schlüsselgleich.
    - Alle sechs Dateien tragen dieselbe Schlüsselmenge.
    - Die Werte tragen echte Umlaute und echte Akzente, genau wie in der Tabelle;
      die Tabelle wird kopiert und nicht abgeschrieben.
    - Platzhalter-Parität: Schlüssel 13 und 19 tragen in allen drei Sprachen
      genau ein `%s`, Schlüssel 20 genau `%1$s`, `%2$s` und `%3$s`.
    - Kein Gedankenstrich, kein Emoji, kein Backtick in einem der 46 neuen Werte.
    - Das gesperrte Wort des Vokabular-Gates kommt in keinem Wert vor.
    - `Présentations` trägt seinen Akzent; das ist zugleich die korrekte
      Übersetzung und die Auflösung des Gleichwort-Konflikts.

    Nach dem Eintragen: die Schlüsselzahl je Datei nachzählen. Sie muss in allen
    sechs 197 betragen. Weicht eine ab, ist ein Schlüssel verrutscht und nicht
    die Zahl falsch.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python - <<'PY'
import json, io, re
base = 'php/l10n/'
js_keys = lambda p: set(re.findall(r'^\s*"((?:[^"\\]|\\.)*)"\s*:', io.open(base+p, encoding='utf-8').read(), re.M))
j = {p: set(json.load(io.open(base+p, encoding='utf-8'))['translations']) for p in ('de.json','de_DE.json','fr.json')}
k = {p: js_keys(p) for p in ('de.js','de_DE.js','fr.js')}
alle = {**j, **k}
for p, keys in alle.items():
    assert len(keys) == 197, (p, len(keys))
assert len(set(map(frozenset, alle.values()))) == 1, 'die Kataloge sind nicht deckungsgleich'
assert io.open(base+'de.json',encoding='utf-8').read() == io.open(base+'de_DE.json',encoding='utf-8').read()
assert io.open(base+'de.js',encoding='utf-8').read() == io.open(base+'de_DE.js',encoding='utf-8').read()
fr = json.load(io.open(base+'fr.json', encoding='utf-8'))['translations']
for key in ('File type','Spreadsheets','Presentations','Sort by','Modified on %s'):
    assert key in fr and fr[key] and fr[key] != key, key
for text in (io.open(base+p, encoding='utf-8').read() for p in alle):
    assert '\u2014' not in text and '\u2013' not in text
print('GRUEN')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Alle sechs Dateien tragen je genau 197 Schlüssel und dieselbe Schlüsselmenge
    - `de_DE.json` ist byteweise textgleich mit `de.json`, `de_DE.js` mit `de.js`
    - Die englischen Schlüssel sind zeichengleich mit den `$l->t()`-Argumenten in `php/templates/search.php`
    - Die deutschen und französischen Werte entsprechen wörtlich der Tabelle, mit echten Umlauten und Akzenten
    - Platzhalter-Parität für die Schlüssel 13, 19 und 20 in allen drei Sprachen
    - Kein Gedankenstrich, kein Emoji, kein Backtick und nicht das gesperrte Wort in einem der neuen Werte
  </acceptance_criteria>
  <done>Die Seite spricht in allen drei Sprachen vollständig, und die sechs Dateien stehen im Gleichschritt.</done>
</task>

<task type="auto">
  <name>Task 2: Die harte Zahl und die drei G2-Ausnahmen</name>
  <files>backend/tests/test_admin_ui_contract.py</files>
  <read_first>
    - backend/tests/test_admin_ui_contract.py (`FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY` mit seinem Kommentar über Liste statt Schwelle; `test_the_german_catalogue_covers_both_german_language_codes` vollständig mit dem Docstring-Absatz zur harten Zahl; `scan_french_completeness`; der Selbsttest, der prüft, dass jede Ausnahme noch einen Schlüssel im Baum hat)
    - php/l10n/fr.json (nach Task 1)
    - .planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-RESEARCH.md (Befund 11 vollständig)
    - .planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-UI-SPEC.md (Abschnitt "Katalog-Gates: was dieser Vertrag auslöst")
  </read_first>
  <action>
    In `backend/tests/test_admin_ui_contract.py`:

    1. Die harte Zahl im Zwillings-Gate zieht von 174 auf 197. Der bestehende
       Begründungsabsatz für den Sprung von 173 auf 174 BLEIBT wörtlich stehen,
       und darunter kommt ein neuer Absatz, der den Sprung auf 197 begründet: 23
       Schlüssel der Phase 13, aufgeschlüsselt als 10 Chips, 3
       Zeilenbeschriftungen, 3 Sortierlinks, 2 Zurücksetzen-Varianten, 2
       Datumsformen und 3 Leerzustand; dazu der Verweis auf FILT-01 bis FILT-04
       und auf 13-UI-SPEC. Der Absatz schreibt seine eigene Fortschreibung vor,
       wie der bestehende es tut: ohne ihn nimmt der nächste Leser eine erhöhte
       Zahl für Schlamperei und senkt sie wieder.

    2. `FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY` bekommt drei Einträge, jeder mit
       seiner Begründung als Wert, in der Form der beiden bestehenden:
       - `PDF`: der Eigenname einer Dateiart, in allen drei Sprachen dasselbe
         Kürzel
       - `Documents`: im Französischen wortgleich mit dem englischen Quellwort;
         eine künstliche Abweichung wäre eine falsche Übersetzung
       - `Images`: dieselbe Lage
       Der bestehende Kommentar über der Liste (Liste statt Schwelle, die
       Begründung reist mit dem Schlüssel, ein dritter Eintrag muss begründet und
       nicht gezählt werden) bleibt stehen und wird um den Satz ergänzt, dass die
       Liste mit dieser Phase von zwei auf fünf wächst und warum genau diese drei.

    3. Der Selbsttest, der prüft, dass jede Ausnahme noch einen Schlüssel im Baum
       hat, bleibt unverändert und wird dadurch für die drei neuen Einträge
       mitgeprüft.

    4. Keine weitere Zahl, keine Schwelle, kein `skip`. Wenn ein Gate nach diesen
       Änderungen noch rot ist, liegt der Fehler in den Katalogen aus Task 1 und
       nicht in der Zahl.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/backend" && uv run pytest tests/test_admin_ui_contract.py -q && uv run ruff check . && uv run ruff format --check . && uv run pyright && grep -c '== 174' tests/test_admin_ui_contract.py | grep -qx 0 && grep -q '"PDF"' tests/test_admin_ui_contract.py && grep -q '"Documents"' tests/test_admin_ui_contract.py && grep -q '"Images"' tests/test_admin_ui_contract.py && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_admin_ui_contract.py -q` läuft vollständig grün, alle vier Katalog-Gates eingeschlossen
    - Die harte Zahl steht auf 197, die Zeichenkette `== 174` kommt nicht mehr vor
    - Der bestehende Begründungsabsatz für 173 auf 174 steht unverändert, darunter der neue Absatz für 174 auf 197 mit der Aufschlüsselung der 23 Schlüssel
    - `FRENCH_VALUES_THAT_MAY_EQUAL_THEIR_KEY` hat genau fünf Einträge, die drei neuen je mit Begründung als Wert
    - Es wurde keine Schwelle eingeführt und kein Test übersprungen
    - `ruff check .`, `ruff format --check .`, `pyright` grün
  </acceptance_criteria>
  <done>Die Gates stehen wieder auf Grün, und die erhöhte Zahl trägt ihren Grund bei sich.</done>
</task>

<task type="auto">
  <name>Task 3: docs/l10n-french.md zieht nach</name>
  <files>docs/l10n-french.md</files>
  <read_first>
    - docs/l10n-french.md (die Zähltabelle am Kopf, der Abschnitt über die G2-Ausnahmen, die große Schlüsseltabelle und ihr Zeilenformat)
    - backend/tests/test_admin_ui_contract.py (nach Task 2)
    - .planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-UI-SPEC.md (Abschnitt "Copywriting Contract")
  </read_first>
  <action>
    In `docs/l10n-french.md`:

    1. Die Zähltabelle zieht von 174 auf 197 nach, im bestehenden Aufbau (Größe,
       Wert, Recherchestand), plus ein Absatz, der die Erhöhung mit denselben
       Worten begründet wie der Gate-Docstring: 23 Schlüssel der Filter- und
       Sortierleiste, aufgeschlüsselt.
    2. Der Abschnitt über die Ausnahmen für das Vollständigkeits-Gate G2 bekommt
       die drei neuen Einträge mit derselben Begründung wie im Gate. Die Zahl der
       Ausnahmen steigt sichtbar von zwei auf fünf; das ist Absicht, weil die
       Liste wachsen soll und eine Schwelle nicht.
    3. Die große Tabelle bekommt die 23 neuen Zeilen im bestehenden Format
       (Schlüssel, DE, FR), in der Reihenfolge des Copywriting Contract.
    4. Kein Gedankenstrich, kein Emoji, und das gesperrte Wort kommt nicht vor.
       Die Datei liegt in `docs/` und ist damit öffentlich.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/l10n-french.md && grep -q '197' "$f" && grep -q 'Feuilles de calcul' "$f" && grep -q 'Les plus anciens' "$f" && python -c "
import io
t=io.open('docs/l10n-french.md',encoding='utf-8').read()
assert '\u2014' not in t and '\u2013' not in t
print('GRUEN')" && cd backend && uv run pytest tests/test_admin_ui_contract.py -q && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Die Zähltabelle nennt 197 und hat einen Absatz, der die Erhöhung begründet
    - Die drei neuen G2-Ausnahmen stehen mit derselben Begründung wie im Gate
    - Die große Tabelle hat 23 neue Zeilen im bestehenden Format, in der Reihenfolge des Copywriting Contract
    - Kein Gedankenstrich, kein Emoji, nicht das gesperrte Wort
    - `uv run pytest tests/test_admin_ui_contract.py -q` bleibt grün
  </acceptance_criteria>
  <done>Die Dokumentation sagt dasselbe wie das Gate, und wer die Zahl das nächste Mal liest, findet ihren Grund an beiden Stellen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Katalogwert → Seite | Ein Katalogwert wird durch `p()` ausgegeben; er ist Text und kein Markup |
| `docs/` → Öffentlichkeit | Die Datei wird veröffentlicht; das Vokabular-Gate gilt |
| Gate-Zahl → nächster Leser | Eine Zahl ohne Grund wird beim nächsten Umbau gesenkt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-52 | Tampering | Platzhalter in einer Übersetzung verändert | mitigate | G3 Platzhalter-Parität bleibt aktiv und wird für die Schlüssel 13, 19 und 20 im Verify eigens geprüft |
| T-13-53 | Repudiation | erhöhte Schlüsselzahl wird für Schlamperei gehalten und gesenkt | mitigate | Neuer Begründungsabsatz im Gate-Docstring UND in `docs/l10n-french.md`, mit Aufschlüsselung der 23 Schlüssel |
| T-13-54 | Tampering | G2 wird durch eine Schwelle statt durch benannte Ausnahmen entschärft | mitigate | Drei namentliche Einträge mit Begründung; Acceptance-Criterion schließt eine Schwelle und ein `skip` ausdrücklich aus |
| T-13-55 | Information Disclosure | gesperrtes Vokabular in einem öffentlichen Erzeugnis | mitigate | Prüfung über alle sechs Kataloge und über `docs/l10n-french.md` vor dem Push |
| T-13-56 | Information Disclosure | eine Übersetzung nennt eine Zahl oder eine Berechtigung | accept | Keiner der 23 Werte nennt eine Trefferzahl oder eine Berechtigung; der Copywriting Contract ist darauf geprüft |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- Alle vier Katalog-Gates sind grün: Schlüsselmengen, deutsche Zwillinge samt Zahl, französische Vollständigkeit, Platzhalter-Parität
- Das Prosa-Gate ist grün: kein Gedankenstrich, kein Emoji in einem Katalog
- Backend-Gates: `uv run ruff check .`, `uv run ruff format --check .` (ganzes Backend), `uv run pyright`, `uv run python -m pytest` vollständig grün, also auch die Gates, die seit 13-09 auf diesen Plan gewartet haben
- Vokabular-Gate: das gesperrte Wort kommt in keinem Katalog und nicht in `docs/l10n-french.md` vor
- Executor-Muster v1.1: sequenziell auf main, Basis-SHA pinnen, Git-Identität NIEMALS per `-c` übersteuern
- FILT-01 bis FILT-04 werden NICHT als erfüllt markiert: 13-12 und 13-13 stehen noch aus
</verification>

<success_criteria>
Sechs Katalogdateien mit je 197 deckungsgleichen Schlüsseln, echte Umlaute und
Akzente in allen 46 neuen Werten, die harte Zahl mit ihrem Begründungsabsatz,
drei benannte G2-Ausnahmen statt einer Schwelle, und eine Dokumentation, die
dasselbe sagt wie das Gate.
</success_criteria>

<output>
Create `.planning/phases/13-filter-und-sortierung-auf-der-ergebnisseite/13-11-SUMMARY.md` when done
</output>
