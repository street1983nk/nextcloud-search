---
phase: 05-h-rtung-und-store-einreichung-v1-0
plan: 19
type: execute
wave: 7
depends_on: [05-08, 05-09, 05-16, 05-18]
files_modified:
  - docs/testing.md
autonomous: false
requirements: [SRCH-04, PKG-05]
must_haves:
  truths:
    - "D-22: Die Gastnutzer-Probe ist von Hand durchgefuehrt und ihr Ergebnis dokumentiert, ohne dass die guests-App in die CI-Matrix wandert"
    - "docs/testing.md beschreibt die Gate-Landschaft dieser Phase vollstaendig, jedes Gate mit dem, was es nicht beweist"
    - "Der Abschnitt The gap ist durch die Abbildung der zwoelf Verhaltensweisen auf ihre Tests ersetzt"
    - "D-26: Der Tag v1.0.0 ist gesetzt, und die Release-Strecke ist darauf gelaufen"
    - "D-09: Der Zustand ist einreichungsbereit, also ein Klick bis zur Abgabe, und die Abgabe selbst bleibt Phase 6"
  artifacts:
    - path: "docs/testing.md"
      provides: "Gate-Landschaft nach Phase 5, Grenzen je Gate, und die Abbildung der zwoelf PHPUnit-Verhaltensweisen"
      contains: "search-parity"
  key_links:
    - from: "docs/testing.md"
      to: "backend/tests"
      via: "jedes neue Gate der Phase steht in der Tabelle mit dem, was es verhindert"
      pattern: "test_"
    - from: "git tag v1.0.0"
      to: ".github/workflows/release.yml"
      via: "der Tag loest den Release-Lauf aus, der die signierten Archive und das GitHub-Release erzeugt"
      pattern: "v1.0.0"
---

<objective>
Die Phase wird abgenommen: eine Probe, die keine Maschine machen kann, eine Doku, die sagt was bewiesen ist und was nicht, und der Tag, der aus dem Repo eine Abgabe macht.

Purpose: Drei Dinge fehlen zum Abschluss. Erstens die Gastnutzer-Probe aus D-22: Szenario 6 wird doppelt belegt, in CI als gruppenloser Minimal-Nutzer und zusaetzlich als Gastnutzer ueber die guests-App von Hand, ausdruecklich ohne guests-Abhaengigkeit in der CI-Matrix. Zweitens die Gate-Landschaft: diese Phase hat sechs neue Gates und drei neue Jobs gebaut, und `docs/testing.md` ist die Datei, in der steht, was jeder davon beweist und was nicht. Sie fuehrt ausserdem noch den Abschnitt "The gap" mit zwoelf ungetesteten Verhaltensweisen, die es seit Plan 05-16 nicht mehr gibt. Drittens der Tag: D-26 setzt ihn am Ende dieser Phase, damit "ein Klick bis zur Abgabe" woertlich wahr ist und nicht ungefaehr.

Nach diesem Plan gibt es etwas Neues: einen Stand, den man einreichen kann, und eine Datei, die einem Fremden erklaert, was daran belegt ist.

Output: das Ergebnis der Gastnutzer-Probe, die aktualisierte Gate-Landschaft, die Abnahme durch den Owner und der Tag v1.0.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-CONTEXT.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-RESEARCH.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-09-SUMMARY.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-16-SUMMARY.md
@.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-18-SUMMARY.md

<interfaces>
D-22 im Wortlaut: Szenario 6 wird DOPPELT belegt. Als CI-Szenario ein gruppenloser
Minimal-Nutzer (keine Gruppen, kein Team-Folder-Zugang, genau ein empfangener View-only-Share,
sieht nur diesen einen Inhalt), das ist seit Plan 05-09 im Job search-parity. Zusaetzlich ein
Gastnutzer ueber die guests-App als manuelle Probe vor der Einreichung, und ausdruecklich KEINE
guests-Abhaengigkeit in der CI-Matrix.
Die guests-App liegt als 4.9.0 fuer NC 32, 33 und 34 vor (Store-API geprueft). Die
Vergleichsprozedur ist dieselbe wie im Job: derselbe Nutzer fragt beide Provider mit
ausdruecklichem limit, und die fileid-Mengen werden symmetrisch verglichen; das Werkzeug dafuer
ist scripts/ci/parity_diff.py aus Plan 05-09 und laeuft auch von Hand.
Der Trefferdeckel gilt auch hier: ohne ausdruecklichen limit-Parameter vergleicht die Probe
zwei auf fuenf gekuerzte Listen. Vor dem ersten Aufruf
occ config:app:set core unified_search_max_results_per_request --value=100.

Was diese Phase an Gates und Jobs gebaut hat und was in docs/testing.md gehoert:
  Gates in backend/tests: test_uninstall_contract.py (Plan 05-02), test_load_corpus.py (05-05),
  test_lockstep_versions.py (05-07), test_parity_diff.py (05-09), test_workflow_pins.py (05-13),
  test_store_metadata.py (05-17, erweitert in 05-18).
  Jobs: deploy-harp (05-01, erweitert 05-08), search-parity (05-09), phpunit (05-15, erweitert
  05-16), release (05-18), plus pgsql als dritter Dialekt von index-search-e2e (05-13).
  Berichte: docs/performance.md (05-10, 05-12, 05-14), docs/uninstall.md (05-02, 05-08),
  docs/store-listing.md (05-17).
Der Abschnitt "The gap" in docs/testing.md:122-162 listet zwoelf ungetestete Verhaltensweisen.
Plan 05-16 hat die Abbildung jeder Nummer auf einen Testnamen erzeugt; sie ersetzt diesen
Abschnitt.
Der Ton der Datei ist gesetzt und bleibt: jede Aussage steht mit ihrer Grenze, und ein
Abnahmetest, der als mehr gelesen wird als er beweist, ist schlimmer als keiner
(docs/testing.md:75-121).

D-26 im Wortlaut: Der Release-Tag v1.0.0 wird ENDE PHASE 5 gesetzt; signierte Releases
entstehen in Phase 5, denn einreichungsbereit heisst ein Klick bis zur Abgabe. Der
release.yml-Trigger auf v-Tags ist hier gewollt. Die Einreichung selbst bleibt gebuendelt nach
Phase 6 (D-09 unveraendert).
Der Nebeneffekt steht in Open Question 5 der Recherche: ein oeffentliches Release 1.0.0, das
noch nicht im Store ist, koennte gefunden und installiert werden. Die Alternative ist ein
Vorabversions-Tag; das semver-Muster der XSD erlaubt Vorabversionen, und der Store wuerde es
als Beta fuehren. Das ist eine Owner-Entscheidung und steht im Checkpoint dieses Plans.
Ein Tag loest ausserdem docker.yml aus, das den Tag gegen beide Versionsangaben und den
image-tag prueft; alle drei stehen seit Plan 05-17 auf 1.0.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Gastnutzer-Probe von Hand, mit demselben Vergleich wie in CI</name>
  <files>docs/testing.md</files>
  <read_first>
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-09-SUMMARY.md (der Aufbau der sechs Szenarien, die Marker-Regel und die Aufrufform von parity_diff.py)
    - scripts/ci/parity_diff.py (die CLI: scenario, native, findling, expect-min)
    - .github/workflows/integration.yml Job search-parity (die ask-Funktion und der hochgesetzte Serverdeckel, damit die Probe dieselben Aufrufe macht)
    - docs/dev-setup.md (die Dev-Instanz, ihr Port und ihre Nutzer)
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-CONTEXT.md D-21 und D-22
    - docs/testing.md Zeilen 49-121 (wie die bestehenden Abnahmen beschrieben sind, samt der Regel, zu jeder Aussage ihre Grenze zu nennen)
  </read_first>
  <action>
    Auf der Dev-Instanz, nicht in CI: die guests-App installieren und aktivieren
    (occ app:install guests, occ app:enable guests), einen Gastnutzer anlegen, und ihm genau
    einen View-only-Share auf eine Marker-Datei geben, deren Marker im Dateinamen UND im Inhalt
    steht. Den Serverdeckel vorher hochsetzen, sonst vergleicht die Probe zwei gekuerzte Listen.

    Dann dieselbe Prozedur wie im Job, von Hand: als Gastnutzer beide Provider mit
    ausdruecklichem limit fragen, die beiden Antworten in Dateien schreiben, und
    scripts/ci/parity_diff.py mit einem Szenarionamen und einer Mindestzahl darauf laufen
    lassen. Zusaetzlich zwei Gegenrichtungen pruefen, weil sie bei einem Gastnutzer der
    interessante Teil sind: der Gast findet den fremden Marker eines anderen Nutzers nicht
    (erwartete Menge leer, in beiden Providern), und der Eigentuemer findet seine Datei
    weiterhin.

    Das Ergebnis kommt als eigener Abschnitt in docs/testing.md, unter den bestehenden
    Abnahmen und in derselben Form: was geprueft wurde, mit welchen Befehlen, was
    herauskam, und was es NICHT beweist. Die Grenzen hier sind zwei und werden beide genannt:
    die Probe ist eine Momentaufnahme und kein Dauergate, und sie gilt fuer die guests-Version,
    die auf der Dev-Instanz lag, samt Versionsnummer. Der Grund fuer die Handarbeit gehoert
    dazu: D-22 haelt die guests-App ausdruecklich aus der CI-Matrix, weil eine
    Fremd-App-Abhaengigkeit in einem Rechte-Gate eine zweite Fehlerquelle waere.

    Faellt die Probe negativ aus, also findet der Gast in einem der beiden Provider mehr oder
    weniger als im anderen, ist das ein Sicherheits- oder Funktionsbefund und wird als solcher
    benannt, mit den beiden Mengen; die Phase ist dann nicht abnahmereif und der Befund geht in
    einen eigenen Plan. Ein negatives Ergebnis wird nicht durch eine Anpassung der Erwartung
    geheilt.

    Die Doku in der Sprache, in der docs/testing.md heute geschrieben ist. Keine Em-Dashes,
    keine Emojis.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "s=open('docs/testing.md',encoding='utf-8').read(); assert 'guests' in s; assert 'parity' in s.lower(); assert '—' not in s and '–' not in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Die Probe ist durchgefuehrt, und die Befehle samt Ausgaben stehen protokolliert in der Zusammenfassung.
    - Der Vergleich lief ueber `scripts/ci/parity_diff.py` mit ausdruecklichem limit und hochgesetztem Serverdeckel.
    - Drei Feststellungen sind belegt: der Gast findet seinen einen Inhalt in beiden Providern, er findet den fremden Marker in keinem der beiden, und der Eigentuemer findet seine Datei weiterhin.
    - `docs/testing.md` enthaelt den Abschnitt zur Gastnutzer-Probe mit Befehlen, Ergebnis, der guests-Version und beiden Grenzen.
    - Ein negatives Ergebnis waere als Befund benannt und nicht durch eine Anpassung der Erwartung geheilt; falls es eintrat, steht der Befund in der Zusammenfassung.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in der Datei.
  </acceptance_criteria>
  <done>Die zweite Haelfte von Szenario 6 ist belegt: ein echter Gastnutzer sieht in Findling genau das, was die native Suche ihm zeigt, und die Probe steht mit ihren Grenzen in der Testdoku.</done>
</task>

<task type="auto">
  <name>Task 2: Die Gate-Landschaft nach Phase 5, und The gap verschwindet</name>
  <files>docs/testing.md</files>
  <read_first>
    - docs/testing.md (die ganze Datei: die Gate-Tabelle in :39-47, der Abschnitt zu den Integrationsjobs :49-73, die drei Abnahmen mit ihren Nicht-Aussagen :75-121, und der Abschnitt The gap :122-178)
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-16-SUMMARY.md (die vollstaendige Abbildung der zwoelf Verhaltensweisen auf Testnamen)
    - die Zusammenfassungen der Plaene 05-01, 05-02, 05-05, 05-07, 05-08, 05-09, 05-13, 05-15, 05-17 und 05-18 (welche Gates und Jobs entstanden sind und was jedes belegt)
    - backend/tests (die tatsaechlich vorhandenen Gate-Dateien, damit die Tabelle nichts nennt, was es nicht gibt)
  </read_first>
  <action>
    docs/testing.md auf den Stand nach dieser Phase bringen, in vier Schritten und ohne den Ton
    der Datei zu aendern.

    Erstens die Gate-Tabelle erweitern, je Zeile mit dem, was das Gate VERHINDERT, nicht mit
    dem, was es tut:
      test_uninstall_contract.py: ein Disable, das Daten loescht, und eine Raeumung an zwei
      Stellen im Code.
      test_lockstep_versions.py: zwei Haelften, die ueber ihr Protokoll uneins sind, weil eine
      Versionszeile allein bewegt wurde.
      test_store_metadata.py: eine Store-Beschreibung, die eine Sprache verliert, zu lang wird,
      ein verbotenes Zeichen traegt, ein leeres Element hat oder auf ein Bild zeigt, das es
      nicht gibt.
      test_parity_diff.py: ein Paritaetsvergleich, der leere Mengen oder eine Richtung fuer
      Erfolg haelt.
      test_workflow_pins.py: eine Pinnung, deren Kommentar luegt, ein beweglicher Verweis, und
      ein Job ohne Deadline.
      test_load_corpus.py: ein Lastkorpus, der sich nicht reproduzieren laesst.
    Zweitens die Jobs beschreiben, jeder mit seiner Nicht-Aussage, denn das ist der Wert dieser
    Datei:
      deploy-harp: belegt install, run und uninstall auf dem Weg einer echten Installation,
      ueber NC 32 bis 35, samt Volume und Tabellen. Es belegt nicht die Weboberflaeche der
      App-Verwaltung, also nicht die Checkbox in NC 32 und 33, und nicht, dass das Image
      entfernt wird, weil es bauartbedingt liegen bleibt.
      search-parity: belegt Sichtbarkeits-Paritaet ueber sechs Szenarien in beide Richtungen,
      mit ausdruecklichem limit. Es belegt kein Ranking, keine Trefferzahl und nichts ueber
      Gastnutzer; letzteres ist die manuelle Probe aus Task 1.
      phpunit: belegt zwoelf benannte Logikeigenschaften der PHP-Haelfte mit Mocks. Es belegt
      nichts ueber das Zusammenspiel mit einer echten Instanz; das tun die Integrationsjobs.
      release: belegt, dass ein Archiv gebaut, validiert, signiert und in der Groesse geprueft
      werden kann. Es belegt nicht, dass der Store es annimmt; das ist Phase 6.
      index-search-e2e mit pgsql: belegt, dass der Dialekt der Zielumgebung durchlaeuft und die
      Queue leer wird. Es belegt nichts ueber Postgres unter Last; das tut der Bericht.
    Drittens den Abschnitt The gap ersetzen. Statt zwoelf ungetesteter Verhaltensweisen steht
    dort ab jetzt die Abbildung aus Plan 05-16: je Nummer die Eigenschaft in einem Satz, der
    Testname und die Datei. Die Einleitung des Abschnitts wird umgeschrieben: er hiess "The
    gap", weil es eine war; jetzt ist er die Liste der Eigenschaften, die die Suite haelt, und
    der Satz darueber sagt, dass die Luecke mit Phase 5 geschlossen wurde und wodurch.
    Viertens die drei Berichte verlinken, die diese Phase erzeugt hat (performance, uninstall,
    store-listing), je mit einem Satz, was darin steht, damit die Testdoku der Einstieg bleibt.

    Kein Gate und kein Job wird genannt, das es nicht gibt: jede Zeile wird gegen den
    Arbeitsbaum geprueft.

    Sprache wie heute in der Datei. Keine Em-Dashes, keine Emojis.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; python -c "
s=open('docs/testing.md',encoding='utf-8').read()
for t in ('test_uninstall_contract','test_lockstep_versions','test_store_metadata','test_parity_diff','test_workflow_pins','test_load_corpus','deploy-harp','search-parity','phpunit','release'):
    assert t in s, t
assert 'The gap' not in s, 'der Abschnitt The gap steht noch drin'
assert 'performance.md' in s and 'uninstall.md' in s
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Die Gate-Tabelle nennt alle sechs neuen Gates, jedes mit dem, was es verhindert.
    - Alle fuenf neuen bzw. erweiterten Jobs sind beschrieben, jeder mit einer ausdruecklichen Nicht-Aussage.
    - Der Abschnitt "The gap" existiert nicht mehr; an seiner Stelle steht die Abbildung aller zwoelf Nummern auf Testname und Datei.
    - Jede genannte Gate-Datei existiert wirklich unter `backend/tests` (per Abgleich geprueft), und jeder genannte Job existiert in einer Workflow-Datei.
    - Die drei Berichte dieser Phase sind verlinkt, je mit einem Satz Inhalt.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in der Datei.
  </acceptance_criteria>
  <done>Ein Fremder liest eine Datei und weiss danach, welche Eigenschaften dieses Projekt maschinell haelt und welche ausdruecklich nicht.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Abnahme der Phase und der Tag v1.0.0</name>
  <what-built>Die Phase ist inhaltlich fertig. Der Lebenszyklus ist auf NC 32 bis 35 ueber HaRP belegt, die Sichtbarkeits-Paritaet laeuft als Dauergate ueber sechs Szenarien und ist fuer einen echten Gastnutzer zusaetzlich von Hand geprueft, der Volllauf ueber 50.000 Dateien ist auf einer 4-GB-ARM-Box ohne OOM durchgelaufen und dokumentiert, das Uninstall-Verhalten ist gemessen und geraeumt wird nur nach ausdruecklicher Absicht, alle offenen Review-Reste und der PHPUnit-Rueckstand sind abgearbeitet, die Store-Texte stehen dreisprachig mit Privacy-Absatz, und ein Workflow-Lauf erzeugt beide signierten Archive. Was fehlt, ist die Abnahme und der Tag.</what-built>
  <how-to-verify>
    1. Die vier Erfolgskriterien der Phase gegen die Belege halten. Die Belege liegen alle im
       Repo, und die Zusammenfassungen der Plaene nennen sie namentlich:
       Kriterium 1, Volllauf ohne OOM mit dokumentierter RSS-Kurve: docs/performance.md.
       Kriterium 2, Paritaetstest ueber sechs Szenarien als Dauergate: der Job search-parity,
       plus die Gastnutzer-Probe in docs/testing.md.
       Kriterium 3, Install, Run und Uninstall auf compose und AIO ueber HaRP, NC 32 bis 34
       (mit 35 nach D-23): der Job deploy-harp, docs/uninstall.md, und der AIO-Teil im
       Messbericht.
       Kriterium 4, gelesen als einreichungsbereit nach D-09: die Artefakte des Release-Laufs,
       docs/store-listing.md und beide info.xml auf 1.0.0.
    2. Entscheiden, welche Form der Tag hat. Zwei Wege, und beide sind vertretbar:
       a) v1.0.0 als regulaerer Tag. Dann ist der Store-Upload in Phase 6 wirklich ein Klick,
          das Image liegt unter dem Tag, den beide info.xml nennen, und es gibt ein
          oeffentliches GitHub-Release. Nebeneffekt: das Release kann gefunden und installiert
          werden, bevor es im Store ist.
       b) v1.0.0-rc.1 als Vorabversion. Das semver-Muster der Store-XSD erlaubt es, und der
          Store wuerde es als Beta fuehren. Dann ist der Nebeneffekt entschaerft, aber Phase 6
          muss einen zweiten Tag schneiden.
       D-26 nennt v1.0.0; die Entscheidung fuer eine Vorabversion waere eine Aenderung daran und
       braucht deshalb diese ausdrueckliche Freigabe.
    3. Nach der Freigabe wird der Tag gesetzt und gepusht. Das loest zwei Laeufe aus:
       docker.yml prueft den Tag gegen beide Versionsangaben und den image-tag und
       veroeffentlicht das Multi-Arch-Image, und release.yml erzeugt die signierten Archive und
       das GitHub-Release. Beide Laeufe werden verfolgt und ihr Ergebnis protokolliert.
    4. Bitte zusaetzlich bestaetigen, dass die Einreichung NICHT jetzt erfolgt. D-08 und D-09
       buendeln sie mit der Semantik aus Phase 6; der Fallback aus D-10 (doch gestaffelt
       einreichen) wird erst gezogen, wenn Phase 6 den Dezember gefaehrdet.
  </how-to-verify>
  <read_first>
    - .planning/ROADMAP.md (die vier Erfolgskriterien der Phase im Wortlaut)
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-CONTEXT.md D-08, D-09, D-10 und D-26
    - .planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-RESEARCH.md Open Question 5 (die beiden Tag-Formen und ihr Nebeneffekt)
    - docs/performance.md, docs/uninstall.md, docs/store-listing.md, docs/testing.md (die Belege, gegen die die vier Kriterien gehalten werden)
    - .github/workflows/docker.yml Zeilen 88-108 (was ein Tag-Lauf prueft)
  </read_first>
  <acceptance_criteria>
    - Die vier Erfolgskriterien sind je einem Beleg im Repo zugeordnet, und die Zuordnung steht in der Zusammenfassung.
    - Die Entscheidung ueber die Tag-Form ist protokolliert; bei einer Abweichung von D-26 steht die Begruendung dabei.
    - Der Tag ist gesetzt und gepusht, und beide ausgeloesten Laeufe (docker.yml und release.yml) sind gruen; die Laufnummern stehen in der Zusammenfassung.
    - Das GitHub-Release traegt beide Archive, und die Download-URLs sind https.
    - `git tag --list` zeigt genau einen neuen Tag, und die drei Versionsangaben in den beiden info.xml stimmen mit ihm ueberein.
    - Es wurde nichts an apps.nextcloud.com hochgeladen; die Einreichung bleibt Phase 6.
  </acceptance_criteria>
  <resume-signal>Schreibe "abgenommen, tag v1.0.0" oder "abgenommen, tag v1.0.0-rc.1", oder nenne das Kriterium, dessen Beleg nicht ueberzeugt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gastnutzer zu Suchergebnis | Ein Konto mit den wenigsten Rechten prueft die Rechtekette am Rand |
| Repo zu Oeffentlichkeit | Ein Tag erzeugt ein oeffentliches Release und ein veroeffentlichtes Image |
| Testdoku zu Leser | Was hier als bewiesen steht, wird geglaubt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-79 | Elevation of Privilege | Ein Gastnutzer sieht in Findling mehr als in der nativen Suche | mitigate | Die manuelle Probe vergleicht symmetrisch mit demselben Werkzeug wie das CI-Gate und prueft ausdruecklich, dass der Gast den fremden Marker in keinem der beiden Provider findet |
| T-05-80 | Repudiation | Die Testdoku behauptet mehr, als die Gates halten | mitigate | Jede Zeile der Gate-Tabelle nennt, was das Gate verhindert, und jeder Job seine ausdrueckliche Nicht-Aussage; jede genannte Datei wird gegen den Arbeitsbaum geprueft |
| T-05-81 | Repudiation | Ein oeffentliches Release 1.0.0 wird vor der Einreichung installiert | mitigate | Die Wahl zwischen regulaerem Tag und Vorabversion liegt beim Owner, mit dem Nebeneffekt ausgeschrieben; keine der beiden Formen wird stillschweigend gewaehlt |
| T-05-82 | Information Disclosure | Der Gastnutzer der Probe bleibt auf der Dev-Instanz stehen | mitigate | Die Probe laeuft auf der Dev-Instanz mit synthetischem Bestand; Konto und Share werden nach der Probe entfernt, und das steht in der Protokollzeile |
| T-05-83 | Tampering | Ein Tag ohne passende Versionsangaben veroeffentlicht ein Image unter falschem Namen | mitigate | docker.yml prueft den Tag gegen beide Versionsangaben und den image-tag; das Lockstep-Gate prueft ihre Gleichheit schon vorher |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Kein Sprachpaket. Neu bezogen wird die guests-App aus dem Nextcloud App Store, ausschliesslich auf der Dev-Instanz und ausdruecklich nicht in der CI-Matrix |
</threat_model>

<verification>
- Die Gastnutzer-Probe ist durchgefuehrt, mit drei Feststellungen und beiden Grenzen in `docs/testing.md`
- `docs/testing.md` nennt alle sechs neuen Gates und alle fuenf neuen oder erweiterten Jobs, je mit Nicht-Aussage, und der Abschnitt "The gap" ist ersetzt
- Jede genannte Gate-Datei und jeder genannte Job existiert wirklich
- Der Tag ist gesetzt, docker.yml und release.yml sind gruen, das GitHub-Release traegt beide Archive
- Es wurde nichts an apps.nextcloud.com hochgeladen
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in `docs/testing.md`
</verification>

<success_criteria>
Die zweite Haelfte von Szenario 6 ist mit einem echten Gastnutzer von Hand belegt, die Testdoku beschreibt die Gate-Landschaft dieser Phase vollstaendig und mit den Grenzen jeder Aussage, der Abschnitt ueber zwoelf ungetestete Verhaltensweisen ist durch ihre Tests ersetzt, die vier Erfolgskriterien sind je einem Beleg zugeordnet und vom Owner abgenommen, und der Tag ist gesetzt, sodass die Abgabe im Store ein Klick ist und in Phase 6 stattfindet.
</success_criteria>

<output>
Create `.planning/phases/05-h-rtung-und-store-einreichung-v1-0/05-19-SUMMARY.md` when done
</output>
