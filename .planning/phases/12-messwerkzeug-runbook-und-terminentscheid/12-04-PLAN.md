---
phase: 12-messwerkzeug-runbook-und-terminentscheid
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py
  - backend/tests/test_measurement_scripts.py
  - .github/workflows/python.yml
autonomous: true
requirements: [MESS-04]

must_haves:
  truths:
    - "Die Fremdbestandszahl entsteht im Container und überquert keine Prozessgrenze, es entsteht also kein Zähl-Orakel"
    - "Die Sonde liefert je Begriff eine ungedeckelte Bestandszahl, die Fensterbelegung beider Ranglisten und, wenn eine eigene Datei-Kennung übergeben wird, deren Rang"
    - "Das neue Laufverzeichnis liegt im engen Geltungsbereich der Messskript-Gates, nicht nur im weiten"
    - "Ein Commit, der ausschliesslich unter docs/measurements/ anlegt, startet die Python-Gates"
  artifacts:
    - path: "docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py"
      provides: "In-Container-Sonde: bestand, fenster_lex, fenster_sem, rang_lex, rang_sem je Begriff"
      contains: "ranked_sides"
      min_lines: 60
    - path: "backend/tests/test_measurement_scripts.py"
      provides: "enger Geltungsbereich um das neue Laufverzeichnis erweitert, Wächter über die gefahrene Fassung 98b"
      contains: "V12_RUN_DIR"
    - path: ".github/workflows/python.yml"
      provides: "Pfadfilter, der den eigenen Prüfgegenstand sieht"
      contains: "docs/measurements/**"
  key_links:
    - from: "docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py"
      to: "backend/src/findling/index/search.py"
      via: "Aufruf von ranked_sides im Prozess, kein zweiter Weg"
      pattern: "from findling.index.search import"
    - from: "backend/tests/test_measurement_scripts.py"
      to: "docs/measurements/2026-09-v12-messung/skripte/"
      via: "NARROW_SCOPE_DIRS-Tupel"
      pattern: "NARROW_SCOPE_DIRS = \\(RUN_DIR, FIX_RUN_DIR, V12_RUN_DIR\\)"
---

<objective>
Die neue Fremdbestands-Messgrösse entsteht als In-Container-Python-Sonde in einem
NEUEN Laufverzeichnis, und die drei Gate-Stellen, die dieses Verzeichnis sonst
nicht sehen, wandern in derselben Welle mit.

Purpose: Erfolgskriterium 1 der Phase. Die alte Vorprüfung misst über die
gedeckelte OCS-Route und liefert für jeden Begriff und jede Tiefe die Zahl 26;
die Schwelle 64 ist damit nie erreichbar und das dreiwertige Urteil fällt auf
zweiwertig zurück (DI-10-02 und DI-11-01). Die Diagnose-Route über den Draht ist
der falsche Weg: sie gibt keine Zahl aus, sondern eine Herkunftsmarke, und eine
Zahl über die Prozessgrenze wäre das Zähl-Orakel aus T-02-93. Die Sonde im
Prozess kennt diese Grenze nicht.

Output: `73-bestand-sonde.py` im neuen Laufverzeichnis, erweiterter enger
Geltungsbereich der Messskript-Gates, geschlossene CI-Pfadfilter-Lücke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-CONTEXT.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md
@.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-PATTERNS.md

<interfaces>
Die Zugänge, die die Sonde benutzt, aus dem eigenen Baum gelesen. Kein zweiter
Weg wird gebaut.

```python
# backend/src/findling/api/resources.py
@dataclass
class ReadSide:
    index: Index
    store: Store
    index_dir: Path
    vectors: VectorStore | None = None

def read_side() -> ReadSide | None: ...   # :273, nie raise, None wenn Index fehlt
def query_model() -> EmbeddingModel: ...  # :254, die geteilte Engine des Prozesses

# backend/src/findling/index/search.py
@dataclass
class SemanticSide:
    vectors: VectorStore
    model: QueryEmbedder
    text: str                              # die rohe Suchzeile, nicht die Query

@dataclass
class RankedSides:
    lexical: list[int]                     # file_ids, keine Punkte (D-14)
    semantic: list[int]

def ranked_sides(index: Index, query: Query, *, semantic: SemanticSide | None = None) -> RankedSides: ...  # :296

# backend/src/findling/query/rewrite.py
def build_query(index: Index, text: str, *, title_only: bool = False) -> RewrittenQuery: ...  # :317
# RewrittenQuery.query ist Query | None
```

Zahlen und ihre Herkunft:

| Grösse | Herkunft | Deckel |
|---|---|---|
| `bestand` | `searcher.search(query, 1, count=True).count` | keiner unterhalb der Indexgrösse |
| `fenster_lex` / `fenster_sem` | `len(ranked_sides(...).lexical)` bzw. `.semantic` | 100, denn `SEARCH_RRF_WINDOW = 100` in `config.py:547` |
| `rang_lex` / `rang_sem` | 1-basierte Position der eigenen `file_id` in der jeweiligen Liste | 100 |

`SearchResult.count` ist im Typstub der installierten tantivy 0.26.0 NICHT
deklariert, zur Laufzeit aber vorhanden; am 14.09.2026 gegen `backend/.venv`
gemessen: 300 Dokumente indexiert, `limit=10`, `count` liefert 300. Unter
`docs/measurements/**/skripte/` läuft weder ruff noch pyright, der fehlende Stub
ist dort folgenlos.

Schwelle: `MAX_RECHECKS_ABSOLUTE = 64` aus `php/lib/Search/Provider.php:90`.

Die zehn Begriffe der Sprachfälle, wörtlich aus
`docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh`
(Zeilen 431, 451, 463, 475, 486, 499, 514, 529, 546, 558):

```
Genehmigung | Frist | Mueller | Vertrag | "drei Monate" | bescheid |
type:pdf bescheid | Belehrung | Auszug | Erinnerung
```

Transportmuster des Fahrers, wörtlich aus
`docs/measurements/2026-09-vergleichsmessung-m7g/skripte/94-grundlast.sh:102-113`
(wird in Plan 12-05 benutzt, hier nur als Kontext):

```sh
CONTAINER="${CONTAINER:-nc_app_findling_backend}"
sudo docker cp "$SONDE" "$CONTAINER:/tmp/73-bestand-sonde.py"
sudo docker exec "$CONTAINER" /app/.venv/bin/python /tmp/73-bestand-sonde.py
```

Der Ist-Zustand der Gate-Konstanten, `backend/tests/test_measurement_scripts.py:62-93`:

```python
REPO_ROOT = Path(__file__).resolve().parents[2]
MEASUREMENTS_DIR = REPO_ROOT / "docs" / "measurements"
RUN_DIR = MEASUREMENTS_DIR / "2026-09-vergleichsmessung-m7g" / "skripte"
FIX_RUN_DIR = MEASUREMENTS_DIR / "2026-09-werkzeugfixe" / "skripte"
NARROW_SCOPE_DIRS = (RUN_DIR, FIX_RUN_DIR)
DRIVEN_LANGUAGE_CASES = RUN_DIR / "98-sprachfaelle.sh"
SUCCESSOR_LANGUAGE_CASES = FIX_RUN_DIR / "98b-sprachfaelle.sh"
DRIVEN_LANGUAGE_CASES_SHA256 = "5f9607fc6f00754b99eb9921aa6c3ce492e7eb3725f7efb2520be6c12f1ade1d"
DRIVEN_LANGUAGE_CASES_BYTES = 23479
DRIVEN_FASSUNG_RULE = (
    "eine gefahrene Messfassung ist Teil des Belegs, und ein Fix entsteht als neue Datei in einem neuen Laufverzeichnis"
)
```

Der gepinnte Test, `test_measurement_scripts.py:992-1004`:

```python
def test_the_narrow_scope_covers_the_two_run_directories_written_under_these_rules() -> None:
    assert NARROW_SCOPE_DIRS == (RUN_DIR, FIX_RUN_DIR)
    found = scripts_of_this_run()
    assert SUCCESSOR_LANGUAGE_CASES in found
    assert DRIVEN_LANGUAGE_CASES in found
    assert not [path for path in found if path.parent.parent.name == "2026-09-05-semantiklauf-m7g"]
```

Der Ist-Zustand der Pfadfilter, `.github/workflows/python.yml:5-25`: `push.paths`
und `pull_request.paths` führen `backend/**`, `scripts/**` und
`.github/workflows/python.yml`. `docs/measurements/**` fehlt in beiden Listen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-Container-Sonde 73-bestand-sonde.py im neuen Laufverzeichnis</name>
  <files>docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py</files>
  <read_first>
    - docs/measurements/2026-09-werkzeugfixe/skripte/72-fremdbestand.py (Formvorlage: Kopf-Docstring, Konstanten oben, ausgerichtete Ausgabezeile je Begriff, Schlussabschnitt "was daraus folgt")
    - backend/src/findling/index/search.py (Zeilen 250 bis 330: `RankedSides`, `_sides`, `ranked_sides`, die Orakel-Regel im Docstring)
    - backend/src/findling/api/resources.py (Zeilen 80 bis 120 und 250 bis 300: `ReadSide`, `query_model`, `read_side`)
    - backend/src/findling/query/rewrite.py (Zeilen 260 bis 340: `RewrittenQuery` und `build_query`)
    - .planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-RESEARCH.md (Befund 1, besonders 1.2 bis 1.6 und Code Example A)
  </read_first>
  <action>
    Lege `docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py` an.

    Shebang exakt `#!/usr/bin/env python3` mit LF-Zeilenende. Danach der
    Kopf-Docstring nach dem Muster von `72-fremdbestand.py`: zuerst der Befund
    (die Vorprüfung vom 10.09.2026 hat über die OCS-Route für jeden Begriff und
    jede Tiefe 26 gemessen, das ist ein Deckel der Antwort und nicht der Bestand
    dahinter, DI-10-02 und DI-11-01), dann was diese Datei misst, dann die zwei
    Regeln: die Zählung bleibt im Container, damit das Zähl-Orakel aus T-02-93
    gar nicht erst entsteht, und es gibt keinen zweiten Weg zu den beiden
    Ranglisten, weil ein Nachbau eine Suche beantworten würde, die dieser
    Container nie gefahren hat.

    Konstanten oben, keine Maschinenpfade:
    - `BEGRIFFE` als Tupel der zehn Sprachfall-Begriffe in der Reihenfolge der
      Fälle 1 bis 10: `Genehmigung`, `Frist`, `Mueller`, `Vertrag`,
      `"drei Monate"` (mit Anführungszeichen als Teil der Zeichenkette),
      `bescheid`, `type:pdf bescheid`, `Belehrung`, `Auszug`, `Erinnerung`
    - `SCHWELLE = 64` mit dem Kommentar, dass die Zahl `MAX_RECHECKS_ABSOLUTE`
      aus `php/lib/Search/Provider.php:90` ist
    - `FENSTER_DECKEL = 100` mit dem Kommentar, dass das `SEARCH_RRF_WINDOW` aus
      `config.py:547` ist und die Fensterzahlen deshalb dort sättigen

    Messfunktion je Begriff:
    1. `read_side()` holen. Ist das Ergebnis `None`, schreibt die Sonde eine
       Zeile `keine-lesbare-seite` und endet mit einem von Null verschiedenen
       Rückgabewert; sie tut NICHT so, als sei nichts gemessen worden.
    2. `build_query(side.index, text, title_only=False)`. Ist `query` `None`,
       Zeile `keine-suchbare-zeile` für diesen Begriff und weiter zum nächsten.
    3. `bestand` aus `side.index.searcher().search(rewritten.query, 1, count=True).count`,
       mit einem Kommentar, dass `count` im Typstub fehlt und am 14.09.2026
       gegen die installierte tantivy 0.26.0 empirisch nachgewiesen wurde.
    4. `SemanticSide(vectors=side.vectors, model=query_model(), text=text)` nur,
       wenn `side.vectors` nicht `None` ist und `settings().embed_enabled` gilt;
       sonst `semantic=None`.
    5. `sides = ranked_sides(side.index, rewritten.query, semantic=semantic)`,
       daraus `len(sides.lexical)` und `len(sides.semantic)`.
    6. Wenn für diesen Begriff eine eigene Datei-Kennung übergeben wurde:
       `rang_lex` und `rang_sem` als 1-basierte Position dieser `file_id` in
       `sides.lexical` beziehungsweise `sides.semantic`, sonst je das Wort
       `ausserhalb`.

    Übergabe der eigenen Datei-Kennungen ausschliesslich über die Umgebung, nie
    über ein Argument: die Umgebungsvariable `DATEI_IDS` trägt eine
    kommagetrennte Liste von `<begriff>=<file_id>`-Paaren. Fehlt sie, misst die
    Sonde nur Bestand und Fensterbelegung; das ist der Vorlauf vor dem Hochladen
    der eigenen Dateien.

    Ausgabeformat, eine ausgerichtete Zeile je Begriff, maschinenlesbar, nach dem
    Muster von `72-fremdbestand.py:57-58`. Jede Zeile beginnt mit dem festen
    Wort `bestand`, damit ein Fahrer sie mit `grep` herausziehen kann, und trägt
    die Felder `begriff`, `index`, `fenster_lex`, `fenster_sem`, `rang_lex`,
    `rang_sem` als `name=wert`-Paare.

    Schlussabschnitt "was daraus folgt", wie im Analog: die Sonde schreibt die
    Lesart ihrer Zahlen selbst hin, damit der Bericht sie nicht erfinden muss.
    Die Lesart, wörtlich aufzunehmen und zu begründen: ein Fall ist NICHT MESSBAR,
    wenn die eigene Datei in BEIDEN Listen fehlt oder in beiden Listen einen Rang
    grösser als `SCHWELLE` hat. Begründung, die in den Text gehört: bei einer
    Fusion nach RRF liegen alle Dokumente, die in einer Liste vor der eigenen
    Datei stehen, auch in der fusionierten Liste vor ihr; ein Rang schlechter als
    64 in beiden Listen kann deshalb durch keine Fusion in die Reichweite der 64
    Rechecks kommen. `bestand` und die Fensterbelegung stehen als erklärende
    Zahlen daneben, sie sind nicht das Urteil.

    Regeln, die dieses Verzeichnis ab Task 2 durchsetzt und die hier schon
    einzuhalten sind: kein Wagenrücklauf, kein Halbgeviert- und kein
    Geviertstrich, keine Maschinenpfade im Code (kein `/home/`, kein
    `sys.path.insert`, kein `sys.path.append`), kein Passwort auf einer
    Kommandozeile. ASCII-Bezeichner im Code (`ue`, `ae`), echte Umlaute nur in
    deutscher Prosa des Docstrings.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && f=docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py && head -c 19 "$f" | grep -q '#!/usr/bin/env python3' && python -c "import ast,sys; ast.parse(open(sys.argv[1],encoding='utf-8').read())" "$f" && grep -q 'ranked_sides' "$f" && grep -q 'count=True' "$f" && grep -q 'DATEI_IDS' "$f" && ! grep -q $'\r' "$f" && ! grep -q $'—\|–' "$f" && ! grep -v '^ *#' "$f" | grep -q 'sys.path.insert\|sys.path.append\|/home/' && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei beginnt exakt mit `#!/usr/bin/env python3` und einem LF
    - `python -c "ast.parse(...)"` über die Datei läuft ohne Fehler
    - Die Datei importiert `ranked_sides` und `SemanticSide` aus `findling.index.search`, `read_side` und `query_model` aus `findling.api.resources`, `build_query` aus `findling.query.rewrite` und `settings` aus `findling.config`
    - Die Datei enthält `count=True` und einen Kommentar, der den fehlenden Typstub und die Messung vom 14.09.2026 nennt
    - `BEGRIFFE` enthält genau zehn Einträge, in der Reihenfolge der Fälle 1 bis 10 von `98b-sprachfaelle.sh`
    - `SCHWELLE = 64` steht mit dem Verweis auf `MAX_RECHECKS_ABSOLUTE` in einem Kommentar
    - Die Datei liest die eigenen Datei-Kennungen aus der Umgebungsvariable `DATEI_IDS` und aus keinem Argument
    - Jede Ergebniszeile beginnt mit `bestand` und trägt die Felder `begriff`, `index`, `fenster_lex`, `fenster_sem`, `rang_lex`, `rang_sem`
    - Der Schlussabschnitt nennt die Nichtmessbarkeitsregel mit der RRF-Begründung
    - Die Datei enthält keinen Wagenrücklauf, kein U+2014, kein U+2013, und ausserhalb von Kommentaren kein `/home/`, `sys.path.insert` oder `sys.path.append`
  </acceptance_criteria>
  <done>Die ungedeckelte Bestandszahl und die Fensterbelegung sind im Container messbar, ohne dass eine Zahl eine Prozessgrenze überquert.</done>
</task>

<task type="auto">
  <name>Task 2: Enger Geltungsbereich, Wächter über 98b und CI-Pfadfilter</name>
  <files>backend/tests/test_measurement_scripts.py, .github/workflows/python.yml</files>
  <read_first>
    - backend/tests/test_measurement_scripts.py (Zeilen 55 bis 110 für die Konstanten, 780 bis 840 für `measurement_scripts`, `scripts_of_this_run`, `carriage_returns_in`, `dashes_in`, `machine_shapes_in_code`, 965 bis 1030 für den gepinnten Bereichstest und den Wächter über die gefahrene Fassung)
    - .github/workflows/python.yml (Zeilen 1 bis 30: das Begründungsmuster des bestehenden Pfadfilters für `scripts/dev/build_corpus.py`, das übertragen wird)
    - docs/measurements/2026-09-v12-messung/skripte/73-bestand-sonde.py (aus Task 1: die Datei, die der enge Bereich ab jetzt prüft)
  </read_first>
  <action>
    In `backend/tests/test_measurement_scripts.py`:

    1. Neue Konstante `V12_RUN_DIR = MEASUREMENTS_DIR / "2026-09-v12-messung" / "skripte"`
       direkt unter `FIX_RUN_DIR`, und `STOCK_PROBE = V12_RUN_DIR / "73-bestand-sonde.py"`
       bei den benannten Dateien.
    2. `NARROW_SCOPE_DIRS = (RUN_DIR, FIX_RUN_DIR, V12_RUN_DIR)`. Schreibe den
       vorhandenen Kommentar fort statt ihn zu ersetzen: die Erweiterung ist eine
       Entscheidung und kein Seiteneffekt, und der Grund ist, dass die Skripte
       des v1.2-Laufs neu sind und alle drei Zusicherungen halten können.
    3. Benenne `test_the_narrow_scope_covers_the_two_run_directories_written_under_these_rules`
       in `..._the_three_run_directories_...` um, setze die Tupel-Zusicherung auf
       `(RUN_DIR, FIX_RUN_DIR, V12_RUN_DIR)` und ergänze `assert STOCK_PROBE in found`.
       Die Ausschluss-Zusicherung für `2026-09-05-semantiklauf-m7g` bleibt
       unverändert stehen.
    4. Neuer Wächter über die zweite gefahrene Fassung: Konstanten
       `SUCCESSOR_LANGUAGE_CASES_SHA256` und `SUCCESSOR_LANGUAGE_CASES_BYTES`,
       beide aus der Datei `docs/measurements/2026-09-werkzeugfixe/skripte/98b-sprachfaelle.sh`
       einmalig berechnet und als Literal eingetragen, niemals zur Laufzeit aus
       der geprüften Datei abgeleitet. Dazu der Test
       `test_the_successor_language_case_script_stays_byte_identical`, der wie der
       bestehende Wächter über `98-sprachfaelle.sh` beide Zahlen prüft und
       `DRIVEN_FASSUNG_RULE` als Meldung führt. Der Docstring nennt den Grund:
       `98b` ist am 10.09.2026 gefahren worden und trägt Rohdaten unter
       `2026-09-werkzeugfixe/rohdaten/`, und die Nachfolgefassung dieser Phase
       heisst `98c` in einem neuen Laufverzeichnis, nicht eine Korrektur in `98b`.

    In `.github/workflows/python.yml`: nimm `'docs/measurements/**'` in BEIDE
    Pfadlisten auf, in `push.paths` und in `pull_request.paths`. Schreibe einen
    Kommentar nach dem bestehenden Begründungsmuster dazu: `test_measurement_scripts.py`
    liest Dateien unter `docs/measurements/**/skripte/`, ein Filter, der enger ist
    als sein Prüfgegenstand, ist ein Gate, das seinen eigenen Gegenstand nicht
    sehen kann, und die Folge wäre, dass der nächste Commit, der zufällig
    `backend/**` berührt, für einen fremden Fehler rot wird.

    Vor dem Fertigmelden über das GANZE Repositorium: `ruff check .` und
    `ruff format --check .` mit der Konfiguration des Backends, dazu der
    Skriptlauf über `../scripts`. Keine Em-Dashes, keine Wagenrückläufe.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/backend" && uv run pytest tests/test_measurement_scripts.py -q && uv run ruff check . && uv run ruff format --check . && cd "$(git rev-parse --show-toplevel)" && grep -c "docs/measurements/\*\*" .github/workflows/python.yml | grep -q '^2$' && echo GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_measurement_scripts.py` läuft vollständig grün
    - `V12_RUN_DIR` und `STOCK_PROBE` existieren als Konstanten, `NARROW_SCOPE_DIRS == (RUN_DIR, FIX_RUN_DIR, V12_RUN_DIR)`
    - `test_the_narrow_scope_covers_the_three_run_directories_written_under_these_rules` existiert und prüft `STOCK_PROBE in found`; die Variante mit `two_run_directories` existiert nicht mehr
    - `test_the_successor_language_case_script_stays_byte_identical` existiert, prüft Bytezahl und sha256 von `98b-sprachfaelle.sh` gegen eingetragene Literale und führt `DRIVEN_FASSUNG_RULE` als Meldung
    - `test_the_driven_language_case_script_stays_byte_identical` ist unverändert und weiterhin grün
    - `.github/workflows/python.yml` enthält die Zeichenkette `docs/measurements/**` genau zweimal, einmal unter `push.paths` und einmal unter `pull_request.paths`, je mit Begründungskommentar
    - `ruff check .` und `ruff format --check .` über das ganze Repositorium grün
  </acceptance_criteria>
  <done>Die Gates sehen das neue Laufverzeichnis, und ein reiner Messskript-Commit färbt nicht mehr den nächsten, falschen Commit rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container-Prozessgrenze | Eine Trefferzahl, die diese Grenze überquert, ist das Zähl-Orakel aus T-02-93 |
| Rechtegrenze der Suche | `ranked_sides` fragt ausdrücklich keinen Berechtigungs-Vorfilter (D-14) |
| Repositorium (öffentlich) ← Messskripte und Rohdaten | Messskripte werden committet und veröffentlicht |
| CI-Pfadfilter → Gate-Lauf | Ein zu enger Filter lässt die Gates an ihrem eigenen Gegenstand vorbeilaufen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Information Disclosure | Zähl-Orakel über die Prozessgrenze | mitigate | Die Zählung findet in der Sonde im Container statt; weder `backend/src/findling/api/` noch `php/lib/Service/AdminViewService.php` werden angefasst, und keine Route bekommt ein Zahlenfeld |
| T-12-15 | Information Disclosure | Messskript im öffentlichen Repositorium | mitigate | Kein Passwort, keine Route, kein Konto und kein Maschinenpfad in der Sonde; die eigenen Datei-Kennungen kommen aus `DATEI_IDS` in der Umgebung und nie aus einem Argument, durchgesetzt von `passwords_on_a_command_line` und `machine_shapes_in_code` ab Task 2 |
| T-12-16 | Information Disclosure | Rechtegrenze | accept | Die Sonde misst bewusst ohne Vorfilter, admin-seitig im Container. Gemessen wird der Bestand des Index und nicht die Sicht eines Kontos; `ranked_sides` ist genau dafür gebaut (D-14). Kein Nutzerkonto sieht dadurch mehr als vorher |
| T-12-17 | Tampering | gefahrene Messfassung `98b-sprachfaelle.sh` | mitigate | Neuer Wächter mit sha256 und Bytezahl über `98b`, Meldung `DRIVEN_FASSUNG_RULE`; die Korrektur entsteht als `98c` in einem neuen Laufverzeichnis |
| T-12-18 | Repudiation | Gate läuft an seinem Prüfgegenstand vorbei | mitigate | `docs/measurements/**` in beide Pfadlisten von `python.yml`; Acceptance-Criterion prüft genau zwei Vorkommen |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Dieser Plan installiert kein Paket; die Sonde benutzt ausschliesslich die im Abbild bereits installierten Projektabhängigkeiten |
</threat_model>

<verification>
- Erfolgskriterium 1 der Phase wird vorbereitet: die Messgrösse ist ungedeckelt und die Schwelle 64 damit prüfbar
- `pytest tests/test_measurement_scripts.py` grün, `ruff check .` und `ruff format --check .` über das ganze Repositorium grün
- Kein Produktionscode unter `backend/src/` oder `php/lib/` wurde angefasst
- Keine Em-Dashes, keine Wagenrückläufe in den neuen und geänderten Dateien
- Repo-Regeln: Git-Identität NICHT per `-c` übersteuern; sequenziell auf main, Basis-SHA gepinnt
</verification>

<success_criteria>
Die Sonde misst je Begriff eine ungedeckelte Bestandszahl und die Belegung
beider Fusionsfenster, und sie kann bei übergebener Datei-Kennung den Rang der
eigenen Datei ausweisen. Das neue Laufverzeichnis steht im engen Geltungsbereich
der Messskript-Gates, und die CI startet bei einem reinen Messskript-Commit.
</success_criteria>

<output>
Create `.planning/phases/12-messwerkzeug-runbook-und-terminentscheid/12-04-SUMMARY.md` when done
</output>
