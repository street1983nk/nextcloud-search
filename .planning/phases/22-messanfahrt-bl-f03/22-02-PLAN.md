---
phase: 22-messanfahrt-bl-f03
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh
  - docs/measurements/2026-09-v13-messung/skripte/92e-umgebung.sh
  - docs/measurements/2026-09-v13-messung/skripte/90e-einzelliste.py
  - docs/measurements/2026-09-v13-messung/skripte/91m-langsame-aufrufe.py
  - backend/tests/test_measurement_scripts.py
  - backend/tests/test_v13_wechsel.py
autonomous: true
requirements: [MESS-07, MESS-08]

must_haves:
  truths:
    - "92d ist Nachfolgefassung von 92c mit genau zwei Änderungen: kein --rm-data und occ upgrade mit gelesenem Rückgabewert; das Volume des Snapshots bleibt erhalten, damit MESS-08 ohne Vollreindex messbar ist"
    - "92d prüft nach der Registrierung das Bestandstor 52.111 / 37 / 0 und bricht sonst unterhalb der tee-Pipeline ab"
    - "92e baut den Produktcontainer aus docker inspect mit genau einer geänderten Umgebungsvariable neu (FINDLING_LANGUAGES oder FINDLING_EMBED_IDLE_RELEASE_SECONDS), nie FINDLING_OCR_LANGUAGES, und liest Grenze und Entladeschalter danach zurück"
    - "90e liest state.db nur read-only: Einzelliste der übersprungenen und fehlgeschlagenen Dateien (Kennung, Endung, Größe, Grund) und die Marken für das Phase-0-Tor"
    - "91m zählt die M-01-Zeilen 'Findling: slow backend call' je Zeitfenster mit innerMs und ceilingMs und meldet 'unklar' statt 0, wenn das Log nicht lesbar ist"
  artifacts:
    - path: "docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh"
      provides: "Wechsel ohne Volumenverlust"
      contains: "occ-upgrade-rueckgabewert"
    - path: "docs/measurements/2026-09-v13-messung/skripte/92e-umgebung.sh"
      provides: "Containerneubau mit geänderter Umgebung"
      contains: "docker inspect"
    - path: "docs/measurements/2026-09-v13-messung/skripte/90e-einzelliste.py"
      provides: "Einzelliste und Markenlesung"
      contains: "mode=ro"
    - path: "docs/measurements/2026-09-v13-messung/skripte/91m-langsame-aufrufe.py"
      provides: "M-01-Leser"
      contains: "Findling: slow backend call"
    - path: "backend/tests/test_v13_wechsel.py"
      provides: "Wächter der vier Werkzeuge"
  key_links:
    - from: "docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh"
      to: "docs/measurements/2026-09-v12-messung/skripte/40b-baumhash.sh"
      via: "WERKZEUGE-Vorgabe, Baumhash als Skript gerufen"
      pattern: "40b-baumhash"
    - from: "backend/tests/test_measurement_scripts.py"
      to: "docs/measurements/2026-09-v13-messung/skripte"
      via: "V13_RUN_DIR in NARROW_SCOPE_DIRS"
      pattern: "V13_RUN_DIR"
---

<objective>
Die Box-Werkzeuge für Wechsel, Umgebungsneubau, Einzelliste/Marken und den M-01-Leser entstehen im neuen Laufverzeichnis docs/measurements/2026-09-v13-messung/skripte/ und stehen unter Wächtern.

Purpose: Research-Befund 1 (Reihenfolgezwang): der erste Wechsel darf das Snapshot-Volume nicht leeren, sonst ist MESS-08 im Deckel (D-01) unmöglich. 90e liefert das Phase-0-Tor und die Einzelliste (MESS-07 Punkt 4), 91m die M-01-Zahl.
Output: vier Werkzeuge, Laufverzeichnis im engen Geltungsbereich der Messskript-Regeln, test_v13_wechsel.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-messanfahrt-bl-f03/22-CONTEXT.md
@.planning/phases/22-messanfahrt-bl-f03/22-RESEARCH.md
@.planning/phases/22-messanfahrt-bl-f03/22-PATTERNS.md
@docs/measurements/2026-09-nachfolgefassungen/skripte/92c-wechsel.sh

<interfaces>
Aus backend/tests/test_measurement_scripts.py (importierbar, Muster test_dutch_analyzer.py: `from test_analyzer import ...`):
Zeile 66/69: V12_RUN_DIR, SUCCESSOR_RUN_DIR; Zeile 92: NARROW_SCOPE_DIRS = (RUN_DIR, FIX_RUN_DIR, V12_RUN_DIR, SUCCESSOR_RUN_DIR)
Zeile 108: NOT_DRIVEN = "DIESE FASSUNG IST NICHT GEFAHREN"
Zeile 1887: test_the_narrow_scope_covers_the_four_run_directories_written_under_these_rules
def a_boxless_run(...) (2094), def the_two_halves_of(text) (2185), def the_three_parts_of(text) (2563), def switches_that_run(code) -> list[int] (2590), def aborts_of(part) -> set[str] (2616)

Abbruchwerte dieses Plans: 40 bis 45 (Katalog 15 bis 39 ist vergeben, 46 bis 49 gehören 22-03, 50 bis 53 gehören 22-04, 54 bis 58 gehören 22-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 92d-wechsel.sh und 92e-umgebung.sh mit Wächtern</name>
  <files>docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh, docs/measurements/2026-09-v13-messung/skripte/92e-umgebung.sh, backend/tests/test_measurement_scripts.py, backend/tests/test_v13_wechsel.py</files>
  <read_first>
    - docs/measurements/2026-09-nachfolgefassungen/skripte/92c-wechsel.sh (ganz, Vorlage)
    - .github/workflows/deploy-harp.yml Zeilen 4488-4611 (Store upgrade 6, Neubau aus inspect)
    - backend/tests/test_measurement_scripts.py Zeilen 60-110, 1880-1900, 2090-2130, 2560-2650, 2930-2980
    - docs/runbook-messbox.md Block 12 und Abschnitt 6.4
  </read_first>
  <behavior>
    - Test: 92d nennt im Kopf den vollen Pfad docs/measurements/2026-09-nachfolgefassungen/skripte/92c-wechsel.sh, trägt NOT_DRIVEN und liegt nicht im Verzeichnis von 92c
    - Test: switches_that_run(Code von 92d) == [] (kein ausgeführtes --rm-data)
    - Test: occ upgrade steht vor unregister, sein Rückgabewert wird außerhalb der tee-Kette gelesen und als "occ-upgrade-rueckgabewert" protokolliert; im Abbruchteil unter der Pipeline steht exit 40
    - Test: aborts_of(Abbruchteil 92d) enthält 36, 37, 38, 39 unverändert und neu 40, 41; 41 hängt am Bestandstor 52111 / 37 / 0
    - Test: 92e enthält "docker inspect", "docker create", "docker update --memory=2g --memory-swap=2g", "memory.swap.max", "entladeschalter-ist", lässt nur FINDLING_LANGUAGES und FINDLING_EMBED_IDLE_RELEASE_SECONDS als Schalter zu und enthält "FINDLING_OCR_LANGUAGES" nur in der Verweigerungszeile
    - Test: 92e gibt Umgebungsnamen, nie Werte aus (kein Ausgeben ganzer Env-Zeilen, APP_SECRET bleibt unsichtbar)
    - Test: a_boxless_run für 92d und 92e liefert Rückgabe 2, "Benutzung:" auf stderr, leeres tmp_path
    - Test: test_the_narrow_scope_covers_the_five_run_directories_written_under_these_rules (Umbenennung des Tests Zeile 1887) mit V13_RUN_DIR
  </behavior>
  <action>
92d als Kopie von 92c mit genau zwei Änderungen, Kopf nach 92c-Muster: "Dies ist die NACHFOLGEFASSUNG von docs/measurements/2026-09-nachfolgefassungen/skripte/92c-wechsel.sh", Satz NOT_DRIVEN, Abschnitt "Die zwei Aenderungen gegen 92c" (1: unregister ohne --rm-data, damit das Snapshot-Volume mit 52.111 / 37 / 0 bleibt; 2: Schritt 1b `occ upgrade` mit Rückgabewert in Arbeitsdatei, Muster registerlog Zeilen 527-539) und "Was ausdruecklich gleich bleibt" (Rückgabewerte 2, 36 bis 39, Fristen, ABBILD_DIGEST-Pflicht sha256:<hex>, WERKZEUGE auf 2026-09-v12-messung/skripte, Baumhash per 40b, Grenze aus cgroup, Entladeschalter-Zeile). OUT-Vorgabe `${OUT:-$SKRIPTE/../rohdaten}` zeigt damit auf das v13-Verzeichnis. Neu nach der Registrierung: `occ findling:index` lesen und Zeile "bestandstor indexiert <n> uebersprungen <n> fehlgeschlagen <n>" schreiben, bei Abweichung von 52111/37/0 Arbeitsdatei, unterhalb der Pipeline exit 41; gescheitertes occ upgrade exit 40. Die Instanzzählung vor jedem unregister bleibt. 92e als POSIX-sh: Argument SCHALTER=WERT, nur die beiden erlaubten Namen (sonst exit 2, FINDLING_OCR_LANGUAGES ausdrücklich verweigert), `docker inspect` in Arbeitsdatei, Entrypoint/Cmd gegen das Abbild prüfen (sonst exit 43), Env ohne die Zielzeile plus neue Zeile in env-Datei (chmod 600 in WORK), Mounts, Labels, Restart-Policy und NetworkMode aus dem inspect übernehmen (kein --network host), Argumente über `set --` sammeln, `docker rm -f`, `docker create`, `docker start`, dann docker update auf 2g/2g, memory.max 2147483648 und memory.swap.max 0 aus der cgroup zurücklesen (sonst exit 42), Zeile "entladeschalter-ist" und "neubau-fertig <UTC>" schreiben. Nur Namen der Umgebung ausgeben (`cut -d= -f1`). In test_measurement_scripts.py V13_RUN_DIR = MEASUREMENTS_DIR / "2026-09-v13-messung" / "skripte" neben Zeile 69 anlegen, in NARROW_SCOPE_DIRS aufnehmen und den Test Zeile 1887 auf fünf Verzeichnisse umbenennen und anpassen. Alle übrigen Wächter in der neuen Datei test_v13_wechsel.py, Helfer per `from test_measurement_scripts import ...`. Skripte mit `git add --chmod=+x`, ASCII, keine Dashes.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_v13_wechsel.py tests/test_measurement_scripts.py tests/test_public_artifacts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh | grep -c -- "--rm-data"` ergibt 0
    - `grep -c "occ-upgrade-rueckgabewert" docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh` >= 1
    - `grep -c "exit 41" docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh` >= 1
    - `grep -c "V13_RUN_DIR" backend/tests/test_measurement_scripts.py` >= 2
    - `git diff --quiet HEAD -- docs/measurements/2026-09-nachfolgefassungen docs/measurements/2026-09-v12-messung` (beide unverändert)
    - pytest der drei Dateien grün
  </acceptance_criteria>
  <done>92d und 92e existieren als Nachfolge- bzw. Neufassung unter Wächtern, das v13-Verzeichnis steht im engen Geltungsbereich.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 90e-einzelliste.py (Einzelliste und Marken) und 91m-langsame-aufrufe.py</name>
  <files>docs/measurements/2026-09-v13-messung/skripte/90e-einzelliste.py, docs/measurements/2026-09-v13-messung/skripte/91m-langsame-aufrufe.py, backend/tests/test_v13_wechsel.py</files>
  <read_first>
    - docs/measurements/2026-09-nachmessung-m7g/skripte/68-bestand-endungen.py (ganz)
    - docs/measurements/2026-09-v12-messung/skripte/96c-lesen.py Zeilen 30-100
    - backend/src/findling/store/repo.py Zeilen 55-70, 290-310, 717-788, 1448-1526 (files-Spalten, meta-Marken, LEGACY_SCHEMA_STEPS)
    - backend/src/findling/index/rebuild.py Zeile 243 (MARKS_A_REBUILD_ANSWERS)
    - backend/src/findling/store/vectors.py Zeilen 255-273 (embedding_version)
    - php/lib/Service/ExAppService.php Zeilen 160-170 und 742-770
  </read_first>
  <behavior>
    - Test: 90e öffnet die Datenbank ausschließlich mit "?mode=ro" und uri=True; kein "insert", "update", "delete" in Kleinschreibung als SQL im Code
    - Test (Verhalten gegen eine tmp-SQLite mit files- und meta-Tabelle): Unterbefehl liste druckt JSON mit file_id, Endung, Größe, state, reason je Zeile und keinen vollen Pfad; fehlt die Spalte file_id, Rückgabe 2
    - Test: Unterbefehl marken mit --erwartung SCHLUESSEL=WERT-Paaren gibt 0 bei Gleichstand, 44 bei Abweichung einer Marke außerhalb von schema_version, languages, wordlist_hash_nl, 45 bei abweichender embedding_version, 0 wenn nur die drei Umbau-Marken abweichen oder fehlen
    - Test (Verhalten gegen tmp-Logdatei mit JSON-Zeilen): 91m zählt nur Zeilen mit message "Findling: slow backend call" im Fenster --von/--bis (UTC), druckt Anzahl, jeden innerMs mit path, Maximum und ceilingMs; unlesbares Log ergibt "unklar", nicht 0; kaputte Zeilen werden gezählt und übersprungen
    - Test: beide Skripte drucken keine Nutzerdaten (91m nur path, innerMs, ceilingMs, time)
  </behavior>
  <action>
90e nach 68-bestand-endungen.py: argparse mit Unterbefehlen liste und marken, --database Pfad (Wirtspfad auf der Box /mnt/findling/docker/volumes/nc_app_findling_backend_data/_data/state.db, als Vorgabe im Kopf dokumentiert, im Code nur als Argument). liste: Schema-Gegenprobe für file_id, path, state, reason, size (Spaltennamen gegen repo.py prüfen und die tatsächlichen verwenden; Annahme A5 im Kopf auflösen), Abfrage `select file_id, path, state, reason ... where state in ('skipped','failed') and deleted_at is null order by state, file_id`, Ausgabe JSON mit Zählung je state und Liste (file_id, extension_of(path), size, state, reason). marken: liest die meta-Tabelle, druckt jede Marke als "marke <name> <wert>", vergleicht gegen --erwartung-Paare, klassifiziert nach der Umbau-Menge schema_version, languages, wordlist_hash_nl (aus rebuild.py:243) und entschuldigten Legacy-Schritten, Rückgaben 0/44/45 wie im Behavior. 91m nach 96c-lesen.py: --log Pfad, --von, --bis (ISO-UTC), --stufe Name; parst je Zeile JSON, filtert message und Zeitfenster (Feld time), liest context path, innerMs, ceilingMs mit Typprüfung (bool ausschließen), druckt "stufe <n> langsame-aufrufe <anzahl>", je Treffer "aufruf <time> <path> innerMs <x> ceilingMs <y>", "maximum innerMs <x>", und "unklar" bei unlesbarem Log. Kopf erklärt Pitfall 5: die Zeile ist info und erscheint nur bei loglevel 1; den Pfad zum Log nimmt das Ablaufskript aus `occ config:system:get logfile` bzw. datadirectory. Beide mit Shebang "#!/usr/bin/env python3", nur stdlib, `git add --chmod=+x`. Wächter in test_v13_wechsel.py unter Namen test_the_single_list_* und test_the_slow_call_reader_*; die Skripte per importlib.util.spec_from_file_location laden (Dateiname beginnt mit Ziffer). Python-Schreibzugriffe in Tests mit newline="\n".
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_v13_wechsel.py tests/test_measurement_scripts.py -x -q && uv run ruff check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mode=ro" docs/measurements/2026-09-v13-messung/skripte/90e-einzelliste.py` >= 1
    - `grep -c "return 45\|45)" docs/measurements/2026-09-v13-messung/skripte/90e-einzelliste.py` >= 1
    - `grep -c "unklar" docs/measurements/2026-09-v13-messung/skripte/91m-langsame-aufrufe.py` >= 1
    - pytest test_v13_wechsel.py und test_measurement_scripts.py grün, ruff und pyright grün
  </acceptance_criteria>
  <done>Einzelliste, Markentor und M-01-Leser existieren und sind gegen Attrappen getestet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Box-Werkzeug -> Produktcontainer | Neubau übernimmt Umgebung mit APP_SECRET |
| state.db / nextcloud.log -> Rohdaten | Rohdaten werden öffentlich committet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | 92d unregister | mitigate | kein --rm-data (switches_that_run == []), Instanzzählung vor unregister bleibt |
| T-22-06 | Information Disclosure | 92e Umgebung | mitigate | env-Datei chmod 600 in WORK, nur Namen per cut -d= -f1 ausgegeben, Test prüft |
| T-22-07 | Information Disclosure | 90e Einzelliste | mitigate | nur Endung und Kennung statt Pfad, read-only (T-02-14) |
| T-22-08 | Tampering | 90e state.db | mitigate | mode=ro, kein schreibendes SQL, Test prüft |
| T-22-09 | Information Disclosure | 91m Log | mitigate | nur path (Route), innerMs, ceilingMs, time |
</threat_model>

<verification>
cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest tests/test_v13_wechsel.py tests/test_measurement_scripts.py tests/test_public_artifacts.py -q
</verification>

<success_criteria>
Vier Box-Werkzeuge unter Wächtern, v13-Verzeichnis im engen Geltungsbereich, v12- und Nachfolgeverzeichnis unverändert, Gates grün.
</success_criteria>

<output>
Create `.planning/phases/22-messanfahrt-bl-f03/22-02-SUMMARY.md` when done
</output>
