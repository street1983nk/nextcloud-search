---
phase: 22-messanfahrt-bl-f03
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh
  - docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh
  - backend/tests/test_v13_wegwerf.py
autonomous: true
requirements: [MESS-07]

must_haves:
  truths:
    - "00-wegwerf.sh fährt B3, B5 und B4 ausschließlich in Wegwerf-Containern desselben Digests mit --network none und explizitem --cpuset-cpus; der Produktcontainer und seine Umgebung werden nicht angefasst"
    - "00-wegwerf.sh prüft vor B3/B5 Arbeitsvorrat 0 und runState idle des Produkts und verweigert sonst (Pitfall 12)"
    - "00-typwechsel.sh läuft nur auf der Entwicklungsmaschine, liest AWS-Zugangsdaten nur aus der Umgebung, druckt sie nie und prüft vor der Anfahrt, dass instanceInitiatedShutdownBehavior stop ist (Annahme A2, Grundlage des harten Stopps nach D-02)"
    - "Der B4-Typwechsel auf m7g.4xlarge liest den Typ zurück, fällt bei fehlender Kapazität auf m7g.2xlarge zurück und protokolliert Start- und Stoppstempel für die Handrechnung der B4-Kosten (Pitfall 9, D-01 B4-Deckelposten)"
  artifacts:
    - path: "docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh"
      provides: "B3, B4, B5 auf der Box"
      contains: "ocr_slot_probe.py"
    - path: "docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh"
      provides: "Vorprüfung Shutdown-Verhalten, Typwechsel hin und zurück, Preis"
      contains: "instanceInitiatedShutdownBehavior"
    - path: "backend/tests/test_v13_wegwerf.py"
      provides: "Wächter beider Skripte"
  key_links:
    - from: "docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh"
      to: "scripts/ops/aws_box.sh"
      via: "stop und start über aws_box.sh, modify-instance-attribute dazwischen"
      pattern: "aws_box\\.sh"
    - from: "docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh"
      to: "scripts/ops/rss_sampler.sh"
      via: "rss_sampler.sh gegen den Bench-Container (--name) für B5"
      pattern: "rss_sampler\\.sh"
---

<objective>
Die Werkzeuge für die Wegwerf-Container-Blöcke (B3, B5, B4) und den Typwechsel samt Shutdown-Vorprüfung entstehen boxlos.

Purpose: Erfolgskriterium 6 (B3, B4, B5 auf Graviton3) und die Voraussetzung des harten Stopps (D-02): ohne bewiesenes "stop"-Verhalten wäre `shutdown -h` ein Terminate, und Volume samt Korpus wären weg.
Output: 00-wegwerf.sh (Box), 00-typwechsel.sh (Entwicklungsmaschine), test_v13_wegwerf.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-messanfahrt-bl-f03/22-RESEARCH.md
@.planning/research/BL-F04-vorarbeit-2026-09-25.md
@scripts/ops/aws_box.sh
@docs/runbook-messbox.md

<interfaces>
W3-Aufruf (aus 22-01): /app/.venv/bin/python /ops/ocr_slot_probe.py --slots N --rounds 3 --scan /scan/scan-8.pdf [--mode single]
Ausgabezeilen W3: arch, cpus_visible, slots, round ..., pages_per_second_median
embed.bench: python -m findling.embed.bench --mode tokens-per-second --batch B --sequence S --threads T
aws_box.sh: Unterbefehle restore, start, stop, status, prices, destroy; INSTANCE_TYPE='m7g.large' gepinnt (Zeile 91); cmd_stop schreibt BOX_LAST_UPTIME_COST_USD mit m7g.large-Satz.
Abbruchwerte dieses Plans: 50 bis 53.
Helfer aus test_measurement_scripts.py: a_boxless_run, the_two_halves_of, aborts_of, passwords_on_a_command_line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 00-wegwerf.sh (B3, B5, B4) mit Wächtern</name>
  <files>docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh, backend/tests/test_v13_wegwerf.py</files>
  <read_first>
    - .planning/research/BL-F04-vorarbeit-2026-09-25.md Abschnitte 1.2 (B3, B4, B5), 1.4, 1.5
    - .github/workflows/measure.yml Schritte B und D (Mount- und Schleifenmuster)
    - scripts/ops/rss_sampler.sh Kopf (Aufruf)
    - .planning/phases/22-messanfahrt-bl-f03/22-RESEARCH.md Pitfalls 8 und 12
  </read_first>
  <behavior>
    - Test: jeder `docker run` im Skript trägt --network none und --cpuset-cpus
    - Test: Unterbefehle b3, b5, b4 stehen in der Benutzung; ohne Argument Rückgabe 2 und "Benutzung:" auf stderr (a_boxless_run)
    - Test: b3 fährt N 1 und 2 auf cpuset 0,1 mit --memory 2g plus --mode single; b5 die vier Kombinationen threads 1/2 und batch 2/8 bei sequence 512 auf cpuset 0,1; b4 N 1, 2, 4, 8, 12, 16 und bench T 1, 2, 4, 8, wobei die N-Liste auf die sichtbare Kernzahl gekürzt und die Kürzung als Zeile "b4-gekuerzt-auf <n>" protokolliert wird
    - Test: vor b3 und b5 steht die Leerlaufprüfung (Vorrat 0, runState idle) mit exit 50 unterhalb der tee-Pipeline
    - Test: das Skript startet oder baut den Produktcontainer nicht neu (kein "docker create", kein "docker rm" gegen den Produktnamen, kein Aufruf von 92e)
    - Test: das Scan-Material entsteht aus build_load_corpus.py im Wegwerf-Container, nie aus einem Nutzerpfad (kein "/mnt/ncdata", kein "files/" im Mount)
  </behavior>
  <action>
POSIX-sh, set -eu, Vorgaben als VAR="${VAR:-...}": ABBILD_DIGEST Pflicht (sha256:<hex>, sonst exit 2), REPO, OUT (Vorgabe v13-rohdaten), SCAN_DIR in mktemp -d unter WORK. Gemeinsamer Schritt scan: Wegwerf-Container desselben Digests mit --network none, --cpuset-cpus 0, scripts/dev read-only gemountet, schreibt scan-8.pdf mit festem Seed über _scan_pdf(rng, 8) (gleiche Aufrufform wie der W4-Job in measure.yml). b3: Leerlaufprüfung des Produkts über die Statusroute bzw. occ findling:index (Vorrat 0, runState idle), sonst Arbeitsdatei und exit 50; dann W3 mit --cpuset-cpus 0,1 --memory 2g für N 1 und 2, je 3 Runden, dann --mode single; Ausgabe je nach "$OUT/b3-slots-N.txt" und "$OUT/b3-single.txt". b5: gleiche Leerlaufprüfung, dann embed.bench in vier Kombinationen (threads 1 und 2, batch 2 und 8, sequence 512) auf cpuset 0,1 mit festem --name je Kombination, daneben scripts/ops/rss_sampler.sh gegen genau diesen Namen (unverändert aufgerufen), max anon je Kombination in "$OUT/b5-*.txt". b4: läuft auf m7g.4xlarge ohne Produkt (AIO gestoppt, Prüfung: kein laufender Container außer dem Wegwerf-Container, sonst exit 51), nproc lesen, N-Liste 1 2 4 8 12 16 auf nproc kürzen, W3 mit --cpuset-cpus 0-(N-1) ohne --memory, protokollierte Grenze "b4-grenze keine", dann embed.bench --batch 2 --sequence 512 --threads T für T 1 2 4 8 auf cpuset 0-(T-1); Ausgabe "$OUT/b4-slots-N.txt", "$OUT/b4-bench-T.txt". Jeder Block schreibt UTC-Stempel beim Betreten und Verlassen. Nach b4 schreibt das Skript "B4-FERTIG". Keine Veränderung am Produktcontainer. ASCII, keine Dashes, `git add --chmod=+x`. Wächter in test_v13_wegwerf.py unter Namen test_the_throwaway_runner_*.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_v13_wegwerf.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-network none" docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh` ist mindestens gleich `grep -c "docker run" docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh`
    - `grep -c "b4-gekuerzt-auf" docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh` >= 1
    - `grep -c "exit 50" docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh` >= 1
    - pytest tests/test_v13_wegwerf.py grün
  </acceptance_criteria>
  <done>B3, B5, B4 sind als Wegwerf-Blöcke fahrbar und können keine BL-F03-Messung verändern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 00-typwechsel.sh (Vorprüfung, hin, zurueck, preis) mit Wächtern</name>
  <files>docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh, backend/tests/test_v13_wegwerf.py</files>
  <read_first>
    - scripts/ops/aws_box.sh (ganz: Zustandsdatei, cmd_stop, cmd_start, Credential-Prüfung, MSYS-Pfadregel)
    - backend/tests/test_ops_scripts.py Zeilen 490-640 (Regeln des AWS-Werkzeugs, die hier sinngemäß gelten)
    - docs/runbook-messbox.md Abschnitte 2.3 (Kostensätze, gezielte Preisabfrage), 3 (Vorbedingungen), Block 10 (Adresse)
  </read_first>
  <behavior>
    - Test: Unterbefehle vorpruefung, hin, zurueck, preis in der Benutzung; ohne Argument 2
    - Test: AWS_ACCESS_KEY_ID und AWS_SECRET_ACCESS_KEY werden nur auf Gesetztheit geprüft und nie ausgegeben (kein echo/printf dieser Variablen), Muster test_the_aws_tool_demands_both_credentials_and_never_prints_them
    - Test: vorpruefung ruft describe-instance-attribute --attribute instanceInitiatedShutdownBehavior und endet mit exit 52, wenn der Wert nicht stop ist
    - Test: hin ruft aws_box.sh stop, dann modify-instance-attribute mit m7g.4xlarge, dann aws_box.sh start, liest describe-instances InstanceType zurück; Rückfall m7g.2xlarge bei Fehler des Starts mit Zeile "b4-rueckfall m7g.2xlarge"; Abweichung des zurückgelesenen Typs exit 53
    - Test: preis nutzt aws pricing get-products mit Filtern (instanceType, location, operatingSystem, tenancy, preInstalledSw, capacitystatus) und lädt keine ganze Preisliste
    - Test: jeder Unterbefehl schreibt Stempel "typwechsel-<schritt> <UTC>" in die Rohdatei, damit die B4-Kosten von Hand aus Start/Stop und Satz gerechnet werden können
    - Test: MSYS_NO_PATHCONV=1 ist gesetzt (Windows-Pfadumschreibung aus, Muster aws_box.sh)
  </behavior>
  <action>
POSIX-sh, set -eu, läuft nur auf der Entwicklungsmaschine (Kopfsatz: "nie auf der Box ausfuehren, AWS-Zugangsdaten duerfen die Box nie erreichen"). Liest Instanzkennung und Region aus der aws_box.sh-Zustandsdatei (gleicher Pfad wie aws_box.sh, nie im Repo). vorpruefung: describe-instance-attribute, schreibt "shutdown-verhalten <wert>", exit 52 bei ungleich stop. hin: Stempel, aws_box.sh stop (wartet mit Waiter), modify-instance-attribute --instance-type Value=m7g.4xlarge, aws_box.sh start; scheitert der Start an Kapazität, modify auf m7g.2xlarge und erneuter Start, Zeile "b4-rueckfall m7g.2xlarge"; scheitert auch das, modify zurück auf m7g.large und Zeile "b4-entfallen kapazitaet" mit Rückgabe 0 (B4 fällt nach D-05 vor B2/B3 weg). Danach InstanceType zurücklesen (exit 53 bei Abweichung) und die neue öffentliche Adresse ausgeben (Block 10, aws_box.sh start verschiebt die SSH-Regel). zurueck: stop, modify auf m7g.large, kein Start (Abbau folgt). preis: gezielte get-products-Abfrage in us-east-1-Pricing-Endpunkt für eu-central-1 und den Typ als Argument, druckt "preis <typ> <USD/h>". Rohdatei "$OUT/00-typwechsel.txt" (OUT Vorgabe v13-rohdaten). Keine Kennungen im Klartext in Rohdaten, die test_public_artifacts.py verbietet: Instanzkennung als Platzhalter nach Runbook-Geheimnisregel ausgeben. ASCII, keine Dashes, `git add --chmod=+x`. Wächter in test_v13_wegwerf.py unter Namen test_the_type_switch_*.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_v13_wegwerf.py tests/test_public_artifacts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "instanceInitiatedShutdownBehavior" docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh` >= 1
    - `grep -c "exit 52" docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh` >= 1
    - `grep -c "m7g.4xlarge" docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh` >= 1
    - `grep -E -c 'echo .*AWS_SECRET_ACCESS_KEY|printf .*AWS_SECRET_ACCESS_KEY' docs/measurements/2026-09-v13-messung/skripte/00-typwechsel.sh` ergibt 0
    - pytest grün
  </acceptance_criteria>
  <done>Typwechsel und Shutdown-Vorprüfung sind fahrbar, Zugangsdaten bleiben unsichtbar, B4-Kosten sind aus Stempeln rechenbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entwicklungsmaschine -> AWS | Zugangsdaten mit Schreibrecht auf EC2 |
| Box -> Wegwerf-Container | derselbe Digest, aber hart verdrahtete Achsen werden verstellt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-13 | Information Disclosure | 00-typwechsel.sh | mitigate | Credentials nur aus Umgebung, nie ausgegeben, nie auf die Box (Test) |
| T-22-14 | Denial of Wallet | Typwechsel m7g.4xlarge | mitigate | Stempel für Handrechnung, Rückfallkette endet auf m7g.large, Timer der Box wird in 22-09 nach dem Start neu gesetzt |
| T-22-15 | Tampering / Loss | shutdown -h als Terminate | mitigate | vorpruefung verlangt instanceInitiatedShutdownBehavior=stop, sonst exit 52 und keine Anfahrt mit Timer |
| T-22-16 | Information Disclosure | Wegwerf-Container | mitigate | --network none an jedem docker run, synthetischer Scan statt Nutzerdateien |
</threat_model>

<verification>
cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest tests/test_v13_wegwerf.py tests/test_public_artifacts.py -q
</verification>

<success_criteria>
Beide Skripte existieren unter Wächtern; B3/B4/B5 können das Produkt nicht verändern; der harte Stopp ist gegen Terminate abgesichert.
</success_criteria>

<output>
Create `.planning/phases/22-messanfahrt-bl-f03/22-04-SUMMARY.md` when done
</output>
