---
phase: 22-messanfahrt-bl-f03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ops/cpu_sampler.sh
  - scripts/ops/proc_anon_sampler.sh
  - scripts/ops/ocr_slot_probe.py
  - backend/tests/test_ops_scripts.py
  - .github/workflows/measure.yml
autonomous: true
requirements: [MESS-07]

must_haves:
  truths:
    - "W1 cpu_sampler.sh schreibt je Takt usage_usec der Container-cgroup und die cpu-Zeile aus /proc/stat, kennt beide cgroup-Layouts und verweigert statt Nullen zu schreiben; rss_sampler.sh ist byteweise unverändert"
    - "W2 proc_anon_sampler.sh liest je Prozess im Container nur Name, RssAnon, VmHWM und nie cmdline (T-02-14)"
    - "W3 ocr_slot_probe.py fährt N Slots mit je eigenem ExtractionWorker über die OCR-Route und druckt nur arch, sichtbare CPUs und Zahlen, dazu Modus single für OMP_THREAD_LIMIT=1 gegen unset"
    - "W4 ist ein eigener Job in measure.yml auf ubuntu-24.04-arm, fährt W3 für N=1,2,4, embed.bench --threads 1,2,4 und die B7-Sonde und druckt F4 nach der Definition aus 22-RESEARCH Open Question 4 (Grundlage der D-03-Regel)"
  artifacts:
    - path: "scripts/ops/cpu_sampler.sh"
      provides: "W1, Kernbelegung für B1/B6"
      contains: "findling-cpu"
    - path: "scripts/ops/proc_anon_sampler.sh"
      provides: "W2, anon je Prozess für B2"
      contains: "RssAnon"
    - path: "scripts/ops/ocr_slot_probe.py"
      provides: "W3, Slot-Probe für B3/B4/W4"
      contains: "ExtractionWorker"
    - path: ".github/workflows/measure.yml"
      provides: "W4-Job und B7 auf arm64"
      contains: "ocr_slot_probe.py"
  key_links:
    - from: ".github/workflows/measure.yml"
      to: "scripts/ops/ocr_slot_probe.py"
      via: "Mount scripts/ops:/ops:ro im Wegwerf-Container desselben Digests"
      pattern: "/ops/ocr_slot_probe\\.py"
    - from: "backend/tests/test_ops_scripts.py"
      to: "scripts/ops/cpu_sampler.sh"
      via: "Parameterliste der Shell-Regeln"
      pattern: "CPU_SAMPLER"
---

<objective>
Die drei neuen Messwerkzeuge W1 bis W3 entstehen boxlos unter scripts/ops, samt Tests, und W4 hängt als eigener arm64-Job in measure.yml, damit der Erstvollzug aller drei im kostenlosen CI-Runner liegt und nicht in bezahlter Boxzeit.

Purpose: Erfolgskriterium 6 (Zusatzauftrag W1 bis W3, W4 vor dem Rechenblatt, D-03) und die B1-Kernbelegung, die in MESS-07/08 ohne Zusatzzeit mitläuft.
Output: drei Skripte, erweiterte test_ops_scripts.py, W4-Job. Der CI-Lauf selbst folgt in 22-06 (braucht Push-Freigabe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-messanfahrt-bl-f03/22-CONTEXT.md
@.planning/phases/22-messanfahrt-bl-f03/22-RESEARCH.md
@.planning/phases/22-messanfahrt-bl-f03/22-PATTERNS.md
@.planning/research/BL-F04-vorarbeit-2026-09-25.md
@scripts/ops/rss_sampler.sh
@backend/tests/test_ops_scripts.py

<interfaces>
Aus backend/src/findling/extract/sandbox.py:287-344:
class ExtractionWorker:
    def __init__(self, *, max_files: int | None = None, timeout_seconds: float | None = None) -> None
    def run(self, path: str, mime: str, size: int, *, route: str | None = None, timeout_seconds: float | None = None) -> ExtractionOutcome
    def stop(self) -> None
Route-String für OCR: "ocr" (dispatch.py:69, Route.OCR).

Aus backend/tests/test_ops_scripts.py:
Zeile 104: @pytest.fixture(params=[RSS_SAMPLER, HETZNER_BOX, AWS_BOX], ids=lambda path: path.name) def script(...)
Zeilen 127-146: test_the_sampler_knows_both_cgroup_driver_layouts, test_the_sampler_reads_the_cgroup_instead_of_asking_the_client, test_the_sampler_refuses_rather_than_writing_zeroes
Zeilen 702-788: Python-Regeln des Lastwerkzeugs (Shebang, Dash/CR, machine_shapes, Importe nur stdlib)

Aus measure.yml: Schritt B (Zeilen 254-267, bench-Schleife), Schritt D (338-350, Mount-Muster), Artefakt-Upload (406-412), Eingabeprüfung image_ref (126-150), Actions nur per SHA.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: W1 cpu_sampler.sh und W2 proc_anon_sampler.sh mit Tests</name>
  <files>scripts/ops/cpu_sampler.sh, scripts/ops/proc_anon_sampler.sh, backend/tests/test_ops_scripts.py</files>
  <read_first>
    - scripts/ops/rss_sampler.sh (ganz, Vorbild; wird NICHT verändert)
    - backend/tests/test_ops_scripts.py Zeilen 40-146 und 702-788
    - .planning/phases/22-messanfahrt-bl-f03/22-PATTERNS.md Abschnitte W1 und W2
  </read_first>
  <behavior>
    - Test: beide Skripte stehen in der Parameterliste der Fixture `script` und bestehen Shebang, Dash/CR und set -eu
    - Test: cpu_sampler.sh enthält "system.slice/docker-" und "/docker/$CONTAINER_ID" und liest cpu.stat, nie "docker stats"
    - Test: cpu_sampler.sh enthält "no readable cpu.stat" und "not one sample was written"
    - Test: cpu_sampler.sh prüft vor jedem Lesen die Lesbarkeit von cpu.stat und endet dann mit Schlusszeile statt stummem Tod (Pitfall 4)
    - Test (Verhalten, nur wo sh verfügbar, sonst skip mit Grund): mit FINDLING_CGROUP_ROOT auf eine Attrappe ohne cpu.stat und einer docker-Attrappe im PATH endet cpu_sampler.sh mit Rückgabe ungleich 0 und schreibt keine Datenzeile
    - Test: proc_anon_sampler.sh enthält "RssAnon" und "VmHWM" und das Wort "cmdline" kommt im Text nicht vor
  </behavior>
  <action>
W1 nach rss_sampler.sh bauen: POSIX-sh, set -eu, PREFIX='findling-cpu', Argumente NAME INTERVAL OUTPUT wie Vorbild (exit 2 bei falscher Benutzung), Variable CONTAINER_ID, cgroup-Suche über "$CGROUP_ROOT/system.slice/docker-$CONTAINER_ID.scope" und "$CGROUP_ROOT/docker/$CONTAINER_ID" mit CGROUP_ROOT="${FINDLING_CGROUP_ROOT:-/sys/fs/cgroup}", Prüfung auf lesbare cpu.stat. Je Takt eine CSV-Zeile epoch,usage_usec,box_total_jiffies,box_idle_jiffies (usage_usec aus cpu.stat, total als Summe der Felder 2 bis 9 der cpu-Zeile aus /proc/stat, idle Feld 5). Vor jedem Lesen `[ -r "$CGROUP/cpu.stat" ]`, sonst finish. finish (trap INT TERM) schreibt Präfixzeile mit Anzahl Proben, mittlerer und maximaler Kernbelegung (Delta usage_usec / Delta Wandzeit in Mikrosekunden) und dem Grund des Endes ("cgroup gone" oder "signal"). Verweigerung: bei keiner lesbaren cpu.stat Meldung "no readable cpu.stat for container" und bei null Proben "not one sample was written", Rückgabe 1. Kommentare Englisch, ASCII, keine Dashes, kein CR.
W2 nach demselben Gerüst: PREFIX='findling-anon', Container-Existenzprüfung wie Vorbild Zeilen 74-79, je Takt ein `docker exec "$NAME" sh -c` über /proc/[0-9]*/status, das nur die Felder Name, RssAnon, VmHWM je PID ausgibt; Zeile epoch,pid,name,rssanon_kb,vmhwm_kb. Keine cmdline, keine environ, keine Pfade. Schlusszeile mit Maximum RssAnon je Prozessname.
In test_ops_scripts.py Konstanten CPU_SAMPLER und PROC_ANON_SAMPLER neben Zeile 48-52 anlegen, Fixture-Parameterliste erweitern, die Behavior-Tests hinzufügen (Namen beginnen mit test_the_cpu_sampler_ bzw. test_the_anon_sampler_). rss_sampler.sh wird nicht berührt. Beide Dateien mit `git add --chmod=+x` aufnehmen.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_ops_scripts.py -x -q && git diff --quiet HEAD -- ../scripts/ops/rss_sampler.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "findling-cpu" scripts/ops/cpu_sampler.sh` >= 1 und `grep -c "cpu.stat" scripts/ops/cpu_sampler.sh` >= 2
    - `grep -v '^#' scripts/ops/proc_anon_sampler.sh | grep -c cmdline` ergibt 0 und `grep -c "cmdline" scripts/ops/proc_anon_sampler.sh` ergibt 0
    - `git diff HEAD -- scripts/ops/rss_sampler.sh` ist leer
    - `git ls-files -s scripts/ops/cpu_sampler.sh scripts/ops/proc_anon_sampler.sh` zeigt Modus 100755
    - pytest tests/test_ops_scripts.py grün
  </acceptance_criteria>
  <done>W1 und W2 existieren, sind in den Shell-Regeln erfasst, verweigern statt Nullen, rss_sampler.sh unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: W3 ocr_slot_probe.py mit Tests</name>
  <files>scripts/ops/ocr_slot_probe.py, backend/tests/test_ops_scripts.py</files>
  <read_first>
    - backend/src/findling/extract/sandbox.py Zeilen 280-360
    - backend/src/findling/extract/ocr.py Zeilen 60-80 und 185-200 (OMP_THREAD_LIMIT)
    - backend/src/findling/embed/bench.py Zeilen 160-180 (sched_getaffinity-Ausgabe)
    - scripts/ops/search_load.py Kopf (Form)
    - scripts/dev/build_load_corpus.py Zeilen 1025-1070 (_scan_pdf, build_scan_single)
  </read_first>
  <behavior>
    - Test: Shebang "#!/usr/bin/env python3", kein Dash, kein CR, keine Maschinenpfade (machine_shapes), Importe nur stdlib plus ausdrücklich benannte Ausnahme findling
    - Test: Text enthält "ExtractionWorker", "route=\"ocr\"" und "sched_getaffinity", enthält nicht "RUSAGE_CHILDREN" als Messquelle der CPU-Zeit (cgroup cpu.stat statt dessen)
    - Test: das Modul lässt sich per importlib laden, ohne findling zu importieren (findling-Import erst in main bzw. einer Funktion), und die Argumentprüfung weist --slots 0 und fehlendes --scan mit Rückgabe 2 ab
    - Test: die Ausgabefunktion druckt für eine Attrappen-Messreihe genau die Zeilen arch, cpus_visible, slots, round, pages_per_second_median, anon_bytes_delta, cpu_usec und keinen Text aus dem Extraktionsergebnis
  </behavior>
  <action>
W3 als stdlib-Skript mit spätem findling-Import bauen. Argumente: --slots N (ganze Zahl >= 1), --rounds R (Vorgabe 3), --scan PATH (Pflicht, synthetische achtseitige Scan-PDF, nie Nutzerdatei), --mode slots|single (Vorgabe slots). Modus slots: je Runde N Threads, jeder Thread eine eigene ExtractionWorker-Instanz, ruft worker.run(scan, "application/pdf", size, route="ocr") und stop() am Ende; gemessen werden Wandzeit, Seiten je Sekunde (Seitenzahl aus dem Ergebnisobjekt oder über die bekannte Seitenzahl der Scan-Datei als Argument --pages, Vorgabe 8), anon aus memory.stat und usage_usec aus cpu.stat der eigenen cgroup (/sys/fs/cgroup im Container, cgroup v2) vor und nach der Runde. Modus single: ruft tesseract auf einer gerenderten Seite einmal mit OMP_THREAD_LIMIT=1 und einmal ohne, je Wandzeit und cpu_usec, prüft damit docs/performance.md:2886. Ausgabe nur Zeilen `arch <x>`, `cpus_visible <n>`, `slots <n>`, `round <r> wall_seconds <s> pages_per_second <p> anon_bytes_delta <b> cpu_usec <u>`, `pages_per_second_median <p>`, in single `omp_limit_1 wall_seconds ...` und `omp_unset wall_seconds ...`. Nie Text des Extraktionsergebnisses, nie Pfade außer dem Basisnamen der Scan-Datei. Tests unter Namen test_the_slot_probe_* in test_ops_scripts.py (Konstante OCR_SLOT_PROBE); die Import-Ausnahme findling benennt der Test ausdrücklich. ruff mit der backend-Konfiguration muss für scripts/ grün sein.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_ops_scripts.py -x -q && uv run ruff check --config pyproject.toml ../scripts && uv run ruff format --config pyproject.toml --check ../scripts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pages_per_second_median" scripts/ops/ocr_slot_probe.py` >= 1
    - `grep -c "RUSAGE_CHILDREN" scripts/ops/ocr_slot_probe.py` ergibt 0
    - `head -1 scripts/ops/ocr_slot_probe.py` ergibt `#!/usr/bin/env python3`
    - pytest tests/test_ops_scripts.py grün, ruff check und ruff format --check für ../scripts grün
  </acceptance_criteria>
  <done>W3 läuft im Abbild mit gemountetem Skript, druckt nur Zahlen und Kennungen, Tests grün.</done>
</task>

<task type="auto">
  <name>Task 3: W4-Job und B7 in measure.yml, F4 definiert</name>
  <files>.github/workflows/measure.yml</files>
  <read_first>
    - .github/workflows/measure.yml (ganz)
    - backend/tests/test_workflow_pins.py (Regeln, besonders Regel 5)
    - scripts/dev/measure_compounds_nl.sh Kopf (Zeilen 1-45, Speichermodus)
    - docs/measurements/2026-09-komposita-nl/README.md Abschnitt 4
  </read_first>
  <action>
Einen zweiten Job `slots` neben `measure` anlegen, name "W4 slot curve on arm64", runs-on ubuntu-24.04-arm, timeout-minutes 40, ohne Matrix (nur arm64). Gleiche SHA-gepinnte Actions wie im Job measure (checkout, login, upload-artifact), gleiche Eingabeprüfung für image_ref aus env und gleicher Schritt zur Auflösung des Digests (Muster Zeilen 126-196), Maschinenblatt mit nproc und lscpu. Schritte: (a) Scan erzeugen: Wegwerf-Container desselben Digests, --network none, scripts/dev read-only gemountet, Ausgabe nach "${RUNNER_TEMP}/scan", ruft _scan_pdf(rng, 8) aus build_load_corpus.py mit festem Seed und schreibt scan-8.pdf; (b) W3 für N in 1, 2, 4 mit --cpuset-cpus 0 bzw. 0,1 bzw. 0-3, --network none, scripts/ops als /ops:ro, Scan als /scan:ro, --entrypoint /app/.venv/bin/python, `/ops/ocr_slot_probe.py --slots N --rounds 3 --scan /scan/scan-8.pdf`, Ausgabe per tee nach "${OUT}/w3-slots-N.txt"; (c) W3 --mode single auf --cpuset-cpus 0-3 nach "${OUT}/w3-single.txt"; (d) embed.bench `--mode tokens-per-second --batch 2 --sequence 512 --threads T` für T in 1, 2, 4 auf cpuset 0 bzw. 0,1 bzw. 0-3 nach "${OUT}/bench-threads-T.txt"; (e) B7: scripts/dev/measure_compounds_nl.sh im Speichermodus nach "${OUT}/b7-nl-automat.txt" (Aufrufform aus dem Skriptkopf übernehmen); (f) F4-Schritt: liest pages_per_second_median aus w3-slots-4.txt und w3-slots-1.txt und schreibt "F4 <Wert mit drei Nachkommastellen>" nach "${OUT}/f4.txt" und ins Job-Log; fehlt ein Wert, schreibt er "F4 unbestimmt" und endet mit Fehler. F4-Definition als Kommentar über dem Schritt: F4 = Seiten je Sekunde bei N = 4 auf cpuset 0-3 geteilt durch Seiten je Sekunde bei N = 1 auf cpuset 0, jeweils Median aus drei Runden; D-03 wendet die Schwelle 1,5 darauf an. (g) Artefakt `w4-arm64` hochladen. Der bestehende Job measure bleibt unverändert. Kommentare Englisch, ASCII.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_workflow_pins.py -x -q && uv run python -c "import yaml,sys; d=yaml.safe_load(open('../.github/workflows/measure.yml',encoding='utf-8')); assert d['jobs']['slots']['runs-on']=='ubuntu-24.04-arm'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ocr_slot_probe.py" .github/workflows/measure.yml` >= 2
    - `grep -c "ubuntu-24.04-arm" .github/workflows/measure.yml` >= 2
    - `grep -c "F4" .github/workflows/measure.yml` >= 2
    - `grep -c "\-\-network none" .github/workflows/measure.yml` ist größer als vor der Änderung
    - `git diff HEAD -- .github/workflows/measure.yml` zeigt keine Änderung innerhalb des Jobs measure
    - test_workflow_pins.py grün
  </acceptance_criteria>
  <done>W4-Job steht, F4 ist vor dem CI-Lauf definiert, Workflow-Pins-Gate grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow-Eingabe -> Shell | image_ref ist fremder Text und erreicht eine Kommandozeile |
| Container -> Sampler-Ausgabe | /proc im Container trägt Kommandozeilen mit Dateipfaden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | proc_anon_sampler.sh | mitigate | liest nur Name, RssAnon, VmHWM; Test verbietet "cmdline" im Text (T-02-14) |
| T-22-02 | Information Disclosure | ocr_slot_probe.py | mitigate | druckt keinen Extraktionstext, nur Basisnamen der synthetischen Scan-Datei; Test prüft Ausgabezeilen |
| T-22-03 | Tampering | measure.yml image_ref | mitigate | Eingabe über env und Musterprüfung wie im bestehenden Job, Actions per SHA (test_workflow_pins.py) |
| T-22-04 | Information Disclosure | Wegwerf-Container W4 | mitigate | --network none für jeden docker run des Jobs |
</threat_model>

<verification>
cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest tests/test_ops_scripts.py tests/test_workflow_pins.py -q && uv run ruff check --config pyproject.toml ../scripts && uv run ruff format --config pyproject.toml --check ../scripts
</verification>

<success_criteria>
W1 bis W3 existieren mit Tests, W4-Job mit F4-Definition steht in measure.yml, rss_sampler.sh unverändert, alle Gates grün.
</success_criteria>

<output>
Create `.planning/phases/22-messanfahrt-bl-f03/22-01-SUMMARY.md` when done
</output>
