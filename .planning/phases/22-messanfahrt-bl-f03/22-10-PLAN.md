---
phase: 22-messanfahrt-bl-f03
plan: 10
type: execute
wave: 7
depends_on: ["22-09"]
files_modified:
  - docs/measurements/2026-09-v13-messung/README.md
  - docs/language-analyzers.md
  - backend/src/findling/query/rewrite.py
  - backend/tests/test_query_rewrite.py
  - backend/tests/test_field_plan_ranking.py
  - backend/tests/test_search_endpoint.py
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [MESS-09]

must_haves:
  truths:
    - "Der disjunction_max-Entscheid wird ausschließlich mit der vor der Anfahrt committeten Regel E10 aus 00-ablauf.md Abschnitt 6 gegen die Box-Rohdaten von 98d gefällt; Urteil nur 'gehalten', 'verfehlt' oder 'nicht entschieden'"
    - "Fällt die Messung für disjunction_max aus, ist per-Wort-dismax noch in Phase 22 in rewrite.py umgesetzt, mit Rückfall auf den heutigen Parserweg bei Operatoren, Phrasen, Klammern und Umlautvarianten, gleicher Treffermenge, unverändertem Filter und allow_regexes=False (D-04)"
    - "Bei Umsetzung ist PACKAGE_TREE_HASH_TODAY im selben Commit wie rewrite.py nachgezogen, und test_field_plan_ranking.py ist unter dismax neu vermessen"
    - "Fällt die Messung gegen disjunction_max aus oder ist sie nicht entscheidbar, ist die Verwerfung mit Zahlen in docs/language-analyzers.md dokumentiert und der Kopf von test_field_plan_ranking.py nennt den Entscheid; rewrite.py bleibt byteweise unverändert"
  artifacts:
    - path: "docs/language-analyzers.md"
      provides: "MESS-09-Entscheid mit Messzahlen"
      contains: "disjunction_max"
  key_links:
    - from: "backend/src/findling/query/rewrite.py"
      to: "tantivy Query.disjunction_max_query"
      via: "per-Wort-Zweig in build_query (nur bei Umsetzung)"
      pattern: "disjunction_max_query"
    - from: "docs/language-analyzers.md"
      to: "docs/measurements/2026-09-v13-messung/rohdaten/98d-dismax-probe.txt"
      via: "Quelle der Zahlen"
      pattern: "2026-09-v13-messung"
---

<objective>
Der MESS-09-Entscheid fällt nach der eingefrorenen Regel auf den Box-Rohdaten; bei Vorteil wird per-Wort-disjunction_max im Suchpfad umgesetzt und CI-beweisbar getestet, sonst wird die Verwerfung dokumentiert.

Purpose: Erfolgskriterium 4 wörtlich (umgesetzt oder dokumentiert verworfen) und D-04 (Umsetzung in Phase 22, keine neue Anfahrt dafür).
Output: Entscheid im README und in docs/language-analyzers.md; bei Umsetzung Codeänderung mit Tests und Baumhash-Ratsche.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-messanfahrt-bl-f03/22-CONTEXT.md
@.planning/phases/22-messanfahrt-bl-f03/22-RESEARCH.md
@docs/measurements/2026-09-v13-messung/skripte/00-ablauf.md
@docs/language-analyzers.md
@backend/src/findling/query/rewrite.py

<interfaces>
rewrite.py:
- build_query(index, text, *, title_only=False, plan=None, ...) Zeilen 575-717; Parseraufruf 692-698 mit default_field_names=searched, field_boosts=dict(plan.boosts), conjunction_by_default=True, allow_regexes=False
- operators = carried_operators(text) und one_term = carries_one_term(text) Zeilen 634-635
- _filter_clause(index, extensions, since, until), danach Query.boolean_query([(Occur.Must, parsed), (Occur.Must, filter_query)])
- Fehlerliste nur LOGGER.debug (699-702); Imports Occur, Query in Zeile 39 vorhanden
- Aufrufer ohne Änderung: api/search.py:248, api/snippets.py:187, api/diagnose.py:198
test_measurement_scripts.py Zeilen 1085-1097: datierter Absatz über PACKAGE_TREE_HASH_TODAY, PACKAGE_FILES_TODAY bleibt 57 ohne neue Datei.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entscheid nach E10 auf den Box-Rohdaten</name>
  <files>docs/measurements/2026-09-v13-messung/README.md, docs/language-analyzers.md</files>
  <read_first>
    - docs/measurements/2026-09-v13-messung/skripte/00-ablauf.md Abschnitt 3 (E10) und Abschnitt 6
    - docs/measurements/2026-09-v13-messung/rohdaten/98d-dismax-probe.txt (bzw. die Rohdatei, die 00-lauf.sh für 98d schreibt)
    - docs/language-analyzers.md Abschnitte "What a question searches" und "Measured numbers"
  </read_first>
  <action>
Fehlt die 98d-Rohdatei (Lücke aus 22-08/22-09), ist das Urteil "nicht entschieden" mit Verweis auf den Lückenbericht; dann gilt für Task 2 der Zweig Verwerfung mit dem Satz, dass die Messung fehlt und eine Umsetzung ohne Messung ausgeschlossen ist. Sonst die Kennzahlen aus der Rohdatei lesen (Mediane je Form, Treffermengengleichheit, rueckfall-Anteil, Latenzen) und E10 wörtlich anwenden, einschließlich der Rolle von tie 0.0 gegen 0.1. Ist in der Rohdatei "treffermenge-ungleich" vermerkt, ist die Probe nach ihrer eigenen Regel falsch und das Urteil "nicht entschieden". README "Bericht" Unterabschnitt "MESS-09": Tabelle der Kennzahlen je Form, angewandte Regel, Urteil E10, Folge "umgesetzt" oder "verworfen". docs/language-analyzers.md: in "Measured numbers" die v1.3-Messung vom Anfahrtsdatum neben die 19-04-Messung (Kipppunkt 0,81 gegen 0,6) stellen, mit Quelle docs/measurements/2026-09-v13-messung; englische Prosa wie das restliche Dokument.
  </action>
  <verify>
    <automated>grep -c "MESS-09" docs/measurements/2026-09-v13-messung/README.md && grep -c "2026-09-v13-messung" docs/language-analyzers.md && cd backend && uv run pytest tests/test_public_artifacts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - README "MESS-09" nennt Urteil E10 mit genau einem der Wörter gehalten, verfehlt, nicht entschieden und die Folge umgesetzt oder verworfen
    - docs/language-analyzers.md verweist auf 2026-09-v13-messung und nennt mindestens den Median RBO@10 je Form
  </acceptance_criteria>
  <done>Der Entscheid steht auf Messbasis und nach vorher festgeschriebener Regel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Umsetzung (bei Vorteil) oder dokumentierte Verwerfung</name>
  <files>backend/src/findling/query/rewrite.py, backend/tests/test_query_rewrite.py, backend/tests/test_field_plan_ranking.py, backend/tests/test_search_endpoint.py, backend/tests/test_measurement_scripts.py, docs/language-analyzers.md</files>
  <read_first>
    - backend/src/findling/query/rewrite.py Zeilen 1-60 (Modulkopf, Begründung der Parser-Einstellungen), 100-175, 280-390, 575-718
    - backend/tests/test_query_rewrite.py, backend/tests/test_field_plan_ranking.py (Kopf "that decision belongs to phase 22", Aussagen 3 und 4), backend/tests/test_search_endpoint.py (Snippet-Fälle)
    - backend/tests/test_measurement_scripts.py Zeilen 1080-1100 und 1250-1265
    - docs/language-analyzers.md Abschnitt "What a question searches" und Zeilen 150-175 (alemanes-Einwortbeweis)
  </read_first>
  <behavior>
    - Nur Zweig Umsetzung: Test: für eine Mehrwortzeile ohne Operatoren baut build_query eine Anfrage, deren Treffermenge auf dem Sechs-Feld-Index der Summenform gleicht und deren Rangfolge der per-Wort-dismax-Form aus 98d gleicht
    - Nur Zweig Umsetzung: Test: Zeilen mit Operator, Phrase, Klammer oder Umlautvariantengruppe laufen über den heutigen Parserweg (gleiche Query-Darstellung wie vorher)
    - Nur Zweig Umsetzung: Test: Filter (Endungen, Zeitraum) und title_only wirken unverändert; allow_regexes bleibt False in jedem Parseraufruf (AST- oder Textprüfung)
    - Nur Zweig Umsetzung: Test: SnippetGenerator liefert für die dismax-Anfrage markierte Treffer im Endpunkttest
    - Nur Zweig Umsetzung: test_field_plan_ranking.py Aussagen 3 und 4 und der Kipppunkt sind unter dismax neu vermessen und mit den neuen Zahlen festgeschrieben
    - Beide Zweige: test_measurement_scripts.py Baumhash-Test grün (bei Umsetzung mit neuem PACKAGE_TREE_HASH_TODAY, bei Verwerfung unverändert)
  </behavior>
  <action>
Zweig nach dem Urteil aus Task 1. Umsetzung (Urteil gehalten, Folge umgesetzt), per D-04: in rewrite.py eine Funktion neben build_query, die für den Fall "keine carried_operators, keine Phrase, keine Klammer, keine Umlautvariantengruppe" je Wort der bereinigten Zeile und je Feld des Plans parse_query_lenient(wort, default_field_names=[feld], field_boosts={feld: boost}, conjunction_by_default=True, allow_regexes=False) aufruft, darüber Query.disjunction_max_query mit dem in E10 beschlossenen tie_breaker, über die Wörter Occur.Must, und danach _filter_clause wie heute; sonst der heutige Parserweg. Fail-closed-Regel des plan-Arguments (Docstring 611-617) bleibt; Parserfehler nur LOGGER.debug. Einwortzeilen: per-Wort gleich ganze Zeile, der alemanes-Beweis bleibt Einwort. Tests zuerst (RED), dann Code (GREEN). test_field_plan_ranking.py neu vermessen und den Modulkopf auf den Entscheid mit Datum umschreiben. docs/language-analyzers.md "What a question searches" beschreibt die neue Anfrageform samt Rückfallfällen. Im selben Commit wie rewrite.py den datierten Absatz über PACKAGE_TREE_HASH_TODAY ergänzen und den Wert aus dem fehlschlagenden Test übernehmen (PACKAGE_FILES_TODAY bleibt 57, solange keine Datei hinzukommt). Der Bericht vermerkt, dass das gemessene Abbild nicht das ausgelieferte 1.3.0 ist. Verwerfung (Urteil verfehlt oder nicht entschieden): rewrite.py nicht anfassen; docs/language-analyzers.md erhält den Absatz "disjunction_max was measured and rejected on <Datum>" mit den Kennzahlen und der Regel; der Kopf von test_field_plan_ranking.py nennt statt "that decision belongs to phase 22" den Entscheid mit Datum und Quelle (Testdatei, keine Ratsche nötig); test_query_rewrite.py und test_search_endpoint.py bleiben unverändert. In beiden Zweigen Python-Schreibzugriffe mit newline="\n".
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_query_rewrite.py tests/test_field_plan_ranking.py tests/test_search_endpoint.py tests/test_measurement_scripts.py tests/test_dismax_probe.py -x -q && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - Zweig Umsetzung: `grep -c "disjunction_max_query" backend/src/findling/query/rewrite.py` >= 1, `grep -c "allow_regexes=True" backend/src/findling/query/rewrite.py` ergibt 0, und `git show --stat HEAD` enthält rewrite.py und test_measurement_scripts.py im selben Commit
    - Zweig Verwerfung: `git diff <Commit vor Task 2> -- backend/src/findling/query/rewrite.py` leer und `grep -c "that decision belongs to phase 22" backend/tests/test_field_plan_ranking.py` ergibt 0
    - docs/language-analyzers.md nennt den Entscheid mit Datum
    - volle Gates und die genannten Tests grün
  </acceptance_criteria>
  <done>disjunction_max ist umgesetzt oder dokumentiert verworfen; Erfolgskriterium 4 ist wörtlich erfüllt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzer-Suchzeile -> Tantivy-Parser | fremder Text wird zu einer Anfrage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-39 | Denial of Service | per-Wort-Zweig in rewrite.py | mitigate | allow_regexes=False in jedem Parseraufruf, Test prüft; Wortzahl folgt der bestehenden Zeilenbegrenzung von build_query |
| T-22-40 | Elevation of Privilege | Filter | mitigate | _filter_clause unverändert als Occur.Must, Test auf Filterwirkung; Berechtigungsfilter bleibt in PHP |
| T-22-41 | Tampering | Baumhash | mitigate | PACKAGE_TREE_HASH_TODAY im selben Commit wie rewrite.py |
| T-22-42 | Repudiation | Entscheid | mitigate | Regel E10 vor der Messung committet, Urteil nur in drei Wörtern |
</threat_model>

<verification>
cd backend && uv run ruff check . && uv run ruff format --check . && PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright && uv run vulture src tests --min-confidence 80 && uv run pytest -q
</verification>

<success_criteria>
MESS-09 entschieden auf Messbasis; umgesetzt mit Tests und Ratsche oder dokumentiert verworfen; volle Suite grün.
</success_criteria>

<output>
Create `.planning/phases/22-messanfahrt-bl-f03/22-10-SUMMARY.md` when done
</output>
