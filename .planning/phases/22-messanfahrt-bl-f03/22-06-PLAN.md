---
phase: 22-messanfahrt-bl-f03
plan: 06
type: execute
wave: 3
depends_on: ["22-05"]
files_modified:
  - docs/measurements/2026-09-v13-messung/README.md
  - docs/measurements/2026-09-v13-messung/rohdaten/w4-ci-arm64/f4.txt
  - docs/measurements/2026-09-v13-messung/skripte/00-lauf.sh
  - docs/measurements/2026-09-v13-messung/skripte/00-wegwerf.sh
  - docs/measurements/2026-09-v13-messung/skripte/92d-wechsel.sh
  - docs/measurements/2026-09-v13-messung/skripte/92e-umgebung.sh
  - docs/measurements/2026-09-v13-messung/skripte/90e-einzelliste.py
  - docs/measurements/2026-09-v13-messung/skripte/91m-langsame-aufrufe.py
  - docs/measurements/2026-09-v13-messung/skripte/94c-bodensatz-zyklen.sh
  - docs/measurements/2026-09-v13-messung/skripte/95c-kaltstart.sh
  - docs/measurements/2026-09-v13-messung/skripte/98d-dismax-probe.py
  - scripts/ops/ocr_slot_probe.py
  - scripts/ops/cpu_sampler.sh
  - scripts/ops/proc_anon_sampler.sh
  - backend/tests/test_ops_scripts.py
  - backend/tests/test_v13_wechsel.py
  - backend/tests/test_v13_zyklen.py
  - backend/tests/test_v13_wegwerf.py
  - backend/tests/test_v13_ablauf.py
  - backend/tests/test_dismax_probe.py
autonomous: false
requirements: [MESS-07]

must_haves:
  truths:
    - "Jedes Box-Werkzeug ist vor der bezahlten Zeit einmal gegen die lokale Test-Nextcloud oder im CI-arm64-Runner gelaufen; jeder Befund ist vor der Anfahrt behoben (Runbook 7.1: kein Werkzeug wird in bezahlter Zeit geändert)"
    - "Der W4-Lauf auf ubuntu-24.04-arm ist grün, seine Rohdaten liegen im Repo, und F4 steht mit Wert im README; D-03 ist angewandt (F4 >= 1,5 heißt B4 gefahren)"
    - "Der Digest des Abbilds, gegen das W4 gemessen hat, ist mit Laufnummer der Abbildstrecke des gepushten Commits belegt (Pitfall 11) und ist der Box-Digest-Kandidat"
    - "B7 (NL-Automat nativ arm64) ist im CI gemessen und nicht auf der Box (D-05: B7 fällt auf der Box zuerst, hier ganz)"
  artifacts:
    - path: "docs/measurements/2026-09-v13-messung/rohdaten/w4-ci-arm64/f4.txt"
      provides: "F4 aus CI"
      contains: "F4"
    - path: "docs/measurements/2026-09-v13-messung/README.md"
      provides: "Abschnitte Generalprobe und W4-Vorabkurve"
      contains: "F4"
  key_links:
    - from: "docs/measurements/2026-09-v13-messung/README.md"
      to: "GitHub-Lauf measure.yml Job slots"
      via: "Laufnummer und Artefaktname w4-arm64"
      pattern: "w4-arm64"
---

<objective>
Generalprobe aller Box-Werkzeuge ohne Box, Push nach Owner-Freigabe, W4-Lauf im CI-arm64-Runner und Ablesen von F4 samt B7.

Purpose: D-03 verlangt W4 VOR dem Rechenblatt; die Lehre aus Phase 15 verlangt den Erstvollzug jedes Werkzeugs außerhalb der bezahlten Zeit. Der Push ist Voraussetzung für den CI-Lauf und braucht Owner-Freigabe.
Output: Generalprobe-Abschnitt, W4-Rohdaten, F4 mit angewandter D-03-Regel, Box-Digest-Kandidat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-messanfahrt-bl-f03/22-CONTEXT.md
@.planning/phases/22-messanfahrt-bl-f03/22-RESEARCH.md
@docs/measurements/2026-09-v13-messung/skripte/00-ablauf.md
@docs/measurements/2026-09-v13-messung/README.md
@scripts/dev/compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalprobe lokal, Befunde beheben</name>
  <files>docs/measurements/2026-09-v13-messung/README.md, sowie bei Befund das betroffene Werkzeug aus files_modified samt seinem Test</files>
  <read_first>
    - scripts/dev/compose.yaml und scripts/dev/register-exapp.sh (lokale Test-Nextcloud)
    - docs/measurements/2026-09-v13-messung/skripte/00-ablauf.md Abschnitte 2 und 4
    - jedes Werkzeug vor seinem Probelauf (Kopf mit Benutzung und Vorgaben)
  </read_first>
  <action>
Lokale Test-Nextcloud starten bzw. den laufenden Stand nutzen (Container findling-nextcloud und Backend-Container, Namen aus compose.yaml lesen). Jeden Probelauf mit OUT auf ein Verzeichnis im Scratchpad bzw. einem mktemp-Verzeichnis außerhalb des Repos fahren, nie in das v13-rohdaten-Verzeichnis. Probeliste mit erwartetem Ausgang: `sh -n` über alle .sh im v13-Verzeichnis und die drei neuen scripts/ops-Skripte (Rückgabe 0); cpu_sampler.sh und proc_anon_sampler.sh je 30 s gegen den Backend-Container (Datenzeilen und Schlusszeile vorhanden, keine cmdline in der Ausgabe); 90e liste und marken gegen eine Kopie der lokalen state.db (JSON, Marken gelesen; Rückgabe nach Stand der lokalen Marken protokollieren); 91m gegen das lokale nextcloud.log nach einer Suche mit loglevel 1 (Zeilen gezählt oder "stufe ... langsame-aufrufe 0", kein "unklar"); 92e mit FINDLING_EMBED_IDLE_RELEASE_SECONDS=120 und zurück auf den Ausgangswert (Neubau gelingt, memory.max und memory.swap.max zurückgelesen, entladeschalter-ist 120 bzw. Ausgangswert; bei lokal abweichender Grenze den Befund protokollieren statt die Grenze zu verlangen, nur wenn das Skript dafür eine Stellschraube hat); 92d mit lokalem Digest und Daemon (erwarteter Ausgang: Tor-Abbruch 41, weil der lokale Bestand nicht 52.111 / 37 / 0 ist, nach durchlaufenem occ upgrade und Registrierung ohne --rm-data; Bestand danach unverändert gegenüber vorher); 95c mit einem Begriff, der lokal Treffer hat (kaltstart-gueltig ja); 94c mit kleinem Zyklus-Ordner (Marken A, C1, C2 geschrieben); 98d per docker cp und exec gegen den lokalen Backend-Container (Rückgabe 0 oder protokolliertes 49 mit Befundanalyse); 00-wegwerf.sh b3 und b5 gegen das lokale Abbild (amd64, Werte ohne Aussagekraft, nur Lauffähigkeit). 00-lauf.sh wird wegen sudo shutdown und Wirtspfaden nicht lokal gefahren; statt dessen `sh -n` und ein Lauf von `00-lauf.sh` ohne Laufwerte (Rückgabe 2). Jeder Befund, der kein erwarteter Tor-Ausgang ist, wird im betroffenen Werkzeug behoben, der zugehörige Test um den Fall ergänzt, und die Probe wiederholt. Ergebnis als Tabelle Werkzeug, Probe, erwarteter Ausgang, tatsächlicher Ausgang, Befund in README Abschnitt "Generalprobe" (Prosa Deutsch, keine lokalen Pfade, keine Adressen).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_v13_ablauf.py tests/test_v13_wechsel.py tests/test_v13_zyklen.py tests/test_v13_wegwerf.py tests/test_dismax_probe.py tests/test_ops_scripts.py tests/test_measurement_scripts.py tests/test_public_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - README Abschnitt "Generalprobe" hat eine Zeile je Werkzeug (mindestens 13 Zeilen: W1, W2, W3 über b3, 90e, 91m, 92d, 92e, 94c, 95c, 98d, 00-wegwerf b3, 00-wegwerf b5, 00-lauf ohne Laufwerte)
    - jede Zeile hat tatsächlichen Ausgang gleich erwartetem Ausgang oder einen benannten behobenen Befund
    - `git status --porcelain docs/measurements/2026-09-v13-messung/rohdaten` zeigt keine Generalprobe-Dateien
    - alle genannten Tests grün
  </acceptance_criteria>
  <done>Kein Box-Werkzeug läuft auf der Box zum ersten Mal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner-Freigabe für den Push (Voraussetzung W4)</name>
    <read_first>
      - docs/measurements/2026-09-v13-messung/README.md Abschnitt Generalprobe
      - Ausgabe von git log --oneline origin/main..HEAD
    </read_first>
    <acceptance_criteria>
      - Owner-Antwort "push freigegeben" liegt vor, bevor Task 3 pusht
    </acceptance_criteria>
  <what-built>Welle 1 und 2 (W1 bis W4, alle Box-Werkzeuge, Ablauf, Erwartungen) plus Generalprobe, lokal committet, alle Gates grün. Der W4-Lauf braucht den Workflow-Stand auf GitHub und das Abbild des gepushten Commits.</what-built>
  <how-to-verify>
    1. `git log --oneline origin/main..HEAD` ansehen: nur Phase-22-Commits (Werkzeuge, Tests, Doku) und gegebenenfalls Phase-23-Commits, die der Owner mitgehen lassen will.
    2. README Abschnitt "Generalprobe" lesen: alle Werkzeuge mit erwartetem Ausgang.
    3. Bestätigen, dass der Push auf main erfolgen darf; der Push löst die Abbildstrecke aus (backend/** geändert), deren Digest die Box später nutzt.
  </how-to-verify>
  <files>docs/measurements/2026-09-v13-messung/README.md (nur lesen)</files>
  <action>Dem Owner die Commit-Liste und den Abschnitt Generalprobe vorlegen und auf die Push-Freigabe warten; ohne Freigabe wird nicht gepusht.</action>
  <verify>Owner-Antwort liegt vor</verify>
  <done>Push ist freigegeben oder die Einwände sind festgehalten.</done>
  <resume-signal>"push freigegeben" oder Einwände</resume-signal>
  <result>Freigegeben am 26.09.2026: Der Owner antwortete "weiter" auf die Frage "Darf ich die 26 Commits auf main pushen?". Push 61255f3..59f05fe ohne force. Dieselbe Freigabe deckt die Folgepushes von 22-06 (92d-Fix, probe-92d.yml, W4-Rohdaten, Summary). Zusatzauftrag Owner-Option B: 92d Phase B als eigener CI-Lauf.</result>
</task>

<task type="auto">
  <name>Task 3: Push, W4-Lauf, F4 und B7 ablesen</name>
  <files>docs/measurements/2026-09-v13-messung/rohdaten/w4-ci-arm64/, docs/measurements/2026-09-v13-messung/README.md</files>
  <read_first>
    - .github/workflows/measure.yml Job slots (aus 22-01)
    - docs/measurements/2026-09-v13-messung/skripte/00-ablauf.md Abschnitt 8
    - docs/measurements/2026-09-v12-messung/rohdaten/03-aufbau.txt (Präzedenz: Digest mit Laufnummer der Abbildstrecke)
  </read_first>
  <action>
Push auf main. Die durch den Push ausgelösten Läufe mit `gh run list --commit <sha>` abwarten; die Abbildstrecke muss grün sein. Digest per `docker buildx imagetools inspect ghcr.io/street1983nk/findling_backend:dev` ablesen und gegen den Merge-Job der Abbildstrecke dieses Commits belegen (Laufnummer notieren). Dann `gh workflow run measure.yml --ref main -f image_ref=<sha256-Digest>` und den Lauf bis zum Ende verfolgen (`gh run watch`). Artefakt `w4-arm64` per `gh run download <id> -n w4-arm64 -D docs/measurements/2026-09-v13-messung/rohdaten/w4-ci-arm64` holen. Ist der Job slots rot, Ursache aus dem Log bestimmen, im Werkzeug beheben (Test ergänzen, Gates grün), erneut nach Owner-Freigabe pushen und neu fahren; nie einen roten Lauf als Messung verwenden. README Abschnitt "W4-Vorabkurve": Laufnummer, Digest, Maschinenblatt (nproc, CPU-Modell), Tabelle Seiten je Sekunde Median für N 1, 2, 4, Faktoren N2/N1 und N4/N1, anon_bytes_delta je N, single-Modus OMP_THREAD_LIMIT=1 gegen unset, bench Tokens je Sekunde für T 1, 2, 4, B7-Wert gegen 24,2 bis 25,3 MB (amd64 produktnah) und 17,6 MB (Research), F4 mit drei Nachkommastellen, und die Zeile "D-03 angewandt: F4 = <x>, B4 <gefahren|entfaellt>". Liegt F4 unter 1,5, zusätzlich die Zeile "K1 ausgeloest: BL-F04-Stufe 2 kippt" (Vorarbeit Abschnitt 5). Zusätzlich die Zeile "Box-Digest-Kandidat: <digest>, Abbildstrecke Lauf <nr>". test_public_artifacts.py über die Rohdaten laufen lassen, dann Rohdaten und README committen.
  </action>
  <verify>
    <automated>test -s docs/measurements/2026-09-v13-messung/rohdaten/w4-ci-arm64/f4.txt && grep -E "^F4 [0-9]+\.[0-9]{3}$" docs/measurements/2026-09-v13-messung/rohdaten/w4-ci-arm64/f4.txt && grep -c "D-03 angewandt" docs/measurements/2026-09-v13-messung/README.md && cd backend && uv run pytest tests/test_public_artifacts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - f4.txt enthält genau eine Zeile "F4 <Zahl mit drei Nachkommastellen>"
    - README enthält "D-03 angewandt: F4 =", "Box-Digest-Kandidat: sha256:" und die Laufnummer des measure.yml-Laufs
    - Rohdaten w3-slots-1.txt, w3-slots-2.txt, w3-slots-4.txt, w3-single.txt, bench-threads-1.txt, bench-threads-2.txt, bench-threads-4.txt, b7-nl-automat.txt liegen im Repo
    - test_public_artifacts.py grün
  </acceptance_criteria>
  <done>F4 ist gemessen und nach D-03 angewandt, B7 steht, der Box-Digest-Kandidat ist belegt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo -> GitHub (Push) | öffentlicher Stand, Owner-Freigabe nötig |
| CI-Artefakt -> Repo | fremd erzeugte Rohdaten werden committet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-22 | Tampering | Push auf main | mitigate | blockierender Owner-Checkpoint vor dem Push |
| T-22-23 | Information Disclosure | W4-Rohdaten | mitigate | test_public_artifacts.py vor dem Commit |
| T-22-24 | Tampering | falsches Abbild gemessen | mitigate | Digest per imagetools gegen Laufnummer der Abbildstrecke belegt (Pitfall 11) |
| T-22-25 | Information Disclosure | Generalprobe-Ausgaben | mitigate | OUT außerhalb des Repos, keine lokalen Pfade oder Adressen im README |
</threat_model>

<verification>
F4 in f4.txt und README, D-03-Zeile, Generalprobe-Tabelle vollständig, test_public_artifacts.py grün, alle v13-Tests grün.
</verification>

<success_criteria>
W4 ist gelaufen und vor dem Rechenblatt ausgewertet; jedes Werkzeug hat seinen Erstvollzug hinter sich.
</success_criteria>

<output>
Create `.planning/phases/22-messanfahrt-bl-f03/22-06-SUMMARY.md` when done
</output>
