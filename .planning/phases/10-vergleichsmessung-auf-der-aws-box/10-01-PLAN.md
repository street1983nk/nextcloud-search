---
phase: 10-vergleichsmessung-auf-der-aws-box
plan: 01
type: execute
wave: 1
# Welle 1 und ohne jede Box-Minute: beide Luecken, die die Recherche gefunden
# hat, machen Messwerkzeug unbrauchbar, das der Lauf ab Welle 5 benutzt. Sie
# gehoeren vor die Anfahrt, nicht in den gedeckelten Lauf.
depends_on: []
files_modified:
  - .gitattributes
  - docs/measurements/2026-09-grundlast-fein/skripte/01-grundlast-fein.py
  - docs/measurements/2026-09-grundlast-fein/skripte/02-nachmessung-fauler-bau.py
  - docs/measurements/2026-09-nachmessung-m7g/skripte/66-generator-endungen.py
  - docs/measurements/2026-09-nachmessung-m7g/skripte/68-bestand-endungen.py
  - docs/measurements/2026-09-nachmessung-m7g/skripte/69-ocr-drei-sprachen.py
  - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py
  - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.sh
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [MESS-01, MESS-03]

must_haves:
  truths:
    - "Ein Messskript unter docs/measurements/**/skripte/ kommt aus einem Windows-Checkout mit LF und startet auf der Box ueber seine Shebang, ohne dass jemand python3 davorsetzt"
    - "Der Baumhash-Beweis schreibt beide Hashes in eine eigene Rohdatei, und eine leere Datei faellt auf statt durchzugehen"
    - "Das Rezept des Baumhashes ist dasselbe wie in 40-abbild.sh: sortierte relative Pfade, Zeilenenden normalisiert, sha256 je Datei"
    - "Ein Gate wird rot, wenn ein Messskript dieses Laufs einen Wagenruecklauf, einen Gedankenstrich, einen Maschinenpfad oder ein Passwort in einem Argument traegt"
  artifacts:
    - path: ".gitattributes"
      provides: "die fehlende Regel fuer die Python-Messskripte, mit Begruendung im Kommentarstil der Datei"
      contains: "docs/measurements"
    - path: "docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py"
      provides: "das Baumhash-Rezept als Datei, ein Wurzelverzeichnis und ein Glob als Argumente, ohne Heredoc"
      contains: "replace"
    - path: "docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.sh"
      provides: "der Beweisschritt mit eigener Rohdatei: Abbild, Arbeitsbaum backend, Arbeitsbaum php"
      contains: "docker cp"
    - path: "backend/tests/test_measurement_scripts.py"
      provides: "die Hausordnung fuer Messskripte, die test_ops_scripts.py nur fuer scripts/ops haelt"
      contains: "carriage"
  key_links:
    - from: "docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.sh"
      to: "docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py"
      via: "das Rezept wird als Datei in den Container kopiert und als Argument gerufen, nicht als stdin"
      pattern: "40b-baumhash.py"
    - from: "backend/tests/test_measurement_scripts.py"
      to: "docs/measurements/2026-09-vergleichsmessung-m7g/skripte/"
      via: "das Gate liest die Skripte dieses Laufs aus dem Verzeichnis, nicht aus einer Liste"
      pattern: "2026-09-vergleichsmessung-m7g"
---

<objective>
Die zwei Werkzeugluecken schliessen, die die Recherche belegt hat, bevor die Box
angefahren wird: der Baumhash-Beweis, der in beiden Vorlaeuferberichten eine
leere Rohdatei hinterlassen hat, und die fehlende Zeilenende-Regel fuer die
Python-Messskripte.

Purpose: Erfolgskriterium 1 verlangt "den Beleg, dass Abbild und Arbeitsbaum
derselbe Stand sind". Genau dieser Beleg fehlt in `2026-09-05-semantiklauf-m7g`
und in `2026-09-nachmessung-m7g` als Rohdatei; beide Berichte behaupten die
Gleichheit, und in beiden ist unter der Abschnittsueberschrift keine Zeile.
Ursache in `40-abbild.sh`: das `docker run` traegt kein `-i`, also bekommt der
Python im Abbild kein stdin und liest sofort EOF. Ein dritter Bericht mit
derselben leeren Zeile waere ein Kriterium, das aus Gewohnheit als erfuellt gilt.

Erfolgskriterium 4 verlangt Skripte im Repo und einen wiederholbar
beschriebenen Lauf. `.gitattributes` deckt `*.sh` global und `scripts/ops/*.py`
ausdruecklich, aber nicht `docs/measurements/**/skripte/*.py`, und der Kommentar
dort nennt die Einschraenkung sogar als Absicht. Gemessen im Arbeitsbaum: fuenf
Python-Messskripte liegen mit CRLF im Checkout. Ein `#!/usr/bin/env python3`
mit Wagenruecklauf laesst den Kernel nach einem Interpreter suchen, dessen Name
auf ein unsichtbares Zeichen endet, und die Fehlermeldung nennt das Zeichen
nicht.

Beides kostet null Box-Minuten. Deshalb steht es in Welle 1: der Lauf ab
Welle 5 ist gedeckelt, und eine Werkzeugreparatur in einem gedeckelten Lauf ist
Laufzeit, die keine Zahl erzeugt.

Output: die Regel in `.gitattributes`, fuenf renormalisierte Dateien, der
Baumhash-Beweis als Skriptpaar mit eigener Rohdatei, und ein Gate, das die
Hausordnung von `scripts/ops` auf die Messskripte dieses Laufs uebertraegt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-vergleichsmessung-auf-der-aws-box/10-RESEARCH.md

<interfaces>
Das Baumhash-Rezept, wortgleich aus `40-abbild.sh` und `61-wechsel.sh`, und es
darf sich nicht aendern, weil die Zahlen sonst nicht vergleichbar sind:

  sha256 ueber die sortierte Folge aus
    relativer Pfad (posix) + NUL + hexdigest(sha256 des Inhalts) + Zeilenumbruch
  Inhalt vorher: read_bytes().replace(b"\r\n", b"\n")
  Wurzel im Abbild:      /app/.venv/lib/python3.13/site-packages/findling, Glob **/*.py
  Wurzel im Arbeitsbaum: backend/src/findling,                             Glob **/*.py
  Wurzel der PHP-Haelfte: php,                                             Glob **/*.php
  Ausgabe: zwei Zeilen, "dateien: <n>" und "baumhash: <hex>"

Die Werte des heutigen Arbeitsbaums, selbst nachgerechnet am 09.09.2026 auf
Commit bd91d2c (identisch mit den Werten der Recherche auf 888e239, weil
bd91d2c nur .planning/ angefasst hat):

  backend/src/findling  54 Dateien  6c47cd219c430bccc9d5d57b1de1d2ff9f8672fa4f42b1160d0a31efb9367476
  php                   58 Dateien  26b55908b12f8139b86d8c8a7c391457c550b2584fb8d639a3957e8b6feb91ae

Zum Vergleich der Nachmessung vom 07.09.: backend 53 Dateien / 278fab52...,
php 47 Dateien / c203da57...

Die fuenf Python-Messskripte, die heute mit CRLF im Arbeitsbaum liegen
(`git check-attr text eol` liefert fuer sie zweimal `unspecified`):

  docs/measurements/2026-09-grundlast-fein/skripte/01-grundlast-fein.py
  docs/measurements/2026-09-grundlast-fein/skripte/02-nachmessung-fauler-bau.py
  docs/measurements/2026-09-nachmessung-m7g/skripte/66-generator-endungen.py
  docs/measurements/2026-09-nachmessung-m7g/skripte/68-bestand-endungen.py
  docs/measurements/2026-09-nachmessung-m7g/skripte/69-ocr-drei-sprachen.py

Alle uebrigen Dateien unter docs/measurements/**/skripte/ liegen mit LF, und
keine einzige traegt heute einen Em-Dash oder En-Dash (selbst gemessen).

Was ruff und CI heute wirklich abdecken, damit dieser Plan keine Zusage macht,
die er nicht halten kann: `.github/workflows/python.yml` ruft `ruff check .` und
`ruff format --check .` in `backend/` und dazu `ruff check --config
pyproject.toml ../scripts`. `docs/measurements` ist in keinem der vier Aufrufe
enthalten, und die Altbestaende dort sind weder ruff-clean (16 Befunde) noch
ruff-format-clean (8 Dateien). Deshalb wird ruff in diesem Plan ausschliesslich
auf das NEUE Verzeichnis gerufen, und die Altbestaende bleiben unangetastet: ein
Reformatieren von Skripten, deren Rohdaten daneben liegen, wuerde die Herkunft
dieser Rohdaten verwischen, ohne eine Zahl zu verbessern.

Die Hausordnung, die es fuer `scripts/ops` schon gibt und die dieses Plan-Gate
fuer die Messskripte nachbaut (`backend/tests/test_ops_scripts.py`):
  MACHINE_SHAPES = ("/home/", "sys.path.insert", "sys.path.append", "drillhelfer")
  machine_shapes(text) -> sortierte Liste der gefundenen Formen
  test_the_script_carries_neither_a_dash_nor_a_carriage_return
  test_the_machine_path_gate_fires_on_a_staged_sample (das Gate wird an einer
    gestellten Probe rot gemacht, damit es nicht gruen bleibt, wenn sein Rumpf
    verschwindet)

Warum das Gate NICHT ueber alle Messskripte laufen darf: `45-suchlast.py` des
Semantiklaufs traegt `sys.path.insert(0, "/home/ubuntu/work")` und
`from drillhelfer import suche`, und `drillhelfer` liegt nicht im Repo. Diese
Datei ist Geschichte mit Rohdaten daneben. Der Maschinenpfad-Teil des Gates
gilt deshalb nur fuer das Verzeichnis dieses Laufs; der Wagenruecklauf- und der
Gedankenstrich-Teil gelten fuer alle, weil sie heute schon ueberall erfuellt
sind (nach Task 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Zeilenende-Regel und die fuenf Altfaelle</name>
  <files>.gitattributes, docs/measurements/2026-09-grundlast-fein/skripte/01-grundlast-fein.py, docs/measurements/2026-09-grundlast-fein/skripte/02-nachmessung-fauler-bau.py, docs/measurements/2026-09-nachmessung-m7g/skripte/66-generator-endungen.py, docs/measurements/2026-09-nachmessung-m7g/skripte/68-bestand-endungen.py, docs/measurements/2026-09-nachmessung-m7g/skripte/69-ocr-drei-sprachen.py</files>
  <read_first>
    - .gitattributes vollstaendig: alle sieben Bloecke, besonders der Kommentar ueber `scripts/ops/*.py`, der die Verengung auf dieses Verzeichnis ausdruecklich begruendet, und der Block `testdata/corpus/** -text`, dessen Begruendung zeigt, wie eine Ausnahme in dieser Datei geschrieben wird
    - docs/measurements/2026-09-nachmessung-m7g/skripte/66-generator-endungen.py, mindestens Kopf und Shebang (die Datei, deren Shebang heute auf einem Wagenruecklauf endet)
    - .planning/phases/10-vergleichsmessung-auf-der-aws-box/10-RESEARCH.md, Pitfall 2 vollstaendig
  </read_first>
  <action>
    Einen achten Block in `.gitattributes` ergaenzen, unmittelbar hinter dem
    Block `scripts/ops/*.py text eol=lf`, weil er dessen Begruendung fortsetzt.
    Die Regel lautet `docs/measurements/**/skripte/*.py text eol=lf`. Der
    Kommentar darueber wird im Stil der Datei geschrieben (englisch, Prosa,
    keine Gedankenstriche) und nennt drei Dinge: dass diese Dateien auf einer
    Linux-Box ueber ihre Shebang gestartet werden und damit unter derselben
    Regel stehen wie die Skripte von `scripts/ops`; dass der Kommentar des
    Blocks darueber die Verengung auf `scripts/ops` mit dem Argument begruendet
    hat, `backend/` werde importiert und nie ueber seine Shebang ausgefuehrt,
    was fuer die Messskripte gerade nicht gilt; und dass die Regel den naechsten
    Fall verhindert, waehrend der ausdrueckliche Aufruf ueber `python3 <datei>`
    im Ablaufplan des Laufs den aktuellen ueberlebt (beides, wie die Recherche
    empfiehlt, nicht eines von beiden).

    Danach die fuenf genannten Altfaelle renormalisieren, also die Dateien mit
    LF neu schreiben, ohne ein Zeichen ihres Inhalts zu aendern. Der Weg ist
    Byte fuer Byte: Inhalt binaer lesen, `\r\n` durch `\n` ersetzen, binaer
    zurueckschreiben. Kein Reformatieren, kein ruff --fix, keine Umbenennung von
    Bezeichnern: diese Dateien haben Rohdaten neben sich liegen, und eine
    inhaltliche Aenderung wuerde die Herkunft dieser Rohdaten verwischen.

    Danach `git add --renormalize` fuer die fuenf Dateien fahren, damit der Index
    die Regel sieht, und mit `git check-attr text eol` gegenpruefen, dass die
    Attribute jetzt `text: set` und `eol: lf` statt zweimal `unspecified`
    liefern.

    Die Altbestaende unter docs/measurements bleiben sonst unangetastet. Sie
    sind nicht ruff-clean, und das ist kein Befund dieses Plans: ruff laeuft in
    CI ueber `backend/` und `../scripts`, nie ueber `docs/measurements`.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -c "import pathlib,subprocess; a=pathlib.Path('../.gitattributes').read_text(encoding='utf-8'); assert 'docs/measurements/**/skripte/*.py text eol=lf' in a, 'the rule is missing'; assert chr(0x2014) not in a and chr(0x2013) not in a; fs=['../docs/measurements/2026-09-grundlast-fein/skripte/01-grundlast-fein.py','../docs/measurements/2026-09-grundlast-fein/skripte/02-nachmessung-fauler-bau.py','../docs/measurements/2026-09-nachmessung-m7g/skripte/66-generator-endungen.py','../docs/measurements/2026-09-nachmessung-m7g/skripte/68-bestand-endungen.py','../docs/measurements/2026-09-nachmessung-m7g/skripte/69-ocr-drei-sprachen.py']; [__import__('sys').exit('CRLF still in '+f) for f in fs if b'\r\n' in pathlib.Path(f).read_bytes()]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitattributes` enthaelt die Zeile `docs/measurements/**/skripte/*.py text eol=lf`.
    - Der Kommentar darueber nennt die Shebang-Ausfuehrung auf der Box, den Unterschied zum importierten Python von `backend/`, und dass die Regel und der ausdrueckliche `python3 <datei>`-Aufruf zusammen gelten.
    - `git check-attr text eol -- docs/measurements/2026-09-grundlast-fein/skripte/01-grundlast-fein.py` liefert `text: set` und `eol: lf`.
    - Keine der fuenf genannten Dateien enthaelt noch ein `\r\n`-Byte-Paar.
    - `git diff --stat` der fuenf Dateien zeigt ausschliesslich Zeilenende-Aenderungen: `git diff --ignore-all-space --stat` ueber diese fuenf Dateien ist leer.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Ein Messskript aus diesem Checkout startet auf der Box ueber seine eigene Shebang, und die naechste Datei, die dort angelegt wird, bekommt die Regel geschenkt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Der Baumhash-Beweis als Skriptpaar mit eigener Rohdatei</name>
  <files>docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py, docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.sh</files>
  <read_first>
    - docs/measurements/2026-09-05-semantiklauf-m7g/skripte/40-abbild.sh vollstaendig: das Rezept im Wortlaut und die Stelle, an der das `docker run` ohne `-i` steht, also die Ursache der leeren Rohdatei
    - docs/measurements/2026-09-nachmessung-m7g/skripte/61-wechsel.sh vollstaendig: dieselbe Stelle mit ergaenztem `-i`, und die Beobachtung, dass die Ausgabe trotzdem fehlt
    - docs/measurements/2026-09-05-semantiklauf-m7g/rohdaten/40-abbild.log um die Zeilen 50 bis 60 und docs/measurements/2026-09-nachmessung-m7g/rohdaten/61-wechsel.txt um die Zeilen 1 bis 10 (der Abschnittstitel ohne Zeilen darunter, das Warnzeichen)
    - backend/tests/test_ops_scripts.py, die Hausordnung fuer scripts/ops als Muster fuer Docstring-Form und Zusicherungen
    - .planning/phases/10-vergleichsmessung-auf-der-aws-box/10-RESEARCH.md, Pitfall 1 und Muster 1 vollstaendig
  </read_first>
  <behavior>
    - Zwei Verzeichnisse mit demselben Dateibestand, eines mit CRLF und eines mit LF, ergeben denselben Baumhash (das ist der Zweck der Normalisierung).
    - Ein geaendertes Byte in einer Datei aendert den Baumhash.
    - Eine umbenannte Datei aendert den Baumhash, weil der relative Pfad in den Digest eingeht.
    - Der Aufruf ueber `backend/src/findling` mit Glob `**/*.py` liefert `dateien: 54` und `baumhash: 6c47cd21...`; der Aufruf ueber `php` mit `**/*.php` liefert `dateien: 58` und `baumhash: 26b55908...` (Stand bd91d2c).
    - Ein nicht existierendes Wurzelverzeichnis fuehrt zu einem Abbruch mit Meldung und Rueckgabewert ungleich null, nicht zu `dateien: 0` und einem Hash ueber nichts.
  </behavior>
  <action>
    `40b-baumhash.py` anlegen: das Rezept aus <interfaces>, Wurzelverzeichnis und
    Glob als Positionsargumente, Ausgabe genau die zwei Zeilen `dateien: <n>` und
    `baumhash: <hex>`. Docstring englisch, und er nennt zwei Dinge: dass das
    Rezept wortgleich das von `40-abbild.sh` ist und deshalb nicht veraendert
    werden darf, weil ein anderes Rezept einen anderen Hash liefert und die
    Vergleichbarkeit gegen `278fab52...` und `6c47cd21...` zerstoert; und dass
    das Skript keinen Dateiinhalt ausgibt, sondern nur Pfad und Hash, weil es
    ueber Nutzerdateien laufen koennte. Ein leeres Ergebnis ist ein Fehler: bei
    null gefundenen Dateien oder fehlendem Wurzelverzeichnis wird mit einer
    Meldung auf stderr und Rueckgabewert 2 abgebrochen. Nur Standardbibliothek,
    keine Fremdabhaengigkeit, damit es im Abbild laeuft, das offline ist.

    `40b-baumhash.sh` anlegen: der Beweisschritt, der in beiden Vorlaeufern leer
    geblieben ist, jetzt als eigener Schritt mit eigener Rohdatei
    `40b-baumhash.txt`. Er faehrt drei Aufrufe und schreibt alle drei in dieselbe
    Datei, jeder mit einer Abschnittszeile darueber:

      1. Im Abbild: das Rezept mit `docker cp` in den laufenden oder mit `-v
         <skripte>:/skripte:ro` in einen wegwerfbaren Container geben und mit
         `--entrypoint /app/.venv/bin/python "$IMAGE" /skripte/40b-baumhash.py
         /app/.venv/lib/python3.13/site-packages/findling '**/*.py'` rufen. Nicht
         als Heredoc auf stdin: genau daran ist der Schritt in `40-abbild.sh`
         gescheitert (kein `-i`, also EOF), und `61-wechsel.sh` hat `-i`
         ergaenzt und die Ausgabe fehlt trotzdem. Ein Argument braucht kein
         stdin.
      2. Im Arbeitsbaum: `python3 40b-baumhash.py <repo>/backend/src/findling '**/*.py'`.
      3. In der PHP-Haelfte: `python3 40b-baumhash.py <repo>/php '**/*.php'`.

    Danach vergleicht das Skript die Ausgaben von 1 und 2 selbst und schreibt
    eine Urteilszeile `baumhash-gleich ja` oder `baumhash-gleich nein` samt
    beider Hashes. Anschliessend prueft es, dass `40b-baumhash.txt` nicht leer
    ist und drei `baumhash:`-Zeilen traegt, und beendet sich mit einem Fehler,
    wenn das nicht so ist. Der Vorlaeuferfehler ist genau der, dass niemand
    danach geschaut hat.

    `set -eu`, POSIX-Shell, Kommentare englisch, keine Gedankenstriche, kein
    Wagenruecklauf. Das Abbild kommt als Variable `IMAGE` mit Vorgabe
    `ghcr.io/street1983nk/findling_backend:dev`; das Repo-Verzeichnis auf der
    Box und das Ausgabeverzeichnis kommen als Variablen mit Vorgaben, damit die
    Datei nicht den Pfad einer Maschine traegt.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; sh -n ../docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.sh &amp;&amp; uv run python ../docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py ../backend/src/findling "**/*.py" &amp;&amp; uv run python ../docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py ../php "**/*.php" &amp;&amp; uv run python -m pytest tests/test_measurement_scripts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `40b-baumhash.py ../backend/src/findling "**/*.py"` gibt `dateien: 54` und `baumhash: 6c47cd219c430bccc9d5d57b1de1d2ff9f8672fa4f42b1160d0a31efb9367476` aus.
    - `40b-baumhash.py ../php "**/*.php"` gibt `dateien: 58` und `baumhash: 26b55908b12f8139b86d8c8a7c391457c550b2584fb8d639a3957e8b6feb91ae` aus.
    - Ein Aufruf gegen ein nicht existierendes Verzeichnis beendet sich mit Rueckgabewert 2 und einer Meldung auf stderr, und gibt keine `baumhash:`-Zeile aus.
    - `40b-baumhash.sh` ruft das Rezept als Argument und nicht ueber ein Heredoc: die Datei enthaelt `40b-baumhash.py` als Argument und kein `<<'PY'`.
    - `40b-baumhash.sh` prueft am Ende selbst, dass die Rohdatei drei `baumhash:`-Zeilen traegt, und bricht sonst mit einem Fehler ab.
    - `40b-baumhash.sh` traegt keinen Pfad einer Maschine: kein `/home/` ausserhalb einer Variablenvorgabe, die als solche kommentiert ist.
    - `sh -n` ueber das Shellskript ohne Befund; nur Standardbibliothek im Python-Skript.
    - Kein Em-Dash, kein En-Dash, kein Wagenruecklauf in beiden Dateien.
  </acceptance_criteria>
  <done>Der Satz "Abbild und Arbeitsbaum sind derselbe Stand" hat ab jetzt eine Rohdatei unter sich, und eine leere Rohdatei laesst das Skript scheitern statt den Bericht.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Das Gate ueber die Messskripte dieses Laufs</name>
  <files>backend/tests/test_measurement_scripts.py</files>
  <behavior>
    - Ein gestelltes Textbeispiel mit `\r\n` macht die Wagenruecklauf-Zusicherung rot (das Gate wird an einer Probe rot gemacht, nicht behauptet).
    - Ein gestelltes Textbeispiel mit U+2014 macht die Gedankenstrich-Zusicherung rot.
    - Ein gestelltes Textbeispiel mit `sys.path.insert(0, "/home/ubuntu/work")` macht die Maschinenpfad-Zusicherung rot.
    - Ueber den echten Bestand sind alle vier Zusicherungen gruen (nach Task 1 und Task 2).
    - Zwei Verzeichnisse mit demselben Bestand, eines CRLF und eines LF, ergeben denselben Baumhash aus `40b-baumhash.py`; ein geaendertes Byte aendert ihn.
  </behavior>
  <read_first>
    - backend/tests/test_ops_scripts.py vollstaendig: die Konstanten OPS_DIR bis SEARCH_LOAD, MACHINE_SHAPES, machine_shapes(), imported_packages(), und besonders test_the_script_carries_neither_a_dash_nor_a_carriage_return und test_the_machine_path_gate_fires_on_a_staged_sample als Muster fuer die Form der Zusicherungen
    - backend/pyproject.toml, die Abschnitte [tool.ruff], [tool.ruff.lint] (der Regelsatz) und [tool.ruff.lint.per-file-ignores] (S101 nur unter tests/)
    - docs/measurements/2026-09-05-semantiklauf-m7g/skripte/45-suchlast.py, Zeilen 1 bis 35 (der Altfall mit Maschinenpfad und drillhelfer, der belegt, warum der Maschinenpfad-Teil des Gates nicht ueber alle Messskripte laufen darf)
    - docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.py aus Task 2
  </read_first>
  <action>
    Eine neue Testdatei `backend/tests/test_measurement_scripts.py` anlegen, in
    der Form von `test_ops_scripts.py`, mit zwei unterschiedlich weiten
    Geltungsbereichen und der Begruendung fuer den Unterschied im Modul-Docstring:

    Weit, ueber alle Dateien unter `docs/measurements/**/skripte/` mit Endung
    `.py` oder `.sh` (andere Dateien werden ausgelassen, im Verzeichnis des
    Semantiklaufs liegt eine `.claude-active`):
      - kein `\r\n` in den Bytes,
      - kein U+2014 und kein U+2013.
    Beide sind nach Task 1 fuer den ganzen Bestand erfuellt, also ist der weite
    Bereich haltbar und keine Wette.

    Eng, nur ueber `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/`:
      - jede Datei beginnt mit einer Shebang (`#!/bin/sh` oder
        `#!/usr/bin/env python3`),
      - keine Form aus MACHINE_SHAPES ausserhalb einer kommentierten
        Variablenvorgabe; die Liste wird aus `test_ops_scripts.py` uebernommen
        und nicht neu erfunden, damit beide Gates dieselbe Definition benutzen,
      - kein Passwort in einer Kommandozeile: die Datei enthaelt weder
        `--password ` noch `-p $` noch eine Zeichenkette, die ein Passwort
        einem Argument statt einer Umgebungsvariablen zuweist. Der erlaubte Weg
        ist der von `search_load.py`, also `--password-env`.
    Der Modul-Docstring nennt den Grund fuer die Verengung: `45-suchlast.py` des
    Semantiklaufs traegt `sys.path.insert(0, "/home/ubuntu/work")` und
    `from drillhelfer import suche`, `drillhelfer` liegt nicht im Repo, und diese
    Datei ist Geschichte mit Rohdaten daneben. Ein Gate, das sie umschreiben
    verlangt, wuerde die Herkunft dieser Rohdaten verwischen.

    Dazu drei Zusicherungen, die die Gates an gestellten Proben rot machen
    (Muster `test_the_machine_path_gate_fires_on_a_staged_sample`), damit keines
    von ihnen gruen bleibt, wenn sein Rumpf verschwindet.

    Dazu zwei Zusicherungen ueber das Rezept aus Task 2, gefahren ueber
    `tmp_path`: derselbe Bestand mit CRLF und mit LF ergibt denselben Baumhash,
    und ein geaendertes Byte ergibt einen anderen. Das Skript wird dafuer als
    Unterprozess mit `sys.executable` gerufen, nicht importiert, weil sein Name
    mit einer Ziffer beginnt und kein gueltiger Modulname ist.

    Kommentare und Testnamen englisch, Testnamen in der Form ganzer Saetze wie
    in `test_ops_scripts.py`. Die Datei muss durch ruff mit dem Vollregelsatz,
    `ruff format --check`, pyright und vulture gehen; S101 ist unter `tests/`
    ausgenommen und braucht keine Ausnahme.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search/backend &amp;&amp; uv run python -m pytest tests/test_measurement_scripts.py -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ruff check --config pyproject.toml ../docs/measurements/2026-09-vergleichsmessung-m7g/skripte &amp;&amp; uv run ruff format --config pyproject.toml --check ../docs/measurements/2026-09-vergleichsmessung-m7g/skripte &amp;&amp; uv run pyright &amp;&amp; uv run vulture src tests --min-confidence 80</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_measurement_scripts.py -q` ist gruen, und die Datei fuehrt mindestens neun Zusicherungen.
    - Drei Zusicherungen machen die drei Gates an gestellten Proben rot, jede mit dem gestellten Text im Rumpf.
    - Der weite Bereich deckt alle `.py`- und `.sh`-Dateien unter `docs/measurements/**/skripte/`, gelesen aus dem Verzeichnis und nicht aus einer Liste von Dateinamen.
    - Der enge Bereich deckt genau `docs/measurements/2026-09-vergleichsmessung-m7g/skripte/`, und der Modul-Docstring nennt `45-suchlast.py` und `drillhelfer` als Grund fuer die Verengung.
    - MACHINE_SHAPES wird aus `test_ops_scripts.py` bezogen oder als woertlich identische Konstante mit einem Verweis darauf gefuehrt; zwei auseinanderlaufende Definitionen sind ein Fehlschlag.
    - `ruff check .`, `ruff format --check .`, `pyright` und `vulture src tests` in `backend/` sind gruen; `ruff check` und `ruff format --check` ueber `../docs/measurements/2026-09-vergleichsmessung-m7g/skripte` sind gruen.
    - Die Altbestaende unter `docs/measurements` sind ausser den fuenf Zeilenende-Renormalisierungen aus Task 1 unveraendert: `git diff --name-only` nennt keine weitere Datei dort.
  </acceptance_criteria>
  <done>Die Hausordnung, die scripts/ops seit Phase 5 hat, gilt ab jetzt auch fuer die Messskripte dieses Laufs, und sie ist an Proben rot gemacht worden statt behauptet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arbeitsbaum zu Abbild | Der Bericht behauptet, einen Codestand gemessen zu haben; der Baumhash ist der einzige Beleg, und `:dev` ist ein wandernder Zeiger |
| Windows-Checkout zu Linux-Box | Zeilenenden wechseln beim Auschecken; ein Wagenruecklauf in einer Shebang ist ein Fehler ohne lesbare Meldung |
| Messskript zu Rohdatei | Ein Skript, das Nutzerinhalte lesen kann, darf sie nicht in eine committete Rohdatei schreiben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Repudiation | Der Baumhash-Beweis bleibt wie in beiden Vorlaeufern leer und das Kriterium gilt aus Gewohnheit als erfuellt | mitigate | Eigener Schritt, eigene Rohdatei `40b-baumhash.txt`, Aufruf als Argument statt ueber stdin, und das Skript prueft selbst auf drei `baumhash:`-Zeilen und bricht sonst ab |
| T-10-02 | Denial of Service | Ein Messskript startet auf der Box nicht, weil seine Shebang auf einem Wagenruecklauf endet, mitten in einem gedeckelten Lauf | mitigate | `.gitattributes`-Regel plus Renormalisierung der fuenf Altfaelle plus Gate ueber den ganzen Bestand |
| T-10-03 | Information Disclosure | Ein neues Messskript nimmt ein Passwort als Argument und es steht in der Prozessliste und im Protokoll | mitigate | Gate: kein Passwort in einer Kommandozeile im Verzeichnis dieses Laufs; erlaubt ist `--password-env` nach dem Muster von `search_load.py` |
| T-10-04 | Tampering | Das Baumhash-Rezept wird "verbessert" und liefert danach andere Hashes als die Vorlaeufer | mitigate | Docstring verbietet die Aenderung mit Begruendung; zwei Zusicherungen halten die Normalisierung und die Pfadabhaengigkeit fest; die zwei Erwartungswerte des heutigen Baums stehen im Akzeptanzkriterium |
| T-10-05 | Information Disclosure | Ein Messskript gibt Dateiinhalte in eine committete Rohdatei | mitigate | `40b-baumhash.py` gibt ausschliesslich Pfad und Hash aus, und der Docstring sagt das ausdruecklich |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Kein Paket installiert. `backend/uv.lock` bleibt unveraendert; `40b-baumhash.py` importiert nur `hashlib`, `pathlib` und `sys` |
</threat_model>

<verification>
- `git check-attr text eol` liefert fuer die fuenf Python-Messskripte `text: set` und `eol: lf`
- Keine Datei unter `docs/measurements/**/skripte/` enthaelt noch ein `\r\n`
- `40b-baumhash.py` reproduziert die beiden Baumhashes des heutigen Arbeitsbaums (54 / 6c47cd21..., 58 / 26b55908...)
- `sh -n docs/measurements/2026-09-vergleichsmessung-m7g/skripte/40b-baumhash.sh` ohne Befund
- `cd backend && uv run python -m pytest tests/test_measurement_scripts.py -q` gruen
- `cd backend && uv run python -m pytest -q` (ganze Suite) gruen, keine Regression gegen 1792 passed / 15 skipped
- `ruff check .`, `ruff format --check .`, `pyright`, `vulture src tests` in `backend/` gruen
- `ruff check` und `ruff format --check` ueber `docs/measurements/2026-09-vergleichsmessung-m7g/skripte` gruen
- `git diff --name-only` nennt ausser den in files_modified genannten Dateien keine
</verification>

<success_criteria>
Die zwei Werkzeugluecken, an denen ein Lauf auf der Box haette scheitern oder
einen leeren Beweis hinterlassen koennen, sind vor der Anfahrt geschlossen: die
Zeilenende-Regel steht mit Begruendung, fuenf Altfaelle sind renormalisiert, der
Baumhash-Beweis ist ein eigener Schritt mit einer Rohdatei, die nicht leer sein
kann, und ein Gate haelt die Hausordnung fuer die Messskripte dieses Laufs.
</success_criteria>

<output>
Create `.planning/phases/10-vergleichsmessung-auf-der-aws-box/10-01-SUMMARY.md` when done
</output>
