---
phase: 07-gemeinsame-embedding-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/performance.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EFF-01, EFF-02]
must_haves:
  truths:
    - "Ein Leser des Messberichts kann ohne Quellcode nachvollziehen, welche fuenf Aufrufstellen im ausgelieferten Container eine Embedding-Instanz brauchen und dass alle fuenf dieselbe ziehen"
    - "Der Beleg fuer 'genau einmal geladen' haengt an einem Zaehler und einem CI-Tor und nicht an einer Speichermessung, und das steht so im Bericht"
    - "Der Bericht sagt ausdruecklich, dass 'eine Engine' eine onnxruntime-Sitzung und einen Satz Gewichte meint und NICHT ein Tokenizer-Objekt, und nennt den Grund fuer die zwei Tokenizer-Instanzen"
    - "Die Kaltstartzahl 1.332,1 ms steht im Bericht neben der Grenze, an der sie wirklich haengt (1,5 s je Containeraufruf), nicht nur neben dem 2,5-s-Gruppenbudget"
    - "Die Modell-Entladung nach Leerlauf ist mit drei Zahlen abgelehnt und damit gegen ihre Wiederkehr in einer spaeteren Phase gesichert"
    - "EFF-01 ist in REQUIREMENTS.md abgehakt und der Haken zeigt auf den Beleg"
  artifacts:
    - path: "docs/performance.md"
      provides: "der Beleg-Abschnitt fuer EFF-01 und EFF-02, mit Datei:Zeile, Zaehler, Tor und den beiden PHP-Decken"
      contains: "shared_model"
    - path: ".planning/REQUIREMENTS.md"
      provides: "der Haken an EFF-01 mit Verweis auf den Beleg"
      contains: "EFF-01"
  key_links:
    - from: "docs/performance.md"
      to: "backend/src/findling/tools/one_load.py"
      via: "der Bericht nennt das CI-Tor, das die Aussage auf jedem Push haelt"
      pattern: "one_load"
    - from: "docs/performance.md"
      to: "php/lib/Service/ExAppService.php"
      via: "der Bericht nennt die Aufrufdecke, gegen die der Kaltstartaufschlag wirklich laeuft"
      pattern: "REQUEST_TIMEOUT_SECONDS"
---

<objective>
Phase 7 beginnt nicht mit einem Umbau, sondern mit einem Beleg. Die gemeinsame
Embedding-Engine steht seit Plan 06.1-02 im Code, ist mit sieben Unit-Tests
abgesichert und haengt seit 06.1-04 an einem CI-Tor. Die Kaltstartzahl aus EFF-02
liegt seit dem 07.09.2026 als Rohdatei vor. Beides ist heute nirgends so
aufgeschrieben, dass ein Leser es ohne Quellcode nachvollziehen kann.

Purpose: Erfolgskriterium 1 der Phase ist heute erfuellt. Es faellt aber durch
jede spaetere Pruefung, solange der Beleg in vier Dateien und einer Rohdatei
verteilt liegt. Dieser Plan schreibt ihn zusammen, mit Datei und Zeile, und er
zieht dabei drei Praezisierungen ein, ohne die der Beleg mehr verspricht als er
haelt: "eine Engine" ist eine Sitzung und ein Satz Gewichte und kein
Tokenizer-Objekt; "erste Suche nach Leerlauf" hat heute genau eine nichttriviale
Lesart, naemlich die erste Suche nach einem Containerstart; und die Grenze, an
der eine kalte Suche zuerst reisst, ist nicht das 2,5-s-Gruppenbudget, sondern
die Decke von 1,5 s je einzelnem Containeraufruf.

Der Plan schreibt ausserdem die Ablehnung der Modell-Entladung nach Leerlauf
fest. Sie steht in REQUIREMENTS.md unter "Future Requirements" und wurde in
06.1-02 schon einmal als Bauform B verworfen; jetzt gibt es drei Zahlen dafuer,
und die gehoeren in den Bericht, damit die Idee nicht in Phase 10 oder 11 erneut
auftaucht.

Output: ein Beleg-Abschnitt in docs/performance.md und ein Haken an EFF-01. Kein
Produktionscode, keine Testdatei, keine Workflow-Aenderung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-gemeinsame-embedding-engine/07-RESEARCH.md

<interfaces>
Der Halter und sein Zaehler:
  backend/src/findling/embed/engine.py:58        shared_model(), Modulglobale _ENGINE
      (Z. 51), RLock (Z. 55), die einzige Konstruktion von EmbeddingModel in der
      Auslieferung (Z. 76-80). Der Aufruf laedt nichts.
  backend/src/findling/embed/model.py:143        load_count()
  backend/src/findling/embed/model.py:298        EmbeddingModel.__init__, setzt nur Felder
  backend/src/findling/embed/model.py:405        _load(), liest die Artefakte
  backend/src/findling/embed/model.py:453        _LOAD_COUNT += 1, genau einmal je
      wirklich gelesenem Artefaktpaar

Die fuenf Aufrufstellen im ausgelieferten Container:
  backend/src/findling/api/search.py:232         resources.query_model()
  backend/src/findling/api/snippets.py:153       resources.query_model()
  backend/src/findling/api/diagnose.py:187       resources.query_model()
  backend/src/findling/api/resources.py:270      return shared_model()
  backend/src/findling/worker/poller.py:1491     self._model = shared_model()

Die Tests und das Tor:
  backend/tests/test_embed_engine.py:194,206,227,269   die vier EFF-01-Faelle
  backend/tests/test_one_load.py:173,198,212           die drei Rotbeweise
  backend/src/findling/tools/one_load.py:265-330       vier Zaehler, jeder andere
      Wert ist ein benannter Befund mit Exit 1
  .github/workflows/resilience.yml:1417-1431           der Schritt "One engine and
      one constituent list per process", Job measurements, ubuntu-24.04, amd64

Die beiden PHP-Decken:
  php/lib/Search/Provider.php:57                 BUDGET_NANOSECONDS = 2_500_000_000,
      Wanduhr der ganzen Ergebnisgruppe
  php/lib/Service/ExAppService.php:89            REQUEST_TIMEOUT_SECONDS = 1.5,
      Decke EINES Containeraufrufs
  php/lib/Search/Provider.php:248-258, :412, :504  jeder Aufruf schrumpft auf
      secondsLeft($deadline); searchCandidates steht in einer Schleife

Die Rohdaten, aus denen die Zahlen kommen:
  docs/measurements/2026-09-nachmessung-m7g/rohdaten/64-stufe-01.json
      3 Anfragen, die erste kalt, 0 Fehler, p50 465,3 ms, p95/max 1.332,1 ms,
      Budget 2.500 ms, p95_within_budget: true
  docs/measurements/2026-09-nachmessung-m7g/rohdaten/67-stufe-1.json
      warme Entsprechung derselben Box, 10 Runden, p95 481,6 ms
  docs/measurements/2026-09-nachmessung-m7g/rohdaten/64-spitze.txt
      anon springt in derselben Sekunde von 694,3 auf 1.116,6 MB
  docs/measurements/2026-09-nachmessung-m7g/rohdaten/63-grundlast.txt
      Schritt 13 (modell-objekt-gebaut-lazy) kostet 0,0 MB
      Die OCR-Phase haelt die Gesamtspitze mit 1.812,7 MB

Der Zielort im Bericht:
  docs/performance.md:2996  "Die Nachmessung: dieselbe Box, nach dem Fix an der
      zweiten Modellinstanz". Der neue Abschnitt kommt NACH diesem und VOR
      "Was der Test gekostet hat" (Z. 3132).

Die gesetzte Entscheidung, die nicht angetastet wird:
  .planning/STATE.md:159  Zwei Tokenizer-Instanzen aus derselben Datei, weil
      enable_truncation eine Eigenschaft des Objekts ist. Eine geteilte Instanz
      haette den 1.024-Token-Deckel aus D-01 still auf 512 halbiert. Upstream
      warnt vor derselben Sache.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der Beleg fuer EFF-01, mit Datei und Zeile</name>
  <files>docs/performance.md</files>
  <read_first>
    - docs/performance.md Z. 2996-3132 vollstaendig (die Nachmessung, ihre Ueberschriftenform, ihre Tabellenform, wie sie Rohdateien zitiert)
    - .planning/phases/07-gemeinsame-embedding-engine/07-RESEARCH.md Teil 1 vollstaendig
    - backend/src/findling/embed/engine.py vollstaendig
    - backend/src/findling/embed/model.py Z. 130-200 und Z. 290-460
    - backend/src/findling/tools/one_load.py Modulkopf (Z. 22-38) und Z. 209-330
    - .github/workflows/resilience.yml Z. 1385-1431
    - .planning/STATE.md Z. 155-165
  </read_first>
  <action>
    Ein neuer Abschnitt in docs/performance.md, direkt nach der Nachmessung und
    vor "Was der Test gekostet hat". Ueberschrift in der Form des Berichts,
    Inhalt deutsche Prosa mit echten Umlauten, alle Bezeichner und Pfade in
    ASCII.

    Der Abschnitt traegt vier Teile.

    Erstens die Tabelle der Aufrufstellen: fuenf Zeilen fuer die fuenf Pfade des
    ausgelieferten Containers, jede mit Datei und Zeile aus dem Interfaces-Block
    oben und mit der Angabe, woher die Engine kommt. Dazu der Satz, dass es im
    gesamten backend/src/findling/ keine zweite Konstruktion von EmbeddingModel
    gibt ausser der einen im Halter, und die Begruendung, warum die
    Konstruktionen in backend/tests/ und in scripts/dev/vector_distances.py
    nicht zaehlen: scripts/ liegt nicht im Laufzeit-Abbild. Der
    Extraktions-Kindprozess laedt kein Modell, weil in backend/src/findling/
    extract/ weder embed noch EmbeddingModel vorkommt.

    Zweitens der Beweisweg: warum der Beleg ein Zaehler ist und keine
    Speichermessung. load_count() zaehlt genau einmal je wirklich gelesenem
    Artefaktpaar; ein Zaehler unterscheidet einen Cache-Treffer von einem
    billigen zweiten Laden, eine RSS-Messung auf einem geteilten Runner kann das
    nicht. Dazu die vier Unit-Faelle mit Zeilennummer und die drei Rotbeweise
    aus test_one_load.py: die Faehigkeit rot zu werden ist per Monkeypatch
    belegt und nicht behauptet. Dazu das CI-Tor mit Workflow, Job, Schritt und
    Architektur, und der Grund fuer die Reihenfolge im Tor: die Suchseite muss
    die Ladezahl allein auf eins bringen, sonst meldet der Lauf eine Null und
    faellt, statt still gruen zu sein.

    Drittens die Praezisierung, ohne die der Beleg zu viel verspricht: "eine
    Engine" heisst eine EmbeddingModel-Instanz, also ein Satz Gewichte und eine
    onnxruntime-Sitzung. Es heisst NICHT "ein Tokenizer-Objekt im Prozess".
    Davon gibt es mindestens zwei, mit voller Absicht, weil enable_truncation
    eine Eigenschaft des Objekts ist und eine geteilte Instanz den
    1.024-Token-Deckel still auf 512 halbiert haette. Diese Entscheidung ist
    gesperrt (STATE.md:159) und vom Upstream-Projekt bestaetigt; wer sie
    aufheben will, halbiert die zweite Haelfte jedes langen Dokuments, ohne dass
    irgendwo etwas faellt.

    Viertens der Stand der Zahlen, damit der Abschnitt nicht als Ersatz fuer
    Phase 10 gelesen wird: die Ersparnis der gemeinsamen Engine ist in der
    Suchphase mit 712,6 MB gemessen und an der Gesamtspitze mit 25,1 MB, weil
    die Gesamtspitze inzwischen der OCR-Phase gehoert. Die amd64-Entsprechung
    liefert Plan 07-02, der Zeile-fuer-Zeile-Vergleich gegen die v1.0-Baseline
    gehoert Phase 10.

    Jede Zahl nennt ihre Rohdatei. Keine Zahl wird gerundet uebernommen, ohne
    dass danebensteht, dass sie gerundet ist. Keine Em-Dashes, keine En-Dashes.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; grep -q "shared_model" docs/performance.md &amp;&amp; grep -q "load_count" docs/performance.md &amp;&amp; grep -q "one_load" docs/performance.md &amp;&amp; grep -q "api/resources.py:270" docs/performance.md &amp;&amp; grep -q "worker/poller.py:1491" docs/performance.md &amp;&amp; python -c "import re,sys;bad=[p for p in sys.argv[1:] if re.search(chr(8212)+chr(124)+chr(8211), open(p,encoding='utf-8').read())];sys.exit('dash found in '+', '.join(bad)) if bad else print('no dash')" docs/performance.md &amp;&amp; ! grep -rn "EmbeddingModel(" backend/src/findling --include=*.py | grep -v "embed/engine.py" | grep -v "embed/model.py" &amp;&amp; cd backend &amp;&amp; uv run python -m pytest tests/test_embed_engine.py tests/test_one_load.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der Abschnitt nennt alle fuenf Aufrufstellen mit Datei und Zeile, und jede Angabe stimmt gegen den Baum.
    - Der Abschnitt nennt load_count(), die vier Unit-Faelle, die drei Rotbeweise und das CI-Tor mit Workflow, Job und Architektur.
    - Die Praezisierung "eine Engine ist keine Aussage ueber Tokenizer-Objekte" steht im Abschnitt, mit dem Grund und dem Verweis auf die gesperrte Entscheidung.
    - Der Gegenbeweis-Grep (keine zweite Konstruktion von EmbeddingModel ausserhalb von embed/) ist leer.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in docs/performance.md.
    - Keine Datei ausser docs/performance.md ist in diesem Task geaendert.
  </acceptance_criteria>
  <done>Ein Leser kann Erfolgskriterium 1 der Phase pruefen, ohne den Quellcode zu oeffnen, und weiss danach genau, was die Zusage nicht umfasst.</done>
</task>

<task type="auto">
  <name>Task 2: Der Beleg fuer EFF-02 und die Ablehnung der Modell-Entladung</name>
  <files>docs/performance.md</files>
  <read_first>
    - docs/measurements/2026-09-nachmessung-m7g/rohdaten/64-stufe-01.json vollstaendig
    - docs/measurements/2026-09-nachmessung-m7g/rohdaten/67-stufe-1.json und die weiteren 67-stufe-*.json (die warmen p95 der Stufen 1, 4 und 8)
    - docs/measurements/2026-09-nachmessung-m7g/rohdaten/64-spitze.txt (der anon-Sprung derselben Sekunde)
    - docs/measurements/2026-09-nachmessung-m7g/rohdaten/63-grundlast.txt (Schritt 13 kostet 0,0 MB)
    - php/lib/Search/Provider.php Z. 40-70 und Z. 240-270 und Z. 400-420 und Z. 495-515
    - php/lib/Service/ExAppService.php Z. 80-100
    - backend/src/findling/embed/engine.py Z. 70-82 und backend/src/findling/embed/model.py Z. 360-400
    - .planning/phases/07-gemeinsame-embedding-engine/07-RESEARCH.md Teil 2 und Teil 3.4
    - .planning/milestones/v1.0-phases/06.1-launch-haertung-vor-der-store-abgabe/06.1-02-SUMMARY.md, Abschnitt Decisions Made
  </read_first>
  <action>
    Derselbe Abschnitt in docs/performance.md bekommt zwei weitere Teile.

    Teil fuenf, die Kaltstartzahl. Die erste Handlung des Laufs 64-spitze.sh war
    genau eine semantische Suche gegen einen Container, der in diesem Start noch
    nie eine gesehen hatte, ueber die OCS-Route und damit ueber Apache, HaRP und
    AppAPI, also ueber den Weg eines Nutzers. Die Tabelle traegt Anfragen,
    Fehler, p50, p95 gleich max, Budget und die warme Entsprechung, jede Zeile
    mit ihrer Rohdatei. Der Kaltstartaufschlag von rund 850 ms wird als Rechnung
    ausgewiesen und nicht als Messung, und die Rechnung steht daneben: 1.332,1
    minus 481,6, drei kalte Anfragen gegen zehn warme, die Richtung stimmt, die
    zweite Nachkommastelle nicht. Der Aufschlag passt zur Speicherbeobachtung
    derselben Sekunde: anon springt um 422,3 MB, und das sind die Gewichte.

    Dazu die Lesart, ohne die das Kriterium einen Zustand misst, den das Produkt
    nicht kennt: es gibt im Code keinen Pfad, der eine geladene Engine wieder
    freigibt. _ENGINE wird nur ersetzt, wenn sich embed_model_dir aendert, und
    _engine wird nach einem erfolgreichen Laden nie wieder auf None gesetzt.
    "Erste Suche nach Leerlauf" hat deshalb heute genau eine nichttriviale
    Lesart, naemlich die erste Suche nach einem Containerstart, und genau die ist
    gemessen. Die zweite, kleinere Lesart bleibt benannt: nach langem Leerlauf
    koennen Index und vectors.db aus dem Seitencache gefallen sein, und dafuer
    gibt es das Kaltscan-Muster aus measure.yml.

    Teil sechs, die Grenze, an der es wirklich haengt. Beide Zahlen werden
    genannt und auseinandergehalten: BUDGET_NANOSECONDS in Provider.php:57 ist
    die Wanduhr der ganzen Ergebnisgruppe, REQUEST_TIMEOUT_SECONDS in
    ExAppService.php:89 ist die Decke EINES Containeraufrufs, und jeder Aufruf
    schrumpft zusaetzlich auf secondsLeft. Eine Suche macht zwei Aufrufe, und
    der erste steht in einer Schleife. Der Ladevorgang laeuft unter dem Lock,
    also warten gleichzeitige Suchen hinter genau einem Laden. Die
    Nebenlaeufigkeitstabelle aus der Recherche kommt mit, ausdruecklich als
    Rechnung markiert, mit dem Zusatz, dass nur die erste Zeile gemessen ist und
    dass der Fall "kalt und acht gleichzeitige Suchen" heute nur den ersten
    Augenblick nach einem Containerstart trifft.

    Danach die Ablehnung der Modell-Entladung nach Leerlauf, mit den drei
    Gruenden und ihren Zahlen: sie senkt die Gesamtspitze nicht, weil die Spitze
    bei 1.812,7 MB in der OCR-Phase liegt und die Einbettung dort mitlaeuft; sie
    senkt die Grundlast kaum, weil Schritt 13 der Aufschluesselung 0,0 MB kostet
    und die Gewichte erst bei der ersten Einbettung kommen; und sie kostet
    EFF-02 direkt, weil jedes Entladen die naechste Suche wieder zur kalten
    Suche macht, unter einem Lock, gegen eine Aufrufdecke von 1,5 s. Der Absatz
    endet mit dem Satz, dass die Entladung in "Future Requirements" bleibt und
    in diesem Milestone nicht mehr geplant wird, und mit dem Verweis auf
    06.1-02, wo sie als Bauform B schon einmal verworfen wurde.

    Ein Schlusssatz haelt fest, was in dieser Phase NICHT belegt wird: die
    Zahlen dieses Abschnitts sind auf arm64 gemessen, die amd64-Entsprechung
    liefert Plan 07-02, ein p95 ueber den vollen Korpus gehoert Phase 10.

    Keine Em-Dashes, keine En-Dashes, Umlaute nur in der Prosa.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; grep -q "1.332,1" docs/performance.md &amp;&amp; grep -q "481,6" docs/performance.md &amp;&amp; grep -q "REQUEST_TIMEOUT_SECONDS" docs/performance.md &amp;&amp; grep -q "BUDGET_NANOSECONDS" docs/performance.md &amp;&amp; grep -q "64-stufe-01.json" docs/performance.md &amp;&amp; grep -q "1.812,7" docs/performance.md &amp;&amp; python -c "import re,sys;bad=[p for p in sys.argv[1:] if re.search(chr(8212)+chr(124)+chr(8211), open(p,encoding='utf-8').read())];sys.exit('dash found in '+', '.join(bad)) if bad else print('no dash')" docs/performance.md &amp;&amp; grep -q "REQUEST_TIMEOUT_SECONDS = 1.5" php/lib/Service/ExAppService.php &amp;&amp; grep -q "BUDGET_NANOSECONDS = 2_500_000_000" php/lib/Search/Provider.php</automated>
  </verify>
  <acceptance_criteria>
    - Die Kaltstartzahl steht mit Rohdatei, Fehlerzahl, p50, p95 und warmer Entsprechung im Bericht.
    - Der Aufschlag von rund 850 ms ist ausdruecklich als Rechnung markiert, und die Rechnung steht daneben.
    - Beide PHP-Decken sind genannt, mit Datei und Zeile, und der Unterschied zwischen Gruppenbudget und Aufrufdecke ist erklaert.
    - Die Nebenlaeufigkeitstabelle ist als Rechnung markiert und nennt, welche Zeile gemessen ist.
    - Die Ablehnung der Modell-Entladung steht mit drei Gruenden und drei Zahlen im Bericht.
    - Die letzten beiden Greps bestaetigen, dass die beiden zitierten PHP-Konstanten im Baum unveraendert so heissen und so stehen.
    - Kein Em-Dash (U+2014) und kein En-Dash (U+2013).
  </acceptance_criteria>
  <done>Wer EFF-02 pruefen will, findet die Zahl, ihre Herkunft und die Grenze, gegen die sie wirklich laeuft, an einer Stelle, und die Entladeidee hat eine Begruendung, die sie nicht ueberlebt.</done>
</task>

<task type="auto">
  <name>Task 3: Der Haken an EFF-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md vollstaendig (Form der Haken, Form der Traceability-Tabelle)
  </read_first>
  <action>
    EFF-01 wird abgehakt. Die Zeile behaelt ihren Wortlaut und bekommt einen
    kurzen Zusatz in Klammern, der auf den Beleg zeigt: belegt in
    docs/performance.md, Abschnitt der Phase 7, mit Zaehler und CI-Tor.

    In der Traceability-Tabelle wechselt EFF-01 von Pending auf Done.

    EFF-02 bleibt offen und behaelt Pending. Die Zahl aus der Nachmessung ist
    auf arm64 gemessen, und Erfolgskriterium 4 der Phase verlangt eine
    amd64-Zahl; die liefert Plan 07-02, und dort faellt auch der Haken.

    STATE.md und ROADMAP.md werden in diesem Plan NICHT angefasst. Sie gehoeren
    dem Phasenabschluss.
  </action>
  <verify>
    <automated>cd /c/Users/Student/nextcloud-search &amp;&amp; grep -q "^- \[x\] \*\*EFF-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[ \] \*\*EFF-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| EFF-01 \| Phase 7 \| Done \|" .planning/REQUIREMENTS.md &amp;&amp; ! git diff --name-only | grep -qE "\.planning/(STATE|ROADMAP)\.md"</automated>
  </verify>
  <acceptance_criteria>
    - EFF-01 traegt den Haken und einen Verweis auf den Beleg.
    - EFF-02 ist unveraendert offen.
    - Die Traceability-Tabelle nennt EFF-01 als Done und EFF-02 als Pending.
    - Weder .planning/STATE.md noch .planning/ROADMAP.md sind in diesem Plan geaendert.
  </acceptance_criteria>
  <done>Der erfuellte Teil der Phase ist als erfuellt markiert, und der offene Teil ist als offen erkennbar.</done>
</task>

</tasks>

<executor_rules>
- Commit early: nach jedem Task ein eigener Commit, mit der Planmarke im Betreff (`docs(07-01): ...`).
- Kein Schreiben in `.planning/STATE.md` und kein Schreiben in `.planning/ROADMAP.md`. Beide gehoeren dem Phasenabschluss.
- Kein `git push`.
- Die SUMMARY wird VOR der Rueckkehr geschrieben und committet.
- Vor jedem Commit lokal gruen: `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q`, dazu `uv run pyright` in der Stufe basic und `uv run vulture src tests --min-confidence 80`. Dieser Plan aendert keinen Python-Code, die Gates laufen trotzdem, damit ein Befund aus einem fremden Stand nicht als Befund dieses Plans erscheint.
- Deutsche Prosa mit echten Umlauten, keine Em-Dashes und keine En-Dashes, Bezeichner und Pfade in ASCII.
- Kein Hinweis auf das Werkzeug, das den Text erzeugt hat, in irgendeinem Artefakt.
</executor_rules>

<verification>
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pytest -q` gruen, `uv run pyright` gruen, `uv run vulture src tests --min-confidence 80` gruen
- Der Gegenbeweis-Grep nach einer zweiten Konstruktion von `EmbeddingModel` ausserhalb von `embed/` ist leer
- Jede im Bericht genannte Datei-und-Zeile-Angabe stimmt gegen den Baum; Abweichungen werden korrigiert und in der SUMMARY benannt
- Kein Em-Dash (U+2014) und kein En-Dash (U+2013) in den geaenderten Dateien
</verification>

<success_criteria>
Erfolgskriterium 1 der Phase ist belegt statt behauptet, die Kaltstartzahl steht
neben der Grenze, an der sie wirklich haengt, und die Modell-Entladung hat eine
Ablehnung mit Zahlen, die eine spaetere Phase nicht versehentlich aufhebt.
</success_criteria>

<output>
Create `.planning/phases/07-gemeinsame-embedding-engine/07-01-SUMMARY.md` when done
</output>
