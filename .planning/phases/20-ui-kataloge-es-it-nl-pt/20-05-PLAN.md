---
phase: 20-ui-kataloge-es-it-nl-pt
plan: 05
type: execute
wave: 5
depends_on: ["20-04"]
files_modified:
  - php/l10n/it.json
  - php/l10n/it.js
  - backend/tests/test_admin_ui_contract.py
  - docs/l10n-italian.md
autonomous: true
requirements: [KAT-01, KAT-02]

must_haves:
  truths:
    - "Ein Nutzer, dessen Nextcloud auf it steht, sieht Findling vollstaendig auf Italienisch, inklusive Fehler- und Diagnosetexten"
    - "it.json und it.js fuehren dieselbe Schluesselmenge wie de.json"
    - "Die italienische Pluralregel steht woertlich in beiden Dateien und traegt nplurals=3"
    - "Die fuenf Pluralwerte tragen drei Formen, Form 1 und Form 2 wortgleich"
    - "Der italienische Katalog traegt einen datierten Review-Vorbehalt nach FR-Muster"
  artifacts:
    - path: "php/l10n/it.json"
      provides: "Italienischer Katalog fuer die PHP-Seite"
      contains: "pluralForm"
    - path: "php/l10n/it.js"
      provides: "Italienischer Katalog fuer den Browser"
      contains: "OC.L10N.register"
    - path: "docs/l10n-italian.md"
      provides: "Wortwahl, Typografie, vollstaendige Tabelle, Pruefergebnisse, datierter Vorbehalt"
      min_lines: 260
  key_links:
    - from: "backend/tests/test_admin_ui_contract.py"
      to: "php/l10n/it.json"
      via: "L10N_IT_JSON im Tupel L10N_CATALOGUES"
      pattern: "L10N_IT_JSON"
    - from: "php/l10n/it.js"
      to: "php/l10n/it.json"
      via: "mechanisch gegossen, gleiche Schluesselmenge, gleiche Regel"
      pattern: "nplurals=3"
---

<objective>
Der italienische Katalog: `php/l10n/it.json` und `php/l10n/it.js` im Gleichstand mit `de.json`, im
Gate verankert und in `docs/l10n-italian.md` dokumentiert.

Purpose: Italienisch trägt dieselbe Pluralregel wie Spanisch (`nplurals=3` mit dem
`n % 1000000`-Zweig), also ist es die Sprache, an der sich zeigt, ob die Parametrisierung aus den
Plänen 20-02 und 20-03 wirklich eine Datentabelle geworden ist oder nur so aussieht: hier darf
außer einem Konstantenpaar und einer Ausnahmeliste nichts am Gate geändert werden. Muss doch
Logik angefasst werden, ist die Parametrisierung unvollständig und das gehört gemeldet, nicht
umgangen.

Output: Zwei Katalogdateien, ein Konstantenpaar im Gate, eine Sprachdokumentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ui-kataloge-es-it-nl-pt/20-RESEARCH.md
@.planning/phases/20-ui-kataloge-es-it-nl-pt/20-PATTERNS.md
@.planning/phases/20-ui-kataloge-es-it-nl-pt/20-04-SUMMARY.md
@docs/l10n-catalogues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: it.json in der Reihenfolge des deutschen Katalogs</name>
  <files>php/l10n/it.json</files>
  <read_first>
    - php/l10n/de.json (vollstaendig: Schluessel, Reihenfolge, deutsche Wortlaute als Kontext)
    - php/l10n/es.json (Ergebnis von Plan 20-04: die Form, in der eine neue Sprache in diesem Repo
      aussieht, inklusive der drei Pluralformen)
    - docs/l10n-catalogues.md, Abschnitte 3 und 4
    - docs/l10n-spanish.md, Abschnitt "Wortwahl" (die Form der Begruendung)
  </read_first>
  <action>
    `php/l10n/it.json` neu anlegen: `translations` und `pluralForm` auf oberster Ebene, vier
    Leerzeichen Einrueckung, UTF-8 ohne BOM, LF, abschliessender Zeilenumbruch.

    Schluessel und Reihenfolge exakt wie `de.json`, erster Schluessel `Findling` mit sich selbst
    als Wert. Werte sind italienische Wortlaute ohne englische Reste.

    Fuenf Begriffsentscheide vor dem Schreiben treffen und in Task 3 begruenden: "the backend"
    (Vorschlag `il servizio`, nach dem Muster `le service` der franzoesischen Datei), "run" als
    Indexlauf, "worker" (Vorschlag `processo di elaborazione`), "index" als Substantiv und als
    Verb (`l'indice`, `indicizzare`), "coverage" (Vorschlag `copertura`).

    Harte Regeln, jede mit einem Gate dahinter:
    - Platzhalter woertlich wie im Schluessel; `%1$s` bleibt `%1$s`.
    - Ein literales Prozentzeichen wird `%%` geschrieben, sonst wirft `vsprintf` einen
      `ValueError` und die Seite bleibt weiss.
    - Kein Pipe-Zeichen in irgendeinem Wert.
    - Keine Halbgeviertstriche, keine Emojis, kein typographischer Apostroph U+2019 und kein
      geschuetztes Leerzeichen. Der Apostroph des Italienischen ist haeufig (`l'indice`,
      `un'immagine`) und wird ausnahmslos als ASCII-Apostroph geschrieben; das ist die Stelle, an
      der eine maschinelle Uebersetzung am zuverlaessigsten U+2019 einschleppt.
    - Echte Akzente sind Pflicht und werden nie durch nachgestellte Apostrophe ersetzt: `perché`,
      `così`, `è`, niemals `e'`.

    Die fuenf zusammengesetzten Pluralschluessel aus Plan 20-01 tragen je **drei** Formen, Form 1
    und Form 2 wortgleich als normaler Plural. `pluralForm` ist die italienische Regel,
    zeichengleich aus `docs/l10n-catalogues.md` uebernommen, mit `nplurals=3`.

    Ausdrueckliches Verbot: nichts unter `php/templates/`, `php/js/`, `php/lib/` und nichts unter
    `backend/src/findling`.
  </action>
  <acceptance_criteria>
    - Die Schluesselliste von `it.json` ist elementweise gleich der von `de.json`, inklusive Reihenfolge
    - Genau fuenf Werte sind Listen mit je drei Eintraegen, Eintrag 1 gleich Eintrag 2
    - `pluralForm` ist zeichengleich mit der italienischen Zeile in `docs/l10n-catalogues.md` und enthaelt `nplurals=3`
    - Kein Zeichen aus der Menge U+2014, U+2013, U+2019, U+00A0, U+202F kommt in der Datei vor
    - Kein Wert enthaelt ein `|`; kein Wert und kein Schluessel traegt ein `%` ausserhalb einer erkannten Direktive
    - Kein Wert enthaelt die Zeichenfolge `e'` als Ersatz fuer `è`
    - `file php/l10n/it.json` nennt kein CRLF, `tail -c 1 php/l10n/it.json | xxd` zeigt `0a`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import json,io,re;a=json.load(io.open('../php/l10n/de.json',encoding='utf-8'));b=json.load(io.open('../php/l10n/it.json',encoding='utf-8'));tr=b['translations'];assert list(a['translations'])==list(tr);p=[k for k,v in tr.items() if isinstance(v,list)];assert len(p)==5 and all(len(tr[k])==3 and tr[k][1]==tr[k][2] for k in p);assert 'nplurals=3' in b['pluralForm'];d=re.compile(r'%%|%\d+\$[sd]|%[sdn]');forms=[(k,f) for k,v in tr.items() for f in (v if isinstance(v,list) else [v])];assert all('|' not in f for k,f in forms);assert all(f.count('%')==sum(m.count('%') for m in d.findall(f)) for k,f in forms);t=io.open('../php/l10n/it.json',encoding='utf-8').read();assert not [c for c in t if ord(c) in (0x2014,0x2013,0x2019,0x00a0,0x202f)];print('ok',len(tr))"</automated>
  </verify>
  <done>Der italienische Katalog fuehrt dieselben Schluessel wie der deutsche, drei Formen je Plural und die woertliche italienische Regel.</done>
</task>

<task type="auto">
  <name>Task 2: it.js giessen und das Gate um Italienisch erweitern</name>
  <files>php/l10n/it.js, backend/tests/test_admin_ui_contract.py</files>
  <read_first>
    - php/l10n/es.js (Ergebnis von Plan 20-04: Rahmen, Einrueckung, Abschluss)
    - php/l10n/it.json (Ergebnis von Task 1)
    - backend/tests/test_admin_ui_contract.py: die Konstantenpaare, `L10N_CATALOGUES`,
      `PLURAL_FORM_OF`, `FORM_COUNT_OF`, `VALUES_THAT_MAY_EQUAL_THEIR_KEY`
    - .planning/phases/20-ui-kataloge-es-it-nl-pt/20-RESEARCH.md, Abschnitt "Code Examples",
      Unterabschnitt "Die .js aus der .json giessen"
  </read_first>
  <action>
    `php/l10n/it.js` aus `php/l10n/it.json` giessen, mit demselben Einmal-Lauf wie in Plan 20-04:
    `json.dumps(..., ensure_ascii=False, indent=4)`, Rumpf um vier Leerzeichen eingerueckt,
    Rahmen `OC.L10N.register(` mit `"findling"`, Regel als vierter Parameter, `newline="\n"`,
    UTF-8 ohne BOM, abschliessender Zeilenumbruch. Kein Generator wird abgelegt.

    Im Gate `L10N_IT_JSON` und `L10N_IT_JS` anlegen, mit Begruendungsabsatz davor: warum zwei
    Dateien, warum der Code `it` ohne Regionalvariante (der Kern liefert fuer Italienisch keine
    weitere Variante, anders als bei Spanisch und Portugiesisch, Verweis auf
    `docs/l10n-catalogues.md`), und woher die Wortlaute stammen. Beide Pfade in
    `L10N_CATALOGUES`.

    Eintrag fuer `it` in `VALUES_THAT_MAY_EQUAL_THEIR_KEY` anlegen, gefunden statt geraten: das
    Vollstaendigkeitsgate einmal mit leerem Mapping laufen lassen, die gemeldeten Schluessel
    einzeln beurteilen, jeden Eintrag mit Grund versehen. Keine Schwelle.

    Pruefen und in der SUMMARY festhalten: ausser dem Konstantenpaar, dem Tupel-Eintrag und der
    Ausnahmeliste wurde **keine** Zeile des Gates geaendert. Musste doch Logik angefasst werden,
    gehoert der Grund in die SUMMARY, weil dann die Parametrisierung aus Plan 20-03 eine Luecke
    hat, die die drei folgenden Sprachen erben.
  </action>
  <acceptance_criteria>
    - `php/l10n/it.js` beginnt mit `OC.L10N.register(` und endet mit der italienischen Regel als viertem Parameter
    - Der aus `it.js` herausgeschnittene Rumpf ist objektgleich mit `translations` aus `it.json`
    - `grep -c 'L10N_IT_JSON\|L10N_IT_JS' backend/tests/test_admin_ui_contract.py` liefert mindestens 4
    - `L10N_CATALOGUES` fuehrt zehn Eintraege
    - `VALUES_THAT_MAY_EQUAL_THEIR_KEY` fuehrt einen Eintrag fuer `it`, jeder Schluessel darin mit Grund
    - `git diff backend/tests/test_admin_ui_contract.py` zeigt nur Zufuegungen in den drei genannten Bereichen, keine geaenderte Scannerlogik
    - `cd backend && uv run pytest -q tests/test_admin_ui_contract.py` endet mit 0 Fehlern
    - `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` sind gruen
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import json,io;j=json.load(io.open('../php/l10n/it.json',encoding='utf-8'))['translations'];s=io.open('../php/l10n/it.js',encoding='utf-8').read();k=json.loads(s[s.index('{'):s.rindex('}')+1]);assert j==k;print('ok',len(k))" && uv run pytest -q tests/test_admin_ui_contract.py --no-header && uv run ruff check . && uv run pyright</automated>
  </verify>
  <done>Zehn Katalogdateien laufen durch dieselben Scanner, und die Parametrisierung hat dafuer keine Logikaenderung gebraucht.</done>
</task>

<task type="auto">
  <name>Task 3: docs/l10n-italian.md mit Tabelle, Pruefergebnissen und datiertem Vorbehalt</name>
  <files>docs/l10n-italian.md</files>
  <read_first>
    - docs/l10n-spanish.md (Ergebnis von Plan 20-04: die Abschnittsfolge, die diese Datei
      uebernimmt)
    - docs/l10n-french.md (das Original des Musters, besonders Ausnahmeliste und Nachtrag)
    - docs/l10n-catalogues.md (der gemeinsame Beweis, auf den verwiesen statt wiederholt wird)
    - php/l10n/it.json und php/l10n/de.json (die Quellen der Tabelle)
  </read_first>
  <action>
    Neue Datei `docs/l10n-italian.md` mit derselben neunteiligen Abschnittsfolge wie
    `docs/l10n-spanish.md`: Titel und Einleitung mit Verweis auf `docs/l10n-catalogues.md`;
    Schluesselmenge aus der Datei gezaehlt; Wortwahl als Tabelle Englisch, Italienisch, Warum;
    Typografie mit dem ausdruecklichen Absatz zum ASCII-Apostroph und zu den echten Akzenten;
    Pluralformen mit der woertlichen Regel und dem Verweis auf die Drei-Formen-Messung; die
    vollstaendige Tabelle mit Kopf `| Schluessel | DE | IT |`, mechanisch aus `de.json` und
    `it.json` erzeugt, Pluralformen durch " / " getrennt in einer Zelle; die Ausnahmeliste mit
    demselben Grund je Eintrag wie im Gate und dem Satz, dass es keine Toleranzschwelle ist; die
    Tabelle der maschinellen Pruefungen mit den tatsaechlich gefahrenen Zahlen; und die Abnahme
    mit dem datierten Vorbehalt.

    Der Vorbehalt traegt Erzeugungsdatum, die Plannummer 20-05, die Liste der gefahrenen Gates,
    den Satz **"von keinem Muttersprachler gelesen"**, den Ort des offenen Community-Review und
    die Feststellung, dass die Auslieferung darauf nicht wartet (gesperrter Entscheid E-17-5,
    Option a).

    Deutsche Prosa mit echten Umlauten, italienische Wortlaute mit echten Akzenten, keine
    Halbgeviertstriche, keine Emojis.
  </action>
  <acceptance_criteria>
    - `docs/l10n-italian.md` traegt alle neun Abschnitte in der Reihenfolge von `docs/l10n-spanish.md`
    - Jeder Schluessel aus `it.json` kommt im Dokument vor
    - Der Abschnitt "Abnahme" enthaelt ein Datum im Format TT.MM.JJJJ, die Zeichenkette `20-05` und woertlich `von keinem Muttersprachler gelesen`
    - Die Ausnahmeliste der Doku und der Eintrag `it` in `VALUES_THAT_MAY_EQUAL_THEIR_KEY` fuehren dieselben Schluessel
    - Kein Halbgeviertstrich im Dokument
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import json,io;b=json.load(io.open('../php/l10n/it.json',encoding='utf-8'))['translations'];t=io.open('../docs/l10n-italian.md',encoding='utf-8').read();m=[k for k in b if k not in t];assert m==[],m[:3];assert 'von keinem Muttersprachler gelesen' in t;assert '20-05' in t;assert not [c for c in t if ord(c) in (0x2014,0x2013)];print('ok')"</automated>
  </verify>
  <done>Die italienische Sprachdoku steht vollstaendig, deckt sich mit dem Gate und traegt einen datierten Vorbehalt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Katalogwert zu vsprintf | Jeder italienische Wert wird in `L10NString::__toString` formatiert |
| Katalogwert zu Browser | `OC.L10N.register` laedt die `.js`, die Werte landen ueber `textContent` in der Seite |
| Doku zu Gate | Ausnahmeliste und Pruefergebnisse stehen an zwei Orten und muessen dasselbe sagen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-19 | Denial of Service | Ein italienischer Wert mit nacktem Prozentzeichen | mitigate | `scan_percent_discipline` laeuft ueber `it.json` und `it.js`, sobald das Paar in `L10N_CATALOGUES` steht |
| T-20-20 | Tampering | Der maschinelle Wortlaut schleppt U+2019 als Apostroph ein (`l'indice`) und laeuft damit an `scan_prose` vorbei | mitigate | Eigenes Abnahmekriterium ueber die Zeichenmenge U+2014, U+2013, U+2019, U+00A0, U+202F in Task 1, plus die Pruefzeile in der Sprachdoku |
| T-20-21 | Tampering | Ein Platzhalter geht verloren oder wechselt die Nummerierung | mitigate | `scan_placeholder_parity` ueber beide neuen Dateien |
| T-20-22 | Tampering | `it.js` und `it.json` laufen auseinander | mitigate | `.js` wird gegossen, Gleichstand per Objektvergleich geprueft |
| T-20-23 | Repudiation | Ein Katalog geht ohne Vorbehalt in den Store und gilt spaeter als abgenommen | mitigate | Datierter Vorbehalt mit Plannummer und dem Satz "von keinem Muttersprachler gelesen" |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | Keine Installation in dieser Phase; die Research fuehrt den Abschnitt "Package Legitimacy Audit" ausdruecklich als nicht anwendbar |
</threat_model>

<verification>
- `cd backend && uv run pytest -q tests/test_admin_ui_contract.py` gruen
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src tests --min-confidence 80` gruen
- `it.json` und `it.js` fuehren dieselbe Schluesselmenge wie `de.json`
- Fuenf Pluralwerte mit drei Formen, Form 1 gleich Form 2, Regel mit `nplurals=3`
- `docs/l10n-italian.md` mit vollstaendiger Tabelle, Pruefergebnissen und datiertem Vorbehalt
- `git diff --name-only -- php/templates php/js php/lib backend/src` ist leer
</verification>

<success_criteria>
Italienisch ist vollstaendig ausgeliefert, im Gate verankert, dokumentiert und mit einem datierten
Vorbehalt versehen, und die Gate-Parametrisierung hat dafuer keine Logikaenderung gebraucht.
</success_criteria>

<output>
Create `.planning/phases/20-ui-kataloge-es-it-nl-pt/20-05-SUMMARY.md` when done
</output>
