---
phase: 19-frageseite-freischalten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/findling/query/rewrite.py
  - backend/tests/test_schema_generations.py
  - backend/tests/test_query_fields_plan.py
  - backend/tests/test_query_rewrite.py
  - backend/tests/test_measurement_scripts.py
autonomous: true
requirements: [LEX-05]

must_haves:
  truths:
    - "Die Feldliste und die Boost-Abbildung einer Anfrage sind EIN Wert, nicht zwei Konstanten"
    - "Ein Aufrufer, der den Plan vergisst, sucht genau die vier Felder von heute und bricht nichts"
    - "Der AST-Waechter ueber die drei Konstanten ist weg und sein Ersatz prueft die Maschine statt der Namen"
  artifacts:
    - path: "backend/src/findling/query/rewrite.py"
      provides: "FieldPlan, LEGACY_PLAN, build_query(plan=...)"
      contains: "class FieldPlan"
    - path: "backend/tests/test_query_fields_plan.py"
      provides: "Der Ersatz des AST-Waechters, der Plan gegen einen echten Schema-1-Index"
      min_lines: 60
  key_links:
    - from: "backend/src/findling/query/rewrite.py"
      to: "index.parse_query_lenient"
      via: "plan.fields und plan.boosts aus demselben Wert"
      pattern: "default_field_names=.*plan\\."
    - from: "backend/tests/test_query_fields_plan.py"
      to: "conftest.schema_1_index"
      via: "pytest-Fixture"
      pattern: "schema_1_index"
---

<objective>
Die drei Modulkonstanten `DEFAULT_FIELDS`, `TITLE_ONLY_FIELDS` und `FIELD_BOOSTS` werden zu EINEM
Wert `FieldPlan`, den `build_query` als keyword-only-Parameter mit sicherem Vorgabewert entgegennimmt.
Im selben Commit faellt der AST-Waechter aus `test_schema_generations.py`, weil er ab hier keinen
Gegenstand mehr hat, und ein staerkerer Ersatz tritt an seine Stelle.

Purpose: `parse_query_lenient` wirft die `ValueError` gemessen auch fuer `field_boosts` und nicht nur
fuer `default_field_names` (RESEARCH Messung M-1). Wer nur die Feldliste beweglich macht, verschiebt
den Totalausfall, statt ihn zu beheben. Beide Haelften gehoeren deshalb in einen Wert, bevor
irgendetwas sie berechnet.

Output: `FieldPlan` und `LEGACY_PLAN` in `query/rewrite.py`, `build_query(..., plan=LEGACY_PLAN)`,
`backend/tests/test_query_fields_plan.py` als Ersatz der Phasengrenze, bewegter
`PACKAGE_TREE_HASH_TODAY`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frageseite-freischalten/19-RESEARCH.md
@.planning/phases/19-frageseite-freischalten/19-PATTERNS.md

<interfaces>
Zielzustand der oeffentlichen Namen dieses Plans. Der Executor baut gegen diese Signaturen und
erkundet den Baum nicht danach.

query/rewrite.py, neu: eine frozen dataclass mit slots namens `FieldPlan`, Felder
`fields: tuple[str, ...]`, `boosts: Mapping[str, float]`, `title_only: tuple[str, ...]`.
Dazu `NAME_BOOST = 3.0`, `TITLE_BOOST = 2.0` und `BODY_BOOST` als geschlossene Abbildung
de 1.0 / en 0.8 / es 0.6 / it 0.6 / nl 0.6 / pt 0.6, sowie `LEGACY_PLAN` mit den Feldern
`FIELD_BODY_DE, FIELD_BODY_EN, FIELD_NAME, FIELD_TITLE` in dieser Reihenfolge, den vier
zugehoerigen Boosts und `title_only=(FIELD_NAME,)`.

query/rewrite.py, geaendert: `build_query` bekommt als letzten keyword-only-Parameter
`plan: FieldPlan = LEGACY_PLAN`; alle uebrigen Parameter und ihre Reihenfolge bleiben.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FieldPlan, LEGACY_PLAN und der plan-Parameter</name>
  <files>backend/src/findling/query/rewrite.py</files>
  <read_first>
    - backend/src/findling/query/rewrite.py (vollstaendig, besonders Modulkopf 1-25, Konstanten
      53-65, RewrittenQuery 337-366, build_query 470-565)
    - backend/src/findling/index/schema.py, Zeilen 80-120 (BODY_FIELD und die Feldnamen-Konstanten)
    - .planning/phases/19-frageseite-freischalten/19-PATTERNS.md, Abschnitt
      "backend/src/findling/query/rewrite.py"
  </read_first>
  <behavior>
    - LEGACY_PLAN.fields ist genau (FIELD_BODY_DE, FIELD_BODY_EN, FIELD_NAME, FIELD_TITLE), in
      dieser Reihenfolge.
    - LEGACY_PLAN.boosts traegt genau vier Schluessel mit 3.0 / 2.0 / 1.0 / 0.8.
    - set(LEGACY_PLAN.boosts) == set(LEGACY_PLAN.fields).
    - build_query(index, "kuendigung") ohne plan liefert dieselbe Trefferliste wie vor der Aenderung.
    - build_query(index, "vertrag", title_only=True) durchsucht weiter nur FIELD_NAME.
  </behavior>
  <action>
    Die drei Konstanten `DEFAULT_FIELDS`, `TITLE_ONLY_FIELDS` und `FIELD_BOOSTS` (Zeilen 53-65)
    entfallen als Modulnamen. Ihre Werte und ihre erklaerenden Kommentare wandern unveraendert in
    `LEGACY_PLAN`; sie werden verschoben und nicht umformuliert.

    Neu in `query/rewrite.py`, in der Form des interfaces-Blocks oben: `FieldPlan` als frozen
    dataclass mit slots, je Feld ein Absatz im Docstring, der sagt, was das Feld NICHT ist (Bauart
    von `ReadSide`, `api/resources.py:100-133`). Der Klassendocstring haelt RESEARCH Messung M-1
    fest: `field_boosts` wirft dieselbe `ValueError` wie `default_field_names`, deshalb sind beide
    Haelften EIN Wert und stehen nicht nebeneinander.

    Die Boost-Zahlen kommen als `NAME_BOOST`, `TITLE_BOOST` und `BODY_BOOST` dazu. `BODY_BOOST`
    traegt 0.6 fuer es, it, nl und pt mit Begruendungskommentar: 0.6 stammt aus
    `.planning/research/FEATURES.md` Frage 3, ist eine Empfehlung und keine Messung, die Rangprobe
    aus Plan 19-04 liefert die Zahl nach, und der Wert liegt ausdruecklich unterhalb `body_en` (0.8),
    was die woertliche Zusage von Erfolgskriterium 3 der Roadmap ist. `LEGACY_PLAN` baut seine
    Boost-Abbildung aus diesen Konstanten, damit jede Zahl nur eine Schreibweise hat.

    `build_query` bekommt `plan: FieldPlan = LEGACY_PLAN` als letzten keyword-only-Parameter. Der
    Vorgabewert ist der Bestandsplan und NIEMALS etwas aus `settings()`; der Docstring uebernimmt
    dafuer die vorhandene Begruendung der Zeilen 486-493 ("keyword arguments with a default so that
    the callers who filter nothing stay exactly as they are") und ergaenzt: wer den Plan vergisst,
    sucht die vier Felder von heute und bricht nichts (RESEARCH Pitfall 6).

    Der `parse_query_lenient`-Aufruf (Zeilen 540-546) liest ab hier
    `default_field_names=list(plan.title_only) if title_only else list(plan.fields)` und
    `field_boosts=dict(plan.boosts)`. `conjunction_by_default=True` und `allow_regexes=False`
    bleiben unangetastet; letzteres wird von `test_index_open.py:377` bewacht.

    Der Modulkopf (Zeilen 1-25) wird nachgezogen: seine zwei Absaetze zu `default_field_names` und
    `field_boosts` sagen heute, die Antwort haenge am Dateinamenfilter. Nach diesem Plan haengt sie
    zusaetzlich am Verzeichnis, und der Kopf sagt das, sonst widerlegt der Diff sich selbst.

    `RewrittenQuery` bekommt KEIN siebtes Feld: der Plan ist Eingabe und nicht Ergebnis.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest -q tests/test_query_rewrite.py tests/test_index_open.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^DEFAULT_FIELDS\|^TITLE_ONLY_FIELDS\|^FIELD_BOOSTS" backend/src/findling/query/rewrite.py`
      liefert keine Zeile.
    - `grep -c "class FieldPlan" backend/src/findling/query/rewrite.py` ist 1.
    - `grep -n "plan: FieldPlan = LEGACY_PLAN" backend/src/findling/query/rewrite.py` liefert genau
      eine Zeile, und sie steht in der Signatur von build_query.
    - `grep -n "allow_regexes=False" backend/src/findling/query/rewrite.py` liefert weiterhin genau
      eine Zeile.
    - Aus backend/: `uv run python -c "from findling.query.rewrite import LEGACY_PLAN as p; assert set(p.boosts)==set(p.fields); assert p.boosts['body_en']==0.8; assert p.fields[0]=='body_de'"`
      endet mit Status 0.
    - tests/test_query_rewrite.py ist gruen, ohne dass eine seiner rund zwei Dutzend
      `build_query(index, ...)`-Zeilen angefasst wurde (`git diff --stat` zeigt die Datei in diesem
      Task nicht).
  </acceptance_criteria>
  <done>Feldliste, Titelliste und Boosts sind ein Wert, der Vorgabewert ist der Bestandsplan, und die vorhandenen Aufrufzeilen laufen unveraendert gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Die Phasengrenze aufloesen und den Ersatz danebenstellen</name>
  <files>backend/tests/test_schema_generations.py, backend/tests/test_query_fields_plan.py, backend/tests/test_query_rewrite.py</files>
  <read_first>
    - backend/tests/test_schema_generations.py (vollstaendig, besonders Modulkopf 1-45,
      FIELDS_SCHEMA_1 66-88, Inklusionen 96-146, die drei Faelle gegen den echten Index 153-180 und
      der Waechterteil ab 186 bis zum Dateiende)
    - backend/tests/conftest.py, Zeilen 215-275 und 390-420 (build_schema_1, open_schema_1_index,
      write_schema_1_index, Fixtures schema_1_index und schema_2_index)
    - backend/tests/test_search_fields_lockstep.py, Zeilen 1-40 (Bauart eines Gates: liest Quellen
      als Text, faellt geschlossen, traegt Selbstproben)
    - backend/src/findling/query/rewrite.py (Ergebnis von Task 1)
  </read_first>
  <action>
    In `test_schema_generations.py` faellt alles ab Zeile 186 ("the syntax tree guard over the three
    field lists"): `FIELD_LISTS` (195), `_field_constants` (198), `_entries` (221), `_field_name`
    (236), `body_fields_outside_the_old_schema` (243),
    `test_the_query_field_lists_name_no_field_the_old_schema_lacks` (292) sowie die sechs gestellten
    Muster samt ihrer Tests (295 bis Dateiende, darunter
    `test_the_gate_fails_closed_when_a_field_list_stops_being_a_list`). Der Modulkopf sagt in den
    Zeilen 34-43 selbst voraus, dass dieser Teil in Phase 19 faellt; er wird entsprechend
    umgeschrieben und nennt `backend/tests/test_query_fields_plan.py` namentlich als das, was an
    seine Stelle getreten ist. Diese Nennung ist die Uebergabebedingung aus `18-03-SUMMARY.md` und
    gehoert in denselben Commit.

    Was BLEIBT: `FIELDS_SCHEMA_1` mit seinem Kommentarblock (66-88), die Mengeninklusionen (96-146)
    und die drei Faelle gegen den echten Schema-1-Index (153-180). Die Inklusionen lesen ab hier
    `LEGACY_PLAN.fields`, `LEGACY_PLAN.title_only` und `LEGACY_PLAN.boosts` statt der drei
    gefallenen Konstanten; die Aussage bleibt dieselbe und der Einzeiler bleibt ein Einzeiler. Die
    Testnamen bleiben unveraendert, damit die Historie lesbar bleibt.

    NEU: `backend/tests/test_query_fields_plan.py`, geschrieben in der Gate-Bauart von
    `test_search_fields_lockstep.py`, mit einem Modulkopf, der sagt, was schiefgeht, was die Datei
    beweist und was sie nicht beweist. Die Haelfte, die dieser Plan liefert:

    - Der Bestandsplan gegen einen echten Schema-1-Index: `LEGACY_PLAN.fields` und
      `LEGACY_PLAN.boosts` gehen GEMEINSAM in einen `schema_1_index.parse_query_lenient(...)`-Aufruf,
      der nicht wirft und eine leere Fehlerliste meldet. Das ist die Maschine statt der Namen und
      damit staerker als der Waechter, den er ersetzt.
    - Die Gegenprobe, die den Fall erst zu einem Fall macht: derselbe Aufruf mit einem zusaetzlichen
      Boost auf `FIELD_BODY_ES` wirft, geprueft mit `pytest.raises(ValueError, match=field)` auf den
      Feldnamen und nicht auf die Klammerung. Das ist RESEARCH Messung M-1 als Zusicherung und der
      Beleg, dass die Boost-Haelfte denselben Pfad aufreisst wie die Feldliste.
    - `set(LEGACY_PLAN.boosts) <= set(LEGACY_PLAN.fields)` als Mengenaussage (RESEARCH Pitfall 1).
    - Derselbe Plan gegen `schema_2_index`: wirft ebenfalls nicht, weil das neue Schema eine
      Obermenge ist.

    In `test_query_rewrite.py` kommt genau ein neuer Fall dazu: `build_query(index, "kuendigung")`
    ohne `plan` erzeugt dieselbe Trefferliste wie derselbe Aufruf mit `plan=LEGACY_PLAN`. Das belegt
    den sicheren Vorgabewert aus RESEARCH Pitfall 6. Die vorhandenen Aufrufzeilen bleiben unberuehrt.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest -q tests/test_query_fields_plan.py tests/test_schema_generations.py tests/test_query_rewrite.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FIELD_LISTS\|_field_constants\|body_fields_outside_the_old_schema" backend/tests/test_schema_generations.py`
      ist 0.
    - `grep -c "test_query_fields_plan" backend/tests/test_schema_generations.py` ist mindestens 1.
    - Aus backend/: `uv run pytest -q tests/test_schema_generations.py --no-header --collect-only`
      listet `test_the_default_field_list_exists_in_the_old_schema` weiterhin auf.
    - backend/tests/test_query_fields_plan.py enthaelt mindestens ein
      `pytest.raises(ValueError, match=` und mindestens eine Verwendung von `schema_1_index`.
    - Aus backend/: `uv run pytest -q tests/test_query_fields_plan.py --no-header` meldet
      mindestens vier bestandene Faelle und keinen uebersprungenen.
  </acceptance_criteria>
  <done>Der AST-Waechter ist weg, sein Ersatz nennt sich im Kopf der geleerten Datei, und die Maschine beweist, was vorher ueber Namen behauptet wurde.</done>
</task>

<task type="auto">
  <name>Task 3: Ratsche nachziehen und die Gates gruen fahren</name>
  <files>backend/tests/test_measurement_scripts.py</files>
  <read_first>
    - backend/tests/test_measurement_scripts.py, Zeilen 860-920 (der datierte Kommentarblock ueber
      PACKAGE_FILES_TODAY und PACKAGE_TREE_HASH_TODAY, ein Absatz je Bewegung) und Zeilen 1062-1083
      (test_the_recipe_reproduces_the_tree_hash_of_the_python_package)
    - ./CLAUDE.md, Abschnitt Qualitaetsgates
  </read_first>
  <action>
    Genau eine der 56 Dateien unter `backend/src/findling` hat in diesem Plan ihre Bytes geaendert,
    `query/rewrite.py`. `PACKAGE_FILES_TODAY` bleibt deshalb bei 56 und nur
    `PACKAGE_TREE_HASH_TODAY` bewegt sich. Den neuen Hash NICHT raten: den Fall
    `test_the_recipe_reproduces_the_tree_hash_of_the_python_package` laufen lassen, den gemeldeten
    Istwert uebernehmen und darueber einen Absatz in der Form der bestehenden Kette anfuegen
    ("Moved on 2026-09-24 a thirty sixth time, ..."), der benennt, welche Datei sich bewegt hat und
    warum die Zahl 56 stehen bleibt. `PHP_FILES_TODAY` und `PHP_TREE_HASH_TODAY` werden NICHT
    angefasst: die PHP-Haelfte aendert sich in dieser Phase nicht (bestaetigt in 19-PATTERNS.md,
    Vorbemerkung zu Annahme A4).

    Danach die vier Qualitaetsgates aus `./CLAUDE.md` lokal gruen fahren, aus `backend/`:
    `uv run ruff check .`, `uv run ruff format --check .`, pyright mit
    `PYRIGHT_PYTHON_FORCE_VERSION=latest`, `uv run vulture src tests --min-confidence 80`. Meldet
    vulture einen neuen oeffentlichen Namen als ungenutzt, ist die richtige Antwort eine Verwendung
    im Test oder der vorhandene Whitelist-Weg dieses Repos, nicht ein Unterdruecker an der
    Definition.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run vulture src tests --min-confidence 80 &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Aus backend/: `uv run pytest -q tests/test_measurement_scripts.py -k test_the_recipe_reproduces_the_tree_hash_of_the_python_package --no-header`
      ist gruen.
    - `grep -n "PACKAGE_FILES_TODAY = 56" backend/tests/test_measurement_scripts.py` liefert genau
      eine Zeile.
    - `git diff --name-only` zeigt `backend/tests/test_measurement_scripts.py` und
      `backend/src/findling/query/rewrite.py` im selben Commit.
    - `PHP_TREE_HASH_TODAY` ist im Diff dieses Plans nicht enthalten.
    - Aus backend/: `uv run pytest -q` endet ohne Fehlschlag, und pyright mit
      `PYRIGHT_PYTHON_FORCE_VERSION=latest` meldet 0 errors.
  </acceptance_criteria>
  <done>Die Ratsche traegt den neuen Baumhash mit einem Absatz, der ihn erklaert, und alle vier Gates plus die volle Suite sind lokal gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nextcloud-Nutzer zu PHP-Companion zu Backend-Suchroute | Der Suchbegriff ist ungeprueftes Nutzereingabe-Material und erreicht hier den Parser |
| Code zu tantivy-Verzeichnis | Die vom Code gestellte Feldliste trifft auf ein persistiertes Schema, das aelter sein kann als der Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Denial of Service | `query/rewrite.py::build_query` | mitigate | Der Vorgabewert von `plan` ist `LEGACY_PLAN`, also die vier Felder, die jedes ausgelieferte Verzeichnis kennt. Ein vergessener Aufrufer kann die dauerhaft leere degraded-Antwort nicht ausloesen. Belegt durch den neuen Fall in `test_query_rewrite.py`. |
| T-19-01-02 | Denial of Service | `field_boosts` | mitigate | Feldliste und Boosts stehen in einem Wert; `set(plan.boosts) <= set(plan.fields)` ist zugesichert. Gegenprobe in `test_query_fields_plan.py`: ein Boost auf `body_es` gegen Schema 1 wirft. |
| T-19-01-03 | Tampering | Nutzereingabe im Suchfeld | accept | Gemessen (RESEARCH M-1): `body_es:vertrag` in der Suchleiste erzeugt einen Eintrag in `errors` und keine Ausnahme. Gefaehrlich ist ausschliesslich die vom Code gestellte Liste. `allow_regexes=False` bleibt gesetzt und von `test_index_open.py:377` bewacht. |
| T-19-01-04 | Information Disclosure | Parser-Fehlerliste im Log | mitigate | Die Zeile bei `rewrite.py:546-549` bleibt auf `debug` und zaehlt nur; der Anfragetext wandert nicht auf `info` oder hoeher. Keine neue Logzeile in diesem Plan fuehrt `text`. |
| T-19-01-SC | Tampering | npm/pip/cargo installs | mitigate | Entfaellt mangels Gegenstand: dieser Plan installiert kein Paket, `backend/pyproject.toml` und `backend/uv.lock` bleiben unveraendert (RESEARCH, Package Legitimacy Audit). Ein Diff an einer der beiden Dateien ist ein Abbruchgrund. |
</threat_model>

<verification>
- `uv run pytest -q` aus `backend/` ist vollstaendig gruen.
- `git diff --stat` nennt genau die fuenf Dateien aus `files_modified` und keine weitere.
- `backend/pyproject.toml` und `backend/uv.lock` stehen nicht im Diff.
- Kein Em-Dash und keine echten Umlaute in Code, Bezeichnern, Kommentaren oder Logtexten.
</verification>

<success_criteria>
- `DEFAULT_FIELDS`, `TITLE_ONLY_FIELDS` und `FIELD_BOOSTS` existieren als Modulnamen nicht mehr.
- `FieldPlan` und `LEGACY_PLAN` stehen in `query/rewrite.py`, `LEGACY_PLAN` traegt die vier heutigen
  Feldnamen und die vier heutigen Boosts.
- `build_query` nimmt `plan` keyword-only mit `LEGACY_PLAN` als Vorgabewert.
- `test_schema_generations.py` hat keinen AST-Waechter mehr und nennt seinen Ersatz im Kopf.
- `backend/tests/test_query_fields_plan.py` existiert und prueft den Plan gegen einen echten
  Schema-1-Index, mit Gegenprobe.
- `PACKAGE_TREE_HASH_TODAY` ist im selben Commit nachgezogen, `PACKAGE_FILES_TODAY` steht auf 56.
- ruff, ruff format, pyright (`PYRIGHT_PYTHON_FORCE_VERSION=latest`), vulture und die volle Suite
  sind lokal gruen.
</success_criteria>

<output>
Create `.planning/phases/19-frageseite-freischalten/19-01-SUMMARY.md` when done
</output>
