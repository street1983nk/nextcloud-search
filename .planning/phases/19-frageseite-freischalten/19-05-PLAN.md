---
phase: 19-frageseite-freischalten
plan: 05
type: execute
wave: 3
depends_on: ["19-03"]
files_modified:
  - backend/tests/test_no_language_detection.py
autonomous: true
requirements: [LEX-05]

must_haves:
  truths:
    - "Es gibt keinen Spracherkennungspfad, weder dokument- noch anfrageseitig, und ein Test haelt das fest"
    - "Die Funktion, die den Feldplan baut, nimmt keinen Anfragetext entgegen: Spracherkennung ist nicht anschliessbar"
    - "build_query liest den Feldplan nur aus seinem Parameter und nie aus settings() oder einer Modulkonstante"
    - "Weder pyproject.toml noch uv.lock fuehren ein Paket aus der Spracherkennungsfamilie"
  artifacts:
    - path: "backend/tests/test_no_language_detection.py"
      provides: "Struktureller Anti-Feature-Waechter mit gestellten Mustern und Selbstproben"
      min_lines: 120
  key_links:
    - from: "backend/tests/test_no_language_detection.py"
      to: "backend/src/findling/api/resources.py"
      via: "ast ueber die Signatur von field_plan_for"
      pattern: "ast\\."
    - from: "backend/tests/test_no_language_detection.py"
      to: "backend/uv.lock"
      via: "Dateilesung gegen eine benannte Namensmenge"
      pattern: "uv.lock"
---

<objective>
Der Anti-Feature-Waechter: ein Quelltextleser, der festhaelt, dass es keinen Spracherkennungspfad
gibt, weder dokument- noch anfrageseitig.

Purpose: Erfolgskriterium 4 der Roadmap verlangt genau das, und die Abwesenheit eines Pfades ist
funktional nicht beweisbar. Ein funktionaler Test prueft, was da ist; eine zweite Route saesse neben
allem, was er anfasst, und jede einzelne Zusicherung bliebe gruen. Das Repo hat dafuer ein
eingefuehrtes Muster, `test_semantic_boundary.py`, und dieser Plan uebertraegt es.

Output: `backend/tests/test_no_language_detection.py` mit vier Aussagen, gestellten Mustern und
Selbstproben.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frageseite-freischalten/19-RESEARCH.md
@.planning/phases/19-frageseite-freischalten/19-PATTERNS.md
@.planning/phases/19-frageseite-freischalten/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Hygiene und die zwei strukturellen Aussagen</name>
  <files>backend/tests/test_no_language_detection.py</files>
  <read_first>
    - backend/tests/test_semantic_boundary.py (vollstaendig; besonders Modulkopf 1-30,
      `_significant_tokens` 79-102, `code_mentions` und `call_sites` 105-122, `functions_calling`
      134-149, die gestellten Muster 265-283 und die Selbstproben 360-396)
    - backend/src/findling/api/resources.py (Ergebnis von Plan 19-03, besonders `field_plan_for`)
    - backend/src/findling/query/rewrite.py (Ergebnis von Plan 19-01, besonders `build_query`)
    - .planning/phases/19-frageseite-freischalten/19-RESEARCH.md, Pattern 5
  </read_first>
  <action>
    Neues Modul `backend/tests/test_no_language_detection.py`. Der Modulkopf uebernimmt den Absatz
    aus `test_semantic_boundary.py:12-18` sinngemaess auf die Spracherkennung: warum diese Datei ein
    Quelltextleser und kein funktionaler Test ist. Dazu die Herkunft des Anti-Features: einstimmig,
    in STATE.md als milestone-tragende Entscheidung gefuehrt, und Erfolgskriterium 4 der Roadmap
    Phase 19.

    Die Hygiene wird uebernommen und nicht nachgebaut: `tokenize` statt Zeilenfilter, damit
    Kommentare und Zeichenketten den Zaehler nicht ausloesen. Genau dieses Modul spricht die
    verbotenen Woerter in seiner Prosa aus, also ist die Hygiene hier keine Formalie. Die Helfer aus
    `test_semantic_boundary.py` (`_significant_tokens`, `code_mentions`, `call_sites`,
    `functions_calling`) werden in der dort erprobten Form mitgenommen; wo der Baum sie schon
    hergibt, werden sie gelesen und nicht kopiert, sonst driften zwei Definitionen auseinander.

    Aussage 1, die staerkste: die Funktion, die den Feldplan baut, nimmt keinen Anfragetext
    entgegen. Als `ast`-Pruefung ueber die Signatur von `field_plan_for` in
    `backend/src/findling/api/resources.py`, mit der Vorlage `functions_calling`, die bereits
    `ast.FunctionDef` und `ast.AsyncFunctionDef` besucht: kein Parameter, der Text heisst oder Text
    sein kann. Die verbotene Namensmenge steht als benannte Konstante im Modul und ist geschlossen
    (`text`, `query`, `term`, `line`, `search`, `q` und die offensichtlichen Varianten). Eine
    Spracherkennung der Anfrage ist damit nicht verboten, sondern nicht anschliessbar.

    Aussage 2: `build_query` liest den Feldplan nur aus seinem Parameter und nie aus `settings()`
    oder einer Modulkonstante. Gezaehlt ueber den entkommentierten Quelltext von
    `query/rewrite.py` mit `code_mentions`: `settings` kommt im Code dieses Moduls nicht in der
    Funktion `build_query` vor, und die drei gefallenen Konstantennamen kommen ueberhaupt nicht mehr
    vor.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest -q tests/test_no_language_detection.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import ast" backend/tests/test_no_language_detection.py` ist 1 und
      `grep -c "import tokenize" backend/tests/test_no_language_detection.py` ist 1.
    - Das Modul enthaelt eine benannte, geschlossene Konstante mit den verbotenen Parameternamen.
    - Aus backend/: `uv run pytest -q tests/test_no_language_detection.py --no-header` meldet
      mindestens zwei bestandene Faelle.
  </acceptance_criteria>
  <done>Die zwei strukturellen Aussagen stehen, gelesen ueber den Syntaxbaum und den entkommentierten Quelltext.</done>
</task>

<task type="auto">
  <name>Task 2: Abhaengigkeitsbaum, Wireformat und die gestellten Gegenproben</name>
  <files>backend/tests/test_no_language_detection.py</files>
  <read_first>
    - backend/tests/test_semantic_boundary.py, Zeilen 260-300 und 355-400 (die gestellten Muster und
      die Selbstproben, die belegen, dass der Zaehler wirklich zaehlt)
    - backend/tests/test_measurement_scripts.py, Zeilen 960-1000 (das Dateilesemuster: Datei als
      Text lesen, gegen eine benannte Konstantenmenge halten, Befund je Treffer)
    - backend/tests/test_search_fields_lockstep.py, Zeilen 1-40 (haelt `extra="forbid"` und die
      Feldliste von `SearchRequest` bereits fest)
    - backend/pyproject.toml und backend/uv.lock (nur daraufhin, wie sie gelesen werden koennen)
  </read_first>
  <action>
    Aussage 3, am Abhaengigkeitsbaum: weder `backend/pyproject.toml` noch `backend/uv.lock` fuehren
    ein Paket aus der Spracherkennungsfamilie. Die geschlossene Namensmenge steht als benannte
    Konstante im Modul und enthaelt mindestens `langdetect`, `lingua`, `langid`, `py3langid`,
    `fasttext`, `cld2`, `cld3` und `pycld`; sie ist am 24.09.2026 gegen beide Dateien geprueft
    worden und war leer. Die Bauart ist das Dateilesemuster aus `test_measurement_scripts.py`:
    Datei als Text lesen, gegen die Menge halten, ein Befund je Treffer, und der Test faellt
    geschlossen, also erzeugt eine Datei, die nicht mehr da ist, einen Befund und nicht null.

    Aussage 4 wird NICHT wiederholt: `test_search_fields_lockstep.py` haelt `extra="forbid"` und die
    Feldliste von `SearchRequest` und `SnippetsRequest` bereits fest. Der Waechter nennt die Datei
    namentlich in einer Zusicherung, die prueft, dass es sie noch gibt, und dupliziert ihre Aussage
    nicht.

    Gegenproben, ohne die der Waechter Dekoration ist, in der Form der gestellten Muster aus
    `test_semantic_boundary.py`:

    - Ein gestelltes Muster einer Planfunktion MIT einem Textparameter in der Signatur muss die
      Pruefung von Aussage 1 rot machen.
    - Ein gestelltes Muster, das `settings` und die verbotenen Woerter ausschliesslich in einem
      Kommentar und in einem Docstring nennt, darf keinen Zaehler bewegen; dazu die Zusicherung,
      dass eine naive Zaehlung an diesem Muster deutlich danebenliegt, sonst beweist das Muster
      nichts.
    - Ein gestelltes Muster einer Abhaengigkeitsdatei mit einem der verbotenen Paketnamen muss die
      Pruefung von Aussage 3 rot machen.
    - Ein leeres Modul muss einen Befund je erwarteter Aussage liefern und nicht null.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest -q tests/test_no_language_detection.py --no-header &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run vulture src tests --min-confidence 80 &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Das Modul enthaelt mindestens vier gestellte Muster als Zeichenkettenkonstanten und je einen
      Fall, der an ihnen rot bzw. unbewegt ist.
    - `grep -c "test_search_fields_lockstep" backend/tests/test_no_language_detection.py` ist
      mindestens 1.
    - `grep -c "langdetect" backend/tests/test_no_language_detection.py` ist mindestens 1, und
      `grep -rc "langdetect" backend/pyproject.toml backend/uv.lock` meldet 0 in beiden Dateien.
    - Aus backend/: `uv run pytest -q tests/test_no_language_detection.py --no-header` meldet
      mindestens sechs bestandene Faelle und keinen uebersprungenen.
    - Aus backend/: `uv run pytest -q` ist vollstaendig gruen und die vier Qualitaetsgates sind
      lokal gruen.
  </acceptance_criteria>
  <done>Alle vier Aussagen stehen, jede mit einer Gegenprobe, und ein geleerter Waechter kann nicht null Befunde melden und gesund aussehen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anfragetext zu Feldwahl | Waere hier eine Verbindung, koennte die Feldliste aus Nutzereingabe entstehen |
| Abhaengigkeitsbaum zu Produkt | Ein still hinzugekommenes Erkennungspaket waere ein Anti-Feature-Bruch ohne Codeaenderung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | Elevation of Privilege | `field_plan_for`-Signatur | mitigate | `ast`-Pruefung ueber die Parameternamen gegen eine geschlossene Menge; ein gestelltes Muster mit Textparameter muss rot werden. |
| T-19-05-02 | Tampering | `query/rewrite.py::build_query` | mitigate | `code_mentions` ueber den entkommentierten Quelltext: kein `settings`-Aufruf in der Funktion, keine der drei gefallenen Konstanten mehr im Modul. |
| T-19-05-03 | Tampering | `pyproject.toml`, `uv.lock` | mitigate | Beide Dateien werden gegen eine benannte Paketnamensmenge gelesen; der Test faellt geschlossen, eine verschwundene Datei erzeugt einen Befund. |
| T-19-05-04 | Repudiation | Der Waechter selbst | mitigate | Selbstproben nach dem Muster von `test_semantic_boundary.py`: ein geleerter Waechter meldet einen Befund je erwarteter Aussage und nicht null. |
| T-19-05-SC | Tampering | npm/pip/cargo installs | mitigate | Entfaellt mangels Gegenstand: kein Paket wird installiert; dieser Plan prueft im Gegenteil, dass keines aus der Erkennungsfamilie dazukommt. |
</threat_model>

<verification>
- `uv run pytest -q` aus `backend/` ist vollstaendig gruen.
- `git diff --name-only` nennt genau `backend/tests/test_no_language_detection.py`.
- Die verbotenen Woerter stehen in Prosa und in gestellten Mustern, und die Hygiene sorgt dafuer,
  dass das keinen Zaehler bewegt; das wird von einer eigenen Zusicherung belegt.
- Keine Em-Dashes, keine echten Umlaute in Code, Bezeichnern oder Kommentaren.
</verification>

<success_criteria>
- `backend/tests/test_no_language_detection.py` existiert und haelt alle vier Aussagen aus
  RESEARCH Pattern 5 fest.
- Jede Aussage hat eine gestellte Gegenprobe.
- Der Waechter loest sich nicht an seiner eigenen Prosa aus, und das ist zugesichert.
- Erfolgskriterium 4 der Roadmap ist damit belegt.
</success_criteria>

<output>
Create `.planning/phases/19-frageseite-freischalten/19-05-SUMMARY.md` when done
</output>
