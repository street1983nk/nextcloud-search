---
phase: 19-frageseite-freischalten
plan: 07
type: execute
wave: 4
depends_on: ["19-03", "19-06"]
files_modified:
  - .github/workflows/deploy-harp.yml
  - backend/tests/test_language_proof_steps.py
autonomous: true
requirements: [LEX-05]

must_haves:
  truths:
    - "Auf einer Instanz mit aktivem Spanisch, Italienisch, Niederlaendisch und Portugiesisch findet der Nutzer je Sprache ein Dokument ueber die Stammform seines Suchworts, in der Unified Search"
    - "Derselbe Beweis laeuft auf allen vier Matrixaesten einschliesslich ubuntu-24.04-arm"
    - "Die Ergebnisseite liefert denselben Treffer, und ihre Route wird von der CI zum ersten Mal beruehrt"
  artifacts:
    - path: ".github/workflows/deploy-harp.yml"
      provides: "Ein ungegateter Sprachbeweis mit vier Dateien und vier Suchen, plus Ergebnisseiten-Abruf"
      contains: "LANGUAGE_PROOF_BUDGET_SECONDS"
    - path: "backend/tests/test_language_proof_steps.py"
      provides: "Textgate, das den Schritt ungegatet und seine Zusicherung auf entries|length haelt"
      min_lines: 80
  key_links:
    - from: ".github/workflows/deploy-harp.yml"
      to: "ocs/v2.php/search/providers/findling/search"
      via: "Pollschleife je Sprache, verlaesst sich beim Treffer"
      pattern: "providers/findling/search"
    - from: ".github/workflows/deploy-harp.yml"
      to: "index.php/apps/findling/?term="
      via: "curl gegen die Ergebnisseite, Dateiname im HTML"
      pattern: "apps/findling/\\?term="
---

<objective>
Ein einziger, ungegateter CI-Schritt beweist auf einer echten Nextcloud, dass die vier neuen Sprachen
ueber die ganz normale Unified Search antworten, und ein Abruf der Ergebnisseite deckt die zweite
Haelfte von Erfolgskriterium 1 ab.

Purpose: "Store upgrade 5" und "Store upgrade 6" tragen beide
`if: matrix.server-version == 'stable34' && matrix.runner == 'ubuntu-24.04' && env.RELEASE_TAG == ''`
(Zeilen 3528 und 3737) und laufen damit auf drei von vier Matrixaesten gar nicht, den arm64-Ast
eingeschlossen. Erfolgskriterium 1 verlangt woertlich "ueber den gruenen arm64-CI-Ast". Der Beweis
muss deshalb in einem Schritt OHNE `if` stehen. Das Vorbild ist "Store install 7" (Zeile 2129).

Entschieden (RESEARCH Open Question 3, Empfehlung): EIN Schritt mit vier Dateien und vier Suchen auf
EINER Instanz, nicht vier Schritte. Die Laufzeit wird beim ersten gruenen Lauf gemessen und als Zahl
in den Kommentar geschrieben, wie es die uebrigen Budgets dieses Workflows tun.

Output: die Sprachvorgabe in der temporaeren info.xml der Entwicklerstrecke, ein neuer ungegateter
Schritt, ein Textgate, das ihn ungegatet haelt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frageseite-freischalten/19-RESEARCH.md
@.planning/phases/19-frageseite-freischalten/19-PATTERNS.md
@.planning/phases/19-frageseite-freischalten/19-03-SUMMARY.md
@.planning/phases/19-frageseite-freischalten/19-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Entwicklerstrecke faehrt sechs Sprachen</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml, Zeilen 200-240 (die Matrix, besonders der arm64-Eintrag
      `runner: ubuntu-24.04-arm` und der Kommentar darueber), Zeilen 280-370 (die env-Bloecke mit
      `ZERO_CONFIG_BUDGET_SECONDS` und `REBUILD_LANGUAGES`), Zeilen 587-700 (Bau der temporaeren
      info.xml, Registrierung des Daemons, Installation der ExApp, Testnutzer und `files:scan`),
      Zeilen 700-760 (die Suche ueber die OCS-Route und der Beginn der Driftstrecke)
    - .github/workflows/deploy-harp.yml, Zeilen 780-800 und 1060-1210 (die weiteren
      `app_api:app:register`-Aufrufe der Entwicklerstrecke und die info.xml, die sie verwenden)
    - backend/appinfo/info.xml, Zeilen 335-360 (der `environment-variables`-Block mit
      `FINDLING_LANGUAGES` und seinem `<default>de,en</default>`)
    - .planning/phases/19-frageseite-freischalten/19-RESEARCH.md, Pattern 7b und Pitfall 3
  </read_first>
  <action>
    Der Schritt "Build the temporary info.xml that points at the local registry" (Zeile 587) baut die
    info.xml der Entwicklerstrecke per `sed` aus `findling-src/backend/appinfo/info.xml`. Dort kommt
    ein vierter Ausdruck dazu, der `<default>de,en</default>` auf `<default>de,en,es,it,nl,pt</default>`
    setzt. Die Zeichenkette `<default>de,en</default>` kommt in der Quelldatei genau einmal vor
    (geprueft am 24.09.2026); der Schritt prueft das, bevor er ersetzt, und bricht sonst ab, statt
    stumm die falsche Vorgabe zu treffen. Die `grep -E`-Zeile darunter, die Registry, Image und Tag
    protokolliert, bekommt die Sprachzeile dazu, damit im Protokoll steht, womit installiert wurde.

    Die Quelldatei bleibt unberuehrt; die vorhandene Zeile `git -C findling-src diff --quiet --
    backend/appinfo/info.xml` prueft das bereits und bleibt stehen.

    Ein Kommentarblock ueber dem neuen Ausdruck sagt, warum die Entwicklerstrecke der Ort ist: sie
    ist ungegatet und laeuft auf allen vier Matrixaesten, die Store-Upgrade-Strecke nicht; die
    Instanz baut ihren Index von Anfang an mit sechs Koerperfeldern, es ist also kein Umbau noetig
    und der Beweis kostet keine Umbauzeit; und der Store-Durchgang bleibt unberuehrt, weil er mit
    `STORE_INFO_XML` und damit mit der ausgelieferten Vorgabe `de,en` registriert.

    Die uebrigen `app_api:app:register`-Aufrufe der Entwicklerstrecke (Driftprobe, Uninstall 2,
    Uninstall 5) werden daraufhin gelesen, aus welcher info.xml sie speisen. Speisen sie aus der
    temporaeren Datei, erben sie die Sprachmenge und es ist nichts zu tun; baut einer seine Datei
    direkt aus der Quelle, bekommt er denselben Ausdruck, damit die Sprachmenge innerhalb der
    Strecke nicht auseinanderlaeuft. Welcher Fall zutrifft, steht in der Summary.

    Beim Start mit sechs Sprachen warnt der Container, dass `FINDLING_OCR_LANGUAGES` (Vorgabe
    `deu+eng+fra`) die Sprachmenge nicht deckt. Das ist die Startwarnung aus Phase 18,
    Erfolgskriterium 4, sie ist erwartet und kein Fehlschlag; die Protokollauswertung dieses Plans
    darf sie nicht als Abbruch werten.
  </action>
  <verify>
    <automated>grep -n "de,en,es,it,nl,pt" .github/workflows/deploy-harp.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "de,en,es,it,nl,pt" .github/workflows/deploy-harp.yml` ist mindestens 1, und die
      Zeile steht im Schritt "Build the temporary info.xml".
    - `grep -c "<default>de,en</default>" backend/appinfo/info.xml` ist weiterhin 1 (die Quelldatei
      ist unveraendert).
    - `git diff --name-only` nennt `backend/appinfo/info.xml` NICHT.
    - Der Schritt bricht ab, wenn die zu ersetzende Zeichenkette nicht genau einmal vorkommt; die
      Pruefung steht als eigene Zeile im Schritt.
  </acceptance_criteria>
  <done>Die Entwicklerinstallation der CI faehrt sechs Sprachen, die Store-Strecke unveraendert zwei, und die Quelldatei ist nachweislich unberuehrt.</done>
</task>

<task type="auto">
  <name>Task 2: Der ungegatete Sprachbeweis, vier Dateien, vier Suchen, eine Ergebnisseite</name>
  <files>.github/workflows/deploy-harp.yml</files>
  <read_first>
    - .github/workflows/deploy-harp.yml, Zeilen 2129-2232 ("Store install 7" in voller Laenge: der
      WebDAV-Upload, die Pollschleife mit `ZERO_CONFIG_BUDGET_SECONDS`, die Auswertung mit `jq`, die
      Pruefung gegen das Protokoll, der Eintrag in die Zusammenfassung)
    - .github/workflows/deploy-harp.yml, Zeilen 700-760 (der vorhandene OCS-Suchschritt der
      Entwicklerstrecke und der Beginn der Driftstrecke, damit der neue Schritt an der richtigen
      Stelle steht)
    - .github/workflows/deploy-harp.yml, Zeilen 840-890 (die beiden vorhandenen Abrufe von
      `index.php/apps/findling/admin/overview` mit ihrer Auswertung ueber `.backend.languagesActive`)
    - .planning/phases/19-frageseite-freischalten/19-RESEARCH.md, Pattern 7b, Pattern 7c,
      Pitfall 4 und Messung M-5
  </read_first>
  <action>
    Ein neuer Schritt OHNE `if`, eingefuegt in der Entwicklerstrecke nach dem vorhandenen Schritt
    "Search over the ordinary OCS route" (Zeile 701) und vor "The drift probe both directions share"
    (Zeile 745). Name in der Form der uebrigen: "Language proof, the four new chains answer on the
    ordinary search route".

    Vorbedingung, und sie steht zuerst: der Schritt liest
    `http://localhost:8080/index.php/apps/findling/admin/overview` und bricht ab, wenn
    `.backend.languagesActive` nicht die sechs Sprachen in Schemafeldreihenfolge meldet. Ohne diese
    Zeile koennte der ganze Beweis ueber eine Instanz mit `de,en` laufen und trotzdem gruen
    aussehen.

    Vier Dateien ueber WebDAV, eine je Sprache, in der Form des Uploads aus "Store install 7"
    (`curl -T`, Statuscode geprueft, 201 oder 204). Inhalt und Frage bilden je ein Formenpaar, das
    NUR die eigene Kette zusammenfuehrt; gemessen am 24.09.2026 (RESEARCH Messungen M-5 und M-6):

    | Sprache | Wort im Dokument | Suchwort | gemeinsamer Term |
    |---|---|---|---|
    | es | alemana | alemanes | aleman |
    | it | informazione | informazioni | inform |
    | nl | beinvloed mit Trema auf dem i | beinvloeden | beinvloed |
    | pt | paises mit Akut auf dem i | pais | pais |

    Alle vier SUCHWOERTER sind reines ASCII und brauchen keine URL-Kodierung; die Akzente stehen
    ausschliesslich im Dateiinhalt, wo sie Sprachdaten sind. Die uebrigen Woerter der vier Dateien
    werden nach der Regel des Kommentarblocks bei Zeile 2646 gewaehlt: nichts, was ein anderer
    Beweis dieses Workflows zaehlt, und nichts, was der deutsche Zerleger in ein solches Wort
    zerlegt.

    Danach eine Pollschleife in der Bauart von "Store install 7": ein eigenes Budget
    `LANGUAGE_PROOF_BUDGET_SECONDS` im env-Block des Jobs, je Runde `php -f cron.php`, danach je
    Sprache eine OCS-Suche gegen
    `ocs/v2.php/search/providers/findling/search?term=...`. Die Schleife verlaesst sich, sobald alle
    vier Sprachen einen Treffer haben, und schlaeft nie eine feste Zeit; das ist die 52-Sekunden-Lehre
    aus Plan 06-11. Ein gerissenes Budget ist ein Befund und kein Grund, das Budget zu heben
    (T-06.1-52); der Fehlerpfad gibt die letzte Antwort, den letzten Cron-Lauf und die Containerlogs
    aus.

    Die Zusicherung je Sprache prueft `.ocs.data.entries | length >= 1` und den Dateinamen in
    `.ocs.data.entries[0].title`. Sie prueft NIEMALS `.ocs.data.entries[0].subline`: ein Treffer, der
    ausschliesslich ueber ein neues Sprachfeld gefunden wurde, kommt gemessen ohne Textauszug zurueck
    (RESEARCH Pitfall 4 und Messung M-4). Ein Kommentar an der Zusicherung sagt genau das, damit ein
    spaeterer Leser die fehlende Unterzeile nicht fuer einen Fehler haelt.

    Zuletzt die Ergebnisseite, die bis heute kein CI-Schritt beruehrt: ein
    `curl -u testuser:"${TESTUSER_PASS}" 'http://localhost:8080/index.php/apps/findling/?term=alemanes'`.
    `PageController::index` traegt `NoAdminRequired`, `NoCSRFRequired` und `FrontpageRoute`
    (`php/lib/Controller/PageController.php:215-218`), der Abruf liefert also HTML. Geprueft wird der
    Statuscode und dass der Dateiname des spanischen Treffers im HTML steht, nicht der Textauszug.

    Der Schritt schreibt am Ende eine Zeile in `GITHUB_STEP_SUMMARY`, in der Form der uebrigen:
    welche vier Sprachen geantwortet haben, nach wie vielen Cron-Runden, und auf welchem Matrixast.
    Die gemessene Laufzeit des Schritts wird nach dem ersten gruenen Lauf als Zahl in den
    Kommentarblock ueber dem Budget geschrieben; bis dahin traegt der Kommentar den Satz, dass die
    Zahl beim ersten Lauf nachgetragen wird.
  </action>
  <verify>
    <automated>grep -n "Language proof, the four new chains" .github/workflows/deploy-harp.yml</automated>
  </verify>
  <acceptance_criteria>
    - Der neue Schritt steht zwischen den Schritten "Search over the ordinary OCS route" und "The
      drift probe both directions share" und traegt KEINE `if:`-Zeile.
    - `grep -c "LANGUAGE_PROOF_BUDGET_SECONDS" .github/workflows/deploy-harp.yml` ist mindestens 2
      (Definition im env-Block und Verwendung im Schritt).
    - Im neuen Schritt kommt `entries | length` vor und `entries[0].subline` nicht.
    - Im neuen Schritt kommt `languagesActive` in einer Vorbedingung vor, die mit `exit 1` endet.
    - Im neuen Schritt kommt `apps/findling/?term=` genau einmal vor.
    - `sleep` steht im neuen Schritt nur innerhalb der Pollschleife und nie vor der ersten Abfrage.
    - Die vier Suchwoerter sind reines ASCII: `grep -n "term=" .github/workflows/deploy-harp.yml`
      zeigt fuer die neuen Zeilen keine Nicht-ASCII-Zeichen.
  </acceptance_criteria>
  <done>Ein ungegateter Schritt laedt vier Dateien hoch, findet sie je Sprache ueber die OCS-Route und holt den spanischen Treffer zusaetzlich von der Ergebnisseite.</done>
</task>

<task type="auto">
  <name>Task 3: Ein Textgate haelt den Schritt ungegatet</name>
  <files>backend/tests/test_language_proof_steps.py</files>
  <read_first>
    - backend/tests/test_lockstep_versions.py, Zeilen 240-290 (warum dieses Repo seine Workflows als
      TEXT liest und nicht mit einem YAML-Parser, und wie eine Matrixzeile erkannt wird)
    - backend/tests/test_workflow_pins.py (die Bauart eines Workflow-Textgates in voller Laenge)
    - .github/workflows/deploy-harp.yml (Ergebnis von Task 1 und 2)
  </read_first>
  <action>
    Neues Modul `backend/tests/test_language_proof_steps.py`, als Textgate und ohne YAML-Abhaengigkeit,
    aus dem Grund, den `test_lockstep_versions.py` in seinen Zeilen 245-250 ausspricht: der Backend
    hat keine yaml-Abhaengigkeit, und ein Gate, das eine braeuchte, liefe nicht neben den Unittests.

    Die Aussagen:

    - Der Schritt mit dem Namen des Sprachbeweises existiert genau einmal in
      `.github/workflows/deploy-harp.yml`.
    - Er traegt keine `if:`-Zeile. Das ist die Aussage, die Erfolgskriterium 1 traegt: ein Schritt mit
      `matrix.runner == 'ubuntu-24.04'` erscheint in der Zusammenfassung einmal statt viermal und
      beweist ueber arm64 nichts. Die beiden vorhandenen gegateten Schritte ("Store upgrade 5" und
      "Store upgrade 6") werden im selben Gate als Gegenbeispiel gelesen: sie tragen die `if:`-Zeile,
      und wenn sie es nicht mehr taeten, waere das Gate kaputt und nicht der Workflow heil.
    - Der Rumpf des Schritts nennt `entries | length` und nennt `entries[0].subline` nicht.
    - Der Rumpf nennt alle vier Sprachcodes und alle vier Suchwoerter.
    - Die Matrix fuehrt weiterhin einen Eintrag mit `ubuntu-24.04-arm`.

    Das Gate faellt geschlossen: ist die Datei nicht mehr da oder der Schrittname nicht mehr zu
    finden, gibt es einen Befund je erwarteter Aussage und nicht null. Dazu Selbstproben an
    gestellten Mustern, wie in `test_workflow_pins.py`: ein gestellter Schritt MIT `if:`-Zeile muss
    die zweite Aussage rot machen, ein gestellter Schritt, der `subline` prueft, die dritte.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest -q tests/test_language_proof_steps.py --no-header &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run vulture src tests --min-confidence 80 &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import yaml" backend/tests/test_language_proof_steps.py` ist 0.
    - Das Modul enthaelt mindestens zwei gestellte Muster als Zeichenkettenkonstanten mit je einem
      Fall, der an ihnen rot wird.
    - `grep -c "ubuntu-24.04-arm" backend/tests/test_language_proof_steps.py` ist mindestens 1.
    - Aus backend/: `uv run pytest -q tests/test_language_proof_steps.py --no-header` meldet
      mindestens fuenf bestandene Faelle.
    - Aus backend/: `uv run pytest -q` ist vollstaendig gruen und die vier Qualitaetsgates sind lokal
      gruen.
  </acceptance_criteria>
  <done>Ein Textgate haelt den Sprachbeweis ungegatet, seine Zusicherung auf dem Treffer statt dem Auszug, und den arm64-Eintrag in der Matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI-Zusicherung zu Wirklichkeit | Ein Schritt, der auf drei von vier Aesten nicht laeuft, ist gruen und beweist nichts |
| Testnutzer zu Nextcloud | Der Beweis laeuft als gewoehnlicher Nutzer ueber die OCS-Route und die Seitenroute, nicht als Admin |
| Vorgabewert der info.xml zu ausgeliefertem Artefakt | Eine Sprachvorgabe, die in die Store-Archive geriete, waere eine stille Produktaenderung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-01 | Repudiation | Der neue CI-Schritt | mitigate | Kein `if:`; das Textgate aus Task 3 haelt das fest und liest die beiden gegateten Schritte als Gegenbeispiel. |
| T-19-07-02 | Repudiation | Beweis auf der falschen Instanz | mitigate | Vorbedingung im Schritt: `.backend.languagesActive` muss die sechs Sprachen melden, sonst `exit 1`. |
| T-19-07-03 | Tampering | Ausgeliefertes `backend/appinfo/info.xml` | mitigate | Die Ersetzung trifft nur die temporaere Datei; die vorhandene `git diff --quiet`-Zeile bleibt stehen und das Akzeptanzkriterium prueft, dass die Quelldatei nicht im Diff steht. |
| T-19-07-04 | Denial of Service | Jobdeadline | mitigate | Eigenes Budget `LANGUAGE_PROOF_BUDGET_SECONDS` neben `timeout-minutes: 45`; die Schleife verlaesst sich beim Treffer. Ein gerissenes Budget ist ein Befund und wird nicht angehoben (T-06.1-52). |
| T-19-07-05 | Information Disclosure | Protokoll des Schritts | mitigate | Ausgegeben werden Statuscodes, Rundenzahlen und Dateinamen; `TESTUSER_PASS` steht nur in der curl-Zeile und wird nie ausgegeben, wie in den vorhandenen Schritten. |
| T-19-07-06 | Spoofing | Ergebnisseiten-Abruf | accept | `PageController::index` traegt `NoCSRFRequired` und ist damit mit Basic Auth erreichbar; das ist der bestehende, absichtliche Zustand und Gegenstand des Abrufs, keine neue Oeffnung. |
| T-19-07-SC | Tampering | npm/pip/cargo installs | mitigate | Entfaellt mangels Gegenstand: kein Paket wird installiert, `backend/pyproject.toml` und `backend/uv.lock` bleiben unveraendert. |
</threat_model>

<verification>
- `uv run pytest -q` aus `backend/` ist vollstaendig gruen.
- `git diff --name-only` nennt genau `.github/workflows/deploy-harp.yml` und
  `backend/tests/test_language_proof_steps.py`.
- Der neue Schritt traegt kein `if:` und erscheint damit auf allen vier Matrixaesten.
- Keine Em-Dashes; Nicht-ASCII ausschliesslich im Dateiinhalt der vier hochgeladenen Beweisdateien.
</verification>

<success_criteria>
- Ein ungegateter Schritt findet je Sprache es, it, nl und pt ein Dokument ueber die Stammform des
  Suchworts, in der Unified Search.
- Der Schritt prueft `entries | length` und nie `entries[0].subline`.
- Die Ergebnisseite liefert den spanischen Treffer mit Dateinamen im HTML.
- Ein Textgate haelt den Schritt ungegatet, ohne YAML-Abhaengigkeit.
- Die Laufzeit ist als Messpunkt vorgesehen und wird nach dem ersten gruenen Lauf eingetragen.
</success_criteria>

<output>
Create `.planning/phases/19-frageseite-freischalten/19-07-SUMMARY.md` when done
</output>
