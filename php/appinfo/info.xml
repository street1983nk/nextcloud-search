<?xml version="1.0"?>
<!--
	The element order follows the app store schema. The store does not validate
	this file directly, it first runs it through pre-info.xslt and validates the
	result, so unknown elements are dropped silently instead of being rejected.
	There is no element for an app to app dependency, which is why app_api is
	checked at runtime in ExAppService instead of being declared here.
-->
<info>
	<id>findling</id>
	<!--
		The three languages of D-12, and the order is the order of the store
		schema: every name, then every summary, then every description. The
		English wording sits in an element WITHOUT a lang attribute, because
		the schema defaults that attribute to "en"; spelling it out would be a
		second "en" and would break the uniqueness constraint on the element
		kind.

		Not one of these elements may be empty. An empty element in an
		info.xml ends the store upload in a server error rather than in a
		validation message, which is the most expensive known trap of the
		sister project, and the cheapest to walk into while translating.

		The wording is not maintained here. docs/store-listing.md holds all
		three languages of both halves side by side, which is what the
		translation follow up rule of D-12 needs, and
		backend/tests/test_store_metadata.py holds the mechanical part of that
		rule: a language that goes missing from one element kind is a red test.
	-->
	<name>Findling</name>
	<name lang="de">Findling</name>
	<name lang="fr">Findling</name>
	<summary>Zero-config full text search for your files, including scanned documents</summary>
	<summary lang="de">Volltextsuche für Ihre Dateien ohne Konfiguration, gescannte Dokumente eingeschlossen</summary>
	<summary lang="fr">Recherche plein texte sans configuration dans vos fichiers, documents numérisés compris</summary>
	<description><![CDATA[Findling finds the contents of your documents from the normal search bar.

This app never modifies your files, and no content ever leaves your server.

Findling needs two more things to be installed: the app "AppAPI" and the
External App "Findling Backend", which does the reading and the indexing inside
your own instance.

Nothing has to be configured. The first index starts on its own and runs as a
background job, and scanned PDFs are read with OCR without a setting being
touched. If this server still uses the default AJAX cron, background jobs only
run while somebody is using the web interface, and the first index will trickle
along accordingly. Switching this instance to the system cron is the difference
between hours and weeks. "occ findling:index --status" shows how far it has
come.

What it costs in memory, measured: A full index and OCR run over 50,000 files
and 20 GB on a 4-GB box peaked at 429 MB of resident anonymous memory, under a
hard 2 GB limit enforced by the kernel, with no OOM kill. The machine was x86
and the repetition on ARM hardware is still open; the report docs/performance.md
in the source code carries the method, the curve and every figure that
repetition will replace.

Privacy: everything runs locally in your own instance. No file content ever
leaves the server, and there is no telemetry of any kind, not even a version
check. What is stored, so that nobody has to guess: the text extracted from
every indexed document is kept in the backend app's own volume, because the
short excerpts shown under a search result are cut out of it on demand. A
backup of that volume therefore contains the text of your indexed documents,
and the index is not encrypted at rest, which is a matter for the host it runs
on.]]></description>
	<description lang="de"><![CDATA[Findling findet den Inhalt Ihrer Dokumente über die gewöhnliche Suchleiste.

Diese App verändert Ihre Dateien nie, und kein Inhalt verlässt Ihren Server.

Findling braucht zwei weitere Installationen: die App "AppAPI" und die External
App "Findling Backend", die das Lesen und das Indexieren innerhalb Ihrer eigenen
Instanz erledigt.

Nichts muss eingerichtet werden. Der erste Indexlauf beginnt von selbst als
Hintergrundauftrag, und gescannte PDF-Dateien werden per Texterkennung gelesen,
ohne dass eine Einstellung angefasst wird. Läuft dieser Server noch mit dem
voreingestellten AJAX-Cron, arbeiten Hintergrundaufträge nur, solange jemand die
Weboberfläche benutzt, und der erste Indexlauf tröpfelt entsprechend dahin. Die
Umstellung dieser Instanz auf den System-Cron ist der Unterschied zwischen
Stunden und Wochen. "occ findling:index --status" zeigt, wie weit er gekommen
ist.

Was es an Arbeitsspeicher kostet, gemessen: Ein vollständiger Index- und
Texterkennungslauf über 50.000 Dateien und 20 GB auf einer 4-GB-Box hatte seine
Spitze bei 429 MB anonymem Arbeitsspeicher, unter einer harten Grenze von 2 GB,
die der Kernel durchsetzt, und ohne einen einzigen Abschuss wegen
Speichermangels. Die Maschine war x86, die Wiederholung auf ARM-Hardware steht
aus; der Bericht docs/performance.md im Quellcode nennt die Methode, die Kurve
und jede Zahl, die diese Wiederholung ersetzen wird.

Datenschutz: Alles läuft lokal in Ihrer eigenen Instanz. Kein Dateiinhalt
verlässt den Server, und es gibt keinerlei Telemetrie, nicht einmal eine
Versionsabfrage. Was gespeichert wird, damit niemand raten muss: Der aus jedem
indexierten Dokument gewonnene Text liegt im eigenen Datenspeicher der
Backend-App, weil die kurzen Auszüge unter einem Suchtreffer bei Bedarf daraus
geschnitten werden. Eine Sicherung dieses Datenspeichers enthält damit den Text
Ihrer indexierten Dokumente, und der Index ist im Ruhezustand nicht
verschlüsselt, was Sache des Wirtssystems ist.]]></description>
	<description lang="fr"><![CDATA[Findling trouve le contenu de vos documents depuis la barre de recherche
habituelle.

Cette application ne modifie jamais vos fichiers, et aucun contenu ne quitte
votre serveur.

Findling a besoin de deux installations supplémentaires : l'application "AppAPI"
et l'External App "Findling Backend", qui se charge de la lecture et de
l'indexation à l'intérieur de votre propre instance.

Rien n'est à configurer. La première indexation démarre d'elle-même comme tâche
de fond, et les PDF numérisés sont lus par reconnaissance optique de caractères
sans qu'un seul réglage soit touché. Si ce serveur utilise encore le cron AJAX
par défaut, les tâches de fond ne s'exécutent que pendant qu'une personne se
sert de l'interface web, et la première indexation avance au compte-gouttes.
Basculer cette instance sur le cron système, c'est la différence entre des
heures et des semaines. "occ findling:index --status" montre où elle en est.

Ce que cela coûte en mémoire, mesuré : une indexation complète avec
reconnaissance optique portant sur 50 000 fichiers et 20 Go sur une machine de
4 Go a culminé à 429 Mo de mémoire anonyme résidente, sous une limite stricte de
2 Go imposée par le noyau, et sans la moindre interruption pour manque de
mémoire. La machine était en x86, la répétition sur du matériel ARM reste à
faire ; le rapport docs/performance.md dans le code source donne la méthode, la
courbe et chaque chiffre que cette répétition remplacera.

Confidentialité : tout fonctionne localement dans votre propre instance. Aucun
contenu de fichier ne quitte le serveur, et il n'y a aucune télémétrie, pas même
une vérification de version. Ce qui est conservé, pour que personne n'ait à le
deviner : le texte extrait de chaque document indexé est gardé dans le volume
propre de l'application backend, parce que les courts extraits affichés sous un
résultat de recherche y sont découpés à la demande. Une sauvegarde de ce volume
contient donc le texte de vos documents indexés, et l'index n'est pas chiffré au
repos, ce qui relève de l'hôte sur lequel il tourne.]]></description>
	<!--
		Both halves of Findling carry the same major and minor version, so that
		nobody ends up with a companion app and a backend that disagree about
		the protocol between them. The bump to 0.2.0 was the pull queue and the
		crawl; the bump to 0.3.0 was the fourth container route, GET /rates,
		which the admin page of this half reads for its estimate of the first
		index.

		The bump to 1.0.0 is the first store release, and it is a decision and
		not a consequence: D-11 names 1.0.0 as the version both halves are
		submitted under, and plan 05-17 is the only place in this phase that
		performs it. Nothing about the protocol changed with it.

		The three places that have to agree are unchanged: this element, the
		<version> of backend/appinfo/info.xml and the <image-tag> next to it.
		docker.yml compares all three against the git tag before it builds, so
		a bump on one side alone fails the release; backend/tests/
		test_lockstep_versions.py asks the same question on every ordinary run,
		which is the cheap half of the same guard.
	-->
	<version>1.0.0</version>
	<licence>agpl</licence>
	<author>street1983nk</author>
	<namespace>Findling</namespace>
	<category>files</category>
	<website>https://github.com/street1983nk/nextcloud-search</website>
	<bugs>https://github.com/street1983nk/nextcloud-search/issues</bugs>
	<repository type="git">https://github.com/street1983nk/nextcloud-search.git</repository>
	<!--
		The images of the store page. The store keeps addresses and not files,
		so they live in this public repository and are linked over https. The
		schema type is secure-url: https and at most 256 characters, which is
		what backend/tests/test_store_metadata.py checks, together with the one
		thing the schema cannot: that every address points at a file that
		actually exists under store/media. An address without an image behind
		it is an empty frame on the store page, and that is worse than no image.

		The branch and not a tag, on purpose. The images have to be reachable
		while the entry is reviewed and for as long as it stands, and a broken
		image should be fixable with a commit rather than with a new release.

		The order is the order the store shows them in, so the header image
		comes first. There is no separate field for it: a store page has
		screenshots and nothing else, and the first one is the one everybody
		sees.

		This half gets all three, because it is the half a person installs and
		searches with: the search image is the promise of the product, and the
		admin image is what whoever runs the instance wants to see first.

		store/media/README.md says what each image shows, how it was taken, and
		why it comes from the development instance rather than from CI.
	-->
	<screenshot>https://raw.githubusercontent.com/street1983nk/nextcloud-search/main/store/media/header.png</screenshot>
	<screenshot>https://raw.githubusercontent.com/street1983nk/nextcloud-search/main/store/media/screenshot-search.png</screenshot>
	<screenshot>https://raw.githubusercontent.com/street1983nk/nextcloud-search/main/store/media/screenshot-admin.png</screenshot>
	<!--
		The same donation link as the sister project of this author. The
		platform is named in the type attribute because the schema knows the
		value, which lets the store show the right destination instead of a
		bare address.
	-->
	<donation type="paypal">https://paypal.me/KhaledCherifDev</donation>
	<dependencies>
		<php min-version="8.2"/>
		<nextcloud min-version="32" max-version="35"/>
	</dependencies>
	<!--
		The two blocks below sit in the order the store schema prescribes, which
		is: dependencies, background jobs, the repair block, two factor
		providers, commands. Each of them is written on one line on purpose. The
		schema pattern for a PHP class name allows no surrounding whitespace, so
		an indented class name on its own line fails the validation.

		The install step is what makes the first index start without an admin
		doing anything, which is the whole zero config promise of this app. The
		first command is the way back out: it is the only way to rebuild the index
		short of uninstalling the app. The second one is the diagnosis of a single
		file without a browser, which is the way a support case takes: the admin
		route of this app demands the request token of a real browser session, so
		occ is the only access to that answer from a shell.

		The uninstall step is the other half of the same lifecycle, and it is
		here for a reason that is easy to get wrong: Nextcloud executes
		repair-steps/uninstall in AppManager::disableApp(), so this entry is
		reached by every disable and not only by a removal. That is why the step
		removes nothing unless an administrator stated the intent first, and why
		docs/uninstall.md carries the measurement instead of a claim.

		The third command belongs to that step and is the only place where an
		administrator states the intent to remove the data of this app. It is a
		command and not a button, because clearing the index bookkeeping is an
		operational procedure and not an everyday click, and because the admin
		page already carries one destructive confirmation with the cleanup of
		the exclusion rules.
	-->
	<repair-steps><install><step>OCA\Findling\Repair\AppInstallStep</step></install><uninstall><step>OCA\Findling\Repair\AppUninstallStep</step></uninstall></repair-steps>
	<commands><command>OCA\Findling\Command\IndexCommand</command><command>OCA\Findling\Command\DiagnoseCommand</command><command>OCA\Findling\Command\PurgeCommand</command></commands>
	<!--
		The settings block, written on one line for the same reason as the two
		above, and standing here because there is nowhere else to put it:
		IRegistrationContext has no registerSettings(), so the two classes
		cannot be registered in Application::register() the way the search
		provider and the event listeners are. Nextcloud reads these class names
		out of the installed appinfo/info.xml in AppManager::loadApp(), which
		makes this file the registration itself.

		The app store transform empties the block: pre-info.xslt has the
		templates for activity and settings swapped, so what the store
		validates is an empty <settings/>, and that passes because every child
		of the settings type carries minOccurs="0". It has no consequence,
		because the block is read at runtime from the installed file and never
		from the normalised copy the store keeps. The workflow step "State the
		settings finding explicitly" holds that finding and turns red the day
		it stops being true.

		The version above moved to 0.3.0 together with the two versions in
		backend/appinfo/info.xml, because plan 04-05 adds the container route
		GET /rates. All three are compared against the git tag in docker.yml, so
		a bump on one side alone fails the release before it builds.
	-->
	<settings><admin>OCA\Findling\Settings\Admin</admin><admin-section>OCA\Findling\Settings\Section</admin-section></settings>
</info>
